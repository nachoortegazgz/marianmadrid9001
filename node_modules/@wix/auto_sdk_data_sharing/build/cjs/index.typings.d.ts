import { NonNullablePaths } from '@wix/sdk-types';

/**
 * A data sharing policy defines how a data collection is shared between sites in the same Wix account.
 *
 * The policy specifies which collection to share, optional filtering rules to control which items are accessible, and serves as the access credential for other sites to connect to the shared collection. Multiple policies can be created for the same collection with different filtering rules or access patterns.
 */
interface DataSharingPolicy {
    /**
     * Data sharing policy ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * ID of the data collection to share.
     * @immutable
     * @maxLength 255
     */
    dataCollectionId?: string;
    /**
     * Filter to restrict which items from the collection are shared.
     *
     * Use [API Query Language syntax](https://dev.wix.com/api/rest/getting-started/api-query-language) to define filtering rules. If not specified,
     * all items in the collection are shared.
     */
    dataItemsFilter?: Record<string, any> | null;
    /**
     * Date and time the data sharing policy was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the data sharing policy was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
}
interface SharedCollectionConnectedEvent {
    /** Shared connection that was created. */
    connection?: SharingConnection;
}
/**
 * A sharing connection represents the connection between 2 sites via a shared data collection.
 *
 * The connection is created when a site connects to a shared collection using a data sharing policy. It contains information about both the source site that owns the collection and created the policy, and the target site that the policy is shared with and now has access to the collection. It also includes the namespace used to access the shared data on the target site.
 */
interface SharingConnection {
    /**
     * ID of the data collection owned and shared by the source site.
     * @maxLength 255
     */
    sourceDataCollectionId?: string;
    /**
     * ID of the source site that owns the collection and created the policy.
     * @format GUID
     */
    sourceSiteId?: string;
    /**
     * ID of the shared collection on the target site.
     * The ID includes the namespace. For example, `"namespace/collectionName"`.
     * @maxLength 255
     */
    targetDataCollectionId?: string;
    /**
     * ID of the target site that the collection is shared with.
     * @format GUID
     */
    targetSiteId?: string;
    /**
     * Optional display name for the namespace that appears on the target site.
     * @maxLength 100
     */
    targetDisplayNamespace?: string | null;
    /**
     * ID of the data sharing policy that controls this connection.
     * @format GUID
     */
    dataSharingPolicyId?: string;
    /**
     * Date and time the connection was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the connection was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
}
interface SharedCollectionDisconnectedEvent {
    /** Shared connection that was removed. */
    connection?: SharingConnection;
}
interface CreateDataSharingPolicyRequest {
    /** Data sharing policy to create. */
    dataSharingPolicy?: DataSharingPolicy;
}
interface CreateDataSharingPolicyResponse {
    /** Created data sharing policy. */
    dataSharingPolicy?: DataSharingPolicy;
}
interface UpdateDataSharingPolicyRequest {
    /** Data sharing policy to update. */
    dataSharingPolicy: DataSharingPolicy;
}
interface UpdateDataSharingPolicyResponse {
    /** Updated data sharing policy. */
    dataSharingPolicy?: DataSharingPolicy;
}
interface DeleteDataSharingPolicyRequest {
    /**
     * Data sharing policy ID.
     * @format GUID
     */
    dataSharingPolicyId: string;
}
interface DeleteDataSharingPolicyResponse {
}
interface GetDataSharingPolicyRequest {
    /**
     * Data sharing policy ID.
     * @format GUID
     */
    dataSharingPolicyId: string;
}
interface GetDataSharingPolicyResponse {
    /** Retrieved data sharing policy. */
    dataSharingPolicy?: DataSharingPolicy;
}
interface ListDataSharingPoliciesRequest {
    /**
     * List of collection IDs to filter policies by.
     *
     * When provided, only returns policies for the specified collections.
     * @maxSize 100
     * @maxLength 255
     */
    dataCollectionIds?: string[];
    /**
     * Paging information.
     * By default, policies are sorted by `id` in ascending order.
     */
    paging?: CursorPaging;
}
interface CursorPaging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * You can get the relevant cursor token
     * from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface ListDataSharingPoliciesResponse {
    /** List of data sharing policies. */
    dataSharingPolicies?: DataSharingPolicy[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor pointing to next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface ListSharedDataCollectionsRequest {
    /**
     * List of collection IDs to filter by.
     *
     * When provided, only returns shared connections for the specified collections.
     * @maxSize 100
     * @maxLength 255
     */
    dataCollectionIds?: string[];
    /**
     * Whether to list collections shared with the current site or shared from the current site.
     * @deprecated
     * @replacedBy shared_with_current_site
     * @targetRemovalDate 2026-12-31
     */
    sharedWithCurrent?: boolean;
    /**
     * Paging information.
     * By default, shared collections are sorted by `id` in ascending order.
     */
    paging?: CursorPaging;
    /**
     * Whether to list collections shared with the current site or shared from the current site.
     * When `true`, lists collections shared with the current site. When `false`, lists collections shared from the current site's policies.
     *
     * Default: `false`
     */
    sharedWithCurrentSite?: boolean;
}
interface ListSharedDataCollectionsResponse {
    /** List of shared collections. */
    connections?: SharingConnection[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface BulkGetSharedCollectionStatsRequest {
    /**
     * Optional. Source data collection IDs to look up.
     *
     * Maximum: 100 IDs per request.
     *
     * If empty, returns one successful result per source data collection on the current site that has
     * at least one active sharing connection.
     * If non-empty, returns one result per distinct provided ID. Duplicate IDs are deduplicated; each
     * ID's `itemMetadata.originalIndex` reflects its first position in the request array.
     * A result has `itemMetadata.success = true` with `stats` populated when the collection has at least
     * one active sharing connection; otherwise `itemMetadata.success = false` with
     * `itemMetadata.error.code = "NOT_FOUND"`.
     * @maxSize 100
     * @maxLength 255
     */
    sourceDataCollectionIds?: string[];
}
interface BulkGetSharedCollectionStatsResponse {
    /**
     * Per-collection sharing stats results.
     *
     * When `sourceDataCollectionIds` is empty, contains one successful result per source data collection
     * on the current site that has at least one active sharing connection.
     * When `sourceDataCollectionIds` is non-empty, contains one result per distinct provided ID.
     */
    results?: BulkSharedCollectionStatsResult[];
    /** Aggregate success and failure counts across `results`. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkSharedCollectionStatsResult {
    /**
     * Per-item metadata: success flag, original request index, and error details when the lookup failed.
     *
     * `itemMetadata.id` is set to the source data collection ID.
     * When `itemMetadata.success` is `false`, `itemMetadata.error.code` is `"NOT_FOUND"`.
     */
    itemMetadata?: ItemMetadata;
    /** Sharing stats for the collection. Populated only when `itemMetadata.success` is `true`. */
    stats?: SharedCollectionStats;
}
interface ItemMetadata {
    /**
     * Item ID. Does not appear if the item ID is missing. For example, when item creation fails.
     * @maxLength 128
     */
    _id?: string | null;
    /** Original index of the item in the array specified in the request. Use this to match items in the request with those in the response. */
    originalIndex?: number;
    /** Whether the requested action was successful. When `false`, the `error` field is populated. */
    success?: boolean;
    /** In case of failure, the error details. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface SharedCollectionStats {
    /**
     * Source data collection ID on the current site.
     * @maxLength 255
     */
    sourceDataCollectionId?: string;
    /**
     * Number of distinct target sites the collection is currently shared with.
     *
     * If the same target site connects through multiple sharing policies, it is counted once.
     */
    connectedSitesCount?: number;
}
interface BulkActionMetadata {
    /** Number of items successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
}
interface ConnectToSharedCollectionRequest {
    /**
     * Namespace to connect collection with on the target site.
     * Must be unique in the site.
     * @minLength 1
     * @maxLength 100
     */
    namespace: string;
    /**
     * Optional display name for the namespace that appears on the target site.
     * @maxLength 100
     */
    displayNamespace?: string | null;
    /**
     * Data sharing policy ID to connect with.
     * @format GUID
     */
    dataSharingPolicyId?: string;
}
interface ConnectToSharedCollectionResponse {
    /** Created connection to shared data collection. */
    connection?: SharingConnection;
}
interface DisconnectFromSharedCollectionRequest {
    /**
     * Shared data collection ID.
     * @maxLength 255
     * @deprecated
     * @replacedBy target_data_collection_id
     * @targetRemovalDate 2026-12-31
     */
    dataCollectionId?: string;
    /**
     * ID of the shared collection on the target site.
     * @maxLength 255
     */
    targetDataCollectionId?: string;
}
interface DisconnectFromSharedCollectionResponse {
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
/** @docsIgnore */
type CreateDataSharingPolicyApplicationErrors = {
    code?: 'WDE0107';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'WDE0025';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type UpdateDataSharingPolicyApplicationErrors = {
    code?: 'WDE0154';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type DeleteDataSharingPolicyApplicationErrors = {
    code?: 'WDE0154';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type GetDataSharingPolicyApplicationErrors = {
    code?: 'WDE0154';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'WDE0144';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type ConnectToSharedCollectionApplicationErrors = {
    code?: 'WDE0154';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'WDE0144';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'WDE0104';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'WDE0102';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'WDE0155';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type DisconnectFromSharedCollectionApplicationErrors = {
    code?: 'WDE0156';
    description?: string;
    data?: Record<string, any>;
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
    accountInfo?: AccountInfoMetadata;
}
interface AccountInfoMetadata {
    /** ID of the Wix account associated with the event */
    accountId: string;
    /** ID of the Wix site associated with the event. Only included when the event is tied to a specific site. */
    siteId?: string;
    /** ID of the parent Wix account. Only included when 'accountId' belongs to a child account. */
    parentAccountId?: string;
}
interface DataSharingPolicyCreatedEnvelope {
    entity: DataSharingPolicy;
    metadata: EventMetadata;
}
/**
 * Triggered when a new data sharing policy is created.
 * @permissionScope Manage Data Collection Sharing
 * @permissionScopeId SCOPE.DATA.MANAGE_COLLECTION_SHARING
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_GET_POLICY
 * @webhook
 * @eventType wix.data.v1.data_sharing_policy_created
 * @slug created
 */
declare function onDataSharingPolicyCreated(handler: (event: DataSharingPolicyCreatedEnvelope) => void | Promise<void>): void;
interface DataSharingPolicyDeletedEnvelope {
    metadata: EventMetadata;
}
/**
 * Triggered when a data sharing policy is deleted.
 * @permissionScope Manage Data Collection Sharing
 * @permissionScopeId SCOPE.DATA.MANAGE_COLLECTION_SHARING
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_GET_POLICY
 * @webhook
 * @eventType wix.data.v1.data_sharing_policy_deleted
 * @slug deleted
 */
declare function onDataSharingPolicyDeleted(handler: (event: DataSharingPolicyDeletedEnvelope) => void | Promise<void>): void;
interface DataSharingPolicySharedCollectionConnectedEnvelope {
    data: SharedCollectionConnectedEvent;
    metadata: EventMetadata;
}
/**
 * Triggered when a new connection is created, which means a collection is shared with another site.
 * @permissionScope Manage Data Collection Sharing
 * @permissionScopeId SCOPE.DATA.MANAGE_COLLECTION_SHARING
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_GET_POLICY
 * @webhook
 * @eventType wix.data.v1.data_sharing_policy_shared_collection_connected
 * @slug shared_collection_connected
 */
declare function onDataSharingPolicySharedCollectionConnected(handler: (event: DataSharingPolicySharedCollectionConnectedEnvelope) => void | Promise<void>): void;
interface DataSharingPolicySharedCollectionDisconnectedEnvelope {
    data: SharedCollectionDisconnectedEvent;
    metadata: EventMetadata;
}
/**
 * Triggered when a connection is disconnected, which means a collection is removed from the site it was previously shared with.
 * @permissionScope Manage Data Collection Sharing
 * @permissionScopeId SCOPE.DATA.MANAGE_COLLECTION_SHARING
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_GET_POLICY
 * @webhook
 * @eventType wix.data.v1.data_sharing_policy_shared_collection_disconnected
 * @slug shared_collection_disconnected
 */
declare function onDataSharingPolicySharedCollectionDisconnected(handler: (event: DataSharingPolicySharedCollectionDisconnectedEnvelope) => void | Promise<void>): void;
interface DataSharingPolicyUpdatedEnvelope {
    entity: DataSharingPolicy;
    metadata: EventMetadata;
}
/**
 * Triggered when a data sharing policy is updated.
 * @permissionScope Manage Data Collection Sharing
 * @permissionScopeId SCOPE.DATA.MANAGE_COLLECTION_SHARING
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_GET_POLICY
 * @webhook
 * @eventType wix.data.v1.data_sharing_policy_updated
 * @slug updated
 */
declare function onDataSharingPolicyUpdated(handler: (event: DataSharingPolicyUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Creates a data sharing policy for the specified collection.
 *
 * You can optionally filter which items in the collection can be shared. If no filters are specified, all items in the collection are shared. You can create multiple policies for the same collection with different filters.
 *
 * You can find which fields can be filtered for each collection by calling [Get Data Collection](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-collections/get-data-collection). The supported filters are returned in the response under `collections.fields.capabilities.queryOperators`.
 *
 * <blockquote class="important">
 *
 * __Important:__
 * Only collections created in the CMS or with the [Data Collections API](https://dev.wix.com/docs/api-reference/business-solutions/cms/introduction) can be shared. External collections and Wix App collections can't be shared.
 *
 * </blockquote>
 * @public
 * @requiredField options.dataSharingPolicy.dataCollectionId
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_CREATE_POLICY
 * @applicableIdentity APP
 * @returns Created data sharing policy.
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.CreateDataSharingPolicy
 */
declare function createDataSharingPolicy(options?: NonNullablePaths<CreateDataSharingPolicyOptions, `dataSharingPolicy.dataCollectionId`, 1>): Promise<NonNullablePaths<DataSharingPolicy, `_id` | `dataCollectionId`, 0> & {
    __applicationErrorsType?: CreateDataSharingPolicyApplicationErrors;
}>;
interface CreateDataSharingPolicyOptions {
    /** Data sharing policy to create. */
    dataSharingPolicy?: DataSharingPolicy;
}
/**
 * Updates an existing data sharing policy.
 *
 * You can only update the `dataItemsFilter` field to change which items from the collection are shared.
 * You can't change which collection is shared. If you want to share a different collection, create a new data sharing policy
 *
 * Any updates to the policy automatically apply to all sites connected using this policy.
 * @param _id - Data sharing policy ID.
 * @public
 * @requiredField _id
 * @requiredField dataSharingPolicy
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_UPDATE_POLICY
 * @applicableIdentity APP
 * @returns Updated data sharing policy.
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.UpdateDataSharingPolicy
 */
declare function updateDataSharingPolicy(_id: string, dataSharingPolicy: UpdateDataSharingPolicy): Promise<NonNullablePaths<DataSharingPolicy, `_id` | `dataCollectionId`, 0> & {
    __applicationErrorsType?: UpdateDataSharingPolicyApplicationErrors;
}>;
interface UpdateDataSharingPolicy {
    /**
     * Data sharing policy ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * ID of the data collection to share.
     * @immutable
     * @maxLength 255
     */
    dataCollectionId?: string;
    /**
     * Filter to restrict which items from the collection are shared.
     *
     * Use [API Query Language syntax](https://dev.wix.com/api/rest/getting-started/api-query-language) to define filtering rules. If not specified,
     * all items in the collection are shared.
     */
    dataItemsFilter?: Record<string, any> | null;
    /**
     * Date and time the data sharing policy was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the data sharing policy was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
}
/**
 * Deletes a data sharing policy and disconnects all associated connections.
 *
 * <blockquote class="warning">
 *
 * __Warning:__
 * When you delete a sharing policy, all target sites connected to the shared collection using this policy immediately lose access to the collection and its data. This can cause code in the target sites to break.
 *
 * </blockquote>
 * @param dataSharingPolicyId - Data sharing policy ID.
 * @public
 * @requiredField dataSharingPolicyId
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_DELETE_POLICY
 * @applicableIdentity APP
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.DeleteDataSharingPolicy
 */
declare function deleteDataSharingPolicy(dataSharingPolicyId: string): Promise<void & {
    __applicationErrorsType?: DeleteDataSharingPolicyApplicationErrors;
}>;
/**
 * Retrieves a data sharing policy specified by ID.
 * @param dataSharingPolicyId - Data sharing policy ID.
 * @public
 * @requiredField dataSharingPolicyId
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_GET_POLICY
 * @applicableIdentity APP
 * @returns Retrieved data sharing policy.
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.GetDataSharingPolicy
 */
declare function getDataSharingPolicy(dataSharingPolicyId: string): Promise<NonNullablePaths<DataSharingPolicy, `_id` | `dataCollectionId`, 0> & {
    __applicationErrorsType?: GetDataSharingPolicyApplicationErrors;
}>;
/**
 * Retrieves a list of up to 1,000 data sharing policies on the current site.
 *
 * Specify `dataCollectionIds` to filter policies for specific collections.
 * @public
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_LIST_POLICIES
 * @applicableIdentity APP
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.ListDataSharingPolicies
 */
declare function listDataSharingPolicies(options?: ListDataSharingPoliciesOptions): Promise<NonNullablePaths<ListDataSharingPoliciesResponse, `dataSharingPolicies` | `dataSharingPolicies.${number}._id` | `dataSharingPolicies.${number}.dataCollectionId`, 2>>;
interface ListDataSharingPoliciesOptions {
    /**
     * List of collection IDs to filter policies by.
     *
     * When provided, only returns policies for the specified collections.
     * @maxSize 100
     * @maxLength 255
     */
    dataCollectionIds?: string[];
    /**
     * Paging information.
     * By default, policies are sorted by `id` in ascending order.
     */
    paging?: CursorPaging;
}
/**
 * Retrieves a list of shared data collections.
 *
 * Returns either the collections that are either being shared by the current site or shared with the current site. Specify `sharedWithCurrentSite` to filter the direction.
 * @public
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_LIST
 * @applicableIdentity APP
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.ListSharedDataCollections
 */
declare function listSharedDataCollections(options?: ListSharedDataCollectionsOptions): Promise<NonNullablePaths<ListSharedDataCollectionsResponse, `connections` | `connections.${number}.sourceDataCollectionId` | `connections.${number}.sourceSiteId` | `connections.${number}.targetDataCollectionId` | `connections.${number}.targetSiteId` | `connections.${number}.dataSharingPolicyId`, 2>>;
interface ListSharedDataCollectionsOptions {
    /**
     * List of collection IDs to filter by.
     *
     * When provided, only returns shared connections for the specified collections.
     * @maxSize 100
     * @maxLength 255
     */
    dataCollectionIds?: string[];
    /**
     * Whether to list collections shared with the current site or shared from the current site.
     * @deprecated
     * @replacedBy shared_with_current_site
     * @targetRemovalDate 2026-12-31
     */
    sharedWithCurrent?: boolean;
    /**
     * Paging information.
     * By default, shared collections are sorted by `id` in ascending order.
     */
    paging?: CursorPaging;
    /**
     * Whether to list collections shared with the current site or shared from the current site.
     * When `true`, lists collections shared with the current site. When `false`, lists collections shared from the current site's policies.
     *
     * Default: `false`
     */
    sharedWithCurrentSite?: boolean;
}
/**
 * Retrieves sharing statistics for data collections shared out from the current site.
 *
 * For each source data collection, returns the number of distinct target sites the collection is
 * currently shared with. Use this when you need a per-collection sharing summary
 * (for example, a "Shared with N sites" indicator) without retrieving the full list of sharing connections.
 *
 * You can look up stats for up to 100 collections per call by specifying `sourceDataCollectionIds`.
 * Leave `sourceDataCollectionIds` empty to return stats for every source data collection on the current site
 * that has at least one active sharing connection.
 *
 * When you specify `sourceDataCollectionIds`, the response contains one result per distinct provided ID.
 * A result has `itemMetadata.success = true` and `stats` populated when the collection has at least
 * one active sharing connection. Otherwise, `itemMetadata.success = false` with
 * `itemMetadata.error.code = "NOT_FOUND"`.
 *
 * To retrieve the full list of sharing connections (including target site and collection IDs), call
 * [List Shared Data Collections](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-sharing-policy-v1/list-shared-data-collections)
 * with `sharedWithCurrentSite` set to `false`.
 *
 * > **Note:** `connectedSitesCount` counts distinct target sites. If the same target site connects
 * > through multiple policies, it is counted once.
 * @public
 * @permissionId data:v1:data_sharing_policy:bulk_get_shared_collection_stats
 * @applicableIdentity APP
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.BulkGetSharedCollectionStats
 */
declare function bulkGetSharedCollectionStats(options?: BulkGetSharedCollectionStatsOptions): Promise<NonNullablePaths<BulkGetSharedCollectionStatsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.stats.sourceDataCollectionId` | `results.${number}.stats.connectedSitesCount` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures`, 4>>;
interface BulkGetSharedCollectionStatsOptions {
    /**
     * Optional. Source data collection IDs to look up.
     *
     * Maximum: 100 IDs per request.
     *
     * If empty, returns one successful result per source data collection on the current site that has
     * at least one active sharing connection.
     * If non-empty, returns one result per distinct provided ID. Duplicate IDs are deduplicated; each
     * ID's `itemMetadata.originalIndex` reflects its first position in the request array.
     * A result has `itemMetadata.success = true` with `stats` populated when the collection has at least
     * one active sharing connection; otherwise `itemMetadata.success = false` with
     * `itemMetadata.error.code = "NOT_FOUND"`.
     * @maxSize 100
     * @maxLength 255
     */
    sourceDataCollectionIds?: string[];
}
/**
 * Connects the current site to a shared data collection.
 *
 * When you call this method, the shared collection appears in the current site's CMS under the specified namespace. You can also access the shared collection by using the [Data Items APIs](https://dev.wix.com/docs/api-reference/business-solutions/cms/data-items/introduction).
 *
 * > **Note:** The `namespace` is an identifier that serves as part of the collection ID when querying collection data. Therefore, it must be unique in a site.
 * @param namespace - Namespace to connect collection with on the target site.
 * Must be unique in the site.
 * @public
 * @requiredField namespace
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_CONNECT
 * @applicableIdentity APP
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.ConnectToSharedCollection
 */
declare function connectToSharedCollection(namespace: string, options?: ConnectToSharedCollectionOptions): Promise<NonNullablePaths<ConnectToSharedCollectionResponse, `connection.sourceDataCollectionId` | `connection.sourceSiteId` | `connection.targetDataCollectionId` | `connection.targetSiteId` | `connection.dataSharingPolicyId`, 1> & {
    __applicationErrorsType?: ConnectToSharedCollectionApplicationErrors;
}>;
interface ConnectToSharedCollectionOptions {
    /**
     * Optional display name for the namespace that appears on the target site.
     * @maxLength 100
     */
    displayNamespace?: string | null;
    /**
     * Data sharing policy ID to connect with.
     * @format GUID
     */
    dataSharingPolicyId?: string;
}
/**
 * Disconnects the current site from a shared data collection.
 *
 * This method removes the local view of the shared collection from the current site. Once removed, the collection is no longer accessible through the CMS or Data Items APIs.
 *
 * > **Note:** This method doesn't affect the collection on the source site or other target sites that share the collection.
 * @public
 * @permissionId WIX_DATA.SHARED_DATA_COLLECTION_DELETE
 * @applicableIdentity APP
 * @fqn com.wixpress.cloud.data.api.share.DataCollectionSharingService.DisconnectFromSharedCollection
 */
declare function disconnectFromSharedCollection(options?: DisconnectFromSharedCollectionOptions): Promise<void & {
    __applicationErrorsType?: DisconnectFromSharedCollectionApplicationErrors;
}>;
interface DisconnectFromSharedCollectionOptions {
    /**
     * Shared data collection ID.
     * @maxLength 255
     * @deprecated
     * @replacedBy target_data_collection_id
     * @targetRemovalDate 2026-12-31
     */
    dataCollectionId?: string;
    /**
     * ID of the shared collection on the target site.
     * @maxLength 255
     */
    targetDataCollectionId?: string;
}

export { type AccountInfo, type AccountInfoMetadata, type ActionEvent, type ApplicationError, type BaseEventMetadata, type BulkActionMetadata, type BulkGetSharedCollectionStatsOptions, type BulkGetSharedCollectionStatsRequest, type BulkGetSharedCollectionStatsResponse, type BulkSharedCollectionStatsResult, type ConnectToSharedCollectionApplicationErrors, type ConnectToSharedCollectionOptions, type ConnectToSharedCollectionRequest, type ConnectToSharedCollectionResponse, type CreateDataSharingPolicyApplicationErrors, type CreateDataSharingPolicyOptions, type CreateDataSharingPolicyRequest, type CreateDataSharingPolicyResponse, type CursorPaging, type CursorPagingMetadata, type Cursors, type DataSharingPolicy, type DataSharingPolicyCreatedEnvelope, type DataSharingPolicyDeletedEnvelope, type DataSharingPolicySharedCollectionConnectedEnvelope, type DataSharingPolicySharedCollectionDisconnectedEnvelope, type DataSharingPolicyUpdatedEnvelope, type DeleteDataSharingPolicyApplicationErrors, type DeleteDataSharingPolicyRequest, type DeleteDataSharingPolicyResponse, type DisconnectFromSharedCollectionApplicationErrors, type DisconnectFromSharedCollectionOptions, type DisconnectFromSharedCollectionRequest, type DisconnectFromSharedCollectionResponse, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, type GetDataSharingPolicyApplicationErrors, type GetDataSharingPolicyRequest, type GetDataSharingPolicyResponse, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, type ListDataSharingPoliciesOptions, type ListDataSharingPoliciesRequest, type ListDataSharingPoliciesResponse, type ListSharedDataCollectionsOptions, type ListSharedDataCollectionsRequest, type ListSharedDataCollectionsResponse, type MessageEnvelope, type RestoreInfo, type SharedCollectionConnectedEvent, type SharedCollectionDisconnectedEvent, type SharedCollectionStats, type SharingConnection, type UpdateDataSharingPolicy, type UpdateDataSharingPolicyApplicationErrors, type UpdateDataSharingPolicyRequest, type UpdateDataSharingPolicyResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, bulkGetSharedCollectionStats, connectToSharedCollection, createDataSharingPolicy, deleteDataSharingPolicy, disconnectFromSharedCollection, getDataSharingPolicy, listDataSharingPolicies, listSharedDataCollections, onDataSharingPolicyCreated, onDataSharingPolicyDeleted, onDataSharingPolicySharedCollectionConnected, onDataSharingPolicySharedCollectionDisconnected, onDataSharingPolicyUpdated, updateDataSharingPolicy };
