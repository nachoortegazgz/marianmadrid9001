import { ReportMemberRequest as ReportMemberRequest$1, ReportMemberResponse as ReportMemberResponse$1, QueryMemberReportsRequest as QueryMemberReportsRequest$1, QueryMemberReportsResponse as QueryMemberReportsResponse$1, ListReportedMembersRequest as ListReportedMembersRequest$1, ListReportedMembersResponse as ListReportedMembersResponse$1, QueryReportedMembersRequest as QueryReportedMembersRequest$1, QueryReportedMembersResponse as QueryReportedMembersResponse$1, DeleteMemberReportsRequest as DeleteMemberReportsRequest$1, DeleteMemberReportsResponse as DeleteMemberReportsResponse$1 } from './index.typings.mjs';
import '@wix/sdk-types';

/**
 * A report submitted by a member about another member.
 * Reports help maintain community safety by allowing members to flag inappropriate behavior such as spam, harassment, or impersonation.
 */
interface MemberReport {
    /**
     * Report ID.
     * @format GUID
     * @readonly
     */
    id?: string | null;
    /**
     * ID of the reported member.
     * @format GUID
     */
    reportedMemberId?: string;
    /**
     * ID of the member who created the report.
     * @readonly
     */
    reportingMemberId?: string | null;
    /** Reason for reporting a member. */
    reason?: Reason;
    /**
     * Date and time the report was created.
     * @readonly
     */
    createdDate?: Date | null;
}
interface Reason {
    /** Report reason type. */
    category?: CategoryWithLiterals;
    /** Explanation of why the member is being reported. */
    description?: string | null;
}
declare enum Category {
    /** Unknown category. This value is unused. */
    UNKNOWN = "UNKNOWN",
    /** The member is reported for spam. */
    SPAM = "SPAM",
    /** The member is reported for impersonation. */
    IMPERSONATION = "IMPERSONATION",
    /** The member is reported for harassment. */
    HARASSMENT = "HARASSMENT",
    /** The member is reported for other reasons. */
    OTHER = "OTHER"
}
/** @enumType */
type CategoryWithLiterals = Category | 'UNKNOWN' | 'SPAM' | 'IMPERSONATION' | 'HARASSMENT' | 'OTHER';
interface ReportMemberRequest {
    /** Details of a member to report. */
    memberReport: MemberReport;
}
interface ReportMemberResponse {
    /** Member report. */
    memberReport?: MemberReport;
}
interface ReportedMemberCreated {
    reportedMember?: ReportedMember;
}
/** Reported member details. */
interface ReportedMember {
    /**
     * ID of the reported member.
     * @format GUID
     */
    reportedMemberId?: string;
    /** Number of reports for this member. */
    reportCount?: string;
    /** Whether reports of this member have been reviewed. */
    reviewed?: boolean;
    /**
     * Date and time when the most recent report was created.
     * @readonly
     */
    lastReportDate?: Date | null;
}
interface SelfReportingForbiddenError {
    /**
     * ID of the member that is reported.
     * @format GUID
     */
    memberId?: string;
}
interface CannotReportBlockedMemberError {
    /**
     * ID of the member that is reported.
     * @format GUID
     */
    memberId?: string;
}
interface QueryMemberReportsRequest {
    /** Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details. */
    query?: QueryV2;
}
interface QueryV2 {
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     *
     * See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details.
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     */
    sorting?: Sorting[];
    /** Paging options to limit and skip the number of items. */
    paging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
    /**
     * When `field_name` is a property of repeated field that is marked as `MATCH_ITEMS` and sort should be done by
     * a specific element from a collection, filter can/should be provided to ensure correct sort value is picked.
     *
     * If multiple filters are provided, they are combined with AND operator.
     *
     * Example:
     * Given we have document like {"id": "1", "nestedField": [{"price": 10, "region": "EU"}, {"price": 20, "region": "US"}]}
     * and `nestedField` is marked as `MATCH_ITEMS`, to ensure that sorting is done by correct region, filter should be
     * { fieldName: "nestedField.price", "select_items_by": [{"nestedField.region": "US"}] }
     * @internal
     * @maxSize 10
     */
    selectItemsBy?: Record<string, any>[] | null;
    /**
     * Origin point for geo-distance sorting on a GEO field
     * results are ordered by distance from this point (ASC = nearest first, DESC = farthest first).
     */
    origin?: AddressLocation;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface AddressLocation {
    /** Address latitude. */
    latitude?: number | null;
    /** Address longitude. */
    longitude?: number | null;
}
interface CursorPaging {
    /**
     * The number of items to load.
     * Default: `100`
     * @max 1000
     */
    limit?: number | null;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursor?: string | null;
}
interface QueryMemberReportsResponse {
    /** Retrieved member reports. */
    memberReports?: MemberReport[];
    /** Metadata for the paginated results. */
    pagingMetadata?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Flag that indicates the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     * @internal
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface MarkMemberReportsAsReviewedRequest {
    /**
     * ID of the member whose reports are to be marked as reviewed.
     * @format GUID
     */
    memberId?: string;
}
interface MarkMemberReportsAsReviewedResponse {
    /** Reported member details. */
    reportedMember?: ReportedMember;
}
interface ListReportedMembersRequest extends ListReportedMembersRequestPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Paging options to limit and skip the number of items. If empty, `cursor_paging` is used.
     * @internal
     */
    paging?: Paging;
}
/** @oneof */
interface ListReportedMembersRequestPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Paging options to limit and skip the number of items. If empty, `cursor_paging` is used.
     * @internal
     */
    paging?: Paging;
}
interface Paging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
interface ListReportedMembersResponse {
    /** Retrieved reported members. */
    reportedMembers?: ReportedMember[];
    /** Metadata for the paginated results. */
    pagingMetadata?: PagingMetadataV2;
}
interface QueryReportedMembersRequest {
    /** Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language) for more details. */
    query?: QueryV2;
}
interface QueryReportedMembersResponse {
    /** Retrieved reported members. */
    reportedMembers?: ReportedMember[];
    /** Metadata for the paginated results. */
    pagingMetadata?: PagingMetadataV2;
}
interface DeleteMemberReportsRequest {
    /**
     * ID of the member whose reports are to be deleted.
     * @format GUID
     */
    memberId: string;
}
interface DeleteMemberReportsResponse {
}
interface ReportedMemberDeleted {
    /** @format GUID */
    reportedMemberId?: string;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entityAsJson?: string;
    /**
     * Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity
     * @internal
     */
    triggeredByUndelete?: boolean | null;
    /** Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity */
    restoreInfo?: RestoreInfo;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntityAsJson?: string;
    /**
     * This field is currently part of the of the EntityUpdatedEvent msg, but scala/node libraries which implements the domain events standard
     * wont populate it / have any reference to it in the API.
     * The main reason for it is that fetching the old entity from the DB will have a performance hit on an update operation so unless truly needed,
     * the developer should send only the new (current) entity.
     * An additional reason is not wanting to send this additional entity over the wire (kafka) since in some cases it can be really big
     * Developers that must reflect the old entity will have to implement their own domain event sender mechanism which will follow the DomainEvent proto message.
     * @internal
     * @deprecated
     */
    previousEntityAsJson?: string | null;
    /**
     * Map of fully qualified field paths to their previous values for fields that changed.
     * For more details please see [AIP](https://dev.wix.com/docs/rnd-general/articles/p13n-guidelines-aips/guidance-aips/design-patterns/7016-events#modified-fields-format)
     * @internal
     */
    modifiedFields?: Record<string, any>;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface EntityDeletedEvent {
    /**
     * Indicates if the entity is sent to trash-bin. only available when trash-bin is enabled
     * @internal
     */
    movedToTrash?: boolean | null;
    /** Entity that was deleted. */
    deletedEntityAsJson?: string | null;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface ActionEvent {
    bodyAsJson?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
/** @docsIgnore */
type ReportMemberApplicationErrors = {
    code?: 'SELF_REPORTING_FORBIDDEN';
    description?: string;
    data?: SelfReportingForbiddenError;
} | {
    code?: 'CANNOT_REPORT_BLOCKED_MEMBER';
    description?: string;
    data?: CannotReportBlockedMemberError;
};
/** @docsIgnore */
type DeleteMemberReportsApplicationErrors = {
    code?: 'CANNOT_DELETE_MEMBER_REPORTS';
    description?: string;
    data?: Record<string, any>;
};

type __PublicMethodMetaInfo<K = string, M = unknown, T = unknown, S = unknown, Q = unknown, R = unknown> = {
    getUrl: (context: any) => string;
    httpMethod: K;
    path: string;
    pathParams: M;
    __requestType: T;
    __originalRequestType: S;
    __responseType: Q;
    __originalResponseType: R;
};
declare function reportMember(): __PublicMethodMetaInfo<'POST', {}, ReportMemberRequest$1, ReportMemberRequest, ReportMemberResponse$1, ReportMemberResponse>;
declare function queryMemberReports(): __PublicMethodMetaInfo<'POST', {}, QueryMemberReportsRequest$1, QueryMemberReportsRequest, QueryMemberReportsResponse$1, QueryMemberReportsResponse>;
declare function listReportedMembers(): __PublicMethodMetaInfo<'GET', {}, ListReportedMembersRequest$1, ListReportedMembersRequest, ListReportedMembersResponse$1, ListReportedMembersResponse>;
declare function queryReportedMembers(): __PublicMethodMetaInfo<'POST', {}, QueryReportedMembersRequest$1, QueryReportedMembersRequest, QueryReportedMembersResponse$1, QueryReportedMembersResponse>;
declare function deleteMemberReports(): __PublicMethodMetaInfo<'DELETE', {
    memberId: string;
}, DeleteMemberReportsRequest$1, DeleteMemberReportsRequest, DeleteMemberReportsResponse$1, DeleteMemberReportsResponse>;

export { type AccountInfo as AccountInfoOriginal, type ActionEvent as ActionEventOriginal, type AddressLocation as AddressLocationOriginal, type CannotReportBlockedMemberError as CannotReportBlockedMemberErrorOriginal, Category as CategoryOriginal, type CategoryWithLiterals as CategoryWithLiteralsOriginal, type CursorPaging as CursorPagingOriginal, type Cursors as CursorsOriginal, type DeleteMemberReportsApplicationErrors as DeleteMemberReportsApplicationErrorsOriginal, type DeleteMemberReportsRequest as DeleteMemberReportsRequestOriginal, type DeleteMemberReportsResponse as DeleteMemberReportsResponseOriginal, type DomainEventBodyOneOf as DomainEventBodyOneOfOriginal, type DomainEvent as DomainEventOriginal, type Empty as EmptyOriginal, type EntityCreatedEvent as EntityCreatedEventOriginal, type EntityDeletedEvent as EntityDeletedEventOriginal, type EntityUpdatedEvent as EntityUpdatedEventOriginal, type IdentificationDataIdOneOf as IdentificationDataIdOneOfOriginal, type IdentificationData as IdentificationDataOriginal, type ListReportedMembersRequest as ListReportedMembersRequestOriginal, type ListReportedMembersRequestPagingMethodOneOf as ListReportedMembersRequestPagingMethodOneOfOriginal, type ListReportedMembersResponse as ListReportedMembersResponseOriginal, type MarkMemberReportsAsReviewedRequest as MarkMemberReportsAsReviewedRequestOriginal, type MarkMemberReportsAsReviewedResponse as MarkMemberReportsAsReviewedResponseOriginal, type MemberReport as MemberReportOriginal, type MessageEnvelope as MessageEnvelopeOriginal, type PagingMetadataV2 as PagingMetadataV2Original, type Paging as PagingOriginal, type QueryMemberReportsRequest as QueryMemberReportsRequestOriginal, type QueryMemberReportsResponse as QueryMemberReportsResponseOriginal, type QueryReportedMembersRequest as QueryReportedMembersRequestOriginal, type QueryReportedMembersResponse as QueryReportedMembersResponseOriginal, type QueryV2 as QueryV2Original, type Reason as ReasonOriginal, type ReportMemberApplicationErrors as ReportMemberApplicationErrorsOriginal, type ReportMemberRequest as ReportMemberRequestOriginal, type ReportMemberResponse as ReportMemberResponseOriginal, type ReportedMemberCreated as ReportedMemberCreatedOriginal, type ReportedMemberDeleted as ReportedMemberDeletedOriginal, type ReportedMember as ReportedMemberOriginal, type RestoreInfo as RestoreInfoOriginal, type SelfReportingForbiddenError as SelfReportingForbiddenErrorOriginal, SortOrder as SortOrderOriginal, type SortOrderWithLiterals as SortOrderWithLiteralsOriginal, type Sorting as SortingOriginal, WebhookIdentityType as WebhookIdentityTypeOriginal, type WebhookIdentityTypeWithLiterals as WebhookIdentityTypeWithLiteralsOriginal, type __PublicMethodMetaInfo, deleteMemberReports, listReportedMembers, queryMemberReports, queryReportedMembers, reportMember };
