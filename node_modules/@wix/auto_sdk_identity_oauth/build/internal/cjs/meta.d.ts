import { RawHttpRequest as RawHttpRequest$1, RawHttpResponse as RawHttpResponse$1, JwksRequest as JwksRequest$1, TokenInfoResponse as TokenInfoResponse$1, UserInfoRequest as UserInfoRequest$1, UserInfoResponse as UserInfoResponse$1 } from './index.typings.js';
import '@wix/sdk-types';

interface RefreshToken {
    token?: string;
}
/**
 * AuthorizeRequest is sent by the client to the authorization server to initiate
 * the authorization process.
 */
interface AuthorizeRequest {
    /**
     * ID of the Wix OAuth app requesting authorization.
     * @format GUID
     */
    clientId?: string;
    /**
     * Desired authorization [grant type](https://auth0.com/docs/authenticate/protocols/oauth#grant-types).
     *
     * Supported values:
     * + `code`: The endpoint returns an authorization code that can be used to obtain an access token.
     * @minLength 1
     */
    responseType?: string;
    /**
     * URI to redirect the browser to after authentication and authorization. The browser is redirected to this URI whether the authentication and authorization process is successful or not.
     * @minLength 1
     */
    redirectUri?: string | null;
    /**
     * Desired scope of access. If this field is left empty, only an access token is granted.
     * To received a refresh token, pass `offline_access` as the value of this field.
     */
    scope?: string | null;
    /**
     * A value used to confirm the state of an application before and after it makes an authorization
     * request. If a value for this field is set in the request, it's added to the `redirectUri` when the browser
     * is redirected there.
     * Learn more about [using the state parameter](https://auth0.com/docs/secure/attack-protection/state-parameters).
     * @minLength 1
     */
    state?: string;
    /**
     * esired response format.
     *
     * Supported values:
     * + `query`: The response parameters are encoded as query string parameters and added to the `redirectUri` when redirecting.
     * + `fragment`: The response parameters are encoded as URI fragment parameters and added to the `redirectUri` when redirecting.
     * + `web_message`: The response parameters are encoded as a JSON object and added to the body of a [web message response](https://datatracker.ietf.org/doc/html/draft-sakimura-oauth-wmrm-00).
     *
     * Default value: `query`
     */
    responseMode?: string | null;
    /**
     * Code challenge to use for PKCE verification.
     * This field is only used if `responseType` is set to `code`.
     */
    codeChallenge?: string | null;
    /**
     * Code challenge method to use for PKCE verification.
     * This field is only used if `responseType` is set to `code`.
     *
     * Supported values:
     * + `S256`: The code challenge is transformed using SHA-256 encyption.
     * + `S512`: The code challenge is transformed using SHA-512 encyption.
     */
    codeChallengeMethod?: string | null;
    /**
     * Token returned by a successful [login](https://dev.wix.com/docs/api-reference/business-management/headless/authentication/login-v-2) or [register](https://dev.wix.com/docs/api-reference/business-management/headless/authentication/register-v-2) call that identifies the authenticated member.
     *
     * In the default SDK flow, session context is passed through a hidden iframe automatically. When using a [full-page redirect](https://dev.wix.com/docs/go-headless/develop-your-project/self-managed-headless/authentication/members/custom-login-page/custom-login/custom-login-using-the-js-sdk#use-a-full-page-redirect-desktop-and-mobile-browsers) instead, you must pass this field explicitly because there is no iframe to carry the session context.
     */
    sessionToken?: string | null;
    /**
     * Optional fields for errors
     * A short error code that describes the type of error that occurred (e.g. "invalid_request")
     * @internal
     */
    error?: string | null;
    /**
     * A human-readable description of the error that occurred
     * @internal
     */
    errorDescription?: string | null;
    /**
     * DEPRECATED: no longer read. The sign-in URL is resolved from the client's OAuthClientConfiguration
     * component (sign_in_url), falling back to the default Wix sign-in page.
     * @internal
     * @format SECURE_WEB_URL
     * @deprecated
     */
    signInUrl?: string | null;
    /**
     * Indicates whether the user has consented to the requested scopes
     * @internal
     */
    userConsented?: boolean | null;
    /**
     * Indicates whether login is optional. If true, will not redirect to the sign_in_url if user is not logged in.
     * @internal
     */
    optionalLogin?: boolean | null;
    /**
     * The site id the authorization is requested for.
     * @internal
     * @format GUID
     */
    metaSiteId?: string | null;
    /**
     * ID of the IAM IdP connection to authenticate with when no member session exists.
     * When set, the sign-in redirect initiates an OIDC login against this IdP connection
     * (IAM's `/sso/login` endpoint) instead of the default Wix login page.
     * @internal
     * @format GUID
     */
    idp?: string | null;
    /**
     * OIDC nonce, bound to the authorization request and echoed back in the id_token.
     * @maxLength 512
     */
    nonce?: string | null;
    /**
     * RFC 9470 / OIDC acr_values: space-separated required ACRs (OR, in preference order) for step-up.
     * @internal
     */
    acrValues?: string | null;
    /**
     * RFC 9470 / OIDC max_age: max seconds since last auth (0 forces re-auth).
     * @internal
     */
    maxAge?: string | null;
    /** OIDC prompt. Only "none" is honored: request silent authorization with no user interaction. */
    prompt?: string | null;
    /**
     * Standard OAuth error code the step-up surface returns on the browser redirect back to authorize
     * (e.g. access_denied, unmet_authentication_requirements). Browser-supplied like acr_values/prompt, so
     * untrusted: allowlisted and forwarded to the client as the OAuth error.
     * @internal
     */
    stepUpError?: string | null;
}
interface RawHttpResponse {
    body?: Uint8Array;
    statusCode?: number | null;
    headers?: HeadersEntry[];
}
interface HeadersEntry {
    key?: string;
    value?: string;
}
interface RawHttpRequest {
    body?: Uint8Array;
    pathParams?: PathParametersEntry[];
    queryParams?: QueryParametersEntry[];
    headers?: HeadersEntry[];
    method?: string;
    rawPath?: string;
    rawQuery?: string;
}
interface PathParametersEntry {
    key?: string;
    value?: string;
}
interface QueryParametersEntry {
    key?: string;
    value?: string;
}
/** Request for the Jwks endpoint. Takes no parameters. */
interface JwksRequest {
}
interface DeviceCodeRequest {
    /** The ID of the application that asks for authorization. */
    clientId?: string;
    /**
     * scope is a space-delimited string that specifies the requested scope of the
     * access request.
     */
    scope?: string | null;
}
interface DeviceCodeResponse {
    /** is the unique code for the device. When the user goes to the verification_uri in their browser-based device, this code will be bound to their session. */
    deviceCode?: string;
    /** contains the code that should be input at the verification_uri to authorize the device. */
    userCode?: string;
    /** contains the URL the user should visit to authorize the device. */
    verificationUri?: string;
    /** indicates the lifetime (in seconds) of the device_code and user_code. */
    expiresIn?: number;
    /** indicates the interval (in seconds) at which the app should poll the token URL to request a token. clients MUST use 5 as the default */
    interval?: number | null;
}
interface DeviceAuthorizeRequest {
    /** The ID of the application that asks for authorization. */
    clientId?: string;
    /** scope is a space-delimited string that specifies the requested scope of the access request. */
    scope?: string | null;
    /**
     * OIDC nonce, bound to the device authorization and echoed back in the id_token at /token.
     * @maxLength 512
     */
    nonce?: string | null;
}
interface DeviceAuthorizeResponse {
    /** the unique code for the device, used to poll the token endpoint. */
    deviceCode?: string;
    /** the code the user enters at the verification_uri to authorize the device. */
    userCode?: string;
    /** the URL the user should visit to authorize the device. */
    verificationUri?: string;
    /** the verification_uri with the user_code embedded as a query param, ready to encode into a QR code. */
    verificationUriComplete?: string;
    /** lifetime (in seconds) of the device_code and user_code. */
    expiresIn?: number;
    /** interval (in seconds) at which the client should poll the token endpoint. clients MUST use 5 as the default. */
    interval?: number | null;
}
interface DeviceVerifyRequest {
    /** User code representing a currently authorizing device. */
    userCode?: string;
}
interface DeviceVerifyResponse {
}
interface DeviceVerifyV2Request {
    /**
     * User code representing a currently authorizing device.
     * @minLength 8
     * @maxLength 8
     */
    userCode?: string;
}
interface DeviceVerifyV2Response {
}
interface DeviceAuthorizationInfoRequest {
    /**
     * User code representing a currently authorizing device.
     * @minLength 8
     * @maxLength 8
     */
    userCode?: string;
}
interface DeviceAuthorizationInfoResponse {
    /** The ID of the application the device is authorizing into. */
    clientId?: string;
    /** Canonical browser name of the initiating browser (e.g. "chrome"). */
    browser?: string | null;
    /** Canonical OS of the initiating browser (e.g. "windows"). */
    os?: string | null;
    /** Canonical device class of the initiating browser (e.g. "desktop"). */
    deviceClass?: string | null;
    /** IP address of the initiating browser. */
    browserIp?: string | null;
    /** Coarse geo-location (country, optional region) derived from the browser IP, e.g. "IL" or "US-NY". */
    geo?: string | null;
}
interface InvalidateUserCodeRequest {
    /** user code to invalidate. Only the authorizing identity is able to invalidate it. */
    userCode?: string;
}
interface InvalidateUserCodeResponse {
}
interface RevokeRefreshTokenRequest {
    /** The refresh token itself. Anyone with the token itself is able to revoke it. */
    token?: string;
    /** The client id that issued the refresh token. */
    clientId?: string | null;
}
interface RevokeRefreshTokenResponse {
}
interface TokenInfoResponse {
    active?: boolean;
    /** subject type. */
    subjectType?: SubjectTypeWithLiterals;
    /** subject id */
    subjectId?: string;
    /** Expiration time of the token */
    exp?: string | null;
    /** Issued time of the token */
    iat?: string | null;
    /** Client id */
    clientId?: string;
    /** Account id */
    accountId?: string | null;
    /** Site id */
    siteId?: string | null;
    /** Instance Id */
    instanceId?: string | null;
    /**
     * Vendor Product Id
     * @maxLength 100
     */
    vendorProductId?: string | null;
}
declare enum SubjectType {
    /** unknown subject type */
    UNKNOWN = "UNKNOWN",
    /** user subject type */
    USER = "USER",
    /** visitor subject type */
    VISITOR = "VISITOR",
    /** member subject type */
    MEMBER = "MEMBER",
    /** app subject type */
    APP = "APP"
}
/** @enumType */
type SubjectTypeWithLiterals = SubjectType | 'UNKNOWN' | 'USER' | 'VISITOR' | 'MEMBER' | 'APP';
interface Empty {
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entityAsJson?: string;
    /**
     * Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity
     * @internal
     */
    triggeredByUndelete?: boolean | null;
    /** Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity */
    restoreInfo?: RestoreInfo;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntityAsJson?: string;
    /**
     * This field is currently part of the of the EntityUpdatedEvent msg, but scala/node libraries which implements the domain events standard
     * wont populate it / have any reference to it in the API.
     * The main reason for it is that fetching the old entity from the DB will have a performance hit on an update operation so unless truly needed,
     * the developer should send only the new (current) entity.
     * An additional reason is not wanting to send this additional entity over the wire (kafka) since in some cases it can be really big
     * Developers that must reflect the old entity will have to implement their own domain event sender mechanism which will follow the DomainEvent proto message.
     * @internal
     * @deprecated
     */
    previousEntityAsJson?: string | null;
    /**
     * Map of fully qualified field paths to their previous values for fields that changed.
     * For more details please see [AIP](https://dev.wix.com/docs/rnd-general/articles/p13n-guidelines-aips/guidance-aips/design-patterns/7016-events#modified-fields-format)
     * @internal
     */
    modifiedFields?: Record<string, any>;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface EntityDeletedEvent {
    /**
     * Indicates if the entity is sent to trash-bin. only available when trash-bin is enabled
     * @internal
     */
    movedToTrash?: boolean | null;
    /** Entity that was deleted. */
    deletedEntityAsJson?: string | null;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface ActionEvent {
    bodyAsJson?: string;
}
interface UserDeletedEvent {
    /**
     * User ID from the deletion event.
     * @format GUID
     */
    userId?: string;
}
interface UserBlockedEvent {
    /**
     * User ID from the deletion event.
     * @format GUID
     */
    userId?: string;
}
interface UserInfoRequest {
}
/** UserInfo endpoint response for OpenID Connect */
interface UserInfoResponse {
    /**
     * Subject identifier — unique identifier for the user (OIDC standard claim).
     * @format GUID
     */
    sub?: string;
    /**
     * The user's given name.
     * @maxLength 20
     */
    givenName?: string | null;
    /**
     * The user's family name.
     * @maxLength 20
     */
    familyName?: string | null;
    /**
     * The user's email address.
     * @format EMAIL
     */
    email?: string | null;
    /** Indicates whether the user's email address has been verified. */
    emailVerified?: boolean | null;
    /**
     * The URL of the user's profile picture.
     * @format WEB_URL
     */
    picture?: string | null;
    /**
     * The user's preferred language, if available.
     * @format LANGUAGE
     */
    language?: string | null;
    /** The user's full name. */
    name?: string | null;
}

type __PublicMethodMetaInfo<K = string, M = unknown, T = unknown, S = unknown, Q = unknown, R = unknown> = {
    getUrl: (context: any) => string;
    httpMethod: K;
    path: string;
    pathParams: M;
    __requestType: T;
    __originalRequestType: S;
    __responseType: Q;
    __originalResponseType: R;
};
declare function token(): __PublicMethodMetaInfo<'POST', {}, RawHttpRequest$1, RawHttpRequest, RawHttpResponse$1, RawHttpResponse>;
declare function jwks(): __PublicMethodMetaInfo<'GET', {}, JwksRequest$1, JwksRequest, RawHttpResponse$1, RawHttpResponse>;
declare function tokenInfo(): __PublicMethodMetaInfo<'POST', {}, RawHttpRequest$1, RawHttpRequest, TokenInfoResponse$1, TokenInfoResponse>;
declare function userInfo(): __PublicMethodMetaInfo<'GET', {}, UserInfoRequest$1, UserInfoRequest, UserInfoResponse$1, UserInfoResponse>;

export { type ActionEvent as ActionEventOriginal, type AuthorizeRequest as AuthorizeRequestOriginal, type DeviceAuthorizationInfoRequest as DeviceAuthorizationInfoRequestOriginal, type DeviceAuthorizationInfoResponse as DeviceAuthorizationInfoResponseOriginal, type DeviceAuthorizeRequest as DeviceAuthorizeRequestOriginal, type DeviceAuthorizeResponse as DeviceAuthorizeResponseOriginal, type DeviceCodeRequest as DeviceCodeRequestOriginal, type DeviceCodeResponse as DeviceCodeResponseOriginal, type DeviceVerifyRequest as DeviceVerifyRequestOriginal, type DeviceVerifyResponse as DeviceVerifyResponseOriginal, type DeviceVerifyV2Request as DeviceVerifyV2RequestOriginal, type DeviceVerifyV2Response as DeviceVerifyV2ResponseOriginal, type DomainEventBodyOneOf as DomainEventBodyOneOfOriginal, type DomainEvent as DomainEventOriginal, type Empty as EmptyOriginal, type EntityCreatedEvent as EntityCreatedEventOriginal, type EntityDeletedEvent as EntityDeletedEventOriginal, type EntityUpdatedEvent as EntityUpdatedEventOriginal, type HeadersEntry as HeadersEntryOriginal, type InvalidateUserCodeRequest as InvalidateUserCodeRequestOriginal, type InvalidateUserCodeResponse as InvalidateUserCodeResponseOriginal, type JwksRequest as JwksRequestOriginal, type PathParametersEntry as PathParametersEntryOriginal, type QueryParametersEntry as QueryParametersEntryOriginal, type RawHttpRequest as RawHttpRequestOriginal, type RawHttpResponse as RawHttpResponseOriginal, type RefreshToken as RefreshTokenOriginal, type RestoreInfo as RestoreInfoOriginal, type RevokeRefreshTokenRequest as RevokeRefreshTokenRequestOriginal, type RevokeRefreshTokenResponse as RevokeRefreshTokenResponseOriginal, SubjectType as SubjectTypeOriginal, type SubjectTypeWithLiterals as SubjectTypeWithLiteralsOriginal, type TokenInfoResponse as TokenInfoResponseOriginal, type UserBlockedEvent as UserBlockedEventOriginal, type UserDeletedEvent as UserDeletedEventOriginal, type UserInfoRequest as UserInfoRequestOriginal, type UserInfoResponse as UserInfoResponseOriginal, type __PublicMethodMetaInfo, jwks, token, tokenInfo, userInfo };
