import * as _wix_sdk_types from '@wix/sdk-types';
import { SearchSpec, QuerySpec, Search, Query, NonNullablePaths } from '@wix/sdk-types';

/** Form submission that was created or retrieved. */
interface FormSubmission {
    /**
     * Submission ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * ID of the form which the submission belongs to.
     * @format GUID
     * @immutable
     */
    formId?: string;
    /**
     * The app which the form submissions belong to. A submission inherits the namespace from its form schema, so you can't set it directly.
     *
     * For example, a submission of a form visible in the Wix Forms app has the namespace `wix.form_app.form`, the namespace of the Wix Forms app, whose app ID is `225dd912-7dea-4738-8688-4b8c6955ffc2`.
     *
     * See a list of namespaces for [apps made by Wix](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/introduction#namespaces-of-apps-made-by-wix) section.
     * To retrieve the ID for a Wix app, see [Apps Created By Wix](https://dev.wix.com/docs/rest/articles/get-started/apps-created-by-wix).
     * @minLength 10
     * @maxLength 50
     * @readonly
     */
    namespace?: string;
    /**
     * Status of the submission.
     *
     * The status a submission is created with depends on the form's namespace and on whether the form collects payment, so read it from the response rather than assuming.
     *
     * A submission for a form that collects payment starts as `PAYMENT_WAITING`. Otherwise, most namespaces record the submission immediately as `CONFIRMED`. Namespaces that need to complete another step first, such as taking payment for a pricing plan, create the submission as `PENDING` and confirm it later with Confirm Submission.
     *
     * A `PENDING` submission isn't recorded yet, and is deleted automatically if it isn't confirmed in time.
     */
    status?: SubmissionStatusWithLiterals;
    /**
     * Values the visitor submitted.
     *
     * Each key is the `target` of a field in the form schema, which is the field's stable storage key rather than its ID or label. Only keys matching a field target are accepted.
     *
     * Each value's shape follows that field's `inputType`: `STRING` fields submit a string, `NUMBER` a number, `BOOLEAN` a boolean, and `ARRAY` an array of the selected option values. Date and time fields are strings. A choice field submits an option's `value`, not its ID or its label.
     *
     * 3 input types submit structured values: `ADDRESS` submits an address object, `PAYMENT` an array holding 1 product object per item bought, and `SCHEDULING` an appointment object.
     *
     * For the full mapping, see [About Submission Values](https://dev.wix.com/docs/api-reference/crm/forms/form-submissions/about-submission-values).
     */
    submissions?: Record<string, any>;
    /**
     * Date and time the form submission was created.
     * @immutable
     */
    _createdDate?: Date | null;
    /**
     * Date and time the form submission was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Revision number, which increments by 1 each time the form submission is updated. To prevent conflicting changes, the existing revision must be used when updating a form submission.
     * @readonly
     */
    revision?: string | null;
    /**
     * ID of the visitor that submitted the form.
     * @readonly
     */
    submitter?: Submitter;
    /** Whether a site owner marked a submission as "seen". */
    seen?: boolean;
    /** Data extension object that holds users' and apps' fields. */
    extendedFields?: ExtendedFields;
    /**
     * Order details. <br>
     * <b>Note</b>: This object is only applicable when submitting a form in the Wix Payments app.
     */
    orderDetails?: OrderDetails;
    /**
     * Contact ID of a site visitor who created the submission.
     * @format GUID
     * @immutable
     */
    contactId?: string | null;
    /**
     * Authorized viewer ID.
     * @format GUID
     * @immutable
     */
    accessRestriction?: string | null;
    /** Tag IDs collections associated with the current entity. */
    tags?: PublicTags;
    /**
     * Appointment details.
     * This object is only applicable when submitting a form that creates a booking appointment.
     */
    appointmentDetails?: AppointmentDetails;
    /**
     * ID of the child site the submission was made on.
     *
     * Only present when the form was submitted on a site that shares its data with a parent site. In that case submissions from every child site are stored on the parent site, and this field identifies which child site the submission came from.
     * @format GUID
     * @readonly
     */
    childMetaSiteId?: string | null;
}
declare enum SubmissionStatus {
    /** A submission is created, but has not yet been recorded in the Wix Forms collection. */
    PENDING = "PENDING",
    /** A submission is recorded in the Wix Forms collection. */
    CONFIRMED = "CONFIRMED",
    /** A form submission requiring payment is created. */
    PAYMENT_WAITING = "PAYMENT_WAITING",
    /** An order of a form submission is canceled. */
    PAYMENT_CANCELED = "PAYMENT_CANCELED"
}
/** @enumType */
type SubmissionStatusWithLiterals = SubmissionStatus | 'PENDING' | 'CONFIRMED' | 'PAYMENT_WAITING' | 'PAYMENT_CANCELED';
interface Submitter extends SubmitterSubmitterOneOf {
    /**
     * Member ID.
     * @format GUID
     */
    memberId?: string | null;
    /**
     * Visitor ID.
     * @format GUID
     */
    visitorId?: string | null;
    /**
     * Application ID.
     * @maxLength 36
     */
    applicationId?: string | null;
    /**
     * User ID.
     * @format GUID
     */
    userId?: string | null;
}
/** @oneof */
interface SubmitterSubmitterOneOf {
    /**
     * Member ID.
     * @format GUID
     */
    memberId?: string | null;
    /**
     * Visitor ID.
     * @format GUID
     */
    visitorId?: string | null;
    /**
     * Application ID.
     * @maxLength 36
     */
    applicationId?: string | null;
    /**
     * User ID.
     * @format GUID
     */
    userId?: string | null;
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface OrderDetails {
    /**
     * ID of the order related to submission (only applicable if a form has payments).
     * @format GUID
     * @readonly
     */
    orderId?: string | null;
    /**
     * Order number.
     * @readonly
     */
    number?: string | null;
    /**
     * Currency.
     * @format CURRENCY
     * @readonly
     */
    currency?: string | null;
    /**
     * Item subtotal.
     * @decimalValue options { gte:0.00, maxScale:2 }
     * @readonly
     */
    itemSubtotal?: string;
    /**
     * ID of the checkout related to submission (only applicable if a form has payments).
     * @format GUID
     * @readonly
     */
    checkoutId?: string;
}
interface PublicTags {
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    tags?: TagList;
}
interface TagList {
    /**
     * List of tag IDs
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
interface AppointmentDetails {
    /**
     * ID of the staff member assigned to the appointment.
     * @format GUID
     */
    staffMemberId?: string | null;
    /**
     * Name of the staff member assigned to the appointment.
     * @minLength 1
     * @maxLength 40
     */
    staffMemberName?: string | null;
}
interface FormSubmissionStatusUpdatedEvent {
    /** Updated submission. */
    submission?: FormSubmission;
    /** Previous status of the submission. */
    previousStatus?: SubmissionStatusWithLiterals;
}
interface RemovedSubmissionFromTrash {
    /** Removed submission. */
    submission?: FormSubmission;
}
interface SubmissionContactMapped {
    /**
     * Mapped upserted contact ID.
     * @format GUID
     * @readonly
     */
    contactId?: string;
    /**
     * Identifies the namespace that the submission's form belongs to.
     * @minLength 10
     * @maxLength 50
     * @immutable
     */
    namespace?: string;
    /** Marketing subscription details */
    marketingSubscriptionDetails?: MarketingSubscriptionDetails;
    /** Identity type of the returned contact. */
    contactIdentityType?: ContactIdentityTypeWithLiterals;
}
interface MarketingSubscriptionDetails {
    /**
     * Form id which was submitted
     * @format GUID
     */
    formId?: string;
    /**
     * Mapped contact emails.
     * @format EMAIL
     * @maxSize 100
     */
    emails?: string[];
    /**
     * Date and time the form submission was created.
     * @readonly
     */
    submittedDate?: Date | null;
    /**
     * Email subscription consent opt in level, either single or double confirmation.
     * Default: SINGLE_CONFIRMATION
     */
    optInLevel?: OptInLevelWithLiterals;
    /**
     * Mapped valid contact phones.
     * @format PHONE
     * @maxSize 100
     */
    phones?: string[];
    /**
     * Phone subscription consent opt in level, either single or double confirmation.
     * Default: SINGLE_CONFIRMATION
     */
    phoneOptInLevel?: OptInLevelWithLiterals;
}
declare enum OptInLevel {
    /** Single confirmation. */
    SINGLE_CONFIRMATION = "SINGLE_CONFIRMATION",
    /** Double confirmation. */
    DOUBLE_CONFIRMATION = "DOUBLE_CONFIRMATION"
}
/** @enumType */
type OptInLevelWithLiterals = OptInLevel | 'SINGLE_CONFIRMATION' | 'DOUBLE_CONFIRMATION';
declare enum ContactIdentityType {
    /** The returned contact ID belongs to a new or existing contact. */
    CONTACT = "CONTACT",
    /** The returned contact ID belongs to the currently logged-in site member. */
    MEMBER = "MEMBER",
    /** The returned contact ID belongs to a site member who is not currently logged in. */
    NOT_AUTHENTICATED_MEMBER = "NOT_AUTHENTICATED_MEMBER"
}
/** @enumType */
type ContactIdentityTypeWithLiterals = ContactIdentityType | 'CONTACT' | 'MEMBER' | 'NOT_AUTHENTICATED_MEMBER';
interface SubmissionContactMappingSkipped {
    /**
     * Form Id.
     * @format GUID
     */
    formId?: string;
    /**
     * Identifies the namespace that the submission's form belongs to.
     * @minLength 10
     * @maxLength 50
     * @immutable
     */
    namespace?: string;
    /**
     * Contact ID resolved from the submission context.
     * @format GUID
     * @readonly
     */
    contactId?: string | null;
}
interface SearchSubmissionsForExtendedExportRequest {
    /** Cursor search. Specify filters for both `namespace` and `formId`. */
    search?: CursorSearch;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     * @maxSize 3
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * You can get the relevant cursor token
     * from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface CursorSearch extends CursorSearchPagingMethodOneOf {
    /**
     * Cursor pointing to page of results.
     * When requesting 'cursor_paging.cursor', no `filter`, `sort` or `search` can be provided.
     */
    cursorPaging?: CursorPaging;
    /** A filter object. See documentation [here](https://bo.wix.com/wix-docs/rnd/platformization-guidelines/api-query-language#platformization-guidelines_api-query-language_defining-in-protobuf) */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the form [{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]
     * @maxSize 10
     */
    sort?: Sorting[];
    /** Free text to match in searchable fields */
    search?: SearchDetails;
}
/** @oneof */
interface CursorSearchPagingMethodOneOf {
    /**
     * Cursor pointing to page of results.
     * When requesting 'cursor_paging.cursor', no `filter`, `sort` or `search` can be provided.
     */
    cursorPaging?: CursorPaging;
}
interface SearchDetails {
    /** Defines how separate search terms in `expression` are combined */
    mode?: ModeWithLiterals;
    /**
     * Search term or expression
     * @maxLength 100
     */
    expression?: string | null;
    /** Flag if should use auto fuzzy search (allowing typos by a managed proximity algorithm) */
    fuzzy?: boolean;
}
declare enum Mode {
    /** Any of the search terms must be present */
    OR = "OR",
    /** All search terms must be present */
    AND = "AND"
}
/** @enumType */
type ModeWithLiterals = Mode | 'OR' | 'AND';
interface SearchSubmissionsForExtendedExportResponse {
    /**
     * Submissions prepared for extended export; each item may include optional quiz-derived metadata.
     * @maxSize 100
     */
    exportRows?: SubmissionExtendedExportRow[];
    /** Paging metadata aligned with the delegated submission export search. */
    metadata?: CursorPagingMetadata;
}
/** Single submission row for extended export: core `submission` plus optional nested quiz result. */
interface SubmissionExtendedExportRow {
    /** Core submission entity including flattened field values for export. */
    submission?: FormSubmission;
    /**
     * Present when quiz grading is completed for this submission.
     * Omitted when the submission isn't graded as a quiz, no quiz result exists, or calculation isn't completed.
     */
    quizResult?: QuizExportResult;
}
/** Quiz grading summary for extended export. */
interface QuizExportResult {
    /** Total points earned for this submission. */
    score?: number | null;
    /** Maximum possible points for the graded form version. */
    maxPossibleScore?: number | null;
    /**
     * Score as a percentage from 0 to 100.
     * @max 100
     */
    percentageScore?: number | null;
    /**
     * Quiz outcome label.
     * Returned values: `Passed` or `Failed`.
     * Omitted when the source outcome is unknown.
     * @maxLength 32
     */
    outcome?: string | null;
    /**
     * Per-field grading outcome keyed by form field target.
     * `true` means the answer was correct. `false` means any non-correct outcome.
     * @maxSize 200
     */
    fieldResults?: Record<string, boolean>;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor pointing to next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
interface CreateCheckoutFromSubmissionRequest extends CreateCheckoutFromSubmissionRequestFormSchemaIdentifierOneOf {
    /** Submission's form. */
    form?: Form;
    /** Submission to create checkout from. */
    submission?: FormSubmission;
}
/** @oneof */
interface CreateCheckoutFromSubmissionRequestFormSchemaIdentifierOneOf {
    /** Submission's form. */
    form?: Form;
}
interface Form {
    /**
     * Form schema ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * Form fields that define the input elements and their validation rules.
     *
     * The order of this array isn't the order the fields are displayed in. Display order comes from `steps`, where each layout item positions a field by `row` and `column`. Don't render fields in array order.
     *
     * For a detailed explanation of how to compose each kind of field, including which `identifier`, `inputType`, and `componentType` values to combine, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).
     * @maxSize 500
     */
    formFields?: Field[];
    /**
     * Layout configuration that defines how form fields are organized across multiple steps.
     * Each step represents a page of the form.
     *
     * Every field in `formFields`, including the submit button, must be placed by a layout item whose `fieldId` matches the field's `id`. A field that isn't placed still stores submitted values, but the Wix dashboard renders it, and the submissions made against it, as empty.
     *
     * Use a single step unless the form is split across pages.
     * @maxSize 100
     */
    steps?: Step[];
    /**
     * Form rules, can be applied to layout and items properties.
     * @maxSize 100
     * @deprecated Form rules, can be applied to layout and items properties.
     * @replacedBy wix.forms.v4.Form.form_rules
     * @targetRemovalDate 2026-04-01
     */
    rules?: FormRule[];
    /**
     * Revision number, which increments by 1 each time the form schema is updated.
     * To prevent conflicting changes, the current revision must be passed when updating the form schema.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time when the form schema was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time when the form schema was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Deprecated. Use `deletedFormFields` instead.
     * @maxSize 150
     * @readonly
     * @deprecated Deprecated. Use `deletedFormFields` instead.
     * @replacedBy wix.forms.v4.Form.deleted_form_fields
     * @targetRemovalDate 2027-02-01
     */
    deletedFields?: FormField[];
    /**
     * Form fields that were soft deleted.
     * @maxSize 150
     * @readonly
     */
    deletedFormFields?: Field[];
    /** Data extensions for storing additional custom properties. */
    extendedFields?: ExtendedFields;
    /**
     * Unique identifier for the app that owns this form schema.
     *
     * For example, for creating a form visible in the Wix Forms app, use `wix.form_app.form`, the namespace of the Wix Forms app, whose app ID is `225dd912-7dea-4738-8688-4b8c6955ffc2`.
     *
     * See a list of namespaces for [apps made by Wix](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/introduction#namespaces-of-apps-made-by-wix) section.
     * To retrieve the ID for a Wix app, see [Apps Created By Wix](https://dev.wix.com/docs/rest/articles/get-started/apps-created-by-wix).
     * @minLength 10
     * @maxLength 50
     * @immutable
     */
    namespace?: string;
    /**
     * Media folder ID for storing uploaded files from form submissions.
     * @maxLength 100
     * @readonly
     */
    mediaFolderId?: string | null;
    /** Submission limits that can automatically disable the form when reached. */
    limitationRule?: LimitationRule;
    /**
     * Level of spam protection applied to form submissions.
     *
     * Default: `ADVANCED`
     */
    spamFilterProtectionLevel?: SpamFilterProtectionLevelWithLiterals;
    /** Configuration for how required field indicators are displayed to the submitter. */
    requiredIndicatorProperties?: RequiredIndicatorProperties;
    /** Configuration for actions taken after a form is successfully submitted. */
    submitSettings?: SubmitSettings;
    /**
     * Message displayed to visitors when the form is disabled.
     * This allows you to inform submitters why the form isn't available.
     */
    disabledFormMessage?: RichContent;
    /**
     * Whether the form is effectively active — `true` only when the user has not disabled the form
     * **and** no system service has placed an `activationBlock` on it.
     *
     * Writing `enabled: true` records the user's intent to activate the form, but the value read
     * back may still be `false` if `activationBlock` is present (for example, a plan restriction is in
     * effect). Once the block is lifted the form becomes active automatically without any additional
     * write.
     *
     * Default: `true`
     */
    enabled?: boolean | null;
    /**
     * Display name of the form schema.
     *
     * Names are unique in a namespace. If the name you pass is already taken, the request still succeeds, and the form schema is stored under a numbered variation of that name instead of returning an error. Read `name` from the response to learn the name the schema was actually saved under.
     * @maxLength 200
     */
    name?: string | null;
    /**
     * Business logic rules that can modify form behavior based on user input or other conditions.
     * These rules can show/hide fields, change validation, or trigger actions.
     * @maxSize 100
     */
    formRules?: Rule[];
    /** Tag IDs collections associated with the current entity. */
    tags?: Tags;
    /**
     * Quiz configuration for a scored form. Set this when the form is a quiz whose
     * answers are scored: it holds the passing criteria (score threshold in points or
     * percentage), how the result is shown on the confirmation page (score, pass/fail
     * status, or both), and the pass/fail messages. Absent on non-quiz forms.
     */
    quizSettings?: QuizSettings;
    /** Source used for contact autofill when filling form fields. Default: FORM_INPUT. */
    autoFillContact?: ContactAutofillWithLiterals;
    /**
     * Controls who can read submissions of this form beyond the standard permission.
     *
     * Default: `OWNER_AND_COLLABORATORS`
     */
    submissionAccess?: SubmissionAccessWithLiterals;
}
interface FormField {
    /**
     * Item ID.
     * @minLength 1
     * @format GUID
     * @immutable
     */
    _id?: string;
    /**
     * Definition of a target where the value of field belongs.
     * @maxLength 200
     * @immutable
     */
    target?: string | null;
    /** Validation of field output value. */
    validation?: Validation;
    /** Mark the field as containing personal information. This will encrypt user data when storing it. */
    pii?: boolean;
    /** Whether the field is hidden. */
    hidden?: boolean;
    /** Field view properties. */
    view?: Record<string, any> | null;
    /** Details identifying field, which is extension of other entity */
    dataExtensionsDetails?: DataExtensionsDetails;
    /**
     * Whether the field is read only.
     * Default: false
     */
    readOnly?: boolean;
}
interface FormFieldStringType extends FormFieldStringTypeFormatOptionsOneOf {
    /** DATE format options */
    dateOptions?: DateTimeAdvancedConstraints;
    /** DATE_TIME format options */
    dateTimeOptions?: StringTypeDateTimeConstraints;
    /** TIME format options */
    timeOptions?: StringTypeDateTimeConstraints;
    /** DATE_OPTIONAL_TIME format options */
    dateOptionalTimeOptions?: StringTypeDateTimeConstraints;
    /** PHONE format options */
    phoneOptions?: StringTypePhoneConstraints;
    /**
     * Minimum length.
     * @max 20000
     */
    minLength?: number | null;
    /**
     * Maximum length.
     * @max 20000
     */
    maxLength?: number | null;
    /**
     * Pattern for a regular expression match.
     * @maxLength 500
     */
    pattern?: string | null;
    /** Format of a string. */
    format?: StringTypeFormatEnumFormatWithLiterals;
    /** Custom error messages when validation fails. */
    errorMessages?: StringErrorMessages;
    /**
     * List of allowed values.
     * @maxSize 500
     * @maxLength 20000
     */
    enum?: string[] | null;
    /** User defined error messages when particular validation constraint fails */
    validationMessages?: StringTypeValidationMessages;
}
/** @oneof */
interface FormFieldStringTypeFormatOptionsOneOf {
    /** DATE format options */
    dateOptions?: DateTimeAdvancedConstraints;
    /** DATE_TIME format options */
    dateTimeOptions?: StringTypeDateTimeConstraints;
    /** TIME format options */
    timeOptions?: StringTypeDateTimeConstraints;
    /** DATE_OPTIONAL_TIME format options */
    dateOptionalTimeOptions?: StringTypeDateTimeConstraints;
    /** PHONE format options */
    phoneOptions?: StringTypePhoneConstraints;
}
declare enum StringTypeFormatEnumFormat {
    UNDEFINED = "UNDEFINED",
    DATE = "DATE",
    TIME = "TIME",
    DATE_TIME = "DATE_TIME",
    EMAIL = "EMAIL",
    URL = "URL",
    UUID = "UUID",
    PHONE = "PHONE",
    URI = "URI",
    HOSTNAME = "HOSTNAME",
    COLOR_HEX = "COLOR_HEX",
    CURRENCY = "CURRENCY",
    LANGUAGE = "LANGUAGE",
    DATE_OPTIONAL_TIME = "DATE_OPTIONAL_TIME"
}
/** @enumType */
type StringTypeFormatEnumFormatWithLiterals = StringTypeFormatEnumFormat | 'UNDEFINED' | 'DATE' | 'TIME' | 'DATE_TIME' | 'EMAIL' | 'URL' | 'UUID' | 'PHONE' | 'URI' | 'HOSTNAME' | 'COLOR_HEX' | 'CURRENCY' | 'LANGUAGE' | 'DATE_OPTIONAL_TIME';
interface StringErrorMessages {
    /**
     * Default error message on invalid validation.
     * @maxLength 200
     */
    default?: string | null;
}
interface DateTimeAdvancedConstraints {
    /**
     * Allows to specify available and unavailable dates and days of the week.
     * If unset, all dates from minimum to maximum are available.
     */
    availability?: Availability;
    /**
     * Support static constrains defined as ISO date/time format, as well as
     * dynamic calculations can be performed using special keywords such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    minimum?: string | null;
    /**
     * Support static constrains defined as ISO date/time format, as well as
     * dynamic calculations can be performed using special keywords such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    maximum?: string | null;
}
interface Availability {
    /**
     * Available date ranges.
     * If empty, all dates are available; if non-empty, only dates within these ranges are available.
     * `unavailableDates` takes priority in case of overlap — for example, an empty `availableDates`
     * with a non-empty `unavailableDates` means all dates are available except the excluded ranges.
     * @maxSize 10
     */
    availableDates?: DateRange[];
    /**
     * Unavailable date ranges. They take priority over `availableDates` in case of overlap.
     * @maxSize 10
     */
    unavailableDates?: DateRange[];
    /**
     * During `availableDates`, only these days of the week are available.
     * @maxSize 7
     */
    availableDays?: DayOfWeekWithLiterals[];
}
interface DateRange {
    /**
     * The beginning of the date range (inclusive). ISO 8601 date format (YYYY-MM-DD).
     * @format LOCAL_DATE
     */
    start?: string;
    /**
     * The end of the date range (inclusive). ISO 8601 date format (YYYY-MM-DD).
     * @format LOCAL_DATE
     */
    end?: string;
}
declare enum DayOfWeek {
    MONDAY = "MONDAY",
    TUESDAY = "TUESDAY",
    WEDNESDAY = "WEDNESDAY",
    THURSDAY = "THURSDAY",
    FRIDAY = "FRIDAY",
    SATURDAY = "SATURDAY",
    SUNDAY = "SUNDAY"
}
/** @enumType */
type DayOfWeekWithLiterals = DayOfWeek | 'MONDAY' | 'TUESDAY' | 'WEDNESDAY' | 'THURSDAY' | 'FRIDAY' | 'SATURDAY' | 'SUNDAY';
interface StringTypeDateTimeConstraints {
    /**
     * Support static constrains defined as ISO date/time format, as well as
     * dynamic calculations can be performed using special keywords such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    minimum?: string | null;
    /**
     * Support static constrains defined as ISO date/time format, as well as
     * dynamic calculations can be performed using special keywords such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    maximum?: string | null;
}
interface StringTypePhoneConstraints {
    /**
     * Country codes for phone number that are allowed
     * @maxSize 250
     * @maxLength 2
     */
    allowedCountryCodes?: string[];
}
interface StringTypeValidationMessages {
    /**
     * Error message shown when validation of patter fails
     * @maxLength 200
     */
    pattern?: string | null;
}
interface FormFieldNumberType {
    /** Inclusive maximum value. */
    maximum?: number | null;
    /** Inclusive minimum value. */
    minimum?: number | null;
    /** Multiple of value. */
    multipleOf?: number | null;
    /** Custom error message when validation fails. */
    errorMessages?: NumberErrorMessages;
    /**
     * List of allowed values.
     * @maxSize 500
     */
    enum?: number[] | null;
}
interface NumberErrorMessages {
    /**
     * Default error message on invalid validation.
     * @maxLength 200
     */
    default?: string | null;
}
interface IntegerType {
    /** Minimum value. */
    maximum?: number | null;
    /** Maximum value. */
    minimum?: number | null;
    /** Multiple of value. */
    multipleOf?: number | null;
    /** Custom error message when validation fails. */
    errorMessages?: NumberErrorMessages;
    /**
     * List of allowed values.
     * @maxSize 500
     */
    enum?: number[] | null;
}
interface FormFieldBooleanType {
    /** Custom error message when validation fails. */
    errorMessages?: BooleanErrorMessages;
    /**
     * List of allowed values.
     * @maxSize 2
     */
    enum?: boolean[];
}
interface BooleanErrorMessages {
    /**
     * Default error message on invalid validation.
     * @maxLength 200
     */
    default?: string | null;
}
interface FormFieldArrayType {
    /**
     * Maximum amount of array elements.
     * @max 1000
     */
    maxItems?: number | null;
    /**
     * Minimum amount of array elements.
     * @max 1000
     */
    minItems?: number | null;
    /** Type of items allowed in array. */
    items?: ArrayTypeArrayItems;
    /** Custom error message when validation fails. */
    errorMessages?: ArrayErrorMessages;
}
interface FormFieldObjectType {
    /** Description of object properties. */
    properties?: Record<string, ObjectTypePropertiesType>;
    /** Custom error message when validation fails. */
    errorMessages?: ObjectErrorMessages;
}
interface ObjectTypePropertiesType extends ObjectTypePropertiesTypePropertiesTypeOneOf {
    /** String type validation for property. */
    string?: FormFieldStringType;
    /** Number type validation for property. */
    number?: FormFieldNumberType;
    /** Boolean type validation for property. */
    boolean?: FormFieldBooleanType;
    /** Integer type validation for property. */
    integer?: IntegerType;
    /** Array type validation for property. */
    array?: FormFieldArrayType;
    /** Whether the property is required. */
    required?: boolean;
}
/** @oneof */
interface ObjectTypePropertiesTypePropertiesTypeOneOf {
    /** String type validation for property. */
    string?: FormFieldStringType;
    /** Number type validation for property. */
    number?: FormFieldNumberType;
    /** Boolean type validation for property. */
    boolean?: FormFieldBooleanType;
    /** Integer type validation for property. */
    integer?: IntegerType;
    /** Array type validation for property. */
    array?: FormFieldArrayType;
}
interface ObjectErrorMessages {
    /**
     * Default error message on invalid validation.
     * @maxLength 200
     */
    default?: string | null;
}
interface ArrayTypeArrayItems extends ArrayTypeArrayItemsItemsOneOf {
    /** String type validation for items. */
    string?: FormFieldStringType;
    /** Number type validation for items. */
    number?: FormFieldNumberType;
    /** Boolean type validation for items. */
    boolean?: FormFieldBooleanType;
    /** Integer type validation for items. */
    integer?: IntegerType;
    /** Object type validation for items */
    object?: FormFieldObjectType;
}
/** @oneof */
interface ArrayTypeArrayItemsItemsOneOf {
    /** String type validation for items. */
    string?: FormFieldStringType;
    /** Number type validation for items. */
    number?: FormFieldNumberType;
    /** Boolean type validation for items. */
    boolean?: FormFieldBooleanType;
    /** Integer type validation for items. */
    integer?: IntegerType;
    /** Object type validation for items */
    object?: FormFieldObjectType;
}
interface ArrayErrorMessages {
    /**
     * Default error message on invalid validation.
     * @maxLength 200
     */
    default?: string | null;
}
interface PredefinedValidation extends PredefinedValidationFormatOptionsOneOf {
    /** Payment input field. */
    paymentOptions?: PaymentType;
    /** Multiline address validation. */
    multilineAddressOptions?: MultilineAddressValidation;
    /** Validation configuration for the object array input. */
    objectArrayOptions?: ObjectArrayType;
    /** Format of predefined validation. */
    format?: ValidationFormatWithLiterals;
}
/** @oneof */
interface PredefinedValidationFormatOptionsOneOf {
    /** Payment input field. */
    paymentOptions?: PaymentType;
    /** Multiline address validation. */
    multilineAddressOptions?: MultilineAddressValidation;
    /** Validation configuration for the object array input. */
    objectArrayOptions?: ObjectArrayType;
}
declare enum ValidationFormat {
    UNDEFINED = "UNDEFINED",
    /** File upload validation. */
    WIX_FILE = "WIX_FILE",
    /** Payment validation. */
    PAYMENT = "PAYMENT",
    /** Multiline address. */
    MULTILINE_ADDRESS = "MULTILINE_ADDRESS",
    /** Scheduling validation. */
    SCHEDULING = "SCHEDULING",
    /** Object array validation. */
    OBJECT_ARRAY = "OBJECT_ARRAY"
}
/** @enumType */
type ValidationFormatWithLiterals = ValidationFormat | 'UNDEFINED' | 'WIX_FILE' | 'PAYMENT' | 'MULTILINE_ADDRESS' | 'SCHEDULING' | 'OBJECT_ARRAY';
interface PaymentType {
    /**
     * Products the submitter can select to make a payment for.
     * @minSize 1
     * @maxSize 100
     */
    products?: Product[];
    /**
     * Minimum number of different products that must be selected.
     * @max 100
     */
    minItems?: number | null;
    /**
     * Maximum number of different products that can be selected.
     * @max 100
     */
    maxItems?: number | null;
}
declare enum ProductType {
    /** Physical product that can be shipped. */
    SHIPPABLE = "SHIPPABLE",
    /** Digital product. */
    DIGITAL = "DIGITAL"
}
/** @enumType */
type ProductTypeWithLiterals = ProductType | 'SHIPPABLE' | 'DIGITAL';
declare enum PriceType {
    /** Product has a single, unchanging price. */
    FIXED_PRICE = "FIXED_PRICE",
    /** Product price can vary within a specified range. */
    DYNAMIC_PRICE = "DYNAMIC_PRICE"
}
/** @enumType */
type PriceTypeWithLiterals = PriceType | 'FIXED_PRICE' | 'DYNAMIC_PRICE';
interface QuantityLimit {
    /**
     * Minimum quantity that must be selected for this product.
     * @min 1
     * @max 100000
     */
    minimum?: number | null;
    /**
     * Maximum quantity that can be selected for this product.
     * @min 1
     * @max 100000
     */
    maximum?: number | null;
}
interface FixedPriceOptions {
    /**
     * Fixed price of the product.
     *
     * Specified as a decimal string with period as decimal separator. For example, `"3.99"`.
     * @decimalValue options { gte:0.00, maxScale:2 }
     */
    price?: string;
}
interface DynamicPriceOptions {
    /**
     * Minimum price of the product.
     *
     * Specified as a decimal string with period as decimal separator. For example, `"1.00"`.
     * @decimalValue options { gte:0.00, maxScale:2 }
     */
    minPrice?: string;
    /**
     * Maximum monetary price of the product.
     *
     * Specified as a decimal string with period as decimal separator. For example, `"10.00"`.
     *
     * If not specified, there is no upper limit on the price.
     * @decimalValue options { gte:0.00, maxScale:2 }
     */
    maxPrice?: string | null;
}
interface Product extends ProductPriceOptionsOneOf {
    /** Configuration for products with fixed pricing. */
    fixedPriceOptions?: FixedPriceOptions;
    /** Configuration for products with variable pricing within a range. */
    dynamicPriceOptions?: DynamicPriceOptions;
    /**
     * Product ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /** Product type. */
    productType?: ProductTypeWithLiterals;
    /** Price type. */
    priceType?: PriceTypeWithLiterals;
    /** Limits on how many units of this product can be selected. */
    quantityLimit?: QuantityLimit;
}
/** @oneof */
interface ProductPriceOptionsOneOf {
    /** Configuration for products with fixed pricing. */
    fixedPriceOptions?: FixedPriceOptions;
    /** Configuration for products with variable pricing within a range. */
    dynamicPriceOptions?: DynamicPriceOptions;
}
interface MultilineAddressValidation {
    /**
     * Countries from which addresses are accepted.
     * If empty, addresses from all countries are allowed.
     * @format COUNTRY
     * @maxSize 200
     */
    allowedCountries?: string[];
    /** Customization configuration for individual address field components. */
    fields?: FieldsOverrides;
}
interface FieldOverrides {
    /** Whether the field is required. */
    required?: boolean;
}
interface FieldsOverrides {
    /** Subdivision settings. */
    subdivision?: FieldOverrides;
    /** City settings. */
    city?: FieldOverrides;
    /** Postal code settings. */
    postalCode?: FieldOverrides;
    /** Street name settings. */
    streetName?: FieldOverrides;
    /** Street number settings. */
    streetNumber?: FieldOverrides;
    /** Address line settings. */
    addressLine?: FieldOverrides;
    /** Address line 2 settings. */
    addressLine2?: FieldOverrides;
    /** Country settings. */
    country?: FieldOverrides;
}
interface ObjectArrayType {
    /**
     * Maximum number of elements allowed in the array.
     * @max 1000
     */
    maxItems?: number | null;
    /**
     * Minimum number of elements required in the array.
     * @max 1000
     */
    minItems?: number | null;
}
interface NestedFormFieldOverrides {
    /** Whether the field is required. Leave blank for no override. */
    required?: boolean | null;
    /** Whether the field is hidden. Leave blank for no override. */
    hidden?: boolean | null;
}
interface Validation extends ValidationValidationOneOf {
    /** Validation of string type. */
    string?: FormFieldStringType;
    /** Validation of number type. */
    number?: FormFieldNumberType;
    /** Validation of integer type. */
    integer?: IntegerType;
    /** Validation of boolean type. */
    boolean?: FormFieldBooleanType;
    /** Validation of array type. */
    array?: FormFieldArrayType;
    /** Validation of object type. */
    object?: FormFieldObjectType;
    /** Predefined validation of specific format */
    predefined?: PredefinedValidation;
    /** Whether the field is required. */
    required?: boolean;
}
/** @oneof */
interface ValidationValidationOneOf {
    /** Validation of string type. */
    string?: FormFieldStringType;
    /** Validation of number type. */
    number?: FormFieldNumberType;
    /** Validation of integer type. */
    integer?: IntegerType;
    /** Validation of boolean type. */
    boolean?: FormFieldBooleanType;
    /** Validation of array type. */
    array?: FormFieldArrayType;
    /** Validation of object type. */
    object?: FormFieldObjectType;
    /** Predefined validation of specific format */
    predefined?: PredefinedValidation;
}
interface DataExtensionsDetails {
    /**
     * FQDNS which can be extended with this field
     * @maxSize 10
     * @maxLength 100
     * @immutable
     */
    fqdns?: string[];
}
interface NestedFormOverrides {
    /** Field overrides by field ID */
    fieldOverrides?: Record<string, NestedFormFieldOverrides>;
}
interface Field extends FieldFieldTypeOptionsOneOf {
    /** Configuration for input fields that collect user data. */
    inputOptions?: InputField;
    /** Configuration for display fields that show information without collecting input. */
    displayOptions?: DisplayField;
    /**
     * Field ID.
     *
     * The ID is stored in lowercase. Because the form's layout references each field by this ID, pass a lowercase GUID so that `id` and the layout's `fieldId` still match after the form schema is stored. IDs must be unique in the form.
     * @minLength 1
     * @format GUID
     * @immutable
     */
    _id?: string;
    /**
     * Whether the field is hidden from submitters.
     *
     * Default: `false`
     */
    hidden?: boolean;
    /**
     * Identification for the field, describing its purpose. For example, `TEXT_INPUT` or `CONTACTS_EMAIL`.
     *
     * Set this to the known value for the kind of field you're creating.
     * Wix accepts an unrecognized value, such as a custom string or a GUID: the request succeeds, and Wix stores the field, returns it in `formFields`, and accepts its submissions. But Wix apps use `identifier` to recognize a field, so the Wix Forms editor doesn't render it, and the Wix user can't see or edit that field. A form whose fields all carry unrecognized identifiers opens empty in the editor. Always map a field onto a known identifier, and set its display name with the component's label.
     *
     * Several fields share a general-purpose `componentType`, so `identifier` is often the only value that distinguishes them.
     * For example, both a short answer field and a long answer field use the `TEXT_INPUT` component, and are told apart by their `TEXT_INPUT` and `TEXT_AREA` identifiers.
     *
     * For the full list of known values, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).
     * @maxLength 50
     */
    identifier?: string | null;
    /**
     * Field type.
     *
     * `INPUT` fields collect data from submitters and are configured in `inputOptions`.
     * `DISPLAY` fields show information without collecting data and are configured in `displayOptions`.
     */
    fieldType?: FieldTypeWithLiterals;
}
/** @oneof */
interface FieldFieldTypeOptionsOneOf {
    /** Configuration for input fields that collect user data. */
    inputOptions?: InputField;
    /** Configuration for display fields that show information without collecting input. */
    displayOptions?: DisplayField;
}
declare enum FieldType {
    /** Unknown field type. */
    UNKNOWN_FIELD_TYPE = "UNKNOWN_FIELD_TYPE",
    /** Input field that collects data from users. */
    INPUT = "INPUT",
    /** Display field that shows information without collecting input. */
    DISPLAY = "DISPLAY"
}
/** @enumType */
type FieldTypeWithLiterals = FieldType | 'UNKNOWN_FIELD_TYPE' | 'INPUT' | 'DISPLAY';
interface InputField extends InputFieldInputTypeOptionsOneOf {
    /** String input field settings. */
    stringOptions?: _String;
    /** Number input field settings. */
    numberOptions?: _Number;
    /** Boolean input field settings. */
    booleanOptions?: _Boolean;
    /** Array input field settings. */
    arrayOptions?: _Array;
    /** Object input field settings. */
    objectOptions?: _Object;
    /**
     * File input field settings. Files are uploaded to the [Media Manager](https://support.wix.com/en/article/wix-media-about-the-media-manager).
     *
     * > **Note:** File upload and signature fields are premium features. The site must have a Core premium plan or higher. Creating a form schema with one of these fields on a site without a qualifying plan fails.
     */
    wixFileOptions?: WixFile;
    /**
     * Payment input field settings.
     *
     * > **Note:** Payment fields are premium features that also depend on another app. The site must have a Core premium plan or higher, and the Wix eCommerce app must be installed. Creating a form schema with a payment field on a site that doesn't meet both requirements fails.
     */
    paymentOptions?: Payment;
    /**
     * Scheduling input field settings.
     *
     * > **Note:** Scheduling fields require the Wix Meetings app to be installed on the site. Creating a form schema with a scheduling field on a site without the app fails.
     */
    schedulingOptions?: Scheduling;
    /** Address input field settings. */
    addressOptions?: Address;
    /**
     * Human readable identifier used to reference a field, used as the key
     * under which the field's value is stored in a submission. For example, `"address"`.
     *
     * Required for input fields. Must be unique within the form (comparison is case-sensitive).
     *
     * Naming rules:
     * - Must start with an ASCII letter (`a`-`z` or `A`-`Z`).
     * - May contain ASCII letters, digits, and underscores (`_`). No spaces, hyphens, or non-ASCII characters.
     * - Consecutive underscores (`__`) are not allowed.
     * - Casing is not enforced, so `camelCase`, `snake_case`, and `PascalCase` are all valid.
     *
     * Can be set once.
     * @minLength 1
     * @maxLength 200
     * @immutable
     */
    target?: string;
    /**
     * Whether this field contains Personally Identifiable Information (PII).
     *
     * PII fields are automatically encrypted when stored.
     *
     * Default: `false`
     */
    pii?: boolean;
    /**
     * Whether the field is required for form submission.
     *
     * Set this on `inputOptions`, alongside `target` and `inputType`. A `required` placed inside a validation object, such as `stringOptions.validation`, isn't part of the schema and has no effect.
     *
     * Default: `false`
     */
    required?: boolean;
    /**
     * Type of the input field that determines what kind of data it collects.
     *
     * `inputType` selects which options object applies. For example, `STRING` is configured in `stringOptions`, and `PAYMENT` in `paymentOptions`.
     * That options object then sets a `componentType`, which selects the UI component and its own nested settings object.
     *
     * For which combinations produce which field, see [About Form Fields](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/about-form-fields).
     */
    inputType?: InputTypeWithLiterals;
    /**
     * Mapping configuration for automatically saving field values to contact properties.
     *
     * When specified, form submissions automatically create or update contacts with the field data.
     */
    contactMapping?: FormFieldContactInfo;
    /**
     * Whether the field is read-only.
     *
     * Default: `false`
     */
    readOnly?: boolean;
}
/** @oneof */
interface InputFieldInputTypeOptionsOneOf {
    /** String input field settings. */
    stringOptions?: _String;
    /** Number input field settings. */
    numberOptions?: _Number;
    /** Boolean input field settings. */
    booleanOptions?: _Boolean;
    /** Array input field settings. */
    arrayOptions?: _Array;
    /** Object input field settings. */
    objectOptions?: _Object;
    /**
     * File input field settings. Files are uploaded to the [Media Manager](https://support.wix.com/en/article/wix-media-about-the-media-manager).
     *
     * > **Note:** File upload and signature fields are premium features. The site must have a Core premium plan or higher. Creating a form schema with one of these fields on a site without a qualifying plan fails.
     */
    wixFileOptions?: WixFile;
    /**
     * Payment input field settings.
     *
     * > **Note:** Payment fields are premium features that also depend on another app. The site must have a Core premium plan or higher, and the Wix eCommerce app must be installed. Creating a form schema with a payment field on a site that doesn't meet both requirements fails.
     */
    paymentOptions?: Payment;
    /**
     * Scheduling input field settings.
     *
     * > **Note:** Scheduling fields require the Wix Meetings app to be installed on the site. Creating a form schema with a scheduling field on a site without the app fails.
     */
    schedulingOptions?: Scheduling;
    /** Address input field settings. */
    addressOptions?: Address;
}
interface StringCorrectAnswersList {
    /**
     * @minLength 1
     * @maxLength 150
     * @maxSize 50
     */
    correctAnswers?: string[];
}
interface StringType extends StringTypeFormatOptionsOneOf {
    /** Validation rules for strings with date format. */
    dateOptions?: DateTimeAdvancedConstraints;
    /** Validation rules for strings with date and time format. */
    dateTimeOptions?: DateTimeConstraints;
    /** Validation rules for strings with time format. */
    timeOptions?: DateTimeConstraints;
    /** Validation rules for string with date and time format, where time is optional. */
    dateOptionalTimeOptions?: DateTimeConstraints;
    /** Validation rules for strings with phone number format. */
    phoneOptions?: PhoneConstraints;
    /**
     * Minimum required length for the string value.
     * @max 20000
     */
    minLength?: number | null;
    /**
     * Maximum allowed length for the string value.
     * @max 20000
     */
    maxLength?: number | null;
    /**
     * Regular expression pattern that the string value must match.
     * @maxLength 500
     */
    pattern?: string | null;
    /** Expected format of the string value. */
    format?: FormatEnumFormatWithLiterals;
    /**
     * List of specific values that are allowed for this field.
     * When specified, the input must match one of these predefined values.
     * @maxSize 500
     * @maxLength 20000
     */
    enum?: string[] | null;
    /** Custom error messages displayed when validation fails. */
    validationMessages?: ValidationMessages;
}
/** @oneof */
interface StringTypeFormatOptionsOneOf {
    /** Validation rules for strings with date format. */
    dateOptions?: DateTimeAdvancedConstraints;
    /** Validation rules for strings with date and time format. */
    dateTimeOptions?: DateTimeConstraints;
    /** Validation rules for strings with time format. */
    timeOptions?: DateTimeConstraints;
    /** Validation rules for string with date and time format, where time is optional. */
    dateOptionalTimeOptions?: DateTimeConstraints;
    /** Validation rules for strings with phone number format. */
    phoneOptions?: PhoneConstraints;
}
declare enum FormatEnumFormat {
    /** No format constraint. This is the default, and the value a field reads back with when it doesn't restrict its format. */
    UNKNOWN_FORMAT = "UNKNOWN_FORMAT",
    /** Date format. */
    DATE = "DATE",
    /** Time format. */
    TIME = "TIME",
    /** Date and time format. */
    DATE_TIME = "DATE_TIME",
    /** Email format. */
    EMAIL = "EMAIL",
    /** URL format. */
    URL = "URL",
    /** UUID format. */
    UUID = "UUID",
    /** Phone number format. */
    PHONE = "PHONE",
    /** URI format. */
    URI = "URI",
    /** Hostname format. */
    HOSTNAME = "HOSTNAME",
    /** Hexadecimal color code format. */
    COLOR_HEX = "COLOR_HEX",
    /** Currency format. */
    CURRENCY = "CURRENCY",
    /** Language code format. */
    LANGUAGE = "LANGUAGE",
    /** Date with optional time format. */
    DATE_OPTIONAL_TIME = "DATE_OPTIONAL_TIME"
}
/** @enumType */
type FormatEnumFormatWithLiterals = FormatEnumFormat | 'UNKNOWN_FORMAT' | 'DATE' | 'TIME' | 'DATE_TIME' | 'EMAIL' | 'URL' | 'UUID' | 'PHONE' | 'URI' | 'HOSTNAME' | 'COLOR_HEX' | 'CURRENCY' | 'LANGUAGE' | 'DATE_OPTIONAL_TIME';
interface DateTimeConstraints {
    /**
     * Minimum allowed datetime value.
     *
     * Supports ISO datetime format or dynamic calculations using "$now" keyword.
     * The dynamic calculation supports times in the future and past.
     * The pattern for dynamic calculations: $now([+-]\d{1,2})([yMdmh]).
     *
     * Examples: `"2023-01-01"`, `"$now-1d"` (1 day ago), `"$now+2h"` (2 hours from now).
     * @maxLength 50
     */
    minimum?: string | null;
    /**
     * Maximum allowed datetime value.
     *
     * Supports ISO datetime format or dynamic calculations using "$now" keyword.
     * The dynamic calculation supports times in the future and past.
     * The pattern for dynamic calculations: $now([+-]\d{1,2})([yMdmh]).
     *
     * Examples: `"2023-01-01"`, `"$now-1d"` (1 day ago), `"$now+2h"` (2 hours from now).
     * @maxLength 50
     */
    maximum?: string | null;
}
interface PhoneConstraints {
    /**
     * Country codes that are allowed for phone number validation.
     *
     * Use ISO 3166-1 alpha-2 country codes. For example, `"US"`, `"GB"`, `"CA"`.
     * @maxSize 250
     * @maxLength 2
     */
    allowedCountryCodes?: string[];
}
interface ValidationMessages {
    /**
     * Error message shown when the pattern validation fails.
     *
     * This message is displayed to users when their input doesn't match the specified regex pattern.
     * @maxLength 200
     */
    pattern?: string | null;
}
declare enum StringComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Text input component. */
    TEXT_INPUT = "TEXT_INPUT",
    /** Radio group component. */
    RADIO_GROUP = "RADIO_GROUP",
    /** Dropdown component. */
    DROPDOWN = "DROPDOWN",
    /** Date and time component. */
    DATE_TIME = "DATE_TIME",
    /** Phone number component. */
    PHONE_INPUT = "PHONE_INPUT",
    /** Date selection component. */
    DATE_INPUT = "DATE_INPUT",
    /** Time selection component. */
    TIME_INPUT = "TIME_INPUT",
    /** Calendar-style date picker component. */
    DATE_PICKER = "DATE_PICKER",
    /** Dropdown component for selecting available services. */
    SERVICES_DROPDOWN = "SERVICES_DROPDOWN",
    /** Component which obscures input characters. */
    PASSWORD = "PASSWORD"
}
/** @enumType */
type StringComponentTypeWithLiterals = StringComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'TEXT_INPUT' | 'RADIO_GROUP' | 'DROPDOWN' | 'DATE_TIME' | 'PHONE_INPUT' | 'DATE_INPUT' | 'TIME_INPUT' | 'DATE_PICKER' | 'SERVICES_DROPDOWN' | 'PASSWORD';
/**
 * Quiz scoring for a single question — how many points a correct answer is worth,
 * the correct answer(s), and the messages shown for a correct or incorrect answer.
 */
interface StringQuizFieldSettings {
    /** Points awarded for correctly answering this quiz question. */
    score?: number;
    /**
     * Correct answer(s) for this quiz question. Returned only to callers with the
     * `WIX_FORMS.QUIZ_ANSWERS_READ` permission, so answers stay hidden from respondents.
     */
    correctAnswersList?: StringCorrectAnswersList;
    /**
     * Message shown to the respondent when the answer is correct.
     * @maxLength 250
     */
    correctAnswerMessage?: string | null;
    /**
     * Message shown to the respondent when the answer is incorrect.
     * @maxLength 250
     */
    incorrectAnswerMessage?: string | null;
}
interface TextInput extends TextInputSemanticOptionsOneOf {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Placeholder text shown inside the field when it's empty.
     * Useful for providing a hint about the expected format or content.
     * @maxLength 100
     */
    placeholder?: string | null;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Default value for the field. This value is pre-populated in the field when the form loads.
     * @maxLength 20000
     */
    default?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
/** @oneof */
interface TextInputSemanticOptionsOneOf {
}
interface RichContent {
    /** Node objects representing a rich content document. */
    nodes?: Node[];
    /** Object metadata. */
    metadata?: Metadata;
    /** Global styling for header, paragraph, block quote, and code block nodes in the object. */
    documentStyle?: DocumentStyle;
}
interface Node extends NodeDataOneOf {
    /** Data for a button node. */
    buttonData?: ButtonData;
    /** Data for a code block node. */
    codeBlockData?: CodeBlockData;
    /** Data for a divider node. */
    dividerData?: DividerData;
    /** Data for a file node. */
    fileData?: FileData;
    /** Data for a gallery node. */
    galleryData?: GalleryData;
    /** Data for a GIF node. */
    gifData?: GIFData;
    /** Data for a heading node. */
    headingData?: HeadingData;
    /** Data for an embedded HTML node. */
    htmlData?: HTMLData;
    /** Data for an image node. */
    imageData?: ImageData;
    /** Data for a link preview node. */
    linkPreviewData?: LinkPreviewData;
    /** @deprecated */
    mapData?: MapData;
    /** Data for a paragraph node. */
    paragraphData?: ParagraphData;
    /** Data for a poll node. */
    pollData?: PollData;
    /** Data for a text node. Used to apply decorations to text. */
    textData?: TextData;
    /** Data for an app embed node. */
    appEmbedData?: AppEmbedData;
    /** Data for a video node. */
    videoData?: VideoData;
    /** Data for an oEmbed node. */
    embedData?: EmbedData;
    /** Data for a collapsible list node. */
    collapsibleListData?: CollapsibleListData;
    /** Data for a table node. */
    tableData?: TableData;
    /** Data for a table cell node. */
    tableCellData?: TableCellData;
    /** Data for a custom external node. */
    externalData?: Record<string, any> | null;
    /** Data for an audio node. */
    audioData?: AudioData;
    /** Data for an ordered list node. */
    orderedListData?: OrderedListData;
    /** Data for a bulleted list node. */
    bulletedListData?: BulletedListData;
    /** Data for a block quote node. */
    blockquoteData?: BlockquoteData;
    /** Data for a caption node. */
    captionData?: CaptionData;
    /** Data for a layout node. Reserved for future use. */
    layoutData?: LayoutData;
    /** Data for a cell node. */
    layoutCellData?: LayoutCellData;
    /** Data for a shape node. */
    shapeData?: ShapeData;
    /** Data for a card node. */
    cardData?: CardData;
    /** Data for a table of contents node. */
    tocData?: TocData;
    /** Data for a smart block node. */
    smartBlockData?: SmartBlockData;
    /** Data for a smart block cell node. */
    smartBlockCellData?: SmartBlockCellData;
    /** Data for a checkbox list node. */
    checkboxListData?: CheckboxListData;
    /** Data for a list item node. */
    listItemData?: ListItemNodeData;
    /** Node type. Use `APP_EMBED` for nodes that embed content from other Wix apps. Use `EMBED` to embed content in [oEmbed](https://oembed.com/) format. */
    type?: NodeTypeWithLiterals;
    /** Node ID. */
    id?: string;
    /** A list of child nodes. */
    nodes?: Node[];
    /** Padding and background color styling for the node. */
    style?: NodeStyle;
}
/** @oneof */
interface NodeDataOneOf {
    /** Data for a button node. */
    buttonData?: ButtonData;
    /** Data for a code block node. */
    codeBlockData?: CodeBlockData;
    /** Data for a divider node. */
    dividerData?: DividerData;
    /** Data for a file node. */
    fileData?: FileData;
    /** Data for a gallery node. */
    galleryData?: GalleryData;
    /** Data for a GIF node. */
    gifData?: GIFData;
    /** Data for a heading node. */
    headingData?: HeadingData;
    /** Data for an embedded HTML node. */
    htmlData?: HTMLData;
    /** Data for an image node. */
    imageData?: ImageData;
    /** Data for a link preview node. */
    linkPreviewData?: LinkPreviewData;
    /** @deprecated */
    mapData?: MapData;
    /** Data for a paragraph node. */
    paragraphData?: ParagraphData;
    /** Data for a poll node. */
    pollData?: PollData;
    /** Data for a text node. Used to apply decorations to text. */
    textData?: TextData;
    /** Data for an app embed node. */
    appEmbedData?: AppEmbedData;
    /** Data for a video node. */
    videoData?: VideoData;
    /** Data for an oEmbed node. */
    embedData?: EmbedData;
    /** Data for a collapsible list node. */
    collapsibleListData?: CollapsibleListData;
    /** Data for a table node. */
    tableData?: TableData;
    /** Data for a table cell node. */
    tableCellData?: TableCellData;
    /** Data for a custom external node. */
    externalData?: Record<string, any> | null;
    /** Data for an audio node. */
    audioData?: AudioData;
    /** Data for an ordered list node. */
    orderedListData?: OrderedListData;
    /** Data for a bulleted list node. */
    bulletedListData?: BulletedListData;
    /** Data for a block quote node. */
    blockquoteData?: BlockquoteData;
    /** Data for a caption node. */
    captionData?: CaptionData;
    /** Data for a layout node. Reserved for future use. */
    layoutData?: LayoutData;
    /** Data for a cell node. */
    layoutCellData?: LayoutCellData;
    /** Data for a shape node. */
    shapeData?: ShapeData;
    /** Data for a card node. */
    cardData?: CardData;
    /** Data for a table of contents node. */
    tocData?: TocData;
    /** Data for a smart block node. */
    smartBlockData?: SmartBlockData;
    /** Data for a smart block cell node. */
    smartBlockCellData?: SmartBlockCellData;
    /** Data for a checkbox list node. */
    checkboxListData?: CheckboxListData;
    /** Data for a list item node. */
    listItemData?: ListItemNodeData;
}
declare enum NodeType {
    PARAGRAPH = "PARAGRAPH",
    TEXT = "TEXT",
    HEADING = "HEADING",
    BULLETED_LIST = "BULLETED_LIST",
    ORDERED_LIST = "ORDERED_LIST",
    LIST_ITEM = "LIST_ITEM",
    BLOCKQUOTE = "BLOCKQUOTE",
    CODE_BLOCK = "CODE_BLOCK",
    VIDEO = "VIDEO",
    DIVIDER = "DIVIDER",
    FILE = "FILE",
    GALLERY = "GALLERY",
    GIF = "GIF",
    HTML = "HTML",
    IMAGE = "IMAGE",
    LINK_PREVIEW = "LINK_PREVIEW",
    /** @deprecated */
    MAP = "MAP",
    POLL = "POLL",
    APP_EMBED = "APP_EMBED",
    BUTTON = "BUTTON",
    COLLAPSIBLE_LIST = "COLLAPSIBLE_LIST",
    TABLE = "TABLE",
    EMBED = "EMBED",
    COLLAPSIBLE_ITEM = "COLLAPSIBLE_ITEM",
    COLLAPSIBLE_ITEM_TITLE = "COLLAPSIBLE_ITEM_TITLE",
    COLLAPSIBLE_ITEM_BODY = "COLLAPSIBLE_ITEM_BODY",
    TABLE_CELL = "TABLE_CELL",
    TABLE_ROW = "TABLE_ROW",
    EXTERNAL = "EXTERNAL",
    AUDIO = "AUDIO",
    CAPTION = "CAPTION",
    LAYOUT = "LAYOUT",
    LAYOUT_CELL = "LAYOUT_CELL",
    SHAPE = "SHAPE",
    CARD = "CARD",
    TOC = "TOC",
    SMART_BLOCK = "SMART_BLOCK",
    SMART_BLOCK_CELL = "SMART_BLOCK_CELL",
    CHECKBOX_LIST = "CHECKBOX_LIST"
}
/** @enumType */
type NodeTypeWithLiterals = NodeType | 'PARAGRAPH' | 'TEXT' | 'HEADING' | 'BULLETED_LIST' | 'ORDERED_LIST' | 'LIST_ITEM' | 'BLOCKQUOTE' | 'CODE_BLOCK' | 'VIDEO' | 'DIVIDER' | 'FILE' | 'GALLERY' | 'GIF' | 'HTML' | 'IMAGE' | 'LINK_PREVIEW' | 'MAP' | 'POLL' | 'APP_EMBED' | 'BUTTON' | 'COLLAPSIBLE_LIST' | 'TABLE' | 'EMBED' | 'COLLAPSIBLE_ITEM' | 'COLLAPSIBLE_ITEM_TITLE' | 'COLLAPSIBLE_ITEM_BODY' | 'TABLE_CELL' | 'TABLE_ROW' | 'EXTERNAL' | 'AUDIO' | 'CAPTION' | 'LAYOUT' | 'LAYOUT_CELL' | 'SHAPE' | 'CARD' | 'TOC' | 'SMART_BLOCK' | 'SMART_BLOCK_CELL' | 'CHECKBOX_LIST';
interface NodeStyle {
    /** The top padding value in pixels. */
    paddingTop?: string | null;
    /** The bottom padding value in pixels. */
    paddingBottom?: string | null;
    /** The background color as a hexadecimal value. */
    backgroundColor?: string | null;
}
interface ButtonData {
    /** Styling for the button's container. */
    containerData?: PluginContainerData;
    /** The button type. */
    type?: ButtonDataTypeWithLiterals;
    /** Styling for the button. */
    styles?: Styles;
    /** The text to display on the button. */
    text?: string | null;
    /** Button link details. */
    link?: Link;
    /** Node animation. */
    animation?: Animation;
}
/** Background type */
declare enum BackgroundType {
    /** Solid color background */
    COLOR = "COLOR",
    /** Gradient background */
    GRADIENT = "GRADIENT"
}
/** @enumType */
type BackgroundTypeWithLiterals = BackgroundType | 'COLOR' | 'GRADIENT';
interface Gradient {
    /** Gradient type. */
    type?: GradientTypeWithLiterals;
    /**
     * Color stops for the gradient.
     * @maxSize 1000
     */
    stops?: Stop[];
    /** Angle in degrees for linear gradient (0-360). */
    angle?: number | null;
    /**
     * Horizontal center position for radial gradient (0-100).
     * @max 100
     */
    centerX?: number | null;
    /**
     * Vertical center position for radial gradient (0-100).
     * @max 100
     */
    centerY?: number | null;
}
/** Gradient type. */
declare enum GradientType {
    /** Linear gradient. */
    LINEAR = "LINEAR",
    /** Radial gradient. */
    RADIAL = "RADIAL"
}
/** @enumType */
type GradientTypeWithLiterals = GradientType | 'LINEAR' | 'RADIAL';
/** A single color stop in the gradient. */
interface Stop {
    /**
     * Stop color as hex value.
     * @maxLength 19
     */
    color?: string | null;
    /** Stop position (0-1). */
    position?: number | null;
}
interface Border {
    /**
     * Deprecated: Use `borderWidth` in `styles` instead.
     * @deprecated
     */
    width?: number | null;
    /**
     * Deprecated: Use `borderRadius` in `styles` instead.
     * @deprecated
     */
    radius?: number | null;
}
interface Colors {
    /**
     * Deprecated: Use `textColor` in `styles` instead.
     * @deprecated
     */
    text?: string | null;
    /**
     * Deprecated: Use `borderColor` in `styles` instead.
     * @deprecated
     */
    border?: string | null;
    /**
     * Deprecated: Use `backgroundColor` in `styles` instead.
     * @deprecated
     */
    background?: string | null;
}
/** Background styling (color or gradient) */
interface Background {
    /** Background type. */
    type?: BackgroundTypeWithLiterals;
    /**
     * Background color as a hexadecimal value.
     * @maxLength 19
     */
    color?: string | null;
    /** Gradient configuration. */
    gradient?: Gradient;
}
interface PluginContainerData {
    /** The width of the node when it's displayed. */
    width?: PluginContainerDataWidth;
    /** The node's alignment within its container. */
    alignment?: PluginContainerDataAlignmentWithLiterals;
    /** Spoiler cover settings for the node. */
    spoiler?: Spoiler;
    /** The height of the node when it's displayed. */
    height?: Height;
    /** Sets whether text should wrap around this node when it's displayed. If `textWrap` is `false`, the node takes up the width of its container. Defaults to `true` for all node types except 'DIVIVDER' where it defaults to `false`. */
    textWrap?: boolean | null;
}
declare enum WidthType {
    /** Width matches the content width */
    CONTENT = "CONTENT",
    /** Small Width */
    SMALL = "SMALL",
    /** Width will match the original asset width */
    ORIGINAL = "ORIGINAL",
    /** coast-to-coast display */
    FULL_WIDTH = "FULL_WIDTH"
}
/** @enumType */
type WidthTypeWithLiterals = WidthType | 'CONTENT' | 'SMALL' | 'ORIGINAL' | 'FULL_WIDTH';
interface PluginContainerDataWidth extends PluginContainerDataWidthDataOneOf {
    /**
     * One of the following predefined width options:
     * `CONTENT`: The width of the container matches the content width.
     * `SMALL`: A small width.
     * `ORIGINAL`: For `imageData` containers only. The width of the container matches the original image width.
     * `FULL_WIDTH`: For `imageData` containers only. The image container takes up the full width of the screen.
     */
    size?: WidthTypeWithLiterals;
    /** A custom width value in pixels. */
    custom?: string | null;
}
/** @oneof */
interface PluginContainerDataWidthDataOneOf {
    /**
     * One of the following predefined width options:
     * `CONTENT`: The width of the container matches the content width.
     * `SMALL`: A small width.
     * `ORIGINAL`: For `imageData` containers only. The width of the container matches the original image width.
     * `FULL_WIDTH`: For `imageData` containers only. The image container takes up the full width of the screen.
     */
    size?: WidthTypeWithLiterals;
    /** A custom width value in pixels. */
    custom?: string | null;
}
declare enum PluginContainerDataAlignment {
    /** Center Alignment */
    CENTER = "CENTER",
    /** Left Alignment */
    LEFT = "LEFT",
    /** Right Alignment */
    RIGHT = "RIGHT"
}
/** @enumType */
type PluginContainerDataAlignmentWithLiterals = PluginContainerDataAlignment | 'CENTER' | 'LEFT' | 'RIGHT';
interface Spoiler {
    /** Sets whether the spoiler cover is enabled for this node. Defaults to `false`. */
    enabled?: boolean | null;
    /** The description displayed on top of the spoiler cover. */
    description?: string | null;
    /** The text for the button used to remove the spoiler cover. */
    buttonText?: string | null;
}
interface Height {
    /** A custom height value in pixels. */
    custom?: string | null;
}
declare enum ButtonDataType {
    /** Regular link button */
    LINK = "LINK",
    /** Triggers custom action that is defined in plugin configuration by the consumer */
    ACTION = "ACTION"
}
/** @enumType */
type ButtonDataTypeWithLiterals = ButtonDataType | 'LINK' | 'ACTION';
interface Styles {
    /**
     * Deprecated: Use `borderWidth` and `borderRadius` instead.
     * @deprecated
     */
    border?: Border;
    /**
     * Deprecated: Use `textColor`, `borderColor` and `backgroundColor` instead.
     * @deprecated
     */
    colors?: Colors;
    /** Border width in pixels. */
    borderWidth?: number | null;
    /**
     * Deprecated: Use `borderWidth` for normal/hover states instead.
     * @deprecated
     */
    borderWidthHover?: number | null;
    /** Border radius in pixels. */
    borderRadius?: number | null;
    /**
     * Border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /**
     * Border color as a hexadecimal value (hover state).
     * @maxLength 19
     */
    borderColorHover?: string | null;
    /**
     * Text color as a hexadecimal value.
     * @maxLength 19
     */
    textColor?: string | null;
    /**
     * Text color as a hexadecimal value (hover state).
     * @maxLength 19
     */
    textColorHover?: string | null;
    /**
     * Deprecated: Use `background` instead.
     * @maxLength 19
     * @deprecated
     */
    backgroundColor?: string | null;
    /**
     * Deprecated: Use `backgroundHover` instead.
     * @maxLength 19
     * @deprecated
     */
    backgroundColorHover?: string | null;
    /** Button size option, one of `SMALL`, `MEDIUM` or `LARGE`. Defaults to `MEDIUM`. */
    buttonSize?: string | null;
    /** Background styling (color or gradient). */
    background?: Background;
    /** Background styling for hover state (color or gradient). */
    backgroundHover?: Background;
}
interface Link extends LinkDataOneOf {
    /** The absolute URL for the linked document. */
    url?: string;
    /** The target node's ID. Used for linking to another node in this object. */
    anchor?: string;
    /**
     * he HTML `target` attribute value for the link. This property defines where the linked document opens as follows:
     * `SELF` - Default. Opens the linked document in the same frame as the link.
     * `BLANK` - Opens the linked document in a new browser tab or window.
     * `PARENT` - Opens the linked document in the link's parent frame.
     * `TOP` - Opens the linked document in the full body of the link's browser tab or window.
     */
    target?: LinkTargetWithLiterals;
    /** The HTML `rel` attribute value for the link. This object specifies the relationship between the current document and the linked document. */
    rel?: Rel;
    /** A serialized object used for a custom or external link panel. */
    customData?: string | null;
}
/** @oneof */
interface LinkDataOneOf {
    /** The absolute URL for the linked document. */
    url?: string;
    /** The target node's ID. Used for linking to another node in this object. */
    anchor?: string;
}
declare enum LinkTarget {
    /** Opens the linked document in the same frame as it was clicked (this is default) */
    SELF = "SELF",
    /** Opens the linked document in a new window or tab */
    BLANK = "BLANK",
    /** Opens the linked document in the parent frame */
    PARENT = "PARENT",
    /** Opens the linked document in the full body of the window */
    TOP = "TOP"
}
/** @enumType */
type LinkTargetWithLiterals = LinkTarget | 'SELF' | 'BLANK' | 'PARENT' | 'TOP';
interface Rel {
    /** Indicates to search engine crawlers not to follow the link. Defaults to `false`. */
    nofollow?: boolean | null;
    /** Indicates to search engine crawlers that the link is a paid placement such as sponsored content or an advertisement. Defaults to `false`. */
    sponsored?: boolean | null;
    /** Indicates that this link is user-generated content and isn't necessarily trusted or endorsed by the page’s author. For example, a link in a fourm post. Defaults to `false`. */
    ugc?: boolean | null;
    /** Indicates that this link protect referral information from being passed to the target website. */
    noreferrer?: boolean | null;
}
interface Animation {
    /** Animation kind. */
    type?: AnimationTypeWithLiterals;
    /** Entrance animation, played once when the node first enters the viewport (VIEW). */
    entrance?: EntranceAnimation;
    /** Looping animation, repeated indefinitely while the node is on the page. When combined with entrance, the loop starts after the entrance finishes (VIEW). */
    loop?: LoopAnimation;
    /**
     * POINTER carries its fields directly on the union member in the JTD wall, hence
     * flat fields here (ts-proto camelCase must equal the JTD property names). A future
     * kind that also needs an `effect` field would contend for this JSON name.
     */
    effect?: PointerEffect;
    /** How quickly the node settles toward the pointer, in milliseconds (POINTER). */
    transitionDuration?: number | null;
}
declare enum AnimationType {
    /** Animations that play while the node is in view. */
    VIEW = "VIEW",
    /** Animations driven by the visitor's mouse position. */
    POINTER = "POINTER"
}
/** @enumType */
type AnimationTypeWithLiterals = AnimationType | 'VIEW' | 'POINTER';
interface EntranceAnimation {
    /** The entrance effect to play. */
    effect?: EntranceEffect;
    /** Effect duration in milliseconds. Each effect has its own default. */
    duration?: number | null;
    /** Delay before the effect starts, in milliseconds. */
    delay?: number | null;
}
interface EntranceEffect {
    /** The entrance effect type. */
    type?: EntranceEffectTypeWithLiterals;
    /**
     * Direction the effect enters from. Allowed values depend on the effect type (e.g. TOP/RIGHT/BOTTOM/LEFT, the diagonals with or without CENTER, corners, HORIZONTAL/VERTICAL, CLOCKWISE/COUNTER_CLOCKWISE).
     * @maxLength 20
     */
    direction?: string | null;
    /**
     * Clip-path shape: ELLIPSE, CIRCLE, RECTANGLE, or DIAMOND (SHAPE).
     * @maxLength 12
     */
    shape?: string | null;
    /**
     * Motion style: GENTLE, MODERATE, or INTENSE. Only the effects that expose one accept it.
     * @maxLength 100
     */
    easing?: string | null;
}
declare enum EntranceEffectType {
    /** Fades in from fully transparent to fully opaque. */
    FADE = "FADE",
    /** Enters along a 3D arc path. */
    ARC = "ARC",
    /** Transitions from blurred to sharp. */
    BLUR = "BLUR",
    /** Bounces into place with an elastic curve. */
    BOUNCE = "BOUNCE",
    /** Shrinks down from a larger size. */
    DROP = "DROP",
    /** Expands from a point, scaling to full size. */
    EXPAND = "EXPAND",
    /** Flips into view with a 3D rotation. */
    FLIP = "FLIP",
    /** Drifts gently into place. */
    FLOAT = "FLOAT",
    /** Unfolds from an edge as if hinged. */
    FOLD = "FOLD",
    /** Glides in smoothly from off-screen. */
    GLIDE = "GLIDE",
    /** Progressively revealed by an expanding clip-path. */
    REVEAL = "REVEAL",
    /** Appears through an expanding geometric clip-path shape. */
    SHAPE = "SHAPE",
    /** Revealed through shutter-like strips. */
    SHUTTERS = "SHUTTERS",
    /** Slides in from one side. */
    SLIDE = "SLIDE",
    /** Spins into view while scaling up. */
    SPIN = "SPIN",
    /** Tilts in from the side with 3D rotation. */
    TILT = "TILT",
    /** Rotates into view around a corner pivot. */
    TURN = "TURN",
    /** Expands from its horizontal or vertical center. */
    WINK = "WINK"
}
/** @enumType */
type EntranceEffectTypeWithLiterals = EntranceEffectType | 'FADE' | 'ARC' | 'BLUR' | 'BOUNCE' | 'DROP' | 'EXPAND' | 'FLIP' | 'FLOAT' | 'FOLD' | 'GLIDE' | 'REVEAL' | 'SHAPE' | 'SHUTTERS' | 'SLIDE' | 'SPIN' | 'TILT' | 'TURN' | 'WINK';
interface LoopAnimation {
    /** The looping effect to repeat. */
    effect?: LoopEffect;
    /** Duration of a single iteration in milliseconds. Each effect has its own default. */
    duration?: number | null;
    /** Pause between iterations in milliseconds. */
    iterationDelay?: number | null;
}
interface LoopEffect {
    /** The looping effect type. */
    type?: LoopEffectTypeWithLiterals;
    /**
     * Direction of the effect. Allowed values depend on the effect type (e.g. TOP/RIGHT/BOTTOM/LEFT, LEFT/RIGHT, HORIZONTAL/VERTICAL with or without CENTER, CLOCKWISE/COUNTER_CLOCKWISE).
     * @maxLength 20
     */
    direction?: string | null;
    /**
     * Motion style: GENTLE, MODERATE, or INTENSE. Only the effects that expose one accept it.
     * @maxLength 100
     */
    easing?: string | null;
}
declare enum LoopEffectType {
    /** Bounces up and down in place. */
    BOUNCE = "BOUNCE",
    /** Scales gently in and out along an axis. */
    BREATHE = "BREATHE",
    /** Drifts toward one side, then re-enters from the opposite side. */
    CROSS = "CROSS",
    /** Fades out to fully transparent and back in. */
    FLASH = "FLASH",
    /** Rotates a full turn around its horizontal or vertical axis. */
    FLIP = "FLIP",
    /** Folds back and forth around a hinged edge. */
    FOLD = "FOLD",
    /** Wobbles with a jelly-like skew. */
    JELLO = "JELLO",
    /** Nudges repeatedly toward a direction. */
    POKE = "POKE",
    /** Scales up and back down rhythmically. */
    PULSE = "PULSE",
    /** Stretches and squashes like a rubber band. */
    RUBBER = "RUBBER",
    /** Rotates continuously around its center. */
    SPIN = "SPIN",
    /** Swings back and forth around an edge pivot. */
    SWING = "SWING",
    /** Rocks side to side with an accumulating rotation. */
    WIGGLE = "WIGGLE"
}
/** @enumType */
type LoopEffectTypeWithLiterals = LoopEffectType | 'BOUNCE' | 'BREATHE' | 'CROSS' | 'FLASH' | 'FLIP' | 'FOLD' | 'JELLO' | 'POKE' | 'PULSE' | 'RUBBER' | 'SPIN' | 'SWING' | 'WIGGLE';
interface PointerEffect {
    /** The mouse effect type. */
    type?: PointerEffectTypeWithLiterals;
    /** Reacts away from the pointer instead of toward it. */
    inverted?: boolean | null;
    /**
     * One field, two value spaces (the effect type says which): the axis the movement is constrained to — HORIZONTAL, VERTICAL, or BOTH (AIRY, SCALE, SKEW, TRACK, TRACK_3D) — or the edge/center axis the node swivels around — TOP, BOTTOM, LEFT, RIGHT, CENTER_HORIZONTAL, or CENTER_VERTICAL (SWIVEL). Only the effects that expose one accept it.
     * @maxLength 20
     */
    axis?: string | null;
    /**
     * Whether the node grows or shrinks as the pointer nears: UP or DOWN (SCALE).
     * @maxLength 8
     */
    direction?: string | null;
    /**
     * Motion style: GENTLE, MODERATE, or INTENSE.
     * @maxLength 100
     */
    easing?: string | null;
}
declare enum PointerEffectType {
    /** Drifts loosely after the pointer with a light rotation. */
    AIRY = "AIRY",
    /** Squashes and stretches toward the pointer. */
    BLOB = "BLOB",
    /** Shifts after the pointer while blurring. */
    BLUR = "BLUR",
    /** Scales as the pointer approaches. */
    SCALE = "SCALE",
    /** Skews toward the pointer. */
    SKEW = "SKEW",
    /** Rotates around a pivot edge or axis, following the pointer. */
    SWIVEL = "SWIVEL",
    /** Tilts in 3D perspective toward the pointer. */
    TILT_3D = "TILT_3D",
    /** Follows the pointer's position. */
    TRACK = "TRACK",
    /** Follows the pointer with a 3D depth motion. */
    TRACK_3D = "TRACK_3D"
}
/** @enumType */
type PointerEffectTypeWithLiterals = PointerEffectType | 'AIRY' | 'BLOB' | 'BLUR' | 'SCALE' | 'SKEW' | 'SWIVEL' | 'TILT_3D' | 'TRACK' | 'TRACK_3D';
interface CodeBlockData {
    /** Styling for the code block's text. */
    textStyle?: TextStyle;
    /** Node animation. */
    animation?: Animation;
}
interface TextStyle {
    /** Text alignment. Defaults to `AUTO`. */
    textAlignment?: TextAlignmentWithLiterals;
    /** A CSS `line-height` value for the text expressed as a ratio relative to the font size. For example, if the font size is 20px, a `lineHeight` value of `'1.5'`` results in a line height of 30px. */
    lineHeight?: string | null;
}
declare enum TextAlignment {
    /** browser default, eqivalent to `initial` */
    AUTO = "AUTO",
    /** Left align */
    LEFT = "LEFT",
    /** Right align */
    RIGHT = "RIGHT",
    /** Center align */
    CENTER = "CENTER",
    /** Text is spaced to line up its left and right edges to the left and right edges of the line box, except for the last line */
    JUSTIFY = "JUSTIFY"
}
/** @enumType */
type TextAlignmentWithLiterals = TextAlignment | 'AUTO' | 'LEFT' | 'RIGHT' | 'CENTER' | 'JUSTIFY';
interface DividerData {
    /** Styling for the divider's container. */
    containerData?: PluginContainerData;
    /** Divider line style. */
    lineStyle?: LineStyleWithLiterals;
    /** Divider width. */
    width?: WidthWithLiterals;
    /** Divider alignment. */
    alignment?: DividerDataAlignmentWithLiterals;
    /** Visual styling for the divider lines. */
    styles?: DividerDataStyles;
    /** Node animation. */
    animation?: Animation;
}
declare enum LineCap {
    /** Square line endings. */
    SQUARE = "SQUARE",
    /** Rounded line endings. */
    ROUND = "ROUND"
}
/** @enumType */
type LineCapWithLiterals = LineCap | 'SQUARE' | 'ROUND';
declare enum LineStyle {
    /** Single Line */
    SINGLE = "SINGLE",
    /** Double Line */
    DOUBLE = "DOUBLE",
    /** Dashed Line */
    DASHED = "DASHED",
    /** Dotted Line */
    DOTTED = "DOTTED"
}
/** @enumType */
type LineStyleWithLiterals = LineStyle | 'SINGLE' | 'DOUBLE' | 'DASHED' | 'DOTTED';
declare enum Width {
    /** Large line */
    LARGE = "LARGE",
    /** Medium line */
    MEDIUM = "MEDIUM",
    /** Small line */
    SMALL = "SMALL"
}
/** @enumType */
type WidthWithLiterals = Width | 'LARGE' | 'MEDIUM' | 'SMALL';
declare enum DividerDataAlignment {
    /** Center alignment */
    CENTER = "CENTER",
    /** Left alignment */
    LEFT = "LEFT",
    /** Right alignment */
    RIGHT = "RIGHT"
}
/** @enumType */
type DividerDataAlignmentWithLiterals = DividerDataAlignment | 'CENTER' | 'LEFT' | 'RIGHT';
interface DividerDataStyles {
    /**
     * Divider color as a hexadecimal value. An alpha channel controls opacity.
     * @maxLength 19
     */
    color?: string | null;
    /** Divider line ending shape. */
    lineCap?: LineCapWithLiterals;
    /**
     * Line thicknesses in pixels, ordered from first line to last line.
     * @maxSize 10
     */
    lineThickness?: number[];
    /** Visible dash length for dashed dividers, in pixels. The pattern is snapped to a whole number of dashes so that the first and last dash touch the divider's edges - narrower containers show fewer dashes at the same size. */
    dashLength?: number | null;
    /** Visible gap between dashes or dots, in pixels. */
    dashGap?: number | null;
    /** Gap between divider lines in pixels. */
    lineGap?: number | null;
}
interface FileData {
    /** Styling for the file's container. */
    containerData?: PluginContainerData;
    /** The source for the file's data. */
    src?: FileSource;
    /** File name. */
    name?: string | null;
    /** File type. */
    type?: string | null;
    /**
     * Use `sizeInKb` instead.
     * @deprecated
     */
    size?: number | null;
    /** Settings for PDF files. */
    pdfSettings?: PDFSettings;
    /** File MIME type. */
    mimeType?: string | null;
    /** File path. */
    path?: string | null;
    /** File size in KB. */
    sizeInKb?: string | null;
    /** Node animation. */
    animation?: Animation;
}
declare enum ViewMode {
    /** No PDF view */
    NONE = "NONE",
    /** Full PDF view */
    FULL = "FULL",
    /** Mini PDF view */
    MINI = "MINI"
}
/** @enumType */
type ViewModeWithLiterals = ViewMode | 'NONE' | 'FULL' | 'MINI';
interface FileSource extends FileSourceDataOneOf {
    /** The absolute URL for the file's source. */
    url?: string | null;
    /**
     * Custom ID. Use `id` instead.
     * @deprecated
     */
    custom?: string | null;
    /** An ID that's resolved to a URL by a resolver function. */
    id?: string | null;
    /** Indicates whether the file's source is private. Defaults to `false`. */
    private?: boolean | null;
}
/** @oneof */
interface FileSourceDataOneOf {
    /** The absolute URL for the file's source. */
    url?: string | null;
    /**
     * Custom ID. Use `id` instead.
     * @deprecated
     */
    custom?: string | null;
    /** An ID that's resolved to a URL by a resolver function. */
    id?: string | null;
}
interface PDFSettings {
    /**
     * PDF view mode. One of the following:
     * `NONE` : The PDF isn't displayed.
     * `FULL` : A full page view of the PDF is displayed.
     * `MINI` : A mini view of the PDF is displayed.
     */
    viewMode?: ViewModeWithLiterals;
    /** Sets whether the PDF download button is disabled. Defaults to `false`. */
    disableDownload?: boolean | null;
    /** Sets whether the PDF print button is disabled. Defaults to `false`. */
    disablePrint?: boolean | null;
}
interface GalleryData {
    /** Styling for the gallery's container. */
    containerData?: PluginContainerData;
    /** The items in the gallery. */
    items?: Item[];
    /** Options for defining the gallery's appearance. */
    options?: GalleryOptions;
    /** Sets whether the gallery's expand button is disabled. Defaults to `false`. */
    disableExpand?: boolean | null;
    /** Sets whether the gallery's download button is disabled. Defaults to `false`. */
    disableDownload?: boolean | null;
    /** Node animation. */
    animation?: Animation;
}
interface Media {
    /** The source for the media's data. */
    src?: FileSource;
    /** Media width in pixels. */
    width?: number | null;
    /** Media height in pixels. */
    height?: number | null;
    /** Media duration in seconds. Only relevant for audio and video files. */
    duration?: number | null;
}
interface Image {
    /** Image file details. */
    media?: Media;
    /** Link details for images that are links. */
    link?: Link;
}
interface Video {
    /** Video file details. */
    media?: Media;
    /** Video thumbnail file details. */
    thumbnail?: Media;
}
interface Item extends ItemDataOneOf {
    /** An image item. */
    image?: Image;
    /** A video item. */
    video?: Video;
    /** Item title. */
    title?: string | null;
    /** Item's alternative text. */
    altText?: string | null;
}
/** @oneof */
interface ItemDataOneOf {
    /** An image item. */
    image?: Image;
    /** A video item. */
    video?: Video;
}
interface GalleryOptions {
    /** Gallery layout. */
    layout?: GalleryOptionsLayout;
    /** Styling for gallery items. */
    item?: ItemStyle;
    /** Styling for gallery thumbnail images. */
    thumbnails?: Thumbnails;
}
declare enum LayoutType {
    /** Collage type */
    COLLAGE = "COLLAGE",
    /** Masonry type */
    MASONRY = "MASONRY",
    /** Grid type */
    GRID = "GRID",
    /** Thumbnail type */
    THUMBNAIL = "THUMBNAIL",
    /** Slider type */
    SLIDER = "SLIDER",
    /** Slideshow type */
    SLIDESHOW = "SLIDESHOW",
    /** Panorama type */
    PANORAMA = "PANORAMA",
    /** Column type */
    COLUMN = "COLUMN",
    /** Magic type */
    MAGIC = "MAGIC",
    /** Fullsize images type */
    FULLSIZE = "FULLSIZE"
}
/** @enumType */
type LayoutTypeWithLiterals = LayoutType | 'COLLAGE' | 'MASONRY' | 'GRID' | 'THUMBNAIL' | 'SLIDER' | 'SLIDESHOW' | 'PANORAMA' | 'COLUMN' | 'MAGIC' | 'FULLSIZE';
declare enum Orientation {
    /** Rows Orientation */
    ROWS = "ROWS",
    /** Columns Orientation */
    COLUMNS = "COLUMNS"
}
/** @enumType */
type OrientationWithLiterals = Orientation | 'ROWS' | 'COLUMNS';
declare enum Crop {
    /** Crop to fill */
    FILL = "FILL",
    /** Crop to fit */
    FIT = "FIT"
}
/** @enumType */
type CropWithLiterals = Crop | 'FILL' | 'FIT';
declare enum ThumbnailsAlignment {
    /** Top alignment */
    TOP = "TOP",
    /** Right alignment */
    RIGHT = "RIGHT",
    /** Bottom alignment */
    BOTTOM = "BOTTOM",
    /** Left alignment */
    LEFT = "LEFT",
    /** No thumbnail */
    NONE = "NONE"
}
/** @enumType */
type ThumbnailsAlignmentWithLiterals = ThumbnailsAlignment | 'TOP' | 'RIGHT' | 'BOTTOM' | 'LEFT' | 'NONE';
interface GalleryOptionsLayout {
    /** Gallery layout type. */
    type?: LayoutTypeWithLiterals;
    /** Sets whether horizontal scroll is enabled. Defaults to `true` unless the layout `type` is set to `GRID` or `COLLAGE`. */
    horizontalScroll?: boolean | null;
    /** Gallery orientation. */
    orientation?: OrientationWithLiterals;
    /** The number of columns to display on full size screens. */
    numberOfColumns?: number | null;
    /** The number of columns to display on mobile screens. */
    mobileNumberOfColumns?: number | null;
}
interface ItemStyle {
    /** Desirable dimension for each item in pixels (behvaior changes according to gallery type) */
    targetSize?: number | null;
    /** Item ratio */
    ratio?: number | null;
    /** Sets how item images are cropped. */
    crop?: CropWithLiterals;
    /** The spacing between items in pixels. */
    spacing?: number | null;
}
interface Thumbnails {
    /** Thumbnail alignment. */
    placement?: ThumbnailsAlignmentWithLiterals;
    /** Spacing between thumbnails in pixels. */
    spacing?: number | null;
}
interface GIFData {
    /** Styling for the GIF's container. */
    containerData?: PluginContainerData;
    /** The source of the full size GIF. */
    original?: GIF;
    /** The source of the downsized GIF. */
    downsized?: GIF;
    /** Height in pixels. */
    height?: number;
    /** Width in pixels. */
    width?: number;
    /** Type of GIF (Sticker or NORMAL). Defaults to `NORMAL`. */
    gifType?: GIFTypeWithLiterals;
    /** Node animation. */
    animation?: Animation;
}
interface GIF {
    /**
     * GIF format URL.
     * @format WEB_URL
     */
    gif?: string | null;
    /**
     * MP4 format URL.
     * @format WEB_URL
     */
    mp4?: string | null;
    /**
     * Thumbnail URL.
     * @format WEB_URL
     */
    still?: string | null;
}
declare enum GIFType {
    NORMAL = "NORMAL",
    STICKER = "STICKER"
}
/** @enumType */
type GIFTypeWithLiterals = GIFType | 'NORMAL' | 'STICKER';
interface HeadingData {
    /** Heading level from 1-6. */
    level?: number;
    /** Styling for the heading text. */
    textStyle?: TextStyle;
    /** Indentation level from 1-4. */
    indentation?: number | null;
    /** Rendered heading level for SEO/accessibility, overrides the HTML tag when set. */
    renderedLevel?: number | null;
    /** Node animation. */
    animation?: Animation;
}
interface HTMLData extends HTMLDataDataOneOf {
    /** The URL for the HTML code for the node. */
    url?: string;
    /** The HTML code for the node. */
    html?: string;
    /**
     * Whether this is an AdSense element. Use `source` instead.
     * @deprecated
     */
    isAdsense?: boolean | null;
    /** The WixelWidget ID for AI_WIDGET source nodes. */
    widgetId?: string;
    /** Styling for the HTML node's container. Height property is irrelevant for HTML embeds when autoHeight is set to `true`. */
    containerData?: PluginContainerData;
    /** The type of HTML code. */
    source?: SourceWithLiterals;
    /** If container height is aligned with its content height. Defaults to `true`. */
    autoHeight?: boolean | null;
    /** Node animation. */
    animation?: Animation;
}
/** @oneof */
interface HTMLDataDataOneOf {
    /** The URL for the HTML code for the node. */
    url?: string;
    /** The HTML code for the node. */
    html?: string;
    /**
     * Whether this is an AdSense element. Use `source` instead.
     * @deprecated
     */
    isAdsense?: boolean | null;
    /** The WixelWidget ID for AI_WIDGET source nodes. */
    widgetId?: string;
}
declare enum Source {
    HTML = "HTML",
    ADSENSE = "ADSENSE",
    AI = "AI",
    AI_WIDGET = "AI_WIDGET"
}
/** @enumType */
type SourceWithLiterals = Source | 'HTML' | 'ADSENSE' | 'AI' | 'AI_WIDGET';
interface ImageData {
    /** Styling for the image's container. */
    containerData?: PluginContainerData;
    /** Image file details. */
    image?: Media;
    /** Link details for images that are links. */
    link?: Link;
    /** Sets whether the image expands to full screen when clicked. Defaults to `false`. */
    disableExpand?: boolean | null;
    /** Image's alternative text. */
    altText?: string | null;
    /**
     * Deprecated: use Caption node instead.
     * @deprecated
     */
    caption?: string | null;
    /** Sets whether the image's download button is disabled. Defaults to `false`. */
    disableDownload?: boolean | null;
    /** Sets whether the image is decorative and does not need an explanation. Defaults to `false`. */
    decorative?: boolean | null;
    /** Styling for the image. */
    styles?: ImageDataStyles;
    /** Non-destructive crop rectangle, expressed as fractions (0-1) of the original image. When omitted, the full image is shown. */
    crop?: ImageDataCrop;
    /** Optional shape mask applied to the visible crop. Supported values: CIRCLE, OVAL, STAR, PENTAGON, HEXAGON, TRIANGLE, HEART, RHOMBUS, FLUID, WINDOW. */
    cropShape?: string | null;
    /** Node animation. */
    animation?: Animation;
}
interface StylesBorder {
    /** Border width in pixels. */
    width?: number | null;
    /**
     * Border color as a hexadecimal value.
     * @maxLength 19
     */
    color?: string | null;
    /** Border radius in pixels. */
    radius?: number | null;
}
interface ImageDataStyles {
    /** Border attributes. */
    border?: StylesBorder;
}
interface ImageDataCrop {
    /** Left edge of the crop, as a fraction (0-1) of the original image width. */
    x?: number | null;
    /** Top edge of the crop, as a fraction (0-1) of the original image height. */
    y?: number | null;
    /** Visible width of the crop, as a fraction (0-1) of the original image width. */
    width?: number | null;
    /** Visible height of the crop, as a fraction (0-1) of the original image height. */
    height?: number | null;
}
interface LinkPreviewData {
    /** Styling for the link preview's container. */
    containerData?: PluginContainerData;
    /** Link details. */
    link?: Link;
    /** Preview title. */
    title?: string | null;
    /** Preview thumbnail URL. */
    thumbnailUrl?: string | null;
    /** Preview description. */
    description?: string | null;
    /** The preview content as HTML. */
    html?: string | null;
    /** Styling for the link preview. */
    styles?: LinkPreviewDataStyles;
    /** Node animation. */
    animation?: Animation;
}
declare enum StylesPosition {
    /** Thumbnail positioned at the start (left in LTR layouts, right in RTL layouts) */
    START = "START",
    /** Thumbnail positioned at the end (right in LTR layouts, left in RTL layouts) */
    END = "END",
    /** Thumbnail positioned at the top */
    TOP = "TOP",
    /** Thumbnail hidden and not displayed */
    HIDDEN = "HIDDEN"
}
/** @enumType */
type StylesPositionWithLiterals = StylesPosition | 'START' | 'END' | 'TOP' | 'HIDDEN';
interface LinkPreviewDataStyles {
    /**
     * Background color as a hexadecimal value.
     * @maxLength 19
     */
    backgroundColor?: string | null;
    /**
     * Title color as a hexadecimal value.
     * @maxLength 19
     */
    titleColor?: string | null;
    /**
     * Subtitle color as a hexadecimal value.
     * @maxLength 19
     */
    subtitleColor?: string | null;
    /**
     * Link color as a hexadecimal value.
     * @maxLength 19
     */
    linkColor?: string | null;
    /** Border width in pixels. */
    borderWidth?: number | null;
    /** Border radius in pixels. */
    borderRadius?: number | null;
    /**
     * Border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Position of thumbnail. Defaults to `START`. */
    thumbnailPosition?: StylesPositionWithLiterals;
}
interface MapData {
    /** Styling for the map's container. */
    containerData?: PluginContainerData;
    /** Map settings. */
    mapSettings?: MapSettings;
}
interface MapSettings {
    /** The address to display on the map. */
    address?: string | null;
    /** Sets whether the map is draggable. */
    draggable?: boolean | null;
    /** Sets whether the location marker is visible. */
    marker?: boolean | null;
    /** Sets whether street view control is enabled. */
    streetViewControl?: boolean | null;
    /** Sets whether zoom control is enabled. */
    zoomControl?: boolean | null;
    /** Location latitude. */
    lat?: number | null;
    /** Location longitude. */
    lng?: number | null;
    /** Location name. */
    locationName?: string | null;
    /** Sets whether view mode control is enabled. */
    viewModeControl?: boolean | null;
    /** Initial zoom value. */
    initialZoom?: number | null;
    /** Map type. `HYBRID` is a combination of the `ROADMAP` and `SATELLITE` map types. */
    mapType?: MapTypeWithLiterals;
}
declare enum MapType {
    /** Roadmap map type */
    ROADMAP = "ROADMAP",
    /** Satellite map type */
    SATELITE = "SATELITE",
    /** Hybrid map type */
    HYBRID = "HYBRID",
    /** Terrain map type */
    TERRAIN = "TERRAIN"
}
/** @enumType */
type MapTypeWithLiterals = MapType | 'ROADMAP' | 'SATELITE' | 'HYBRID' | 'TERRAIN';
interface ParagraphData {
    /** Styling for the paragraph text. */
    textStyle?: TextStyle;
    /** Indentation level from 1-4. */
    indentation?: number | null;
    /** Paragraph level */
    level?: number | null;
    /** Node animation. */
    animation?: Animation;
}
interface PollData {
    /** Styling for the poll's container. */
    containerData?: PluginContainerData;
    /** Poll data. */
    poll?: Poll;
    /** Layout settings for the poll and voting options. */
    layout?: PollDataLayout;
    /** Styling for the poll and voting options. */
    design?: Design;
    /** Node animation. */
    animation?: Animation;
}
declare enum ViewRole {
    /** Only Poll creator can view the results */
    CREATOR = "CREATOR",
    /** Anyone who voted can see the results */
    VOTERS = "VOTERS",
    /** Anyone can see the results, even if one didn't vote */
    EVERYONE = "EVERYONE"
}
/** @enumType */
type ViewRoleWithLiterals = ViewRole | 'CREATOR' | 'VOTERS' | 'EVERYONE';
declare enum VoteRole {
    /** Logged in member */
    SITE_MEMBERS = "SITE_MEMBERS",
    /** Anyone */
    ALL = "ALL"
}
/** @enumType */
type VoteRoleWithLiterals = VoteRole | 'SITE_MEMBERS' | 'ALL';
interface Permissions {
    /** Sets who can view the poll results. */
    view?: ViewRoleWithLiterals;
    /** Sets who can vote. */
    vote?: VoteRoleWithLiterals;
    /** Sets whether one voter can vote multiple times. Defaults to `false`. */
    allowMultipleVotes?: boolean | null;
}
interface PollOption {
    /** Option ID. */
    id?: string | null;
    /** Option title. */
    title?: string | null;
    /** The image displayed with the option. */
    image?: Media;
}
interface Settings {
    /** Permissions settings for voting. */
    permissions?: Permissions;
    /** Sets whether voters are displayed in the vote results. Defaults to `true`. */
    showVoters?: boolean | null;
    /** Sets whether the vote count is displayed. Defaults to `true`. */
    showVotesCount?: boolean | null;
}
declare enum PollLayoutType {
    /** List */
    LIST = "LIST",
    /** Grid */
    GRID = "GRID"
}
/** @enumType */
type PollLayoutTypeWithLiterals = PollLayoutType | 'LIST' | 'GRID';
declare enum PollLayoutDirection {
    /** Left-to-right */
    LTR = "LTR",
    /** Right-to-left */
    RTL = "RTL"
}
/** @enumType */
type PollLayoutDirectionWithLiterals = PollLayoutDirection | 'LTR' | 'RTL';
interface PollLayout {
    /** The layout for displaying the voting options. */
    type?: PollLayoutTypeWithLiterals;
    /** The direction of the text displayed in the voting options. Text can be displayed either right-to-left or left-to-right. */
    direction?: PollLayoutDirectionWithLiterals;
    /** Sets whether to display the main poll image. Defaults to `false`. */
    enableImage?: boolean | null;
}
interface OptionLayout {
    /** Sets whether to display option images. Defaults to `false`. */
    enableImage?: boolean | null;
}
declare enum PollDesignBackgroundType {
    /** Color background type */
    COLOR = "COLOR",
    /** Image background type */
    IMAGE = "IMAGE",
    /** Gradiant background type */
    GRADIENT = "GRADIENT"
}
/** @enumType */
type PollDesignBackgroundTypeWithLiterals = PollDesignBackgroundType | 'COLOR' | 'IMAGE' | 'GRADIENT';
interface BackgroundGradient {
    /** The gradient angle in degrees. */
    angle?: number | null;
    /**
     * The start color as a hexademical value.
     * @maxLength 19
     */
    startColor?: string | null;
    /**
     * The end color as a hexademical value.
     * @maxLength 19
     */
    lastColor?: string | null;
}
interface PollDesignBackground extends PollDesignBackgroundBackgroundOneOf {
    /**
     * The background color as a hexademical value.
     * @maxLength 19
     */
    color?: string | null;
    /** An image to use for the background. */
    image?: Media;
    /** Details for a gradient background. */
    gradient?: BackgroundGradient;
    /** Background type. For each option, include the relevant details. */
    type?: PollDesignBackgroundTypeWithLiterals;
}
/** @oneof */
interface PollDesignBackgroundBackgroundOneOf {
    /**
     * The background color as a hexademical value.
     * @maxLength 19
     */
    color?: string | null;
    /** An image to use for the background. */
    image?: Media;
    /** Details for a gradient background. */
    gradient?: BackgroundGradient;
}
interface PollDesign {
    /** Background styling. */
    background?: PollDesignBackground;
    /** Border radius in pixels. */
    borderRadius?: number | null;
}
interface OptionDesign {
    /** Border radius in pixels. */
    borderRadius?: number | null;
}
interface Poll {
    /** Poll ID. */
    id?: string | null;
    /** Poll title. */
    title?: string | null;
    /** Poll creator ID. */
    creatorId?: string | null;
    /** Main poll image. */
    image?: Media;
    /** Voting options. */
    options?: PollOption[];
    /** The poll's permissions and display settings. */
    settings?: Settings;
}
interface PollDataLayout {
    /** Poll layout settings. */
    poll?: PollLayout;
    /** Voting otpions layout settings. */
    options?: OptionLayout;
}
interface Design {
    /** Styling for the poll. */
    poll?: PollDesign;
    /** Styling for voting options. */
    options?: OptionDesign;
}
interface TextData {
    /** The text to apply decorations to. */
    text?: string;
    /** The decorations to apply. */
    decorations?: Decoration[];
}
/** Adds appearence changes to text */
interface Decoration extends DecorationDataOneOf {
    /** Data for an anchor link decoration. */
    anchorData?: AnchorData;
    /** Data for a color decoration. */
    colorData?: ColorData;
    /** Data for an external link decoration. */
    linkData?: LinkData;
    /** Data for a mention decoration. */
    mentionData?: MentionData;
    /** Data for a font size decoration. */
    fontSizeData?: FontSizeData;
    /** Font weight for a bold decoration. */
    fontWeightValue?: number | null;
    /** Data for an italic decoration. Defaults to `true`. */
    italicData?: boolean | null;
    /** Data for an underline decoration. Defaults to `true`. */
    underlineData?: boolean | null;
    /** Data for a spoiler decoration. */
    spoilerData?: SpoilerData;
    /** Data for a strikethrough decoration. Defaults to `true`. */
    strikethroughData?: boolean | null;
    /** Data for a superscript decoration. Defaults to `true`. */
    superscriptData?: boolean | null;
    /** Data for a subscript decoration. Defaults to `true`. */
    subscriptData?: boolean | null;
    /** Data for a font family decoration. */
    fontFamilyData?: FontFamilyData;
    /** Data for a hand-drawn sketch annotation decoration. */
    sketchData?: SketchData;
    /** The type of decoration to apply. */
    type?: DecorationTypeWithLiterals;
}
/** @oneof */
interface DecorationDataOneOf {
    /** Data for an anchor link decoration. */
    anchorData?: AnchorData;
    /** Data for a color decoration. */
    colorData?: ColorData;
    /** Data for an external link decoration. */
    linkData?: LinkData;
    /** Data for a mention decoration. */
    mentionData?: MentionData;
    /** Data for a font size decoration. */
    fontSizeData?: FontSizeData;
    /** Font weight for a bold decoration. */
    fontWeightValue?: number | null;
    /** Data for an italic decoration. Defaults to `true`. */
    italicData?: boolean | null;
    /** Data for an underline decoration. Defaults to `true`. */
    underlineData?: boolean | null;
    /** Data for a spoiler decoration. */
    spoilerData?: SpoilerData;
    /** Data for a strikethrough decoration. Defaults to `true`. */
    strikethroughData?: boolean | null;
    /** Data for a superscript decoration. Defaults to `true`. */
    superscriptData?: boolean | null;
    /** Data for a subscript decoration. Defaults to `true`. */
    subscriptData?: boolean | null;
    /** Data for a font family decoration. */
    fontFamilyData?: FontFamilyData;
    /** Data for a hand-drawn sketch annotation decoration. */
    sketchData?: SketchData;
}
declare enum DecorationType {
    BOLD = "BOLD",
    ITALIC = "ITALIC",
    UNDERLINE = "UNDERLINE",
    SPOILER = "SPOILER",
    ANCHOR = "ANCHOR",
    MENTION = "MENTION",
    LINK = "LINK",
    COLOR = "COLOR",
    FONT_SIZE = "FONT_SIZE",
    EXTERNAL = "EXTERNAL",
    STRIKETHROUGH = "STRIKETHROUGH",
    SUPERSCRIPT = "SUPERSCRIPT",
    SUBSCRIPT = "SUBSCRIPT",
    FONT_FAMILY = "FONT_FAMILY",
    SKETCH = "SKETCH"
}
/** @enumType */
type DecorationTypeWithLiterals = DecorationType | 'BOLD' | 'ITALIC' | 'UNDERLINE' | 'SPOILER' | 'ANCHOR' | 'MENTION' | 'LINK' | 'COLOR' | 'FONT_SIZE' | 'EXTERNAL' | 'STRIKETHROUGH' | 'SUPERSCRIPT' | 'SUBSCRIPT' | 'FONT_FAMILY' | 'SKETCH';
interface AnchorData {
    /** The target node's ID. */
    anchor?: string;
}
interface ColorData {
    /** The text's background color as a hexadecimal value. */
    background?: string | null;
    /** The text's foreground color as a hexadecimal value. */
    foreground?: string | null;
}
interface LinkData {
    /** Link details. */
    link?: Link;
}
interface MentionData {
    /** The mentioned user's name. */
    name?: string;
    /** The version of the user's name that appears after the `@` character in the mention. */
    slug?: string;
    /** Mentioned user's ID. */
    id?: string | null;
    /** The kind of entity the mention refers to. Any value is accepted, and it's usually used to mark a mention as a group mention (for example, `group`). When empty, the mention refers to a member. */
    type?: string | null;
}
interface FontSizeData {
    /** The units used for the font size. */
    unit?: FontTypeWithLiterals;
    /** Font size value. */
    value?: number | null;
}
declare enum FontType {
    PX = "PX",
    EM = "EM"
}
/** @enumType */
type FontTypeWithLiterals = FontType | 'PX' | 'EM';
interface SpoilerData {
    /** Spoiler ID. */
    id?: string | null;
}
interface FontFamilyData {
    /** @maxLength 1000 */
    value?: string | null;
}
interface SketchData {
    /** The sketch annotation variant to draw over the text. */
    variant?: VariantWithLiterals;
    /**
     * Annotation color. Defaults to the theme action color.
     * @maxLength 19
     */
    color?: string | null;
    /** Whether the annotation animates on first paint. Defaults to `true`. */
    animate?: boolean | null;
}
declare enum Variant {
    UNDERLINE = "UNDERLINE",
    BOX = "BOX",
    CIRCLE = "CIRCLE",
    HIGHLIGHT = "HIGHLIGHT",
    STRIKETHROUGH = "STRIKETHROUGH",
    CROSSED_OFF = "CROSSED_OFF"
}
/** @enumType */
type VariantWithLiterals = Variant | 'UNDERLINE' | 'BOX' | 'CIRCLE' | 'HIGHLIGHT' | 'STRIKETHROUGH' | 'CROSSED_OFF';
interface AppEmbedData extends AppEmbedDataAppDataOneOf {
    /** Data for embedded Wix Bookings content. */
    bookingData?: BookingData;
    /** Data for embedded Wix Events content. */
    eventData?: EventData;
    /** The type of Wix App content being embedded. */
    type?: AppTypeWithLiterals;
    /** The ID of the embedded content. */
    itemId?: string | null;
    /** The name of the embedded content. */
    name?: string | null;
    /**
     * Deprecated: Use `image` instead.
     * @deprecated
     */
    imageSrc?: string | null;
    /** The URL for the embedded content. */
    url?: string | null;
    /** An image for the embedded content. */
    image?: Media;
    /** Whether to hide the image. */
    hideImage?: boolean | null;
    /** Whether to hide the title. */
    hideTitle?: boolean | null;
    /** Whether to hide the price. */
    hidePrice?: boolean | null;
    /** Whether to hide the description (Event and Booking). */
    hideDescription?: boolean | null;
    /** Whether to hide the date and time (Event). */
    hideDateTime?: boolean | null;
    /** Whether to hide the location (Event). */
    hideLocation?: boolean | null;
    /** Whether to hide the duration (Booking). */
    hideDuration?: boolean | null;
    /** Whether to hide the button. */
    hideButton?: boolean | null;
    /** Whether to hide the ribbon. */
    hideRibbon?: boolean | null;
    /** Button styling options. */
    buttonStyles?: ButtonStyles;
    /** Image styling options. */
    imageStyles?: ImageStyles;
    /** Ribbon styling options. */
    ribbonStyles?: RibbonStyles;
    /** Card styling options. */
    cardStyles?: CardStyles;
    /** Styling for the app embed's container. */
    containerData?: PluginContainerData;
    /** Pricing data for embedded Wix App content. */
    pricingData?: PricingData;
    /** Node animation. */
    animation?: Animation;
}
/** @oneof */
interface AppEmbedDataAppDataOneOf {
    /** Data for embedded Wix Bookings content. */
    bookingData?: BookingData;
    /** Data for embedded Wix Events content. */
    eventData?: EventData;
}
declare enum Position {
    /** Image positioned at the start (left in LTR layouts, right in RTL layouts) */
    START = "START",
    /** Image positioned at the end (right in LTR layouts, left in RTL layouts) */
    END = "END",
    /** Image positioned at the top */
    TOP = "TOP"
}
/** @enumType */
type PositionWithLiterals = Position | 'START' | 'END' | 'TOP';
declare enum AspectRatio {
    /** 1:1 aspect ratio */
    SQUARE = "SQUARE",
    /** 16:9 aspect ratio */
    RECTANGLE = "RECTANGLE"
}
/** @enumType */
type AspectRatioWithLiterals = AspectRatio | 'SQUARE' | 'RECTANGLE';
declare enum Resizing {
    /** Fill the container, may crop the image */
    FILL = "FILL",
    /** Fit the image within the container */
    FIT = "FIT"
}
/** @enumType */
type ResizingWithLiterals = Resizing | 'FILL' | 'FIT';
declare enum Placement {
    /** Ribbon placed on the image */
    IMAGE = "IMAGE",
    /** Ribbon placed on the product information */
    PRODUCT_INFO = "PRODUCT_INFO"
}
/** @enumType */
type PlacementWithLiterals = Placement | 'IMAGE' | 'PRODUCT_INFO';
declare enum CardStylesType {
    /** Card with visible border and background */
    CONTAINED = "CONTAINED",
    /** Card without visible border */
    FRAMELESS = "FRAMELESS"
}
/** @enumType */
type CardStylesTypeWithLiterals = CardStylesType | 'CONTAINED' | 'FRAMELESS';
declare enum CardStylesAlignment {
    /** Content aligned to start (left in LTR layouts, right in RTL layouts) */
    START = "START",
    /** Content centered */
    CENTER = "CENTER",
    /** Content aligned to end (right in LTR layouts, left in RTL layouts) */
    END = "END"
}
/** @enumType */
type CardStylesAlignmentWithLiterals = CardStylesAlignment | 'START' | 'CENTER' | 'END';
declare enum Layout {
    /** Elements stacked vertically */
    STACKED = "STACKED",
    /** Elements arranged horizontally */
    SIDE_BY_SIDE = "SIDE_BY_SIDE"
}
/** @enumType */
type LayoutWithLiterals = Layout | 'STACKED' | 'SIDE_BY_SIDE';
declare enum AppType {
    PRODUCT = "PRODUCT",
    EVENT = "EVENT",
    BOOKING = "BOOKING"
}
/** @enumType */
type AppTypeWithLiterals = AppType | 'PRODUCT' | 'EVENT' | 'BOOKING';
interface BookingData {
    /** Booking duration in minutes. */
    durations?: string | null;
}
interface EventData {
    /** Event schedule. */
    scheduling?: string | null;
    /** Event location. */
    location?: string | null;
}
interface ButtonStyles {
    /** Text to display on the button. */
    buttonText?: string | null;
    /** Border width in pixels. */
    borderWidth?: number | null;
    /** Border radius in pixels. */
    borderRadius?: number | null;
    /**
     * Border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /**
     * Text color as a hexadecimal value.
     * @maxLength 19
     */
    textColor?: string | null;
    /**
     * Background color as a hexadecimal value.
     * @maxLength 19
     */
    backgroundColor?: string | null;
    /**
     * Border color as a hexadecimal value (hover state).
     * @maxLength 19
     */
    borderColorHover?: string | null;
    /**
     * Text color as a hexadecimal value (hover state).
     * @maxLength 19
     */
    textColorHover?: string | null;
    /**
     * Background color as a hexadecimal value (hover state).
     * @maxLength 19
     */
    backgroundColorHover?: string | null;
    /** Button size option, one of `SMALL`, `MEDIUM` or `LARGE`. Defaults to `MEDIUM`. */
    buttonSize?: string | null;
}
interface ImageStyles {
    /** Whether to hide the image. */
    hideImage?: boolean | null;
    /** Position of image. Defaults to `START`. */
    imagePosition?: PositionWithLiterals;
    /** Aspect ratio for the image. Defaults to `SQUARE`. */
    aspectRatio?: AspectRatioWithLiterals;
    /** How the image should be resized. Defaults to `FILL`. */
    resizing?: ResizingWithLiterals;
    /**
     * Image border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Image border width in pixels. */
    borderWidth?: number | null;
    /** Image border radius in pixels. */
    borderRadius?: number | null;
}
interface RibbonStyles {
    /** Text to display on the ribbon. */
    ribbonText?: string | null;
    /**
     * Ribbon background color as a hexadecimal value.
     * @maxLength 19
     */
    backgroundColor?: string | null;
    /**
     * Ribbon text color as a hexadecimal value.
     * @maxLength 19
     */
    textColor?: string | null;
    /**
     * Ribbon border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Ribbon border width in pixels. */
    borderWidth?: number | null;
    /** Ribbon border radius in pixels. */
    borderRadius?: number | null;
    /** Placement of the ribbon. Defaults to `IMAGE`. */
    ribbonPlacement?: PlacementWithLiterals;
}
interface CardStyles {
    /**
     * Card background color as a hexadecimal value.
     * @maxLength 19
     */
    backgroundColor?: string | null;
    /**
     * Card border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Card border width in pixels. */
    borderWidth?: number | null;
    /** Card border radius in pixels. */
    borderRadius?: number | null;
    /** Card type. Defaults to `CONTAINED`. */
    type?: CardStylesTypeWithLiterals;
    /** Content alignment. Defaults to `START`. */
    alignment?: CardStylesAlignmentWithLiterals;
    /** Layout for title and price. Defaults to `STACKED`. */
    titlePriceLayout?: LayoutWithLiterals;
    /**
     * Title text color as a hexadecimal value.
     * @maxLength 19
     */
    titleColor?: string | null;
    /**
     * Text color as a hexadecimal value.
     * @maxLength 19
     */
    textColor?: string | null;
}
interface PricingData {
    /**
     * Minimum numeric price value as string (e.g., "10.99").
     * @decimalValue options { maxScale:2 }
     */
    valueFrom?: string | null;
    /**
     * Maximum numeric price value as string (e.g., "19.99").
     * @decimalValue options { maxScale:2 }
     */
    valueTo?: string | null;
    /**
     * Numeric price value as string after discount application (e.g., "15.99").
     * @decimalValue options { maxScale:2 }
     */
    discountedValue?: string | null;
    /**
     * Currency of the value in ISO 4217 format (e.g., "USD", "EUR").
     * @format CURRENCY
     */
    currency?: string | null;
    /**
     * Pricing plan ID.
     * @format GUID
     */
    pricingPlanId?: string | null;
}
interface VideoData {
    /** Styling for the video's container. */
    containerData?: PluginContainerData;
    /** Video details. */
    video?: Media;
    /** Video thumbnail details. */
    thumbnail?: Media;
    /** Sets whether the video's download button is disabled. Defaults to `false`. */
    disableDownload?: boolean | null;
    /** Video title. */
    title?: string | null;
    /** Video options. */
    options?: PlaybackOptions;
    /** Node animation. */
    animation?: Animation;
}
interface PlaybackOptions {
    /** Sets whether the media will automatically start playing. */
    autoPlay?: boolean | null;
    /** Sets whether media's will be looped. */
    playInLoop?: boolean | null;
    /** Sets whether media's controls will be shown. */
    showControls?: boolean | null;
}
interface EmbedData {
    /** Styling for the oEmbed node's container. */
    containerData?: PluginContainerData;
    /** An [oEmbed](https://www.oembed.com) object. */
    oembed?: Oembed;
    /** Origin asset source. */
    src?: string | null;
    /** Node animation. */
    animation?: Animation;
}
interface Oembed {
    /** The resource type. */
    type?: string | null;
    /** The width of the resource specified in the `url` property in pixels. */
    width?: number | null;
    /** The height of the resource specified in the `url` property in pixels. */
    height?: number | null;
    /** Resource title. */
    title?: string | null;
    /** The source URL for the resource. */
    url?: string | null;
    /** HTML for embedding a video player. The HTML should have no padding or margins. */
    html?: string | null;
    /** The name of the author or owner of the resource. */
    authorName?: string | null;
    /** The URL for the author or owner of the resource. */
    authorUrl?: string | null;
    /** The name of the resource provider. */
    providerName?: string | null;
    /** The URL for the resource provider. */
    providerUrl?: string | null;
    /** The URL for a thumbnail image for the resource. If this property is defined, `thumbnailWidth` and `thumbnailHeight` must also be defined. */
    thumbnailUrl?: string | null;
    /** The width of the resource's thumbnail image. If this property is defined, `thumbnailUrl` and `thumbnailHeight` must also be defined. */
    thumbnailWidth?: string | null;
    /** The height of the resource's thumbnail image. If this property is defined, `thumbnailUrl` and `thumbnailWidth`must also be defined. */
    thumbnailHeight?: string | null;
    /** The URL for an embedded viedo. */
    videoUrl?: string | null;
    /** The oEmbed version number.  This value must be `1.0`. */
    version?: string | null;
}
interface CollapsibleListData {
    /** Styling for the collapsible list's container. */
    containerData?: PluginContainerData;
    /** If `true`, only one item can be expanded at a time. Defaults to `false`. */
    expandOnlyOne?: boolean | null;
    /** Sets which items are expanded when the page loads. */
    initialExpandedItems?: InitialExpandedItemsWithLiterals;
    /** The direction of the text in the list. Either left-to-right or right-to-left. */
    direction?: DirectionWithLiterals;
    /** If `true`, The collapsible item will appear in search results as an FAQ. */
    isQapageData?: boolean | null;
    /** Node animation. */
    animation?: Animation;
}
declare enum InitialExpandedItems {
    /** First item will be expended initally */
    FIRST = "FIRST",
    /** All items will expended initally */
    ALL = "ALL",
    /** All items collapsed initally */
    NONE = "NONE"
}
/** @enumType */
type InitialExpandedItemsWithLiterals = InitialExpandedItems | 'FIRST' | 'ALL' | 'NONE';
declare enum Direction {
    /** Left-to-right */
    LTR = "LTR",
    /** Right-to-left */
    RTL = "RTL"
}
/** @enumType */
type DirectionWithLiterals = Direction | 'LTR' | 'RTL';
interface TableData {
    /** Styling for the table's container. */
    containerData?: PluginContainerData;
    /** The table's dimensions. */
    dimensions?: Dimensions;
    /**
     * Deprecated: Use `rowHeader` and `columnHeader` instead.
     * @deprecated
     */
    header?: boolean | null;
    /** Sets whether the table's first row is a header. Defaults to `false`. */
    rowHeader?: boolean | null;
    /** Sets whether the table's first column is a header. Defaults to `false`. */
    columnHeader?: boolean | null;
    /** The spacing between cells in pixels. Defaults to `0`. */
    cellSpacing?: number | null;
    /**
     * Padding in pixels for cells. Follows CSS order: top, right, bottom, left.
     * @maxSize 4
     */
    cellPadding?: number[];
    /** Table's alternative text. */
    altText?: string | null;
    /** Node animation. */
    animation?: Animation;
}
interface Dimensions {
    /** An array representing relative width of each column in relation to the other columns. */
    colsWidthRatio?: number[];
    /** An array representing the height of each row in pixels. */
    rowsHeight?: number[];
    /** An array representing the minimum width of each column in pixels. */
    colsMinWidth?: number[];
}
interface TableCellData {
    /** Styling for the cell's background color and text alignment. */
    cellStyle?: CellStyle;
    /** The cell's border colors. */
    borderColors?: BorderColors;
    /** Defines how many columns the cell spans. Default: 1. */
    colspan?: number | null;
    /** Defines how many rows the cell spans. Default: 1. */
    rowspan?: number | null;
    /** The cell's border widths. */
    borderWidths?: BorderWidths;
}
declare enum VerticalAlignment {
    /** Top alignment */
    TOP = "TOP",
    /** Middle alignment */
    MIDDLE = "MIDDLE",
    /** Bottom alignment */
    BOTTOM = "BOTTOM"
}
/** @enumType */
type VerticalAlignmentWithLiterals = VerticalAlignment | 'TOP' | 'MIDDLE' | 'BOTTOM';
interface CellStyle {
    /** Vertical alignment for the cell's text. */
    verticalAlignment?: VerticalAlignmentWithLiterals;
    /**
     * Cell background color as a hexadecimal value.
     * @maxLength 19
     */
    backgroundColor?: string | null;
}
interface BorderColors {
    /**
     * Left border color as a hexadecimal value.
     * @maxLength 19
     */
    left?: string | null;
    /**
     * Right border color as a hexadecimal value.
     * @maxLength 19
     */
    right?: string | null;
    /**
     * Top border color as a hexadecimal value.
     * @maxLength 19
     */
    top?: string | null;
    /**
     * Bottom border color as a hexadecimal value.
     * @maxLength 19
     */
    bottom?: string | null;
}
interface BorderWidths {
    /** Left border width in pixels. */
    left?: number | null;
    /** Right border width in pixels. */
    right?: number | null;
    /** Top border width in pixels. */
    top?: number | null;
    /** Bottom border width in pixels. */
    bottom?: number | null;
}
/**
 * `NullValue` is a singleton enumeration to represent the null value for the
 * `Value` type union.
 *
 * The JSON representation for `NullValue` is JSON `null`.
 */
declare enum NullValue {
    /** Null value. */
    NULL_VALUE = "NULL_VALUE"
}
/** @enumType */
type NullValueWithLiterals = NullValue | 'NULL_VALUE';
/**
 * `ListValue` is a wrapper around a repeated field of values.
 *
 * The JSON representation for `ListValue` is JSON array.
 */
interface ListValue {
    /** Repeated field of dynamically typed values. */
    values?: any[];
}
interface AudioData {
    /** Styling for the audio node's container. */
    containerData?: PluginContainerData;
    /** Audio file details. */
    audio?: Media;
    /** Sets whether the audio node's download button is disabled. Defaults to `false`. */
    disableDownload?: boolean | null;
    /** Cover image. */
    coverImage?: Media;
    /** Track name. */
    name?: string | null;
    /** Author name. */
    authorName?: string | null;
    /** An HTML version of the audio node. */
    html?: string | null;
    /** Node animation. */
    animation?: Animation;
}
interface OrderedListData {
    /** Indentation level from 0-4. */
    indentation?: number;
    /** Offset level from 0-4. */
    offset?: number | null;
    /** List start number. */
    start?: number | null;
    /** Node animation. */
    animation?: Animation;
}
interface BulletedListData {
    /** Indentation level from 0-4. */
    indentation?: number;
    /** Offset level from 0-4. */
    offset?: number | null;
    /** Node animation. */
    animation?: Animation;
}
interface BlockquoteData {
    /** Indentation level from 1-4. */
    indentation?: number;
    /** Node animation. */
    animation?: Animation;
}
interface CaptionData {
    textStyle?: TextStyle;
}
interface LayoutData {
    /**
     * Deprecated: Use `background` instead.
     * @maxLength 19
     * @deprecated
     */
    backgroundColor?: string | null;
    /** Background image. */
    backgroundImage?: LayoutDataBackgroundImage;
    /**
     * Border color as a hexadecimal value.
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Border width in pixels. */
    borderWidth?: number | null;
    /** Border radius in pixels. */
    borderRadius?: number | null;
    /**
     * Deprecated: Use `backdrop` instead.
     * @maxLength 19
     * @deprecated
     */
    backdropColor?: string | null;
    /** Backdrop image. */
    backdropImage?: LayoutDataBackgroundImage;
    /** Backdrop top padding. */
    backdropPaddingTop?: number | null;
    /** Backdrop bottom padding */
    backdropPaddingBottom?: number | null;
    /** Horizontal and vertical gap between columns */
    gap?: number | null;
    /**
     * Padding in pixels for cells. Follows CSS order: top, right, bottom, left
     * @maxSize 4
     */
    cellPadding?: number[];
    /** Vertical alignment for the cell's items. */
    cellVerticalAlignment?: VerticalAlignmentAlignmentWithLiterals;
    /** Responsiveness behaviour of columns when responsiveness applies. Either stacks or wrappers. */
    responsivenessBehaviour?: ResponsivenessBehaviourWithLiterals;
    /** Size in pixels when responsiveness_behaviour applies */
    responsivenessBreakpoint?: number | null;
    /** Styling for the layout's container. */
    containerData?: PluginContainerData;
    /** Defines where selected design propertied applies to */
    designTarget?: DesignTargetWithLiterals;
    /** Banner configuration. When present, this layout is attached to a document edge (top or bottom). */
    banner?: Banner;
    /** Background styling (color or gradient). */
    background?: LayoutDataBackground;
    /** Backdrop styling (color or gradient). */
    backdrop?: Backdrop;
    /** Node animation. */
    animation?: Animation;
    /** Shape divider at the top edge of the layout. */
    topDivider?: SectionDivider;
    /** Shape divider at the bottom edge of the layout. */
    bottomDivider?: SectionDivider;
}
declare enum ImageScalingScaling {
    /** Auto image scaling */
    AUTO = "AUTO",
    /** Contain image scaling */
    CONTAIN = "CONTAIN",
    /** Cover image scaling */
    COVER = "COVER"
}
/** @enumType */
type ImageScalingScalingWithLiterals = ImageScalingScaling | 'AUTO' | 'CONTAIN' | 'COVER';
declare enum LayoutDataImagePosition {
    /** Image positioned at the center */
    CENTER = "CENTER",
    /** Image positioned on the left */
    CENTER_LEFT = "CENTER_LEFT",
    /** Image positioned on the right */
    CENTER_RIGHT = "CENTER_RIGHT",
    /** Image positioned at the center top */
    TOP = "TOP",
    /** Image positioned at the top left */
    TOP_LEFT = "TOP_LEFT",
    /** Image positioned at the top right */
    TOP_RIGHT = "TOP_RIGHT",
    /** Image positioned at the center bottom */
    BOTTOM = "BOTTOM",
    /** Image positioned at the bottom left */
    BOTTOM_LEFT = "BOTTOM_LEFT",
    /** Image positioned at the bottom right */
    BOTTOM_RIGHT = "BOTTOM_RIGHT"
}
/** @enumType */
type LayoutDataImagePositionWithLiterals = LayoutDataImagePosition | 'CENTER' | 'CENTER_LEFT' | 'CENTER_RIGHT' | 'TOP' | 'TOP_LEFT' | 'TOP_RIGHT' | 'BOTTOM' | 'BOTTOM_LEFT' | 'BOTTOM_RIGHT';
/** Background styling (color or gradient) */
interface LayoutDataBackground {
    /** Background type. */
    type?: LayoutDataBackgroundTypeWithLiterals;
    /**
     * Background color as a hexadecimal value.
     * @maxLength 19
     */
    color?: string | null;
    /** Gradient configuration. */
    gradient?: Gradient;
}
/** Background type */
declare enum LayoutDataBackgroundType {
    /** Solid color background */
    COLOR = "COLOR",
    /** Gradient background */
    GRADIENT = "GRADIENT"
}
/** @enumType */
type LayoutDataBackgroundTypeWithLiterals = LayoutDataBackgroundType | 'COLOR' | 'GRADIENT';
declare enum Origin {
    /** Banner originated from an image */
    IMAGE = "IMAGE",
    /** Banner originated from a layout */
    LAYOUT = "LAYOUT"
}
/** @enumType */
type OriginWithLiterals = Origin | 'IMAGE' | 'LAYOUT';
declare enum BannerPosition {
    /** Attached to the top edge (banner) */
    TOP = "TOP",
    /** Attached to the bottom edge (footer) */
    BOTTOM = "BOTTOM"
}
/** @enumType */
type BannerPositionWithLiterals = BannerPosition | 'TOP' | 'BOTTOM';
/** How the stacked layers fade relative to the base shape. Named for a bottom divider; top dividers render mirrored, so the visual fade flips. */
declare enum LayerEffect {
    /** Shape keeps its height, a solid filler bar pushes it away from the edge */
    CENTER = "CENTER",
    /** Deeper (more transparent) layers shrink toward the edge inside the original band while a solid filler bar keeps its full height, so the faded copies hug the top of the band */
    FADE_TO_TOP = "FADE_TO_TOP",
    /** Deeper (more transparent) layers stretch taller into the section, so the faded copies fan out above the base shape and descend to the layout edge */
    FADE_TO_BOTTOM = "FADE_TO_BOTTOM"
}
/** @enumType */
type LayerEffectWithLiterals = LayerEffect | 'CENTER' | 'FADE_TO_TOP' | 'FADE_TO_BOTTOM';
/** Divider shape preset */
declare enum Shape {
    /** Ellipse shape */
    ELLIPSE = "ELLIPSE",
    /** Tilt shape */
    TILT = "TILT",
    /** Liquid shape */
    LIQUID = "LIQUID",
    /** Left wave shape */
    LEFT_WAVE = "LEFT_WAVE",
    /** Paint scribble shape */
    PAINT_SCRIBBLE = "PAINT_SCRIBBLE",
    /** Inverted ellipse shape */
    INVERTED_ELLIPSE = "INVERTED_ELLIPSE",
    /** Right wave shape */
    RIGHT_WAVE = "RIGHT_WAVE",
    /** Dunes shape */
    DUNES = "DUNES",
    /** Waves shape */
    WAVES = "WAVES",
    /** Triangle shape */
    TRIANGLE = "TRIANGLE",
    /** Semi-ellipse shape */
    SEMI_ELLIPSE = "SEMI_ELLIPSE",
    /** Plants shape */
    PLANTS = "PLANTS",
    /** Layered ellipse shape */
    LAYERED_ELLIPSE = "LAYERED_ELLIPSE",
    /** Pixels shape */
    PIXELS = "PIXELS",
    /** Paint shape */
    PAINT = "PAINT",
    /** Clouds shape */
    CLOUDS = "CLOUDS",
    /** Optical illusion shape */
    OPTICAL_ILLUSION = "OPTICAL_ILLUSION",
    /** Stripes shape */
    STRIPES = "STRIPES",
    /** Blobs shape */
    BLOBS = "BLOBS",
    /** Semi-circles shape */
    SEMI_CIRCLES = "SEMI_CIRCLES",
    /** Hill shape */
    HILL = "HILL",
    /** Brush shape */
    BRUSH = "BRUSH",
    /** Peaks shape */
    PEAKS = "PEAKS",
    /** Angled triangle shape */
    ANGLED_TRIANGLE = "ANGLED_TRIANGLE"
}
/** @enumType */
type ShapeWithLiterals = Shape | 'ELLIPSE' | 'TILT' | 'LIQUID' | 'LEFT_WAVE' | 'PAINT_SCRIBBLE' | 'INVERTED_ELLIPSE' | 'RIGHT_WAVE' | 'DUNES' | 'WAVES' | 'TRIANGLE' | 'SEMI_ELLIPSE' | 'PLANTS' | 'LAYERED_ELLIPSE' | 'PIXELS' | 'PAINT' | 'CLOUDS' | 'OPTICAL_ILLUSION' | 'STRIPES' | 'BLOBS' | 'SEMI_CIRCLES' | 'HILL' | 'BRUSH' | 'PEAKS' | 'ANGLED_TRIANGLE';
/** Layers effect - stacked semi-transparent copies of the divider shape */
interface Layers {
    /** Total number of layers including the base shape (1-4). Absent or 1 disables the effect. */
    count?: number | null;
    /** Vertical distance in pixels between the silhouettes of consecutive layers: each successive layer's silhouette is offset from the previous one by this amount. The divider's own `height` is unchanged. Defaults to 20. */
    offsetY?: number | null;
    /** How the stacked layers fade relative to the base shape. */
    layerEffect?: LayerEffectWithLiterals;
    /** Horizontal distance between consecutive layers in pixels. Defaults to 0. Only applies to shapes that support horizontal offset. */
    offsetX?: number | null;
}
/** Backdrop type */
declare enum BackdropType {
    /** Solid color backdrop */
    COLOR = "COLOR",
    /** Gradient backdrop */
    GRADIENT = "GRADIENT"
}
/** @enumType */
type BackdropTypeWithLiterals = BackdropType | 'COLOR' | 'GRADIENT';
interface LayoutDataBackgroundImage {
    /** Background image. */
    media?: Media;
    /**
     * Deprecated: use `overlay` instead. Legacy image opacity (0–100) that dimmed the image to reveal the `background`/`backdrop` color behind it.
     * @deprecated
     */
    opacity?: number | null;
    /** Background image scaling. */
    scaling?: ImageScalingScalingWithLiterals;
    /** Position of background. Defaults to `CENTER`. */
    position?: LayoutDataImagePositionWithLiterals;
    /** Blur radius in pixels applied to the image layer. `0` (default) leaves the image unblurred; blur is independent of any color overlay and the two stack. */
    blur?: number | null;
    /** Color or gradient drawn on top of the image. When present, this is the authoritative overlay and `opacity` is ignored. Its presence also marks content as authored under the new overlay model (vs. the legacy `opacity`-based dimming on `background`/`backdrop`). */
    overlay?: LayoutDataBackground;
}
declare enum VerticalAlignmentAlignment {
    /** Top alignment */
    TOP = "TOP",
    /** Middle alignment */
    MIDDLE = "MIDDLE",
    /** Bottom alignment */
    BOTTOM = "BOTTOM"
}
/** @enumType */
type VerticalAlignmentAlignmentWithLiterals = VerticalAlignmentAlignment | 'TOP' | 'MIDDLE' | 'BOTTOM';
declare enum ResponsivenessBehaviour {
    /** Stacking of columns */
    STACK = "STACK",
    /** Wrapping of columns */
    WRAP = "WRAP"
}
/** @enumType */
type ResponsivenessBehaviourWithLiterals = ResponsivenessBehaviour | 'STACK' | 'WRAP';
declare enum DesignTarget {
    /** Design applied to layout */
    LAYOUT = "LAYOUT",
    /** Design applied to cells */
    CELL = "CELL"
}
/** @enumType */
type DesignTargetWithLiterals = DesignTarget | 'LAYOUT' | 'CELL';
interface Banner {
    /** Origin of the banner */
    origin?: OriginWithLiterals;
    /** Position of the banner */
    position?: BannerPositionWithLiterals;
}
/** Backdrop styling (color or gradient) */
interface Backdrop {
    /** Backdrop type. */
    type?: BackdropTypeWithLiterals;
    /**
     * Backdrop color as a hexadecimal value.
     * @maxLength 19
     */
    color?: string | null;
    /** Gradient configuration. */
    gradient?: Gradient;
}
/** Decorative shape divider at a layout edge */
interface SectionDivider {
    /** Divider shape preset. Determines which customization properties apply. */
    type?: ShapeWithLiterals;
    /**
     * Divider fill color as a hexadecimal value (may include alpha).
     * @maxLength 19
     */
    color?: string | null;
    /** Divider height in pixels. */
    height?: number | null;
    /** Horizontal offset in pixels. */
    offsetX?: number | null;
    /** Whether the shape is flipped horizontally. */
    flipped?: boolean | null;
    /** Layers effect - stacked semi-transparent copies of the divider shape. */
    layers?: Layers;
}
interface LayoutCellData {
    /** Size of the cell in 12 columns grid. */
    colSpan?: number | null;
}
interface ShapeData {
    /** Styling for the shape's container. */
    containerData?: PluginContainerData;
    /** Shape file details. */
    shape?: Media;
    /** Styling for the shape. */
    styles?: ShapeDataStyles;
    /** Node animation. */
    animation?: Animation;
    /** Link details for shapes that are links. */
    link?: Link;
    /**
     * Alternate text describing the link's purpose for accessibility. Applies only when `link` is set.
     * @maxLength 1000
     */
    altText?: string | null;
}
interface ShapeDataStyles {
    /**
     * Shape fill color as a hexadecimal value.
     * @maxLength 19
     */
    color?: string | null;
    /** Map of original color keys to their new color values. */
    colors?: Record<string, string>;
}
interface CardData {
    /** Background styling (color or gradient). */
    background?: CardDataBackground;
    /** Background image. */
    backgroundImage?: BackgroundImage;
}
declare enum Scaling {
    /** Auto image scaling */
    AUTO = "AUTO",
    /** Contain image scaling */
    CONTAIN = "CONTAIN",
    /** Cover image scaling */
    COVER = "COVER"
}
/** @enumType */
type ScalingWithLiterals = Scaling | 'AUTO' | 'CONTAIN' | 'COVER';
declare enum ImagePositionPosition {
    /** Image positioned at the center */
    CENTER = "CENTER",
    /** Image positioned on the left */
    CENTER_LEFT = "CENTER_LEFT",
    /** Image positioned on the right */
    CENTER_RIGHT = "CENTER_RIGHT",
    /** Image positioned at the center top */
    TOP = "TOP",
    /** Image positioned at the top left */
    TOP_LEFT = "TOP_LEFT",
    /** Image positioned at the top right */
    TOP_RIGHT = "TOP_RIGHT",
    /** Image positioned at the center bottom */
    BOTTOM = "BOTTOM",
    /** Image positioned at the bottom left */
    BOTTOM_LEFT = "BOTTOM_LEFT",
    /** Image positioned at the bottom right */
    BOTTOM_RIGHT = "BOTTOM_RIGHT"
}
/** @enumType */
type ImagePositionPositionWithLiterals = ImagePositionPosition | 'CENTER' | 'CENTER_LEFT' | 'CENTER_RIGHT' | 'TOP' | 'TOP_LEFT' | 'TOP_RIGHT' | 'BOTTOM' | 'BOTTOM_LEFT' | 'BOTTOM_RIGHT';
/** Background styling (color or gradient) */
interface CardDataBackground {
    /** Background type. */
    type?: CardDataBackgroundTypeWithLiterals;
    /**
     * Background color as a hexadecimal value.
     * @maxLength 19
     */
    color?: string | null;
    /** Gradient configuration. */
    gradient?: Gradient;
}
/** Background type */
declare enum CardDataBackgroundType {
    /** Solid color background */
    COLOR = "COLOR",
    /** Gradient background */
    GRADIENT = "GRADIENT"
}
/** @enumType */
type CardDataBackgroundTypeWithLiterals = CardDataBackgroundType | 'COLOR' | 'GRADIENT';
interface BackgroundImage {
    /** Background image. */
    media?: Media;
    /**
     * Deprecated: use `overlay` instead. Legacy image opacity (0–100) that dimmed the image to reveal the `background` color behind it.
     * @deprecated
     */
    opacity?: number | null;
    /** Background image scaling. */
    scaling?: ScalingWithLiterals;
    /** Position of background. Defaults to `CENTER`. */
    position?: ImagePositionPositionWithLiterals;
    /** Color or gradient drawn on top of the image. When present, this is the authoritative overlay and `opacity` is ignored. Its presence also marks content as authored under the new overlay model (vs. the legacy `opacity`-based dimming on `background`). */
    overlay?: CardDataBackground;
    /** Blur radius in pixels applied to the image layer. `0` (default) leaves the image unblurred; blur is independent of any color overlay and the two stack. */
    blur?: number | null;
}
interface TocData {
    /** Heading levels included in the table of contents. Default: [1, 2, 3, 4, 5, 6]. */
    includedHeadings?: number[];
    /** List style. Default: PLAIN. */
    listStyle?: ListStyleWithLiterals;
    /** Optional override for the font size in pixels. */
    fontSize?: number | null;
    /** Optional override for the vertical spacing between items in pixels. */
    itemSpacing?: number | null;
    /**
     * Optional override for the text color.
     * @maxLength 19
     */
    color?: string | null;
    /** Indentation style. Default: NESTED. */
    indentation?: IndentationWithLiterals;
    /** Node animation. */
    animation?: Animation;
}
/** List style. */
declare enum ListStyle {
    /** No markers (default) */
    PLAIN = "PLAIN",
    /** Numbered list */
    NUMBERED = "NUMBERED",
    /** Alphabetic letters */
    LETTERS = "LETTERS",
    /** Roman numerals */
    ROMAN = "ROMAN",
    /** Bulleted list */
    BULLETED = "BULLETED",
    /** Alphabetical index */
    ALPHABETICAL_INDEX = "ALPHABETICAL_INDEX",
    /** Alphabetical index (compact top-row only) */
    ALPHABETICAL_INDEX_COMPACT = "ALPHABETICAL_INDEX_COMPACT"
}
/** @enumType */
type ListStyleWithLiterals = ListStyle | 'PLAIN' | 'NUMBERED' | 'LETTERS' | 'ROMAN' | 'BULLETED' | 'ALPHABETICAL_INDEX' | 'ALPHABETICAL_INDEX_COMPACT';
/** Indentation style. */
declare enum Indentation {
    /** Sub-headings indented under parents (default) */
    NESTED = "NESTED",
    /** All items at the same level */
    FLAT = "FLAT"
}
/** @enumType */
type IndentationWithLiterals = Indentation | 'NESTED' | 'FLAT';
/** Data for a smart block node. */
interface SmartBlockData {
    /** The type of the smart block. */
    type?: SmartBlockDataTypeWithLiterals;
    /** Layout orientation. HORIZONTAL or VERTICAL. Optional for variants with fixed orientation. */
    orientation?: string | null;
    /** Column size controlling cells per row. */
    columnSize?: ColumnSizeWithLiterals;
    /**
     * Border color (for SOLID_JOINED_BOXES variant).
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Border width in pixels (for SOLID_JOINED_BOXES variant). */
    borderWidth?: number | null;
    /** Border radius in pixels (for SOLID_JOINED_BOXES variant). */
    borderRadius?: number | null;
    /** Node animation. */
    animation?: Animation;
}
/** Layout type of the smart block */
declare enum SmartBlockDataType {
    /** Grid-based layouts with solid box items containing title, body, and icon/image. */
    SOLID_BOXES = "SOLID_BOXES",
    /** Numbered boxes. */
    NUMBERED_BOXES = "NUMBERED_BOXES",
    /** Statistics display with large numbers/values. */
    STATS = "STATS",
    /** Statistics with circular visual elements. */
    CIRCLE_STATS = "CIRCLE_STATS",
    /** Staggered/zigzag grid layout with alternating box positions. */
    SOLID_BOXES_ALTERNATING = "SOLID_BOXES_ALTERNATING",
    /** Grid layout with boxes visually joined (no gaps, shared container border). */
    SOLID_JOINED_BOXES = "SOLID_JOINED_BOXES",
    /** Transparent cells with only a left side line. */
    SIDE_LINE_TEXT = "SIDE_LINE_TEXT",
    /** Transparent cells with only a top line. */
    TOP_LINE_TEXT = "TOP_LINE_TEXT",
    /** Outlined boxes with a numbered/icon circle at the top. */
    OUTLINE_BOXES_WITH_TOP_CIRCLE = "OUTLINE_BOXES_WITH_TOP_CIRCLE",
    /** Large icon bullets with text content. */
    BIG_BULLETS = "BIG_BULLETS",
    /** Small dot bullets with text content. */
    SMALL_BULLETS = "SMALL_BULLETS",
    /** Arrow icon bullets with text content. */
    ARROW_BULLETS = "ARROW_BULLETS",
    /** Process steps with numbered/icon labels above a horizontal line. */
    PROCESS_STEPS = "PROCESS_STEPS",
    /** Statistics with bar visual elements. */
    BAR_STATS = "BAR_STATS",
    /** Timeline layout with numbered chips on a connecting line; cells alternate around the line. */
    TIMELINE = "TIMELINE",
    /** Timeline layout with plain dot indicators; no numbers or shapes; cells alternate around the line. */
    MINIMAL_TIMELINE = "MINIMAL_TIMELINE",
    /** Numbered pill-shaped labels (stadium chips) with text content; supports HORIZONTAL (pill on top) and VERTICAL (pill on left) orientations. */
    PILLS = "PILLS",
    /** Star rating display with stars and a numeric value per cell. */
    STAR_RATING = "STAR_RATING",
    /** Outlined boxes with decorative quote glyphs at the top-left and bottom-right corners. */
    QUOTE_BOXES = "QUOTE_BOXES",
    /** Donut/ring with numbered annular-sector segments; cell text labels sit outside the ring. */
    CIRCLE = "CIRCLE",
    /** Hierarchical pyramid where each cell renders as a horizontal slice (apex at top, base at bottom) with the cell number or shape centered inside; text content sits beside (desktop) or below (mobile) the pyramid. */
    PYRAMID = "PYRAMID",
    /** Cells render a horizontal bar whose width scales with cell index, creating a staircase pattern; bar carries the cell number or a custom shape. */
    STAIRCASE = "STAIRCASE",
    /** Hierarchical funnel where each cell renders as a horizontal slice (wide at top, narrowing toward the bottom) with the cell number or shape centered inside; text content sits beside (desktop) or below (mobile) the funnel. */
    VERTICAL_FUNNEL = "VERTICAL_FUNNEL"
}
/** @enumType */
type SmartBlockDataTypeWithLiterals = SmartBlockDataType | 'SOLID_BOXES' | 'NUMBERED_BOXES' | 'STATS' | 'CIRCLE_STATS' | 'SOLID_BOXES_ALTERNATING' | 'SOLID_JOINED_BOXES' | 'SIDE_LINE_TEXT' | 'TOP_LINE_TEXT' | 'OUTLINE_BOXES_WITH_TOP_CIRCLE' | 'BIG_BULLETS' | 'SMALL_BULLETS' | 'ARROW_BULLETS' | 'PROCESS_STEPS' | 'BAR_STATS' | 'TIMELINE' | 'MINIMAL_TIMELINE' | 'PILLS' | 'STAR_RATING' | 'QUOTE_BOXES' | 'CIRCLE' | 'PYRAMID' | 'STAIRCASE' | 'VERTICAL_FUNNEL';
/** Column size controlling how many cells appear per row. */
declare enum ColumnSize {
    /** Up to 4 cells in a row. */
    SMALL = "SMALL",
    /** Up to 3 cells in a row (default). */
    MEDIUM = "MEDIUM",
    /** Up to 2 cells in a row. */
    LARGE = "LARGE",
    /** 1 cell in a row. */
    EXTRA_LARGE = "EXTRA_LARGE"
}
/** @enumType */
type ColumnSizeWithLiterals = ColumnSize | 'SMALL' | 'MEDIUM' | 'LARGE' | 'EXTRA_LARGE';
/** Data for a smart block cell node. */
interface SmartBlockCellData {
    /** Optional label text for the cell (e.g., for stats variants). */
    label?: string | null;
    /** Shape file details. */
    shape?: Media;
    /**
     * Border color of the cell.
     * @maxLength 19
     */
    borderColor?: string | null;
    /** Border width in pixels. */
    borderWidth?: number | null;
    /** Border radius in pixels. */
    borderRadius?: number | null;
    /** The type of the parent smart block (must match parent). */
    type?: SmartBlockDataTypeWithLiterals;
    /**
     * Accent color for non-background variants (e.g., line, bullet, label color).
     * @maxLength 19
     */
    accentColor?: string | null;
    /**
     * Background color for background-based variants (SOLID_BOXES, SOLID_BOXES_ALTERNATING, SOLID_JOINED_BOXES).
     * @maxLength 19
     */
    backgroundColor?: string | null;
    /**
     * Shape fill color as a hexadecimal value.
     * @maxLength 19
     */
    shapeColor?: string | null;
}
interface CheckboxListData {
    /** Indentation level from 0-4. */
    indentation?: number;
    /** Offset level from 0-4. */
    offset?: number | null;
    /** Node animation. */
    animation?: Animation;
}
interface ListItemNodeData {
    /** Checkbox list item state. Defaults to `false`. */
    checked?: boolean | null;
    /** Node animation. */
    animation?: Animation;
}
interface Metadata {
    /** Schema version. */
    version?: number;
    /**
     * When the object was created.
     * @readonly
     * @deprecated
     */
    createdTimestamp?: Date | null;
    /**
     * When the object was most recently updated.
     * @deprecated
     */
    updatedTimestamp?: Date | null;
    /** Object ID. */
    _id?: string | null;
}
interface DocumentStyle {
    /** Styling for H1 nodes. */
    headerOne?: TextNodeStyle;
    /** Styling for H2 nodes. */
    headerTwo?: TextNodeStyle;
    /** Styling for H3 nodes. */
    headerThree?: TextNodeStyle;
    /** Styling for H4 nodes. */
    headerFour?: TextNodeStyle;
    /** Styling for H5 nodes. */
    headerFive?: TextNodeStyle;
    /** Styling for H6 nodes. */
    headerSix?: TextNodeStyle;
    /** Styling for paragraph nodes. */
    paragraph?: TextNodeStyle;
    /** Styling for block quote nodes. */
    blockquote?: TextNodeStyle;
    /** Styling for code block nodes. */
    codeBlock?: TextNodeStyle;
}
interface TextNodeStyle {
    /** The decorations to apply to the node. */
    decorations?: Decoration[];
    /** Padding and background color for the node. */
    nodeStyle?: NodeStyle;
    /** Line height for text in the node. */
    lineHeight?: string | null;
}
interface MediaItem extends MediaItemMediaOneOf {
    /** WixMedia image. */
    image?: string;
}
/** @oneof */
interface MediaItemMediaOneOf {
    /** WixMedia image. */
    image?: string;
}
interface MediaSettings {
    /**
     * Specifies where the image should be displayed relative to the field's label.
     * Default: `BELOW`
     */
    imagePosition?: ImagePositionWithLiterals;
    /**
     * Specifies the image alignment.
     * Default: `CENTER`
     */
    imageAlignment?: AlignmentWithLiterals;
    /**
     * Specifies how an image should be resized to fit.
     * Default: `COVER`
     */
    imageFit?: ImageFitWithLiterals;
}
declare enum ImagePosition {
    /** Above the field's label. */
    ABOVE = "ABOVE",
    /** Below the field's label. */
    BELOW = "BELOW"
}
/** @enumType */
type ImagePositionWithLiterals = ImagePosition | 'ABOVE' | 'BELOW';
declare enum Alignment {
    /** Align left. */
    LEFT = "LEFT",
    /** Align center. */
    CENTER = "CENTER",
    /** Align right. */
    RIGHT = "RIGHT"
}
/** @enumType */
type AlignmentWithLiterals = Alignment | 'LEFT' | 'CENTER' | 'RIGHT';
declare enum ImageFit {
    /** Scale the image to maintain its aspect ratio while filling the element's entire content box. Portions of the image may be clipped if the aspect ratios don't match. */
    COVER = "COVER",
    /** Resize the image to fill the element's content box. The image's original sspect ratio may not be preserved. */
    CONTAIN = "CONTAIN"
}
/** @enumType */
type ImageFitWithLiterals = ImageFit | 'COVER' | 'CONTAIN';
interface EmailOptions {
    /**
     * Acts as a flag for clients (e.g., UI/Form builders) to enforce additional
     * email deliverability validation on their end.
     * When true, the client should run advanced checks (like domain deliverability
     * or strict typo prevention like "..@gmal.com") before submitting the data.
     */
    deliverabilityValidationEnabled?: boolean;
}
interface RadioGroup {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * List of options the submitter can select from.
     * @maxSize 400
     */
    options?: RadioGroupOption[];
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Custom option which can be specified by the submitter. */
    customOption?: CustomOption;
    /**
     * Specifies the number of columns used to display the selections within the component.
     *
     * Default: `ONE`
     */
    numberOfColumns?: NumberOfColumnsWithLiterals;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface RadioGroupOption {
    /**
     * Selectable option label.
     * @maxLength 400
     */
    label?: string | null;
    /**
     * Value stored on submission when this option is selected.
     * @maxLength 20000
     */
    value?: string | null;
    /** Whether this option is selected by default. */
    default?: boolean;
    /**
     * Option ID. Can be used to connect this option with [Wix Multilingual](https://dev.wix.com/docs/api-reference/business-management/multilingual/translation/introduction) for translation.
     * @format GUID
     * @immutable
     */
    _id?: string;
}
interface CustomOption {
    /**
     * Custom option label.
     * @maxLength 350
     */
    label?: string | null;
    /**
     * Custom option placeholder text.
     * @maxLength 100
     */
    placeholder?: string | null;
}
declare enum NumberOfColumns {
    /** Zero columns. */
    ZERO = "ZERO",
    /** One column. */
    ONE = "ONE",
    /** Two columns. */
    TWO = "TWO",
    /** Three columns. */
    THREE = "THREE"
}
/** @enumType */
type NumberOfColumnsWithLiterals = NumberOfColumns | 'ZERO' | 'ONE' | 'TWO' | 'THREE';
interface Dropdown {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * List of options the submitter can select from.
     * @maxSize 400
     */
    options?: DropdownOption[];
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Custom option which can be specified by the submitter. */
    customOption?: CustomOption;
    /**
     * Placeholder text shown when no option is selected.
     * @maxLength 100
     */
    placeholder?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface DropdownOption {
    /**
     * Selectable option label.
     * @maxLength 400
     */
    label?: string | null;
    /**
     * Value stored on submission when this option is selected.
     * @maxLength 20000
     */
    value?: string | null;
    /** Whether this option is selected by default. */
    default?: boolean;
    /**
     * Option ID. Can be used to connect this option with [Wix Multilingual](https://dev.wix.com/docs/api-reference/business-management/multilingual/translation/introduction) for translation.
     * @format GUID
     * @immutable
     */
    _id?: string;
}
interface DateTimeInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Whether to display placeholder text in the field when it's empty.
     *
     * Default: `true`
     */
    showPlaceholder?: boolean | null;
    /**
     * Whether to use 24-hour time format.
     *
     * Default: `false`
     */
    use24HourFormat?: boolean;
    /**
     * Default value for the field. This value is pre-populated in the field when the form loads.
     * Supports static constrains defined as ISO date-time format, as well as dynamic calculations using special keywords
     * such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    default?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
    /**
     * Flag identifying whether to show or hide the date labels.
     * Default: true
     */
    showDateLabels?: boolean | null;
}
interface PhoneInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Placeholder text shown inside the field when it's empty.
     * @maxLength 100
     */
    placeholder?: string | null;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Default country code for the phone number input. Two-letter country code in ISO-3166 alpha-2 format.
     * @minLength 1
     * @maxLength 5
     */
    defaultCountryCode?: string | null;
    /**
     * Whether to display the country flag next to the country code.
     *
     * Default: `false`
     */
    showCountryFlag?: boolean;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface DateInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Whether to display placeholder text in the field when it's empty.
     *
     * Default: `true`
     */
    showPlaceholder?: boolean | null;
    /**
     * Default value for the field. This value is pre-populated in the field when the form loads.
     * Supports static constrains defined as ISO date format, as well as dynamic calculations using special keywords
     * such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    default?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
    /**
     * Flag identifying whether to show or hide the date labels.
     * Default: true
     */
    showDateLabels?: boolean | null;
}
interface TimeInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Field description. */
    description?: RichContent;
    /**
     * Whether to display the label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Whether to display placeholder text in the field when it's empty.
     *
     * Default: `true`
     */
    showPlaceholder?: boolean | null;
    /**
     * Whether to use 24-hour time format.
     *
     * Default: `false`
     */
    use24HourFormat?: boolean;
    /**
     * Default value for the field. This value is pre-populated in the field when the form loads.
     * Supports static constrains defined as ISO time format, as well as dynamic calculations using special keywords
     * such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    default?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface DatePicker {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Placeholder text shown when no date is selected.
     * @maxLength 100
     */
    placeholder?: string | null;
    /** First day of the week displayed on the date picker. */
    firstDayOfWeek?: FirstDayOfWeekWithLiterals;
    /**
     * Default value for the field. This value is pre-populated in the field when the form loads.
     * Supports static constrains defined as ISO date format, as well as dynamic calculations using special keywords
     * such as "$now" to represent the current date and time.
     * The dynamic calculation supports expressions like "$now+2d" (2 days in the future), "$now-1h" (1 hour in the past), etc.
     * The regex pattern for dynamic calculations is: \$now([+-]\d{1,2})([yMdmh])
     * @maxLength 50
     */
    default?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
declare enum FirstDayOfWeek {
    /** First day of the week is Monday. */
    MONDAY = "MONDAY",
    /** First day of the week is Sunday. */
    SUNDAY = "SUNDAY"
}
/** @enumType */
type FirstDayOfWeekWithLiterals = FirstDayOfWeek | 'MONDAY' | 'SUNDAY';
interface ServicesDropdown {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Placeholder text shown when no service is selected.
     * @maxLength 100
     */
    placeholder?: string | null;
    /**
     * List of service options the submitter can select from.
     * @maxSize 400
     */
    options?: ServiceOption[];
    /** Custom option which can be specified by the submitter. */
    customOption?: CustomOption;
    /** Additional description or instructions for the field. */
    description?: RichContent;
}
interface ServiceOption {
    /**
     * Service name/label
     * @maxLength 400
     */
    label?: string | null;
    /**
     * Option id. Used as binding for translations. Corresponds to the Service ID.
     * @format GUID
     * @immutable
     */
    _id?: string;
    /**
     * Selectable option value, which is saved to DB. Corresponds to the Service ID.
     * @format GUID
     * @immutable
     */
    value?: string | null;
}
interface Password {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Placeholder text shown inside the field when it's empty.
     * Useful for providing a hint about the expected format or content.
     * @maxLength 100
     */
    placeholder?: string | null;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Default value for the field. This value is pre-populated in the field when the form loads.
     * @maxLength 20000
     */
    default?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface NumberCorrectAnswersList {
    /** @maxSize 50 */
    correctAnswers?: number[];
}
interface NumberType {
    /** The maximum value of the number. Inclusive. */
    maximum?: number | null;
    /** The minimum value of the number. Inclusive. */
    minimum?: number | null;
    /** A number that the value must be a multiple of. */
    multipleOf?: number | null;
    /**
     * List of allowed values.
     * @maxSize 500
     */
    enum?: number[] | null;
}
declare enum NumberComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Numeric input component. */
    NUMBER_INPUT = "NUMBER_INPUT",
    /** Rating input component. */
    RATING_INPUT = "RATING_INPUT"
}
/** @enumType */
type NumberComponentTypeWithLiterals = NumberComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'NUMBER_INPUT' | 'RATING_INPUT';
/**
 * Quiz scoring for a single question — how many points a correct answer is worth,
 * the correct answer(s), and the messages shown for a correct or incorrect answer.
 */
interface NumberQuizFieldSettings {
    /** Points awarded for correctly answering this quiz question. */
    score?: number;
    /**
     * Correct answer(s) for this quiz question. Returned only to callers with the
     * `WIX_FORMS.QUIZ_ANSWERS_READ` permission, so answers stay hidden from respondents.
     */
    correctAnswersList?: NumberCorrectAnswersList;
    /**
     * Message shown to the respondent when the answer is correct.
     * @maxLength 250
     */
    correctAnswerMessage?: string | null;
    /**
     * Message shown to the respondent when the answer is incorrect.
     * @maxLength 250
     */
    incorrectAnswerMessage?: string | null;
}
interface NumberInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Placeholder text shown inside the field when it's empty.
     * @maxLength 100
     */
    placeholder?: string | null;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Default value for the field. This value is pre-populated in the field when the form loads. */
    default?: number | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface RatingInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Default rating value for the field.
     * @min 1
     * @max 5
     */
    defaultValue?: number | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface BooleanType {
    /**
     * List of allowed values.
     * @maxSize 2
     */
    enum?: boolean[];
}
declare enum BooleanComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Checkbox component. */
    CHECKBOX = "CHECKBOX"
}
/** @enumType */
type BooleanComponentTypeWithLiterals = BooleanComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'CHECKBOX';
interface Checkbox {
    /** Field label. */
    label?: RichContent;
    /**
     * Whether the checkbox is selected by default when the form loads.
     *
     * Default: `false`
     */
    checked?: boolean;
}
interface CorrectAnswersList {
    /** @maxSize 50 */
    correctAnswers?: any[];
}
interface ArrayType {
    /**
     * Maximum number of elements allowed in the array.
     * @max 1000
     */
    maxItems?: number | null;
    /**
     * Minimum number of elements required in the array.
     * @max 1000
     */
    minItems?: number | null;
    /** Type of items allowed in the array. */
    items?: ArrayItems;
}
declare enum ItemType {
    /** Array can contain string values. */
    STRING = "STRING",
    /** Array can contain numeric values. */
    NUMBER = "NUMBER",
    /** Array can contain boolean values. */
    BOOLEAN = "BOOLEAN"
}
/** @enumType */
type ItemTypeWithLiterals = ItemType | 'STRING' | 'NUMBER' | 'BOOLEAN';
interface ObjectType {
    /** Definition of object properties and their validation rules. */
    properties?: Record<string, PropertiesType>;
}
declare enum PropertiesTypeEnum {
    /** Property must contain a string value. */
    STRING = "STRING",
    /** Property must contain a numeric value. */
    NUMBER = "NUMBER",
    /** Property must contain a boolean value. */
    BOOLEAN = "BOOLEAN",
    /** Property must contain an array. */
    ARRAY = "ARRAY"
}
/** @enumType */
type PropertiesTypeEnumWithLiterals = PropertiesTypeEnum | 'STRING' | 'NUMBER' | 'BOOLEAN' | 'ARRAY';
interface PropertiesType extends PropertiesTypePropertiesTypeOptionsOneOf {
    /** Validation rules for string properties. */
    stringOptions?: StringType;
    /** Validation rules for numeric properties. */
    numberOptions?: NumberType;
    /** Validation rules for boolean properties. */
    booleanOptions?: BooleanType;
    /** Validation rules for array properties. */
    arrayOptions?: ArrayType;
    /**
     * Data type of the object property.
     * @readonly
     */
    propertiesType?: PropertiesTypeEnumWithLiterals;
    /** Whether the object property is required for validation. */
    required?: boolean;
}
/** @oneof */
interface PropertiesTypePropertiesTypeOptionsOneOf {
    /** Validation rules for string properties. */
    stringOptions?: StringType;
    /** Validation rules for numeric properties. */
    numberOptions?: NumberType;
    /** Validation rules for boolean properties. */
    booleanOptions?: BooleanType;
    /** Validation rules for array properties. */
    arrayOptions?: ArrayType;
}
interface ArrayItems extends ArrayItemsItemTypeOptionsOneOf {
    /** Validation rules for string array elements. */
    stringOptions?: StringType;
    /** Validation rules for numeric array elements. */
    numberOptions?: NumberType;
    /** Validation rules for boolean array elements. */
    booleanOptions?: BooleanType;
    /** Allowed item type. */
    itemType?: ItemTypeWithLiterals;
}
/** @oneof */
interface ArrayItemsItemTypeOptionsOneOf {
    /** Validation rules for string array elements. */
    stringOptions?: StringType;
    /** Validation rules for numeric array elements. */
    numberOptions?: NumberType;
    /** Validation rules for boolean array elements. */
    booleanOptions?: BooleanType;
}
declare enum ArrayComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Checkbox group component. */
    CHECKBOX_GROUP = "CHECKBOX_GROUP",
    /** Tags component. */
    TAGS = "TAGS",
    SERVICES_CHECKBOX_GROUP = "SERVICES_CHECKBOX_GROUP"
}
/** @enumType */
type ArrayComponentTypeWithLiterals = ArrayComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'CHECKBOX_GROUP' | 'TAGS' | 'SERVICES_CHECKBOX_GROUP';
/**
 * Quiz scoring for a single question — how many points a correct answer is worth,
 * the correct answer(s), and the messages shown for a correct or incorrect answer.
 */
interface QuizFieldSettings {
    /** Points awarded for correctly answering this quiz question. */
    score?: number;
    /**
     * Correct answer(s) for this quiz question. Returned only to callers with the
     * `WIX_FORMS.QUIZ_ANSWERS_READ` permission, so answers stay hidden from respondents.
     */
    correctAnswersList?: CorrectAnswersList;
    /**
     * Message shown to the respondent when the answer is correct.
     * @maxLength 250
     */
    correctAnswerMessage?: string | null;
    /**
     * Message shown to the respondent when the answer is incorrect.
     * @maxLength 250
     */
    incorrectAnswerMessage?: string | null;
}
interface CheckboxGroup {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * List of options the submitter can select from.
     * @maxSize 400
     */
    options?: Option[];
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Custom option which can be specified by the submitter. */
    customOption?: CustomOption;
    /**
     * Specifies the number of columns used to display the selections within the component.
     *
     * Default: `ONE`
     */
    numberOfColumns?: NumberOfColumnsWithLiterals;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
    /** Display settings for `Option.media`, applied uniformly to all options. */
    optionsMediaSettings?: OptionsMediaSettings;
}
interface Option {
    /**
     * Selectable option label.
     * @maxLength 400
     */
    label?: string | null;
    /** Value stored on submission when this option is selected. */
    value?: any;
    /** Whether this option is selected by default. */
    default?: boolean;
    /**
     * Option ID. Can be used to connect this option with [Wix Multilingual](https://dev.wix.com/docs/api-reference/business-management/multilingual/translation/introduction) for translation.
     * @format GUID
     * @immutable
     */
    _id?: string;
    /** Media content associated with this option. */
    media?: MediaItem;
    /**
     * Whether to display the option label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
}
interface OptionsMediaSettings {
    /**
     * Specifies the image alignment.
     * Default: `CENTER`
     */
    imageAlignment?: AlignmentWithLiterals;
    /**
     * Specifies how an image should be resized to fit.
     * Default: `COVER`
     */
    imageFit?: ImageFitWithLiterals;
}
interface ComponentsTags {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * List of options the submitter can select from.
     * @maxSize 400
     */
    options?: TagsOption[];
    /**
     * Whether to display the label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Custom option which can be specified by the submitter. */
    customOption?: CustomOption;
    /**
     * Specifies the number of columns used to display the selections within the component.
     *
     * Default: `ONE`
     */
    numberOfColumns?: NumberOfColumnsWithLiterals;
    /** Media item. Media, associated with field, like image. */
    media?: MediaItem;
    /** Configuration for the media item. */
    mediaSettings?: MediaSettings;
}
interface TagsOption {
    /**
     * Selectable option label.
     * @maxLength 400
     */
    label?: string | null;
    /** Value stored on submission when this option is selected. */
    value?: any;
    /** Flag identifying that option should be selected by default. */
    default?: boolean;
    /**
     * Option id. Used as binding for translations
     * @format GUID
     * @immutable
     */
    _id?: string;
    /** Media content associated with the option, such as an image. */
    media?: MediaItem;
}
interface ServicesCheckboxGroup {
    /**
     * Label of the field
     * @maxLength 350
     */
    label?: string | null;
    /** Description of the field */
    description?: RichContent;
    /**
     * List of options to select from
     * @maxSize 100
     */
    options?: ServiceOption[];
    /**
     * Flag identifying to hide or not label
     * Default: true
     */
    showLabel?: boolean | null;
    /** Option which can be specified by UoU, enabled when this object is specified. */
    customOption?: CustomOption;
    /**
     * Specifies the number of columns used to display the selections within the component.
     * Default: ONE
     */
    numberOfColumns?: NumberOfColumnsWithLiterals;
}
declare enum WixFileComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** File upload component. */
    FILE_UPLOAD = "FILE_UPLOAD",
    /** Signature component. */
    SIGNATURE = "SIGNATURE"
}
/** @enumType */
type WixFileComponentTypeWithLiterals = WixFileComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'FILE_UPLOAD' | 'SIGNATURE';
interface FileType {
    /**
     * Maximum number of files that can be uploaded.
     * @min 1
     * @max 30
     */
    fileLimit?: number;
    /**
     * Supported file formats for uploaded files.
     * @maxSize 6
     */
    uploadFileFormats?: UploadFileFormatWithLiterals[];
}
declare enum UploadFileFormat {
    /** Video files. */
    VIDEO = "VIDEO",
    /** Image files. */
    IMAGE = "IMAGE",
    /** Audio files. */
    AUDIO = "AUDIO",
    /** Document files. */
    DOCUMENT = "DOCUMENT",
    /** Archive files. */
    ARCHIVE = "ARCHIVE",
    /** 3D model files. */
    MODEL_3D = "MODEL_3D"
}
/** @enumType */
type UploadFileFormatWithLiterals = UploadFileFormat | 'VIDEO' | 'IMAGE' | 'AUDIO' | 'DOCUMENT' | 'ARCHIVE' | 'MODEL_3D';
interface FileUpload {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Text displayed on the upload button.
     * @maxLength 500
     */
    buttonText?: string | null;
    /**
     * Custom text displayed when a file is uploaded. If not specified, the file name is shown.
     * @maxLength 255
     */
    explanationText?: string | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface Signature {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /** Whether image upload is enabled for the signature field. */
    imageUploadEnabled?: boolean;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media item. */
    mediaSettings?: MediaSettings;
}
declare enum PaymentComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Checkbox group component. */
    CHECKBOX_GROUP = "CHECKBOX_GROUP",
    /** Donation input component. */
    DONATION_INPUT = "DONATION_INPUT",
    /** Payment input component. */
    PAYMENT_INPUT = "PAYMENT_INPUT",
    /** Fixed payment component. */
    FIXED_PAYMENT = "FIXED_PAYMENT"
}
/** @enumType */
type PaymentComponentTypeWithLiterals = PaymentComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'CHECKBOX_GROUP' | 'DONATION_INPUT' | 'PAYMENT_INPUT' | 'FIXED_PAYMENT';
interface ProductCheckboxGroup {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * List of product options the submitter can select from.
     * @maxSize 400
     */
    options?: ProductCheckboxGroupOption[];
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * How an image should be resized to fit within its option container.
     *
     * Default: `COVER`
     */
    imageFit?: ImageFitWithLiterals;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface ProductCheckboxGroupOption {
    /**
     * Selectable option label.
     * @maxLength 400
     */
    label?: string | null;
    /** Value stored on submission when this option is selected. Must match a product ID found in the field's products list. */
    value?: any;
    /**
     * Option ID. Can be used to connect this option with [Wix Multilingual](https://dev.wix.com/docs/api-reference/business-management/multilingual/translation/introduction) for translation.
     * @format GUID
     * @immutable
     */
    _id?: string;
    /** Media content associated with this option, such as an image. */
    media?: MediaItem;
    /** Whether this option is selected by default. */
    default?: boolean;
}
interface DonationInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * List of donation amount options the submitter can select from.
     * @maxSize 400
     */
    options?: DonationInputOption[];
    /** Custom option which can be specified by the submitter. */
    customOption?: CustomOption;
    /**
     * Specifies the number of columns used to display the selections within the component.
     *
     * Default: `ONE`
     */
    numberOfColumns?: NumberOfColumnsWithLiterals;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface DonationInputOption {
    /**
     * Value stored on submission when this option is selected. Must match a product ID found in the field's products list.
     * @format GUID
     */
    value?: string;
    /** Whether this option is selected by default. */
    default?: boolean;
}
interface PaymentInput {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Placeholder text shown inside the field when it's empty.
     * @maxLength 100
     */
    placeholder?: string | null;
    /** Default value for the field. This value is pre-populated in the field when the form loads. */
    default?: number | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
interface FixedPayment {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /** Media content associated with the field, such as an image. */
    media?: MediaItem;
    /** Configuration for the media content. */
    mediaSettings?: MediaSettings;
}
declare enum ComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Multiline address component. */
    MULTILINE_ADDRESS = "MULTILINE_ADDRESS"
}
/** @enumType */
type ComponentTypeWithLiterals = ComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'MULTILINE_ADDRESS';
interface MultilineAddress {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /** Whether to display country flags in the country selection. */
    showCountryFlags?: boolean;
    /** Default country configuration for the address field. */
    defaultCountryConfig?: DefaultCountryConfig;
    /** Fields settings. */
    fieldSettings?: FieldsSettings;
    /** Whether autocomplete is enabled for the address line field. */
    autocompleteEnabled?: boolean;
    /**
     * Flag identifying whether to show or hide the address labels.
     * Default: true
     */
    showAddressLabels?: boolean | null;
    /**
     * Flag identifying whether to show or hide the label.
     * Default: true
     */
    showLabel?: boolean | null;
}
declare enum Type {
    /** Unknown default country configuration type. */
    UNKNOWN_DEFAULT_COUNTRY = "UNKNOWN_DEFAULT_COUNTRY",
    /** Country is determined by the submitter's IP address. */
    BY_IP = "BY_IP",
    /** Pre-selected default country. */
    COUNTRY = "COUNTRY"
}
/** @enumType */
type TypeWithLiterals = Type | 'UNKNOWN_DEFAULT_COUNTRY' | 'BY_IP' | 'COUNTRY';
interface AddressLine2 {
    /** Whether to display the address line 2 field. */
    show?: boolean;
}
interface DefaultCountryConfig extends DefaultCountryConfigOptionsOneOf {
    /**
     * Country code for the pre-selected default country. Two-letter country code in ISO-3166 alpha-2 format.
     * @format COUNTRY
     */
    countryOptions?: string;
    /** Default country type. */
    type?: TypeWithLiterals;
}
/** @oneof */
interface DefaultCountryConfigOptionsOneOf {
    /**
     * Country code for the pre-selected default country. Two-letter country code in ISO-3166 alpha-2 format.
     * @format COUNTRY
     */
    countryOptions?: string;
}
interface FieldsSettings {
    /** Configuration for the address line 2 field. */
    addressLine2?: AddressLine2;
}
declare enum ObjectArrayComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Repeater component. */
    REPEATER = "REPEATER"
}
/** @enumType */
type ObjectArrayComponentTypeWithLiterals = ObjectArrayComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'REPEATER';
interface Repeater {
    /**
     * Field label.
     * @maxLength 350
     */
    label?: string | null;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
    /**
     * Form fields that define the input elements inside repeater.
     * @maxSize 10
     */
    fields?: Field[];
    /** Layout configuration that defines how form fields are arranged within this repeater. */
    layout?: FormLayout;
}
interface FormLayout {
    /** Layout configuration for large screen breakpoints (desktop). */
    large?: BreakPoint;
    /** Layout configuration for medium screen breakpoints (tablet). */
    medium?: BreakPoint;
    /** Layout configuration for small screen breakpoints (mobile). */
    small?: BreakPoint;
}
interface BreakPoint {
    /**
     * Configuration for the layout of items in the breakpoint.
     * Every field of the step must have exactly 1 corresponding item in each breakpoint that is defined.
     * @maxSize 500
     */
    items?: ItemLayout[];
    /** Number of columns in the layout grid. */
    columns?: number | null;
    /** Height of each row in the layout grid, measured in pixels. */
    rowHeight?: number | null;
    /** Margin spacing configuration for the form elements. */
    margin?: Margin;
    /** Padding spacing inside form elements in pixels. */
    padding?: Margin;
}
interface ItemLayout extends ItemLayoutItemOneOf {
    /**
     * ID of the form field to be positioned in the layout.
     * @format GUID
     */
    fieldId?: string;
    /** Group configuration for positioning multiple fields as a single layout unit. */
    group?: Group;
    /**
     * Row position in the grid where this item is placed.
     *
     * Required. `row`, `column`, `width`, and `height` have no defaults. An item missing
     * any of them isn't rendered, so the field doesn't appear in the form.
     */
    row?: number | null;
    /** Column position in the grid where this item is placed. Required. */
    column?: number | null;
    /** Width of the item, in grid columns. Full width in the default 12-column grid is `12`. Required. */
    width?: number | null;
    /** Height of the item, in grid rows. `1` for most field types. Required. */
    height?: number | null;
}
/** @oneof */
interface ItemLayoutItemOneOf {
    /**
     * ID of the form field to be positioned in the layout.
     * @format GUID
     */
    fieldId?: string;
    /** Group configuration for positioning multiple fields as a single layout unit. */
    group?: Group;
}
interface Group {
    /**
     * ID of the field group.
     * @format GUID
     */
    groupId?: string;
    /**
     * Layout configuration for individual items within this group.
     * @minSize 1
     * @maxSize 500
     */
    items?: ItemLayout[];
}
interface Margin {
    /** Horizontal spacing in pixels. */
    horizontal?: number | null;
    /** Vertical spacing in pixels. */
    vertical?: number | null;
}
interface Section {
    /**
     * Section ID.
     * @format GUID
     * @minLength 1
     */
    _id?: string;
    /** Row position where this section is placed in the grid. */
    row?: number | null;
    /**
     * A list of field IDs that can be placed within the section.
     * The section only accepts fields with IDs specified in this list.
     * If the `$new` key is contained in this list, the section allows the inclusion of fields not explicitly listed, enabling dynamic addition of new fields.
     * @maxSize 100
     * @maxLength 100
     */
    allowedFieldIds?: string[];
}
declare enum SchedulingComponentType {
    /** Unknown component type. */
    UNKNOWN_COMPONENT_TYPE = "UNKNOWN_COMPONENT_TYPE",
    /** Appointment component. */
    APPOINTMENT = "APPOINTMENT"
}
/** @enumType */
type SchedulingComponentTypeWithLiterals = SchedulingComponentType | 'UNKNOWN_COMPONENT_TYPE' | 'APPOINTMENT';
interface Appointment extends AppointmentFormatInfoOneOf {
    /** Configuration for in-person appointments. */
    inPersonOptions?: InPersonOptions;
    /** Configuration for video conference appointments. */
    videoConferenceOptions?: VideoConferenceOptions;
    /** Configuration for phone appointments. */
    phoneOptions?: PhoneOptions;
    /**
     * Field label.
     * @maxLength 255
     */
    label?: string | null;
    /**
     * Appointment name.
     * @minLength 1
     * @maxLength 400
     */
    name?: string | null;
    /**
     * Duration of the appointment in minutes.
     * @min 1
     * @max 44639
     */
    durationInMinutes?: number | null;
    /** Whether appointments require manual approval before confirmation. */
    manualApprovalRequired?: boolean | null;
    /**
     * IDs of staff members who provide this service.
     * @maxSize 220
     * @format GUID
     */
    staffIds?: string[] | null;
    /** Appointment format. */
    format?: FormatWithLiterals;
    /** Additional description or instructions for the field. */
    description?: RichContent;
    /**
     * Whether to display the field label.
     *
     * Default: `true`
     */
    showLabel?: boolean | null;
}
/** @oneof */
interface AppointmentFormatInfoOneOf {
    /** Configuration for in-person appointments. */
    inPersonOptions?: InPersonOptions;
    /** Configuration for video conference appointments. */
    videoConferenceOptions?: VideoConferenceOptions;
    /** Configuration for phone appointments. */
    phoneOptions?: PhoneOptions;
}
declare enum WaitType {
    /** Customers can rebook immediately; no waiting period. */
    NO_WAIT = "NO_WAIT",
    /** Customers must wait a fixed period before rebooking. */
    WAIT = "WAIT",
    /** Customers can never rebook the same service. */
    NO_REBOOKING = "NO_REBOOKING"
}
/** @enumType */
type WaitTypeWithLiterals = WaitType | 'NO_WAIT' | 'WAIT' | 'NO_REBOOKING';
interface WaitOptions {
    /**
     * Minutes a customer must wait before rebooking the same service.
     * @min 1
     * @max 5256000
     */
    minutes?: number | null;
}
interface Location extends LocationLocationInfoOneOf {
    /**
     * Custom address specified as a text string.
     * @maxLength 512
     */
    customAddress?: string | null;
    /**
     * ID of a predefined business [location](https://dev.wix.com/docs/rest/crm/members-contacts/locations/locations/introduction).
     * @format GUID
     */
    businessLocationId?: string | null;
}
/** @oneof */
interface LocationLocationInfoOneOf {
    /**
     * Custom address specified as a text string.
     * @maxLength 512
     */
    customAddress?: string | null;
    /**
     * ID of a predefined business [location](https://dev.wix.com/docs/rest/crm/members-contacts/locations/locations/introduction).
     * @format GUID
     */
    businessLocationId?: string | null;
}
declare enum Format {
    /** Face-to-face meeting at a physical location. */
    IN_PERSON = "IN_PERSON",
    /** Online meeting via video conference. */
    VIDEO_CONFERENCE = "VIDEO_CONFERENCE",
    /** Appointment conducted over the phone. */
    PHONE = "PHONE"
}
/** @enumType */
type FormatWithLiterals = Format | 'IN_PERSON' | 'VIDEO_CONFERENCE' | 'PHONE';
declare enum MeetingType {
    /** Single staff member provides the service. */
    SINGLE_STAFF = "SINGLE_STAFF",
    /** Multiple staff members rotate via round robin scheduling. */
    ROUND_ROBIN = "ROUND_ROBIN"
}
/** @enumType */
type MeetingTypeWithLiterals = MeetingType | 'SINGLE_STAFF' | 'ROUND_ROBIN';
declare enum StaffStrategySelection {
    /** Staff selected randomly. */
    AUTOMATIC = "AUTOMATIC",
    /** Staff sorted by priority, highest first. */
    HIGH_TO_LOW_PRIORITY = "HIGH_TO_LOW_PRIORITY",
    /** Staff sorted by priority, lowest first. */
    LOW_TO_HIGH_PRIORITY = "LOW_TO_HIGH_PRIORITY"
}
/** @enumType */
type StaffStrategySelectionWithLiterals = StaffStrategySelection | 'AUTOMATIC' | 'HIGH_TO_LOW_PRIORITY' | 'LOW_TO_HIGH_PRIORITY';
interface RebookingPolicy extends RebookingPolicyOptionsOneOf {
    /** Waiting-period configuration. Set only when `waitType` is `WAIT`. */
    waitOptions?: WaitOptions;
    /**
     * How the waiting period before rebooking the same service is applied.
     *
     * Default: `NO_WAIT`
     */
    waitType?: WaitTypeWithLiterals;
}
/** @oneof */
interface RebookingPolicyOptionsOneOf {
    /** Waiting-period configuration. Set only when `waitType` is `WAIT`. */
    waitOptions?: WaitOptions;
}
interface InPersonOptions {
    /**
     * Physical locations where the appointment can take place.
     * @minSize 1
     * @maxSize 1
     */
    locations?: Location[];
}
interface VideoConferenceOptions {
    /**
     * Description or instructions for video conference appointments.
     * @maxLength 512
     */
    description?: string | null;
}
interface PhoneOptions {
    /**
     * Description or instructions for phone appointments.
     * @maxLength 512
     */
    description?: string | null;
}
declare enum InputType {
    /** Unknown input type. */
    UNKNOWN_INPUT_TYPE = "UNKNOWN_INPUT_TYPE",
    /** String input. */
    STRING = "STRING",
    /** Numeric input. */
    NUMBER = "NUMBER",
    /** Boolean input. */
    BOOLEAN = "BOOLEAN",
    /** Array input. */
    ARRAY = "ARRAY",
    /** Object input. */
    OBJECT = "OBJECT",
    /** File upload input. */
    WIX_FILE = "WIX_FILE",
    /** Payment input. */
    PAYMENT = "PAYMENT",
    /** Scheduling input. */
    SCHEDULING = "SCHEDULING",
    /** Address input. */
    ADDRESS = "ADDRESS"
}
/** @enumType */
type InputTypeWithLiterals = InputType | 'UNKNOWN_INPUT_TYPE' | 'STRING' | 'NUMBER' | 'BOOLEAN' | 'ARRAY' | 'OBJECT' | 'WIX_FILE' | 'PAYMENT' | 'SCHEDULING' | 'ADDRESS';
interface FormFieldContactInfo extends FormFieldContactInfoAdditionalInfoOneOf {
    /** Configuration for email contact fields. */
    emailInfo?: EmailInfo;
    /** Configuration for phone contact fields. */
    phoneInfo?: PhoneInfo;
    /** Configuration for address contact fields. */
    addressInfo?: AddressInfo;
    /** Configuration for custom contact fields. */
    customFieldInfo?: CustomFieldInfo;
    /** Configuration for subscription contact fields. */
    subscriptionInfo?: SubscriptionInfo;
    /** The [Contact](https://dev.wix.com/docs/rest/crm/members-contacts/contacts/contacts/introduction) field that this form field maps to. */
    contactField?: ContactFieldWithLiterals;
}
/** @oneof */
interface FormFieldContactInfoAdditionalInfoOneOf {
    /** Configuration for email contact fields. */
    emailInfo?: EmailInfo;
    /** Configuration for phone contact fields. */
    phoneInfo?: PhoneInfo;
    /** Configuration for address contact fields. */
    addressInfo?: AddressInfo;
    /** Configuration for custom contact fields. */
    customFieldInfo?: CustomFieldInfo;
    /** Configuration for subscription contact fields. */
    subscriptionInfo?: SubscriptionInfo;
}
declare enum EmailInfoTag {
    /** Untagged email address. */
    UNTAGGED = "UNTAGGED",
    /** Primary email address for the contact. */
    MAIN = "MAIN"
}
/** @enumType */
type EmailInfoTagWithLiterals = EmailInfoTag | 'UNTAGGED' | 'MAIN';
declare enum PhoneInfoTag {
    /** Untagged phone number. */
    UNTAGGED = "UNTAGGED",
    /** Primary phone number for the contact. */
    MAIN = "MAIN"
}
/** @enumType */
type PhoneInfoTagWithLiterals = PhoneInfoTag | 'UNTAGGED' | 'MAIN';
declare enum Tag {
    /** Untagged address. */
    UNTAGGED = "UNTAGGED",
    /** Home address. */
    HOME = "HOME"
}
/** @enumType */
type TagWithLiterals = Tag | 'UNTAGGED' | 'HOME';
declare enum ConfirmationLevel {
    /** Unknown confirmation level. */
    UNKNOWN_CONFIRMATION_LEVEL = "UNKNOWN_CONFIRMATION_LEVEL",
    /** Single confirmation level. */
    SINGLE_CONFIRMATION = "SINGLE_CONFIRMATION",
    /** Double confirmation level. */
    DOUBLE_CONFIRMATION = "DOUBLE_CONFIRMATION"
}
/** @enumType */
type ConfirmationLevelWithLiterals = ConfirmationLevel | 'UNKNOWN_CONFIRMATION_LEVEL' | 'SINGLE_CONFIRMATION' | 'DOUBLE_CONFIRMATION';
declare enum SubscriptionChannel {
    /** Email subscription channel. */
    EMAIL = "EMAIL",
    /** SMS subscription channel. */
    SMS = "SMS"
}
/** @enumType */
type SubscriptionChannelWithLiterals = SubscriptionChannel | 'EMAIL' | 'SMS';
declare enum ContactField {
    /** Contact's first name. */
    FIRST_NAME = "FIRST_NAME",
    /** Contact's last name. */
    LAST_NAME = "LAST_NAME",
    /** Contact's company or organization. */
    COMPANY = "COMPANY",
    /** Contact's job title or position. */
    POSITION = "POSITION",
    /** Contact's email address. */
    EMAIL = "EMAIL",
    /** Contact's phone number. */
    PHONE = "PHONE",
    /** Contact's physical address. */
    ADDRESS = "ADDRESS",
    /** Contact's birth date. */
    BIRTHDATE = "BIRTHDATE",
    /** Custom contact field defined by the site owner. */
    CUSTOM_FIELD = "CUSTOM_FIELD",
    /** Contact's subscription preferences. */
    SUBSCRIPTION = "SUBSCRIPTION",
    /** Contact's VAT identification number. */
    VAT_ID = "VAT_ID"
}
/** @enumType */
type ContactFieldWithLiterals = ContactField | 'FIRST_NAME' | 'LAST_NAME' | 'COMPANY' | 'POSITION' | 'EMAIL' | 'PHONE' | 'ADDRESS' | 'BIRTHDATE' | 'CUSTOM_FIELD' | 'SUBSCRIPTION' | 'VAT_ID';
interface EmailInfo {
    /** Email categorization tag. */
    tag?: EmailInfoTagWithLiterals;
}
interface PhoneInfo {
    /** Phone number categorization tag. */
    tag?: PhoneInfoTagWithLiterals;
}
interface AddressInfo {
    /** Address categorization tag. */
    tag?: TagWithLiterals;
}
interface CustomFieldInfo {
    /**
     * Custom field key.
     * @minLength 1
     * @maxLength 500
     */
    key?: string;
}
interface SubscriptionInfo {
    /**
     * Subscription consent opt-in level, either single or double confirmation.
     * Default: `SINGLE_CONFIRMATION`
     */
    confirmationLevel?: ConfirmationLevelWithLiterals;
    /**
     * Subscription channels.
     * When not specified, defaults to `EMAIL`.
     * @maxSize 2
     */
    subscriptionChannels?: SubscriptionChannelWithLiterals[];
}
interface _String extends _StringComponentTypeOptionsOneOf {
    /** Text input component settings. */
    textInputOptions?: TextInput;
    /** Radio group component settings. */
    radioGroupOptions?: RadioGroup;
    /** Dropdown component settings. */
    dropdownOptions?: Dropdown;
    /** Date and time input component settings. */
    dateTimeOptions?: DateTimeInput;
    /** Phone input component settings. */
    phoneInputOptions?: PhoneInput;
    /** Date input component settings. */
    dateInputOptions?: DateInput;
    /** Time input component settings. */
    timeInputOptions?: TimeInput;
    /** Date picker component settings. */
    datePickerOptions?: DatePicker;
    /**
     * Services dropdown component settings. Each option's `value` is a Wix Services service ID.
     *
     * > **Note:** Service fields require the Wix Services app to be installed on the site. Creating a form schema with a service field on a site without the app fails.
     */
    servicesDropdownOptions?: ServicesDropdown;
    /** Password component settings. */
    passwordOptions?: Password;
    /**
     * Validation configuration for the string input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: StringType;
    /** Component type of the string input field. */
    componentType?: StringComponentTypeWithLiterals;
    /**
     * Quiz scoring for this field — the points a correct answer is worth and the
     * correct answer(s). Set only on fields that are quiz questions.
     */
    quizFieldSettings?: StringQuizFieldSettings;
}
/** @oneof */
interface _StringComponentTypeOptionsOneOf {
    /** Text input component settings. */
    textInputOptions?: TextInput;
    /** Radio group component settings. */
    radioGroupOptions?: RadioGroup;
    /** Dropdown component settings. */
    dropdownOptions?: Dropdown;
    /** Date and time input component settings. */
    dateTimeOptions?: DateTimeInput;
    /** Phone input component settings. */
    phoneInputOptions?: PhoneInput;
    /** Date input component settings. */
    dateInputOptions?: DateInput;
    /** Time input component settings. */
    timeInputOptions?: TimeInput;
    /** Date picker component settings. */
    datePickerOptions?: DatePicker;
    /**
     * Services dropdown component settings. Each option's `value` is a Wix Services service ID.
     *
     * > **Note:** Service fields require the Wix Services app to be installed on the site. Creating a form schema with a service field on a site without the app fails.
     */
    servicesDropdownOptions?: ServicesDropdown;
    /** Password component settings. */
    passwordOptions?: Password;
}
interface _Number extends _NumberComponentTypeOptionsOneOf {
    /** Number input settings. */
    numberInputOptions?: NumberInput;
    /** Rating input settings. */
    ratingInputOptions?: RatingInput;
    /**
     * Validation configuration for the number input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: NumberType;
    /** Component type of the number input field. */
    componentType?: NumberComponentTypeWithLiterals;
    /**
     * Quiz scoring for this field — the points a correct answer is worth and the
     * correct answer(s). Set only on fields that are quiz questions.
     */
    quizFieldSettings?: NumberQuizFieldSettings;
}
/** @oneof */
interface _NumberComponentTypeOptionsOneOf {
    /** Number input settings. */
    numberInputOptions?: NumberInput;
    /** Rating input settings. */
    ratingInputOptions?: RatingInput;
}
interface _Boolean extends _BooleanComponentTypeOptionsOneOf {
    /** Checkbox component settings. */
    checkboxOptions?: Checkbox;
    /**
     * Validation configuration for the boolean input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: BooleanType;
    /** Component type of the boolean input field. */
    componentType?: BooleanComponentTypeWithLiterals;
}
/** @oneof */
interface _BooleanComponentTypeOptionsOneOf {
    /** Checkbox component settings. */
    checkboxOptions?: Checkbox;
}
interface _Array extends _ArrayComponentTypeOptionsOneOf {
    /** Checkbox group component settings. */
    checkboxGroupOptions?: CheckboxGroup;
    /** Tags component settings. */
    tagsOptions?: ComponentsTags;
    /**
     * Services checkbox group component settings. Each option's `value` is a Wix Services service ID.
     *
     * > **Note:** Service fields require the Wix Services app to be installed on the site. Creating a form schema with a service field on a site without the app fails.
     */
    servicesCheckboxGroupOptions?: ServicesCheckboxGroup;
    /**
     * Validation configuration for the array input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: ArrayType;
    /** Component type of the array input field. */
    componentType?: ArrayComponentTypeWithLiterals;
    /**
     * Quiz scoring for this field — the points a correct answer is worth and the
     * correct answer(s). Set only on fields that are quiz questions.
     */
    quizFieldSettings?: QuizFieldSettings;
}
/** @oneof */
interface _ArrayComponentTypeOptionsOneOf {
    /** Checkbox group component settings. */
    checkboxGroupOptions?: CheckboxGroup;
    /** Tags component settings. */
    tagsOptions?: ComponentsTags;
    /**
     * Services checkbox group component settings. Each option's `value` is a Wix Services service ID.
     *
     * > **Note:** Service fields require the Wix Services app to be installed on the site. Creating a form schema with a service field on a site without the app fails.
     */
    servicesCheckboxGroupOptions?: ServicesCheckboxGroup;
}
interface _Object {
    /**
     * Validation configuration for the object input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: ObjectType;
}
interface WixFile extends WixFileComponentTypeOptionsOneOf {
    /** File upload component settings. */
    fileUploadOptions?: FileUpload;
    /** Signature component settings. */
    signatureOptions?: Signature;
    /** Component type of the file input field. */
    componentType?: WixFileComponentTypeWithLiterals;
    /**
     * Validation configuration for the file input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: FileType;
}
/** @oneof */
interface WixFileComponentTypeOptionsOneOf {
    /** File upload component settings. */
    fileUploadOptions?: FileUpload;
    /** Signature component settings. */
    signatureOptions?: Signature;
}
interface Payment extends PaymentComponentTypeOptionsOneOf {
    /** Checkbox group component settings. */
    checkboxGroupOptions?: ProductCheckboxGroup;
    /** Donation input component settings. */
    donationInputOptions?: DonationInput;
    /** Payment input component settings. */
    paymentInputOptions?: PaymentInput;
    /** Fixed payment component settings. */
    fixedPaymentOptions?: FixedPayment;
    /** Component type of the payment input field. */
    componentType?: PaymentComponentTypeWithLiterals;
    /**
     * Validation configuration for the payment input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: PaymentType;
}
/** @oneof */
interface PaymentComponentTypeOptionsOneOf {
    /** Checkbox group component settings. */
    checkboxGroupOptions?: ProductCheckboxGroup;
    /** Donation input component settings. */
    donationInputOptions?: DonationInput;
    /** Payment input component settings. */
    paymentInputOptions?: PaymentInput;
    /** Fixed payment component settings. */
    fixedPaymentOptions?: FixedPayment;
}
interface Scheduling extends SchedulingComponentTypeOptionsOneOf {
    /** Appointment component settings. */
    appointmentOptions?: Appointment;
    /** Component type of the scheduling field. */
    componentType?: SchedulingComponentTypeWithLiterals;
}
/** @oneof */
interface SchedulingComponentTypeOptionsOneOf {
    /** Appointment component settings. */
    appointmentOptions?: Appointment;
}
interface Address extends AddressComponentTypeOptionsOneOf {
    /** Multiline address component settings. */
    multilineAddressOptions?: MultilineAddress;
    /** Component type of the multiline address field. */
    componentType?: ComponentTypeWithLiterals;
    /**
     * Validation configuration for the multiline address input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: MultilineAddressValidation;
}
/** @oneof */
interface AddressComponentTypeOptionsOneOf {
    /** Multiline address component settings. */
    multilineAddressOptions?: MultilineAddress;
}
interface ObjectArray extends ObjectArrayComponentTypeOptionsOneOf {
    /** Repeater component settings. */
    repeaterOptions?: Repeater;
    /** Component type of the object array input. */
    componentType?: ObjectArrayComponentTypeWithLiterals;
    /**
     * Validation configuration for the object array input.
     *
     * Always include this object, even when it's empty: `{}`.
     */
    validation?: ObjectArrayType;
}
/** @oneof */
interface ObjectArrayComponentTypeOptionsOneOf {
    /** Repeater component settings. */
    repeaterOptions?: Repeater;
}
interface DisplayField extends DisplayFieldDisplayFieldTypeOptionsOneOf {
    /** Configuration for rich content display fields. */
    richContentOptions?: RichContentOptions;
    /** Configuration for page navigation display fields such as navigation or submit buttons. */
    pageNavigationOptions?: PageNavigationOptions;
    /** Type of display field that determines its appearance and behavior. */
    displayFieldType?: DisplayFieldTypeWithLiterals;
}
/** @oneof */
interface DisplayFieldDisplayFieldTypeOptionsOneOf {
    /** Configuration for rich content display fields. */
    richContentOptions?: RichContentOptions;
    /** Configuration for page navigation display fields such as navigation or submit buttons. */
    pageNavigationOptions?: PageNavigationOptions;
}
declare enum DisplayFieldType {
    /** Unknown field type. */
    UNKNOWN_FIELD_TYPE = "UNKNOWN_FIELD_TYPE",
    /** Rich content display field. */
    RICH_CONTENT = "RICH_CONTENT",
    /** Page navigation display field. */
    PAGE_NAVIGATION = "PAGE_NAVIGATION",
    /** Login bar display field. */
    LOGIN_BAR = "LOGIN_BAR"
}
/** @enumType */
type DisplayFieldTypeWithLiterals = DisplayFieldType | 'UNKNOWN_FIELD_TYPE' | 'RICH_CONTENT' | 'PAGE_NAVIGATION' | 'LOGIN_BAR';
interface RichContentOptions {
    /** Rich content to display in the field. */
    richContent?: RichContent;
    /**
     * Maximum number of paragraphs to show initially.
     * Additional content is hidden behind an expandable section. If not specified, all content is visible.
     */
    maxShownParagraphs?: number | null;
}
interface PageNavigationOptions {
    /**
     * Text displayed on the button when it navigates to the next page.
     * Only applicable when the button is not on the final form page.
     * @maxLength 65
     */
    nextPageText?: string | null;
    /**
     * Text displayed on the button when it navigates to the previous page.
     * Only applicable when the button is not on the first form page.
     * @maxLength 65
     */
    previousPageText?: string | null;
    /**
     * Text displayed on the button when it submits the form.
     * Only applicable when the button is on the final form page.
     * @maxLength 65
     */
    submitText?: string | null;
}
interface Step {
    /**
     * Step ID.
     * @format GUID
     * @immutable
     */
    _id?: string;
    /**
     * Name of the step.
     * @maxLength 200
     */
    name?: string | null;
    /** Whether the step is hidden. */
    hidden?: boolean;
    /** Layout configuration that defines how form fields are arranged within this step. */
    layout?: FormLayout;
}
interface FormRule {
    /**
     * Rule ID.
     * @format GUID
     * @immutable
     */
    _id?: string;
    /** Condition that determines when this rule's overrides should be applied. */
    condition?: Record<string, any> | null;
    /**
     * Changes to apply to form elements when the condition evaluates to true.
     * @maxSize 500
     */
    overrides?: FormOverride[];
    /**
     * Name for the rule.
     * @maxLength 200
     */
    name?: string | null;
    /**
     * Namespace rules are part of business logic of app owning namespace, have limited editing possibilities
     * @immutable
     */
    namespaceRule?: boolean | null;
}
declare enum OverrideEntityType {
    /** Unknown override entity type. */
    UNKNOWN = "UNKNOWN",
    /** Override applies to a form field. */
    FIELD = "FIELD"
}
/** @enumType */
type OverrideEntityTypeWithLiterals = OverrideEntityType | 'UNKNOWN' | 'FIELD';
interface FormOverride {
    /**
     * Type of form element that this override targets.
     * @immutable
     */
    entityType?: OverrideEntityTypeWithLiterals;
    /**
     * ID of the form element to modify.
     * @immutable
     * @format GUID
     */
    entityId?: string | null;
    /**
     * Property changes to apply to the form element when the rule condition is met.
     * Each key represents a property path, and the value is the new value to set.
     */
    valueChanges?: Record<string, any>;
}
interface FormProperties {
    /**
     * Form name.
     * @maxLength 200
     * @deprecated
     * @replacedBy wix.forms.v4.Form.name
     * @targetRemovalDate 2026-01-06
     */
    name?: string | null;
    /**
     * Identifies if the form is disabled.
     * @deprecated Identifies if the form is disabled.
     * @replacedBy wix.forms.v4.Form.enabled
     * @targetRemovalDate 2026-01-06
     */
    disabled?: boolean;
}
declare enum Kind {
    /** Return forms specific to tenant, excluding template based forms. */
    REGULAR = "REGULAR",
    /** Return forms based on common templates, with applied user overrides if they exist. */
    EXTENSION = "EXTENSION"
}
/** @enumType */
type KindWithLiterals = Kind | 'REGULAR' | 'EXTENSION';
interface PostSubmissionTriggers {
    /** Upserts a [contact](https://support.wix.com/en/article/about-your-contact-list) to the Wix site from the form submission data. */
    upsertContact?: UpsertContact;
}
interface UpsertContact {
    /**
     * Fields mapping between the form fields and the contact fields.
     *
     * This is the target form field mapped to the corresponding contact field.
     *
     * The key is the form field ID, and the value is the contact field.
     * @maxSize 250
     */
    fieldsMapping?: Record<string, V4FormFieldContactInfo>;
    /**
     * List of [labels](https://support.wix.com/en/article/creating-contact-labels) to add to the contact to help categorize it.
     * @minLength 1
     * @maxLength 100
     * @maxSize 20
     */
    labels?: string[];
}
interface V4FormFieldContactInfo extends V4FormFieldContactInfoAdditionalInfoOneOf {
    /** Email info. */
    emailInfo?: FormFieldContactInfoEmailInfo;
    /** Phone info. */
    phoneInfo?: FormFieldContactInfoPhoneInfo;
    /** Address info. */
    addressInfo?: FormFieldContactInfoAddressInfo;
    /** Custom field info. */
    customFieldInfo?: FormFieldContactInfoCustomFieldInfo;
    /** Subscription info. */
    subscriptionInfo?: FormFieldContactInfoSubscriptionInfo;
    /** The contact field that the form field maps to. */
    contactField?: FormFieldContactInfoContactFieldWithLiterals;
}
/** @oneof */
interface V4FormFieldContactInfoAdditionalInfoOneOf {
    /** Email info. */
    emailInfo?: FormFieldContactInfoEmailInfo;
    /** Phone info. */
    phoneInfo?: FormFieldContactInfoPhoneInfo;
    /** Address info. */
    addressInfo?: FormFieldContactInfoAddressInfo;
    /** Custom field info. */
    customFieldInfo?: FormFieldContactInfoCustomFieldInfo;
    /** Subscription info. */
    subscriptionInfo?: FormFieldContactInfoSubscriptionInfo;
}
declare enum FormFieldContactInfoEmailInfoTag {
    /** Email without a specific tag. */
    UNTAGGED = "UNTAGGED",
    /** Primary email address for the contact. */
    MAIN = "MAIN"
}
/** @enumType */
type FormFieldContactInfoEmailInfoTagWithLiterals = FormFieldContactInfoEmailInfoTag | 'UNTAGGED' | 'MAIN';
declare enum FormFieldContactInfoPhoneInfoTag {
    /** Phone number without a specific tag. */
    UNTAGGED = "UNTAGGED",
    /** Primary phone number for the contact. */
    MAIN = "MAIN"
}
/** @enumType */
type FormFieldContactInfoPhoneInfoTagWithLiterals = FormFieldContactInfoPhoneInfoTag | 'UNTAGGED' | 'MAIN';
declare enum AddressInfoTag {
    /** Address without a specific tag. */
    UNTAGGED = "UNTAGGED",
    /** Home address for the contact. */
    HOME = "HOME"
}
/** @enumType */
type AddressInfoTagWithLiterals = AddressInfoTag | 'UNTAGGED' | 'HOME';
declare enum SubscriptionInfoOptInLevel {
    /** Unknown opt-in level. */
    UNKNOWN = "UNKNOWN",
    /** Single confirmation opt-in. The contact is subscribed immediately. */
    SINGLE_CONFIRMATION = "SINGLE_CONFIRMATION",
    /** Double confirmation opt-in. The contact must confirm their subscription via email. */
    DOUBLE_CONFIRMATION = "DOUBLE_CONFIRMATION"
}
/** @enumType */
type SubscriptionInfoOptInLevelWithLiterals = SubscriptionInfoOptInLevel | 'UNKNOWN' | 'SINGLE_CONFIRMATION' | 'DOUBLE_CONFIRMATION';
declare enum FormFieldContactInfoContactField {
    /** First name. */
    FIRST_NAME = "FIRST_NAME",
    /** CLast name. */
    LAST_NAME = "LAST_NAME",
    /** Company name. */
    COMPANY = "COMPANY",
    /** Job position or title. */
    POSITION = "POSITION",
    /** Email address. */
    EMAIL = "EMAIL",
    /** Phone number. */
    PHONE = "PHONE",
    /** Physical address. */
    ADDRESS = "ADDRESS",
    /** Birth date. */
    BIRTHDATE = "BIRTHDATE",
    /** Custom field. */
    CUSTOM_FIELD = "CUSTOM_FIELD",
    /** Subscription status. */
    SUBSCRIPTION = "SUBSCRIPTION",
    /** VAT identification number. */
    VAT_ID = "VAT_ID"
}
/** @enumType */
type FormFieldContactInfoContactFieldWithLiterals = FormFieldContactInfoContactField | 'FIRST_NAME' | 'LAST_NAME' | 'COMPANY' | 'POSITION' | 'EMAIL' | 'PHONE' | 'ADDRESS' | 'BIRTHDATE' | 'CUSTOM_FIELD' | 'SUBSCRIPTION' | 'VAT_ID';
interface FormFieldContactInfoEmailInfo {
    /** Email tag. */
    tag?: FormFieldContactInfoEmailInfoTagWithLiterals;
}
interface FormFieldContactInfoPhoneInfo {
    /** Phone tag. */
    tag?: FormFieldContactInfoPhoneInfoTagWithLiterals;
}
interface FormFieldContactInfoAddressInfo {
    /** Address tag. */
    tag?: AddressInfoTagWithLiterals;
}
interface FormFieldContactInfoCustomFieldInfo {
    /**
     * Custom field key.
     * @minLength 1
     * @maxLength 500
     */
    key?: string;
}
interface FormFieldContactInfoSubscriptionInfo {
    /**
     * Subscription consent opt-in level.
     * Default: `SINGLE_CONFIRMATION`
     */
    optInLevel?: SubscriptionInfoOptInLevelWithLiterals;
    /**
     * Subscription channels.
     * When not specified, defaults to `EMAIL`.
     * @maxSize 2
     */
    subscriptionChannels?: SubscriptionChannelWithLiterals[];
}
interface NestedForm {
    /**
     * Targets which have this form.
     * @maxSize 10
     * @maxLength 200
     */
    targets?: string[];
    /** Nested form. */
    form?: Form;
}
interface LimitationRule {
    /** Limitation by submission count, disables form when a set amount of submissions is reached. */
    maxAllowedSubmissions?: number | null;
    /** Limitation by submission date, disables form when a set date and time is reached. */
    dateTimeDeadline?: Date | null;
    /**
     * Limitation per user submission count, disables form when a set amount of submissions per user is reached.
     * @min 1
     */
    submissionLimitPerUser?: number | null;
}
declare enum SpamFilterProtectionLevel {
    /** Unknown spam filter protection level. */
    UNKNOWN = "UNKNOWN",
    /** No spam filtering. All submissions are accepted without screening. */
    NONE = "NONE",
    /** Basic spam filtering. Provides minimal protection against obvious spam and the form is open to a high risk of spam submissions. */
    BASIC = "BASIC",
    /** Advanced spam filtering. Provides comprehensive protection with intelligent screening and the form is open to a low risk of spam submissions. */
    ADVANCED = "ADVANCED"
}
/** @enumType */
type SpamFilterProtectionLevelWithLiterals = SpamFilterProtectionLevel | 'UNKNOWN' | 'NONE' | 'BASIC' | 'ADVANCED';
interface RequiredIndicatorProperties {
    /** Type of visual indicator used to mark required fields. */
    requiredIndicator?: RequiredIndicatorWithLiterals;
    /** Position where the required indicator appears relative to the field title. */
    requiredIndicatorPlacement?: RequiredIndicatorPlacementWithLiterals;
}
declare enum RequiredIndicator {
    /** Display an asterisk (*) symbol to indicate required fields. */
    ASTERISK = "ASTERISK",
    /** Display the text "Required" to indicate required fields. */
    TEXT = "TEXT",
    /** No visual indicator is shown for required fields. */
    NONE = "NONE"
}
/** @enumType */
type RequiredIndicatorWithLiterals = RequiredIndicator | 'ASTERISK' | 'TEXT' | 'NONE';
declare enum RequiredIndicatorPlacement {
    /** Place the required indicator after the field title. */
    AFTER_FIELD_TITLE = "AFTER_FIELD_TITLE",
    /** Place the required indicator before the field title. */
    BEFORE_FIELD_TITLE = "BEFORE_FIELD_TITLE"
}
/** @enumType */
type RequiredIndicatorPlacementWithLiterals = RequiredIndicatorPlacement | 'AFTER_FIELD_TITLE' | 'BEFORE_FIELD_TITLE';
interface SubmitSettings extends SubmitSettingsSubmitSuccessActionOptionsOneOf {
    /** Configuration for displaying a thank you message after submission. */
    thankYouMessageOptions?: ThankYouMessageOptions;
    /** Configuration for redirecting submitters to a URL after submission. */
    redirectOptions?: RedirectOptions;
    /** Configuration for displaying a popup after submission. */
    popupOptions?: PopupOptions;
    /** Action that's triggered after a form is successfully submitted. */
    submitSuccessAction?: SubmitSuccessActionWithLiterals;
}
/** @oneof */
interface SubmitSettingsSubmitSuccessActionOptionsOneOf {
    /** Configuration for displaying a thank you message after submission. */
    thankYouMessageOptions?: ThankYouMessageOptions;
    /** Configuration for redirecting submitters to a URL after submission. */
    redirectOptions?: RedirectOptions;
    /** Configuration for displaying a popup after submission. */
    popupOptions?: PopupOptions;
}
declare enum Target {
    /** Unknown target behavior. */
    UNKNOWN_TARGET = "UNKNOWN_TARGET",
    /** Open the URL in the same browser tab, replacing the current page. */
    SELF = "SELF",
    /** Open the URL in a new browser tab. */
    BLANK = "BLANK"
}
/** @enumType */
type TargetWithLiterals = Target | 'UNKNOWN_TARGET' | 'SELF' | 'BLANK';
declare enum SubmitSuccessAction {
    /** No action is taken after form submission. */
    NO_ACTION = "NO_ACTION",
    /** Display a thank you message after form submission. */
    THANK_YOU_MESSAGE = "THANK_YOU_MESSAGE",
    /** Redirect the user to a specified URL after form submission. */
    REDIRECT = "REDIRECT",
    /** Display a popup after form submission. */
    POPUP = "POPUP"
}
/** @enumType */
type SubmitSuccessActionWithLiterals = SubmitSuccessAction | 'NO_ACTION' | 'THANK_YOU_MESSAGE' | 'REDIRECT' | 'POPUP';
interface ThankYouMessageOptions {
    /**
     * Duration in seconds before the thank you message automatically disappears.
     * If set to 0, the message remains visible until manually dismissed.
     */
    durationInSeconds?: number | null;
    /** Rich content message displayed to users after successful form submission. */
    richContent?: RichContent;
}
interface RedirectOptions {
    /**
     * URL to which the user should be redirected after successfully submitting the form.
     * @maxLength 2000
     */
    redirectUrl?: string | null;
    /** How the redirect URL should be opened in the browser. */
    target?: TargetWithLiterals;
}
interface PopupOptions {
    /**
     * ID of the site popup to open after successful form submission.
     * @minLength 1
     * @maxLength 100
     */
    popupId?: string;
}
interface FieldGroup {
    /**
     * Id of group, on layout
     * @format GUID
     */
    _id?: string;
    /**
     * Label of group
     * @maxLength 350
     */
    label?: string | null;
}
interface Rule {
    /**
     * Rule ID.
     * @format GUID
     * @immutable
     */
    _id?: string;
    /**
     * Conditional expression that determines when this rule should be applied.
     * The overrides defined in this rule are applied when this expression evaluates to true.
     *
     * The root of the expression must be an `and` or `or` group. To evaluate a
     * single condition, wrap it in a group with 1 condition.
     */
    expression?: ConditionNode;
    /**
     * Form modifications that are applied when the rule expression evaluates to true.
     * Each override specifies how a form element should be changed.
     * @maxSize 500
     */
    overrides?: RuleFormOverride[];
    /**
     * Human-readable name for the rule.
     * @maxLength 200
     */
    name?: string | null;
    /**
     * Namespace rules are part of business logic of app owning namespace, have limited editing possibilities
     * @immutable
     */
    namespaceRule?: boolean | null;
}
/** Enum for different types of field properties that can be modified by rules. */
declare enum ChangeableProperty {
    /** Required property type. */
    REQUIRED = "REQUIRED",
    /** Hidden property type. */
    HIDDEN = "HIDDEN"
}
/** @enumType */
type ChangeablePropertyWithLiterals = ChangeableProperty | 'REQUIRED' | 'HIDDEN';
interface RequiredOptions {
    /** Whether the field should be required. */
    required?: boolean;
}
interface HiddenOptions {
    /** Whether the field should be hidden. */
    hidden?: boolean;
}
interface AllowedValuesOptions {
    /**
     * List of values that are allowed for the field when this rule is active.
     * Restricts user input to only these predefined options.
     * @maxSize 300
     */
    allowedValues?: any[];
}
declare enum OverrideEntityTypeEnumOverrideEntityType {
    /** Override applies to a form field. */
    FIELD = "FIELD"
}
/** @enumType */
type OverrideEntityTypeEnumOverrideEntityTypeWithLiterals = OverrideEntityTypeEnumOverrideEntityType | 'FIELD';
interface FieldOverride extends FieldOverridePropertyTypeOptionsOneOf {
    /** Configuration for changing the required property of a field. */
    requiredOptions?: RequiredOptions;
    /** Configuration for changing the visibility property of a field. */
    hiddenOptions?: HiddenOptions;
    /** Configuration for changing the allowed values of a field. */
    allowedValuesOptions?: AllowedValuesOptions;
    /**
     * ID of the field to be modified by this override.
     * @immutable
     * @format GUID
     */
    fieldId?: string;
    /** The new value for the field property that will be set when the rule conditions are met. */
    propertyType?: ChangeablePropertyWithLiterals;
}
/** @oneof */
interface FieldOverridePropertyTypeOptionsOneOf {
    /** Configuration for changing the required property of a field. */
    requiredOptions?: RequiredOptions;
    /** Configuration for changing the visibility property of a field. */
    hiddenOptions?: HiddenOptions;
    /** Configuration for changing the allowed values of a field. */
    allowedValuesOptions?: AllowedValuesOptions;
}
/**
 * Node of a rule's conditional expression. Either a logical group of nested
 * nodes, or a single condition.
 *
 * The root node of a rule's expression must be an `and` or `or` group.
 * A single condition is only supported in nested nodes.
 */
interface ConditionNode extends ConditionNodeNodeOneOf {
    /** Logical AND condition with 1 or more child conditions. All child conditions must be true. */
    and?: AndCondition;
    /** Logical OR condition with 1 or more child conditions. At least 1 child condition must be true. */
    or?: OrCondition;
    /** Single condition to evaluate. Not supported as the root of a rule's expression. */
    condition?: Condition;
}
/** @oneof */
interface ConditionNodeNodeOneOf {
    /** Logical AND condition with 1 or more child conditions. All child conditions must be true. */
    and?: AndCondition;
    /** Logical OR condition with 1 or more child conditions. At least 1 child condition must be true. */
    or?: OrCondition;
    /** Single condition to evaluate. Not supported as the root of a rule's expression. */
    condition?: Condition;
}
interface AndCondition {
    /**
     * List of conditions that must all evaluate to true.
     * @minSize 1
     * @maxSize 400
     */
    conditions?: ConditionNode[];
}
interface OrCondition {
    /**
     * List of conditions where at least 1 must evaluate to true.
     * @minSize 1
     * @maxSize 400
     */
    conditions?: ConditionNode[];
}
interface Condition {
    /**
     * Human readable identifier used to reference a field.
     * Supports dot notation for nested properties. For example, `"address.city"`.
     * @maxLength 100
     */
    target?: string;
    /** Comparison operator to use for evaluating the condition. */
    operator?: OperatorWithLiterals;
    /** Value to compare against the value in the target field. */
    value?: any;
}
declare enum Operator {
    /** Specified value equals the value in the target field. */
    EQUAL = "EQUAL",
    /** Specified value doesn't equal the value in the target field. */
    NOT_EQUAL = "NOT_EQUAL",
    /** Target field is empty or has no value. */
    EMPTY = "EMPTY",
    /** Target field isn't empty and has a value. */
    NOT_EMPTY = "NOT_EMPTY",
    /** String value in the target field contains the specified substring. */
    CONTAINS = "CONTAINS",
    /** String value in the target field doesn't contain the specified substring. */
    NOT_CONTAINS = "NOT_CONTAINS",
    /** Numeric value in the target field is less than the specified value. */
    LESS_THAN = "LESS_THAN",
    /** Numeric value in the target field is less than or equal to the specified value. */
    LESS_THAN_OR_EQUALS = "LESS_THAN_OR_EQUALS",
    /** Numeric value in the target field is greater than the specified value. */
    GREATER_THAN = "GREATER_THAN",
    /** Numeric value in the target field is greater than or equal to the specified value. */
    GREATER_THAN_OR_EQUALS = "GREATER_THAN_OR_EQUALS",
    /** Date value in the target field is before the specified date. */
    BEFORE = "BEFORE",
    /** Date value in the target field is before or equal to the specified date. */
    BEFORE_OR_EQUAL = "BEFORE_OR_EQUAL",
    /** Date value in the target field is after the specified date. */
    AFTER = "AFTER",
    /** Date value in the target field is after or equal to the specified date. */
    AFTER_OR_EQUAL = "AFTER_OR_EQUAL",
    /** Value in the target field falls within the specified range. */
    BETWEEN = "BETWEEN",
    /** Value in the target field matches any of the specified values. */
    ANY = "ANY",
    /** Array value in the target field exactly equals the specified array. */
    ARRAY_EQUAL = "ARRAY_EQUAL",
    /** Array value in the target field doesn't equal the specified array. */
    ARRAY_NOT_EQUAL = "ARRAY_NOT_EQUAL",
    /** Checkbox or boolean field value in the target field is checked. */
    CHECKED = "CHECKED",
    /** Checkbox or boolean field value in the target field isn't checked. */
    NOT_CHECKED = "NOT_CHECKED",
    /** Value in the target field is included in the specified list. */
    IN = "IN",
    /** Value in the target field isn't included in the specified list. */
    NOT_IN = "NOT_IN",
    /** Date value in the target field is older than the specified time period. */
    IS_DATE_OLDER_THAN = "IS_DATE_OLDER_THAN",
    /** Date value in the target field is older than or equal to the specified time period. */
    IS_DATE_OLDER_THAN_OR_EQUAL = "IS_DATE_OLDER_THAN_OR_EQUAL",
    /** Date value in the target field is newer than the specified time period. */
    IS_DATE_NEWER_THAN = "IS_DATE_NEWER_THAN",
    /** Date value in the target field is newer than or equal to the specified time period. */
    IS_DATE_NEWER_THAN_OR_EQUAL = "IS_DATE_NEWER_THAN_OR_EQUAL"
}
/** @enumType */
type OperatorWithLiterals = Operator | 'EQUAL' | 'NOT_EQUAL' | 'EMPTY' | 'NOT_EMPTY' | 'CONTAINS' | 'NOT_CONTAINS' | 'LESS_THAN' | 'LESS_THAN_OR_EQUALS' | 'GREATER_THAN' | 'GREATER_THAN_OR_EQUALS' | 'BEFORE' | 'BEFORE_OR_EQUAL' | 'AFTER' | 'AFTER_OR_EQUAL' | 'BETWEEN' | 'ANY' | 'ARRAY_EQUAL' | 'ARRAY_NOT_EQUAL' | 'CHECKED' | 'NOT_CHECKED' | 'IN' | 'NOT_IN' | 'IS_DATE_OLDER_THAN' | 'IS_DATE_OLDER_THAN_OR_EQUAL' | 'IS_DATE_NEWER_THAN' | 'IS_DATE_NEWER_THAN_OR_EQUAL';
interface RuleFormOverride extends RuleFormOverrideEntityTypeOptionsOneOf {
    /** Field override settings. */
    fieldOptions?: FieldOverride;
    /**
     * Type of form element being modified by this override.
     * @immutable
     */
    entityType?: OverrideEntityTypeEnumOverrideEntityTypeWithLiterals;
}
/** @oneof */
interface RuleFormOverrideEntityTypeOptionsOneOf {
    /** Field override settings. */
    fieldOptions?: FieldOverride;
}
/**
 * Common object for tags.
 * Should be use as in this example:
 * message Foo {
 * option (.wix.api.decomposite_of) = "wix.common.tags.Foo";
 * string id = 1;
 * ...
 * Tags tags = 5
 * }
 *
 * example of taggable entity
 * {
 * id: "123"
 * tags: {
 * tags: {
 * tag_ids:["11","22"]
 * },
 * private_tags: {
 * tag_ids: ["33", "44"]
 * }
 * }
 * }
 */
interface Tags {
    /** Tags that require an additional permission in order to access them, normally not given to site members or visitors. */
    privateTags?: TagsTagList;
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    tags?: TagsTagList;
}
interface TagsTagList {
    /**
     * List of tag IDs
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
/**
 * Scoring and result configuration for a quiz form.
 *
 * A quiz form scores the respondent's answers and shows a tailored result on the
 * confirmation page — for example a knowledge test, a trivia quiz, or a
 * self-assessment where each answer is worth points and the total decides whether the
 * respondent passes.
 *
 * This message holds the form-level scoring configuration: the passing threshold
 * (`passing_criteria`), what to reveal to the respondent (`results_display` — the
 * score, the pass/fail status, or both), and the messages shown for each outcome.
 * The points and correct answers behind the score are assigned per question on the
 * individual quiz fields.
 */
interface QuizSettings extends QuizSettingsPassingCriteriaOneOf, QuizSettingsResultsDisplayOptionsOneOf {
    /** Score required to pass the quiz (in points). */
    passingScorePoints?: number;
    /**
     * Score required to pass the quiz as a percentage (0-100).
     * @max 100
     */
    passingScorePercentage?: number;
    /** Configuration for displaying only the pass/fail message. */
    passingStatusOptions?: PassFailMessages;
    /** Configuration for displaying both the score and pass/fail message. */
    scoreAndPassingStatusOptions?: PassFailMessages;
    /**
     * Rich-content message shown on the quiz result (confirmation) page, alongside any
     * score or pass/fail status configured in `results_display`.
     */
    message?: RichContent;
    /**
     * How to display quiz results in the confirmation page.
     * Default: `DO_NOT_DISPLAY`
     */
    resultsDisplay?: ResultsDisplayWithLiterals;
}
/** @oneof */
interface QuizSettingsPassingCriteriaOneOf {
    /** Score required to pass the quiz (in points). */
    passingScorePoints?: number;
    /**
     * Score required to pass the quiz as a percentage (0-100).
     * @max 100
     */
    passingScorePercentage?: number;
}
/** @oneof */
interface QuizSettingsResultsDisplayOptionsOneOf {
    /** Configuration for displaying only the pass/fail message. */
    passingStatusOptions?: PassFailMessages;
    /** Configuration for displaying both the score and pass/fail message. */
    scoreAndPassingStatusOptions?: PassFailMessages;
}
declare enum ResultsDisplay {
    /** Do not show quiz score and passing status. */
    DO_NOT_DISPLAY = "DO_NOT_DISPLAY",
    /** Display only the user's score. */
    SCORE = "SCORE",
    /** Display the pass or fail message based on the user's score. */
    PASSING_STATUS = "PASSING_STATUS",
    /** Display both the score and pass/fail message. */
    SCORE_AND_PASSING_STATUS = "SCORE_AND_PASSING_STATUS"
}
/** @enumType */
type ResultsDisplayWithLiterals = ResultsDisplay | 'DO_NOT_DISPLAY' | 'SCORE' | 'PASSING_STATUS' | 'SCORE_AND_PASSING_STATUS';
interface PassFailMessages {
    /** Message displayed when the user passes the quiz. */
    passMessage?: RichContent;
    /** Message displayed when the user fails the quiz. */
    failMessage?: RichContent;
}
declare enum ContactAutofill {
    /** Use site member data to autofill contact fields. */
    MEMBER_DATA = "MEMBER_DATA",
    /** Use form input data to autofill contact fields. */
    FORM_INPUT = "FORM_INPUT"
}
/** @enumType */
type ContactAutofillWithLiterals = ContactAutofill | 'MEMBER_DATA' | 'FORM_INPUT';
/** Controls who can read submissions of this form. */
declare enum SubmissionAccess {
    /** Unknown submission access. */
    UNKNOWN_SUBMISSION_ACCESS = "UNKNOWN_SUBMISSION_ACCESS",
    /** Only users with `WIX_FORM.SUBMISSIONS_READ_ANY` permission. Default behavior. */
    OWNER_AND_COLLABORATORS = "OWNER_AND_COLLABORATORS",
    /** Logged-in site members can read submissions. */
    MEMBERS = "MEMBERS",
    /** Any site visitor with a valid Wix session can read submissions. */
    PUBLIC = "PUBLIC"
}
/** @enumType */
type SubmissionAccessWithLiterals = SubmissionAccess | 'UNKNOWN_SUBMISSION_ACCESS' | 'OWNER_AND_COLLABORATORS' | 'MEMBERS' | 'PUBLIC';
interface CreateCheckoutFromSubmissionResponse {
    /**
     * Checkout ID (applicable for form involving payments).
     * @format GUID
     */
    checkoutId?: string | null;
    /** Checkout (applicable for form involving payments). */
    checkout?: Checkout;
}
interface Checkout {
    /**
     * Checkout ID.
     * @format GUID
     */
    _id?: string;
    /**
     * The currency used when submitting the order.
     * @format CURRENCY
     * @readonly
     */
    currency?: string;
}
interface IsFormSubmittableRequest {
}
interface IsFormSubmittableResponse {
}
interface Empty {
}
interface UpsertContactFromSubmissionRequest {
    /**
     * Submission from which contact needs to be upserted.
     * @format GUID
     */
    submissionId: string;
    /**
     * Optional contact id to which submission should be mapped.
     * @format GUID
     */
    contactId?: string | null;
    /** Indicates contact has verified primary email. */
    emailVerified?: boolean;
}
interface UpsertContactFromSubmissionResponse {
    /** Submit contact response. */
    submitContactResponse?: SubmitContactResponse;
}
interface SubmitContactResponse {
    /**
     * ID of the contact that was found or created.
     * @format GUID
     */
    contactId?: string;
    /**
     * Identity type of the returned contact.
     *
     * - `CONTACT`: The returned contact ID belongs to a new or existing contact.
     * - `MEMBER`: The returned contact ID belongs to the currently logged-in site member.
     * - `NOT_AUTHENTICATED_MEMBER`: The returned contact ID belongs to a site member who is not currently logged in.
     */
    identityType?: IdentityTypeWithLiterals;
    /**
     * Indicates whether the contact was just created or already existed.
     *
     * If the contact was just created, returns `true`.
     * If it already existed, returns `false`.
     */
    newContact?: boolean;
}
declare enum IdentityType {
    UNKNOWN = "UNKNOWN",
    /** Existing or new contact */
    CONTACT = "CONTACT",
    /** Member is logged in, matching logic skipped */
    MEMBER = "MEMBER",
    /** Matching contact is a member, Merge logic won't be applied */
    NOT_AUTHENTICATED_MEMBER = "NOT_AUTHENTICATED_MEMBER"
}
/** @enumType */
type IdentityTypeWithLiterals = IdentityType | 'UNKNOWN' | 'CONTACT' | 'MEMBER' | 'NOT_AUTHENTICATED_MEMBER';
interface CreateSubmissionRequest {
    /** Submission to create. */
    submission: FormSubmission;
    /**
     * Captcha token.
     * @minLength 1
     * @maxLength 3000
     */
    captchaToken?: string | null;
    /** Optional metadata passed to SPI implementer, allowing clients to include additional business-specific data. */
    additionalMetadata?: Record<string, any> | null;
}
interface CreateSubmissionResponse {
    /** The created submission. */
    submission?: FormSubmission;
}
interface SubmissionValidationErrorsDetails {
    /**
     * Submission validation errors details.
     * @maxSize 100
     */
    errors?: ValidationError[];
}
interface ValidationError {
    /**
     * Path to the value that failed validation, relative to the submission's values.
     *
     * For a top-level field this is the field's `target`, such as `email`. For a value nested inside a field, the path continues with `/`, such as `address/postal_code` for an address subfield, or `attachments/0/fileId` for an element of an array field.
     * @maxLength 1000
     */
    errorPath?: string;
    /**
     * Error type.
     *
     * Switch on this rather than parsing `errorMessage`, which carries the underlying validator's wording and isn't a stable contract.
     */
    errorType?: ErrorTypeWithLiterals;
    /**
     * Human readable description of the error, in English.
     *
     * This comes from the schema validator and is meant for debugging. It isn't localized and its wording can change, so don't parse it or show it to submitters. Use `errorType` to decide what to display.
     * @maxLength 1000
     */
    errorMessage?: string;
    /** Additional error parameters, to help identify additional info. */
    params?: Record<string, any> | null;
    /**
     * Whether to use a custom error message.
     * If `false`, the API takes the message from the `errorType` field.
     * Default: `false`
     */
    useCustomErrorMessage?: boolean;
}
declare enum ErrorType {
    /** Error is unknown or not suitable for any of options bellow */
    UNKNOWN_ERROR = "UNKNOWN_ERROR",
    /** Type of submitted value is incorrect */
    TYPE_ERROR = "TYPE_ERROR",
    /** Value is required to be provided */
    REQUIRED_VALUE_ERROR = "REQUIRED_VALUE_ERROR",
    /** Value contains additional properties not expected in schema */
    UNKNOWN_VALUE_ERROR = "UNKNOWN_VALUE_ERROR",
    /** Text value exceeds max length */
    MAX_LENGTH_ERROR = "MAX_LENGTH_ERROR",
    /** Text value not reaches min length */
    MIN_LENGTH_ERROR = "MIN_LENGTH_ERROR",
    /** Text value not applicable for expected pattern */
    PATTERN_ERROR = "PATTERN_ERROR",
    /** Text value not applicable for expected format */
    FORMAT_ERROR = "FORMAT_ERROR",
    /** Number value is too big */
    MAX_VALUE_ERROR = "MAX_VALUE_ERROR",
    /** Number value is too small */
    MIN_VALUE_ERROR = "MIN_VALUE_ERROR",
    /** Number value is not multiple of expected number */
    MULTIPLE_OF_VALUE_ERROR = "MULTIPLE_OF_VALUE_ERROR",
    /** Array value has too much items */
    MIN_ITEMS_ERROR = "MIN_ITEMS_ERROR",
    /** Array value has not enough items */
    MAX_ITEMS_ERROR = "MAX_ITEMS_ERROR",
    /** Value is not in list of allowed values */
    NOT_ALLOWED_VALUE_ERROR = "NOT_ALLOWED_VALUE_ERROR",
    /** Form fields are incompatible */
    FIELDS_COMPATIBILITY_ERROR = "FIELDS_COMPATIBILITY_ERROR",
    /** Submitted form is disabled */
    DISABLED_FORM_ERROR = "DISABLED_FORM_ERROR",
    /** Too much forms */
    FORMS_COUNT_RESTRICTIONS_ERROR = "FORMS_COUNT_RESTRICTIONS_ERROR",
    /** Too much fields on form */
    FIELDS_COUNT_RESTRICTIONS_ERROR = "FIELDS_COUNT_RESTRICTIONS_ERROR",
    /** Too much steps on forms */
    STEPS_COUNT_RESTRICTIONS_ERROR = "STEPS_COUNT_RESTRICTIONS_ERROR",
    /** Too much rules on forms */
    RULES_COUNT_RESTRICTIONS_ERROR = "RULES_COUNT_RESTRICTIONS_ERROR",
    /** File upload is not allowed */
    FILE_UPLOAD_RESTRICTIONS_ERROR = "FILE_UPLOAD_RESTRICTIONS_ERROR",
    /** Country code is not correct or missing from the phone number */
    INVALID_PHONE_COUNTRY_CODE_ERROR = "INVALID_PHONE_COUNTRY_CODE_ERROR",
    /** Invalid staff member ID on appointment */
    INVALID_STAFF_ID_ERROR = "INVALID_STAFF_ID_ERROR",
    /** Invalid business location ID on appointment */
    INVALID_LOCATION_ID_ERROR = "INVALID_LOCATION_ID_ERROR",
    /** Missing service option on service picker field */
    MISSING_SERVICE_OPTION_ERROR = "MISSING_SERVICE_OPTION_ERROR",
    /** Some of the service options on service picker field are deleted or unavailable */
    INVALID_SERVICE_OPTIONS_ERROR = "INVALID_SERVICE_OPTIONS_ERROR",
    /** All of the service options on service picker field are deleted or unavailable */
    NO_AVAILABLE_SERVICE_OPTIONS_ERROR = "NO_AVAILABLE_SERVICE_OPTIONS_ERROR",
    /** Text value does not match the exact required length */
    EXACT_CHARACTER_LENGTH_ERROR = "EXACT_CHARACTER_LENGTH_ERROR",
    /** Character count is not within the required range */
    CHARACTER_LENGTH_RANGE_ERROR = "CHARACTER_LENGTH_RANGE_ERROR",
    /** Number is not within the required range */
    VALUE_RANGE_ERROR = "VALUE_RANGE_ERROR",
    /** Field not have the exact number of options selected */
    EXACT_ITEMS_NUMBER_ERROR = "EXACT_ITEMS_NUMBER_ERROR",
    /** Number has incorrect number of decimal places */
    DECIMAL_POINT_ERROR = "DECIMAL_POINT_ERROR",
    /** Date value is incomplete or missing required parts */
    INCOMPLETE_DATE_ERROR = "INCOMPLETE_DATE_ERROR",
    /** Value does not match the required pattern format */
    INVALID_VALUE_FOR_PATTERN_ERROR = "INVALID_VALUE_FOR_PATTERN_ERROR",
    /** Default value does not pass form field validation */
    INVALID_DEFAULT_VALUE_ERROR = "INVALID_DEFAULT_VALUE_ERROR",
    /** No resource on the site is connected to a video conferencing provider, so the appointment's online meeting format can't be offered */
    NO_CONNECTED_CONFERENCING_RESOURCE_ERROR = "NO_CONNECTED_CONFERENCING_RESOURCE_ERROR"
}
/** @enumType */
type ErrorTypeWithLiterals = ErrorType | 'UNKNOWN_ERROR' | 'TYPE_ERROR' | 'REQUIRED_VALUE_ERROR' | 'UNKNOWN_VALUE_ERROR' | 'MAX_LENGTH_ERROR' | 'MIN_LENGTH_ERROR' | 'PATTERN_ERROR' | 'FORMAT_ERROR' | 'MAX_VALUE_ERROR' | 'MIN_VALUE_ERROR' | 'MULTIPLE_OF_VALUE_ERROR' | 'MIN_ITEMS_ERROR' | 'MAX_ITEMS_ERROR' | 'NOT_ALLOWED_VALUE_ERROR' | 'FIELDS_COMPATIBILITY_ERROR' | 'DISABLED_FORM_ERROR' | 'FORMS_COUNT_RESTRICTIONS_ERROR' | 'FIELDS_COUNT_RESTRICTIONS_ERROR' | 'STEPS_COUNT_RESTRICTIONS_ERROR' | 'RULES_COUNT_RESTRICTIONS_ERROR' | 'FILE_UPLOAD_RESTRICTIONS_ERROR' | 'INVALID_PHONE_COUNTRY_CODE_ERROR' | 'INVALID_STAFF_ID_ERROR' | 'INVALID_LOCATION_ID_ERROR' | 'MISSING_SERVICE_OPTION_ERROR' | 'INVALID_SERVICE_OPTIONS_ERROR' | 'NO_AVAILABLE_SERVICE_OPTIONS_ERROR' | 'EXACT_CHARACTER_LENGTH_ERROR' | 'CHARACTER_LENGTH_RANGE_ERROR' | 'VALUE_RANGE_ERROR' | 'EXACT_ITEMS_NUMBER_ERROR' | 'DECIMAL_POINT_ERROR' | 'INCOMPLETE_DATE_ERROR' | 'INVALID_VALUE_FOR_PATTERN_ERROR' | 'INVALID_DEFAULT_VALUE_ERROR' | 'NO_CONNECTED_CONFERENCING_RESOURCE_ERROR';
interface CreateSubmissionBySubmitterRequest {
    /** Submission to create. */
    submission?: FormSubmission;
    /** A flag indicating whether this operation is a repeated creation, such as restoring a previously manually reported as spam entity. */
    repeatedCreation?: boolean;
    /** Validation will be mode is more forgiving, for example "required" won't be validated. */
    lenientValidation?: boolean;
}
interface CreateSubmissionBySubmitterResponse {
    /** The created submission. */
    submission?: FormSubmission;
}
interface BulkCreateSubmissionBySubmitterRequest {
    /**
     * Form id. Restricts submissions creation for a single form.
     * @format GUID
     */
    formId?: string;
    /**
     * Submissions to create.
     * Deprecated
     * @maxSize 100
     */
    submissions?: FormSubmission[];
    /** When set, items will be returned on successful create. */
    returnEntity?: boolean;
    /**
     * Submissions data to create.
     * @maxSize 100
     */
    submissionsV2?: BulkCreateSubmissionBySubmitterData[];
    /** Validation will be mode is more forgiving, for example "required" won't be validated. */
    lenientValidation?: boolean;
}
interface BulkCreateSubmissionBySubmitterData {
    /** Submissions to create. */
    submission?: FormSubmission;
    /** A flag indicating whether this operation is a repeated creation, such as restoring a previously manually reported as spam entity. */
    repeatedCreation?: boolean;
}
interface BulkCreateSubmissionBySubmitterResponse {
    /** Created submissions with metadata */
    results?: BulkSubmissionResult[];
    /** Metadata of request */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkSubmissionResult {
    /** Created submission metadata */
    itemMetadata?: ItemMetadata;
    /** The created submission. */
    item?: FormSubmission;
}
interface ItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface GetSubmissionRequest {
    /**
     * ID of the submission to retrieve.
     * @format GUID
     */
    submissionId: string;
}
interface GetSubmissionResponse {
    /** The retrieved submission. */
    submission?: FormSubmission;
}
interface GetSubmissionByCheckoutIdRequest {
    /**
     * Checkout ID of the submission to retrieve.
     * @format GUID
     */
    checkoutId?: string;
}
interface GetSubmissionByCheckoutIdResponse {
    /** The retrieved submission. */
    submission?: FormSubmission;
}
interface UpdateSubmissionRequest {
    /** Submission to update. */
    submission: FormSubmission;
}
interface UpdateSubmissionResponse {
    /** The updated submission. */
    submission?: FormSubmission;
}
interface ConfirmSubmissionRequest {
    /**
     * Submission ID to confirm.
     * @format GUID
     */
    submissionId: string;
}
interface ConfirmSubmissionResponse {
    /** The confirmed submission. */
    submission?: FormSubmission;
}
interface DeleteSubmissionRequest {
    /**
     * ID of the submission to delete.
     * @format GUID
     */
    submissionId: string;
    /**
     * Delete the submission, bypassing the trash bin. This means that the submission is permanently deleted and cannot be restored.
     *
     *
     * Default: `false`
     */
    permanent?: boolean;
    /** Whether to preserve files, associated with the submission. If the value is `false`, then the files are deleted after 210 days. */
    preserveFiles?: boolean;
}
interface DeleteSubmissionResponse {
}
interface BulkDeleteSubmissionRequest {
    /**
     * Form ID.
     * @format GUID
     */
    formId: string;
    /**
     * Submission ids.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    submissionIds?: string[];
    /**
     * Delete submission bypassing trash-bin
     * Default: false
     */
    permanent?: boolean;
    /** Preserve files. */
    preserveFiles?: boolean;
}
interface BulkDeleteSubmissionResponse {
    /**
     * Results of bulk submission delete
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkDeleteSubmissionResult[];
    /** Metadata of request */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkDeleteSubmissionResult {
    /** Deleted item metadata */
    itemMetadata?: ItemMetadata;
}
interface RestoreSubmissionFromTrashBinRequest {
    /**
     * ID of the submission to restore.
     * @format GUID
     */
    submissionId: string;
}
interface RestoreSubmissionFromTrashBinResponse {
    /** The restored submission. */
    submission?: FormSubmission;
}
interface RemoveSubmissionFromTrashBinRequest {
    /**
     * ID of the submission to restore.
     * @format GUID
     */
    submissionId: string;
}
interface RemoveSubmissionFromTrashBinResponse {
}
interface BulkRemoveSubmissionFromTrashBinRequest {
    /**
     * Form ID.
     * @format GUID
     */
    formId: string;
    /**
     * Submission ids.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    submissionIds?: string[];
}
interface BulkRemoveSubmissionFromTrashBinResponse {
    /**
     * Results of bulk submission removal from trash
     * @maxSize 100
     */
    results?: BulkRemoveSubmissionFromTrashBinResult[];
    /** Metadata of request */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkRemoveSubmissionFromTrashBinResult {
    /** Deleted item metadata */
    itemMetadata?: ItemMetadata;
}
interface ListDeletedSubmissionsRequest {
    /**
     * Form ID.
     * @format GUID
     */
    formId: string;
    /**
     * Submission ids.
     * @maxSize 10
     * @format GUID
     */
    submissionIds?: string[];
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not filter or `order`. */
    paging?: CursorPaging;
    /**
     * List of statuses of submissions which should be returned
     * Default: CONFIRMED
     * @maxSize 4
     */
    statuses?: SubmissionStatusWithLiterals[];
    /**
     * Identifies the app which the form submissions belong to. For example, the namespace for the Wix Forms App is `"wix.form_app.form"`. The namespace of a submission can be retrieved using the Get Submission endpoint.
     * @maxLength 50
     */
    namespace?: string;
}
interface ListDeletedSubmissionsResponse {
    /** The retrieved Submissions. */
    submissions?: FormSubmission[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface GetDeletedSubmissionRequest {
    /**
     * Submission id.
     * @format GUID
     */
    submissionId: string;
}
interface GetDeletedSubmissionResponse {
    /** The retrieved Submission. */
    submission?: FormSubmission;
}
interface QuerySubmissionRequest {
    /** Query options. */
    query: CursorQuery;
    /** Whether to return only your own submissions. If `false`, returns all submissions based on query filters. */
    onlyYourOwn?: boolean;
}
interface QuerySubmissionResponse {
    /** The retrieved submissions. */
    submissions?: FormSubmission[];
    /** Paging metadata. */
    metadata?: CursorPagingMetadata;
}
interface SearchSubmissionsByNamespaceRequest {
    /** Query options. */
    search: CursorSearch;
}
interface SearchSubmissionsByNamespaceResponse {
    /** The retrieved Submissions. */
    submissions?: FormSubmission[];
    /** Paging metadata. */
    metadata?: CursorPagingMetadata;
}
interface SearchSubmissionsByNamespaceForExportRequest {
    /** Legacy path: reconstructed into `CursorSearch` for `searchSubmissionsByNamespace` via `SubmissionMappers.searchForExportToSearchRequest`. */
    query?: CursorQuery;
    /** When set with a non-empty `CursorSearch` (filter / sort / free-text / paging), forwarded as `SearchSubmissionsByNamespaceRequest.search`. */
    search?: CursorSearch;
}
interface SearchSubmissionsByNamespaceForExportResponse {
    /** The retrieved Submissions. */
    submissions?: FormSubmission[];
    /** Paging metadata. */
    metadata?: CursorPagingMetadata;
}
interface QuerySubmissionsByNamespaceRequest {
    /** Query options. */
    query: CursorQuery;
    /** Whether to return only your own submissions. If `false`, returns all submissions based on query filters. */
    onlyYourOwn?: boolean;
}
interface QuerySubmissionsByNamespaceResponse {
    /** The retrieved Submissions. */
    submissions?: FormSubmission[];
    /** Paging metadata. */
    metadata?: CursorPagingMetadata;
}
interface QuerySubmissionsByNamespaceForExportRequest {
    /** Query options. */
    query?: CursorQuery;
}
interface QuerySubmissionsByNamespaceForExportResponse {
    /** The retrieved Submissions. */
    submissions?: FormSubmission[];
    /** Paging metadata. */
    metadata?: CursorPagingMetadata;
}
interface CountSubmissionsByFilterRequest {
    /** A filter object. Must filter by namespace. */
    filter: Record<string, any> | null;
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
}
interface CountSubmissionsByFilterResponse {
    /** Forms submission count. */
    formsSubmissionsCount?: FormSubmissionsCount[];
}
interface FormSubmissionsCount {
    /**
     * Form ID.
     * @format GUID
     */
    formId?: string;
    /** Total number of submissions. */
    totalCount?: number;
    /** Number of submissions that the site owner hasn't seen yet. */
    unseenCount?: number;
}
interface CountSubmissionsRequest {
    /**
     * Form IDs which submissions should be counted.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    formIds: string[];
    /**
     * The app which the form submissions belong to. For example, the namespace for the Wix Forms app is `wix.form_app.form`. Call `getSubmission()` to retrieve the namespace.
     * @minLength 10
     * @maxLength 50
     */
    namespace: string;
    /**
     * Status of the submission.
     * - `PENDING`: A submission is created, but has not yet been recorded in the Wix Forms collection.
     * - `PAYMENT_WAITING`: A form submission requiring payment is created.
     * - `PAYMENT_CANCELED`: An order of a form submission is canceled.
     * - `CONFIRMED`: A submission is recorded in the Wix Forms collection.
     * @maxSize 4
     */
    statuses?: SubmissionStatusWithLiterals[];
}
interface CountSubmissionsResponse {
    /** Forms submission count. */
    formsSubmissionsCount?: FormSubmissionsCount[];
}
interface CountDeletedSubmissionsRequest {
    /**
     * Form IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    formIds: string[];
    /**
     * Identifies the app which the form submissions belong to. For example, the namespace for the Wix Forms App is `"wix.form_app.form"`. The namespace of a submission can be retrieved using the Get Submission endpoint.
     * @minLength 10
     * @maxLength 50
     */
    namespace: string;
    /**
     * List of statuses of submissions which should be taken into count
     * Default: CONFIRMED, PAYMENT_WAITING, PAYMENT_CANCELED
     * @maxSize 4
     */
    statuses?: SubmissionStatusWithLiterals[];
}
interface CountDeletedSubmissionsResponse {
    /** Forms submission count. */
    formsDeletedSubmissionsCount?: FormDeletedSubmissionsCount[];
}
interface FormDeletedSubmissionsCount {
    /**
     * Form ID.
     * @format GUID
     */
    formId?: string;
    /** Total number of submissions. */
    totalCount?: number;
}
interface GetMediaUploadURLRequest {
    /**
     * Form ID.
     * @format GUID
     */
    formId: string;
    /**
     * Name of file to upload.
     * @maxLength 300
     */
    filename: string;
    /**
     * [Mime type](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types#) of file to upload.
     *
     * For example, `'image/png'`
     * @maxLength 100
     */
    mimeType: string;
}
interface GetMediaUploadURLResponse {
    /**
     * Url to upload file.
     * @maxLength 500
     */
    uploadUrl?: string;
}
interface BulkMarkSubmissionsAsSeenRequest {
    /**
     * Submission IDs to mark as seen.
     * @format GUID
     * @maxSize 100
     */
    ids?: string[];
    /**
     * ID of the form which the submissions belong to.
     * @format GUID
     */
    formId: string;
}
interface BulkMarkSubmissionsAsSeenResponse {
    /**
     * Job id for the bulk update operation
     * @format GUID
     */
    jobId?: string;
}
interface GetSubmissionDownloadUrlRequest {
    /**
     * Submission ID.
     * @format GUID
     */
    submissionId: string;
}
interface GetSubmissionDownloadUrlResponse {
    /** The retrieved submission document. */
    document?: SubmissionDocument;
}
interface SubmissionDocument extends SubmissionDocumentDocumentOneOf {
    /** Data of ready to download document */
    readyDetails?: DocumentReady;
    /**
     * Document ID.
     * @readonly
     * @maxLength 50
     */
    _id?: string | null;
    /** Document status. */
    status?: StatusWithLiterals;
}
/** @oneof */
interface SubmissionDocumentDocumentOneOf {
    /** Data of ready to download document */
    readyDetails?: DocumentReady;
}
declare enum Status {
    UNKNOWN_STATUS = "UNKNOWN_STATUS",
    READY = "READY",
    PENDING = "PENDING",
    FAILED = "FAILED"
}
/** @enumType */
type StatusWithLiterals = Status | 'UNKNOWN_STATUS' | 'READY' | 'PENDING' | 'FAILED';
interface DocumentReady {
    /**
     * Document download URL.
     * @readonly
     * @maxLength 300
     */
    downloadUrl?: string;
}
interface DownloadSubmissionRequest {
    /**
     * Submission id for which the document is generated.
     * @format GUID
     */
    submissionId: string;
    /**
     * Access token to download the document.
     * @maxLength 10000
     */
    accessToken: string;
}
interface RawHttpResponse {
    body?: Uint8Array;
    statusCode?: number | null;
    headers?: HeadersEntry[];
}
interface HeadersEntry {
    key?: string;
    value?: string;
}
interface GetFormattedSubmissionRequest {
    /**
     * Submission ID.
     * @format GUID
     */
    submissionId: string;
}
interface GetFormattedSubmissionResponse {
    /**
     * Submission ID.
     * @format GUID
     */
    submissionId?: string;
    /**
     * Ordered by form layout list of field submissions.
     * @maxSize 100
     */
    submissions?: FormattedSubmission[];
}
interface FormattedSubmission {
    /**
     * Label of form field
     * @maxLength 500
     */
    label?: string | null;
    /**
     * Key of the submission, and target of a form field
     * @maxLength 200
     */
    key?: string;
    /**
     * Value of the submission, formatted as a string
     * @maxLength 500
     */
    value?: string;
    /**
     * Id of the form field
     * @format GUID
     */
    fieldId?: string;
}
interface ListFormattedSubmissionsRequest {
    /**
     * Namespace.
     * @minLength 10
     * @maxLength 50
     */
    namespace?: string;
    /**
     * Submission IDs.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    submissionIds?: string[];
    /**
     * Optional additional filter of form IDs.
     * @format GUID
     * @maxSize 100
     */
    formIds?: string[];
}
interface ListFormattedSubmissionsResponse {
    /**
     * List of formatted submissions
     * @maxSize 100
     */
    formattedFormSubmissions?: FormattedFormSubmission[];
}
interface FormattedFormSubmission {
    /**
     * Submission ID.
     * @format GUID
     */
    submissionId?: string;
    /**
     * Ordered by form layout list of field submissions.
     * @maxSize 100
     */
    submissions?: FormattedSubmission[];
    /**
     * Name of the submitted form
     * @maxLength 200
     */
    formName?: string;
}
interface UpdateExtendedFieldsRequest {
    /** ID of the entity to update. */
    _id: string;
    /** Identifier for the app whose extended fields are being updated. */
    namespace: string;
    /** Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured. */
    namespaceData: Record<string, any> | null;
}
interface UpdateExtendedFieldsResponse {
    /**
     * namespace that was updated
     * @minLength 10
     * @maxLength 50
     */
    namespace?: string;
    /** only data from UpdateExtendedFieldsRequest `namespaceData` */
    namespaceData?: Record<string, any> | null;
}
interface BulkUpdateFormSubmissionTagsRequest {
    /**
     * Submission IDs to update tags for.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    submissionIds: string[];
    /** Tags to be added to submissions */
    assignTags?: PublicTags;
    /** Tags to be removed from submissions */
    unassignTags?: PublicTags;
}
interface BulkUpdateFormSubmissionTagsResponse {
    /**
     * Updated submissions with results
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateFormSubmissionTagsResult[];
    /** Metadata of request */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateFormSubmissionTagsResult {
    /** Updated item metadata */
    itemMetadata?: ItemMetadata;
}
interface BulkUpdateFormSubmissionTagsByFilterRequest {
    /** Filter to select the submission to update */
    filter: Record<string, any> | null;
    /** Tags to be added to submissions */
    assignTags?: PublicTags;
    /** Tags to be removed from submissions */
    unassignTags?: PublicTags;
}
interface BulkUpdateFormSubmissionTagsByFilterResponse {
    /**
     * Job id for the bulk update operation
     * @format GUID
     */
    jobId?: string;
}
interface ValidateFormSubmissionRequest {
    /** Submission to validate. */
    submission: FormSubmission;
    /** Optional metadata passed to SPI implementer, allowing clients to include additional business-specific data. */
    additionalMetadata?: Record<string, any> | null;
    /**
     * List of field targets/keys to validate, in case when only several fields should to be validated.
     * If empty, all form fields be validated
     * @maxSize 200
     * @maxLength 200
     */
    fieldsToValidate?: string[];
}
interface ValidateFormSubmissionResponse {
    /**
     * Flag indicating if the submission is valid
     * Default: true
     */
    valid?: boolean | null;
    /**
     * Submission validation violations
     * @maxSize 100
     */
    violationErrors?: FieldViolation[];
}
interface FieldViolation extends FieldViolationErrorDataOneOf {
    /** Validation errors */
    submissionValidationErrors?: SubmissionValidationErrors;
    /**
     * Name of violated entity
     * @maxLength 50
     */
    name?: string;
    /**
     * Description of the error
     * @maxLength 512
     */
    description?: string;
    /**
     * Rule name
     * @maxLength 50
     */
    ruleName?: string;
}
/** @oneof */
interface FieldViolationErrorDataOneOf {
    /** Validation errors */
    submissionValidationErrors?: SubmissionValidationErrors;
}
interface SubmissionValidationErrors {
    /**
     * Submission validation errors.
     * @maxSize 100
     */
    errors?: SubmissionValidationError[];
}
interface SubmissionValidationError extends SubmissionValidationErrorErrorMessageOneOf {
    /** Predefined error type. */
    errorType?: SubmissionErrorTypeWithLiterals;
    /**
     * Custom error message. The message is displayed instead of an error type.
     * @maxLength 500
     */
    customErrorMessage?: string;
    /**
     * Path indicating the source of the error, such as `form.fields.target`.
     * @maxLength 500
     */
    errorPath?: string;
    /** Additional error parameters. */
    params?: Record<string, any> | null;
}
/** @oneof */
interface SubmissionValidationErrorErrorMessageOneOf {
    /** Predefined error type. */
    errorType?: SubmissionErrorTypeWithLiterals;
    /**
     * Custom error message. The message is displayed instead of an error type.
     * @maxLength 500
     */
    customErrorMessage?: string;
}
declare enum SubmissionErrorType {
    /** Error is unknown or unsuitable for any of the options below. */
    UNKNOWN_ERROR = "UNKNOWN_ERROR",
    /** Type of submitted value is incorrect. */
    TYPE_ERROR = "TYPE_ERROR",
    /** Value is required to be provided. */
    REQUIRED_VALUE_ERROR = "REQUIRED_VALUE_ERROR",
    /** Value contains additional properties not expected in schema. */
    UNKNOWN_VALUE_ERROR = "UNKNOWN_VALUE_ERROR",
    /** Text value exceeds max length. */
    MAX_LENGTH_ERROR = "MAX_LENGTH_ERROR",
    /** Text value does not reach min length. */
    MIN_LENGTH_ERROR = "MIN_LENGTH_ERROR",
    /** Text value is not applicable for expected pattern. */
    PATTERN_ERROR = "PATTERN_ERROR",
    /** Text value is not applicable for expected format. */
    FORMAT_ERROR = "FORMAT_ERROR",
    /** Number value is too big. */
    MAX_VALUE_ERROR = "MAX_VALUE_ERROR",
    /** Number value is too small. */
    MIN_VALUE_ERROR = "MIN_VALUE_ERROR",
    /** Number value is not a multiple of expected number. */
    MULTIPLE_OF_VALUE_ERROR = "MULTIPLE_OF_VALUE_ERROR",
    /** Array value has too many items. */
    MIN_ITEMS_ERROR = "MIN_ITEMS_ERROR",
    /** Array value has not enough items. */
    MAX_ITEMS_ERROR = "MAX_ITEMS_ERROR",
    /** Value is not in the list of allowed values. */
    NOT_ALLOWED_VALUE_ERROR = "NOT_ALLOWED_VALUE_ERROR",
    /** Submitted form is disabled. */
    DISABLED_FORM_ERROR = "DISABLED_FORM_ERROR"
}
/** @enumType */
type SubmissionErrorTypeWithLiterals = SubmissionErrorType | 'UNKNOWN_ERROR' | 'TYPE_ERROR' | 'REQUIRED_VALUE_ERROR' | 'UNKNOWN_VALUE_ERROR' | 'MAX_LENGTH_ERROR' | 'MIN_LENGTH_ERROR' | 'PATTERN_ERROR' | 'FORMAT_ERROR' | 'MAX_VALUE_ERROR' | 'MIN_VALUE_ERROR' | 'MULTIPLE_OF_VALUE_ERROR' | 'MIN_ITEMS_ERROR' | 'MAX_ITEMS_ERROR' | 'NOT_ALLOWED_VALUE_ERROR' | 'DISABLED_FORM_ERROR';
/** @docsIgnore */
type CreateSubmissionApplicationErrors = {
    code?: 'INVALID_CAPTCHA';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SITE_IS_A_TEMPLATE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'FORM_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'FORM_RETRIEVAL_ERROR';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type CreateSubmissionValidationErrors = {
    ruleName?: 'SUBMISSION_VALIDATION';
};
/** @docsIgnore */
type UpdateSubmissionValidationErrors = {
    ruleName?: 'SUBMISSION_VALIDATION';
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
    accountInfo?: AccountInfoMetadata;
}
interface AccountInfoMetadata {
    /** ID of the Wix account associated with the event */
    accountId: string;
    /** ID of the Wix site associated with the event. Only included when the event is tied to a specific site. */
    siteId?: string;
    /** ID of the parent Wix account. Only included when 'accountId' belongs to a child account. */
    parentAccountId?: string;
}
interface SubmissionCreatedEnvelope {
    entity: FormSubmission;
    metadata: EventMetadata;
}
/**
 * > **Note:** The Form Submission API only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.
 * <br>
 * Triggered when a submission is created.
 * @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_created
 * @slug created
 */
declare function onSubmissionCreated(handler: (event: SubmissionCreatedEnvelope) => void | Promise<void>): void;
interface SubmissionDeletedEnvelope {
    entity: FormSubmission;
    metadata: EventMetadata;
}
/**
 * > **Note:** The Form Submission API only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.
 * <br>
 * Triggered when a submission is deleted.
 * @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_deleted
 * @slug deleted
 */
declare function onSubmissionDeleted(handler: (event: SubmissionDeletedEnvelope) => void | Promise<void>): void;
interface SubmissionRemovedSubmissionFromTrashEnvelope {
    data: RemovedSubmissionFromTrash;
    metadata: EventMetadata;
}
/** @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_removed_submission_from_trash
 * @slug removed_submission_from_trash
 */
declare function onSubmissionRemovedSubmissionFromTrash(handler: (event: SubmissionRemovedSubmissionFromTrashEnvelope) => void | Promise<void>): void;
interface SubmissionStatusUpdatedEnvelope {
    data: FormSubmissionStatusUpdatedEvent;
    metadata: EventMetadata;
}
/** @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_status_updated
 * @slug status_updated
 */
declare function onSubmissionStatusUpdated(handler: (event: SubmissionStatusUpdatedEnvelope) => void | Promise<void>): void;
interface SubmissionContactMappedEnvelope {
    data: SubmissionContactMapped;
    metadata: EventMetadata;
}
/** @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_submission_contact_mapped
 * @slug submission_contact_mapped
 */
declare function onSubmissionContactMapped(handler: (event: SubmissionContactMappedEnvelope) => void | Promise<void>): void;
interface SubmissionContactMappingSkippedEnvelope {
    data: SubmissionContactMappingSkipped;
    metadata: EventMetadata;
}
/** @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_submission_contact_mapping_skipped
 * @slug submission_contact_mapping_skipped
 */
declare function onSubmissionContactMappingSkipped(handler: (event: SubmissionContactMappingSkippedEnvelope) => void | Promise<void>): void;
interface SubmissionUpdatedEnvelope {
    entity: FormSubmission;
    metadata: EventMetadata;
    /** @hidden */
    modifiedFields: Record<string, any>;
}
/**
 * > **Note:** The Form Submission API only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.
 * <br>
 * Triggered when:
 * - Submission is updated.
 * - Submission order status is updated.
 * - Submission is confirmed.
 * @permissionScope Manage Submissions
 * @permissionScopeId SCOPE.DC-FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-ORDERS
 * @permissionScope Manage form submissions.
 * @permissionScopeId SCOPE.FORMS.MANAGE-SUBMISSIONS
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-PAIDPLANS.READ-ORDERS
 * @permissionScope Manage plans and orders of Pricing Plans
 * @permissionScopeId SCOPE.DC-PAIDPLANS.MANAGE-PLANS-AND-ORDERS
 * @permissionScope Read Submissions
 * @permissionScopeId SCOPE.DC-FORMS.READ-SUBMISSIONS
 * @permissionId WIX_FORMS.SUBMISSION_READ_ANY
 * @webhook
 * @eventType wix.forms.v4.submission_updated
 * @slug updated
 */
declare function onSubmissionUpdated(handler: (event: SubmissionUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Upserts contact from submission.
 * @param submissionId - Submission from which contact needs to be upserted.
 * @public
 * @documentationMaturity preview
 * @requiredField submissionId
 * @permissionId WIX_FORMS.SUBMISSION_CONTACT_UPSERT
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionContactUpsertService.UpsertContactFromSubmission
 */
declare function upsertContactFromSubmission(submissionId: string, options?: UpsertContactFromSubmissionOptions): Promise<NonNullablePaths<UpsertContactFromSubmissionResponse, `submitContactResponse.contactId` | `submitContactResponse.identityType` | `submitContactResponse.newContact`, 1>>;
interface UpsertContactFromSubmissionOptions {
    /**
     * Optional contact id to which submission should be mapped.
     * @format GUID
     */
    contactId?: string | null;
    /** Indicates contact has verified primary email. */
    emailVerified?: boolean;
}
/**
 * Creates a submission.
 *
 *
 * The `createSubmission()` function is an alternative way to the [`WixFormsV2`](https://www.wix.com/velo/reference/$w/wixformsv2/submit) element for submitting a form. In this case, clicking the submit button is unnecessary, the submission is automatically created when calling this function.
 * @param submission - Submission to create.
 * @public
 * @requiredField submission
 * @requiredField submission.formId
 * @param options - Optional fields.
 * @permissionId forms:v4:submission:create_submission
 * @applicableIdentity APP
 * @returns The created submission.
 * @fqn wix.forms.v4.FormSubmissionService.CreateSubmission
 */
declare function createSubmission(submission: NonNullablePaths<FormSubmission, `formId`, 0>, options?: CreateSubmissionOptions): Promise<NonNullablePaths<FormSubmission, `formId` | `namespace` | `status` | `seen` | `orderDetails.itemSubtotal` | `orderDetails.checkoutId` | `tags.tags.tagIds`, 2> & {
    __applicationErrorsType?: CreateSubmissionApplicationErrors;
    __validationErrorsType?: CreateSubmissionValidationErrors;
}>;
interface CreateSubmissionOptions {
    /**
     * Captcha token.
     * @minLength 1
     * @maxLength 3000
     */
    captchaToken?: string | null;
    /** Optional metadata passed to SPI implementer, allowing clients to include additional business-specific data. */
    additionalMetadata?: Record<string, any> | null;
}
/**
 * Retrieves a submission by ID.
 * @param submissionId - ID of the submission to retrieve.
 * @public
 * @requiredField submissionId
 * @permissionId forms:v4:submission:get_submission
 * @applicableIdentity APP
 * @returns The retrieved submission.
 * @fqn wix.forms.v4.FormSubmissionService.GetSubmission
 */
declare function getSubmission(submissionId: string): Promise<NonNullablePaths<FormSubmission, `formId` | `namespace` | `status` | `seen` | `orderDetails.itemSubtotal` | `orderDetails.checkoutId` | `tags.tags.tagIds`, 2>>;
/**
 * Updates a submission.
 *
 *
 * Each time the submission is updated, `revision` increments by 1. The existing `revision` must be included when updating the submission. This ensures you're working with the latest submission information, and prevents unintended overwrites.
 * @param _id - Submission ID.
 * @public
 * @requiredField _id
 * @requiredField submission
 * @requiredField submission.formId
 * @requiredField submission.revision
 * @param submission - Submission to update.
 * @permissionId forms:v4:submission:update_submission
 * @applicableIdentity APP
 * @returns The updated submission.
 * @fqn wix.forms.v4.FormSubmissionService.UpdateSubmission
 */
declare function updateSubmission(_id: string, submission: NonNullablePaths<UpdateSubmission, `formId` | `revision`, 0>): Promise<NonNullablePaths<FormSubmission, `formId` | `namespace` | `status` | `seen` | `orderDetails.itemSubtotal` | `orderDetails.checkoutId` | `tags.tags.tagIds`, 2> & {
    __validationErrorsType?: UpdateSubmissionValidationErrors;
}>;
interface UpdateSubmission {
    /**
     * Submission ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * ID of the form which the submission belongs to.
     * @format GUID
     * @immutable
     */
    formId?: string;
    /**
     * The app which the form submissions belong to. A submission inherits the namespace from its form schema, so you can't set it directly.
     *
     * For example, a submission of a form visible in the Wix Forms app has the namespace `wix.form_app.form`, the namespace of the Wix Forms app, whose app ID is `225dd912-7dea-4738-8688-4b8c6955ffc2`.
     *
     * See a list of namespaces for [apps made by Wix](https://dev.wix.com/docs/api-reference/crm/forms/form-schemas/introduction#namespaces-of-apps-made-by-wix) section.
     * To retrieve the ID for a Wix app, see [Apps Created By Wix](https://dev.wix.com/docs/rest/articles/get-started/apps-created-by-wix).
     * @minLength 10
     * @maxLength 50
     * @readonly
     */
    namespace?: string;
    /**
     * Status of the submission.
     *
     * The status a submission is created with depends on the form's namespace and on whether the form collects payment, so read it from the response rather than assuming.
     *
     * A submission for a form that collects payment starts as `PAYMENT_WAITING`. Otherwise, most namespaces record the submission immediately as `CONFIRMED`. Namespaces that need to complete another step first, such as taking payment for a pricing plan, create the submission as `PENDING` and confirm it later with Confirm Submission.
     *
     * A `PENDING` submission isn't recorded yet, and is deleted automatically if it isn't confirmed in time.
     */
    status?: SubmissionStatusWithLiterals;
    /**
     * Values the visitor submitted.
     *
     * Each key is the `target` of a field in the form schema, which is the field's stable storage key rather than its ID or label. Only keys matching a field target are accepted.
     *
     * Each value's shape follows that field's `inputType`: `STRING` fields submit a string, `NUMBER` a number, `BOOLEAN` a boolean, and `ARRAY` an array of the selected option values. Date and time fields are strings. A choice field submits an option's `value`, not its ID or its label.
     *
     * 3 input types submit structured values: `ADDRESS` submits an address object, `PAYMENT` an array holding 1 product object per item bought, and `SCHEDULING` an appointment object.
     *
     * For the full mapping, see [About Submission Values](https://dev.wix.com/docs/api-reference/crm/forms/form-submissions/about-submission-values).
     */
    submissions?: Record<string, any>;
    /**
     * Date and time the form submission was created.
     * @immutable
     */
    _createdDate?: Date | null;
    /**
     * Date and time the form submission was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Revision number, which increments by 1 each time the form submission is updated. To prevent conflicting changes, the existing revision must be used when updating a form submission.
     * @readonly
     */
    revision?: string | null;
    /**
     * ID of the visitor that submitted the form.
     * @readonly
     */
    submitter?: Submitter;
    /** Whether a site owner marked a submission as "seen". */
    seen?: boolean;
    /** Data extension object that holds users' and apps' fields. */
    extendedFields?: ExtendedFields;
    /**
     * Order details. <br>
     * <b>Note</b>: This object is only applicable when submitting a form in the Wix Payments app.
     */
    orderDetails?: OrderDetails;
    /**
     * Contact ID of a site visitor who created the submission.
     * @format GUID
     * @immutable
     */
    contactId?: string | null;
    /**
     * Authorized viewer ID.
     * @format GUID
     * @immutable
     */
    accessRestriction?: string | null;
    /** Tag IDs collections associated with the current entity. */
    tags?: PublicTags;
    /**
     * Appointment details.
     * This object is only applicable when submitting a form that creates a booking appointment.
     */
    appointmentDetails?: AppointmentDetails;
    /**
     * ID of the child site the submission was made on.
     *
     * Only present when the form was submitted on a site that shares its data with a parent site. In that case submissions from every child site are stored on the parent site, and this field identifies which child site the submission came from.
     * @format GUID
     * @readonly
     */
    childMetaSiteId?: string | null;
}
/**
 * Confirms a submission.
 *
 *
 * You can only confirm a submission that has a `PENDING` status.
 * When using forms from the [Wix Pricing Plans](https://www.wix.com/app-market/paid-plans?referral=collection&appIndex=42&referralTag=made-by-wix&referralSectionName=made-by-wix) app, the default submission status is `PENDING`.
 * When using forms from the [Wix Forms]() app, the default form submission status is `CONFIRMED`. You can change the default status for individual submissions using the `updateSubmission()` method.
 * @param submissionId - Submission ID to confirm.
 * @public
 * @requiredField submissionId
 * @permissionId forms:v4:submission:confirm_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.ConfirmSubmission
 */
declare function confirmSubmission(submissionId: string): Promise<NonNullablePaths<ConfirmSubmissionResponse, `submission.formId` | `submission.namespace` | `submission.status` | `submission.seen` | `submission.orderDetails.itemSubtotal` | `submission.orderDetails.checkoutId` | `submission.tags.tags.tagIds`, 3>>;
/**
 * Deletes a submission.
 *
 *
 * This function moves the form submission into the trash bin. To delete the submission permanently, change the default `permanent` field value to `true.`
 * @param submissionId - ID of the submission to delete.
 * @public
 * @requiredField submissionId
 * @param options - Optional fields.
 * @permissionId forms:v4:submission:delete_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.DeleteSubmission
 */
declare function deleteSubmission(submissionId: string, options?: DeleteSubmissionOptions): Promise<void>;
interface DeleteSubmissionOptions {
    /**
     * Delete the submission, bypassing the trash bin. This means that the submission is permanently delete and cannot be restored.
     *
     *
     * Default: `false`
     */
    permanent?: boolean;
    /** Whether to preserve files, associated with the submission. If the value is `false`, then the files are deleted after 210 days. */
    preserveFiles?: boolean;
}
/**
 * Deletes submissions by IDS for specific form.
 * @param formId - Form ID.
 * @public
 * @documentationMaturity preview
 * @requiredField formId
 * @permissionId forms:v4:submission:bulk_delete_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.BulkDeleteSubmission
 */
declare function bulkDeleteSubmission(formId: string, options?: BulkDeleteSubmissionOptions): Promise<NonNullablePaths<BulkDeleteSubmissionResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkDeleteSubmissionOptions {
    /**
     * Submission ids.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    submissionIds?: string[];
    /**
     * Delete submission bypassing trash-bin
     * Default: false
     */
    permanent?: boolean;
    /** Preserve files. */
    preserveFiles?: boolean;
}
/**
 * Restores deleted submission
 * @param submissionId - ID of the submission to restore.
 * @public
 * @documentationMaturity preview
 * @requiredField submissionId
 * @permissionId forms:v4:submission:restore_submission_from_trash_bin
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.RestoreSubmissionFromTrashBin
 */
declare function restoreSubmissionFromTrashBin(submissionId: string): Promise<NonNullablePaths<RestoreSubmissionFromTrashBinResponse, `submission.formId` | `submission.namespace` | `submission.status` | `submission.seen` | `submission.orderDetails.itemSubtotal` | `submission.orderDetails.checkoutId` | `submission.tags.tags.tagIds`, 3>>;
/**
 * Remove deleted submission
 * @param submissionId - ID of the submission to restore.
 * @public
 * @documentationMaturity preview
 * @requiredField submissionId
 * @permissionId forms:v4:submission:remove_submission_from_trash_bin
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.RemoveSubmissionFromTrashBin
 */
declare function removeSubmissionFromTrashBin(submissionId: string): Promise<void>;
/**
 * Remove multiple deleted submissions
 * @param formId - Form ID.
 * @public
 * @documentationMaturity preview
 * @requiredField formId
 * @permissionId forms:v4:submission:bulk_remove_submission_from_trash_bin
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.BulkRemoveSubmissionFromTrashBin
 */
declare function bulkRemoveSubmissionFromTrashBin(formId: string, options?: BulkRemoveSubmissionFromTrashBinOptions): Promise<NonNullablePaths<BulkRemoveSubmissionFromTrashBinResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkRemoveSubmissionFromTrashBinOptions {
    /**
     * Submission ids.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    submissionIds?: string[];
}
/**
 * List deleted submissions
 * @param formId - Form ID.
 * @public
 * @documentationMaturity preview
 * @requiredField formId
 * @permissionId forms:v4:submission:list_deleted_submissions
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.ListDeletedSubmissions
 */
declare function listDeletedSubmissions(formId: string, options?: ListDeletedSubmissionsOptions): Promise<NonNullablePaths<ListDeletedSubmissionsResponse, `submissions` | `submissions.${number}.formId` | `submissions.${number}.namespace` | `submissions.${number}.status` | `submissions.${number}.seen` | `submissions.${number}.orderDetails.itemSubtotal` | `submissions.${number}.orderDetails.checkoutId`, 3>>;
interface ListDeletedSubmissionsOptions {
    /**
     * Submission ids.
     * @maxSize 10
     * @format GUID
     */
    submissionIds?: string[];
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not filter or `order`. */
    paging?: CursorPaging;
    /**
     * List of statuses of submissions which should be returned
     * Default: CONFIRMED
     * @maxSize 4
     */
    statuses?: SubmissionStatusWithLiterals[];
    /**
     * Identifies the app which the form submissions belong to. For example, the namespace for the Wix Forms App is `"wix.form_app.form"`. The namespace of a submission can be retrieved using the Get Submission endpoint.
     * @maxLength 50
     */
    namespace?: string;
}
/**
 * Get deleted submission
 * @param submissionId - Submission id.
 * @public
 * @documentationMaturity preview
 * @requiredField submissionId
 * @permissionId forms:v4:submission:get_deleted_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.GetDeletedSubmission
 */
declare function getDeletedSubmission(submissionId: string): Promise<NonNullablePaths<GetDeletedSubmissionResponse, `submission.formId` | `submission.namespace` | `submission.status` | `submission.seen` | `submission.orderDetails.itemSubtotal` | `submission.orderDetails.checkoutId` | `submission.tags.tags.tagIds`, 3>>;
/**
 * Deprecated on '2023-08-08'. Use QuerySubmissionsByNamespace.
 * @param query - Query options.
 * @public
 * @documentationMaturity preview
 * @requiredField query
 * @permissionId forms:v4:submission:query_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.QuerySubmission
 * @deprecated
 * @replacedBy com.wixpress.forms.v4.FormSubmissionService.QuerySubmissionsByNamespace
 * @targetRemovalDate 2025-04-01
 */
declare function querySubmission(query: CursorQuery, options?: QuerySubmissionOptions): Promise<NonNullablePaths<QuerySubmissionResponse, `submissions` | `submissions.${number}.formId` | `submissions.${number}.namespace` | `submissions.${number}.status` | `submissions.${number}.seen` | `submissions.${number}.orderDetails.itemSubtotal` | `submissions.${number}.orderDetails.checkoutId`, 3>>;
interface QuerySubmissionOptions {
    /** Whether to return only your own submissions. If `false`, returns all submissions based on query filters. */
    onlyYourOwn?: boolean;
}
interface FormSubmissionSearchSpec extends SearchSpec {
    paging: 'cursor';
    wql: [
        {
            operators: ['$eq', '$in', '$ne', '$nin'];
            fields: ['_id'];
            sort: 'BOTH';
        },
        {
            operators: ['$eq', '$in', '$ne', '$nin'];
            fields: ['formId'];
            sort: 'BOTH';
        },
        {
            operators: ['$eq'];
            fields: ['namespace'];
            sort: 'NONE';
        },
        {
            operators: ['$eq', '$in', '$ne', '$nin'];
            fields: ['status'];
            sort: 'BOTH';
        },
        {
            operators: ['$eq', '$gt', '$gte', '$in', '$lt', '$lte', '$ne', '$nin'];
            fields: ['_createdDate'];
            sort: 'BOTH';
        }
    ];
}
type CommonSearchWithEntityContext = Search<FormSubmission, FormSubmissionSearchSpec>;
type FormSubmissionSearch = {
    /**
    Cursor pointing to page of results.
    When requesting 'cursor_paging.cursor', no `filter`, `sort` or `search` can be provided.
    */
    cursorPaging?: {
        /**
      Number of items to load.
      @max: 100
      */
        limit?: NonNullable<CommonSearchWithEntityContext['cursorPaging']>['limit'] | null;
        /**
      Pointer to the next or previous page in the list of results.
    
      You can get the relevant cursor token
      from the `pagingMetadata` object in the previous call's response.
      Not relevant for the first request.
      @maxLength: 16000
      */
        cursor?: NonNullable<CommonSearchWithEntityContext['cursorPaging']>['cursor'] | null;
    };
    /**
    A filter object. See documentation [here](https://bo.wix.com/wix-docs/rnd/platformization-guidelines/api-query-language#platformization-guidelines_api-query-language_defining-in-protobuf)
    */
    filter?: CommonSearchWithEntityContext['filter'] | null;
    /**
    Sort object in the form [{"fieldName":"sortField1"},{"fieldName":"sortField2","direction":"DESC"}]
    @maxSize: 10
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 512
      */
        fieldName?: NonNullable<CommonSearchWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
      */
        order?: NonNullable<CommonSearchWithEntityContext['sort']>[number]['order'];
    }[];
    /**
    Free text to match in searchable fields
    */
    search?: {
        /**
      Defines how separate search terms in `expression` are combined
      */
        mode?: NonNullable<CommonSearchWithEntityContext['search']>['mode'];
        /**
      Search term or expression
      @maxLength: 100
      */
        expression?: NonNullable<CommonSearchWithEntityContext['search']>['expression'] | null;
        /**
      Flag if should use auto fuzzy search (allowing typos by a managed proximity algorithm)
      */
        fuzzy?: NonNullable<CommonSearchWithEntityContext['search']>['fuzzy'];
    };
};
declare const utils: {
    search: _wix_sdk_types.SearchHelpers<FormSubmission, FormSubmissionSearchSpec, FormSubmissionSearch>;
    query: _wix_sdk_types.QueryHelpers<FormSubmission, FormSubmissionQuerySpec, FormSubmissionQuery>;
};
/**
 * Creates a query to retrieve a list of submissions.
 *
 *
 * The `querySubmissionsByNamespace()` method builds a query to retrieve a list of submissions from the specified namespace and returns a [`SubmissionsQueryBuilder`](#submissionsquerybuilder) object.
 * >**Note:** You can only query submissions from a specified namespace. Use the query filter on the `namespace` field, otherwise you will receive an error.
 *
 * The returned object contains the query definition, which is typically used to run the query using the [`find()`](#submissionsquerybuilder/find) method.
 *
 * You can refine the query by chaining `SubmissionsQueryBuilder` methods onto the query. `SubmissionsQueryBuilder` methods enable you to sort, filter, and control the results that `querySubmissionsByNamespace()` returns.
 *
 * The following `SubmissionsQueryBuilder` methods are supported for `querySubmissionsByNamespace()`. For a full description of the Submissions object, see the object returned for the [`items`](#submissionsqueryresult/items) property in [`SubmissionsQueryResult`](#submissionsqueryresult).
 * @public
 * @param options - Query options.
 * @permissionId forms:v4:submission:query_submissions_by_namespace
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.QuerySubmissionsByNamespace
 */
declare function querySubmissionsByNamespace(options?: QuerySubmissionsByNamespaceOptions): SubmissionsQueryBuilder;
interface QuerySubmissionsByNamespaceOptions {
    /** Whether to return only your own submissions. If `false`, returns all submissions based on query filters. */
    onlyYourOwn?: boolean | undefined;
}
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface SubmissionsQueryResult extends QueryCursorResult {
    items: FormSubmission[];
    query: SubmissionsQueryBuilder;
    next: () => Promise<SubmissionsQueryResult>;
    prev: () => Promise<SubmissionsQueryResult>;
}
interface SubmissionsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    eq: (propertyName: '_id' | 'formId' | 'namespace' | 'status' | '_createdDate' | '_updatedDate' | 'submitter.memberId' | 'submitter.visitorId' | 'submitter.applicationId' | 'submitter.userId' | 'seen' | 'orderDetails.checkoutId' | 'contactId' | 'childMetaSiteId', value: any) => SubmissionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ne: (propertyName: '_id' | 'formId' | 'status' | '_createdDate' | '_updatedDate' | 'submitter.memberId' | 'submitter.visitorId' | 'submitter.applicationId' | 'submitter.userId' | 'seen' | 'orderDetails.checkoutId' | 'contactId' | 'childMetaSiteId', value: any) => SubmissionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ge: (propertyName: '_createdDate' | '_updatedDate', value: any) => SubmissionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    gt: (propertyName: '_createdDate' | '_updatedDate', value: any) => SubmissionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    le: (propertyName: '_createdDate' | '_updatedDate', value: any) => SubmissionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    lt: (propertyName: '_createdDate' | '_updatedDate', value: any) => SubmissionsQueryBuilder;
    in: (propertyName: '_id' | 'formId' | 'status' | '_createdDate' | '_updatedDate' | 'submitter.memberId' | 'submitter.visitorId' | 'submitter.applicationId' | 'submitter.userId' | 'orderDetails.checkoutId' | 'contactId' | 'childMetaSiteId', value: any) => SubmissionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    ascending: (...propertyNames: Array<'_id' | 'formId' | 'status' | '_createdDate' | '_updatedDate' | 'submitter.memberId' | 'submitter.visitorId' | 'submitter.applicationId' | 'submitter.userId' | 'seen' | 'orderDetails.checkoutId' | 'contactId'>) => SubmissionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    descending: (...propertyNames: Array<'_id' | 'formId' | 'status' | '_createdDate' | '_updatedDate' | 'submitter.memberId' | 'submitter.visitorId' | 'submitter.applicationId' | 'submitter.userId' | 'seen' | 'orderDetails.checkoutId' | 'contactId'>) => SubmissionsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object. */
    limit: (limit: number) => SubmissionsQueryBuilder;
    /** @param cursor - A pointer to specific record */
    skipTo: (cursor: string) => SubmissionsQueryBuilder;
    find: () => Promise<SubmissionsQueryResult>;
}
/**
 * @hidden
 * @fqn wix.forms.v4.FormSubmissionService.QuerySubmissionsByNamespace
 * @requiredField query
 */
declare function typedQuerySubmissionsByNamespace(query: FormSubmissionQuery, options?: QuerySubmissionsByNamespaceOptions): Promise<NonNullablePaths<QuerySubmissionsByNamespaceResponse, `submissions` | `submissions.${number}.formId` | `submissions.${number}.namespace` | `submissions.${number}.status` | `submissions.${number}.seen` | `submissions.${number}.orderDetails.itemSubtotal` | `submissions.${number}.orderDetails.checkoutId`, 3>>;
interface FormSubmissionQuerySpec extends QuerySpec {
    paging: 'cursor';
    wql: [
        {
            fields: ['_id'];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: ['formId'];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: ['contactId'];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: ['namespace'];
            operators: ['$eq'];
            sort: 'NONE';
        },
        {
            fields: ['status'];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: ['seen'];
            operators: ['$eq', '$ne'];
            sort: 'BOTH';
        },
        {
            fields: ['_createdDate', '_updatedDate'];
            operators: ['$eq', '$gt', '$gte', '$in', '$lt', '$lte', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: [
                'submitter.applicationId',
                'submitter.memberId',
                'submitter.userId',
                'submitter.visitorId'
            ];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: ['orderDetails.checkoutId'];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'BOTH';
        },
        {
            fields: ['childMetaSiteId'];
            operators: ['$eq', '$in', '$ne', '$nin'];
            sort: 'NONE';
        }
    ];
}
type CommonQueryWithEntityContext = Query<FormSubmission, FormSubmissionQuerySpec>;
type FormSubmissionQuery = {
    /**
    Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.
    */
    cursorPaging?: {
        /**
      Number of items to load.
      @max: 100
      */
        limit?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['limit'] | null;
        /**
      Pointer to the next or previous page in the list of results.
    
      You can get the relevant cursor token
      from the `pagingMetadata` object in the previous call's response.
      Not relevant for the first request.
      @maxLength: 16000
      */
        cursor?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['cursor'] | null;
    };
    /**
    Filter object in the following format:
    `"filter" : {
    "fieldName1": "value1",
    "fieldName2":{"$operator":"value2"}
    }`
    Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
    */
    filter?: CommonQueryWithEntityContext['filter'] | null;
    /**
    Sort object in the following format:
    `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
    @maxSize: 3
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 512
      */
        fieldName?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
      */
        order?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['order'];
    }[];
};
/**
 * > **Note:** The Form Submission API only works with the Wix Forms app. Call [GetAppInstance](https://dev.wix.com/docs/rest/api-reference/app-management/apps/app-instance/get-app-instance) to confirm that the app named `wix_forms` is installed on the site.
 * <br>
 * Counts the number of submissions belonging to forms that were filtered and contain a provided expression.
 * @param filter - A filter object. Must filter by namespace.
 * @public
 * @documentationMaturity preview
 * @requiredField filter
 * @permissionId forms:v4:submission:count_submissions_by_filter
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.CountSubmissionsByFilter
 */
declare function countSubmissionsByFilter(filter: Record<string, any>, options?: CountSubmissionsByFilterOptions): Promise<NonNullablePaths<CountSubmissionsByFilterResponse, `formsSubmissionsCount` | `formsSubmissionsCount.${number}.formId` | `formsSubmissionsCount.${number}.totalCount` | `formsSubmissionsCount.${number}.unseenCount`, 2>>;
interface CountSubmissionsByFilterOptions {
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
}
/**
 * Counts the number of submissions belonging to the specified forms.
 *
 *
 * The `countSubmissions()` function is useful for analytics and tracking purposes. For example, if you have a contact form on your website, you can use this function to track how many submissions it receives daily, weekly, or monthly.
 * @public
 * @requiredField formIds
 * @requiredField namespace
 * @param namespace - The app which the form submissions belong to. For example, the namespace for the Wix Forms app is `wix.form_app.form`. Call `getSubmission()` to retrieve the namespace.
 * @param formIds - Form IDs which submissions should be counted.
 * @permissionId forms:v4:submission:count_submissions
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.CountSubmissions
 */
declare function countSubmissions(formIds: string[], namespace: string, options?: CountSubmissionsOptions): Promise<NonNullablePaths<CountSubmissionsResponse, `formsSubmissionsCount` | `formsSubmissionsCount.${number}.formId` | `formsSubmissionsCount.${number}.totalCount` | `formsSubmissionsCount.${number}.unseenCount`, 2>>;
interface CountSubmissionsOptions {
    /**
     * Status of the submission.
     * - `PENDING`: A submission is created, but has not yet been recorded in the Wix Forms collection.
     * - `PAYMENT_WAITING`: A form submission requiring payment is created.
     * - `PAYMENT_CANCELED`: An order of a form submission is canceled.
     * - `CONFIRMED`: A submission is recorded in the Wix Forms collection.
     * @maxSize 4
     */
    statuses?: SubmissionStatusWithLiterals[];
}
/**
 * > **Note:**
 * > The Submissions API is only available in the Wix Studio editor.
 *
 * Counts the number of submissions belonging to the specified forms.
 * @param formIds - Form IDs.
 * @param namespace - Identifies the app which the form submissions belong to. For example, the namespace for the Wix Forms App is `"wix.form_app.form"`. The namespace of a submission can be retrieved using the Get Submission endpoint.
 * @public
 * @documentationMaturity preview
 * @requiredField formIds
 * @requiredField namespace
 * @permissionId forms:v4:submission:count_deleted_submissions
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.CountDeletedSubmissions
 */
declare function countDeletedSubmissions(formIds: string[], namespace: string, options?: CountDeletedSubmissionsOptions): Promise<NonNullablePaths<CountDeletedSubmissionsResponse, `formsDeletedSubmissionsCount` | `formsDeletedSubmissionsCount.${number}.formId` | `formsDeletedSubmissionsCount.${number}.totalCount`, 2>>;
interface CountDeletedSubmissionsOptions {
    /**
     * List of statuses of submissions which should be taken into count
     * Default: CONFIRMED, PAYMENT_WAITING, PAYMENT_CANCELED
     * @maxSize 4
     */
    statuses?: SubmissionStatusWithLiterals[];
}
/**
 * Retrieves a URL generated by the [Media Manager](https://www.wix.com/velo/reference/wix-media-v2/files/generatefileuploadurl) to use when creating a submission that includes a field for uploading files.
 * > **Note:** You need at least a [Standard Premium](https://support.wix.com/en/article/choosing-a-premium-plan) plan for your site to upload files.
 *
 *
 * To learn how external clients can use the generated upload URL to upload a file to the Media Manager, see [Upload API](https://www.wix.com/velo/reference/wix-media-v2/files/upload-api).
 * @param formId - Form ID.
 * @param filename - Name of file to upload.
 * @param mimeType - [Mime type](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types#) of file to upload.
 *
 * For example, `'image/png'`
 * @public
 * @requiredField filename
 * @requiredField formId
 * @requiredField mimeType
 * @permissionId forms:v4:submission:get_media_upload_url
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.GetMediaUploadURL
 */
declare function getMediaUploadUrl(formId: string, filename: string, mimeType: string): Promise<NonNullablePaths<GetMediaUploadURLResponse, `uploadUrl`, 0>>;
/**
 * Marks form submissions as "seen".
 *
 *
 * This function marks the submissions as if they were seen by the site owner. Only site collaborators with the **[Manage Submission](https://support.wix.com/en/article/roles-permissions-accessing-roles-permissions)** permissions can mark submissions.
 * @public
 * @requiredField formId
 * @requiredField ids
 * @param ids - IDs of submissions to mark as seen.
 * @param formId - ID of the form which the submissions belong to.
 * @permissionId forms:v4:submission:bulk_mark_submissions_as_seen
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.BulkMarkSubmissionsAsSeen
 */
declare function bulkMarkSubmissionsAsSeen(ids: string[], formId: string): Promise<NonNullablePaths<BulkMarkSubmissionsAsSeenResponse, `jobId`, 0>>;
/**
 * Gets submission as document which may include the document download url if the document is ready.
 * @param submissionId - Submission ID.
 * @public
 * @documentationMaturity preview
 * @requiredField submissionId
 * @permissionId forms:v4:submission:get_submission_download_url
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.GetSubmissionDownloadUrl
 */
declare function getSubmissionDownloadUrl(submissionId: string): Promise<NonNullablePaths<GetSubmissionDownloadUrlResponse, `document.readyDetails.downloadUrl` | `document.status`, 2>>;
/**
 * Download a submission document by redirecting to the document URL.
 * @param submissionId - Submission id for which the document is generated.
 * @param accessToken - Access token to download the document.
 * @public
 * @documentationMaturity preview
 * @requiredField accessToken
 * @requiredField submissionId
 * @fqn wix.forms.v4.FormSubmissionService.DownloadSubmission
 */
declare function downloadSubmission(submissionId: string, accessToken: string): Promise<NonNullablePaths<RawHttpResponse, `body` | `headers` | `headers.${number}.key` | `headers.${number}.value`, 2>>;
/**
 * Retrieves a submission formatted by language
 * @param submissionId - Submission ID.
 * @public
 * @documentationMaturity preview
 * @requiredField submissionId
 * @permissionId forms:v4:submission:get_formatted_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.GetFormattedSubmission
 */
declare function getFormattedSubmission(submissionId: string): Promise<NonNullablePaths<GetFormattedSubmissionResponse, `submissionId` | `submissions` | `submissions.${number}.key` | `submissions.${number}.value` | `submissions.${number}.fieldId`, 2>>;
/**
 * Update Extended Fields of the Form
 * @param _id - ID of the entity to update.
 * @param namespace - Identifier for the app whose extended fields are being updated.
 * @public
 * @documentationMaturity preview
 * @requiredField _id
 * @requiredField namespace
 * @requiredField options
 * @requiredField options.namespaceData
 * @permissionId forms:v4:submission:update_extended_fields
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.UpdateExtendedFields
 */
declare function updateExtendedFields(_id: string, namespace: string, options: NonNullablePaths<UpdateExtendedFieldsOptions, `namespaceData`, 0>): Promise<NonNullablePaths<UpdateExtendedFieldsResponse, `namespace`, 0>>;
interface UpdateExtendedFieldsOptions {
    /** Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured. */
    namespaceData: Record<string, any> | null;
}
interface BulkUpdateFormSubmissionTagsOptions {
    /** Tags to be added to submissions */
    assignTags?: PublicTags;
    /** Tags to be removed from submissions */
    unassignTags?: PublicTags;
}
interface BulkUpdateFormSubmissionTagsByFilterOptions {
    /** Tags to be added to submissions */
    assignTags?: PublicTags;
    /** Tags to be removed from submissions */
    unassignTags?: PublicTags;
}
/**
 * Called to validate submission before creation.
 * @param submission - Submission to validate.
 * @public
 * @documentationMaturity preview
 * @requiredField submission
 * @permissionId forms:v4:submission:validate_form_submission
 * @applicableIdentity APP
 * @fqn wix.forms.v4.FormSubmissionService.ValidateFormSubmission
 */
declare function validateFormSubmission(submission: FormSubmission, options?: ValidateFormSubmissionOptions): Promise<NonNullablePaths<ValidateFormSubmissionResponse, `violationErrors` | `violationErrors.${number}.name` | `violationErrors.${number}.description` | `violationErrors.${number}.ruleName`, 2>>;
interface ValidateFormSubmissionOptions {
    /** Optional metadata passed to SPI implementer, allowing clients to include additional business-specific data. */
    additionalMetadata?: Record<string, any> | null;
    /**
     * List of field targets/keys to validate, in case when only several fields should to be validated.
     * If empty, all form fields be validated
     * @maxSize 200
     * @maxLength 200
     */
    fieldsToValidate?: string[];
}

export { Mode as $, type ValidateFormSubmissionResponse as A, type BulkDeleteSubmissionOptions as B, type CreateSubmissionOptions as C, type DeleteSubmissionOptions as D, type SubmissionCreatedEnvelope as E, type FormSubmission as F, type GetDeletedSubmissionResponse as G, type SubmissionDeletedEnvelope as H, type SubmissionRemovedSubmissionFromTrashEnvelope as I, type SubmissionStatusUpdatedEnvelope as J, type SubmissionContactMappedEnvelope as K, type ListDeletedSubmissionsOptions as L, type SubmissionContactMappingSkippedEnvelope as M, type SubmissionUpdatedEnvelope as N, type FormSubmissionQuery as O, type QuerySubmissionsByNamespaceOptions as P, type QuerySubmissionOptions as Q, type RestoreSubmissionFromTrashBinResponse as R, type SearchSubmissionsByNamespaceResponse as S, typedQuerySubmissionsByNamespace as T, type UpsertContactFromSubmissionOptions as U, type ValidateFormSubmissionOptions as V, type SubmissionsQueryBuilder as W, SubmissionStatus as X, OptInLevel as Y, ContactIdentityType as Z, SortOrder as _, type UpsertContactFromSubmissionResponse as a, ResponsivenessBehaviour as a$, WebhookIdentityType as a0, StringTypeFormatEnumFormat as a1, DayOfWeek as a2, ValidationFormat as a3, ProductType as a4, PriceType as a5, FieldType as a6, FormatEnumFormat as a7, StringComponentType as a8, NodeType as a9, PollLayoutType as aA, PollLayoutDirection as aB, PollDesignBackgroundType as aC, DecorationType as aD, FontType as aE, Variant as aF, Position as aG, AspectRatio as aH, Resizing as aI, Placement as aJ, CardStylesType as aK, CardStylesAlignment as aL, Layout as aM, AppType as aN, InitialExpandedItems as aO, Direction as aP, VerticalAlignment as aQ, NullValue as aR, ImageScalingScaling as aS, LayoutDataImagePosition as aT, LayoutDataBackgroundType as aU, Origin as aV, BannerPosition as aW, LayerEffect as aX, Shape as aY, BackdropType as aZ, VerticalAlignmentAlignment as a_, BackgroundType as aa, GradientType as ab, WidthType as ac, PluginContainerDataAlignment as ad, ButtonDataType as ae, LinkTarget as af, AnimationType as ag, EntranceEffectType as ah, LoopEffectType as ai, PointerEffectType as aj, TextAlignment as ak, LineCap as al, LineStyle as am, Width as an, DividerDataAlignment as ao, ViewMode as ap, LayoutType as aq, Orientation as ar, Crop as as, ThumbnailsAlignment as at, GIFType as au, Source as av, StylesPosition as aw, MapType as ax, ViewRole as ay, VoteRole as az, type CreateSubmissionApplicationErrors as b, type PublicTags as b$, DesignTarget as b0, Scaling as b1, ImagePositionPosition as b2, CardDataBackgroundType as b3, ListStyle as b4, Indentation as b5, SmartBlockDataType as b6, ColumnSize as b7, ImagePosition as b8, Alignment as b9, DisplayFieldType as bA, OverrideEntityType as bB, Kind as bC, FormFieldContactInfoEmailInfoTag as bD, FormFieldContactInfoPhoneInfoTag as bE, AddressInfoTag as bF, SubscriptionInfoOptInLevel as bG, FormFieldContactInfoContactField as bH, SpamFilterProtectionLevel as bI, RequiredIndicator as bJ, RequiredIndicatorPlacement as bK, Target as bL, SubmitSuccessAction as bM, ChangeableProperty as bN, OverrideEntityTypeEnumOverrideEntityType as bO, Operator as bP, ResultsDisplay as bQ, ContactAutofill as bR, SubmissionAccess as bS, IdentityType as bT, ErrorType as bU, Status as bV, SubmissionErrorType as bW, type Submitter as bX, type SubmitterSubmitterOneOf as bY, type ExtendedFields as bZ, type OrderDetails as b_, ImageFit as ba, NumberOfColumns as bb, FirstDayOfWeek as bc, NumberComponentType as bd, BooleanComponentType as be, ItemType as bf, PropertiesTypeEnum as bg, ArrayComponentType as bh, WixFileComponentType as bi, UploadFileFormat as bj, PaymentComponentType as bk, ComponentType as bl, Type as bm, ObjectArrayComponentType as bn, SchedulingComponentType as bo, WaitType as bp, Format as bq, MeetingType as br, StaffStrategySelection as bs, InputType as bt, EmailInfoTag as bu, PhoneInfoTag as bv, Tag as bw, ConfirmationLevel as bx, SubscriptionChannel as by, ContactField as bz, type CreateSubmissionValidationErrors as c, type ProductPriceOptionsOneOf as c$, type TagList as c0, type AppointmentDetails as c1, type FormSubmissionStatusUpdatedEvent as c2, type RemovedSubmissionFromTrash as c3, type SubmissionContactMapped as c4, type MarketingSubscriptionDetails as c5, type SubmissionContactMappingSkipped as c6, type SearchSubmissionsForExtendedExportRequest as c7, type CursorQueryPagingMethodOneOf as c8, type Sorting as c9, type StringErrorMessages as cA, type DateTimeAdvancedConstraints as cB, type Availability as cC, type DateRange as cD, type StringTypeDateTimeConstraints as cE, type StringTypePhoneConstraints as cF, type StringTypeValidationMessages as cG, type FormFieldNumberType as cH, type NumberErrorMessages as cI, type IntegerType as cJ, type FormFieldBooleanType as cK, type BooleanErrorMessages as cL, type FormFieldArrayType as cM, type FormFieldObjectType as cN, type ObjectTypePropertiesType as cO, type ObjectTypePropertiesTypePropertiesTypeOneOf as cP, type ObjectErrorMessages as cQ, type ArrayTypeArrayItems as cR, type ArrayTypeArrayItemsItemsOneOf as cS, type ArrayErrorMessages as cT, type PredefinedValidation as cU, type PredefinedValidationFormatOptionsOneOf as cV, type PaymentType as cW, type QuantityLimit as cX, type FixedPriceOptions as cY, type DynamicPriceOptions as cZ, type Product as c_, type CursorPaging as ca, type CursorSearch as cb, type CursorSearchPagingMethodOneOf as cc, type SearchDetails as cd, type SearchSubmissionsForExtendedExportResponse as ce, type SubmissionExtendedExportRow as cf, type QuizExportResult as cg, type CursorPagingMetadata as ch, type Cursors as ci, type DomainEvent as cj, type DomainEventBodyOneOf as ck, type EntityCreatedEvent as cl, type RestoreInfo as cm, type EntityUpdatedEvent as cn, type EntityDeletedEvent as co, type ActionEvent as cp, type MessageEnvelope as cq, type IdentificationData as cr, type IdentificationDataIdOneOf as cs, type AccountInfo as ct, type CreateCheckoutFromSubmissionRequest as cu, type CreateCheckoutFromSubmissionRequestFormSchemaIdentifierOneOf as cv, type Form as cw, type FormField as cx, type FormFieldStringType as cy, type FormFieldStringTypeFormatOptionsOneOf as cz, type UpdateSubmission as d, type GalleryOptionsLayout as d$, type MultilineAddressValidation as d0, type FieldOverrides as d1, type FieldsOverrides as d2, type ObjectArrayType as d3, type NestedFormFieldOverrides as d4, type Validation as d5, type ValidationValidationOneOf as d6, type DataExtensionsDetails as d7, type NestedFormOverrides as d8, type Field as d9, type Spoiler as dA, type Height as dB, type Styles as dC, type Link as dD, type LinkDataOneOf as dE, type Rel as dF, type Animation as dG, type EntranceAnimation as dH, type EntranceEffect as dI, type LoopAnimation as dJ, type LoopEffect as dK, type PointerEffect as dL, type CodeBlockData as dM, type TextStyle as dN, type DividerData as dO, type DividerDataStyles as dP, type FileData as dQ, type FileSource as dR, type FileSourceDataOneOf as dS, type PDFSettings as dT, type GalleryData as dU, type Media as dV, type Image as dW, type Video as dX, type Item as dY, type ItemDataOneOf as dZ, type GalleryOptions as d_, type FieldFieldTypeOptionsOneOf as da, type InputField as db, type InputFieldInputTypeOptionsOneOf as dc, type StringCorrectAnswersList as dd, type StringType as de, type StringTypeFormatOptionsOneOf as df, type DateTimeConstraints as dg, type PhoneConstraints as dh, type ValidationMessages as di, type StringQuizFieldSettings as dj, type TextInput as dk, type TextInputSemanticOptionsOneOf as dl, type RichContent as dm, type Node as dn, type NodeDataOneOf as dp, type NodeStyle as dq, type ButtonData as dr, type Gradient as ds, type Stop as dt, type Border as du, type Colors as dv, type Background as dw, type PluginContainerData as dx, type PluginContainerDataWidth as dy, type PluginContainerDataWidthDataOneOf as dz, type UpdateSubmissionValidationErrors as e, type OrderedListData as e$, type ItemStyle as e0, type Thumbnails as e1, type GIFData as e2, type GIF as e3, type HeadingData as e4, type HTMLData as e5, type HTMLDataDataOneOf as e6, type ImageData as e7, type StylesBorder as e8, type ImageDataStyles as e9, type MentionData as eA, type FontSizeData as eB, type SpoilerData as eC, type FontFamilyData as eD, type SketchData as eE, type AppEmbedData as eF, type AppEmbedDataAppDataOneOf as eG, type BookingData as eH, type EventData as eI, type ButtonStyles as eJ, type ImageStyles as eK, type RibbonStyles as eL, type CardStyles as eM, type PricingData as eN, type VideoData as eO, type PlaybackOptions as eP, type EmbedData as eQ, type Oembed as eR, type CollapsibleListData as eS, type TableData as eT, type Dimensions as eU, type TableCellData as eV, type CellStyle as eW, type BorderColors as eX, type BorderWidths as eY, type ListValue as eZ, type AudioData as e_, type ImageDataCrop as ea, type LinkPreviewData as eb, type LinkPreviewDataStyles as ec, type MapData as ed, type MapSettings as ee, type ParagraphData as ef, type PollData as eg, type Permissions as eh, type PollOption as ei, type Settings as ej, type PollLayout as ek, type OptionLayout as el, type BackgroundGradient as em, type PollDesignBackground as en, type PollDesignBackgroundBackgroundOneOf as eo, type PollDesign as ep, type OptionDesign as eq, type Poll as er, type PollDataLayout as es, type Design as et, type TextData as eu, type Decoration as ev, type DecorationDataOneOf as ew, type AnchorData as ex, type ColorData as ey, type LinkData as ez, type ConfirmSubmissionResponse as f, type FileUpload as f$, type BulletedListData as f0, type BlockquoteData as f1, type CaptionData as f2, type LayoutData as f3, type LayoutDataBackground as f4, type Layers as f5, type LayoutDataBackgroundImage as f6, type Banner as f7, type Backdrop as f8, type SectionDivider as f9, type TimeInput as fA, type DatePicker as fB, type ServicesDropdown as fC, type ServiceOption as fD, type Password as fE, type NumberCorrectAnswersList as fF, type NumberType as fG, type NumberQuizFieldSettings as fH, type NumberInput as fI, type RatingInput as fJ, type BooleanType as fK, type Checkbox as fL, type CorrectAnswersList as fM, type ArrayType as fN, type ObjectType as fO, type PropertiesType as fP, type PropertiesTypePropertiesTypeOptionsOneOf as fQ, type ArrayItems as fR, type ArrayItemsItemTypeOptionsOneOf as fS, type QuizFieldSettings as fT, type CheckboxGroup as fU, type Option as fV, type OptionsMediaSettings as fW, type ComponentsTags as fX, type TagsOption as fY, type ServicesCheckboxGroup as fZ, type FileType as f_, type LayoutCellData as fa, type ShapeData as fb, type ShapeDataStyles as fc, type CardData as fd, type CardDataBackground as fe, type BackgroundImage as ff, type TocData as fg, type SmartBlockData as fh, type SmartBlockCellData as fi, type CheckboxListData as fj, type ListItemNodeData as fk, type Metadata as fl, type DocumentStyle as fm, type TextNodeStyle as fn, type MediaItem as fo, type MediaItemMediaOneOf as fp, type MediaSettings as fq, type EmailOptions as fr, type RadioGroup as fs, type RadioGroupOption as ft, type CustomOption as fu, type Dropdown as fv, type DropdownOption as fw, type DateTimeInput as fx, type PhoneInput as fy, type DateInput as fz, type BulkDeleteSubmissionResponse as g, type FormProperties as g$, type Signature as g0, type ProductCheckboxGroup as g1, type ProductCheckboxGroupOption as g2, type DonationInput as g3, type DonationInputOption as g4, type PaymentInput as g5, type FixedPayment as g6, type MultilineAddress as g7, type AddressLine2 as g8, type DefaultCountryConfig as g9, type SubscriptionInfo as gA, type _String as gB, type _StringComponentTypeOptionsOneOf as gC, type _Number as gD, type _NumberComponentTypeOptionsOneOf as gE, type _Boolean as gF, type _BooleanComponentTypeOptionsOneOf as gG, type _Array as gH, type _ArrayComponentTypeOptionsOneOf as gI, type _Object as gJ, type WixFile as gK, type WixFileComponentTypeOptionsOneOf as gL, type Payment as gM, type PaymentComponentTypeOptionsOneOf as gN, type Scheduling as gO, type SchedulingComponentTypeOptionsOneOf as gP, type Address as gQ, type AddressComponentTypeOptionsOneOf as gR, type ObjectArray as gS, type ObjectArrayComponentTypeOptionsOneOf as gT, type DisplayField as gU, type DisplayFieldDisplayFieldTypeOptionsOneOf as gV, type RichContentOptions as gW, type PageNavigationOptions as gX, type Step as gY, type FormRule as gZ, type FormOverride as g_, type DefaultCountryConfigOptionsOneOf as ga, type FieldsSettings as gb, type Repeater as gc, type FormLayout as gd, type BreakPoint as ge, type ItemLayout as gf, type ItemLayoutItemOneOf as gg, type Group as gh, type Margin as gi, type Section as gj, type Appointment as gk, type AppointmentFormatInfoOneOf as gl, type WaitOptions as gm, type Location as gn, type LocationLocationInfoOneOf as go, type RebookingPolicy as gp, type RebookingPolicyOptionsOneOf as gq, type InPersonOptions as gr, type VideoConferenceOptions as gs, type PhoneOptions as gt, type FormFieldContactInfo as gu, type FormFieldContactInfoAdditionalInfoOneOf as gv, type EmailInfo as gw, type PhoneInfo as gx, type AddressInfo as gy, type CustomFieldInfo as gz, type BulkRemoveSubmissionFromTrashBinOptions as h, type ConfirmSubmissionRequest as h$, type PostSubmissionTriggers as h0, type UpsertContact as h1, type V4FormFieldContactInfo as h2, type V4FormFieldContactInfoAdditionalInfoOneOf as h3, type FormFieldContactInfoEmailInfo as h4, type FormFieldContactInfoPhoneInfo as h5, type FormFieldContactInfoAddressInfo as h6, type FormFieldContactInfoCustomFieldInfo as h7, type FormFieldContactInfoSubscriptionInfo as h8, type NestedForm as h9, type PassFailMessages as hA, type CreateCheckoutFromSubmissionResponse as hB, type Checkout as hC, type IsFormSubmittableRequest as hD, type IsFormSubmittableResponse as hE, type Empty as hF, type UpsertContactFromSubmissionRequest as hG, type SubmitContactResponse as hH, type CreateSubmissionRequest as hI, type CreateSubmissionResponse as hJ, type SubmissionValidationErrorsDetails as hK, type ValidationError as hL, type CreateSubmissionBySubmitterRequest as hM, type CreateSubmissionBySubmitterResponse as hN, type BulkCreateSubmissionBySubmitterRequest as hO, type BulkCreateSubmissionBySubmitterData as hP, type BulkCreateSubmissionBySubmitterResponse as hQ, type BulkSubmissionResult as hR, type ItemMetadata as hS, type ApplicationError as hT, type BulkActionMetadata as hU, type GetSubmissionRequest as hV, type GetSubmissionResponse as hW, type GetSubmissionByCheckoutIdRequest as hX, type GetSubmissionByCheckoutIdResponse as hY, type UpdateSubmissionRequest as hZ, type UpdateSubmissionResponse as h_, type LimitationRule as ha, type RequiredIndicatorProperties as hb, type SubmitSettings as hc, type SubmitSettingsSubmitSuccessActionOptionsOneOf as hd, type ThankYouMessageOptions as he, type RedirectOptions as hf, type PopupOptions as hg, type FieldGroup as hh, type Rule as hi, type RequiredOptions as hj, type HiddenOptions as hk, type AllowedValuesOptions as hl, type FieldOverride as hm, type FieldOverridePropertyTypeOptionsOneOf as hn, type ConditionNode as ho, type ConditionNodeNodeOneOf as hp, type AndCondition as hq, type OrCondition as hr, type Condition as hs, type RuleFormOverride as ht, type RuleFormOverrideEntityTypeOptionsOneOf as hu, type Tags as hv, type TagsTagList as hw, type QuizSettings as hx, type QuizSettingsPassingCriteriaOneOf as hy, type QuizSettingsResultsDisplayOptionsOneOf as hz, type BulkRemoveSubmissionFromTrashBinResponse as i, type SortOrderWithLiterals as i$, type DeleteSubmissionRequest as i0, type DeleteSubmissionResponse as i1, type BulkDeleteSubmissionRequest as i2, type BulkDeleteSubmissionResult as i3, type RestoreSubmissionFromTrashBinRequest as i4, type RemoveSubmissionFromTrashBinRequest as i5, type RemoveSubmissionFromTrashBinResponse as i6, type BulkRemoveSubmissionFromTrashBinRequest as i7, type BulkRemoveSubmissionFromTrashBinResult as i8, type ListDeletedSubmissionsRequest as i9, type ListFormattedSubmissionsRequest as iA, type ListFormattedSubmissionsResponse as iB, type FormattedFormSubmission as iC, type UpdateExtendedFieldsRequest as iD, type BulkUpdateFormSubmissionTagsRequest as iE, type BulkUpdateFormSubmissionTagsResponse as iF, type BulkUpdateFormSubmissionTagsResult as iG, type BulkUpdateFormSubmissionTagsByFilterRequest as iH, type BulkUpdateFormSubmissionTagsByFilterResponse as iI, type ValidateFormSubmissionRequest as iJ, type FieldViolation as iK, type FieldViolationErrorDataOneOf as iL, type SubmissionValidationErrors as iM, type SubmissionValidationError as iN, type SubmissionValidationErrorErrorMessageOneOf as iO, type BaseEventMetadata as iP, type EventMetadata as iQ, type AccountInfoMetadata as iR, type FormSubmissionSearchSpec as iS, type SubmissionsQueryResult as iT, type FormSubmissionQuerySpec as iU, type BulkUpdateFormSubmissionTagsOptions as iV, type BulkUpdateFormSubmissionTagsByFilterOptions as iW, utils as iX, type SubmissionStatusWithLiterals as iY, type OptInLevelWithLiterals as iZ, type ContactIdentityTypeWithLiterals as i_, type GetDeletedSubmissionRequest as ia, type QuerySubmissionRequest as ib, type SearchSubmissionsByNamespaceRequest as ic, type SearchSubmissionsByNamespaceForExportRequest as id, type SearchSubmissionsByNamespaceForExportResponse as ie, type QuerySubmissionsByNamespaceRequest as ig, type QuerySubmissionsByNamespaceResponse as ih, type QuerySubmissionsByNamespaceForExportRequest as ii, type QuerySubmissionsByNamespaceForExportResponse as ij, type CountSubmissionsByFilterRequest as ik, type FormSubmissionsCount as il, type CountSubmissionsRequest as im, type CountDeletedSubmissionsRequest as io, type FormDeletedSubmissionsCount as ip, type GetMediaUploadURLRequest as iq, type BulkMarkSubmissionsAsSeenRequest as ir, type GetSubmissionDownloadUrlRequest as is, type SubmissionDocument as it, type SubmissionDocumentDocumentOneOf as iu, type DocumentReady as iv, type DownloadSubmissionRequest as iw, type HeadersEntry as ix, type GetFormattedSubmissionRequest as iy, type FormattedSubmission as iz, type ListDeletedSubmissionsResponse as j, type VerticalAlignmentAlignmentWithLiterals as j$, type ModeWithLiterals as j0, type WebhookIdentityTypeWithLiterals as j1, type StringTypeFormatEnumFormatWithLiterals as j2, type DayOfWeekWithLiterals as j3, type ValidationFormatWithLiterals as j4, type ProductTypeWithLiterals as j5, type PriceTypeWithLiterals as j6, type FieldTypeWithLiterals as j7, type FormatEnumFormatWithLiterals as j8, type StringComponentTypeWithLiterals as j9, type VoteRoleWithLiterals as jA, type PollLayoutTypeWithLiterals as jB, type PollLayoutDirectionWithLiterals as jC, type PollDesignBackgroundTypeWithLiterals as jD, type DecorationTypeWithLiterals as jE, type FontTypeWithLiterals as jF, type VariantWithLiterals as jG, type PositionWithLiterals as jH, type AspectRatioWithLiterals as jI, type ResizingWithLiterals as jJ, type PlacementWithLiterals as jK, type CardStylesTypeWithLiterals as jL, type CardStylesAlignmentWithLiterals as jM, type LayoutWithLiterals as jN, type AppTypeWithLiterals as jO, type InitialExpandedItemsWithLiterals as jP, type DirectionWithLiterals as jQ, type VerticalAlignmentWithLiterals as jR, type NullValueWithLiterals as jS, type ImageScalingScalingWithLiterals as jT, type LayoutDataImagePositionWithLiterals as jU, type LayoutDataBackgroundTypeWithLiterals as jV, type OriginWithLiterals as jW, type BannerPositionWithLiterals as jX, type LayerEffectWithLiterals as jY, type ShapeWithLiterals as jZ, type BackdropTypeWithLiterals as j_, type NodeTypeWithLiterals as ja, type BackgroundTypeWithLiterals as jb, type GradientTypeWithLiterals as jc, type WidthTypeWithLiterals as jd, type PluginContainerDataAlignmentWithLiterals as je, type ButtonDataTypeWithLiterals as jf, type LinkTargetWithLiterals as jg, type AnimationTypeWithLiterals as jh, type EntranceEffectTypeWithLiterals as ji, type LoopEffectTypeWithLiterals as jj, type PointerEffectTypeWithLiterals as jk, type TextAlignmentWithLiterals as jl, type LineCapWithLiterals as jm, type LineStyleWithLiterals as jn, type WidthWithLiterals as jo, type DividerDataAlignmentWithLiterals as jp, type ViewModeWithLiterals as jq, type LayoutTypeWithLiterals as jr, type OrientationWithLiterals as js, type CropWithLiterals as jt, type ThumbnailsAlignmentWithLiterals as ju, type GIFTypeWithLiterals as jv, type SourceWithLiterals as jw, type StylesPositionWithLiterals as jx, type MapTypeWithLiterals as jy, type ViewRoleWithLiterals as jz, type CursorQuery as k, onSubmissionDeleted as k$, type ResponsivenessBehaviourWithLiterals as k0, type DesignTargetWithLiterals as k1, type ScalingWithLiterals as k2, type ImagePositionPositionWithLiterals as k3, type CardDataBackgroundTypeWithLiterals as k4, type ListStyleWithLiterals as k5, type IndentationWithLiterals as k6, type SmartBlockDataTypeWithLiterals as k7, type ColumnSizeWithLiterals as k8, type ImagePositionWithLiterals as k9, type ContactFieldWithLiterals as kA, type DisplayFieldTypeWithLiterals as kB, type OverrideEntityTypeWithLiterals as kC, type KindWithLiterals as kD, type FormFieldContactInfoEmailInfoTagWithLiterals as kE, type FormFieldContactInfoPhoneInfoTagWithLiterals as kF, type AddressInfoTagWithLiterals as kG, type SubscriptionInfoOptInLevelWithLiterals as kH, type FormFieldContactInfoContactFieldWithLiterals as kI, type SpamFilterProtectionLevelWithLiterals as kJ, type RequiredIndicatorWithLiterals as kK, type RequiredIndicatorPlacementWithLiterals as kL, type TargetWithLiterals as kM, type SubmitSuccessActionWithLiterals as kN, type ChangeablePropertyWithLiterals as kO, type OverrideEntityTypeEnumOverrideEntityTypeWithLiterals as kP, type OperatorWithLiterals as kQ, type ResultsDisplayWithLiterals as kR, type ContactAutofillWithLiterals as kS, type SubmissionAccessWithLiterals as kT, type IdentityTypeWithLiterals as kU, type ErrorTypeWithLiterals as kV, type StatusWithLiterals as kW, type SubmissionErrorTypeWithLiterals as kX, type CommonSearchWithEntityContext as kY, type CommonQueryWithEntityContext as kZ, onSubmissionCreated as k_, type AlignmentWithLiterals as ka, type ImageFitWithLiterals as kb, type NumberOfColumnsWithLiterals as kc, type FirstDayOfWeekWithLiterals as kd, type NumberComponentTypeWithLiterals as ke, type BooleanComponentTypeWithLiterals as kf, type ItemTypeWithLiterals as kg, type PropertiesTypeEnumWithLiterals as kh, type ArrayComponentTypeWithLiterals as ki, type WixFileComponentTypeWithLiterals as kj, type UploadFileFormatWithLiterals as kk, type PaymentComponentTypeWithLiterals as kl, type ComponentTypeWithLiterals as km, type TypeWithLiterals as kn, type ObjectArrayComponentTypeWithLiterals as ko, type SchedulingComponentTypeWithLiterals as kp, type WaitTypeWithLiterals as kq, type FormatWithLiterals as kr, type MeetingTypeWithLiterals as ks, type StaffStrategySelectionWithLiterals as kt, type InputTypeWithLiterals as ku, type EmailInfoTagWithLiterals as kv, type PhoneInfoTagWithLiterals as kw, type TagWithLiterals as kx, type ConfirmationLevelWithLiterals as ky, type SubscriptionChannelWithLiterals as kz, type QuerySubmissionResponse as l, onSubmissionRemovedSubmissionFromTrash as l0, onSubmissionStatusUpdated as l1, onSubmissionContactMapped as l2, onSubmissionContactMappingSkipped as l3, onSubmissionUpdated as l4, upsertContactFromSubmission as l5, createSubmission as l6, getSubmission as l7, updateSubmission as l8, confirmSubmission as l9, deleteSubmission as la, bulkDeleteSubmission as lb, restoreSubmissionFromTrashBin as lc, removeSubmissionFromTrashBin as ld, bulkRemoveSubmissionFromTrashBin as le, listDeletedSubmissions as lf, getDeletedSubmission as lg, querySubmission as lh, querySubmissionsByNamespace as li, countSubmissionsByFilter as lj, countSubmissions as lk, countDeletedSubmissions as ll, getMediaUploadUrl as lm, bulkMarkSubmissionsAsSeen as ln, getSubmissionDownloadUrl as lo, downloadSubmission as lp, getFormattedSubmission as lq, updateExtendedFields as lr, validateFormSubmission as ls, type FormSubmissionSearch as m, type CountSubmissionsByFilterOptions as n, type CountSubmissionsByFilterResponse as o, type CountSubmissionsOptions as p, type CountSubmissionsResponse as q, type CountDeletedSubmissionsOptions as r, type CountDeletedSubmissionsResponse as s, type GetMediaUploadURLResponse as t, type BulkMarkSubmissionsAsSeenResponse as u, type GetSubmissionDownloadUrlResponse as v, type RawHttpResponse as w, type GetFormattedSubmissionResponse as x, type UpdateExtendedFieldsOptions as y, type UpdateExtendedFieldsResponse as z };
