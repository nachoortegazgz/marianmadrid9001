import { BlockMemberRequest as BlockMemberRequest$1, BlockMemberResponse as BlockMemberResponse$1, UnblockMemberRequest as UnblockMemberRequest$1, UnblockMemberResponse as UnblockMemberResponse$1, ListCurrentMemberBlockingRequest as ListCurrentMemberBlockingRequest$1, ListCurrentMemberBlockingResponse as ListCurrentMemberBlockingResponse$1, ListMemberBlocksRequest as ListMemberBlocksRequest$1, ListMemberBlocksResponse as ListMemberBlocksResponse$1 } from './index.typings.mjs';
import '@wix/sdk-types';

/** Member-to-member block. */
interface MemberToMemberBlock {
    /**
     * ID of the member that is the creator of this block.
     * @format GUID
     */
    blockingMemberId?: string;
    /**
     * ID of the member that is blocked by this block.
     * @format GUID
     */
    blockedMemberId?: string;
}
interface BlockMemberRequest {
    /**
     * ID of a member to block.
     * @format GUID
     */
    memberId: string;
}
interface BlockMemberResponse {
}
interface MemberBlockedByMember {
    /** The block that has been created. */
    block?: MemberToMemberBlock;
}
interface MemberAlreadyBlockedError {
    /** @format GUID */
    initiatorMemberId?: string;
    /** @format GUID */
    targetMemberId?: string;
}
interface SelfBlockingForbiddenError {
    /** @format GUID */
    initiatorMemberId?: string;
}
interface AdminBlockingForbiddenError {
    /** @format GUID */
    initiatorMemberId?: string;
    /** @format GUID */
    targetMemberId?: string;
    /** @format GUID */
    adminMemberId?: string;
}
interface UnblockMemberRequest {
    /**
     * ID of a member to unblock.
     * @format GUID
     */
    memberId: string;
}
interface UnblockMemberResponse {
}
interface MemberUnblockedByMember {
    /** The block that has been removed. */
    block?: MemberToMemberBlock;
}
interface SelfUnblockingForbiddenError {
    /** @format GUID */
    initiatorMemberId?: string;
}
interface BlockDoesNotExistError {
    /** @format GUID */
    blockingMemberId?: string;
    /** @format GUID */
    blockedMemberId?: string;
}
interface ListCurrentMemberBlockingRequest {
    /** Pagination parameters. */
    cursorPaging?: CursorPaging;
}
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface ListCurrentMemberBlockingResponse {
    /**
     * IDs of members blocked by the current member.
     * @format GUID
     */
    blockedMemberIds?: string[];
    /** Metadata for the paginated results. */
    pagingMetadata?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Flag that indicates the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     * @internal
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface ListMemberBlocksRequest {
    /**
     * ID of a member whose blocks are requested.
     * @format GUID
     */
    memberId: string;
    /** Pagination parameters. */
    cursorPaging?: CursorPaging;
}
interface ListMemberBlocksResponse {
    /** List of members who are either blocking or blocked by the specified member. */
    memberBlocks?: MemberBlock[];
    /** Metadata for the paginated results. */
    pagingMetadata?: PagingMetadataV2;
}
interface MemberBlock {
    /**
     * Member ID.
     * @format GUID
     */
    memberId?: string;
    /**
     * Indicates whether the member with `memberId` is blocking or blocked by the specified member.
     *
     * - `BLOCKING`: The member with `memberId` is blocking the requested member.
     * - `BLOCKED`: The member with `memberId` is blocked by the requested member.
     */
    blockDirection?: BlockDirectionWithLiterals;
}
declare enum BlockDirection {
    UNKNOWN = "UNKNOWN",
    BLOCKING = "BLOCKING",
    BLOCKED = "BLOCKED"
}
/** @enumType */
type BlockDirectionWithLiterals = BlockDirection | 'UNKNOWN' | 'BLOCKING' | 'BLOCKED';
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entityAsJson?: string;
    /**
     * Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity
     * @internal
     */
    triggeredByUndelete?: boolean | null;
    /** Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity */
    restoreInfo?: RestoreInfo;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntityAsJson?: string;
    /**
     * This field is currently part of the of the EntityUpdatedEvent msg, but scala/node libraries which implements the domain events standard
     * wont populate it / have any reference to it in the API.
     * The main reason for it is that fetching the old entity from the DB will have a performance hit on an update operation so unless truly needed,
     * the developer should send only the new (current) entity.
     * An additional reason is not wanting to send this additional entity over the wire (kafka) since in some cases it can be really big
     * Developers that must reflect the old entity will have to implement their own domain event sender mechanism which will follow the DomainEvent proto message.
     * @internal
     * @deprecated
     */
    previousEntityAsJson?: string | null;
    /**
     * Map of fully qualified field paths to their previous values for fields that changed.
     * For more details please see [AIP](https://dev.wix.com/docs/rnd-general/articles/p13n-guidelines-aips/guidance-aips/design-patterns/7016-events#modified-fields-format)
     * @internal
     */
    modifiedFields?: Record<string, any>;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface EntityDeletedEvent {
    /**
     * Indicates if the entity is sent to trash-bin. only available when trash-bin is enabled
     * @internal
     */
    movedToTrash?: boolean | null;
    /** Entity that was deleted. */
    deletedEntityAsJson?: string | null;
    /**
     * Optional domain-specific metadata defined by the API owner, encoded as JSON.
     * @internal
     */
    additionalMetadataAsJson?: string | null;
}
interface ActionEvent {
    bodyAsJson?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
/** @docsIgnore */
type BlockMemberApplicationErrors = {
    code?: 'MEMBER_ALREADY_BLOCKED';
    description?: string;
    data?: MemberAlreadyBlockedError;
} | {
    code?: 'SELF_BLOCKING_FORBIDDEN';
    description?: string;
    data?: SelfBlockingForbiddenError;
} | {
    code?: 'ADMIN_BLOCKING_FORBIDDEN';
    description?: string;
    data?: AdminBlockingForbiddenError;
};
/** @docsIgnore */
type UnblockMemberApplicationErrors = {
    code?: 'SELF_UNBLOCKING_FORBIDDEN';
    description?: string;
    data?: SelfUnblockingForbiddenError;
} | {
    code?: 'BLOCK_DOES_NOT_EXIST';
    description?: string;
    data?: BlockDoesNotExistError;
};

type __PublicMethodMetaInfo<K = string, M = unknown, T = unknown, S = unknown, Q = unknown, R = unknown> = {
    getUrl: (context: any) => string;
    httpMethod: K;
    path: string;
    pathParams: M;
    __requestType: T;
    __originalRequestType: S;
    __responseType: Q;
    __originalResponseType: R;
};
declare function blockMember(): __PublicMethodMetaInfo<'POST', {}, BlockMemberRequest$1, BlockMemberRequest, BlockMemberResponse$1, BlockMemberResponse>;
declare function unblockMember(): __PublicMethodMetaInfo<'DELETE', {
    memberId: string;
}, UnblockMemberRequest$1, UnblockMemberRequest, UnblockMemberResponse$1, UnblockMemberResponse>;
declare function listCurrentMemberBlocking(): __PublicMethodMetaInfo<'GET', {}, ListCurrentMemberBlockingRequest$1, ListCurrentMemberBlockingRequest, ListCurrentMemberBlockingResponse$1, ListCurrentMemberBlockingResponse>;
declare function listMemberBlocks(): __PublicMethodMetaInfo<'GET', {
    memberId: string;
}, ListMemberBlocksRequest$1, ListMemberBlocksRequest, ListMemberBlocksResponse$1, ListMemberBlocksResponse>;

export { type AccountInfo as AccountInfoOriginal, type ActionEvent as ActionEventOriginal, type AdminBlockingForbiddenError as AdminBlockingForbiddenErrorOriginal, BlockDirection as BlockDirectionOriginal, type BlockDirectionWithLiterals as BlockDirectionWithLiteralsOriginal, type BlockDoesNotExistError as BlockDoesNotExistErrorOriginal, type BlockMemberApplicationErrors as BlockMemberApplicationErrorsOriginal, type BlockMemberRequest as BlockMemberRequestOriginal, type BlockMemberResponse as BlockMemberResponseOriginal, type CursorPaging as CursorPagingOriginal, type Cursors as CursorsOriginal, type DomainEventBodyOneOf as DomainEventBodyOneOfOriginal, type DomainEvent as DomainEventOriginal, type Empty as EmptyOriginal, type EntityCreatedEvent as EntityCreatedEventOriginal, type EntityDeletedEvent as EntityDeletedEventOriginal, type EntityUpdatedEvent as EntityUpdatedEventOriginal, type IdentificationDataIdOneOf as IdentificationDataIdOneOfOriginal, type IdentificationData as IdentificationDataOriginal, type ListCurrentMemberBlockingRequest as ListCurrentMemberBlockingRequestOriginal, type ListCurrentMemberBlockingResponse as ListCurrentMemberBlockingResponseOriginal, type ListMemberBlocksRequest as ListMemberBlocksRequestOriginal, type ListMemberBlocksResponse as ListMemberBlocksResponseOriginal, type MemberAlreadyBlockedError as MemberAlreadyBlockedErrorOriginal, type MemberBlock as MemberBlockOriginal, type MemberBlockedByMember as MemberBlockedByMemberOriginal, type MemberToMemberBlock as MemberToMemberBlockOriginal, type MemberUnblockedByMember as MemberUnblockedByMemberOriginal, type MessageEnvelope as MessageEnvelopeOriginal, type PagingMetadataV2 as PagingMetadataV2Original, type RestoreInfo as RestoreInfoOriginal, type SelfBlockingForbiddenError as SelfBlockingForbiddenErrorOriginal, type SelfUnblockingForbiddenError as SelfUnblockingForbiddenErrorOriginal, type UnblockMemberApplicationErrors as UnblockMemberApplicationErrorsOriginal, type UnblockMemberRequest as UnblockMemberRequestOriginal, type UnblockMemberResponse as UnblockMemberResponseOriginal, WebhookIdentityType as WebhookIdentityTypeOriginal, type WebhookIdentityTypeWithLiterals as WebhookIdentityTypeWithLiteralsOriginal, type __PublicMethodMetaInfo, blockMember, listCurrentMemberBlocking, listMemberBlocks, unblockMember };
