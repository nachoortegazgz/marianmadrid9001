/**
 * Maps legacy service definition IDs to their canonical replacements.
 * Used by the services manager to resolve both old and new IDs to the same
 * service instance during the migration period. Once all callers have been
 * updated to use the new IDs, entries can be removed from this map.
 */
export declare const serviceIdMigrationMap: Record<string, string>;
