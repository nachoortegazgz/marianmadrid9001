import * as _wix_sdk_types from '@wix/sdk-types';
import { QuerySpec, Query as Query$1, NonNullablePaths } from '@wix/sdk-types';

/** Label that was found or created. */
interface ContactLabel {
    /**
     * Label namespace.
     *
     * Labels created by calling the Find Or Create Label method
     * are automatically assigned to the `custom` namespace.
     * @readonly
     * @minLength 1
     * @maxLength 25
     */
    namespace?: string | null;
    /**
     * Display name for the namespace,
     * used to organize the list of labels in the site dashboard.
     * @readonly
     * @minLength 1
     * @maxLength 180
     */
    namespaceDisplayName?: string | null;
    /**
     * Label key.
     *
     * `key` is generated when the label is created.
     * It can't be modified, even if `displayName` is updated.
     * @readonly
     * @minLength 1
     * @maxLength 80
     */
    key?: string;
    /**
     * Label display name shown in the dashboard.
     * @minLength 1
     * @maxLength 180
     */
    displayName?: string;
    /**
     * Label type indicating how the label was created.
     * @readonly
     */
    labelType?: LabelTypeWithLiterals;
    /**
     * Date and time the label was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the label was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
}
declare enum LabelType {
    /** Default system label for the contact list. */
    SYSTEM = "SYSTEM",
    /** Label created by calling the Find Or Create Label method. */
    USER_DEFINED = "USER_DEFINED",
    /** Label created by an app built by Wix. */
    WIX_APP_DEFINED = "WIX_APP_DEFINED"
}
/** @enumType */
type LabelTypeWithLiterals = LabelType | 'SYSTEM' | 'USER_DEFINED' | 'WIX_APP_DEFINED';
/** Label filter options. */
interface ListLabelsRequest {
    /** Filter for labels of the specified type. */
    labelType?: LabelTypeWithLiterals;
    /**
     * Filter for labels in the specified namespace.
     * @minLength 3
     * @maxLength 25
     */
    namespace?: string | null;
    /**
     * Filter for labels where `displayName` starts with the specified case-sensitive string.
     * @minLength 1
     * @maxLength 180
     */
    startsWith?: string | null;
    /** Sorting options. */
    sort?: Sorting;
    /** Paging options. */
    paging?: Paging;
    /** @format LANGUAGE */
    language?: string | null;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 3000
     */
    fieldName?: string;
    /**
     * Sort order.
     *
     * Default: `ASC`.
     */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    /** Sort by ascending order. */
    ASC = "ASC",
    /** Sort by descending order. */
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface Paging {
    /**
     * Number of items to return.
     *
     * Defaults to `1000`. <br>
     * Maximum: `2000`.
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
/** List of labels. */
interface ListLabelsResponse {
    /** List of labels. */
    labels?: ContactLabel[];
    /** Metadata for the page of results. */
    metadata?: PagingMetadata;
}
interface PagingMetadata {
    /** Number of items returned. */
    count?: number | null;
    /** Requested offset. */
    offset?: number | null;
    /** Number of items that matched the query. */
    total?: number | null;
    /**
     * Indicates if `total` calculation timed out before the response was sent.
     * Typically this happens if there is a large set of results.
     */
    tooManyToCount?: boolean | null;
}
/** Label to find or create. */
interface FindOrCreateLabelRequest {
    /**
     * Display name to retrieve or create.
     *
     * If an existing label is an exact match
     * for the specified display name,
     * the existing label is returned.
     * If not, a new label is created and returned.
     * @minLength 1
     * @maxLength 180
     */
    displayName: string;
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
/** Label that was found or created. */
interface FindOrCreateLabelResponse {
    /** Label that was found or created. */
    label?: ContactLabel;
    /**
     * Indicates whether the label was just created or already existed.
     *
     * Returns `true` if the label was just created.
     */
    newLabel?: boolean;
}
interface LabelsQuotaReached {
    /** The maximum number of labels allowed for the site. */
    labelsQuota?: string | null;
    /** The current total number of labels on the site. */
    labelsCurrentTotal?: string | null;
}
interface ListLabelNamespacesRequest {
    /**
     * Language for localization
     * @format LANGUAGE
     */
    language?: string | null;
}
interface ListLabelNamespacesResponse {
    /** List of namespaces */
    namespaces?: ContactLabelNamespace[];
}
interface ContactLabelNamespace {
    /**
     * Namespace key
     * @readonly
     * @minLength 1
     * @maxLength 25
     */
    key?: string;
    /**
     * Namespace display name
     * @readonly
     * @minLength 1
     * @maxLength 25
     */
    displayName?: string | null;
}
interface GetLabelRequest {
    /**
     * Label key.
     *
     * `key` is generated when the label is created.
     * It can't be modified, even if `displayName` is updated.
     * @minLength 1
     * @maxLength 80
     */
    key: string;
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
/** The specified label. */
interface GetLabelResponse {
    /** The specified label. */
    label?: ContactLabel;
}
interface GetLabelByLegacyIdRequest {
    /**
     * Legacy id of the label
     * @format GUID
     */
    legacyId?: string;
    /**
     * Language for localization
     * @format LANGUAGE
     */
    language?: string | null;
}
interface GetLabelByLegacyIdResponse {
    /** Requested label */
    label?: ContactLabel;
}
interface UpdateLabelRequest {
    /** Label to rename. */
    label?: ContactLabel;
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
/** Updated label. */
interface UpdateLabelResponse {
    /** Renamed label. */
    label?: ContactLabel;
}
interface DeleteLabelRequest {
    /**
     * Label key to delete.
     * @minLength 1
     * @maxLength 80
     */
    key: string;
}
interface DeleteLabelResponse {
}
interface PurgeRequest {
    /**
     * Contacts Instance ID of the site.
     * @format GUID
     */
    instanceId?: string;
    /**
     * Labels to exclude from the purge.
     * @maxLength 80
     * @maxSize 80
     */
    exludingLabelKeys?: string[];
}
interface PurgeResponse {
    /** Number of deleted items */
    deletedItems?: number;
}
interface GdprListRequest {
    /**
     * Contacts Instance ID of the site.
     * @format GUID
     */
    instanceId?: string;
}
interface GdprListResponse {
    /** List of labels */
    labels?: ContactLabel[];
}
interface QueryLabelsRequest {
    /** Query options. */
    query?: Query;
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
interface Query {
    /**
     * ilter object.
     *
     * Possible filters: `$eq`, `$gt`, `$gte`, `$in`, `$lt`, `$lte`, `$ne`, `$startsWith`.
     *
     * For a detailed list of supported filters, see [sorting and filtering for labels](https://dev.wix.com/api/rest/contacts/labels/sort-and-filter).
     *
     * Example: `{ "filter": {
     * "displayName": {
     * "$startsWith": "Referral"
     * }
     * }
     * }`
     */
    filter?: Record<string, any> | null;
    /**
     * Sorting options. For a list of fields that can be sorted, see [sorting and filtering for labels](https://dev.wix.com/api/rest/contacts/labels/sort-and-filter).
     *
     * Example: `{ "sort": [{"fieldName": "displayName", "order": "DESC"}] }`
     * @maxSize 100
     */
    sort?: Sorting[];
    /** Pagination options. */
    paging?: Paging;
}
interface QueryLabelsResponse {
    /** List of labels */
    labels?: ContactLabel[];
    /** Details on the paged set of results returned. */
    pagingMetadata?: PagingMetadata;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
/** @docsIgnore */
type FindOrCreateLabelApplicationErrors = {
    code?: 'LABELS_QUOTA_REACHED';
    description?: string;
    data?: LabelsQuotaReached;
};
/** @docsIgnore */
type QueryLabelsApplicationErrors = {
    code?: 'INVALID_SORT';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MALFORMED_FILTER';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNSUPPORTED_FILTER';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNSUPPORTED_FILTER_FIELD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNSUPPORTED_FILTER_OPERATOR';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNSUPPORTED_FILTER_VALUE';
    description?: string;
    data?: Record<string, any>;
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
    accountInfo?: AccountInfoMetadata;
}
interface AccountInfoMetadata {
    /** ID of the Wix account associated with the event */
    accountId: string;
    /** ID of the Wix site associated with the event. Only included when the event is tied to a specific site. */
    siteId?: string;
    /** ID of the parent Wix account. Only included when 'accountId' belongs to a child account. */
    parentAccountId?: string;
}
interface LabelCreatedEnvelope {
    entity: ContactLabel;
    metadata: EventMetadata;
}
/**
 * Triggered when a label is created.
 * @permissionScope Manage_Wix_Rentals_App_Forms_submissions
 * @permissionScopeId SCOPE.RENTALS.MANAGE
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Bookings Services and Settings
 * @permissionScopeId SCOPE.BOOKINGS.CONFIGURATION
 * @permissionScope View settlement reports and payouts
 * @permissionScopeId SCOPE.PAYMENTS.PAYOUTS-VIEW-FOR-SITE
 * @permissionScope Manage Contact Labels
 * @permissionScopeId SCOPE.DC-CONTACTS.MANAGE-LABELS
 * @permissionScope Manage Events
 * @permissionScopeId SCOPE.EVENTS.MANAGE-EVENTS
 * @permissionScope Manage Online Programs
 * @permissionScopeId SCOPE.CHALLENGES.MANAGE
 * @permissionScope Manage Portfolio
 * @permissionScopeId SCOPE.PORTFOLIO.MANAGE-PORTFOLIO
 * @permissionScope View SEO Settings
 * @permissionScopeId SCOPE.PROMOTE.VIEW-SEO
 * @permissionScope Manage Social Posts
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS
 * @permissionScope Manage SEO Settings
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-SEO
 * @permissionScope Access Verticals by Automations
 * @permissionScopeId SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope View Site Analytics
 * @permissionScopeId SCOPE.MEDIA.VIEW-SITE-ANALYTICS
 * @permissionScope Set Up Automations
 * @permissionScopeId SCOPE.CRM.SETUP-AUTOMATIONS
 * @permissionScope Manage google ads campaigns
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-GOOGLE-ADS
 * @permissionScope Manage Members and Contacts - all permissions
 * @permissionScopeId SCOPE.DC-CONTACTS-MEGA.MANAGE-MEMBERS-CONTACTS
 * @permissionScope View google ads campaigns
 * @permissionScopeId SCOPE.PROMOTE.VIEW-GOOGLE-ADS
 * @permissionId CONTACTS_LABELS.VIEW
 * @webhook
 * @eventType wix.contacts.v4.label_created
 * @serviceIdentifier com.wixpress.contacts.labels.v4.ContactLabelsServiceV4
 * @slug created
 */
declare function onLabelCreated(handler: (event: LabelCreatedEnvelope) => void | Promise<void>): void;
interface LabelDeletedEnvelope {
    metadata: EventMetadata;
}
/**
 * Triggered when a label is deleted.
 * @permissionScope Manage_Wix_Rentals_App_Forms_submissions
 * @permissionScopeId SCOPE.RENTALS.MANAGE
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Bookings Services and Settings
 * @permissionScopeId SCOPE.BOOKINGS.CONFIGURATION
 * @permissionScope View settlement reports and payouts
 * @permissionScopeId SCOPE.PAYMENTS.PAYOUTS-VIEW-FOR-SITE
 * @permissionScope Manage Contact Labels
 * @permissionScopeId SCOPE.DC-CONTACTS.MANAGE-LABELS
 * @permissionScope Manage Events
 * @permissionScopeId SCOPE.EVENTS.MANAGE-EVENTS
 * @permissionScope Manage Online Programs
 * @permissionScopeId SCOPE.CHALLENGES.MANAGE
 * @permissionScope Manage Portfolio
 * @permissionScopeId SCOPE.PORTFOLIO.MANAGE-PORTFOLIO
 * @permissionScope View SEO Settings
 * @permissionScopeId SCOPE.PROMOTE.VIEW-SEO
 * @permissionScope Manage Social Posts
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS
 * @permissionScope Manage SEO Settings
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-SEO
 * @permissionScope Access Verticals by Automations
 * @permissionScopeId SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope View Site Analytics
 * @permissionScopeId SCOPE.MEDIA.VIEW-SITE-ANALYTICS
 * @permissionScope Set Up Automations
 * @permissionScopeId SCOPE.CRM.SETUP-AUTOMATIONS
 * @permissionScope Manage google ads campaigns
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-GOOGLE-ADS
 * @permissionScope Manage Members and Contacts - all permissions
 * @permissionScopeId SCOPE.DC-CONTACTS-MEGA.MANAGE-MEMBERS-CONTACTS
 * @permissionScope View google ads campaigns
 * @permissionScopeId SCOPE.PROMOTE.VIEW-GOOGLE-ADS
 * @permissionId CONTACTS_LABELS.VIEW
 * @webhook
 * @eventType wix.contacts.v4.label_deleted
 * @serviceIdentifier com.wixpress.contacts.labels.v4.ContactLabelsServiceV4
 * @slug deleted
 */
declare function onLabelDeleted(handler: (event: LabelDeletedEnvelope) => void | Promise<void>): void;
interface LabelUpdatedEnvelope {
    entity: ContactLabel;
    metadata: EventMetadata;
}
/**
 * Triggered when a label is updated.
 * @permissionScope Manage_Wix_Rentals_App_Forms_submissions
 * @permissionScopeId SCOPE.RENTALS.MANAGE
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Bookings Services and Settings
 * @permissionScopeId SCOPE.BOOKINGS.CONFIGURATION
 * @permissionScope View settlement reports and payouts
 * @permissionScopeId SCOPE.PAYMENTS.PAYOUTS-VIEW-FOR-SITE
 * @permissionScope Manage Contact Labels
 * @permissionScopeId SCOPE.DC-CONTACTS.MANAGE-LABELS
 * @permissionScope Manage Events
 * @permissionScopeId SCOPE.EVENTS.MANAGE-EVENTS
 * @permissionScope Manage Online Programs
 * @permissionScopeId SCOPE.CHALLENGES.MANAGE
 * @permissionScope Manage Portfolio
 * @permissionScopeId SCOPE.PORTFOLIO.MANAGE-PORTFOLIO
 * @permissionScope View SEO Settings
 * @permissionScopeId SCOPE.PROMOTE.VIEW-SEO
 * @permissionScope Manage Social Posts
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-SOCIAL-POSTS
 * @permissionScope Manage SEO Settings
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-SEO
 * @permissionScope Access Verticals by Automations
 * @permissionScopeId SCOPE.CRM.ACCESS-VERTICALS-BY-AUTOMATIONS
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope View Site Analytics
 * @permissionScopeId SCOPE.MEDIA.VIEW-SITE-ANALYTICS
 * @permissionScope Set Up Automations
 * @permissionScopeId SCOPE.CRM.SETUP-AUTOMATIONS
 * @permissionScope Manage google ads campaigns
 * @permissionScopeId SCOPE.PROMOTE.MANAGE-GOOGLE-ADS
 * @permissionScope Manage Members and Contacts - all permissions
 * @permissionScopeId SCOPE.DC-CONTACTS-MEGA.MANAGE-MEMBERS-CONTACTS
 * @permissionScope View google ads campaigns
 * @permissionScopeId SCOPE.PROMOTE.VIEW-GOOGLE-ADS
 * @permissionId CONTACTS_LABELS.VIEW
 * @webhook
 * @eventType wix.contacts.v4.label_updated
 * @serviceIdentifier com.wixpress.contacts.labels.v4.ContactLabelsServiceV4
 * @slug updated
 */
declare function onLabelUpdated(handler: (event: LabelUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Lists all contact labels for a site.
 * @public
 * @permissionId CONTACTS_LABELS.VIEW
 * @permissionId contacts:v4:label:consistent_read
 * @applicableIdentity APP
 * @returns List of labels.
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.ListLabels
 */
declare function listLabels(options?: ListLabelsOptions): Promise<NonNullablePaths<ListLabelsResponse, `labels` | `labels.${number}.key` | `labels.${number}.displayName` | `labels.${number}.labelType`, 2>>;
interface ListLabelsOptions {
    /** Filter for labels of the specified type. */
    labelType?: LabelTypeWithLiterals;
    /**
     * Filter for labels in the specified namespace.
     * @minLength 3
     * @maxLength 25
     */
    namespace?: string | null;
    /**
     * Filter for labels where `displayName` starts with the specified case-sensitive string.
     * @minLength 1
     * @maxLength 180
     */
    startsWith?: string | null;
    /** Sorting options. */
    sort?: Sorting;
    /** Paging options. */
    paging?: Paging;
    /** @format LANGUAGE */
    language?: string | null;
}
/**
 * Retrieves a label with a specified name, or creates one if it doesn't exist.
 *
 * Successful calls to this method always return a label,
 * which can be specified in subsequent calls.
 *
 * For example, in the Contacts API, Label Contact and Unlabel Contact
 * calls will fail if you include a non-existent label.
 * To ensure successful calls, you can call this method first,
 * and then use the response in the Label Contact and Unlabel Contact calls.
 *
 * To find an existing label without potentially creating a new one, call
 * Get Label or Query Labels.
 * @param displayName - Display name to retrieve or create.
 *
 * If an existing label is an exact match
 * for the specified display name,
 * the existing label is returned.
 * If not, a new label is created and returned.
 * @public
 * @requiredField displayName
 * @param options - Language options.
 * @permissionId CONTACTS_LABELS.MODIFY
 * @applicableIdentity APP
 * @returns Label that was found or created.
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.FindOrCreateLabel
 */
declare function findOrCreateLabel(displayName: string, options?: FindOrCreateLabelOptions): Promise<NonNullablePaths<FindOrCreateLabelResponse, `label.key` | `label.displayName` | `label.labelType` | `newLabel`, 1> & {
    __applicationErrorsType?: FindOrCreateLabelApplicationErrors;
}>;
interface FindOrCreateLabelOptions {
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
/**
 * Retrieves a label by the specified label key.
 * @param key - Label key.
 *
 * `key` is generated when the label is created.
 * It can't be modified, even if `displayName` is updated.
 * @public
 * @requiredField key
 * @param options - Language options.
 * @permissionId CONTACTS_LABELS.VIEW
 * @applicableIdentity APP
 * @returns The specified label.
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.GetLabel
 */
declare function getLabel(key: string, options?: GetLabelOptions): Promise<NonNullablePaths<ContactLabel, `key` | `displayName` | `labelType`, 0>>;
interface GetLabelOptions {
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
/**
 * Renames a label.
 * @param key - Label key.
 *
 * `key` is generated when the label is created.
 * It can't be modified, even if `displayName` is updated.
 * @public
 * @requiredField key
 * @requiredField label
 * @requiredField label.displayName
 * @param options - Language options.
 * @param label - Label to rename.
 * @permissionId CONTACTS_LABELS.MODIFY
 * @applicableIdentity APP
 * @returns Renamed label.
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.UpdateLabel
 */
declare function renameLabel(key: string, label: NonNullablePaths<RenameLabel, `displayName`, 0>, options?: RenameLabelOptions): Promise<NonNullablePaths<ContactLabel, `key` | `displayName` | `labelType`, 0>>;
interface RenameLabel {
    /**
     * Label namespace.
     *
     * Labels created by calling the Find Or Create Label method
     * are automatically assigned to the `custom` namespace.
     * @readonly
     * @minLength 1
     * @maxLength 25
     */
    namespace?: string | null;
    /**
     * Display name for the namespace,
     * used to organize the list of labels in the site dashboard.
     * @readonly
     * @minLength 1
     * @maxLength 180
     */
    namespaceDisplayName?: string | null;
    /**
     * Label display name shown in the dashboard.
     * @minLength 1
     * @maxLength 180
     */
    displayName?: string;
    /**
     * Label type indicating how the label was created.
     * @readonly
     */
    labelType?: LabelTypeWithLiterals;
    /**
     * Date and time the label was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the label was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
}
interface RenameLabelOptions {
    /**
     * Language for localization.
     * 2-letter language code in [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639_language_codes) format.
     * @format LANGUAGE
     */
    language?: string | null;
}
/**
 * Deletes the specified label from a site and removes it from the contacts it applies to.
 * @param key - Label key to delete.
 * @public
 * @requiredField key
 * @permissionId CONTACTS_LABELS.MODIFY
 * @applicableIdentity APP
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.DeleteLabel
 */
declare function deleteLabel(key: string): Promise<void>;
/**
 * Creates a query to retrieve a list of labels.
 *
 * The `queryLabels()` method builds a query to retrieve a list of labels and returns a `LabelsQueryBuilder` object.
 *
 * The returned object contains the query definition, which is used to call the query using the `find()` method.
 *
 * You can refine the query by chaining `LabelsQueryBuilder` methods onto the query. `LabelsQueryBuilder` methods enable you to filter, sort, and control the response that `queryLabels()` returns.
 *
 * `queryLabels()` is called with the following `LabelsQueryBuilder` defaults, which you can override:
 * - `skip(0)`
 * - `limit(50)`
 * - `descending('_createdDate')`
 *
 * The following `LabelsQueryBuilder` methods are supported for `queryLabels()`. For a full description of the `Labels` object, see the object returned for the `items` field in `LabelsQueryResult`.
 *
 *
 * @public
 * @param options - Language options.
 * @permissionId CONTACTS_LABELS.VIEW
 * @applicableIdentity APP
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.QueryLabels
 */
declare function queryLabels(options?: QueryLabelsOptions): LabelsQueryBuilder;
interface QueryLabelsOptions {
    /**
     * Language for localization.
     *
     * 2-letter language code in
     * [ISO 639-1 alpha-2](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) format.
     * @format LANGUAGE
     */
    language?: string | null | undefined;
}
interface QueryOffsetResult {
    currentPage: number | undefined;
    totalPages: number | undefined;
    totalCount: number | undefined;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface LabelsQueryResult extends QueryOffsetResult {
    items: ContactLabel[];
    query: LabelsQueryBuilder;
    next: () => Promise<LabelsQueryResult>;
    prev: () => Promise<LabelsQueryResult>;
}
interface LabelsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    eq: (propertyName: 'namespace' | 'key' | 'displayName' | 'labelType' | '_createdDate' | '_updatedDate', value: any) => LabelsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ne: (propertyName: 'namespace' | 'key' | 'displayName' | '_createdDate' | '_updatedDate', value: any) => LabelsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ge: (propertyName: '_createdDate' | '_updatedDate', value: any) => LabelsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    gt: (propertyName: '_createdDate' | '_updatedDate', value: any) => LabelsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    le: (propertyName: '_createdDate' | '_updatedDate', value: any) => LabelsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    lt: (propertyName: '_createdDate' | '_updatedDate', value: any) => LabelsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     */
    startsWith: (propertyName: 'displayName', value: string) => LabelsQueryBuilder;
    in: (propertyName: 'key' | 'displayName', value: any) => LabelsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    ascending: (...propertyNames: Array<'displayName' | '_createdDate' | '_updatedDate'>) => LabelsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    descending: (...propertyNames: Array<'displayName' | '_createdDate' | '_updatedDate'>) => LabelsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object. */
    limit: (limit: number) => LabelsQueryBuilder;
    /** @param skip - Number of items to skip in the query results before returning the results. */
    skip: (skip: number) => LabelsQueryBuilder;
    find: () => Promise<LabelsQueryResult>;
}
/**
 * @hidden
 * @fqn com.wixpress.contacts.labels.v4.ContactLabelsServiceV4.QueryLabels
 * @requiredField query
 */
declare function typedQueryLabels(query: ContactLabelQuery, options?: QueryLabelsOptions): Promise<NonNullablePaths<QueryLabelsResponse, `labels` | `labels.${number}.key` | `labels.${number}.displayName` | `labels.${number}.labelType`, 2> & {
    __applicationErrorsType?: QueryLabelsApplicationErrors;
}>;
interface ContactLabelQuerySpec extends QuerySpec {
    paging: 'offset';
    wql: [
        {
            fields: ['key'];
            operators: ['$eq', '$in', '$ne'];
            sort: 'NONE';
        },
        {
            fields: ['displayName'];
            operators: ['$eq', '$in', '$ne', '$startsWith'];
            sort: 'BOTH';
        },
        {
            fields: ['_createdDate', '_updatedDate'];
            operators: ['$eq', '$gt', '$gte', '$lt', '$lte', '$ne'];
            sort: 'BOTH';
        },
        {
            fields: ['namespace'];
            operators: ['$eq', '$ne'];
            sort: 'NONE';
        },
        {
            fields: ['labelType'];
            operators: ['$eq'];
            sort: 'NONE';
        }
    ];
}
type CommonQueryWithEntityContext = Query$1<ContactLabel, ContactLabelQuerySpec>;
type ContactLabelQuery = {
    /**
    ilter object.
  
    Possible filters: `$eq`, `$gt`, `$gte`, `$in`, `$lt`, `$lte`, `$ne`, `$startsWith`.
  
    For a detailed list of supported filters, see [sorting and filtering for labels](https://dev.wix.com/api/rest/contacts/labels/sort-and-filter).
  
    Example: `{ "filter": {
    "displayName": {
    "$startsWith": "Referral"
    }
    }
    }`
    */
    filter?: CommonQueryWithEntityContext['filter'] | null;
    /**
    Sorting options. For a list of fields that can be sorted, see [sorting and filtering for labels](https://dev.wix.com/api/rest/contacts/labels/sort-and-filter).
  
    Example: `{ "sort": [{"fieldName": "displayName", "order": "DESC"}] }`
    @maxSize: 100
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 3000
      */
        fieldName?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
    
      Default: `ASC`.
      */
        order?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['order'];
    }[];
    /**
    Pagination options.
    */
    paging?: {
        /**
      Number of items to return.
    
      Defaults to `1000`. <br>
      Maximum: `2000`.
      */
        limit?: NonNullable<CommonQueryWithEntityContext['paging']>['limit'] | null;
        /**
      Number of items to skip in the current sort order.
      */
        offset?: NonNullable<CommonQueryWithEntityContext['paging']>['offset'] | null;
    };
};
declare const utils: {
    query: _wix_sdk_types.QueryHelpers<ContactLabel, ContactLabelQuerySpec, ContactLabelQuery>;
};

export { type AccountInfo, type AccountInfoMetadata, type ActionEvent, type BaseEventMetadata, type CommonQueryWithEntityContext, type ContactLabel, type ContactLabelNamespace, type ContactLabelQuery, type ContactLabelQuerySpec, type DeleteLabelRequest, type DeleteLabelResponse, type DomainEvent, type DomainEventBodyOneOf, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, type FindOrCreateLabelApplicationErrors, type FindOrCreateLabelOptions, type FindOrCreateLabelRequest, type FindOrCreateLabelResponse, type GdprListRequest, type GdprListResponse, type GetLabelByLegacyIdRequest, type GetLabelByLegacyIdResponse, type GetLabelOptions, type GetLabelRequest, type GetLabelResponse, type IdentificationData, type IdentificationDataIdOneOf, type LabelCreatedEnvelope, type LabelDeletedEnvelope, LabelType, type LabelTypeWithLiterals, type LabelUpdatedEnvelope, type LabelsQueryBuilder, type LabelsQueryResult, type LabelsQuotaReached, type ListLabelNamespacesRequest, type ListLabelNamespacesResponse, type ListLabelsOptions, type ListLabelsRequest, type ListLabelsResponse, type MessageEnvelope, type Paging, type PagingMetadata, type PurgeRequest, type PurgeResponse, type Query, type QueryLabelsApplicationErrors, type QueryLabelsOptions, type QueryLabelsRequest, type QueryLabelsResponse, type RenameLabel, type RenameLabelOptions, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, type UpdateLabelRequest, type UpdateLabelResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, deleteLabel, findOrCreateLabel, getLabel, listLabels, onLabelCreated, onLabelDeleted, onLabelUpdated, queryLabels, renameLabel, typedQueryLabels, utils };
