import { createRequire as _createRequire } from 'node:module';
const require = _createRequire(import.meta.url);
import {
  getExtensionDir
} from "./chunk-C7KTTYNA.js";
import {
  init_esm_shims
} from "./chunk-EXLZF52D.js";

// ../cli-astro-definitions/src/site-extensions.ts
init_esm_shims();
import { join } from "node:path";
function getSiteWidgetExtensionsDir(projectFolder) {
  return join(getExtensionDir(projectFolder), "site", "widgets");
}
function getSiteWidgetExtensionPath(projectFolder, extensionPath) {
  return join(getSiteWidgetExtensionsDir(projectFolder), extensionPath);
}
function getSitePluginExtensionsDir(projectFolder) {
  return join(getExtensionDir(projectFolder), "site", "plugins");
}
function getSitePluginExtensionPath(projectFolder, extensionPath) {
  return join(getSitePluginExtensionsDir(projectFolder), extensionPath);
}
function getContextProviderExtensionsDir(projectFolder) {
  return join(getExtensionDir(projectFolder), "site", "contexts");
}
function getContextProviderExtensionPath(projectFolder, extensionPath) {
  return join(getContextProviderExtensionsDir(projectFolder), extensionPath);
}
function getEmbeddedScriptExtensionsDir(projectFolder) {
  return join(getExtensionDir(projectFolder), "site", "embedded-scripts");
}
function getEmbeddedScriptExtensionPath(projectFolder, extensionPath) {
  return join(getEmbeddedScriptExtensionsDir(projectFolder), extensionPath);
}
function getEditorReactComponentExtensionsDir(projectFolder) {
  return join(getExtensionDir(projectFolder), "site", "components");
}
function getEditorReactComponentExtensionPath(projectFolder, extensionPath) {
  return join(
    getEditorReactComponentExtensionsDir(projectFolder),
    extensionPath
  );
}
function getFunctionLibraryExtensionsDir(projectFolder) {
  return join(getExtensionDir(projectFolder), "site", "function-libraries");
}
function getFunctionLibraryExtensionPath(projectFolder, extensionPath) {
  return join(getFunctionLibraryExtensionsDir(projectFolder), extensionPath);
}

export {
  getSiteWidgetExtensionsDir,
  getSiteWidgetExtensionPath,
  getSitePluginExtensionsDir,
  getSitePluginExtensionPath,
  getContextProviderExtensionsDir,
  getContextProviderExtensionPath,
  getEmbeddedScriptExtensionsDir,
  getEmbeddedScriptExtensionPath,
  getEditorReactComponentExtensionsDir,
  getEditorReactComponentExtensionPath,
  getFunctionLibraryExtensionsDir,
  getFunctionLibraryExtensionPath
};
//# sourceMappingURL=chunk-CJ57RXWR.js.map