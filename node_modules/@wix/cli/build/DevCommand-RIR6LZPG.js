import { createRequire as _createRequire } from 'node:module';
const require = _createRequire(import.meta.url);
import {
  useTunnel
} from "./chunk-Q4IR72L2.js";
import {
  SiteSelector,
  readAppConfig,
  saveAppConfig
} from "./chunk-IT2JZFBU.js";
import {
  useWixCliAppBi
} from "./chunk-FAQEHEMQ.js";
import {
  LifecycleState,
  OverrideType,
  PlatformLinksSection,
  looseComponentSchema,
  parseLooseAppManifest,
  useAppLinks,
  useDevCenterOverridesClient,
  useSiteLinks,
  useUpdateDeploymentTopologyForApp,
  useUpdateDeploymentTopologyForSite
} from "./chunk-D7ORRBCE.js";
import {
  importExtensionsPackageNodeApi,
  isStandaloneExtensionsProject
} from "./chunk-OKVQTYJ5.js";
import {
  createAstroLogger
} from "./chunk-OBROFIK7.js";
import "./chunk-VSMVJJMH.js";
import {
  OnboardingState,
  useVeloClient
} from "./chunk-737RXJMC.js";
import "./chunk-7QKOBCTG.js";
import "./chunk-SYZVVCEX.js";
import "./chunk-QRGKMYDD.js";
import "./chunk-PGRPPBIK.js";
import {
  ComponentType
} from "./chunk-7NOKHY7Z.js";
import {
  createPackageManager,
  getRepoType
} from "./chunk-RMJ3IRO3.js";
import {
  execa
} from "./chunk-NMCF7XG5.js";
import "./chunk-MHV22UIU.js";
import {
  useProjectModel,
  useSiteConfig
} from "./chunk-TMWD75YU.js";
import {
  ASTRO_APP_CONFIG_PATH,
  devMetadataSchema,
  getDebugLogPath
} from "./chunk-C7KTTYNA.js";
import {
  useHttpClient
} from "./chunk-BGEGAGDQ.js";
import {
  ErrorViewer,
  getHttpErrorInfo,
  isHttpValidationError,
  resolveUrl,
  serializer,
  toURLSearchParams,
  useBiLogger,
  useDebugLog,
  useErrorReporter
} from "./chunk-5XKGOUNA.js";
import "./chunk-4EAO7VWS.js";
import "./chunk-YC3JSVSJ.js";
import {
  Alert,
  Box_default,
  ConfirmInput,
  Key,
  PointerIcon,
  Spinner,
  Text,
  useAsync,
  useAsyncCallback,
  useExit,
  use_input_default
} from "./chunk-4LMXM7B3.js";
import {
  stripAnsi
} from "./chunk-AFPDHQYI.js";
import "./chunk-HYKBBZLS.js";
import "./chunk-C5RTKQVL.js";
import {
  usePanoramaMethodLogger
} from "./chunk-SUANVVJS.js";
import "./chunk-JHSYPFGD.js";
import {
  extractHttpError
} from "./chunk-XRIF32OW.js";
import {
  createEnv
} from "./chunk-QH7LPHNT.js";
import "./chunk-RPTAJN5Y.js";
import {
  require_react
} from "./chunk-TODKQSCF.js";
import {
  readUserConfig,
  updateUserConfig
} from "./chunk-AMYLKVBU.js";
import "./chunk-4UTFAKA5.js";
import {
  outputDir
} from "./chunk-SARPTPX4.js";
import {
  getTestOverrides
} from "./chunk-B6IA5Q6I.js";
import {
  external_exports
} from "./chunk-SDLCFZRL.js";
import {
  CliError,
  CliErrorCode,
  normalizeError,
  require_lib
} from "./chunk-CR5MBLNJ.js";
import {
  __toESM,
  init_esm_shims
} from "./chunk-EXLZF52D.js";

// ../cli-astro-commands/src/components/DevCommand/index.ts
init_esm_shims();

// ../cli-astro-commands/src/components/DevCommand/DevCommand.tsx
init_esm_shims();
var import_react24 = __toESM(require_react(), 1);
var import_variant9 = __toESM(require_lib(), 1);

// ../cli-astro-commands/src/components/DevCommand/utils/useAstroLogger.tsx
init_esm_shims();
var import_react = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/DevCommand/utils/devLogStream.tsx
init_esm_shims();
import { EOL } from "node:os";
import { createWriteStream } from "node:fs";
import { PassThrough } from "node:stream";
import { pipeline } from "node:stream/promises";
import { dirname } from "node:path";
var streamCache = /* @__PURE__ */ new Map();
var createNoopStream = () => ({
  write: () => {
  },
  close: () => {
  },
  isClosed: () => true
});
var getDevLogStream = async (options) => {
  const { filePath } = options;
  const entry = streamCache.get(filePath);
  if (entry && !entry.stream.isClosed()) {
    entry.refCount++;
    return {
      write: (msg) => entry.stream.write(msg),
      release: () => {
        entry.refCount--;
        if (entry.refCount === 0) {
          entry.stream.close();
          streamCache.delete(filePath);
        }
      }
    };
  }
  let stream;
  try {
    stream = await createDevLogStream(options);
  } catch {
    stream = createNoopStream();
  }
  const newEntry = { stream, refCount: 1 };
  streamCache.set(filePath, newEntry);
  return {
    write: (msg) => newEntry.stream.write(msg),
    release: () => {
      newEntry.refCount--;
      if (newEntry.refCount === 0) {
        newEntry.stream.close();
        streamCache.delete(filePath);
      }
    }
  };
};
var createDevLogStream = async ({ filePath }) => {
  await outputDir(dirname(filePath));
  const source = new PassThrough();
  const fileStream = createWriteStream(filePath, { flags: "w" });
  let closed = false;
  const isClosed = () => closed;
  pipeline(source, fileStream).catch(() => {
    if (!closed) {
      closed = true;
    }
  });
  const write = (msg) => {
    if (closed) {
      return;
    }
    source.write(stripAnsi(msg).concat(EOL));
  };
  const close = () => {
    if (closed) {
      return;
    }
    closed = true;
    source.end();
  };
  return {
    write,
    close,
    isClosed
  };
};

// ../cli-astro-commands/src/components/DevCommand/utils/useAstroLogger.tsx
var FORMAT_REGEXP = /^\d{1,2}:\d{1,2}:\d{1,2}\s*/;
var formatMessage = (msg, type = "info") => `[${(/* @__PURE__ */ new Date()).toISOString()}] [cli] [${type}] ${stripAnsi(msg).replace(FORMAT_REGEXP, "")}`;
var useAstroLogger = ({
  isPrefixed = true
} = {}) => {
  const {
    model: { projectFolder }
  } = useProjectModel();
  const streamRef = (0, import_react.useRef)();
  const logger = (0, import_react.useMemo)(
    () => createAstroLogger({
      isPrefixed,
      onLog: (msg) => {
        streamRef.current?.write(formatMessage(msg));
      },
      onError: (msg) => {
        streamRef.current?.write(formatMessage(msg, "error"));
      }
    }),
    [isPrefixed]
  );
  (0, import_react.useEffect)(() => {
    let shouldCleanStream = false;
    void getDevLogStream({
      filePath: getDebugLogPath(projectFolder)
    }).then((devLogStream) => {
      if (shouldCleanStream) {
        devLogStream.release();
        return;
      }
      streamRef.current = devLogStream;
    });
    return () => {
      shouldCleanStream = true;
      streamRef.current?.release();
    };
  }, [projectFolder]);
  return logger;
};

// ../cli-astro-commands/src/components/DevCommand/hooks/useFetchDevMetadata.ts
init_esm_shims();

// ../../node_modules/ufo/dist/index.mjs
init_esm_shims();
var r = String.fromCharCode;
var HASH_RE = /#/g;
var AMPERSAND_RE = /&/g;
var SLASH_RE = /\//g;
var EQUAL_RE = /=/g;
var PLUS_RE = /\+/g;
var ENC_CARET_RE = /%5e/gi;
var ENC_BACKTICK_RE = /%60/gi;
var ENC_PIPE_RE = /%7c/gi;
var ENC_SPACE_RE = /%20/gi;
function encode(text) {
  return encodeURI("" + text).replace(ENC_PIPE_RE, "|");
}
function encodeQueryValue(input) {
  return encode(typeof input === "string" ? input : JSON.stringify(input)).replace(PLUS_RE, "%2B").replace(ENC_SPACE_RE, "+").replace(HASH_RE, "%23").replace(AMPERSAND_RE, "%26").replace(ENC_BACKTICK_RE, "`").replace(ENC_CARET_RE, "^").replace(SLASH_RE, "%2F");
}
function encodeQueryKey(text) {
  return encodeQueryValue(text).replace(EQUAL_RE, "%3D");
}
function decode(text = "") {
  try {
    return decodeURIComponent("" + text);
  } catch {
    return "" + text;
  }
}
function decodeQueryKey(text) {
  return decode(text.replace(PLUS_RE, " "));
}
function decodeQueryValue(text) {
  return decode(text.replace(PLUS_RE, " "));
}
function parseQuery(parametersString = "") {
  const object = /* @__PURE__ */ Object.create(null);
  if (parametersString[0] === "?") {
    parametersString = parametersString.slice(1);
  }
  for (const parameter of parametersString.split("&")) {
    const s = parameter.match(/([^=]+)=?(.*)/) || [];
    if (s.length < 2) {
      continue;
    }
    const key = decodeQueryKey(s[1]);
    if (key === "__proto__" || key === "constructor") {
      continue;
    }
    const value = decodeQueryValue(s[2] || "");
    if (object[key] === void 0) {
      object[key] = value;
    } else if (Array.isArray(object[key])) {
      object[key].push(value);
    } else {
      object[key] = [object[key], value];
    }
  }
  return object;
}
function encodeQueryItem(key, value) {
  if (typeof value === "number" || typeof value === "boolean") {
    value = String(value);
  }
  if (!value) {
    return encodeQueryKey(key);
  }
  if (Array.isArray(value)) {
    return value.map(
      (_value) => `${encodeQueryKey(key)}=${encodeQueryValue(_value)}`
    ).join("&");
  }
  return `${encodeQueryKey(key)}=${encodeQueryValue(value)}`;
}
function stringifyQuery(query) {
  return Object.keys(query).filter((k) => query[k] !== void 0).map((k) => encodeQueryItem(k, query[k])).filter(Boolean).join("&");
}
var PROTOCOL_STRICT_REGEX = /^[\s\w\0+.-]{2,}:([/\\]{1,2})/;
var PROTOCOL_REGEX = /^[\s\w\0+.-]{2,}:([/\\]{2})?/;
var PROTOCOL_RELATIVE_REGEX = /^([/\\]\s*){2,}[^/\\]/;
var TRAILING_SLASH_RE = /\/$|\/\?|\/#/;
function hasProtocol(inputString, opts = {}) {
  if (typeof opts === "boolean") {
    opts = { acceptRelative: opts };
  }
  if (opts.strict) {
    return PROTOCOL_STRICT_REGEX.test(inputString);
  }
  return PROTOCOL_REGEX.test(inputString) || (opts.acceptRelative ? PROTOCOL_RELATIVE_REGEX.test(inputString) : false);
}
function hasTrailingSlash(input = "", respectQueryAndFragment) {
  if (!respectQueryAndFragment) {
    return input.endsWith("/");
  }
  return TRAILING_SLASH_RE.test(input);
}
function withTrailingSlash(input = "", respectQueryAndFragment) {
  if (!respectQueryAndFragment) {
    return input.endsWith("/") ? input : input + "/";
  }
  if (hasTrailingSlash(input, true)) {
    return input || "/";
  }
  let path = input;
  let fragment = "";
  const fragmentIndex = input.indexOf("#");
  if (fragmentIndex !== -1) {
    path = input.slice(0, fragmentIndex);
    fragment = input.slice(fragmentIndex);
    if (!path) {
      return fragment;
    }
  }
  const [s0, ...s] = path.split("?");
  return s0 + "/" + (s.length > 0 ? `?${s.join("?")}` : "") + fragment;
}
function hasLeadingSlash(input = "") {
  return input.startsWith("/");
}
function withoutLeadingSlash(input = "") {
  return (hasLeadingSlash(input) ? input.slice(1) : input) || "/";
}
function isNonEmptyURL(url) {
  return url && url !== "/";
}
function resolveURL(base = "", ...inputs) {
  if (typeof base !== "string") {
    throw new TypeError(
      `URL input should be string received ${typeof base} (${base})`
    );
  }
  const filteredInputs = inputs.filter((input) => isNonEmptyURL(input));
  if (filteredInputs.length === 0) {
    return base;
  }
  const url = parseURL(base);
  for (const inputSegment of filteredInputs) {
    const urlSegment = parseURL(inputSegment);
    if (urlSegment.pathname) {
      url.pathname = withTrailingSlash(url.pathname) + withoutLeadingSlash(urlSegment.pathname);
    }
    if (urlSegment.hash && urlSegment.hash !== "#") {
      url.hash = urlSegment.hash;
    }
    if (urlSegment.search && urlSegment.search !== "?") {
      if (url.search && url.search !== "?") {
        const queryString = stringifyQuery({
          ...parseQuery(url.search),
          ...parseQuery(urlSegment.search)
        });
        url.search = queryString.length > 0 ? "?" + queryString : "";
      } else {
        url.search = urlSegment.search;
      }
    }
  }
  return stringifyParsedURL(url);
}
var protocolRelative = Symbol.for("ufo:protocolRelative");
function parseURL(input = "", defaultProto) {
  const _specialProtoMatch = input.match(
    /^[\s\0]*(blob:|data:|javascript:|vbscript:)(.*)/i
  );
  if (_specialProtoMatch) {
    const [, _proto, _pathname = ""] = _specialProtoMatch;
    return {
      protocol: _proto.toLowerCase(),
      pathname: _pathname,
      href: _proto + _pathname,
      auth: "",
      host: "",
      search: "",
      hash: ""
    };
  }
  if (!hasProtocol(input, { acceptRelative: true })) {
    return defaultProto ? parseURL(defaultProto + input) : parsePath(input);
  }
  const [, protocol = "", auth, hostAndPath = ""] = input.replace(/\\/g, "/").match(/^[\s\0]*([\w+.-]{2,}:)?\/\/([^/@]+@)?(.*)/) || [];
  let [, host = "", path = ""] = hostAndPath.match(/([^#/?]*)(.*)?/) || [];
  if (protocol === "file:") {
    path = path.replace(/\/(?=[A-Za-z]:)/, "");
  }
  const { pathname, search, hash } = parsePath(path);
  return {
    protocol: protocol.toLowerCase(),
    auth: auth ? auth.slice(0, Math.max(0, auth.length - 1)) : "",
    host,
    pathname,
    search,
    hash,
    [protocolRelative]: !protocol
  };
}
function parsePath(input = "") {
  const [pathname = "", search = "", hash = ""] = (input.match(/([^#?]*)(\?[^#]*)?(#.*)?/) || []).splice(1);
  return {
    pathname,
    search,
    hash
  };
}
function stringifyParsedURL(parsed) {
  const pathname = parsed.pathname || "";
  const search = parsed.search ? (parsed.search.startsWith("?") ? "" : "?") + parsed.search : "";
  const hash = parsed.hash || "";
  const auth = parsed.auth ? parsed.auth + "@" : "";
  const host = parsed.host || "";
  const proto = parsed.protocol || parsed[protocolRelative] ? (parsed.protocol || "") + "//" : "";
  return proto + auth + host + pathname + search + hash;
}

// ../cli-astro-commands/src/components/DevCommand/hooks/useFetchDevMetadata.ts
function useFetchDevMetadata({ base }) {
  const http = useHttpClient({ type: "standalone" });
  return useAsyncCallback(async (_, serverLocalUrl) => {
    const url = resolveURL(
      serverLocalUrl,
      base ?? "",
      "_wix/dev-metadata.json"
    );
    try {
      const { data } = await http.get(url.toString());
      return {
        devMetadata: devMetadataSchema.parse(data),
        serverLocalUrl
      };
    } catch {
      return {
        devMetadata: null,
        serverLocalUrl
      };
    }
  });
}

// ../cli-astro-commands/src/components/DevCommand/DevScreenBorder.tsx
init_esm_shims();
var import_react2 = __toESM(require_react(), 1);
var DevScreenBorder = ({ children }) => {
  return /* @__PURE__ */ import_react2.default.createElement(
    Box_default,
    {
      flexDirection: "column",
      borderTop: true,
      borderLeft: false,
      borderBottom: false,
      borderRight: false,
      borderStyle: "round",
      borderColor: "blueBright",
      width: "95%"
    },
    children
  );
};

// ../cli-astro-commands/src/components/DevCommand/SiteDevFlow.tsx
init_esm_shims();
var import_react9 = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/DevCommand/DevEnvironment.tsx
init_esm_shims();
var import_react6 = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/DevCommand/hooks/useDevCenterUpdater.tsx
init_esm_shims();
var import_react4 = __toESM(require_react(), 1);
var import_variant2 = __toESM(require_lib(), 1);

// ../cli-astro-commands/src/components/DevCommand/hooks/useFetchAppManifest.ts
init_esm_shims();
var import_react3 = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/DevCommand/utils/isTunnelRequired.ts
init_esm_shims();
function isTunnelRequired(appManifest) {
  for (const extension of appManifest.components) {
    const knownAppManifest = parseKnownExtensionManifest(extension);
    if (knownAppManifest != null && knownAppManifest.compType === ComponentType.WEBHOOK) {
      return !URL.canParse(knownAppManifest.compData.webhook.callbackUrl);
    }
  }
  return false;
}
function parseKnownExtensionManifest(extensionManifest) {
  const appManifestResult = looseComponentSchema.safeParse(extensionManifest);
  if (appManifestResult.error) {
    return null;
  }
  return appManifestResult.data;
}

// ../cli-astro-commands/src/components/DevCommand/hooks/useFetchAppManifest.ts
function useFetchAppManifest({ base }) {
  const http = useHttpClient({ type: "standalone" });
  return (0, import_react3.useCallback)(
    async (serverLocalUrl, devMetadata, baseUrl, tunnelUrl) => {
      const url = resolveURL(
        serverLocalUrl,
        base ?? "",
        "_wix/app-manifest.json"
      );
      const { data: text } = await http.get(url.toString(), {
        responseType: "text",
        transitional: {
          silentJSONParsing: true,
          forcedJSONParsing: false,
          clarifyTimeoutError: false
        },
        disableWixHeaders: true
      });
      if (text === "") {
        return {
          manifest: null,
          isTunnelRequired: false
        };
      }
      if (devMetadata != null) {
        const { staticsUrlPlaceholder, serverUrlPlaceholder } = devMetadata;
        const placeholders = /* @__PURE__ */ new Map();
        placeholders.set(staticsUrlPlaceholder, baseUrl);
        placeholders.set(serverUrlPlaceholder, tunnelUrl);
        return {
          manifest: parseLooseAppManifest(text, placeholders),
          isTunnelRequired: text.includes(serverUrlPlaceholder)
        };
      }
      const manifestV1 = parseLooseAppManifest(text);
      return {
        manifest: manifestV1,
        isTunnelRequired: isTunnelRequired(manifestV1)
      };
    },
    [base, http]
  );
}

// ../cli-astro-commands/src/components/DevCommand/utils/DevCenterWatcher.ts
init_esm_shims();
import EventEmitter from "node:events";
var DevCenterWatcher = class extends EventEmitter {
  onManifestUpdated(listener) {
    this.on("manifestUpdated", listener);
    return () => {
      this.off("manifestUpdated", listener);
    };
  }
};

// ../cli-astro-commands/src/components/DevCommand/hooks/checkManifestForChanges.ts
init_esm_shims();
var import_variant = __toESM(require_lib(), 1);
import { isDeepStrictEqual } from "node:util";
var ManifestUpdateResult = (0, import_variant.variant)({
  Changed: (0, import_variant.fields)(),
  Unchanged: {}
});
function checkManifestForChanges(previous, latest) {
  if (previous == null) {
    return ManifestUpdateResult.Changed({ updatedComponents: [] });
  }
  if (isDeepStrictEqual(previous, latest)) {
    return ManifestUpdateResult.Unchanged();
  }
  const oldComponentsMap = new Map(
    previous.components.map((comp) => [comp.compId, comp])
  );
  const changedComponents = latest.components.filter((newComp) => {
    const oldComp = oldComponentsMap.get(newComp.compId);
    return !isDeepStrictEqual(newComp, oldComp);
  });
  return ManifestUpdateResult.Changed({ updatedComponents: changedComponents });
}

// ../cli-astro-commands/src/components/DevCommand/hooks/useDevCenterUpdater.tsx
var { pollInterval } = getTestOverrides();
var { devCenterDraftOverrides } = createEnv();
var overrideLifecycleState = devCenterDraftOverrides ? LifecycleState.DRAFT : void 0;
var useDevCenterUpdater = ({
  siteId,
  base,
  serverLocalUrl,
  serverExposedUrl,
  devMetadata
}) => {
  const devCenterWatcher = (0, import_react4.useMemo)(() => new DevCenterWatcher(), []);
  const [versionOverrideId, setVersionOverrideId] = (0, import_react4.useState)();
  const [manifest, setManifest] = (0, import_react4.useState)(null);
  const { reportError } = useErrorReporter();
  const writeToDebugLog = useDebugLog();
  const exit = useExit();
  const {
    model: { projectFolder, releaseAppId }
  } = useProjectModel();
  const logger = useAstroLogger({ isPrefixed: true });
  const devCenterOverrides = useDevCenterOverridesClient({ siteId });
  const openTunnel = useTunnel();
  const tunnelUrl = (0, import_react4.useRef)();
  const [tunnelRequired, setTunnelRequired] = (0, import_react4.useState)(false);
  const fetchAppManifest = useFetchAppManifest({ base });
  const baseUrl = serverExposedUrl ?? serverLocalUrl;
  const updateDevCenter = (0, import_react4.useCallback)(
    async (latestManifest, isTunnelRequired2) => {
      const manifestUpdateResult = checkManifestForChanges(
        manifest,
        latestManifest
      );
      if ((0, import_variant2.isType)(manifestUpdateResult, "Unchanged")) {
        return manifestUpdateResult;
      }
      if (!serverExposedUrl && !tunnelUrl.current && isTunnelRequired2) {
        const config = await readUserConfig();
        if (!config.tunneling) {
          setTunnelRequired(true);
          return ManifestUpdateResult.Unchanged();
        }
        const serverLocalPort = parseInt(new URL(serverLocalUrl).port, 10);
        const tunnel = await openTunnel(serverLocalPort);
        tunnelUrl.current = tunnel.url;
        return ManifestUpdateResult.Unchanged();
      }
      const appConfig = await readAppConfig(projectFolder, writeToDebugLog);
      let updatedVersionOverrideId = appConfig?.devArmTag;
      if (!updatedVersionOverrideId) {
        updatedVersionOverrideId = await devCenterOverrides.createComponentsOverride({
          type: OverrideType.DEV,
          appManifest: latestManifest,
          projectId: releaseAppId,
          lifecycleState: overrideLifecycleState
        });
      } else {
        const updateState = await devCenterOverrides.updateComponentsOverrides({
          id: updatedVersionOverrideId,
          type: OverrideType.DEV,
          appManifest: latestManifest,
          projectId: releaseAppId
        });
        if (!(0, import_variant2.isType)(updateState, "Updated")) {
          updatedVersionOverrideId = await devCenterOverrides.createComponentsOverride({
            type: OverrideType.DEV,
            appManifest: latestManifest,
            projectId: releaseAppId,
            lifecycleState: overrideLifecycleState
          });
        }
      }
      if (updatedVersionOverrideId !== appConfig?.devArmTag) {
        await saveAppConfig(projectFolder, {
          devArmTag: updatedVersionOverrideId
        });
      }
      setManifest(latestManifest);
      setVersionOverrideId(updatedVersionOverrideId);
      devCenterWatcher.emit("manifestUpdated", {
        versionOverrideId: updatedVersionOverrideId,
        updatedComponents: manifestUpdateResult.updatedComponents
      });
      return manifestUpdateResult;
    },
    [
      releaseAppId,
      manifest,
      serverLocalUrl,
      serverExposedUrl,
      devCenterOverrides,
      projectFolder,
      openTunnel,
      devCenterWatcher,
      writeToDebugLog
    ]
  );
  const [timer, setTimer] = (0, import_react4.useState)(Date.now());
  (0, import_react4.useEffect)(() => {
    let disposed = false;
    async function fetchAndUpdate() {
      const { manifest: manifest2, isTunnelRequired: isTunnelRequired2 } = await fetchAppManifest(
        serverLocalUrl,
        devMetadata,
        baseUrl,
        tunnelUrl.current ?? baseUrl
      );
      if (manifest2 == null) {
        return;
      }
      try {
        const result = await updateDevCenter(manifest2, isTunnelRequired2);
        if ((0, import_variant2.isType)(result, "Changed")) {
          logger.log(
            "App manifest updated. Refresh existing browser tabs to view the latest changes."
          );
          if (process.env.REMOTE_MACHINE_TASK_EVENTS_URL) {
            const eventData = {
              type: "manifest_update",
              appId: manifest2.appId,
              overrideId: versionOverrideId,
              result: "SUCCESS"
            };
            void fetch(process.env.REMOTE_MACHINE_TASK_EVENTS_URL, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                eventName: "manifest-update",
                taskId: process.env.REMOTE_MACHINE_TASK_ID,
                executionId: process.env.REMOTE_MACHINE_EXECUTION_ID,
                timestamp: (/* @__PURE__ */ new Date()).toISOString(),
                eventData
              })
            });
          }
        }
      } catch (error) {
        setManifest(manifest2);
        logger.error(/* @__PURE__ */ import_react4.default.createElement(ErrorViewer, { error }));
        writeToDebugLog(error);
        reportError(error);
        if (process.env.REMOTE_MACHINE_TASK_EVENTS_URL) {
          const normalizedError = normalizeError(error);
          const httpError = extractHttpError(normalizedError);
          if (httpError) {
            const errorInfo = getHttpErrorInfo(httpError);
            let validationViolations;
            try {
              if (isHttpValidationError(httpError)) {
                validationViolations = httpError.response?.data.details.validationError.fieldViolations.map(
                  (violation) => `${violation.field}: ${violation.description}`
                ).join("\n");
              }
            } catch {
            }
            const eventData = {
              type: "manifest_update",
              appId: manifest2.appId,
              overrideId: versionOverrideId ?? "",
              result: "FAILURE",
              code: errorInfo.response.status,
              message: errorInfo.response.data.message,
              validationViolations
            };
            void fetch(process.env.REMOTE_MACHINE_TASK_EVENTS_URL, {
              method: "POST",
              headers: { "Content-Type": "application/json" },
              body: JSON.stringify({
                eventName: "manifest-update",
                taskId: process.env.REMOTE_MACHINE_TASK_ID,
                executionId: process.env.REMOTE_MACHINE_EXECUTION_ID,
                timestamp: (/* @__PURE__ */ new Date()).toISOString(),
                eventData
              })
            });
          }
        }
        throw error;
      }
    }
    void fetchAndUpdate().catch((error) => {
      if (error instanceof Error && error.name === "TrustedAppIsNotMarkedAsInternal") {
        exit(error);
      }
    }).finally(() => {
      if (!disposed) {
        setTimeout(() => setTimer(Date.now()), pollInterval ?? 1e3);
      }
    });
    return () => {
      disposed = true;
    };
  }, [timer]);
  return { baseUrl, tunnelRequired, versionOverrideId, devCenterWatcher };
};

// ../cli-astro-commands/src/components/DevCommand/TunnelingConsent.tsx
init_esm_shims();
var import_react5 = __toESM(require_react(), 1);
var import_variant3 = __toESM(require_lib(), 1);
var TunnelingConsent = ({ required, children }) => {
  const exit = useExit();
  const [approved, setApproved] = (0, import_react5.useState)();
  const { status } = useAsync(async () => {
    const config = await readUserConfig();
    if (config.tunneling) {
      setApproved(true);
    }
    return config;
  }, []);
  if (!required || approved) {
    return children;
  }
  return /* @__PURE__ */ import_react5.default.createElement(import_react5.default.Fragment, null, (0, import_variant3.match)(status, {
    Success: () => /* @__PURE__ */ import_react5.default.createElement(
      ConfirmInput,
      {
        label: "To continue, a secure tunnel will be set up from your machine for development purposes.",
        initialValue: true,
        onSubmit: async (approved2) => {
          setApproved(approved2);
          await updateUserConfig({ tunneling: approved2 });
          if (!approved2) {
            exit();
          }
        }
      }
    ),
    Error: () => null,
    Loading: () => null
  }), approved === false && /* @__PURE__ */ import_react5.default.createElement(Text, { skin: "error" }, "Operation aborted. Tunneling is necessary to run your application."));
};

// ../cli-astro-commands/src/components/DevCommand/DevEnvironment.tsx
var DevEnvironmentContext = (0, import_react6.createContext)(null);
var DevEnvironmentProvider = ({
  siteId,
  base,
  serverLocalUrl,
  serverExposedUrl,
  devMetadata,
  children
}) => {
  const { baseUrl, tunnelRequired, versionOverrideId, devCenterWatcher } = useDevCenterUpdater({
    siteId,
    base,
    serverLocalUrl,
    serverExposedUrl,
    devMetadata
  });
  return /* @__PURE__ */ import_react6.default.createElement(TunnelingConsent, { required: tunnelRequired }, versionOverrideId == null ? /* @__PURE__ */ import_react6.default.createElement(Spinner, { text: "Preparing your dashboard..." }) : /* @__PURE__ */ import_react6.default.createElement(
    DevEnvironmentContext.Provider,
    {
      value: { baseUrl, versionOverrideId, devCenterWatcher }
    },
    children
  ));
};
var useDevEnvironment = () => {
  const ctx = (0, import_react6.useContext)(DevEnvironmentContext);
  if (!ctx) {
    throw new Error(
      "useDevEnvironment must be used inside DevEnvironmentProvider"
    );
  }
  return ctx;
};

// ../cli-astro-commands/src/components/DevCommand/site-flow/SiteDev.tsx
init_esm_shims();
var import_react8 = __toESM(require_react(), 1);
var import_variant4 = __toESM(require_lib(), 1);

// ../cli-astro-commands/src/components/DevCommand/site-flow/useSiteDeploymentTopologyUpdater.tsx
init_esm_shims();
var import_react7 = __toESM(require_react(), 1);
function useSiteDeploymentTopologyUpdater() {
  const { baseUrl, versionOverrideId } = useDevEnvironment();
  const { siteId } = useSiteConfig();
  const updateTopology = useUpdateDeploymentTopologyForSite(siteId);
  const exit = useExit();
  (0, import_react7.useEffect)(() => {
    updateTopology({
      versionOverrideId,
      siteUrl: baseUrl,
      environment: "development"
    }).catch(exit);
  }, [versionOverrideId, baseUrl, updateTopology, exit]);
}

// ../cli-astro-commands/src/components/DevCommand/site-flow/SiteDev.tsx
var SiteDev = () => {
  const { appId, siteId } = useSiteConfig();
  const { baseUrl, versionOverrideId } = useDevEnvironment();
  const veloClient = useVeloClient();
  const links = useSiteLinks({
    cmd: "dev",
    siteId,
    overrideId: versionOverrideId,
    siteUrl: baseUrl
  });
  const { status: gitHubOnboardingStatus } = useAsync(
    () => veloClient.getGitHubOnboardingStatus(appId),
    [appId]
  );
  useSiteDeploymentTopologyUpdater();
  const isGitHubEnabled = (0, import_variant4.match)(gitHubOnboardingStatus, {
    Success: ({ result }) => result?.status === OnboardingState.GITHUB_ENABLED,
    Error: () => false,
    Loading: () => false
  });
  const visibleLinks = isGitHubEnabled ? links : links.filter((link) => link.platform !== "editor");
  return /* @__PURE__ */ import_react8.default.createElement(import_react8.default.Fragment, null, /* @__PURE__ */ import_react8.default.createElement(Text, null, "Development environment ready"), /* @__PURE__ */ import_react8.default.createElement(PlatformLinksSection, { links: visibleLinks }));
};

// ../cli-astro-commands/src/components/DevCommand/SiteDevFlow.tsx
var SiteDevFlow = (props) => {
  const { siteId } = useSiteConfig();
  return /* @__PURE__ */ import_react9.default.createElement(DevEnvironmentProvider, { ...props, siteId }, /* @__PURE__ */ import_react9.default.createElement(SiteDev, null));
};

// ../cli-astro-commands/src/components/DevCommand/AppDevFlow.tsx
init_esm_shims();
var import_react22 = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/index.ts
init_esm_shims();

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/SiteProvider.tsx
init_esm_shims();
var import_react12 = __toESM(require_react(), 1);
var import_variant6 = __toESM(require_lib(), 1);
import process2 from "node:process";

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/ResolveSite.tsx
init_esm_shims();
var import_react10 = __toESM(require_react(), 1);
var ResolveSite = ({ onSubmit }) => {
  const exit = useExit();
  const { model } = useProjectModel();
  const biLogger = useBiLogger();
  return /* @__PURE__ */ import_react10.default.createElement(import_react10.default.Fragment, null, /* @__PURE__ */ import_react10.default.createElement(Alert, { type: "insight" }, "You don't have a development site currently configured."), /* @__PURE__ */ import_react10.default.createElement(
    SiteSelector,
    {
      appId: model.config.appId,
      biLogger,
      onSubmit: (site) => onSubmit(site.id),
      onError: exit
    }
  ));
};

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/context.ts
init_esm_shims();
var import_react11 = __toESM(require_react(), 1);
var SiteContext = (0, import_react11.createContext)(null);

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/site-value.ts
init_esm_shims();
var import_variant5 = __toESM(require_lib(), 1);
var SiteValue = (0, import_variant5.variant)({
  NotConfigured: {},
  Resolved: (0, import_variant5.fields)()
});

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/SiteProvider.tsx
var SiteProvider = ({
  children
}) => {
  const exit = useExit();
  const { model } = useProjectModel();
  const { projectFolder, env } = model;
  const userDefinedSiteId = env.WIX_SITE_ID;
  const writeDebugLog = useDebugLog();
  const { status, merge } = useAsync(async () => {
    if (userDefinedSiteId) {
      return SiteValue.Resolved({ siteId: userDefinedSiteId });
    }
    const siteId = (await readAppConfig(projectFolder, writeDebugLog))?.siteId;
    if (!siteId && (process2.stdin.isTTY !== true || process2.stdout.isTTY !== true)) {
      throw new CliError({
        code: CliErrorCode.DevelopmentSiteNotConfigured(),
        cause: null
      });
    }
    return siteId ? SiteValue.Resolved({ siteId }) : SiteValue.NotConfigured();
  }, []);
  const setSiteId = (0, import_react12.useCallback)(
    async (siteId) => {
      if (userDefinedSiteId) {
        throw new Error("Cannot change site: siteId is user-defined");
      }
      try {
        await saveAppConfig(projectFolder, { siteId });
        merge({ result: SiteValue.Resolved({ siteId }) });
      } catch (e) {
        throw new CliError({
          code: CliErrorCode.FailedToSaveDevelopmentSite({
            path: ASTRO_APP_CONFIG_PATH
          }),
          cause: e
        });
      }
    },
    [merge, projectFolder, userDefinedSiteId]
  );
  const handleResolveSite = (0, import_react12.useCallback)(
    (siteId) => setSiteId(siteId).catch(exit),
    [exit, setSiteId]
  );
  return (0, import_variant6.match)(status, {
    Error: () => null,
    Loading: () => /* @__PURE__ */ import_react12.default.createElement(Spinner, { text: "Resolving development site..." }),
    Success: ({ result: state }) => {
      return (0, import_variant6.match)(state, {
        NotConfigured: () => /* @__PURE__ */ import_react12.default.createElement(ResolveSite, { onSubmit: handleResolveSite }),
        Resolved: ({ siteId }) => /* @__PURE__ */ import_react12.default.createElement(
          SiteContext.Provider,
          {
            value: {
              siteId,
              setSiteId,
              canChangeSite: !userDefinedSiteId
            }
          },
          children(siteId)
        )
      });
    }
  });
};

// ../cli-astro-commands/src/components/DevCommand/app-flow/site/useSite.ts
init_esm_shims();
var import_react13 = __toESM(require_react(), 1);
function useSite() {
  const context = (0, import_react13.useContext)(SiteContext);
  if (context == null) {
    throw new Error("useSite() cannot be used without an <SiteProvider>");
  }
  return context;
}

// ../cli-astro-commands/src/components/DevCommand/app-flow/AppDev.tsx
init_esm_shims();
var import_react21 = __toESM(require_react(), 1);
var import_variant8 = __toESM(require_lib(), 1);

// ../cli-astro-commands/src/components/DevCommand/app-flow/useAppDeploymentTopologyUpdater.tsx
init_esm_shims();
var import_react14 = __toESM(require_react(), 1);
function useAppDeploymentTopologyUpdater() {
  const { versionOverrideId } = useDevEnvironment();
  const { siteId } = useSite();
  const updateTopology = useUpdateDeploymentTopologyForApp(siteId);
  const exit = useExit();
  (0, import_react14.useEffect)(() => {
    updateTopology({ versionOverrideId, environment: "development" }).catch(
      exit
    );
  }, [versionOverrideId, updateTopology, exit]);
}

// ../cli-astro-commands/src/components/DevCommand/app-flow/useERCManifestUpdateNotifier.ts
init_esm_shims();
var import_react16 = __toESM(require_react(), 1);

// ../metasite-manager-client/src/index.ts
init_esm_shims();

// ../metasite-manager-client/src/useMetasiteManagerClient.ts
init_esm_shims();
var import_react15 = __toESM(require_react(), 1);

// ../metasite-manager-client/src/metasite-manager-client.ts
init_esm_shims();

// ../../node_modules/@wix/ambassador-metasite-api-v1-meta-site/build/es/http.impl.js
init_esm_shims();
var _createMetaSiteFromTemplateRequest = {};
var _createMetaSiteFromTemplateResponse = { metaSite: "_metaSite" };
var _getUrlRequest = {};
var _getUrlResponse = {};
var _metaSite = {
  dateCreated: "google.protobuf.Timestamp",
  dateUpdated: "google.protobuf.Timestamp",
  accountCreatedDate: "google.protobuf.Timestamp"
};
function resolveComWixpressMetasiteApiV1MetaSiteReadApiUrl(opts) {
  var domainToMappings = {
    "www._base_domain_": [
      {
        srcPath: "/_api/msm-server",
        destPath: ""
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get",
        destPath: "/v1/meta-site/get"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/is-site-name-free",
        destPath: "/v1/meta-site/is-site-name-free"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/has-sites",
        destPath: "/v1/meta-site/has-sites"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get-url",
        destPath: "/v1/meta-site/get-url"
      }
    ],
    "manage._base_domain_": [
      {
        srcPath: "/_api/msm-server",
        destPath: ""
      },
      {
        srcPath: "/_api/msm/v1/meta-site/is-site-name-free",
        destPath: "/v1/meta-site/is-site-name-free"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/has-sites",
        destPath: "/v1/meta-site/has-sites"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get-url",
        destPath: "/v1/meta-site/get-url"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get",
        destPath: "/v1/meta-site/get"
      }
    ],
    "editor-flow.wixapps.net": [
      {
        srcPath: "/_api/msm/v1/meta-site/get",
        destPath: "/v1/meta-site/get"
      }
    ],
    "editor._base_domain_": [
      {
        srcPath: "/_api/msm/v1/meta-site/get",
        destPath: "/v1/meta-site/get"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get-url",
        destPath: "/v1/meta-site/get-url"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/has-sites",
        destPath: "/v1/meta-site/has-sites"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/is-site-name-free",
        destPath: "/v1/meta-site/is-site-name-free"
      }
    ],
    "blocks._base_domain_": [
      {
        srcPath: "/_api/msm/v1/meta-site/get",
        destPath: "/v1/meta-site/get"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get-url",
        destPath: "/v1/meta-site/get-url"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/has-sites",
        destPath: "/v1/meta-site/has-sites"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/is-site-name-free",
        destPath: "/v1/meta-site/is-site-name-free"
      }
    ],
    "create.editorx": [
      {
        srcPath: "/_api/msm/v1/meta-site/get",
        destPath: "/v1/meta-site/get"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/get-url",
        destPath: "/v1/meta-site/get-url"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/has-sites",
        destPath: "/v1/meta-site/has-sites"
      },
      {
        srcPath: "/_api/msm/v1/meta-site/is-site-name-free",
        destPath: "/v1/meta-site/is-site-name-free"
      }
    ],
    "www.wixgateway.com": [
      {
        srcPath: "/v1/meta-site/session-token",
        destPath: "/v1/meta-site/session-token"
      }
    ],
    "api._api_base_domain_": [
      {
        srcPath: "/msm-server",
        destPath: ""
      }
    ],
    "dev._base_domain_": [
      {
        srcPath: "/_api/msm/v1/meta-site/has-sites",
        destPath: "/v1/meta-site/has-sites"
      }
    ],
    "online.codux.com": [
      {
        srcPath: "/_api/msm/v1/meta-site",
        destPath: "/v1/meta-site"
      }
    ]
  };
  return resolveUrl(Object.assign(opts, { domainToMappings }));
}
function resolveComWixpressMetasiteApiV1MetaSiteLifecycleUrl(opts) {
  var domainToMappings = {
    "www._base_domain_": [
      {
        srcPath: "/_api/msm-writer",
        destPath: ""
      },
      {
        srcPath: "/_api/msm",
        destPath: ""
      }
    ],
    "manage._base_domain_": [
      {
        srcPath: "/_api/msm-writer",
        destPath: ""
      },
      {
        srcPath: "/_api/msm",
        destPath: ""
      }
    ],
    "www.wixapis.com": [
      {
        srcPath: "/msm",
        destPath: ""
      }
    ],
    "editor._base_domain_": [
      {
        srcPath: "/_api/msm-writer",
        destPath: ""
      }
    ],
    "blocks._base_domain_": [
      {
        srcPath: "/_api/msm-writer",
        destPath: ""
      }
    ],
    "create.editorx": [
      {
        srcPath: "/_api/msm-writer",
        destPath: ""
      }
    ]
  };
  return resolveUrl(Object.assign(opts, { domainToMappings }));
}
function createMetaSiteFromTemplate(payload) {
  var _a = serializer(_createMetaSiteFromTemplateRequest, {}), toReq = _a.toJSON, fromReq = _a.fromJSON;
  var fromRes = serializer(_createMetaSiteFromTemplateResponse, { _metaSite }).fromJSON;
  function __createMetaSiteFromTemplate(_a2) {
    var host = _a2.host;
    var serializedData = toReq(payload);
    var metadata = {
      entityFqdn: "wix.metasite.api.v1.meta_site",
      method: "POST",
      methodFqn: "com.wixpress.metasite.api.v1.MetaSiteLifecycle.CreateMetaSiteFromTemplate",
      url: resolveComWixpressMetasiteApiV1MetaSiteLifecycleUrl({
        protoPath: "/v1/meta-site/create-from-template",
        data: serializedData,
        host
      }),
      data: serializedData,
      transformResponse: fromRes
    };
    return metadata;
  }
  __createMetaSiteFromTemplate.fromReq = fromReq;
  __createMetaSiteFromTemplate.__isAmbassador = true;
  return __createMetaSiteFromTemplate;
}
function getUrl(payload) {
  var _a = serializer(_getUrlRequest, {}), toReq = _a.toJSON, fromReq = _a.fromJSON;
  var fromRes = serializer(_getUrlResponse, {}).fromJSON;
  function __getUrl(_a2) {
    var host = _a2.host;
    var serializedData = toReq(payload);
    var metadata = {
      entityFqdn: "wix.metasite.api.v1.meta_site",
      method: "GET",
      methodFqn: "com.wixpress.metasite.api.v1.MetaSiteReadApi.GetUrl",
      url: resolveComWixpressMetasiteApiV1MetaSiteReadApiUrl({
        protoPath: "/v1/meta-site/get-url",
        data: serializedData,
        host
      }),
      params: toURLSearchParams(serializedData),
      transformResponse: fromRes,
      fallback: [
        {
          method: "GET",
          url: resolveComWixpressMetasiteApiV1MetaSiteReadApiUrl({
            protoPath: "/v1/meta-site/get-url",
            data: serializedData,
            host
          }),
          params: toURLSearchParams(serializedData)
        }
      ]
    };
    return metadata;
  }
  __getUrl.fromReq = fromReq;
  __getUrl.__isAmbassador = true;
  return __getUrl;
}

// ../../node_modules/@wix/ambassador-metasite-api-v1-meta-site/build/es/types.impl.js
init_esm_shims();
var State;
(function(State2) {
  State2["UNKNOWN"] = "UNKNOWN";
  State2["ENABLED"] = "ENABLED";
  State2["DISABLED"] = "DISABLED";
  State2["TEMPLATE"] = "TEMPLATE";
  State2["PENDING"] = "PENDING";
})(State || (State = {}));
var Namespace;
(function(Namespace2) {
  Namespace2["UNKNOWN_NAMESPACE"] = "UNKNOWN_NAMESPACE";
  Namespace2["WIX"] = "WIX";
  Namespace2["SHOUT_OUT"] = "SHOUT_OUT";
  Namespace2["ALBUMS"] = "ALBUMS";
  Namespace2["WIX_STORES_TEST_DRIVE"] = "WIX_STORES_TEST_DRIVE";
  Namespace2["HOTELS"] = "HOTELS";
  Namespace2["CLUBS"] = "CLUBS";
  Namespace2["ONBOARDING_DRAFT"] = "ONBOARDING_DRAFT";
  Namespace2["DEV_SITE"] = "DEV_SITE";
  Namespace2["LOGOS"] = "LOGOS";
  Namespace2["VIDEO_MAKER"] = "VIDEO_MAKER";
  Namespace2["PARTNER_DASHBOARD"] = "PARTNER_DASHBOARD";
  Namespace2["DEV_CENTER_COMPANY"] = "DEV_CENTER_COMPANY";
  Namespace2["HTML_DRAFT"] = "HTML_DRAFT";
  Namespace2["SITELESS_BUSINESS"] = "SITELESS_BUSINESS";
  Namespace2["CREATOR_ECONOMY"] = "CREATOR_ECONOMY";
  Namespace2["DASHBOARD_FIRST"] = "DASHBOARD_FIRST";
  Namespace2["ANYWHERE"] = "ANYWHERE";
  Namespace2["HEADLESS"] = "HEADLESS";
  Namespace2["ACCOUNT_MASTER_CMS"] = "ACCOUNT_MASTER_CMS";
  Namespace2["RISE"] = "RISE";
  Namespace2["BRANDED_FIRST"] = "BRANDED_FIRST";
  Namespace2["NOWNIA"] = "NOWNIA";
  Namespace2["UGC_TEMPLATE"] = "UGC_TEMPLATE";
  Namespace2["CODUX"] = "CODUX";
  Namespace2["MEDIA_DESIGN_CREATOR"] = "MEDIA_DESIGN_CREATOR";
  Namespace2["SHARED_BLOG_ENTERPRISE"] = "SHARED_BLOG_ENTERPRISE";
  Namespace2["STANDALONE_FORMS"] = "STANDALONE_FORMS";
  Namespace2["STANDALONE_EVENTS"] = "STANDALONE_EVENTS";
  Namespace2["MIMIR"] = "MIMIR";
  Namespace2["TWINS"] = "TWINS";
  Namespace2["NANO"] = "NANO";
})(Namespace || (Namespace = {}));
var DocumentType;
(function(DocumentType2) {
  DocumentType2["UNKNOWN_DOCUMENT_TYPE"] = "UNKNOWN_DOCUMENT_TYPE";
  DocumentType2["UGC"] = "UGC";
  DocumentType2["TEMPLATE"] = "TEMPLATE";
  DocumentType2["WIX_SITE"] = "WIX_SITE";
})(DocumentType || (DocumentType = {}));
var PremiumFeature;
(function(PremiumFeature2) {
  PremiumFeature2["UNKNOWN_PREMIUM_FEATURE"] = "UNKNOWN_PREMIUM_FEATURE";
  PremiumFeature2["SHOW_WIX_WHILE_LOADING"] = "SHOW_WIX_WHILE_LOADING";
  PremiumFeature2["ADS_FREE"] = "ADS_FREE";
  PremiumFeature2["HAS_ECOMMERCE"] = "HAS_ECOMMERCE";
  PremiumFeature2["HAS_DOMAIN"] = "HAS_DOMAIN";
  PremiumFeature2["ALWAYS_SHOW_FOOTER"] = "ALWAYS_SHOW_FOOTER";
  PremiumFeature2["NO_ADS_IN_SOCIAL_SITES"] = "NO_ADS_IN_SOCIAL_SITES";
})(PremiumFeature || (PremiumFeature = {}));
var ApplicationType;
(function(ApplicationType2) {
  ApplicationType2["UNKNOWN_APPLICATION_TYPE"] = "UNKNOWN_APPLICATION_TYPE";
  ApplicationType2["FLASH"] = "FLASH";
  ApplicationType2["FLASH_FACEBOOK"] = "FLASH_FACEBOOK";
  ApplicationType2["FLASH_WIDGET"] = "FLASH_WIDGET";
  ApplicationType2["FLASH_BANNER"] = "FLASH_BANNER";
  ApplicationType2["HTML_MOBILE"] = "HTML_MOBILE";
  ApplicationType2["HTML_WEB"] = "HTML_WEB";
  ApplicationType2["HTML_FACEBOOK"] = "HTML_FACEBOOK";
  ApplicationType2["BLOG"] = "BLOG";
  ApplicationType2["WIX_LISTS"] = "WIX_LISTS";
  ApplicationType2["STATIC_MOUNT"] = "STATIC_MOUNT";
  ApplicationType2["STATIC_MOUNT_REDIRECT"] = "STATIC_MOUNT_REDIRECT";
  ApplicationType2["AMP_BLOG"] = "AMP_BLOG";
  ApplicationType2["COMMUNITIES_AMP_BLOG"] = "COMMUNITIES_AMP_BLOG";
  ApplicationType2["HTML_ANYWHERE"] = "HTML_ANYWHERE";
  ApplicationType2["HOPP"] = "HOPP";
})(ApplicationType || (ApplicationType = {}));
var Compatibility;
(function(Compatibility2) {
  Compatibility2["UNKNOWN_COMPATIBILITY"] = "UNKNOWN_COMPATIBILITY";
  Compatibility2["WEB"] = "WEB";
  Compatibility2["MOBILE"] = "MOBILE";
  Compatibility2["ALL"] = "ALL";
})(Compatibility || (Compatibility = {}));
var ApiState;
(function(ApiState2) {
  ApiState2["UNKNOWN_STATE"] = "UNKNOWN_STATE";
  ApiState2["TEMPLATE"] = "TEMPLATE";
  ApiState2["STUB"] = "STUB";
  ApiState2["INITIALIZED"] = "INITIALIZED";
})(ApiState || (ApiState = {}));
var Kind;
(function(Kind2) {
  Kind2["OPEN"] = "OPEN";
  Kind2["CLOSED"] = "CLOSED";
  Kind2["APPLY_FOR_MEMBERSHIP"] = "APPLY_FOR_MEMBERSHIP";
})(Kind || (Kind = {}));
var FormFace;
(function(FormFace2) {
  FormFace2["REGISTER"] = "REGISTER";
  FormFace2["LOGIN"] = "LOGIN";
})(FormFace || (FormFace = {}));
var CollectionExposure;
(function(CollectionExposure2) {
  CollectionExposure2["PUBLIC"] = "PUBLIC";
  CollectionExposure2["WIX_INTERNAL"] = "WIX_INTERNAL";
})(CollectionExposure || (CollectionExposure = {}));
var HttpsPolicy;
(function(HttpsPolicy2) {
  HttpsPolicy2["UNKNOWN_HTTPS_POLICY"] = "UNKNOWN_HTTPS_POLICY";
  HttpsPolicy2["DISALLOW"] = "DISALLOW";
  HttpsPolicy2["ALLOW"] = "ALLOW";
  HttpsPolicy2["ENFORCE"] = "ENFORCE";
})(HttpsPolicy || (HttpsPolicy = {}));
var Flag;
(function(Flag2) {
  Flag2["UNKNOWN_FLAG"] = "UNKNOWN_FLAG";
  Flag2["URL_MIGRATED"] = "URL_MIGRATED";
  Flag2["USE_HTTPS"] = "USE_HTTPS";
  Flag2["EDITOR_X"] = "EDITOR_X";
  Flag2["BLOCKED"] = "BLOCKED";
  Flag2["DONT_USE_DEFAULT_ROUTING"] = "DONT_USE_DEFAULT_ROUTING";
  Flag2["STUDIO"] = "STUDIO";
  Flag2["CRITICAL_ASSET"] = "CRITICAL_ASSET";
  Flag2["ODEDITOR"] = "ODEDITOR";
  Flag2["PICASSO"] = "PICASSO";
})(Flag || (Flag = {}));
var PendingReason;
(function(PendingReason2) {
  PendingReason2["UNKNOWN"] = "UNKNOWN";
  PendingReason2["PREMIUM"] = "PREMIUM";
  PendingReason2["APP_MARKET"] = "APP_MARKET";
})(PendingReason || (PendingReason = {}));
var ResolutionMethod;
(function(ResolutionMethod2) {
  ResolutionMethod2["QUERY_PARAM"] = "QUERY_PARAM";
  ResolutionMethod2["SUBDOMAIN"] = "SUBDOMAIN";
  ResolutionMethod2["SUBDIRECTORY"] = "SUBDIRECTORY";
})(ResolutionMethod || (ResolutionMethod = {}));
var DeleteStatus;
(function(DeleteStatus2) {
  DeleteStatus2["UNKNOWN"] = "UNKNOWN";
  DeleteStatus2["TRASH"] = "TRASH";
  DeleteStatus2["DELETED"] = "DELETED";
  DeleteStatus2["PENDING_PURGE"] = "PENDING_PURGE";
  DeleteStatus2["PURGED_EXTERNALLY"] = "PURGED_EXTERNALLY";
})(DeleteStatus || (DeleteStatus = {}));
var Context;
(function(Context2) {
  Context2["UNKNOWN"] = "UNKNOWN";
  Context2["CREATE"] = "CREATE";
  Context2["UPDATE"] = "UPDATE";
  Context2["HARD_DELETE"] = "HARD_DELETE";
  Context2["ARCHIVE"] = "ARCHIVE";
  Context2["RESTORE"] = "RESTORE";
  Context2["UPDATE_DELETED"] = "UPDATE_DELETED";
  Context2["PURGED_EXTERNALLY"] = "PURGED_EXTERNALLY";
})(Context || (Context = {}));
var SiteCreatedContext;
(function(SiteCreatedContext2) {
  SiteCreatedContext2["OTHER"] = "OTHER";
  SiteCreatedContext2["FROM_TEMPLATE"] = "FROM_TEMPLATE";
  SiteCreatedContext2["DUPLICATE_BY_SITE_TRANSFER"] = "DUPLICATE_BY_SITE_TRANSFER";
  SiteCreatedContext2["DUPLICATE"] = "DUPLICATE";
  SiteCreatedContext2["OLD_SITE_TRANSFER"] = "OLD_SITE_TRANSFER";
  SiteCreatedContext2["FLASH"] = "FLASH";
})(SiteCreatedContext || (SiteCreatedContext = {}));
var AssetState;
(function(AssetState2) {
  AssetState2["UNKNOWN"] = "UNKNOWN";
  AssetState2["ENABLED"] = "ENABLED";
  AssetState2["DISABLED"] = "DISABLED";
  AssetState2["PENDING"] = "PENDING";
  AssetState2["DEMO"] = "DEMO";
})(AssetState || (AssetState = {}));
var PlacementType;
(function(PlacementType2) {
  PlacementType2["BEFORE"] = "BEFORE";
  PlacementType2["AFTER"] = "AFTER";
  PlacementType2["REPLACE"] = "REPLACE";
})(PlacementType || (PlacementType = {}));
var DayOfWeek;
(function(DayOfWeek2) {
  DayOfWeek2["MONDAY"] = "MONDAY";
  DayOfWeek2["TUESDAY"] = "TUESDAY";
  DayOfWeek2["WEDNESDAY"] = "WEDNESDAY";
  DayOfWeek2["THURSDAY"] = "THURSDAY";
  DayOfWeek2["FRIDAY"] = "FRIDAY";
  DayOfWeek2["SATURDAY"] = "SATURDAY";
  DayOfWeek2["SUNDAY"] = "SUNDAY";
})(DayOfWeek || (DayOfWeek = {}));
var V4ResolutionMethod;
(function(V4ResolutionMethod2) {
  V4ResolutionMethod2["QUERY_PARAM"] = "QUERY_PARAM";
  V4ResolutionMethod2["SUBDOMAIN"] = "SUBDOMAIN";
  V4ResolutionMethod2["SUBDIRECTORY"] = "SUBDIRECTORY";
})(V4ResolutionMethod || (V4ResolutionMethod = {}));
var WebhookIdentityType;
(function(WebhookIdentityType2) {
  WebhookIdentityType2["UNKNOWN"] = "UNKNOWN";
  WebhookIdentityType2["ANONYMOUS_VISITOR"] = "ANONYMOUS_VISITOR";
  WebhookIdentityType2["MEMBER"] = "MEMBER";
  WebhookIdentityType2["WIX_USER"] = "WIX_USER";
  WebhookIdentityType2["APP"] = "APP";
})(WebhookIdentityType || (WebhookIdentityType = {}));
var Cause;
(function(Cause2) {
  Cause2["EMPTY"] = "EMPTY";
  Cause2["NAMESPACE_LIMIT_REACHED"] = "NAMESPACE_LIMIT_REACHED";
  Cause2["AUTOMATION_USER_LIMIT_REACHED"] = "AUTOMATION_USER_LIMIT_REACHED";
  Cause2["WIX_EMPLOYEE_LIMIT_REACHED"] = "WIX_EMPLOYEE_LIMIT_REACHED";
})(Cause || (Cause = {}));
var UrlFilter;
(function(UrlFilter2) {
  UrlFilter2["NO_URL_TYPE_FILTER"] = "NO_URL_TYPE_FILTER";
  UrlFilter2["FREE"] = "FREE";
})(UrlFilter || (UrlFilter = {}));
var MigrationResult;
(function(MigrationResult2) {
  MigrationResult2["UNKNOWN_FORMAT"] = "UNKNOWN_FORMAT";
  MigrationResult2["OK"] = "OK";
  MigrationResult2["SKIP"] = "SKIP";
})(MigrationResult || (MigrationResult = {}));
var MigrateEmbeddedServiceToTpaResponseMigrationResult;
(function(MigrateEmbeddedServiceToTpaResponseMigrationResult2) {
  MigrateEmbeddedServiceToTpaResponseMigrationResult2["UNKNOWN_FORMAT"] = "UNKNOWN_FORMAT";
  MigrateEmbeddedServiceToTpaResponseMigrationResult2["OK"] = "OK";
  MigrateEmbeddedServiceToTpaResponseMigrationResult2["SKIP"] = "SKIP";
})(MigrateEmbeddedServiceToTpaResponseMigrationResult || (MigrateEmbeddedServiceToTpaResponseMigrationResult = {}));

// ../../node_modules/@wix/ambassador-document-management-document-store-v1-transaction/build/es/http.impl.js
init_esm_shims();
var _distributeRequest = {};
var _distributeResponse = {};
function resolveComWixpressEditorApiDocumentStoreServiceUrl(opts) {
  var domainToMappings = {
    "api._api_base_domain_": [
      {
        srcPath: "/editor-document-store",
        destPath: ""
      }
    ],
    "www._base_domain_": [
      {
        srcPath: "/_api/editor-document-store",
        destPath: ""
      }
    ],
    "editor._base_domain_": [
      {
        srcPath: "/_api/editor-document-store",
        destPath: ""
      }
    ],
    "blocks._base_domain_": [
      {
        srcPath: "/_api/editor-document-store",
        destPath: ""
      }
    ],
    "create.editorx": [
      {
        srcPath: "/_api/editor-document-store",
        destPath: ""
      }
    ]
  };
  return resolveUrl(Object.assign(opts, { domainToMappings }));
}
function distribute(payload) {
  var _a = serializer(_distributeRequest, {}), toReq = _a.toJSON, fromReq = _a.fromJSON;
  var fromRes = serializer(_distributeResponse, {}).fromJSON;
  function __distribute(_a2) {
    var host = _a2.host;
    var serializedData = toReq(payload);
    var metadata = {
      entityFqdn: "wix.document_management.document_store.v1.transaction",
      method: "POST",
      methodFqn: "com.wixpress.editor.api.DocumentStoreService.Distribute",
      migrationOptions: {
        optInTransformResponse: true
      },
      url: resolveComWixpressEditorApiDocumentStoreServiceUrl({
        protoPath: "/v1/distribute",
        data: serializedData,
        host
      }),
      data: serializedData,
      transformResponse: fromRes
    };
    return metadata;
  }
  __distribute.fromReq = fromReq;
  __distribute.__isAmbassador = true;
  return __distribute;
}

// ../metasite-manager-client/src/schemas.ts
init_esm_shims();
var clientSpecMapSchema = external_exports.record(
  external_exports.object({
    appDefinitionId: external_exports.string().optional(),
    permissions: external_exports.object({ revoked: external_exports.boolean() }).optional(),
    instanceId: external_exports.string().optional()
  })
);
var createMetaSiteFromTemplateSchema = external_exports.object({
  metaSite: external_exports.object({
    metaSiteId: external_exports.string()
  })
});
var getMetaSiteUrlSchema = external_exports.object({
  url: external_exports.string(),
  published: external_exports.boolean()
});

// ../metasite-manager-client/src/metasite-manager-client.ts
var MetasiteManagerClient = class {
  constructor(httpClient) {
    this.httpClient = httpClient;
  }
  getClientSpecMap = async (metaSiteId) => {
    try {
      const { data } = await this.httpClient.request({
        url: `/_api/msm/v1/meta-site/editor-client-spec-map/${metaSiteId}`
      });
      return clientSpecMapSchema.parse(data);
    } catch (e) {
      throw new CliError({
        code: CliErrorCode.FailedToGetClientSpecMap(),
        cause: e
      });
    }
  };
  createMetaSiteFromTemplate = async (request) => {
    try {
      const { data } = await this.httpClient.request(
        createMetaSiteFromTemplate(request)
      );
      return createMetaSiteFromTemplateSchema.parse(data).metaSite;
    } catch (e) {
      throw new CliError({
        code: CliErrorCode.FailedToCreateMetaSiteFromTemplate(),
        cause: e
      });
    }
  };
  getSiteInstalledApps = async (metaSiteId) => {
    try {
      const clientSpecMap = await this.getClientSpecMap(metaSiteId);
      return Object.values(clientSpecMap).filter((spec) => spec.appDefinitionId && !spec.permissions?.revoked).map((spec) => spec.appDefinitionId);
    } catch (e) {
      throw new CliError({
        code: CliErrorCode.FailedToGetSiteInstalledApps(),
        cause: e
      });
    }
  };
  getInstalledAppInstanceId = async ({
    metaSiteId,
    appId
  }) => {
    try {
      const clientSpecMap = await this.getClientSpecMap(metaSiteId);
      const installedApp = Object.values(clientSpecMap).find(
        (spec) => spec.appDefinitionId === appId
      );
      return installedApp?.instanceId ?? "";
    } catch (e) {
      throw new CliError({
        code: CliErrorCode.FailedToGetSiteInstalledAppInstanceId(),
        cause: e
      });
    }
  };
  getMetaSiteUrl = async () => {
    try {
      const { data } = await this.httpClient.request(
        getUrl({ urlFilter: UrlFilter.NO_URL_TYPE_FILTER })
      );
      return getMetaSiteUrlSchema.parse(data);
    } catch (e) {
      throw new CliError({
        code: CliErrorCode.FailedToGetUrl(),
        cause: e
      });
    }
  };
  distributeClientUpdate = async (appId) => {
    try {
      await this.httpClient.request(
        distribute({
          payloads: [
            {
              id: appId,
              messageType: "MANIFEST_UPDATE",
              data: {
                appId
              }
            }
          ],
          branchId: "00000000-0000-0000-0000-000000000000"
        })
      );
    } catch (e) {
      throw new CliError({
        code: CliErrorCode.FailedToDistributeClientManifestUpdate(),
        cause: e
      });
    }
  };
};

// ../metasite-manager-client/src/useMetasiteManagerClient.ts
function useMetasiteManagerClient({
  siteId
} = {}) {
  const httpClient = useHttpClient({ type: "general", siteId });
  const client = (0, import_react15.useMemo)(
    () => new MetasiteManagerClient(httpClient),
    [httpClient]
  );
  return usePanoramaMethodLogger(client);
}

// ../cli-astro-commands/src/components/DevCommand/app-flow/useERCManifestUpdateNotifier.ts
function useERCManifestUpdateNotifier() {
  const { siteId } = useSite();
  const { devCenterWatcher } = useDevEnvironment();
  const metasiteManagerClient = useMetasiteManagerClient({ siteId });
  const {
    model: {
      config: { appId }
    }
  } = useProjectModel();
  const writeToDebugLog = useDebugLog();
  const errorReporter = useErrorReporter();
  (0, import_react16.useEffect)(() => {
    const unsubscribe = devCenterWatcher.onManifestUpdated(
      ({ updatedComponents }) => {
        const editorComponentsUpdated = updatedComponents.filter(
          (comp) => comp.compType === ComponentType.PANEL || comp.compType === ComponentType.EDITOR_REACT_COMPONENT
        );
        if (editorComponentsUpdated.length > 0) {
          metasiteManagerClient.distributeClientUpdate(appId).catch((error) => {
            errorReporter.reportError(error);
            writeToDebugLog(error);
          });
        }
      }
    );
    return () => {
      unsubscribe();
    };
  }, [
    devCenterWatcher,
    metasiteManagerClient,
    appId,
    writeToDebugLog,
    errorReporter
  ]);
}

// ../cli-astro-commands/src/components/DevCommand/app-flow/screen-state.ts
init_esm_shims();
var import_variant7 = __toESM(require_lib(), 1);
var ScreenState = (0, import_variant7.variant)({
  Home: {},
  SiteSelector: {}
});

// ../cli-astro-commands/src/components/DevCommand/app-flow/SiteSelectorScreen.tsx
init_esm_shims();
var import_react18 = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/CloseScreen/index.tsx
init_esm_shims();

// ../cli-astro-commands/src/components/CloseScreen/CloseScreen.tsx
init_esm_shims();
var import_react17 = __toESM(require_react(), 1);
var CloseScreen = ({ onClose }) => {
  const bi = useWixCliAppBi();
  use_input_default((_, key) => {
    if (key.escape) {
      bi.cliAction({
        question: "Press to close",
        questionKey: "general.close_screen",
        action: "close",
        key: "esc"
      });
      onClose();
    }
  });
  return /* @__PURE__ */ import_react17.default.createElement(Text, { skin: "secondary" }, "Press ", /* @__PURE__ */ import_react17.default.createElement(Key, { value: "esc", skin: "secondary" }), " to close");
};

// ../cli-astro-commands/src/components/DevCommand/app-flow/SiteSelectorScreen.tsx
var SiteSelectorScreen = ({ onScreenChange }) => {
  const { model } = useProjectModel();
  const biLogger = useBiLogger();
  const { siteId, setSiteId } = useSite();
  const { reportError } = useErrorReporter();
  const [error, setError] = (0, import_react18.useState)(null);
  const handleError = (0, import_react18.useCallback)(
    (error2) => {
      reportError(error2);
      setError(error2);
    },
    [reportError, setError]
  );
  const handleSubmit = (0, import_react18.useCallback)(
    async (site) => {
      try {
        await setSiteId(site.id);
        onScreenChange(ScreenState.Home());
      } catch (e) {
        handleError(e);
      }
    },
    [setSiteId, onScreenChange, handleError]
  );
  return /* @__PURE__ */ import_react18.default.createElement(Box_default, { flexDirection: "column", gap: 1 }, /* @__PURE__ */ import_react18.default.createElement(Box_default, { gap: 1 }, /* @__PURE__ */ import_react18.default.createElement(PointerIcon, null), /* @__PURE__ */ import_react18.default.createElement(Text, null, "Current development site: ", siteId)), /* @__PURE__ */ import_react18.default.createElement(Box_default, { flexDirection: "column" }, /* @__PURE__ */ import_react18.default.createElement(
    SiteSelector,
    {
      appId: model.config.appId,
      biLogger,
      onSubmit: handleSubmit,
      onError: handleError
    }
  ), error ? /* @__PURE__ */ import_react18.default.createElement(
    ErrorViewer,
    {
      error,
      systemErrorOverride: /* @__PURE__ */ import_react18.default.createElement(Alert, { type: "error" }, "Failed to change development site. Try selecting a different site or run the dev command again.")
    }
  ) : null), /* @__PURE__ */ import_react18.default.createElement(CloseScreen, { onClose: () => onScreenChange(ScreenState.Home()) }));
};

// ../cli-astro-commands/src/components/DevCommand/app-flow/HomeScreen.tsx
init_esm_shims();
var import_react20 = __toESM(require_react(), 1);
import process3 from "node:process";

// ../cli-astro-commands/src/components/DevCommand/app-flow/SwitchSite.tsx
init_esm_shims();
var import_react19 = __toESM(require_react(), 1);
var SwitchSite = ({ onScreenChange }) => {
  const bi = useWixCliAppBi();
  use_input_default(async (input) => {
    if (input === "c") {
      bi.cliAction({
        question: "Switch development site",
        questionKey: "dev_command.dev_site.change",
        action: "change_site",
        key: input
      });
      onScreenChange(ScreenState.SiteSelector());
    }
  });
  return /* @__PURE__ */ import_react19.default.createElement(Text, { skin: "secondary" }, "(Press ", /* @__PURE__ */ import_react19.default.createElement(Key, { value: "C", skin: "secondary" }), " to switch)");
};

// ../cli-astro-commands/src/components/DevCommand/app-flow/HomeScreen.tsx
var HomeScreen = ({ onScreenChange }) => {
  const { siteId, canChangeSite } = useSite();
  const { versionOverrideId } = useDevEnvironment();
  const links = useAppLinks({
    cmd: "dev",
    siteId,
    overrideId: versionOverrideId
  });
  return /* @__PURE__ */ import_react20.default.createElement(import_react20.default.Fragment, null, /* @__PURE__ */ import_react20.default.createElement(Box_default, { gap: 1 }, /* @__PURE__ */ import_react20.default.createElement(Text, null, "Current development site:"), /* @__PURE__ */ import_react20.default.createElement(Text, { bold: true, skin: "info" }, siteId), process3.stdin.isTTY && canChangeSite && /* @__PURE__ */ import_react20.default.createElement(SwitchSite, { onScreenChange })), /* @__PURE__ */ import_react20.default.createElement(PlatformLinksSection, { links }));
};

// ../cli-astro-commands/src/components/DevCommand/app-flow/AppDev.tsx
var AppDev = () => {
  const [screen, setScreen] = (0, import_react21.useState)(ScreenState.Home());
  const {
    model: { pluginsInterface }
  } = useProjectModel();
  const { devCenterWatcher } = useDevEnvironment();
  useAppDeploymentTopologyUpdater();
  useERCManifestUpdateNotifier();
  (0, import_react21.useEffect)(() => {
    return devCenterWatcher.onManifestUpdated(({ versionOverrideId }) => {
      void pluginsInterface?.eventsEmitter.emit("dc-updated", {
        overrideId: versionOverrideId
      });
    });
  }, [devCenterWatcher, pluginsInterface]);
  return (0, import_variant8.match)(screen, {
    Home: () => /* @__PURE__ */ import_react21.default.createElement(HomeScreen, { onScreenChange: setScreen }),
    SiteSelector: () => /* @__PURE__ */ import_react21.default.createElement(SiteSelectorScreen, { onScreenChange: setScreen })
  });
};

// ../cli-astro-commands/src/components/DevCommand/AppDevFlow.tsx
var AppDevFlow = (props) => {
  return /* @__PURE__ */ import_react22.default.createElement(SiteProvider, null, (siteId) => /* @__PURE__ */ import_react22.default.createElement(DevEnvironmentProvider, { ...props, siteId }, /* @__PURE__ */ import_react22.default.createElement(AppDev, null)));
};

// ../cli-astro-commands/src/components/DevCommand/hooks/useDevServerLocalUrl.ts
init_esm_shims();
var import_react23 = __toESM(require_react(), 1);

// ../cli-astro-commands/src/components/DevCommand/utils/startDevServer.ts
init_esm_shims();

// ../cli-astro-commands/src/components/DevCommand/utils/runAstroDev.ts
init_esm_shims();
var EVENT_LINE = "line";
var EVENT_SERVER_ADDRESS = "server_address";
function runAstroDev({
  packageManager,
  projectFolder,
  port,
  allowedHosts
}) {
  const binaryArgs = ["astro", "dev"];
  if (port) {
    binaryArgs.push("--port", `${port}`);
  }
  if (allowedHosts) {
    binaryArgs.push("--allowed-hosts", allowedHosts);
  }
  const { file, args } = packageManager.getRunBinaryCmd(binaryArgs);
  const result = execa(file, args, {
    cwd: projectFolder,
    stdio: "pipe",
    env: { FORCE_COLOR: "1" },
    cleanup: true,
    windowsHide: false
  });
  let stdoutBuffer = "";
  result.stdout?.on("data", (chunk) => {
    stdoutBuffer += chunk.toString();
    const lines = stdoutBuffer.split("\n");
    stdoutBuffer = lines.pop() ?? "";
    for (const line of lines) {
      result.stdout?.emit(EVENT_LINE, line);
      if (line.includes("http://")) {
        const match6 = /(http:\/\/[^\s]+)/.exec(line);
        if (match6?.[1]) {
          const address = stripAnsi(match6[1]).trim();
          result.stdout?.emit(EVENT_SERVER_ADDRESS, address);
        }
      }
    }
  });
  let stdErrBuffer = "";
  result.stderr?.on("data", (chunk) => {
    stdErrBuffer += chunk.toString();
    const lines = stdErrBuffer.split("\n");
    stdErrBuffer = lines.pop() ?? "";
    for (const line of lines) {
      result.stderr?.emit(EVENT_LINE, line);
    }
  });
  return result;
}

// ../cli-astro-commands/src/components/DevCommand/utils/startDevServer.ts
async function startDevServer({
  model: { pluginsInterface, projectFolder },
  port,
  allowedHosts,
  logger,
  onUnexpectedExit,
  base
}) {
  if (await isStandaloneExtensionsProject(projectFolder)) {
    return startStandaloneExtensionsDevServer({
      projectFolder,
      port,
      allowedHosts,
      base,
      pluginsInterface
    });
  }
  return startAstroDevServer({
    projectFolder,
    port,
    allowedHosts,
    logger,
    onUnexpectedExit
  });
}
async function startStandaloneExtensionsDevServer({
  projectFolder,
  port,
  allowedHosts,
  base,
  pluginsInterface
}) {
  const { dev } = await importExtensionsPackageNodeApi(projectFolder);
  const onRuntimeError = pluginsInterface != null && pluginsInterface.eventsEmitter.listenerCount("runtime-error") > 0 ? (event) => {
    void pluginsInterface.eventsEmitter.emit("runtime-error", event);
  } : void 0;
  const server = await dev({
    rootDir: projectFolder,
    base,
    onRuntimeError,
    port,
    allowedHosts: allowedHosts?.split(",").map((host) => host.trim())
  });
  return {
    ready: Promise.resolve(`http://localhost:${server.address.port}`),
    stop: () => server.stop().catch(() => {
    })
  };
}
async function startAstroDevServer({
  projectFolder,
  port,
  allowedHosts,
  logger,
  onUnexpectedExit
}) {
  const repoType = await getRepoType(projectFolder);
  const packageManager = await createPackageManager(repoType);
  const astro = runAstroDev({
    packageManager,
    projectFolder,
    port,
    allowedHosts
  });
  const ready = new Promise((resolve) => {
    astro.stdout?.once(EVENT_SERVER_ADDRESS, (address) => {
      resolve(address);
    });
  });
  astro.stdout?.on(EVENT_LINE, (line) => logger.log(line));
  astro.stderr?.on(EVENT_LINE, (line) => logger.error(line));
  let stoppedByUs = false;
  let cleanedUp = false;
  const cleanupAstroProcess = () => {
    if (cleanedUp) {
      return;
    }
    cleanedUp = true;
    astro.stdout?.destroy();
    astro.stderr?.destroy();
    astro.stdin?.end();
  };
  void astro.on("exit", () => {
    cleanupAstroProcess();
    if (!stoppedByUs) {
      onUnexpectedExit();
    }
  });
  return {
    ready,
    stop: async () => {
      stoppedByUs = true;
      if (astro.pid) {
        try {
          astro.kill("SIGTERM");
        } catch {
        }
      }
      cleanupAstroProcess();
    }
  };
}

// ../cli-astro-commands/src/components/DevCommand/hooks/useDevServerLocalUrl.ts
function useDevServerLocalUrl({
  model,
  port,
  allowedHosts,
  logger,
  base
}) {
  const exit = useExit();
  const devServer = (0, import_react23.useRef)(null);
  const isUnmounted = (0, import_react23.useRef)(false);
  const { status } = useAsync(async () => {
    const server = await startDevServer({
      model,
      port,
      allowedHosts,
      logger,
      base,
      onUnexpectedExit: () => exit(
        new CliError({
          cause: void 0,
          code: CliErrorCode.DevCommandFailed()
        })
      )
    });
    if (isUnmounted.current) {
      void server.stop();
    } else {
      devServer.current = server;
    }
    return server.ready;
  }, []);
  (0, import_react23.useEffect)(() => {
    return () => {
      isUnmounted.current = true;
      const server = devServer.current;
      if (server) {
        devServer.current = null;
        void server.stop();
      }
    };
  }, []);
  return status;
}

// ../cli-astro-commands/src/components/DevCommand/DevCommand.tsx
var DevCommand = ({
  port,
  allowedHosts,
  serverExposedUrl,
  base
}) => {
  const { model } = useProjectModel();
  const logger = useAstroLogger({ isPrefixed: false });
  const fetchDevMetadataAction = useFetchDevMetadata({ base });
  const devServerStatus = useDevServerLocalUrl({
    model,
    base,
    port,
    allowedHosts,
    logger
  });
  const serverLocalUrl = (0, import_variant9.isType)(devServerStatus, "Success") ? devServerStatus.result : void 0;
  (0, import_react24.useEffect)(() => {
    if (serverLocalUrl) {
      void fetchDevMetadataAction.execute(serverLocalUrl);
    }
  }, [serverLocalUrl]);
  if ((0, import_variant9.isType)(devServerStatus, "Error")) {
    return null;
  }
  if (!serverLocalUrl) {
    return /* @__PURE__ */ import_react24.default.createElement(Spinner, { text: "Starting dev environment..." });
  }
  return (0, import_variant9.match)(fetchDevMetadataAction.status, {
    NotRequested: () => /* @__PURE__ */ import_react24.default.createElement(Spinner, { text: "Starting dev environment..." }),
    Error: () => null,
    Loading: () => /* @__PURE__ */ import_react24.default.createElement(Spinner, { text: "Checking dev environment settings..." }),
    Success: ({ result: { devMetadata, serverLocalUrl: serverLocalUrl2 } }) => /* @__PURE__ */ import_react24.default.createElement(
      DevFlowScreen,
      {
        model,
        base,
        serverLocalUrl: serverLocalUrl2,
        serverExposedUrl,
        devMetadata
      }
    )
  });
};
var DevFlowScreen = ({ model, base, serverLocalUrl, serverExposedUrl, devMetadata }) => /* @__PURE__ */ import_react24.default.createElement(DevScreenBorder, null, model.config.projectType === "Site" ? /* @__PURE__ */ import_react24.default.createElement(
  SiteDevFlow,
  {
    serverLocalUrl,
    serverExposedUrl,
    devMetadata
  }
) : /* @__PURE__ */ import_react24.default.createElement(
  AppDevFlow,
  {
    base,
    serverLocalUrl,
    serverExposedUrl,
    devMetadata
  }
));
export {
  DevCommand
};
//# sourceMappingURL=DevCommand-RIR6LZPG.js.map