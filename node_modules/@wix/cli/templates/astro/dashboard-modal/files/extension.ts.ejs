---
to: <%- route -%>.extension.ts
---
import { extensions } from '<%- builderPackage -%>'
import config from './<%- route -%>.config.ts';

export default extensions.dashboardModal({
  id: '<%- config.id -%>',
  title: config.title,
  width: config.width,
  height: config.height,
  component: '<%- config.component -%>',
});
