export declare const validateSingleEvent: (userEvent: any, allFieldsAreMandatory?: boolean) => void;
export declare class EventsFilter<T = any> {
    private getAllEvents;
    private src;
    private evid;
    private validate;
    constructor(getAllEvents: () => any[], src: number, evid: number, validate?: (event: any) => void);
    last(): Partial<T>;
    all(): Partial<T>[];
}
