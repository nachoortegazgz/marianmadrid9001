import * as _wix_sdk_types from '@wix/sdk-types';
import { QuerySpec, SearchSpec, Query, Search, NonNullablePaths } from '@wix/sdk-types';

/** A contact represents a person in a site's contact list. It stores profile details such as name, email addresses, phone numbers, and addresses, and can optionally be linked to a site member. */
interface Contact {
    /**
     * Contact ID.
     * @format GUID
     * @immutable
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the Contact is updated.
     * To prevent conflicting changes,
     * the current revision must be passed when updating the Contact.
     *
     * Ignored when creating a Contact.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the contact was created.
     * @readonly
     * @immutable
     */
    _createdDate?: Date | null;
    /**
     * Date and time the contact was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Contact external ID. Used to identify contacts from external systems.
     * This field can only be set once and can't be updated after creation.
     * @maxLength 100
     */
    externalId?: string | null;
    /** Contact's name information including first and last name. */
    name?: ContactName;
    /**
     * The main email of the contact with subscription status and deliverability information.
     * This email is used for contact matching and deduplication logic.
     */
    email?: ContactEmail;
    /**
     * Additional emails of the contact.
     * These emails do not affect subscription status or contact matching/deduplication.
     * The main email field must be set before additional emails can be added.
     * @maxSize 10
     */
    additionalEmails?: AdditionalEmail[];
    /**
     * The main phone of the contact with subscription status and deliverability information.
     * This phone is used for contact matching and deduplication logic.
     * At least one of name, email, or phone is required for contact creation.
     */
    phone?: ContactPhone;
    /**
     * Additional phones of the contact.
     * These phones do not affect subscription status or contact matching/deduplication.
     * The main phone field must be set before additional phones can be added.
     * @maxSize 10
     */
    additionalPhones?: AdditionalPhone[];
    /**
     * Contact's physical addresses.
     * Each address can be tagged (HOME, WORK, BILLING, SHIPPING, OTHER) and one can be set as default.
     * If no address is marked as default, the first address automatically becomes the default.
     * @maxSize 50
     */
    addresses?: ContactAddress[];
    /** Contact's company information including company name and job title. */
    company?: ContactCompany;
    /**
     * Locale in
     * [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * Typically, this is a lowercase 2-letter language code,
     * followed by a hyphen, followed by an uppercase 2-letter country code.
     * For example, `en-US` for U.S. English, and `de-DE` for Germany German.
     * @format LANGUAGE_TAG
     */
    locale?: string | null;
    /**
     * Birth date in `YYYY-MM-DD` format. For example, `2020-03-15`.
     * @format LOCAL_DATE
     */
    birthdate?: string | null;
    /**
     * IANA time zone identifier for the contact's location.
     * Valid values are IANA time zone IDs, such as 'Europe/Rome' or 'America/New_York'.
     * @maxLength 50
     */
    timeZone?: string | null;
    /**
     * Site contributor user ID assigned to manage communication with this contact.
     * Must be a valid GUID referencing an existing site contributor.
     * Only one assignee can be set per contact.
     * @format GUID
     */
    assigneeId?: string | null;
    /**
     * List of business location IDs associated with this contact.
     * Used for multi-location businesses to organize contacts by physical business locations.
     * Each ID must be a valid GUID referencing an existing business location.
     * @format GUID
     * @maxSize 50
     */
    locationIds?: string[];
    /**
     * Contact's profile image.
     * If the contact is also a site member, clients should prefer to view `member_info.profile_info.photo`
     * when it exists, as it reflects the member's current profile photo.
     */
    image?: string;
    /**
     * Details about the origin of the contact.
     * This field is immutable and set automatically when the contact is created.
     * @immutable
     */
    source?: ContactSource;
    /**
     * Details about the contact's most recent activity on the site.
     * Includes activity date and template ID. This field is read-only and updated automatically.
     * @readonly
     */
    lastActivity?: ContactActivity;
    /**
     * IDs of the segments the contact belongs to.
     * Segments are used for organizing and targeting groups of contacts.
     * @maxSize 60
     * @readonly
     * @format GUID
     */
    segmentIds?: string[];
    /**
     * Contact's site membership information including member ID, status, and profile details.
     * This field is read-only and populated automatically when the contact is also a site member.
     *
     * Updates are eventually consistent: changes to the underlying member, identity, or profile are reflected after the corresponding events are processed.
     * @readonly
     */
    memberInfo?: MemberInfo;
    /**
     * Contact's display name. The field is used for sorting only and is not returned.
     * @internal
     * @maxLength 1000
     * @readonly
     */
    displayName?: string | null;
    /** Tags assigned to the contact for organization and filtering. */
    tags?: PrivateTags;
    /**
     * Extended fields for storing custom contact data beyond the standard fields.
     * Allows adding custom properties specific to business needs.
     * To define a custom extended field, use the [Schema Service API](https://dev.wix.com/docs/api-reference/business-management/data-extension-schema/about-the-data-extension-schema-api) with the contacts FQDN: `wix.contacts.*.contact`.
     */
    extendedFields?: ExtendedFields;
}
interface ContactName {
    /**
     * Contact's first name.
     * @maxLength 1000
     */
    first?: string | null;
    /**
     * Contact's last name.
     * @maxLength 1000
     */
    last?: string | null;
    /**
     * Contact's full name. Automatically generated from first and last names.
     * @internal
     * @maxLength 1000
     * @readonly
     */
    full?: string | null;
}
interface ContactEmail {
    /**
     * Main email address for the contact. Used for communication and contact matching.
     *
     * Stored in normalized form: leading and trailing whitespace is trimmed and the address is lowercased.
     * @format EMAIL
     * @maxLength 320
     */
    email?: string;
    /**
     * Indicates the recipient's opt-in or opt-out status for marketing emails.
     * This field is read-only and managed by the subscription system.
     *
     * Updates are eventually consistent, reflecting marketing consent changes after they're processed.
     * @readonly
     */
    subscriptionStatus?: ContactSubscription;
    /**
     * Indicates the last reported delivery status of emails sent to this address.
     * This field is read-only and updated automatically by the email delivery system.
     *
     * - `NOT_SET`: No delivery status reported. Default value.
     * - `VALID`: Emails are being successfully delivered to this address.
     * - `BOUNCED`: The last email to this address bounced or was rejected by the recipient's email server.
     * - `SPAM_COMPLAINT`: Recipient registered a spam complaint with their email provider.
     * - `INACTIVE`: Multiple emails have been delivered without any engagement from the recipient.
     * @readonly
     */
    deliverabilityStatus?: EmailDeliverabilityStatusWithLiterals;
}
interface ContactSubscription {
    /**
     * Indicates the recipient's opt-in or opt-out status for marketing communications.
     *
     * - `NO_SUBSCRIPTION_STATUS`: No status exists. Used when a phone is not a valid E164 number.
     * - `NOT_SET`: No subscription status specified. Default value.
     * - `PENDING`: Subscription confirmation was requested, but recipient hasn't confirmed yet.
     * - `SUBSCRIBED`: Recipient has explicitly opted in to receive marketing communications.
     * - `UNSUBSCRIBED`: Recipient has opted out of marketing communications.
     */
    status?: SubscriptionStatusWithLiterals;
    /** Date and time when the subscription status was last updated. */
    _updatedDate?: Date | null;
}
declare enum SubscriptionStatus {
    NO_SUBSCRIPTION_STATUS = "NO_SUBSCRIPTION_STATUS",
    NOT_SET = "NOT_SET",
    PENDING = "PENDING",
    SUBSCRIBED = "SUBSCRIBED",
    UNSUBSCRIBED = "UNSUBSCRIBED"
}
/** @enumType */
type SubscriptionStatusWithLiterals = SubscriptionStatus | 'NO_SUBSCRIPTION_STATUS' | 'NOT_SET' | 'PENDING' | 'SUBSCRIBED' | 'UNSUBSCRIBED';
declare enum EmailDeliverabilityStatus {
    NOT_SET = "NOT_SET",
    VALID = "VALID",
    BOUNCED = "BOUNCED",
    SPAM_COMPLAINT = "SPAM_COMPLAINT",
    INACTIVE = "INACTIVE"
}
/** @enumType */
type EmailDeliverabilityStatusWithLiterals = EmailDeliverabilityStatus | 'NOT_SET' | 'VALID' | 'BOUNCED' | 'SPAM_COMPLAINT' | 'INACTIVE';
interface AdditionalEmail {
    /**
     * Additional email address for the contact.
     * Does not affect subscription status or contact matching logic.
     * @format EMAIL
     * @maxLength 320
     */
    email?: string;
}
interface ContactPhone {
    /**
     * Phone type classification. Helps categorize the phone number's purpose.
     *
     * Default: `OTHER`
     */
    tag?: PhoneTagWithLiterals;
    /**
     * [ISO-3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code for the phone number.
     * This field is private and maintained for backwards compatibility.
     * @internal
     * @format COUNTRY
     */
    countryCode?: string | null;
    /**
     * Primary phone number for the contact. Used for communication and contact matching.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string;
    /**
     * Phone number in [ITU E.164 international](https://www.itu.int/rec/T-REC-E.164/) format (e.g., +12345550123).
     * This field is automatically generated from the phone field if it represents a valid E.164 number.
     * @readonly
     * @maxLength 50
     */
    e164?: string | null;
    /**
     * Indicates the recipient's opt-in or opt-out status for SMS/text message marketing.
     * This field is read-only and managed by the subscription system.
     *
     * Updates are eventually consistent, reflecting marketing consent changes after they're processed.
     * @readonly
     */
    subscriptionStatus?: ContactSubscription;
    /**
     * Indicates the delivery status for SMS messages sent to this phone number.
     * This field is read-only and updated automatically by the SMS delivery system.
     * - `NO_PHONE_DELIVERABILITY_STATUS`: No status exists. Used when the phone is not a valid E164 number.
     * - `NOT_SET`: No delivery status reported. Default when the phone is a valid E164 number.
     * - `VALID`: Last message was sent and delivered successfully.
     * - `INVALID`: The number is invalid (landline, number disconnected, etc.).
     * @readonly
     */
    deliverabilityStatus?: PhoneDeliverabilityStatusWithLiterals;
}
declare enum PhoneTag {
    OTHER = "OTHER",
    MAIN = "MAIN",
    HOME = "HOME",
    MOBILE = "MOBILE",
    WORK = "WORK",
    FAX = "FAX"
}
/** @enumType */
type PhoneTagWithLiterals = PhoneTag | 'OTHER' | 'MAIN' | 'HOME' | 'MOBILE' | 'WORK' | 'FAX';
declare enum PhoneDeliverabilityStatus {
    NO_PHONE_DELIVERABILITY_STATUS = "NO_PHONE_DELIVERABILITY_STATUS",
    NOT_SET = "NOT_SET",
    VALID = "VALID",
    INVALID = "INVALID"
}
/** @enumType */
type PhoneDeliverabilityStatusWithLiterals = PhoneDeliverabilityStatus | 'NO_PHONE_DELIVERABILITY_STATUS' | 'NOT_SET' | 'VALID' | 'INVALID';
interface AdditionalPhone {
    /**
     * Phone type classification for the additional phone number.
     *
     * Default: `OTHER`
     */
    tag?: PhoneTagWithLiterals;
    /**
     * [ISO-3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code for the additional phone number.
     * This field is private and maintained for backwards compatibility.
     * @internal
     * @format COUNTRY
     */
    countryCode?: string | null;
    /**
     * Additional phone number for the contact.
     * Does not affect subscription status or contact matching logic.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string;
    /**
     * Additional phone number in [ITU E.164 international](https://www.itu.int/rec/T-REC-E.164/) format (e.g., +12345550123).
     * This field is automatically generated from the phone field if it represents a valid E.164 number.
     * @readonly
     * @maxLength 50
     */
    e164?: string | null;
}
interface ContactAddress {
    /**
     * Address type classification. Helps categorize the address purpose.
     *
     * Default: `OTHER`
     */
    tag?: AddressTagWithLiterals;
    /**
     * Physical address information including street, city, country, and postal code.
     * Contains structured address data following common address formatting standards.
     */
    address?: Address;
    /**
     * Whether this address is the contact's default address.
     * Only one address can be designated as default per contact.
     * If no address is explicitly set as default, the first address automatically becomes the default.
     */
    defaultAddress?: boolean | null;
    /**
     * Address ID.
     * @format GUID
     */
    _id?: string | null;
    /** Address recipient information for delivery purposes. */
    recipient?: Recipient;
}
declare enum AddressTag {
    OTHER = "OTHER",
    HOME = "HOME",
    WORK = "WORK",
    BILLING = "BILLING",
    SHIPPING = "SHIPPING"
}
/** @enumType */
type AddressTagWithLiterals = AddressTag | 'OTHER' | 'HOME' | 'WORK' | 'BILLING' | 'SHIPPING';
/** Physical address, such as a billing or shipping address. */
interface Address extends AddressStreetOneOf {
    /** Street address object, with number and name in separate fields. */
    streetAddress?: StreetAddress;
    /**
     * Main address line, usually street and number, as free text.
     * @maxLength 1000
     */
    addressLine1?: string | null;
    /**
     * 2-letter country code in an
     * [ISO-3166 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) format.
     * @format COUNTRY
     */
    country?: string | null;
    /**
     * Subdivision shorthand. Usually, a short code (2 or 3 letters) that represents a state, region, prefecture, or province. For example, `NY`.
     * @format SUBDIVISION
     */
    subdivision?: string | null;
    /**
     * City name.
     * @maxLength 300
     */
    city?: string | null;
    /**
     * Postal or zip code.
     * @maxLength 100
     */
    postalCode?: string | null;
    /**
     * Free text providing more detailed address information,
     * such as apartment, suite, or floor.
     * @maxLength 1000
     */
    addressLine2?: string | null;
    /**
     * Human-readable address string.
     * If not provided, the value is generated from the available address data.
     * @maxLength 2000
     */
    formatted?: string | null;
    /**
     * Instructions to help someone find the address, such as landmarks or where the entrance is.
     * @maxLength 200
     */
    hint?: string | null;
    /** Coordinates of the physical address. */
    location?: AddressLocation;
    /**
     * Full country name.
     * @maxLength 100
     */
    countryFullname?: string | null;
    /**
     * Hierarchical list of subdivisions, where the first array item is the highest level of hierarchy, and so on.
     * @maxSize 6
     */
    subdivisions?: Subdivision[];
    /**
     * Subdivision full name.
     * @maxLength 100
     */
    subdivisionFullname?: string | null;
}
/** @oneof */
interface AddressStreetOneOf {
    /** Street address object, with number and name in separate fields. */
    streetAddress?: StreetAddress;
    /**
     * Main address line, usually street and number, as free text.
     * @maxLength 1000
     */
    addressLine?: string | null;
}
interface StreetAddress {
    /**
     * Street number.
     * @maxLength 100
     */
    number?: string;
    /**
     * Street name.
     * @maxLength 300
     */
    name?: string;
    /**
     * Apartment number.
     * @maxLength 50
     */
    apt?: string;
    /**
     * Optional formatted version of the street address line.
     * @maxLength 2000
     */
    formattedAddressLine?: string | null;
}
interface AddressLocation {
    /**
     * Address latitude.
     * @min -90
     * @max 90
     */
    latitude?: number | null;
    /**
     * Address longitude.
     * @min -180
     * @max 180
     */
    longitude?: number | null;
}
interface Subdivision {
    /**
     * Subdivision short code.
     * @maxLength 20
     */
    code?: string;
    /**
     * Full subdivision name.
     * @maxLength 100
     */
    name?: string;
    /** Subdivision level. */
    type?: SubdivisionTypeWithLiterals;
    /**
     * Free text description of subdivision type.
     * Examples: `"state"`, `"province"`, `"prefecture"`.
     * @maxLength 100
     */
    typeInfo?: string | null;
    /**
     * Standard organization details, such as ISO.
     * @internal
     */
    standardDetails?: StandardDetails;
}
declare enum SubdivisionType {
    UNKNOWN_SUBDIVISION_TYPE = "UNKNOWN_SUBDIVISION_TYPE",
    /** State. */
    ADMINISTRATIVE_AREA_LEVEL_1 = "ADMINISTRATIVE_AREA_LEVEL_1",
    /** County. */
    ADMINISTRATIVE_AREA_LEVEL_2 = "ADMINISTRATIVE_AREA_LEVEL_2",
    /** City/town. */
    ADMINISTRATIVE_AREA_LEVEL_3 = "ADMINISTRATIVE_AREA_LEVEL_3",
    /** Neighborhood/quarter. */
    ADMINISTRATIVE_AREA_LEVEL_4 = "ADMINISTRATIVE_AREA_LEVEL_4",
    /** Street/block. */
    ADMINISTRATIVE_AREA_LEVEL_5 = "ADMINISTRATIVE_AREA_LEVEL_5",
    /** Equivalent to `ADMINISTRATIVE_AREA_LEVEL_0`. Indicates the national political entity, and is typically the highest order type returned by the Geocoder. */
    COUNTRY = "COUNTRY"
}
/** @enumType */
type SubdivisionTypeWithLiterals = SubdivisionType | 'UNKNOWN_SUBDIVISION_TYPE' | 'ADMINISTRATIVE_AREA_LEVEL_1' | 'ADMINISTRATIVE_AREA_LEVEL_2' | 'ADMINISTRATIVE_AREA_LEVEL_3' | 'ADMINISTRATIVE_AREA_LEVEL_4' | 'ADMINISTRATIVE_AREA_LEVEL_5' | 'COUNTRY';
/** Subdivision concordance values. */
interface StandardDetails {
    /**
     * Subdivision code according to [ISO 3166-2](https://en.wikipedia.org/wiki/ISO_3166-2). For example, `US-NY`, `GB-SCT`, `NO-30`.
     * @maxLength 20
     */
    iso31662?: string | null;
}
interface Recipient {
    /**
     * Recipient's first name.
     * @maxLength 1000
     */
    firstName?: string | null;
    /**
     * Recipient's last name.
     * @maxLength 1000
     */
    lastName?: string | null;
    /**
     * Recipient's company name.
     * @maxLength 1000
     */
    company?: string | null;
    /**
     * Recipient's phone number.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string | null;
}
interface ContactCompany {
    /**
     * Contact's company name.
     * @maxLength 1000
     */
    name?: string | null;
    /**
     * Contact's job title.
     * @maxLength 1000
     */
    jobTitle?: string | null;
}
interface ContactSource {
    /**
     * ID of the user who created this contact.
     * @readonly
     * @format GUID
     * @immutable
     */
    userId?: string | null;
    /**
     * ID of the application that created this contact.
     * @readonly
     * @format GUID
     * @immutable
     */
    appId?: string | null;
    /**
     * Additional information about the contact's origin or creation context.
     * @maxLength 1000
     * @immutable
     */
    additionalInfo?: string | null;
}
interface ContactActivity {
    /** Date and time when the contact's most recent activity occurred. */
    date?: Date | null;
    /**
     * ID of the activity template that defines the type of activity performed.
     * References a template in the contacts activity log system.
     * @format GUID
     */
    templateId?: string | null;
}
/**
 * Information about the site member linked to the contact.
 *
 * This object is read-only and populated automatically when the contact is linked to a member. It's eventually consistent with the member's data, so changes made to the member may take a short time to appear here.
 */
interface MemberInfo {
    /**
     * Member ID.
     * @format GUID
     */
    memberId?: string | null;
    /** Date and time the member signed up. */
    signupDate?: Date | null;
    /**
     * Email the member uses to log in.
     * @format EMAIL
     */
    email?: string | null;
    /** Whether the email is verified. */
    emailVerified?: boolean | null;
    /** Whether the member is required to verify their email before they can log in. */
    emailVerificationRequired?: boolean | null;
    /**
     * Member status.
     * - `PENDING`: Member created and is waiting for approval by site owner.
     * - `ACTIVE`: Member can log in to the site.
     * - `BLOCKED`: Member is blocked and cannot log in to the site.
     * - `OFFLINE`: Member is a [managed writer](https://support.wix.com/en/article/wix-blog-adding-managed-writers-to-your-blog) for the site blog and cannot log in to the site.
     */
    status?: MemberStatusWithLiterals;
    /** Information about the member's public profile. */
    profileInfo?: ProfileInfo;
    /** Information about the member's user account. */
    userInfo?: UserInfo;
    /** Information about the member's login sessions. */
    sessionInfo?: SessionInfo;
    /** Information about the groups the member belongs to. */
    groupInfo?: GroupInfo;
}
declare enum MemberStatus {
    PENDING = "PENDING",
    ACTIVE = "ACTIVE",
    BLOCKED = "BLOCKED",
    OFFLINE = "OFFLINE"
}
/** @enumType */
type MemberStatusWithLiterals = MemberStatus | 'PENDING' | 'ACTIVE' | 'BLOCKED' | 'OFFLINE';
interface ProfileInfo {
    /**
     * Member's nickname.
     * @maxLength 255
     */
    nickname?: string | null;
    /** Whether the member's profile is visible to other site members and visitors. */
    privacyStatus?: PrivacyStatusWithLiterals;
    /** Whether the member has been reported by another member. */
    reported?: boolean | null;
    /** Whether the member has been muted and can't post or comment on the site. */
    muted?: boolean | null;
    /** Member's profile photo. */
    photo?: string;
}
declare enum PrivacyStatus {
    PRIVATE = "PRIVATE",
    PUBLIC = "PUBLIC"
}
/** @enumType */
type PrivacyStatusWithLiterals = PrivacyStatus | 'PRIVATE' | 'PUBLIC';
interface UserInfo {
    /**
     * User ID.
     * @format GUID
     */
    userId?: string;
    /** Member's role on the site. */
    role?: RoleWithLiterals;
}
declare enum Role {
    OWNER = "OWNER",
    CONTRIBUTOR = "CONTRIBUTOR",
    MEMBER = "MEMBER"
}
/** @enumType */
type RoleWithLiterals = Role | 'OWNER' | 'CONTRIBUTOR' | 'MEMBER';
interface SessionInfo {
    /** Date and time the member last logged in to the site from a browser. */
    lastWebLogin?: Date | null;
    /** Date and time the member last logged in from one of the mobile apps. */
    lastMobileLogin?: Date | null;
    /**
     * Mobile apps the member has ever logged in to.
     * @maxSize 50
     * @maxLength 50
     */
    mobileAppNames?: string[];
}
interface GroupInfo {
    /**
     * IDs of the groups the member belongs to.
     * @format GUID
     * @maxSize 1000
     */
    groupIds?: string[];
}
/** Tags assigned to an entity that require an additional permission to access. */
interface PrivateTags {
    /** Tags that require an additional permission to access, normally not given to site members or visitors. */
    privateTags?: TagList;
}
interface TagList {
    /**
     * A list of tag IDs.
     * To create a tag, use the [Tags API](https://dev.wix.com/docs/api-reference/business-management/tags/introduction) with the contacts FQDN: `wix.contacts.*.contact`.
     * @maxSize 1000
     * @maxLength 5
     */
    tagIds?: string[];
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
/**
 * Message for reindexing search data to a given search schema. Support both upsert and delete flows as well as
 * performs context manipulation with adding tenant, provided in message to callscope.
 */
interface ReindexMessage extends ReindexMessageActionOneOf {
    upsert?: Upsert;
    delete?: ReindexMessageDelete;
    entityFqdn?: string;
    tenantId?: string;
    eventTime?: Date | null;
    entityEventSequence?: string | null;
    schema?: Schema;
}
/** @oneof */
interface ReindexMessageActionOneOf {
    upsert?: Upsert;
    delete?: ReindexMessageDelete;
}
interface Upsert {
    entityId?: string;
    entityAsJson?: string;
}
interface ReindexMessageDelete {
    entityId?: string;
}
interface Schema {
    label?: string;
    clusterName?: string;
}
/** Triggered when one or more contacts are merged into another contact. */
interface ContactMerged {
    /**
     * ID of the contact the source contacts were merged into.
     * @format GUID
     */
    targetContactId?: string;
    /**
     * IDs of contacts that were merged into the target contact.
     * @format GUID
     * @minSize 1
     * @maxSize 5
     */
    sourceContactIds?: string[];
    /** Updated target contact. */
    targetContact?: Contact;
}
/** Triggered when a contact's email subscription status changes. */
interface ContactEmailSubscriptionUpdated {
    /**
     * The updated contact ID.
     * @format GUID
     */
    contactId?: string;
    /** The revision of the contact. */
    revision?: string | null;
    /** The email of the contact with the updated subscription status. */
    email?: ContactEmail;
    /** Date and time the contact was updated with the email subscription status. */
    _updatedDate?: Date | null;
}
/** Triggered when a contact's SMS subscription status changes. */
interface ContactPhoneSubscriptionUpdated {
    /**
     * The updated contact ID.
     * @format GUID
     */
    contactId?: string;
    /** The revision of the contact. */
    revision?: string | null;
    /** The phone of the contact with the updated subscription status. */
    phone?: ContactPhone;
    /** Date and time the contact was updated with the SMS subscription status. */
    _updatedDate?: Date | null;
}
/** Triggered when a contact's image is uploaded. */
interface ContactImageUploaded {
    /**
     * The updated contact ID.
     * @format GUID
     */
    contactId?: string;
    /** The revision of the contact. */
    revision?: string | null;
    /** The contact's uploaded image. */
    image?: string;
    /** Date and time the contact's image was updated. */
    _updatedDate?: Date | null;
    /**
     * The upload ID.
     * @format GUID
     */
    uploadId?: string | null;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
interface BulkUpdateContactSubscriptionsRequest {
    /**
     * A filter for the contacts that the subscription statuses will be applied to.
     *
     * Example:
     * `{ "filter": { "name.last": "Smith" } }`
     */
    filter: Record<string, any> | null;
    /** Free text to match in searchable fields for the contacts that the subscription statuses will be applied to. */
    search?: SearchDetails;
    /** The subscription status to apply to the contact's email address. */
    emailSubscriptionStatus?: V5SubscriptionStatusWithLiterals;
    /** The subscription status to apply to the contact's phone. */
    phoneSubscriptionStatus?: V5SubscriptionStatusWithLiterals;
}
interface SearchDetails {
    /** How separate search terms in `expression` are combined. */
    mode?: ModeWithLiterals;
    /**
     * Search term or expression.
     * @maxLength 100
     */
    expression?: string | null;
    /**
     * Fields to search in. If empty, searches all searchable fields. Use dot notation to specify the JSON path.
     * @maxLength 200
     * @maxSize 20
     */
    fields?: string[];
    /** Whether to use fuzzy search, which allows typos using a managed proximity algorithm. */
    fuzzy?: boolean;
}
declare enum Mode {
    /** Any of the search terms must be present. */
    OR = "OR",
    /** All search terms must be present. */
    AND = "AND"
}
/** @enumType */
type ModeWithLiterals = Mode | 'OR' | 'AND';
declare enum V5SubscriptionStatus {
    /** No subscription status has been reported yet. */
    NOT_SET = "NOT_SET",
    /** Opted in to receive marketing messages. */
    SUBSCRIBED = "SUBSCRIBED",
    /** Opted out of receiving marketing messages. */
    UNSUBSCRIBED = "UNSUBSCRIBED"
}
/** @enumType */
type V5SubscriptionStatusWithLiterals = V5SubscriptionStatus | 'NOT_SET' | 'SUBSCRIBED' | 'UNSUBSCRIBED';
interface BulkUpdateContactSubscriptionsResponse {
    /**
     * Job ID. Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job) to retrieve the status of the asynchronous operation.
     * @format GUID
     */
    jobId?: string;
}
/** Message for reindexing an entity across all active schemas (for fqdn). Tenant needs to be provided in call context. */
interface ReindexEntityForFQDN extends ReindexEntityForFQDNActionOneOf {
    /** to upsert an entity */
    index?: Index;
    /** to remove an entity */
    delete?: Delete;
    /** to report completion of reindexing */
    reportBatchCompleted?: ReportBatchCompleted;
    /** to bulk upsert entities */
    bulkIndex?: BulkIndex;
    /** to bulk delete entities */
    bulkDelete?: BulkDelete;
    /**
     * entity fqdn
     * @maxLength 256
     */
    entityFqdn?: string;
    /** event time */
    eventTime?: Date | null;
}
/** @oneof */
interface ReindexEntityForFQDNActionOneOf {
    /** to upsert an entity */
    index?: Index;
    /** to remove an entity */
    delete?: Delete;
    /** to report completion of reindexing */
    reportBatchCompleted?: ReportBatchCompleted;
    /** to bulk upsert entities */
    bulkIndex?: BulkIndex;
    /** to bulk delete entities */
    bulkDelete?: BulkDelete;
}
/** Will create or update entity in search index */
interface Index {
    /**
     * entity id
     * @format GUID
     */
    entityId?: string;
    /** entity as json string */
    entityAsJson?: string;
    /** entity sequence for versioning */
    entityEventSequence?: string | null;
}
/** Will delete entity from search index */
interface Delete {
    /**
     * entity id
     * @format GUID
     */
    entityId?: string;
    /** entity sequence for versioning */
    entityEventSequence?: string | null;
}
/** Will report completion of reindexing */
interface ReportBatchCompleted {
    /** completion time */
    batchStart?: Date | null;
}
/** Will create or update entities in search index. Max 1000 entities per message. */
interface BulkIndex {
    items?: Index[];
}
/** Will delete entities from search index. Max 1000 entities per message. */
interface BulkDelete {
    items?: Delete[];
}
interface CreateContactRequest {
    /** Contact to create. */
    contact: Contact;
    /**
     * Whether to create the contact even if it duplicates an existing contact.
     * More about duplicate contacts can be found [here](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
     */
    allowDuplicates?: boolean;
}
interface CreateContactResponse {
    /** Created contact. */
    contact?: Contact;
    /**
     * The upload ID of the image upload process.
     * @maxLength 100
     */
    imageUploadId?: string | null;
}
interface DuplicateContactExists {
    /**
     * ID of an existing contact that matches a duplicate in the request.
     * @format GUID
     */
    matchingContactId?: string | null;
    /** Field or fields of an existing contact that match a duplicate in the request. */
    matchType?: MatchTypeWithLiterals;
}
declare enum MatchType {
    EMAIL = "EMAIL",
    PHONE = "PHONE",
    EXTERNAL_ID = "EXTERNAL_ID",
    EMAIL_AND_PHONE = "EMAIL_AND_PHONE"
}
/** @enumType */
type MatchTypeWithLiterals = MatchType | 'EMAIL' | 'PHONE' | 'EXTERNAL_ID' | 'EMAIL_AND_PHONE';
interface GetContactRequest {
    /**
     * ID of the contact to retrieve.
     * @format GUID
     */
    contactId: string;
    /**
     * Whether to read the contact from the database in a consistent manner.
     * @internal
     */
    consistent?: boolean | null;
}
interface GetContactResponse {
    /** Retrieved contact. */
    contact?: Contact;
}
interface UpdateContactRequest {
    /** Contact to update. May be partial. */
    contact: Contact;
    /**
     * Set of fields to update.
     *
     * Fields that aren't included in `fieldMask.paths` are ignored.
     * @internal
     */
    fieldMask?: string[];
    /**
     * Whether to update the contact even if it becomes a duplicate of another contact.
     * More about duplicate contacts can be found [here](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
     */
    allowDuplicates?: boolean;
}
interface UpdateContactResponse {
    /** Updated contact. */
    contact?: Contact;
    /**
     * The upload ID of the image upload process.
     * @maxLength 100
     */
    imageUploadId?: string | null;
}
interface UpsertContactRequest {
    /** Contact to upsert. */
    contact: Contact;
    /**
     * Set of fields to update.
     *
     * Fields that aren't included in `fieldMask.paths` are ignored.
     * @internal
     */
    fieldMask?: string[];
    /** Mode that determines how an existing contact is updated when a match is found. See the endpoint description for the supported modes. */
    upsertMode?: UpsertModeWithLiterals;
    /** Whether to update the contact even if it's a site member. */
    updateMember?: boolean | null;
}
declare enum UpsertMode {
    /** Replaces existing field values. Default value. */
    OVERWRITE = "OVERWRITE",
    /** Sets a non-array field only if it doesn't already have a value, and appends to array fields. */
    APPEND = "APPEND",
    /** Overwrites non-array fields and appends to array fields. */
    OVERWRITE_APPEND_ARRAYS = "OVERWRITE_APPEND_ARRAYS"
}
/** @enumType */
type UpsertModeWithLiterals = UpsertMode | 'OVERWRITE' | 'APPEND' | 'OVERWRITE_APPEND_ARRAYS';
interface UpsertContactResponse {
    /** Upserted contact. */
    contact?: Contact;
    /** The action performed on the contact. */
    action?: UpsertContactResponseActionWithLiterals;
    /**
     * The upload ID of the image upload process.
     * @maxLength 100
     */
    imageUploadId?: string | null;
}
declare enum UpsertContactResponseAction {
    UPDATED = "UPDATED",
    CREATED = "CREATED"
}
/** @enumType */
type UpsertContactResponseActionWithLiterals = UpsertContactResponseAction | 'UPDATED' | 'CREATED';
interface FailedMemberUpdate {
    /**
     * Contact ID.
     * @format GUID
     */
    contactId?: string | null;
}
interface UpdateContactTagsRequest {
    /**
     * ID of the contact whose tags to update.
     * @format GUID
     */
    contactId: string;
    /** Tags to assign. */
    assignTags?: PrivateTags;
    /** Tags to unassign. */
    unassignTags?: PrivateTags;
}
interface UpdateContactTagsResponse {
    /** Updated contact. */
    contact?: Contact;
}
interface MergeContactsRequest {
    /**
     * Target contact ID.
     * @format GUID
     */
    targetContactId: string;
    /**
     * Revision number of the target contact, which increments by 1 each time the contact is updated.
     * The current revision must be passed to prevent conflicting changes.
     */
    targetContactRevision: string | null;
    /** Contact to overwrite the target with, instead of merging in the source contacts' data. See the endpoint description for details. */
    targetContact?: Contact;
    /**
     * Set of fields to update.
     *
     * Fields that aren't included in `fieldMask.paths` are ignored.
     * @internal
     */
    fieldMask?: string[];
    /**
     * IDs of up to 5 contacts to merge into the target contact.
     * If merging more than one source contact,
     * the first source is given precedence, then the second, and so on.
     * @minSize 1
     * @maxSize 5
     * @format GUID
     */
    sourceContactIds?: string[];
    /**
     * Whether to return a preview of the merged contact instead of performing the actual merge.
     *
     * Default: `false`
     */
    preview?: boolean | null;
    /**
     * Whether to update the target contact even if it becomes a duplicate of another contact. Relevant only when `targetContact` is provided.
     * More about duplicate contacts can be found [here](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
     */
    allowDuplicates?: boolean;
}
interface MergeContactsResponse {
    /** Updated target contact. */
    contact?: Contact;
}
interface DeleteContactRequest {
    /**
     * ID of the contact to delete.
     * @format GUID
     */
    contactId: string;
    /**
     * Whether to delete the contact even if it's a site member. If `true`, the site member is also deleted.
     *
     * Default: `false`
     */
    deleteMember?: boolean | null;
}
interface DeleteContactResponse {
}
interface FindMatchingContactsRequest {
    /**
     * Contact external ID.
     * @maxLength 100
     */
    externalId?: string | null;
    /**
     * Email address.
     * @format EMAIL
     * @maxLength 320
     */
    email?: string | null;
    /**
     * Phone number.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string | null;
}
interface FindMatchingContactsResponse {
    /** Matching contacts, ordered by match strength. At most 100 contacts are returned. */
    contacts?: Contact[];
}
interface QueryContactsRequest {
    /** WQL expression. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
    /**
     * When `field_name` is a property of repeated field that is marked as `MATCH_ITEMS` and sort should be done by
     * a specific element from a collection, filter can/should be provided to ensure correct sort value is picked.
     *
     * If multiple filters are provided, they are combined with AND operator.
     *
     * Example:
     * Given we have document like {"id": "1", "nestedField": [{"price": 10, "region": "EU"}, {"price": 20, "region": "US"}]}
     * and `nestedField` is marked as `MATCH_ITEMS`, to ensure that sorting is done by correct region, filter should be
     * { fieldName: "nestedField.price", "select_items_by": [{"nestedField.region": "US"}] }
     * @internal
     * @maxSize 10
     */
    selectItemsBy?: Record<string, any>[] | null;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryContactsResponse {
    /** Retrieved contacts. */
    contacts?: Contact[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
/** Paging metadata for cursor-based pagination. */
interface CursorPagingMetadata {
    /** Number of items returned in the current page. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
    /**
     * Total number of items matching the filter.
     * Available only on the first page of Search results, not included in Query or List results.
     * If the Search results span multiple pages, the value of `total` can exceed the number of items returned on the first page.
     */
    total?: number | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface SearchContactsRequest {
    /** WQL search expression. */
    search?: CursorSearch;
    /**
     * Name Format for sorting and searching.
     * When sorting by `displayName`, the returned contacts are ordered by first and last names, by default.
     * Alternatively, when sorting by `displayName` with `BY_LAST_NAME`, the returned contacts are ordered by last and first names.
     * For contacts that do not have names, sorting is by email or phone.
     * When searching by `name.full`, the contacts returned are matched and ordered by first and last names.
     * Alternatively, when searching by `name.full` with `BY_LAST_NAME`, the contacts returned
     * are matched and ordered by last and first names.
     */
    nameFormat?: NameFormatWithLiterals;
    /**
     * Natural language query for semantic search. Mutually exclusive with cursor paging with a cursor.
     * @maxLength 1000
     */
    semanticQuery?: string | null;
}
interface CursorSearch extends CursorSearchPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
     */
    filter?: Record<string, any> | null;
    /**
     * List of sort objects.
     *
     * Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
     * @maxSize 10
     */
    sort?: Sorting[];
    /**
     * Aggregations, also known as faceted search. Lets you explore large amounts of data by displaying summaries about different partitions of the data, and narrow the navigation to a specific partition.
     * @maxSize 10
     */
    aggregations?: Aggregation[];
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
    /**
     * UTC offset or IANA time zone. Valid values are
     * ISO 8601 UTC offsets, such as +02:00 or -06:00,
     * and IANA time zone IDs, such as Europe/Rome.
     *
     * Affects the values returned by all filters and aggregations.
     * You can override this behavior in a specific filter by providing
     * timestamps that include a time zone. For example, `"2023-12-20T10:52:34.795Z"`.
     * @maxLength 50
     */
    timeZone?: string | null;
}
/** @oneof */
interface CursorSearchPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
}
interface Aggregation extends AggregationKindOneOf {
    /** Value aggregation. */
    value?: ValueAggregation;
    /** Range aggregation. */
    range?: RangeAggregation;
    /** Scalar aggregation. */
    scalar?: ScalarAggregation;
    /** Date histogram aggregation. */
    dateHistogram?: DateHistogramAggregation;
    /** Nested aggregation. */
    nested?: NestedAggregation;
    /**
     * User-defined name for the aggregation. Must be unique. Appears in the aggregation results.
     * @maxLength 100
     */
    name?: string | null;
    /** Type of aggregation. Must match the aggregation type provided in the `kind` field. */
    type?: AggregationTypeWithLiterals;
    /**
     * Field to aggregate by. Use dot notation to specify the JSON path.
     * @maxLength 200
     */
    fieldPath?: string;
}
/** @oneof */
interface AggregationKindOneOf {
    /** Value aggregation. */
    value?: ValueAggregation;
    /** Range aggregation. */
    range?: RangeAggregation;
    /** Scalar aggregation. */
    scalar?: ScalarAggregation;
    /** Date histogram aggregation. */
    dateHistogram?: DateHistogramAggregation;
    /** Nested aggregation. */
    nested?: NestedAggregation;
}
interface RangeBucket {
    /** Inclusive lower bound of the range. Required if `to` isn't given. */
    from?: number | null;
    /** Exclusive upper bound of the range. Required if `from` isn't given. */
    to?: number | null;
}
declare enum SortType {
    /** Sorts by number of matches. */
    COUNT = "COUNT",
    /** Sorts by the field's value, alphabetically. */
    VALUE = "VALUE"
}
/** @enumType */
type SortTypeWithLiterals = SortType | 'COUNT' | 'VALUE';
declare enum SortDirection {
    /** Sorts in descending order. */
    DESC = "DESC",
    /** Sorts in ascending order. */
    ASC = "ASC"
}
/** @enumType */
type SortDirectionWithLiterals = SortDirection | 'DESC' | 'ASC';
declare enum MissingValues {
    /** Missing values are excluded from the aggregation results. */
    EXCLUDE = "EXCLUDE",
    /** Missing values are included in the aggregation results. */
    INCLUDE = "INCLUDE"
}
/** @enumType */
type MissingValuesWithLiterals = MissingValues | 'EXCLUDE' | 'INCLUDE';
interface IncludeMissingValuesOptions {
    /**
     * Custom name for the bucket that holds missing values.
     *
     * Default: `"N/A"` for string fields, `"0"` for integer fields, `"false"` for boolean fields.
     * @maxLength 20
     */
    addToBucket?: string;
}
declare enum ScalarType {
    /** Count of distinct values. */
    COUNT_DISTINCT = "COUNT_DISTINCT",
    /** Minimum value. */
    MIN = "MIN",
    /** Maximum value. */
    MAX = "MAX",
    /**
     * Sum of values.
     * @internal
     */
    SUM = "SUM",
    /**
     * Average of values.
     * @internal
     */
    AVG = "AVG"
}
/** @enumType */
type ScalarTypeWithLiterals = ScalarType | 'COUNT_DISTINCT' | 'MIN' | 'MAX';
declare enum NestedAggregationType {
    /** An aggregation where result buckets are dynamically built, one per unique value. */
    VALUE = "VALUE",
    /** An aggregation where you can define a set of ranges, each representing a bucket. */
    RANGE = "RANGE",
    /** A single-value metric aggregation, such as min, max, sum, or avg. */
    SCALAR = "SCALAR",
    /** An aggregation where result buckets are dynamically built, one per time interval (hour, day, week, and so on). */
    DATE_HISTOGRAM = "DATE_HISTOGRAM"
}
/** @enumType */
type NestedAggregationTypeWithLiterals = NestedAggregationType | 'VALUE' | 'RANGE' | 'SCALAR' | 'DATE_HISTOGRAM';
interface ValueAggregation extends ValueAggregationOptionsOneOf {
    /** Options for including missing values. Used only when `missingValues` is `INCLUDE`. */
    includeOptions?: IncludeMissingValuesOptions;
    /** How to sort the aggregation results: by number of matches or by field value. */
    sortType?: SortTypeWithLiterals;
    /** Sort direction: ascending or descending. */
    sortDirection?: SortDirectionWithLiterals;
    /**
     * Number of aggregations to return.
     *
     * Default: `10`
     */
    limit?: number | null;
    /**
     * Whether to include missing values in the aggregation results.
     *
     * Default: `EXCLUDE`
     */
    missingValues?: MissingValuesWithLiterals;
}
/** @oneof */
interface ValueAggregationOptionsOneOf {
    /** Options for including missing values. Used only when `missingValues` is `INCLUDE`. */
    includeOptions?: IncludeMissingValuesOptions;
}
interface RangeAggregation {
    /**
     * Range buckets used for the aggregation. Each entity is placed in the first bucket whose range its value falls into.
     * @maxSize 50
     */
    buckets?: RangeBucket[];
}
interface ScalarAggregation {
    /** Operator for the scalar aggregation. */
    type?: ScalarTypeWithLiterals;
}
interface DateHistogramAggregation {
    /** Interval for the date histogram aggregation. */
    interval?: IntervalWithLiterals;
}
declare enum Interval {
    /** Yearly interval. */
    YEAR = "YEAR",
    /** Monthly interval. */
    MONTH = "MONTH",
    /** Weekly interval. */
    WEEK = "WEEK",
    /** Daily interval. */
    DAY = "DAY",
    /** Hourly interval. */
    HOUR = "HOUR",
    /** Minute interval. */
    MINUTE = "MINUTE",
    /** Second interval. */
    SECOND = "SECOND"
}
/** @enumType */
type IntervalWithLiterals = Interval | 'YEAR' | 'MONTH' | 'WEEK' | 'DAY' | 'HOUR' | 'MINUTE' | 'SECOND';
interface NestedAggregationItem extends NestedAggregationItemKindOneOf {
    /** Value aggregation. */
    value?: ValueAggregation;
    /** Range aggregation. */
    range?: RangeAggregation;
    /** Scalar aggregation. */
    scalar?: ScalarAggregation;
    /** Date histogram aggregation. */
    dateHistogram?: DateHistogramAggregation;
    /**
     * User-defined name for the aggregation. Must be unique. Appears in the aggregation results.
     * @maxLength 100
     */
    name?: string | null;
    /** Type of aggregation. Must match the aggregation type provided in the `kind` field. */
    type?: NestedAggregationTypeWithLiterals;
    /**
     * Field to aggregate by. Use dot notation to specify the JSON path.
     * @maxLength 200
     */
    fieldPath?: string;
}
/** @oneof */
interface NestedAggregationItemKindOneOf {
    /** Value aggregation. */
    value?: ValueAggregation;
    /** Range aggregation. */
    range?: RangeAggregation;
    /** Scalar aggregation. */
    scalar?: ScalarAggregation;
    /** Date histogram aggregation. */
    dateHistogram?: DateHistogramAggregation;
}
declare enum AggregationType {
    /** An aggregation where result buckets are dynamically built, one per unique value. */
    VALUE = "VALUE",
    /** An aggregation where you can define a set of ranges, each representing a bucket. */
    RANGE = "RANGE",
    /** A single-value metric aggregation, such as min, max, sum, or avg. */
    SCALAR = "SCALAR",
    /** An aggregation where result buckets are dynamically built, one per time interval (hour, day, week, and so on). */
    DATE_HISTOGRAM = "DATE_HISTOGRAM",
    /** Multi-level aggregation, where each next aggregation is nested within the previous one. */
    NESTED = "NESTED"
}
/** @enumType */
type AggregationTypeWithLiterals = AggregationType | 'VALUE' | 'RANGE' | 'SCALAR' | 'DATE_HISTOGRAM' | 'NESTED';
/** Nested aggregation expressed as a list of aggregations, where each next aggregation is nested within the previous one. */
interface NestedAggregation {
    /**
     * Flattened list of aggregations, where each next aggregation is nested within the previous one.
     * @minSize 2
     * @maxSize 3
     */
    nestedAggregations?: NestedAggregationItem[];
}
/** Name format types for sorting and searching. */
declare enum NameFormat {
    /** Names will be formatted starting with the first name followed by the last name. */
    BY_FIRST_NAME = "BY_FIRST_NAME",
    /** Names will be formatted starting with the last name followed by the first name. */
    BY_LAST_NAME = "BY_LAST_NAME"
}
/** @enumType */
type NameFormatWithLiterals = NameFormat | 'BY_FIRST_NAME' | 'BY_LAST_NAME';
interface SearchContactsResponse {
    /** Retrieved contacts. */
    contacts?: Contact[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
    /** Aggregation data. */
    aggregationData?: AggregationData;
}
interface AggregationData {
    /**
     * Results of the requested aggregations.
     * @maxSize 10000
     */
    results?: AggregationResults[];
}
interface ValueAggregationResult {
    /**
     * Value of the field.
     * @maxLength 100
     */
    value?: string;
    /** Count of entities with this value. */
    count?: number;
}
interface RangeAggregationResult {
    /** Inclusive lower bound of the range. */
    from?: number | null;
    /** Exclusive upper bound of the range. */
    to?: number | null;
    /** Count of entities in this range. */
    count?: number;
}
interface ValueResults {
    /**
     * Value aggregation results.
     * @maxSize 250
     */
    results?: ValueAggregationResult[];
}
interface RangeResults {
    /**
     * Range results, returned in the same order as requested.
     * @maxSize 50
     */
    results?: RangeAggregationResult[];
}
interface AggregationResultsScalarResult {
    /** Type of scalar aggregation. */
    type?: ScalarTypeWithLiterals;
    /** Value of the scalar aggregation. */
    value?: number;
}
interface ValueResult {
    /**
     * Value of the field.
     * @maxLength 1000
     */
    value?: string;
    /** Count of entities with this value. */
    count?: number | null;
}
interface RangeResult {
    /** Inclusive lower bound of the range. */
    from?: number | null;
    /** Exclusive upper bound of the range. */
    to?: number | null;
    /** Count of entities in this range. */
    count?: number | null;
}
interface ScalarResult {
    /** Value of the scalar aggregation. */
    value?: number;
}
interface NestedResultValue extends NestedResultValueResultOneOf {
    /** Value aggregation result. */
    value?: ValueResult;
    /** Range aggregation result. */
    range?: RangeResult;
    /** Scalar aggregation result. */
    scalar?: ScalarResult;
    /** Date histogram aggregation result. */
    dateHistogram?: ValueResult;
}
/** @oneof */
interface NestedResultValueResultOneOf {
    /** Value aggregation result. */
    value?: ValueResult;
    /** Range aggregation result. */
    range?: RangeResult;
    /** Scalar aggregation result. */
    scalar?: ScalarResult;
    /** Date histogram aggregation result. */
    dateHistogram?: ValueResult;
}
interface Results {
    /** Nested aggregation results, keyed by aggregation name. */
    results?: Record<string, NestedResultValue>;
}
interface DateHistogramResult {
    /**
     * Date in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
     * @maxLength 100
     */
    value?: string;
    /** Count of documents in the bucket. */
    count?: number;
}
interface DateHistogramResults {
    /**
     * Date histogram aggregation results.
     * @maxSize 200
     */
    results?: DateHistogramResult[];
}
/** Results of the `NESTED` aggregation type in a flattened form. Entries in the resulting array are keyed by the requested aggregation's `name`. */
interface NestedResults {
    /**
     * Nested aggregation results.
     * @maxSize 1000
     */
    results?: Results[];
}
interface AggregationResults extends AggregationResultsResultOneOf {
    /** Value aggregation results. */
    values?: ValueResults;
    /** Range aggregation results. */
    ranges?: RangeResults;
    /** Scalar aggregation results. */
    scalar?: AggregationResultsScalarResult;
    /** Date histogram aggregation results. */
    dateHistogram?: DateHistogramResults;
    /** Nested aggregation results. */
    nested?: NestedResults;
    /**
     * User-defined name of the aggregation, as provided in the search request.
     * @maxLength 100
     */
    name?: string;
    /** Type of aggregation. Matches the `kind` provided in the search request. */
    type?: AggregationTypeWithLiterals;
    /**
     * Field to aggregate by, as provided in the search request.
     * @maxLength 200
     */
    fieldPath?: string;
}
/** @oneof */
interface AggregationResultsResultOneOf {
    /** Value aggregation results. */
    values?: ValueResults;
    /** Range aggregation results. */
    ranges?: RangeResults;
    /** Scalar aggregation results. */
    scalar?: AggregationResultsScalarResult;
    /** Date histogram aggregation results. */
    dateHistogram?: DateHistogramResults;
    /** Nested aggregation results. */
    nested?: NestedResults;
}
interface CountContactsRequest {
    /**
     * Filter object.
     *
     * Example:
     * `{ "filter": { "name.last": "Smith" } }`
     */
    filter?: Record<string, any> | null;
    /**
     * Plain text search for an exact match.
     *
     * Supported search properties are:
     *
     * - `name.full`
     * - `email.email`
     * - `phone.phone`
     * - `member_info.email`
     * - `member_info.profile_info.nickname`
     * - `company.name`
     * - `company.job_title`
     *
     * Max: 100 characters
     * @maxLength 100
     */
    search?: string | null;
}
interface CountContactsResponse {
    /** Number of contacts that match the provided filter and search criteria. */
    count?: number;
}
interface GenerateContactImageUploadUrlRequest {
    /**
     * ID of the contact to generate an image upload URL for.
     * @format GUID
     */
    contactId: string;
    /**
     * MIME type. Must be one of: `image/png`, `image/jpeg`.
     *
     * Default: `image/png`
     * @maxLength 100
     */
    mimeType?: string | null;
}
interface GenerateContactImageUploadUrlResponse {
    /**
     * URL to upload the image to.
     * @format WEB_URL
     */
    uploadUrl?: string;
    /**
     * Upload ID. Pass this ID to [Get Contact Image Upload Status](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/get-contact-image-upload-status) to check the status of the upload.
     * @format GUID
     */
    imageUploadId?: string;
}
interface AddContactAdditionalEmailRequest {
    /**
     * ID of the contact to add an email to.
     * @format GUID
     */
    contactId: string;
    /** Email to add. */
    email: AdditionalEmail;
    /** Contact revision number. */
    revision: string | null;
}
interface AddContactAdditionalEmailResponse {
    /** Updated contact. */
    contact?: Contact;
}
interface AddContactAdditionalPhoneRequest {
    /**
     * ID of the contact to add a phone to.
     * @format GUID
     */
    contactId: string;
    /** Phone to add. */
    phone: AdditionalPhone;
    /** Contact revision number. */
    revision: string | null;
}
interface AddContactAdditionalPhoneResponse {
    /** Updated contact. */
    contact?: Contact;
}
interface AddContactAddressRequest {
    /**
     * ID of the contact to add an address to.
     * @format GUID
     */
    contactId: string;
    /** Address to add. */
    address: ContactAddress;
    /** Contact revision number. */
    revision: string | null;
}
interface AddContactAddressResponse {
    /** Updated contact. */
    contact?: Contact;
}
interface DeleteContactAddressRequest {
    /**
     * ID of the contact to delete the address from.
     * @format GUID
     */
    contactId: string;
    /**
     * ID of the address to delete.
     * @format GUID
     */
    addressId: string;
    /** Contact revision number. */
    revision: string | null;
}
interface DeleteContactAddressResponse {
    /** Updated contact. */
    contact?: Contact;
}
interface UpdateContactAddressRequest {
    /**
     * ID of the contact to update the address on.
     * @format GUID
     */
    contactId: string;
    /** Address to update. Must include `address.id`. */
    address: ContactAddress;
    /** Contact revision number. */
    revision: string | null;
}
interface UpdateContactAddressResponse {
    /** Updated contact. */
    contact?: Contact;
}
interface BulkCreateContactsRequest {
    /**
     * Contacts to create.
     * @minSize 1
     * @maxSize 100
     */
    contacts: Contact[];
    /** Whether to include the created contacts in the response. */
    returnEntity?: boolean;
    /** Whether to create the contact even if it duplicates an existing contact. */
    allowDuplicates?: boolean;
}
interface BulkCreateContactsResponse {
    /**
     * Results of the bulk create operation, including the created contacts and metadata.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkContactResult[];
    /** Metadata regarding the bulk create operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface ItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkContactResult {
    /** Metadata regarding the creation of this specific contact. */
    itemMetadata?: ItemMetadata;
    /** Returned only when `returnEntity` is `true` in the request. */
    item?: Contact;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface BulkUpdateContactsRequest {
    /**
     * Contacts to update.
     * @minSize 1
     * @maxSize 100
     */
    contacts: MaskedContact[];
    /** Whether to include the updated contacts in the response. */
    returnEntity?: boolean;
    /** Whether to update the contact even if it becomes a duplicate of another contact. */
    allowDuplicates?: boolean;
}
interface MaskedContact {
    /** Contact to update. May be partial. */
    contact?: Contact;
    /**
     * Explicit list of fields to update.
     * @internal
     */
    fieldMask?: string[];
}
interface BulkUpdateContactsResponse {
    /**
     * Results of the update for each contact.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateContactsResponseBulkContactResult[];
    /** Metadata regarding the bulk update operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateContactsResponseBulkContactResult {
    /** Metadata regarding the update of this specific contact. */
    itemMetadata?: ItemMetadata;
    /** Returned only when `returnEntity` is `true` in the request. */
    item?: Contact;
}
interface BulkUpdateContactTagsRequest {
    /**
     * IDs of the contacts to add or remove tags on.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    contactIds: string[];
    /** Whether to include the updated contacts in the response. */
    returnEntity?: boolean;
    /** Tags to assign. */
    assignTags?: PrivateTags;
    /** Tags to unassign. */
    unassignTags?: PrivateTags;
}
interface BulkUpdateContactTagsResponse {
    /**
     * Results of the tag update for each contact.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateContactTagsResult[];
    /** Summary of successes and failures. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateContactTagsResult {
    /** Metadata regarding the tag update for this specific contact. */
    itemMetadata?: ItemMetadata;
    /** Returned only when `returnEntity` is `true` in the request. */
    item?: Contact;
}
interface BulkUpdateContactTagsByFilterRequest {
    /** Filter object. See documentation [here](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#filters). */
    filter: Record<string, any> | null;
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
    /** Tags to assign to the matching contacts. */
    assignTags?: PrivateTags;
    /** Tags to unassign from the matching contacts. */
    unassignTags?: PrivateTags;
}
interface BulkUpdateContactTagsByFilterResponse {
    /**
     * Job ID. Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job) to retrieve the status of the asynchronous operation.
     * @format GUID
     */
    jobId?: string;
}
interface BulkUpsertContactsRequest {
    /**
     * Contacts to create or update.
     * @minSize 1
     * @maxSize 100
     */
    contacts: BulkUpsertContactsRequestMaskedContact[];
    /** Whether to include the upserted contacts in the response. */
    returnEntity?: boolean;
    /** Mode that determines how an existing contact is updated when a match is found. See the endpoint description for the supported modes. */
    upsertMode?: UpsertModeWithLiterals;
    /** Whether to update a contact even if it's a site member. */
    updateMember?: boolean | null;
}
interface BulkUpsertContactsRequestMaskedContact {
    /** Contact to create or update. May be partial. */
    contact?: Contact;
    /**
     * Explicit list of fields to update.
     * @internal
     */
    fieldMask?: string[];
}
interface BulkUpsertContactsResponse {
    /**
     * Results of the upsert for each contact.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpsertContactsResponseBulkContactResult[];
    /** Metadata regarding the bulk upsert operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
declare enum Action {
    UNKNOWN_ACTION = "UNKNOWN_ACTION",
    UPDATED = "UPDATED",
    CREATED = "CREATED"
}
/** @enumType */
type ActionWithLiterals = Action | 'UNKNOWN_ACTION' | 'UPDATED' | 'CREATED';
interface BulkUpsertContactsResponseBulkContactResult {
    /** Metadata regarding the upsert of this specific contact. */
    itemMetadata?: ItemMetadata;
    /** Returned only when `returnEntity` is `true` in the request. */
    item?: Contact;
    /** The action performed on the contact. */
    action?: ActionWithLiterals;
}
interface BulkDeleteContactsRequest {
    /**
     * IDs of the contacts to delete.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    contactIds: string[];
    /**
     * Whether to delete the associated site member for any contact that's a site member. If `true`, the site member is also deleted.
     *
     * Default: `false`
     */
    deleteMembers?: boolean | null;
}
interface BulkDeleteContactsResponse {
    /**
     * Results of the delete operation for each contact.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkDeleteContactsResponseBulkContactResult[];
    /** Metadata regarding the bulk delete operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkDeleteContactsResponseBulkContactResult {
    /** Metadata regarding the deletion of this specific contact. */
    itemMetadata?: ItemMetadata;
}
interface BulkDeleteContactsByFilterRequest {
    /**
     * Filter object.
     *
     * Example:
     * `{ "filter": { "name.last": "Smith" } }`
     */
    filter: Record<string, any> | null;
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
    /**
     * Whether to delete the associated site member for any contact that's a site member. If `true`, the site member is also deleted.
     *
     * Default: `false`
     */
    deleteMembers?: boolean | null;
}
interface BulkDeleteContactsByFilterResponse {
    /**
     * Job ID. Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job) to retrieve the status of the asynchronous operation.
     * @format GUID
     */
    jobId?: string;
}
interface RemoveContactFromTrashBinRequest {
    /**
     * ID of the contact to permanently delete from the trash bin.
     * @format GUID
     */
    contactId: string;
}
interface RemoveContactFromTrashBinResponse {
}
interface RestoreContactFromTrashBinRequest {
    /**
     * ID of the contact to restore from the trash bin.
     * @format GUID
     */
    contactId: string;
}
interface RestoreContactFromTrashBinResponse {
    /** Restored contact. */
    contact?: Contact;
}
interface BulkRestoreContactsFromTrashBinRequest {
    /**
     * IDs of the contacts to restore from the trash bin.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    contactIds?: string[];
    /** Whether to include the restored contacts in the response. */
    returnEntity?: boolean;
}
interface BulkRestoreContactsFromTrashBinResponse {
    /**
     * Results of the restore operation for each contact.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkRestoreContactsFromTrashBinResponseBulkContactResult[];
    /** Metadata regarding the bulk restore operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkRestoreContactsFromTrashBinResponseBulkContactResult {
    /** Metadata regarding the restoration of this specific contact. */
    itemMetadata?: ItemMetadata;
    /** Returned only when `returnEntity` is `true` in the request. */
    item?: Contact;
}
interface GetDeletedContactRequest {
    /**
     * ID of the contact to retrieve from the trash bin.
     * @format GUID
     */
    contactId: string;
}
interface GetDeletedContactResponse {
    /** Retrieved contact. */
    contact?: Contact;
}
interface ListDeletedContactsRequest {
    /**
     * External ID.
     * @maxLength 100
     */
    externalId?: string | null;
    /**
     * Email address.
     * @format EMAIL
     * @maxLength 320
     */
    email?: string | null;
    /**
     * Phone number.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string | null;
    /**
     * Contact IDs.
     * @maxSize 100
     * @format GUID
     */
    contactIds?: string[];
    /** Filters to contacts deleted on or after this date and time. */
    deletedFromDate?: Date | null;
    /** Filters to contacts deleted on or before this date and time. */
    deletedToDate?: Date | null;
    /** Cursor paging. */
    paging?: CursorPaging;
}
interface ListDeletedContactsResponse {
    /** Retrieved contacts. */
    contacts?: Contact[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface BulkAddSegmentToContactsRequest {
    /**
     * Segment ID.
     * @format GUID
     */
    segmentId?: string;
    /**
     * IDs of the contacts to add the segment to.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    contactIds?: string[];
    /**
     * IDs of segments on the contact that no longer exist, used to remove them from the contact.
     * @format GUID
     * @maxSize 100
     */
    existingSegmentIds?: string[];
}
interface BulkAddSegmentToContactsResponse {
    /** Results of the action for each contact. */
    results?: ItemMetadata[];
    /** Metadata regarding the bulk action. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkRemoveSegmentFromContactsRequest {
    /**
     * Segment ID.
     * @format GUID
     */
    segmentId?: string;
    /**
     * IDs of the contacts to remove the segment from.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    contactIds?: string[];
}
interface BulkRemoveSegmentFromContactsResponse {
    /** Results of the action for each contact. */
    results?: ItemMetadata[];
    /** Metadata regarding the bulk action. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface QueryContactsByOffsetInternalRequest {
    /** WQL expression. */
    query?: QueryV2;
    /**
     * Plain text search for an exact match.
     *
     * Supported search properties are:
     *
     * - `name.full`
     * - `email.email`
     * - `phone.phone`
     * - `member_info.email`
     * - `member_info.profile_info.nickname`
     * - `company.name`
     * - `company.job_title`
     *
     * Max: 100 characters
     * @maxLength 100
     */
    search?: string | null;
    /**
     * Whether to omit the total count of matching contacts from the response.
     * @internal
     */
    omitTotal?: boolean | null;
    /**
     * Name Format for sorting and searching.
     * When sorting by `displayName`, the returned contacts are ordered by first and last names, by default.
     * Alternatively, when sorting by `displayName` with `BY_LAST_NAME`, the returned contacts are ordered by last and first names.
     * For contacts that do not have names, sorting is by email or phone.
     * When searching by `name.full`, the contacts returned are matched and ordered by first and last names.
     * Alternatively, when searching by `name.full` with `BY_LAST_NAME`, the contacts returned
     * are matched and ordered by last and first names.
     */
    nameFormat?: QueryContactsByOffsetInternalRequestNameFormatWithLiterals;
}
interface QueryV2 extends QueryV2PagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
     * @maxSize 100
     */
    sort?: Sorting[];
    /**
     * Projected fields to return. If `fieldsets` is also specified, the union of `fieldsets` and `fields` is returned.
     * @maxSize 1000
     * @maxLength 100
     */
    fields?: string[];
    /**
     * Predefined, named sets of projected fields to return. Specifying multiple `fieldsets` returns the union of fields from all sets. If `fields` is also specified, the union of `fieldsets` and `fields` is returned.
     * @maxSize 100
     * @maxLength 100
     */
    fieldsets?: string[];
}
/** @oneof */
interface QueryV2PagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Paging {
    /**
     * Number of items to load.
     * @max 100
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
/** Name format types for sorting and searching. */
declare enum QueryContactsByOffsetInternalRequestNameFormat {
    BY_FIRST_NAME = "BY_FIRST_NAME",
    BY_LAST_NAME = "BY_LAST_NAME"
}
/** @enumType */
type QueryContactsByOffsetInternalRequestNameFormatWithLiterals = QueryContactsByOffsetInternalRequestNameFormat | 'BY_FIRST_NAME' | 'BY_LAST_NAME';
interface QueryContactsByOffsetInternalResponse {
    /** Retrieved contacts. */
    contacts?: Contact[];
    /** Paging metadata. */
    pagingMetadata?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Whether the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
    /**
     * Whether there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     * @internal
     */
    hasNext?: boolean | null;
}
interface GetContactImageUploadStatusRequest {
    /**
     * ID of the contact whose image upload status to retrieve.
     * @format GUID
     */
    contactId: string;
    /**
     * The upload ID of the image upload process. Returned when an image URL is provided while creating or updating a contact, or from [Generate Contact Image Upload Url](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/generate-contact-image-upload-url).
     * @maxLength 100
     */
    imageUploadId: string;
}
interface GetContactImageUploadStatusResponse {
    /** The status of the image upload process. */
    status?: ImageUploadStatusWithLiterals;
}
declare enum ImageUploadStatus {
    /** Image upload is in progress. */
    IN_PROGRESS = "IN_PROGRESS",
    /** Image is uploaded. */
    SUCCESS = "SUCCESS",
    /** Image upload failed. */
    FAILURE = "FAILURE"
}
/** @enumType */
type ImageUploadStatusWithLiterals = ImageUploadStatus | 'IN_PROGRESS' | 'SUCCESS' | 'FAILURE';
interface BulkUpdatePhoneCountryCodeByFilterRequest {
    /**
     * A filter for the contacts that the country code will be applied to.
     *
     * Example:
     * `{ "filter": { "name.last": "Smith" } }`
     */
    filter?: Record<string, any> | null;
    /** Free text to match in searchable fields for the contacts that the country code will be applied to. */
    search?: SearchDetails;
    /**
     * [ISO-3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code to apply.
     *
     * Applied only to contacts whose phone number isn't already in [ITU E.164 international](https://www.itu.int/rec/T-REC-E.164/) format. If the phone number still can't be converted to E.164 format after applying the country code, that contact fails with `PHONE_CANNOT_BE_FIXED_WITH_COUNTRY_CODE`.
     * @format COUNTRY
     */
    countryCode: string;
}
interface BulkUpdatePhoneCountryCodeByFilterResponse {
    /**
     * Job ID. Pass this ID to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job) to retrieve the status of the asynchronous operation.
     * @format GUID
     */
    jobId?: string;
}
interface Empty {
}
interface MediaFileUploadedEvent {
    fileId?: string;
    path?: string;
    mimeType?: string;
    acl?: string | null;
    size?: string | null;
    dateCreated?: Date | null;
    dateUpdated?: Date | null;
}
/** @docsIgnore */
type BulkUpdateContactSubscriptionsApplicationErrors = {
    code?: 'MISSING_SUBSCRIPTION_STATUS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type CreateContactApplicationErrors = {
    code?: 'INSUFFICIENT_CONTACT_DATA';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_PHONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MULTIPLE_DEFAULT_ADDRESSES';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'EMAIL_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'PHONE_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_CONTACT_EXISTS';
    description?: string;
    data?: DuplicateContactExists;
} | {
    code?: 'INVALID_TIME_ZONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_BIRTHDATE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_IMAGE_URL';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type UpdateContactApplicationErrors = {
    code?: 'INSUFFICIENT_CONTACT_DATA';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_PHONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MULTIPLE_DEFAULT_ADDRESSES';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'EMAIL_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'PHONE_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_CHANGE_EXTERNAL_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_CONTACT_EXISTS';
    description?: string;
    data?: DuplicateContactExists;
} | {
    code?: 'CANNOT_UPDATE_MEMBER_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_TIME_ZONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_IMAGE_URL';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type UpsertContactApplicationErrors = {
    code?: 'INSUFFICIENT_CONTACT_DATA';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_PHONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MULTIPLE_DEFAULT_ADDRESSES';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'EMAIL_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'PHONE_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_TIME_ZONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_IMAGE_URL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_CHANGE_EXTERNAL_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_UPDATE_MEMBER_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNAUTHORIZED_UPDATE_MEMBER';
    description?: string;
    data?: FailedMemberUpdate;
} | {
    code?: 'CANNOT_UPDATE_MEMBER';
    description?: string;
    data?: FailedMemberUpdate;
};
/** @docsIgnore */
type UpdateContactTagsApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type MergeContactsApplicationErrors = {
    code?: 'CANNOT_MERGE_SOURCE_MEMBER';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_UPDATE_MEMBER_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_CONTACT_MERGE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SOURCE_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INSUFFICIENT_CONTACT_DATA';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_EMAIL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_PHONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MULTIPLE_DEFAULT_ADDRESSES';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'EMAIL_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'PHONE_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_CHANGE_EXTERNAL_ID';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_CONTACT_EXISTS';
    description?: string;
    data?: DuplicateContactExists;
} | {
    code?: 'INVALID_TIME_ZONE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_IMAGE_URL';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type DeleteContactApplicationErrors = {
    code?: 'UNAUTHORIZED_DELETE_MEMBER';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_DELETE_MEMBER';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_DELETE_CONTACT_WITH_BILLING_SUBSCRIPTION';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_DELETE_WIX_USERS_MEMBER';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'FAILED_DELETE_CONTACT_AFTER_MEMBER_DELETION';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type FindMatchingContactsApplicationErrors = {
    code?: 'NO_IDENTIFIER_PROVIDED';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type GenerateContactImageUploadUrlApplicationErrors = {
    code?: 'INVALID_MIME_TYPE';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type AddContactAdditionalEmailApplicationErrors = {
    code?: 'EMAIL_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'ADDITIONAL_EMAILS_LIMIT_REACHED';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type AddContactAdditionalPhoneApplicationErrors = {
    code?: 'PHONE_REQUIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'ADDITIONAL_PHONES_LIMIT_REACHED';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type AddContactAddressApplicationErrors = {
    code?: 'ADDRESSES_LIMIT_REACHED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS_ID';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type DeleteContactAddressApplicationErrors = {
    code?: 'ADDRESS_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS_ID';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type UpdateContactAddressApplicationErrors = {
    code?: 'ADDRESS_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ADDRESS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkUpdateContactTagsApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkUpdateContactTagsByFilterApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkDeleteContactsApplicationErrors = {
    code?: 'UNAUTHORIZED_DELETE_MEMBER';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkDeleteContactsByFilterApplicationErrors = {
    code?: 'UNAUTHORIZED_DELETE_MEMBER';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type GetContactImageUploadStatusApplicationErrors = {
    code?: 'UPLOAD_ID_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
};
/**
 * Updates the email and SMS marketing subscription statuses of multiple contacts that match the provided filter. Works asynchronously.
 *
 * If a requested subscription status matches a contact's current status, that contact is left unchanged. Provide at least one of `emailSubscriptionStatus` or `phoneSubscriptionStatus`.
 *
 * To check the status of the bulk update, pass the returned `jobId` to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job).
 * @param filter - A filter for the contacts that the subscription statuses will be applied to.
 *
 * Example:
 * `{ "filter": { "name.last": "Smith" } }`
 * @public
 * @requiredField filter
 * @permissionId CONTACTS.CONTACT_SUBSCRIPTION_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.ContactSubscriptions.BulkUpdateContactSubscriptions
 */
declare function bulkUpdateContactSubscriptions(filter: Record<string, any>, options?: BulkUpdateContactSubscriptionsOptions): Promise<NonNullablePaths<BulkUpdateContactSubscriptionsResponse, `jobId`, 0> & {
    __applicationErrorsType?: BulkUpdateContactSubscriptionsApplicationErrors;
}>;
interface BulkUpdateContactSubscriptionsOptions {
    /** Free text to match in searchable fields for the contacts that the subscription statuses will be applied to. */
    search?: SearchDetails;
    /** The subscription status to apply to the contact's email address. */
    emailSubscriptionStatus?: V5SubscriptionStatusWithLiterals;
    /** The subscription status to apply to the contact's phone. */
    phoneSubscriptionStatus?: V5SubscriptionStatusWithLiterals;
}
/**
 * Creates a contact.
 *
 * At least one of `name.first`, `name.last`, `email.email`, or `phone.phone` must be provided.
 *
 * Duplicate handling: By default, creation fails if a contact with the same identifiers (`externalId`, `email.email`, or `phone.phone`) already exists. Set `allowDuplicates` to `true` to bypass this check. Learn more about [contact matching and duplicates](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
 *
 * Image upload: When you provide an image URL, the image is uploaded asynchronously and an `imageUploadId` is returned for tracking upload progress. Call [Get Contact Image Upload Status](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/get-contact-image-upload-status) to monitor the upload.
 *
 * To create multiple contacts in a single request, call [Bulk Create Contacts](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-create-contacts).
 * @param contact - Contact to create.
 * @public
 * @requiredField contact
 * @permissionId CONTACTS.CONTACT_CREATE
 * @applicableIdentity APP
 * @returns Created contact.
 * @fqn wix.contacts.v5.Contacts.CreateContact
 */
declare function createContact(contact: Contact, options?: CreateContactOptions): Promise<NonNullablePaths<Contact, `email.email` | `email.subscriptionStatus.status` | `email.deliverabilityStatus` | `additionalEmails` | `additionalEmails.${number}.email` | `phone.tag` | `phone.phone` | `phone.deliverabilityStatus` | `additionalPhones` | `additionalPhones.${number}.tag` | `additionalPhones.${number}.phone` | `addresses` | `addresses.${number}.tag` | `addresses.${number}.address.streetAddress.number` | `addresses.${number}.address.streetAddress.name` | `addresses.${number}.address.streetAddress.apt` | `locationIds` | `segmentIds` | `memberInfo.status` | `memberInfo.profileInfo.privacyStatus` | `memberInfo.userInfo.userId` | `memberInfo.userInfo.role` | `memberInfo.sessionInfo.mobileAppNames` | `memberInfo.groupInfo.groupIds` | `tags.privateTags.tagIds`, 4> & {
    __applicationErrorsType?: CreateContactApplicationErrors;
}>;
interface CreateContactOptions {
    /**
     * Whether to create the contact even if it duplicates an existing contact.
     * More about duplicate contacts can be found [here](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
     */
    allowDuplicates?: boolean;
}
/**
 * Retrieves a single contact by ID.
 * @param contactId - ID of the contact to retrieve.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.CONTACT_READ
 * @applicableIdentity APP
 * @returns Retrieved contact.
 * @fqn wix.contacts.v5.Contacts.GetContact
 */
declare function getContact(contactId: string): Promise<NonNullablePaths<Contact, `email.email` | `email.subscriptionStatus.status` | `email.deliverabilityStatus` | `additionalEmails` | `additionalEmails.${number}.email` | `phone.tag` | `phone.phone` | `phone.deliverabilityStatus` | `additionalPhones` | `additionalPhones.${number}.tag` | `additionalPhones.${number}.phone` | `addresses` | `addresses.${number}.tag` | `addresses.${number}.address.streetAddress.number` | `addresses.${number}.address.streetAddress.name` | `addresses.${number}.address.streetAddress.apt` | `locationIds` | `segmentIds` | `memberInfo.status` | `memberInfo.profileInfo.privacyStatus` | `memberInfo.userInfo.userId` | `memberInfo.userInfo.role` | `memberInfo.sessionInfo.mobileAppNames` | `memberInfo.groupInfo.groupIds` | `tags.privateTags.tagIds`, 4>>;
/**
 * Updates a contact.
 *
 * Each time the contact is updated, `revision` increments by 1. The current `revision` must be passed when updating the contact. This ensures you're working with the latest contact and prevents unintended overwrites.
 *
 * Restrictions:
 * - `externalId` can be set only once and can't be changed after creation.
 * - The main email of a contact that is a site member can't be changed. Update it through the [Members API](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/member-management/members/introduction) instead.
 * - At least one of `name`, `email`, or `phone` must remain after the update.
 *
 * Image upload: When you provide an image URL, the image is uploaded asynchronously and an `imageUploadId` is returned for tracking upload progress. Call [Get Contact Image Upload Status](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/get-contact-image-upload-status) to monitor the upload.
 *
 * To update multiple contacts in a single request, call [Bulk Update Contacts](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-update-contacts).
 * @param _id - Contact ID.
 * @public
 * @requiredField _id
 * @requiredField contact
 * @requiredField contact.revision
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @permissionId CONTACTS.CONTACT_MEMBER_EMAIL_UPDATE
 * @applicableIdentity APP
 * @returns Updated contact.
 * @fqn wix.contacts.v5.Contacts.UpdateContact
 */
declare function updateContact(_id: string, contact: NonNullablePaths<UpdateContact, `revision`, 0>, options?: UpdateContactOptions): Promise<NonNullablePaths<Contact, `email.email` | `email.subscriptionStatus.status` | `email.deliverabilityStatus` | `additionalEmails` | `additionalEmails.${number}.email` | `phone.tag` | `phone.phone` | `phone.deliverabilityStatus` | `additionalPhones` | `additionalPhones.${number}.tag` | `additionalPhones.${number}.phone` | `addresses` | `addresses.${number}.tag` | `addresses.${number}.address.streetAddress.number` | `addresses.${number}.address.streetAddress.name` | `addresses.${number}.address.streetAddress.apt` | `locationIds` | `segmentIds` | `memberInfo.status` | `memberInfo.profileInfo.privacyStatus` | `memberInfo.userInfo.userId` | `memberInfo.userInfo.role` | `memberInfo.sessionInfo.mobileAppNames` | `memberInfo.groupInfo.groupIds` | `tags.privateTags.tagIds`, 4> & {
    __applicationErrorsType?: UpdateContactApplicationErrors;
}>;
interface UpdateContact {
    /**
     * Contact ID.
     * @format GUID
     * @immutable
     * @readonly
     */
    _id?: string | null;
    /**
     * Revision number, which increments by 1 each time the Contact is updated.
     * To prevent conflicting changes,
     * the current revision must be passed when updating the Contact.
     *
     * Ignored when creating a Contact.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the contact was created.
     * @readonly
     * @immutable
     */
    _createdDate?: Date | null;
    /**
     * Date and time the contact was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Contact external ID. Used to identify contacts from external systems.
     * This field can only be set once and can't be updated after creation.
     * @maxLength 100
     */
    externalId?: string | null;
    /** Contact's name information including first and last name. */
    name?: ContactName;
    /**
     * The main email of the contact with subscription status and deliverability information.
     * This email is used for contact matching and deduplication logic.
     */
    email?: ContactEmail;
    /**
     * Additional emails of the contact.
     * These emails do not affect subscription status or contact matching/deduplication.
     * The main email field must be set before additional emails can be added.
     * @maxSize 10
     */
    additionalEmails?: AdditionalEmail[];
    /**
     * The main phone of the contact with subscription status and deliverability information.
     * This phone is used for contact matching and deduplication logic.
     * At least one of name, email, or phone is required for contact creation.
     */
    phone?: ContactPhone;
    /**
     * Additional phones of the contact.
     * These phones do not affect subscription status or contact matching/deduplication.
     * The main phone field must be set before additional phones can be added.
     * @maxSize 10
     */
    additionalPhones?: AdditionalPhone[];
    /**
     * Contact's physical addresses.
     * Each address can be tagged (HOME, WORK, BILLING, SHIPPING, OTHER) and one can be set as default.
     * If no address is marked as default, the first address automatically becomes the default.
     * @maxSize 50
     */
    addresses?: ContactAddress[];
    /** Contact's company information including company name and job title. */
    company?: ContactCompany;
    /**
     * Locale in
     * [IETF BCP 47 language tag](https://en.wikipedia.org/wiki/IETF_language_tag) format.
     * Typically, this is a lowercase 2-letter language code,
     * followed by a hyphen, followed by an uppercase 2-letter country code.
     * For example, `en-US` for U.S. English, and `de-DE` for Germany German.
     * @format LANGUAGE_TAG
     */
    locale?: string | null;
    /**
     * Birth date in `YYYY-MM-DD` format. For example, `2020-03-15`.
     * @format LOCAL_DATE
     */
    birthdate?: string | null;
    /**
     * IANA time zone identifier for the contact's location.
     * Valid values are IANA time zone IDs, such as 'Europe/Rome' or 'America/New_York'.
     * @maxLength 50
     */
    timeZone?: string | null;
    /**
     * Site contributor user ID assigned to manage communication with this contact.
     * Must be a valid GUID referencing an existing site contributor.
     * Only one assignee can be set per contact.
     * @format GUID
     */
    assigneeId?: string | null;
    /**
     * List of business location IDs associated with this contact.
     * Used for multi-location businesses to organize contacts by physical business locations.
     * Each ID must be a valid GUID referencing an existing business location.
     * @format GUID
     * @maxSize 50
     */
    locationIds?: string[];
    /**
     * Contact's profile image.
     * If the contact is also a site member, clients should prefer to view `member_info.profile_info.photo`
     * when it exists, as it reflects the member's current profile photo.
     */
    image?: string;
    /**
     * Details about the origin of the contact.
     * This field is immutable and set automatically when the contact is created.
     * @immutable
     */
    source?: ContactSource;
    /**
     * Details about the contact's most recent activity on the site.
     * Includes activity date and template ID. This field is read-only and updated automatically.
     * @readonly
     */
    lastActivity?: ContactActivity;
    /**
     * IDs of the segments the contact belongs to.
     * Segments are used for organizing and targeting groups of contacts.
     * @maxSize 60
     * @readonly
     * @format GUID
     */
    segmentIds?: string[];
    /**
     * Contact's site membership information including member ID, status, and profile details.
     * This field is read-only and populated automatically when the contact is also a site member.
     *
     * Updates are eventually consistent: changes to the underlying member, identity, or profile are reflected after the corresponding events are processed.
     * @readonly
     */
    memberInfo?: MemberInfo;
    /**
     * Contact's display name. The field is used for sorting only and is not returned.
     * @internal
     * @maxLength 1000
     * @readonly
     */
    displayName?: string | null;
    /** Tags assigned to the contact for organization and filtering. */
    tags?: PrivateTags;
    /**
     * Extended fields for storing custom contact data beyond the standard fields.
     * Allows adding custom properties specific to business needs.
     * To define a custom extended field, use the [Schema Service API](https://dev.wix.com/docs/api-reference/business-management/data-extension-schema/about-the-data-extension-schema-api) with the contacts FQDN: `wix.contacts.*.contact`.
     */
    extendedFields?: ExtendedFields;
}
interface UpdateContactOptions {
    /**
     * Whether to update the contact even if it becomes a duplicate of another contact.
     * More about duplicate contacts can be found [here](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
     */
    allowDuplicates?: boolean;
}
/**
 * Creates a contact, or updates an existing one when a match is found based on the contact's identifiers.
 *
 * Matching uses `externalId`, `email.email`, and `phone.phone` to find an existing contact. See [contact matching](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts) for the detailed matching rules.
 *
 * Upsert modes: Set `upsertMode` to control how an existing contact is updated:
 * - `OVERWRITE` (default): Replaces existing field values.
 * - `APPEND`: Sets a non-array field only if it doesn't already have a value, and appends to array fields.
 * - `OVERWRITE_APPEND_ARRAYS`: Overwrites non-array fields and appends to array fields. `email`/`additionalEmails` and `phone`/`additionalPhones` are each treated as a single array field: the incoming value is set as `email`/`phone` only if none exists yet, otherwise it's appended to `additionalEmails`/`additionalPhones`.
 *
 * Member contacts: Set `updateMember` to `true` when upserting a contact that is a site member.
 *
 * The response's `action` field indicates whether the contact was `CREATED` or `UPDATED`.
 *
 * Image upload: When you provide an image URL, the image is uploaded asynchronously and an `imageUploadId` is returned for tracking upload progress. Call [Get Contact Image Upload Status](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/get-contact-image-upload-status) to monitor the upload.
 *
 * To create or update multiple contacts in a single request, call [Bulk Upsert Contacts](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-upsert-contacts).
 * @param contact - Contact to upsert.
 * @public
 * @requiredField contact
 * @permissionId CONTACTS.CONTACT_UPSERT
 * @permissionId CONTACTS.CONTACT_MEMBER_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.UpsertContact
 */
declare function upsertContact(contact: Contact, options?: UpsertContactOptions): Promise<NonNullablePaths<UpsertContactResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds` | `action`, 5> & {
    __applicationErrorsType?: UpsertContactApplicationErrors;
}>;
interface UpsertContactOptions {
    /** Mode that determines how an existing contact is updated when a match is found. See the endpoint description for the supported modes. */
    upsertMode?: UpsertModeWithLiterals;
    /** Whether to update the contact even if it's a site member. */
    updateMember?: boolean | null;
}
/**
 * Adds or removes tags on a contact.
 *
 * Specify which tags to assign and which to unassign. A tag that appears in both the list to assign and the list to unassign is assigned. At least one tag must be specified to assign or unassign.
 *
 * To update tags on multiple contacts, call [Bulk Update Contact Tags](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-update-contact-tags).
 * @param contactId - ID of the contact whose tags to update.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.UpdateContactTags
 */
declare function updateContactTags(contactId: string, options?: UpdateContactTagsOptions): Promise<NonNullablePaths<UpdateContactTagsResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: UpdateContactTagsApplicationErrors;
}>;
interface UpdateContactTagsOptions {
    /** Tags to assign. */
    assignTags?: PrivateTags;
    /** Tags to unassign. */
    unassignTags?: PrivateTags;
}
/**
 * Merges up to 5 source contacts into a target contact.
 *
 * The current target contact revision must be passed as `targetContactRevision`. This ensures you're working with the latest contact and prevents unintended overwrites.
 *
 * The merge behaves in one of two ways, depending on whether you provide `targetContact` in the request:
 *
 * - Without `targetContact`: The source contacts are merged into the existing target contact. Non-empty fields from the sources fill empty fields on the target, and array fields (emails, phones, and addresses) are combined and deduplicated. When you provide multiple sources, they're merged in order: the first source into the second, that result into the third, and so on, and finally into the target. Earlier sources take precedence over later ones.
 * - With `targetContact`: The provided `targetContact` overwrites the target according to `fieldMask`, and the source contacts are deleted without their data being merged in.
 *
 * In both cases, the source contacts are permanently deleted after a successful merge.
 *
 * Set `preview` to `true` to return the merged result without applying it, so you can review the outcome before committing.
 *
 * Restrictions:
 * - Source contacts can't be site members. The target contact can be a site member.
 * - `externalId` can be set only once and can't be changed if it already has a value.
 * - The target contact must remain valid after the merge, keeping at least one of `name`, `email`, or `phone`.
 * @param targetContactId - Target contact ID.
 * @param targetContactRevision - Revision number of the target contact, which increments by 1 each time the contact is updated.
 * The current revision must be passed to prevent conflicting changes.
 * @public
 * @requiredField targetContactId
 * @requiredField targetContactRevision
 * @permissionId CONTACTS.CONTACT_MERGE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.MergeContacts
 */
declare function mergeContacts(targetContactId: string, targetContactRevision: string, options?: MergeContactsOptions): Promise<NonNullablePaths<MergeContactsResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: MergeContactsApplicationErrors;
}>;
interface MergeContactsOptions {
    /** Contact to overwrite the target with, instead of merging in the source contacts' data. See the endpoint description for details. */
    targetContact?: Contact;
    /**
     * IDs of up to 5 contacts to merge into the target contact.
     * If merging more than one source contact,
     * the first source is given precedence, then the second, and so on.
     * @minSize 1
     * @maxSize 5
     * @format GUID
     */
    sourceContactIds?: string[];
    /**
     * Whether to return a preview of the merged contact instead of performing the actual merge.
     *
     * Default: `false`
     */
    preview?: boolean | null;
    /**
     * Whether to update the target contact even if it becomes a duplicate of another contact. Relevant only when `targetContact` is provided.
     * More about duplicate contacts can be found [here](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
     */
    allowDuplicates?: boolean;
}
/**
 * Deletes a contact.
 *
 * Deleted contacts are moved to the trash bin and can be restored within 90 days by calling [Restore Contact From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/restore-contact-from-trash-bin) or [Bulk Restore Contacts From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-restore-contacts-from-trash-bin). After 90 days, they're permanently deleted.
 *
 * Member contacts: To delete a contact that is also a site member, set `deleteMember` to `true`. This deletes both the contact and the member.
 *
 * Restrictions:
 * - A contact with an active billing subscription can't be deleted.
 * - A site owner or contributor can't be deleted.
 *
 * To delete multiple contacts in a single request, call [Bulk Delete Contacts](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-delete-contacts).
 * @param contactId - ID of the contact to delete.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.CONTACT_DELETE
 * @permissionId MEMBERS.MEMBER_DELETE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.DeleteContact
 */
declare function deleteContact(contactId: string, options?: DeleteContactOptions): Promise<void & {
    __applicationErrorsType?: DeleteContactApplicationErrors;
}>;
interface DeleteContactOptions {
    /**
     * Whether to delete the contact even if it's a site member. If `true`, the site member is also deleted.
     *
     * Default: `false`
     */
    deleteMember?: boolean | null;
}
/**
 * Retrieves a list of up to 100 contacts that match the provided identifiers.
 *
 * Matching uses `externalId`, `email`, and `phone` to find contacts that likely represent the same person. Provide at least one identifier.
 *
 * Results are ordered by match strength: contacts matching more or higher-priority identifiers appear first. Within each group, contacts are ordered by creation date, oldest first. Identifier priority is `externalId` (highest), then `email`, then `phone` (lowest).
 *
 * Learn more about [contact matching](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts).
 * @public
 * @permissionId CONTACTS.CONTACT_READ
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.FindMatchingContacts
 */
declare function findMatchingContacts(options?: FindMatchingContactsOptions): Promise<NonNullablePaths<FindMatchingContactsResponse, `contacts` | `contacts.${number}.email.email` | `contacts.${number}.email.subscriptionStatus.status` | `contacts.${number}.email.deliverabilityStatus` | `contacts.${number}.phone.tag` | `contacts.${number}.phone.phone` | `contacts.${number}.phone.deliverabilityStatus` | `contacts.${number}.memberInfo.status` | `contacts.${number}.memberInfo.profileInfo.privacyStatus` | `contacts.${number}.memberInfo.userInfo.userId` | `contacts.${number}.memberInfo.userInfo.role`, 4> & {
    __applicationErrorsType?: FindMatchingContactsApplicationErrors;
}>;
interface FindMatchingContactsOptions {
    /**
     * Contact external ID.
     * @maxLength 100
     */
    externalId?: string | null;
    /**
     * Email address.
     * @format EMAIL
     * @maxLength 320
     */
    email?: string | null;
    /**
     * Phone number.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string | null;
}
/**
 * Retrieves a list of up to 100 contacts, given the provided filtering, sorting, and cursor paging.
 *
 *
 * Query Contacts runs with these defaults, which you can override:
 *
 * - `createdDate` is sorted in `DESC` order
 * - `paging.limit` is `50`
 *
 * To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).
 * @public
 * @permissionId CONTACTS.CONTACT_READ
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.QueryContacts
 */
declare function queryContacts(): ContactsQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface ContactsQueryResult extends QueryCursorResult {
    items: Contact[];
    query: ContactsQueryBuilder;
    next: () => Promise<ContactsQueryResult>;
    prev: () => Promise<ContactsQueryResult>;
}
interface ContactsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    eq: (propertyName: '_id' | '_createdDate' | '_updatedDate' | 'externalId' | 'email.email' | 'phone.phone', value: any) => ContactsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ge: (propertyName: '_createdDate' | '_updatedDate', value: any) => ContactsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    gt: (propertyName: '_createdDate' | '_updatedDate', value: any) => ContactsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    le: (propertyName: '_createdDate' | '_updatedDate', value: any) => ContactsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    lt: (propertyName: '_createdDate' | '_updatedDate', value: any) => ContactsQueryBuilder;
    in: (propertyName: '_id' | 'externalId' | 'email.email' | 'phone.phone', value: any) => ContactsQueryBuilder;
    exists: (propertyName: 'externalId' | 'email.email' | 'phone.phone', value: boolean) => ContactsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    ascending: (...propertyNames: Array<'_createdDate' | '_updatedDate'>) => ContactsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object. */
    limit: (limit: number) => ContactsQueryBuilder;
    /** @param cursor - A pointer to specific record */
    skipTo: (cursor: string) => ContactsQueryBuilder;
    find: () => Promise<ContactsQueryResult>;
}
/**
 * @hidden
 * @fqn wix.contacts.v5.Contacts.QueryContacts
 * @requiredField query
 */
declare function typedQueryContacts(query: ContactQuery): Promise<NonNullablePaths<QueryContactsResponse, `contacts` | `contacts.${number}.email.email` | `contacts.${number}.email.subscriptionStatus.status` | `contacts.${number}.email.deliverabilityStatus` | `contacts.${number}.phone.tag` | `contacts.${number}.phone.phone` | `contacts.${number}.phone.deliverabilityStatus` | `contacts.${number}.memberInfo.status` | `contacts.${number}.memberInfo.profileInfo.privacyStatus` | `contacts.${number}.memberInfo.userInfo.userId` | `contacts.${number}.memberInfo.userInfo.role`, 4>>;
interface ContactQuerySpec extends QuerySpec {
    paging: 'cursor';
    wql: [
        {
            fields: ['_id'];
            operators: ['$eq', '$in'];
            sort: 'NONE';
        },
        {
            fields: ['_createdDate', '_updatedDate'];
            operators: ['$eq', '$gt', '$gte', '$lt', '$lte'];
            sort: 'ASC';
        },
        {
            fields: ['email.email', 'externalId', 'phone.phone'];
            operators: ['$eq', '$exists', '$in'];
            sort: 'NONE';
        }
    ];
}
type CommonQueryWithEntityContext = Query<Contact, ContactQuerySpec>;
type ContactQuery = {
    /**
    Cursor paging options.
  
    Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
    */
    cursorPaging?: {
        /**
      Maximum number of items to return in the results.
      @max: 100
      */
        limit?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['limit'] | null;
        /**
      Pointer to the next or previous page in the list of results.
    
      Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
      Not relevant for the first request.
      @maxLength: 16000
      */
        cursor?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['cursor'] | null;
    };
    /**
    Filter object.
  
    Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
    */
    filter?: CommonQueryWithEntityContext['filter'] | null;
    /**
    Sort object.
  
    Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
    @maxSize: 5
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 512
      */
        fieldName?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
      */
        order?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['order'];
        /**
      When `field_name` is a property of repeated field that is marked as `MATCH_ITEMS` and sort should be done by
      a specific element from a collection, filter can/should be provided to ensure correct sort value is picked.
    
      If multiple filters are provided, they are combined with AND operator.
    
      Example:
      Given we have document like {"id": "1", "nestedField": [{"price": 10, "region": "EU"}, {"price": 20, "region": "US"}]}
      and `nestedField` is marked as `MATCH_ITEMS`, to ensure that sorting is done by correct region, filter should be
      { fieldName: "nestedField.price", "select_items_by": [{"nestedField.region": "US"}] }
      @internal: undefined,
      @maxSize: 10
      */
        selectItemsBy?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['selectItemsBy'] | null;
    }[];
};
declare const utils: {
    query: _wix_sdk_types.QueryHelpers<Contact, ContactQuerySpec, ContactQuery>;
    search: _wix_sdk_types.SearchHelpers<Contact, ContactSearchSpec, ContactSearch>;
};
interface SearchContactsOptions {
    /**
     * Name Format for sorting and searching.
     * When sorting by `displayName`, the returned contacts are ordered by first and last names, by default.
     * Alternatively, when sorting by `displayName` with `BY_LAST_NAME`, the returned contacts are ordered by last and first names.
     * For contacts that do not have names, sorting is by email or phone.
     * When searching by `name.full`, the contacts returned are matched and ordered by first and last names.
     * Alternatively, when searching by `name.full` with `BY_LAST_NAME`, the contacts returned
     * are matched and ordered by last and first names.
     */
    nameFormat?: NameFormatWithLiterals;
    /**
     * Natural language query for semantic search. Mutually exclusive with cursor paging with a cursor.
     * @maxLength 1000
     */
    semanticQuery?: string | null;
}
interface ContactSearchSpec extends SearchSpec {
    searchable: [
        'company.jobTitle',
        'company.name',
        'email.email',
        'memberInfo.email',
        'memberInfo.profileInfo.nickname',
        'name.full',
        'phone.phone'
    ];
    aggregatable: [
        '_createdDate',
        '_id',
        '_updatedDate',
        'addresses.address.country',
        'addresses.address.subdivision',
        'addresses.address.subdivisionFullname',
        'assigneeId',
        'email.deliverabilityStatus',
        'email.subscriptionStatus.status',
        'lastActivity.date',
        'locale',
        'locationIds',
        'memberInfo.emailVerificationRequired',
        'memberInfo.emailVerified',
        'memberInfo.groupInfo.groupIds',
        'memberInfo.memberId',
        'memberInfo.profileInfo.muted',
        'memberInfo.profileInfo.privacyStatus',
        'memberInfo.profileInfo.reported',
        'memberInfo.sessionInfo.lastMobileLogin',
        'memberInfo.sessionInfo.lastWebLogin',
        'memberInfo.sessionInfo.mobileAppNames',
        'memberInfo.signupDate',
        'memberInfo.status',
        'memberInfo.userInfo.role',
        'phone.deliverabilityStatus',
        'phone.subscriptionStatus.status',
        'segmentIds',
        'source.additionalInfo',
        'source.appId',
        'source.userId',
        'tags.privateTags.tagIds',
        'timeZone'
    ];
    paging: 'cursor';
    wql: [
        {
            operators: ['$eq', '$exists', '$in', '$ne', '$nin'];
            fields: [
                'assigneeId',
                'memberInfo.memberId',
                'source.appId',
                'source.userId',
                'timeZone'
            ];
            sort: 'NONE';
        },
        {
            operators: ['$eq', '$exists', '$in', '$ne', '$nin'];
            fields: [
                'memberInfo.profileInfo.privacyStatus',
                'memberInfo.status',
                'memberInfo.userInfo.role'
            ];
            sort: 'BOTH';
        },
        {
            operators: ['$eq', '$ne'];
            fields: [
                'memberInfo.emailVerificationRequired',
                'memberInfo.emailVerified',
                'memberInfo.profileInfo.muted',
                'memberInfo.profileInfo.reported'
            ];
            sort: 'NONE';
        },
        {
            operators: ['$eq', '$gt', '$gte', '$lt', '$lte', '$ne'];
            fields: [
                '_createdDate',
                '_updatedDate',
                'lastActivity.date',
                'memberInfo.sessionInfo.lastMobileLogin',
                'memberInfo.sessionInfo.lastWebLogin',
                'memberInfo.signupDate'
            ];
            sort: 'BOTH';
        },
        {
            operators: ['$eq', '$exists', '$in', '$ne', '$nin', '$startsWith'];
            fields: [
                'company.jobTitle',
                'company.name',
                'email.email',
                'memberInfo.email',
                'memberInfo.profileInfo.nickname',
                'name.first',
                'name.last',
                'phone.phone'
            ];
            sort: 'BOTH';
        },
        {
            operators: ['$eq', '$in', '$ne', '$nin'];
            fields: [
                '_id',
                'email.deliverabilityStatus',
                'email.subscriptionStatus.status',
                'phone.deliverabilityStatus',
                'phone.subscriptionStatus.status'
            ];
            sort: 'NONE';
        },
        {
            operators: ['$eq', '$exists', '$in', '$ne', '$nin', '$startsWith'];
            fields: ['locale', 'phone.e164', 'source.additionalInfo'];
            sort: 'NONE';
        },
        {
            operators: ['$exists', '$hasAll', '$hasSome', '$isEmpty'];
            fields: [
                'locationIds',
                'memberInfo.groupInfo.groupIds',
                'memberInfo.sessionInfo.mobileAppNames',
                'segmentIds',
                'tags.privateTags.tagIds'
            ];
            sort: 'NONE';
        },
        {
            operators: ['$exists', '$hasAll', '$hasSome'];
            fields: [
                'addresses.address.country',
                'addresses.address.subdivision',
                'addresses.recipient.phone'
            ];
            sort: 'NONE';
        },
        {
            operators: ['$exists', '$hasAll', '$hasSome', '$startsWith'];
            fields: [
                'additionalEmails.email',
                'additionalPhones.e164',
                'additionalPhones.phone',
                'addresses.address.addressLine',
                'addresses.address.city',
                'addresses.address.postalCode',
                'addresses.address.streetAddress.name',
                'addresses.address.subdivisionFullname',
                'addresses.recipient.company',
                'addresses.recipient.firstName',
                'addresses.recipient.lastName'
            ];
            sort: 'NONE';
        },
        {
            operators: ['$eq', '$gt', '$gte', '$lt', '$lte', '$ne'];
            fields: ['birthdate'];
            sort: 'BOTH';
        }
    ];
}
type CommonSearchWithEntityContext = Search<Contact, ContactSearchSpec>;
type ContactSearch = {
    /**
    Cursor paging options.
  
    Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
    */
    cursorPaging?: {
        /**
      Maximum number of items to return in the results.
      @max: 100
      */
        limit?: NonNullable<CommonSearchWithEntityContext['cursorPaging']>['limit'] | null;
        /**
      Pointer to the next or previous page in the list of results.
    
      Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
      Not relevant for the first request.
      @maxLength: 16000
      */
        cursor?: NonNullable<CommonSearchWithEntityContext['cursorPaging']>['cursor'] | null;
    };
    /**
    Filter object.
  
    Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
    */
    filter?: CommonSearchWithEntityContext['filter'] | null;
    /**
    List of sort objects.
  
    Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
    @maxSize: 10
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 512
      */
        fieldName?: NonNullable<CommonSearchWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
      */
        order?: NonNullable<CommonSearchWithEntityContext['sort']>[number]['order'];
        /**
      When `field_name` is a property of repeated field that is marked as `MATCH_ITEMS` and sort should be done by
      a specific element from a collection, filter can/should be provided to ensure correct sort value is picked.
    
      If multiple filters are provided, they are combined with AND operator.
    
      Example:
      Given we have document like {"id": "1", "nestedField": [{"price": 10, "region": "EU"}, {"price": 20, "region": "US"}]}
      and `nestedField` is marked as `MATCH_ITEMS`, to ensure that sorting is done by correct region, filter should be
      { fieldName: "nestedField.price", "select_items_by": [{"nestedField.region": "US"}] }
      @internal: undefined,
      @maxSize: 10
      */
        selectItemsBy?: NonNullable<CommonSearchWithEntityContext['sort']>[number]['selectItemsBy'] | null;
    }[];
    /**
    Aggregations, also known as faceted search. Lets you explore large amounts of data by displaying summaries about different partitions of the data, and narrow the navigation to a specific partition.
    @maxSize: 10
    */
    aggregations?: {
        /**
      Value aggregation.
      */
        value?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['value'];
        /**
      Range aggregation.
      */
        range?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['range'];
        /**
      Scalar aggregation.
      */
        scalar?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['scalar'];
        /**
      Date histogram aggregation.
      */
        dateHistogram?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['dateHistogram'];
        /**
      Nested aggregation.
      */
        nested?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['nested'];
        /**
      User-defined name for the aggregation. Must be unique. Appears in the aggregation results.
      @maxLength: 100
      */
        name?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['name'] | null;
        /**
      Type of aggregation. Must match the aggregation type provided in the `kind` field.
      */
        type?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['type'];
        /**
      Field to aggregate by. Use dot notation to specify the JSON path.
      @maxLength: 200
      */
        fieldPath?: NonNullable<CommonSearchWithEntityContext['aggregations']>[number]['fieldPath'];
    }[];
    /**
    Free text to match in searchable fields.
    */
    search?: {
        /**
      How separate search terms in `expression` are combined.
      */
        mode?: NonNullable<CommonSearchWithEntityContext['search']>['mode'];
        /**
      Search term or expression.
      @maxLength: 100
      */
        expression?: NonNullable<CommonSearchWithEntityContext['search']>['expression'] | null;
        /**
      Fields to search in. If empty, searches all searchable fields. Use dot notation to specify the JSON path.
      @maxLength: 200,
      @maxSize: 20
      */
        fields?: NonNullable<CommonSearchWithEntityContext['search']>['fields'];
        /**
      Whether to use fuzzy search, which allows typos using a managed proximity algorithm.
      */
        fuzzy?: NonNullable<CommonSearchWithEntityContext['search']>['fuzzy'];
    };
    /**
    UTC offset or IANA time zone. Valid values are
    ISO 8601 UTC offsets, such as +02:00 or -06:00,
    and IANA time zone IDs, such as Europe/Rome.
  
    Affects the values returned by all filters and aggregations.
    You can override this behavior in a specific filter by providing
    timestamps that include a time zone. For example, `"2023-12-20T10:52:34.795Z"`.
    @maxLength: 50
    */
    timeZone?: CommonSearchWithEntityContext['timeZone'] | null;
};
/**
 * Retrieves the number of contacts that match the provided filter and search criteria.
 * @public
 * @permissionId CONTACTS.CONTACT_READ
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.CountContacts
 */
declare function countContacts(options?: CountContactsOptions): Promise<NonNullablePaths<CountContactsResponse, `count`, 0>>;
interface CountContactsOptions {
    /**
     * Filter object.
     *
     * Example:
     * `{ "filter": { "name.last": "Smith" } }`
     */
    filter?: Record<string, any> | null;
    /**
     * Plain text search for an exact match.
     *
     * Supported search properties are:
     *
     * - `name.full`
     * - `email.email`
     * - `phone.phone`
     * - `member_info.email`
     * - `member_info.profile_info.nickname`
     * - `company.name`
     * - `company.job_title`
     *
     * Max: 100 characters
     * @maxLength 100
     */
    search?: string | null;
}
/**
 * Generates an upload URL to allow external clients to upload a contact image.
 * To learn how external clients can use the generated upload URL in the response to upload a file,
 * see the [Upload API](https://dev.wix.com/docs/api-reference/assets/media/media-manager/files/upload-api) article.
 *
 * You can call [Get Contact Image Upload Status](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/get-contact-image-upload-status) to query the status of the image upload.
 * @param contactId - ID of the contact to generate an image upload URL for.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.GENERATE_IMAGE_UPLOAD_URL
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.GenerateContactImageUploadUrl
 */
declare function generateContactImageUploadUrl(contactId: string, options?: GenerateContactImageUploadUrlOptions): Promise<NonNullablePaths<GenerateContactImageUploadUrlResponse, `uploadUrl` | `imageUploadId`, 0> & {
    __applicationErrorsType?: GenerateContactImageUploadUrlApplicationErrors;
}>;
interface GenerateContactImageUploadUrlOptions {
    /**
     * MIME type. Must be one of: `image/png`, `image/jpeg`.
     *
     * Default: `image/png`
     * @maxLength 100
     */
    mimeType?: string | null;
}
/**
 * Adds an additional email to a contact.
 *
 * The contact must have a main email (`email.email`) before an additional email can be added.
 *
 * Each time the contact is updated, `revision` increments by 1. The current `revision` must be passed. This ensures you're working with the latest contact and prevents unintended overwrites.
 * @param contactId - ID of the contact to add an email to.
 * @param email - Email to add.
 * @public
 * @requiredField contactId
 * @requiredField email
 * @requiredField options
 * @requiredField options.revision
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.AddContactAdditionalEmail
 */
declare function addContactAdditionalEmail(contactId: string, email: AdditionalEmail, options: NonNullablePaths<AddContactAdditionalEmailOptions, `revision`, 0>): Promise<NonNullablePaths<AddContactAdditionalEmailResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: AddContactAdditionalEmailApplicationErrors;
}>;
interface AddContactAdditionalEmailOptions {
    /** Contact revision number. */
    revision: string | null;
}
/**
 * Adds an additional phone to a contact.
 *
 * The contact must have a main phone (`phone.phone`) before an additional phone can be added. If the contact already has the same phone number with the same tag, the request is ignored.
 *
 * Each time the contact is updated, `revision` increments by 1. The current `revision` must be passed. This ensures you're working with the latest contact and prevents unintended overwrites.
 * @param contactId - ID of the contact to add a phone to.
 * @param phone - Phone to add.
 * @public
 * @requiredField contactId
 * @requiredField options
 * @requiredField options.revision
 * @requiredField phone
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.AddContactAdditionalPhone
 */
declare function addContactAdditionalPhone(contactId: string, phone: AdditionalPhone, options: NonNullablePaths<AddContactAdditionalPhoneOptions, `revision`, 0>): Promise<NonNullablePaths<AddContactAdditionalPhoneResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: AddContactAdditionalPhoneApplicationErrors;
}>;
interface AddContactAdditionalPhoneOptions {
    /** Contact revision number. */
    revision: string | null;
}
/**
 * Adds an address to a contact.
 *
 * The address is appended to the contact's list of addresses. If the contact already has the same address with the same tag, the request is ignored.
 *
 * Each time the contact is updated, `revision` increments by 1. The current `revision` must be passed. This ensures you're working with the latest contact and prevents unintended overwrites.
 * @param contactId - ID of the contact to add an address to.
 * @param address - Address to add.
 * @public
 * @requiredField address
 * @requiredField contactId
 * @requiredField options
 * @requiredField options.revision
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.AddContactAddress
 */
declare function addContactAddress(contactId: string, address: ContactAddress, options: NonNullablePaths<AddContactAddressOptions, `revision`, 0>): Promise<NonNullablePaths<AddContactAddressResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: AddContactAddressApplicationErrors;
}>;
interface AddContactAddressOptions {
    /** Contact revision number. */
    revision: string | null;
}
/**
 * Deletes an address from a contact by address ID.
 *
 * Each time the contact is updated, `revision` increments by 1. The current `revision` must be passed. This ensures you're working with the latest contact and prevents unintended overwrites.
 * @param revision - Contact revision number.
 * @public
 * @requiredField identifiers
 * @requiredField identifiers.addressId
 * @requiredField identifiers.contactId
 * @requiredField revision
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.DeleteContactAddress
 */
declare function deleteContactAddress(identifiers: NonNullablePaths<DeleteContactAddressIdentifiers, `addressId` | `contactId`, 0>, revision: string): Promise<NonNullablePaths<DeleteContactAddressResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: DeleteContactAddressApplicationErrors;
}>;
interface DeleteContactAddressIdentifiers {
    /**
     * ID of the contact to delete the address from.
     * @format GUID
     */
    contactId: string;
    /**
     * ID of the address to delete.
     * @format GUID
     */
    addressId: string;
}
/**
 * Updates an address on a contact.
 *
 * Each time the contact is updated, `revision` increments by 1. The current `revision` must be passed. This ensures you're working with the latest contact and prevents unintended overwrites.
 * @public
 * @requiredField address
 * @requiredField identifiers
 * @requiredField identifiers.addressId
 * @requiredField identifiers.contactId
 * @requiredField options
 * @requiredField options.revision
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.UpdateContactAddress
 */
declare function updateContactAddress(identifiers: NonNullablePaths<UpdateContactAddressIdentifiers, `addressId` | `contactId`, 0>, address: UpdateContactAddress, options: NonNullablePaths<UpdateContactAddressOptions, `revision`, 0>): Promise<NonNullablePaths<UpdateContactAddressResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5> & {
    __applicationErrorsType?: UpdateContactAddressApplicationErrors;
}>;
interface UpdateContactAddressIdentifiers {
    /**
     * ID of the contact to update the address on.
     * @format GUID
     */
    contactId: string;
    /**
     * Address ID.
     * @format GUID
     */
    addressId?: string | null;
}
interface UpdateContactAddress {
    /**
     * Address type classification. Helps categorize the address purpose.
     *
     * Default: `OTHER`
     */
    tag?: AddressTagWithLiterals;
    /**
     * Physical address information including street, city, country, and postal code.
     * Contains structured address data following common address formatting standards.
     */
    address?: Address;
    /**
     * Whether this address is the contact's default address.
     * Only one address can be designated as default per contact.
     * If no address is explicitly set as default, the first address automatically becomes the default.
     */
    defaultAddress?: boolean | null;
    /**
     * Address ID.
     * @format GUID
     */
    _id?: string | null;
    /** Address recipient information for delivery purposes. */
    recipient?: Recipient;
}
interface UpdateContactAddressOptions {
    /** Contact revision number. */
    revision: string | null;
}
/**
 * Creates multiple contacts in a single request. Works synchronously.
 *
 * When you provide an image URL for a contact, the image is uploaded in the background and the returned contact temporarily has no image. Once the upload succeeds, the contact's image is updated.
 *
 * To create a single contact, call [Create Contact](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/create-contact).
 * @param contacts - Contacts to create.
 * @public
 * @requiredField contacts
 * @permissionId CONTACTS.CONTACT_CREATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkCreateContacts
 */
declare function bulkCreateContacts(contacts: Contact[], options?: BulkCreateContactsOptions): Promise<NonNullablePaths<BulkCreateContactsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.email.email` | `results.${number}.item.email.deliverabilityStatus` | `results.${number}.item.phone.tag` | `results.${number}.item.phone.phone` | `results.${number}.item.phone.deliverabilityStatus` | `results.${number}.item.memberInfo.status` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkCreateContactsOptions {
    /** Whether to include the created contacts in the response. */
    returnEntity?: boolean;
    /** Whether to create the contact even if it duplicates an existing contact. */
    allowDuplicates?: boolean;
}
/**
 * Updates multiple contacts in a single request. Works synchronously.
 *
 * When you provide an image URL for a contact, the image is uploaded in the background and the returned contact temporarily keeps its previous image. Once the upload succeeds, the contact's image is updated.
 *
 * `externalId` can be set only once. If it already has a value, it can't be updated.
 *
 * To update a single contact, call [Update Contact](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/update-contact).
 * @param contacts - Contacts to update.
 * @public
 * @requiredField contacts
 * @requiredField contacts.contact._id
 * @requiredField contacts.contact.revision
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkUpdateContacts
 */
declare function bulkUpdateContacts(contacts: NonNullablePaths<MaskedContact, `contact._id` | `contact.revision`, 1>[], options?: BulkUpdateContactsOptions): Promise<NonNullablePaths<BulkUpdateContactsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.email.email` | `results.${number}.item.email.deliverabilityStatus` | `results.${number}.item.phone.tag` | `results.${number}.item.phone.phone` | `results.${number}.item.phone.deliverabilityStatus` | `results.${number}.item.memberInfo.status` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkUpdateContactsOptions {
    /** Whether to include the updated contacts in the response. */
    returnEntity?: boolean;
    /** Whether to update the contact even if it becomes a duplicate of another contact. */
    allowDuplicates?: boolean;
}
/**
 * Adds or removes tags on multiple contacts, specified by contact ID. Works synchronously.
 *
 * Specify which tags to assign and which to unassign. A tag that appears in both the list to assign and the list to unassign is assigned. At least one tag must be specified to assign or unassign.
 *
 * To update tags for a single contact, call [Update Contact Tags](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/update-contact-tags).
 * @param contactIds - IDs of the contacts to add or remove tags on.
 * @public
 * @requiredField contactIds
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkUpdateContactTags
 */
declare function bulkUpdateContactTags(contactIds: string[], options?: BulkUpdateContactTagsOptions): Promise<NonNullablePaths<BulkUpdateContactTagsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.email.email` | `results.${number}.item.email.deliverabilityStatus` | `results.${number}.item.phone.tag` | `results.${number}.item.phone.phone` | `results.${number}.item.phone.deliverabilityStatus` | `results.${number}.item.memberInfo.status` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4> & {
    __applicationErrorsType?: BulkUpdateContactTagsApplicationErrors;
}>;
interface BulkUpdateContactTagsOptions {
    /** Whether to include the updated contacts in the response. */
    returnEntity?: boolean;
    /** Tags to assign. */
    assignTags?: PrivateTags;
    /** Tags to unassign. */
    unassignTags?: PrivateTags;
}
/**
 * Adds or removes tags on multiple contacts that match the provided filter. Works asynchronously. An empty filter matches all contacts.
 *
 * Specify which tags to assign and which to unassign. A tag that appears in both the list to assign and the list to unassign is assigned. At least one tag must be specified to assign or unassign.
 *
 * To check the status of the bulk update, pass the returned `jobId` to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job).
 *
 * To update tags for a single contact, call [Update Contact Tags](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/update-contact-tags).
 * @param filter - Filter object. See documentation [here](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language#filters).
 * @public
 * @requiredField filter
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkUpdateContactTagsByFilter
 */
declare function bulkUpdateContactTagsByFilter(filter: Record<string, any>, options?: BulkUpdateContactTagsByFilterOptions): Promise<NonNullablePaths<BulkUpdateContactTagsByFilterResponse, `jobId`, 0> & {
    __applicationErrorsType?: BulkUpdateContactTagsByFilterApplicationErrors;
}>;
interface BulkUpdateContactTagsByFilterOptions {
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
    /** Tags to assign to the matching contacts. */
    assignTags?: PrivateTags;
    /** Tags to unassign from the matching contacts. */
    unassignTags?: PrivateTags;
}
/**
 * Creates or updates multiple contacts in a single request. Works synchronously.
 *
 * Each contact is matched against existing contacts using the [contact matching](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/matching-contacts) logic. If a match is found, the contact is updated. Otherwise, a new contact is created.
 *
 * When a contact is updated, `upsertMode` controls how:
 * - `OVERWRITE` (default): Replaces existing field values.
 * - `APPEND`: Sets a non-array field only if it doesn't already have a value, and appends to array fields.
 * - `OVERWRITE_APPEND_ARRAYS`: Overwrites non-array fields and appends to array fields. `email`/`additionalEmails` and `phone`/`additionalPhones` are each treated as a single array field: the incoming value is set as `email`/`phone` only if none exists yet, otherwise it's appended to `additionalEmails`/`additionalPhones`.
 *
 * In `APPEND` or `OVERWRITE_APPEND_ARRAYS` mode, the default address flag is ignored unless the contact has no existing addresses.
 *
 * When you provide an image URL and the upsert mode allows the image to be updated, the image is uploaded in the background. The returned contact temporarily has no image if it was created, or its previous image if it was updated. Once the upload succeeds, the contact's image is updated.
 *
 * To create or update a single contact, call [Upsert Contact](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/upsert-contact).
 * @param contacts - Contacts to create or update.
 * @public
 * @requiredField contacts
 * @permissionId CONTACTS.CONTACT_UPSERT
 * @permissionId CONTACTS.CONTACT_MEMBER_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkUpsertContacts
 */
declare function bulkUpsertContacts(contacts: BulkUpsertContactsRequestMaskedContact[], options?: BulkUpsertContactsOptions): Promise<NonNullablePaths<BulkUpsertContactsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.email.email` | `results.${number}.item.email.deliverabilityStatus` | `results.${number}.item.phone.tag` | `results.${number}.item.phone.phone` | `results.${number}.item.phone.deliverabilityStatus` | `results.${number}.item.memberInfo.status` | `results.${number}.action` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkUpsertContactsOptions {
    /** Whether to include the upserted contacts in the response. */
    returnEntity?: boolean;
    /** Mode that determines how an existing contact is updated when a match is found. See the endpoint description for the supported modes. */
    upsertMode?: UpsertModeWithLiterals;
    /** Whether to update a contact even if it's a site member. */
    updateMember?: boolean | null;
}
/**
 * Deletes multiple contacts in a single request. Works synchronously.
 *
 * Deleted contacts are moved to the trash bin and can be restored within 90 days by calling [Restore Contact From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/restore-contact-from-trash-bin) or [Bulk Restore Contacts From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-restore-contacts-from-trash-bin).
 *
 * To delete a single contact, call [Delete Contact](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/delete-contact).
 * @param contactIds - IDs of the contacts to delete.
 * @public
 * @requiredField contactIds
 * @permissionId CONTACTS.CONTACT_DELETE
 * @permissionId MEMBERS.MEMBER_DELETE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkDeleteContacts
 */
declare function bulkDeleteContacts(contactIds: string[], options?: BulkDeleteContactsOptions): Promise<NonNullablePaths<BulkDeleteContactsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4> & {
    __applicationErrorsType?: BulkDeleteContactsApplicationErrors;
}>;
interface BulkDeleteContactsOptions {
    /**
     * Whether to delete the associated site member for any contact that's a site member. If `true`, the site member is also deleted.
     *
     * Default: `false`
     */
    deleteMembers?: boolean | null;
}
/**
 * Deletes multiple contacts that match the provided filter. Works asynchronously.
 *
 * All contacts that meet the specified `filter` and `search` criteria are deleted. The request must contain a `filter` value, a `search` value, or both. To preview which contacts match before deleting, call [Search Contacts](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/search-contacts) with the same criteria.
 *
 * To check the status of the bulk deletion, pass the returned `jobId` to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job).
 *
 * Deleted contacts are moved to the trash bin and can be restored within 90 days by calling [Restore Contact From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/restore-contact-from-trash-bin) or [Bulk Restore Contacts From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-restore-contacts-from-trash-bin).
 *
 * Individual contacts can fail during processing. These failures appear in the job's results with one of the following error codes:
 * - `CANNOT_DELETE_SITE_MEMBERS`: Contact is a site member and can't be deleted. The site member must be deleted first.
 * - `CANNOT_DELETE_CONTACT_WITH_BILLING_SUBSCRIPTION`: Contact has an active billing subscription and can't be deleted.
 * - `CANNOT_DELETE_WIX_USERS_MEMBER`: Site member can't be deleted because it's the owner or a contributor.
 * - `FAILED_DELETE_CONTACT_AFTER_MEMBER_DELETION`: Site member was deleted, but the contact was not.
 * - `FAILED_DELETE_CONTACT`: Contact couldn't be deleted.
 * @param filter - Filter object.
 *
 * Example:
 * `{ "filter": { "name.last": "Smith" } }`
 * @public
 * @requiredField filter
 * @permissionId CONTACTS.CONTACT_DELETE
 * @permissionId MEMBERS.MEMBER_DELETE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkDeleteContactsByFilter
 */
declare function bulkDeleteContactsByFilter(filter: Record<string, any>, options?: BulkDeleteContactsByFilterOptions): Promise<NonNullablePaths<BulkDeleteContactsByFilterResponse, `jobId`, 0> & {
    __applicationErrorsType?: BulkDeleteContactsByFilterApplicationErrors;
}>;
interface BulkDeleteContactsByFilterOptions {
    /** Free text to match in searchable fields. */
    search?: SearchDetails;
    /**
     * Whether to delete the associated site member for any contact that's a site member. If `true`, the site member is also deleted.
     *
     * Default: `false`
     */
    deleteMembers?: boolean | null;
}
/**
 * Permanently deletes a contact from the trash bin.
 *
 * After removal, the contact can no longer be restored.
 * @param contactId - ID of the contact to permanently delete from the trash bin.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.CONTACT_REMOVE_FROM_TRASHBIN
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.RemoveContactFromTrashBin
 */
declare function removeContactFromTrashBin(contactId: string): Promise<void>;
/**
 * Restores a soft-deleted contact from the trash bin.
 *
 * To restore multiple contacts in a single request, call [Bulk Restore Contacts From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/bulk-restore-contacts-from-trash-bin).
 * @param contactId - ID of the contact to restore from the trash bin.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.CONTACT_RESTORE_FROM_TRASHBIN
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.RestoreContactFromTrashBin
 */
declare function restoreContactFromTrashBin(contactId: string): Promise<NonNullablePaths<RestoreContactFromTrashBinResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5>>;
/**
 * Restores multiple soft-deleted contacts from the trash bin.
 *
 * To restore a single contact, call [Restore Contact From Trash Bin](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/restore-contact-from-trash-bin).
 * @public
 * @permissionId CONTACTS.CONTACT_RESTORE_FROM_TRASHBIN
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkRestoreContactsFromTrashBin
 */
declare function bulkRestoreContactsFromTrashBin(options?: BulkRestoreContactsFromTrashBinOptions): Promise<NonNullablePaths<BulkRestoreContactsFromTrashBinResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `results.${number}.item.email.email` | `results.${number}.item.email.deliverabilityStatus` | `results.${number}.item.phone.tag` | `results.${number}.item.phone.phone` | `results.${number}.item.phone.deliverabilityStatus` | `results.${number}.item.memberInfo.status` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkRestoreContactsFromTrashBinOptions {
    /**
     * IDs of the contacts to restore from the trash bin.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    contactIds?: string[];
    /** Whether to include the restored contacts in the response. */
    returnEntity?: boolean;
}
/**
 * Retrieves a soft-deleted contact from the trash bin by ID.
 * @param contactId - ID of the contact to retrieve from the trash bin.
 * @public
 * @requiredField contactId
 * @permissionId CONTACTS.CONTACT_READ_FROM_TRASHBIN
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.GetDeletedContact
 */
declare function getDeletedContact(contactId: string): Promise<NonNullablePaths<GetDeletedContactResponse, `contact.email.email` | `contact.email.subscriptionStatus.status` | `contact.email.deliverabilityStatus` | `contact.additionalEmails` | `contact.additionalEmails.${number}.email` | `contact.phone.tag` | `contact.phone.phone` | `contact.phone.deliverabilityStatus` | `contact.additionalPhones` | `contact.additionalPhones.${number}.tag` | `contact.additionalPhones.${number}.phone` | `contact.addresses` | `contact.addresses.${number}.tag` | `contact.addresses.${number}.address.streetAddress.number` | `contact.addresses.${number}.address.streetAddress.name` | `contact.addresses.${number}.address.streetAddress.apt` | `contact.locationIds` | `contact.segmentIds` | `contact.memberInfo.status` | `contact.memberInfo.profileInfo.privacyStatus` | `contact.memberInfo.userInfo.userId` | `contact.memberInfo.userInfo.role` | `contact.memberInfo.sessionInfo.mobileAppNames` | `contact.memberInfo.groupInfo.groupIds` | `contact.tags.privateTags.tagIds`, 5>>;
/**
 * Retrieves a list of soft-deleted contacts from the trash bin, filtered by the provided identifiers and deletion date range.
 * @public
 * @permissionId CONTACTS.CONTACT_READ_FROM_TRASHBIN
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.ListDeletedContacts
 */
declare function listDeletedContacts(options?: ListDeletedContactsOptions): Promise<NonNullablePaths<ListDeletedContactsResponse, `contacts` | `contacts.${number}.email.email` | `contacts.${number}.email.subscriptionStatus.status` | `contacts.${number}.email.deliverabilityStatus` | `contacts.${number}.phone.tag` | `contacts.${number}.phone.phone` | `contacts.${number}.phone.deliverabilityStatus` | `contacts.${number}.memberInfo.status` | `contacts.${number}.memberInfo.profileInfo.privacyStatus` | `contacts.${number}.memberInfo.userInfo.userId` | `contacts.${number}.memberInfo.userInfo.role`, 4>>;
interface ListDeletedContactsOptions {
    /**
     * External ID.
     * @maxLength 100
     */
    externalId?: string | null;
    /**
     * Email address.
     * @format EMAIL
     * @maxLength 320
     */
    email?: string | null;
    /**
     * Phone number.
     * @minLength 1
     * @maxLength 50
     */
    phone?: string | null;
    /**
     * Contact IDs.
     * @maxSize 100
     * @format GUID
     */
    contactIds?: string[];
    /** Filters to contacts deleted on or after this date and time. */
    deletedFromDate?: Date | null;
    /** Filters to contacts deleted on or before this date and time. */
    deletedToDate?: Date | null;
    /** Cursor paging. */
    paging?: CursorPaging;
}
/**
 * Retrieves the status of the contact image upload process.
 * @public
 * @requiredField identifiers
 * @requiredField identifiers.contactId
 * @requiredField identifiers.imageUploadId
 * @permissionId CONTACTS.CONTACT_IMAGE_STATUS_READ
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.GetContactImageUploadStatus
 */
declare function getContactImageUploadStatus(identifiers: NonNullablePaths<GetContactImageUploadStatusIdentifiers, `contactId` | `imageUploadId`, 0>): Promise<NonNullablePaths<GetContactImageUploadStatusResponse, `status`, 0> & {
    __applicationErrorsType?: GetContactImageUploadStatusApplicationErrors;
}>;
interface GetContactImageUploadStatusIdentifiers {
    /**
     * ID of the contact whose image upload status to retrieve.
     * @format GUID
     */
    contactId: string;
    /**
     * The upload ID of the image upload process. Returned when an image URL is provided while creating or updating a contact, or from [Generate Contact Image Upload Url](https://dev.wix.com/docs/api-reference/crm/members-contacts/contacts/contacts-v5/generate-contact-image-upload-url).
     * @maxLength 100
     */
    imageUploadId: string;
}
/**
 * Adds a country code to the main phone of multiple contacts that match the provided filter. Works asynchronously.
 *
 * A contact's main phone is changed only if adding the country code produces a valid phone number. Otherwise, the contact is left unchanged.
 *
 * To check the status of the bulk update, pass the returned `jobId` to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job).
 * @public
 * @requiredField options.countryCode
 * @permissionId CONTACTS.CONTACT_UPDATE
 * @applicableIdentity APP
 * @fqn wix.contacts.v5.Contacts.BulkUpdatePhoneCountryCodeByFilter
 */
declare function bulkUpdatePhoneCountryCodeByFilter(options?: NonNullablePaths<BulkUpdatePhoneCountryCodeByFilterOptions, `countryCode`, 0>): Promise<NonNullablePaths<BulkUpdatePhoneCountryCodeByFilterResponse, `jobId`, 0>>;
interface BulkUpdatePhoneCountryCodeByFilterOptions {
    /**
     * A filter for the contacts that the country code will be applied to.
     *
     * Example:
     * `{ "filter": { "name.last": "Smith" } }`
     */
    filter?: Record<string, any> | null;
    /** Free text to match in searchable fields for the contacts that the country code will be applied to. */
    search?: SearchDetails;
    /**
     * [ISO-3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2) country code to apply.
     *
     * Applied only to contacts whose phone number isn't already in [ITU E.164 international](https://www.itu.int/rec/T-REC-E.164/) format. If the phone number still can't be converted to E.164 format after applying the country code, that contact fails with `PHONE_CANNOT_BE_FIXED_WITH_COUNTRY_CODE`.
     * @format COUNTRY
     */
    countryCode: string;
}

export { type MaskedContact as $, type AdditionalEmail as A, type BulkUpdateContactSubscriptionsOptions as B, type Contact as C, type DeleteContactOptions as D, type AdditionalPhone as E, type FindMatchingContactsOptions as F, type GenerateContactImageUploadUrlOptions as G, type AddContactAdditionalPhoneOptions as H, type AddContactAdditionalPhoneResponse as I, type AddContactAdditionalPhoneApplicationErrors as J, type ContactAddress as K, type AddContactAddressOptions as L, type MergeContactsOptions as M, type AddContactAddressResponse as N, type AddContactAddressApplicationErrors as O, type DeleteContactAddressIdentifiers as P, type DeleteContactAddressResponse as Q, type DeleteContactAddressApplicationErrors as R, type SearchContactsOptions as S, type UpdateContactAddressIdentifiers as T, type UpdateContact as U, type UpdateContactAddress as V, type UpdateContactAddressOptions as W, type UpdateContactAddressResponse as X, type UpdateContactAddressApplicationErrors as Y, type BulkCreateContactsOptions as Z, type BulkCreateContactsResponse as _, type BulkUpdateContactSubscriptionsResponse as a, type AddressStreetOneOf as a$, type BulkUpdateContactsOptions as a0, type BulkUpdateContactsResponse as a1, type BulkUpdateContactTagsOptions as a2, type BulkUpdateContactTagsResponse as a3, type BulkUpdateContactTagsApplicationErrors as a4, type BulkUpdateContactTagsByFilterOptions as a5, type BulkUpdateContactTagsByFilterResponse as a6, type BulkUpdateContactTagsByFilterApplicationErrors as a7, type BulkUpsertContactsRequestMaskedContact as a8, type BulkUpsertContactsOptions as a9, SubdivisionType as aA, MemberStatus as aB, PrivacyStatus as aC, Role as aD, WebhookIdentityType as aE, Mode as aF, V5SubscriptionStatus as aG, MatchType as aH, UpsertMode as aI, UpsertContactResponseAction as aJ, SortOrder as aK, SortType as aL, SortDirection as aM, MissingValues as aN, ScalarType as aO, NestedAggregationType as aP, Interval as aQ, AggregationType as aR, NameFormat as aS, Action as aT, QueryContactsByOffsetInternalRequestNameFormat as aU, ImageUploadStatus as aV, type ContactName as aW, type ContactEmail as aX, type ContactSubscription as aY, type ContactPhone as aZ, type Address as a_, type BulkUpsertContactsResponse as aa, type BulkDeleteContactsOptions as ab, type BulkDeleteContactsResponse as ac, type BulkDeleteContactsApplicationErrors as ad, type BulkDeleteContactsByFilterOptions as ae, type BulkDeleteContactsByFilterResponse as af, type BulkDeleteContactsByFilterApplicationErrors as ag, type RestoreContactFromTrashBinResponse as ah, type BulkRestoreContactsFromTrashBinOptions as ai, type BulkRestoreContactsFromTrashBinResponse as aj, type GetDeletedContactResponse as ak, type ListDeletedContactsOptions as al, type ListDeletedContactsResponse as am, type GetContactImageUploadStatusIdentifiers as an, type GetContactImageUploadStatusResponse as ao, type GetContactImageUploadStatusApplicationErrors as ap, type BulkUpdatePhoneCountryCodeByFilterOptions as aq, type BulkUpdatePhoneCountryCodeByFilterResponse as ar, type ContactsQueryBuilder as as, type ContactQuery as at, typedQueryContacts as au, SubscriptionStatus as av, EmailDeliverabilityStatus as aw, PhoneTag as ax, PhoneDeliverabilityStatus as ay, AddressTag as az, type BulkUpdateContactSubscriptionsApplicationErrors as b, type CursorPaging as b$, type StreetAddress as b0, type AddressLocation as b1, type Subdivision as b2, type StandardDetails as b3, type Recipient as b4, type ContactCompany as b5, type ContactSource as b6, type ContactActivity as b7, type MemberInfo as b8, type ProfileInfo as b9, type BulkUpdateContactSubscriptionsRequest as bA, type SearchDetails as bB, type ReindexEntityForFQDN as bC, type ReindexEntityForFQDNActionOneOf as bD, type Index as bE, type Delete as bF, type ReportBatchCompleted as bG, type BulkIndex as bH, type BulkDelete as bI, type CreateContactRequest as bJ, type CreateContactResponse as bK, type DuplicateContactExists as bL, type GetContactRequest as bM, type GetContactResponse as bN, type UpdateContactRequest as bO, type UpdateContactResponse as bP, type UpsertContactRequest as bQ, type FailedMemberUpdate as bR, type UpdateContactTagsRequest as bS, type MergeContactsRequest as bT, type DeleteContactRequest as bU, type DeleteContactResponse as bV, type FindMatchingContactsRequest as bW, type QueryContactsRequest as bX, type CursorQuery as bY, type CursorQueryPagingMethodOneOf as bZ, type Sorting as b_, type UserInfo as ba, type SessionInfo as bb, type GroupInfo as bc, type PrivateTags as bd, type TagList as be, type ExtendedFields as bf, type ReindexMessage as bg, type ReindexMessageActionOneOf as bh, type Upsert as bi, type ReindexMessageDelete as bj, type Schema as bk, type ContactMerged as bl, type ContactEmailSubscriptionUpdated as bm, type ContactPhoneSubscriptionUpdated as bn, type ContactImageUploaded as bo, type DomainEvent as bp, type DomainEventBodyOneOf as bq, type EntityCreatedEvent as br, type RestoreInfo as bs, type EntityUpdatedEvent as bt, type EntityDeletedEvent as bu, type ActionEvent as bv, type MessageEnvelope as bw, type IdentificationData as bx, type IdentificationDataIdOneOf as by, type AccountInfo as bz, type CreateContactOptions as c, type ListDeletedContactsRequest as c$, type QueryContactsResponse as c0, type CursorPagingMetadata as c1, type Cursors as c2, type SearchContactsRequest as c3, type CursorSearch as c4, type CursorSearchPagingMethodOneOf as c5, type Aggregation as c6, type AggregationKindOneOf as c7, type RangeBucket as c8, type IncludeMissingValuesOptions as c9, type GenerateContactImageUploadUrlRequest as cA, type AddContactAdditionalEmailRequest as cB, type AddContactAdditionalPhoneRequest as cC, type AddContactAddressRequest as cD, type DeleteContactAddressRequest as cE, type UpdateContactAddressRequest as cF, type BulkCreateContactsRequest as cG, type ItemMetadata as cH, type ApplicationError as cI, type BulkContactResult as cJ, type BulkActionMetadata as cK, type BulkUpdateContactsRequest as cL, type BulkUpdateContactsResponseBulkContactResult as cM, type BulkUpdateContactTagsRequest as cN, type BulkUpdateContactTagsResult as cO, type BulkUpdateContactTagsByFilterRequest as cP, type BulkUpsertContactsRequest as cQ, type BulkUpsertContactsResponseBulkContactResult as cR, type BulkDeleteContactsRequest as cS, type BulkDeleteContactsResponseBulkContactResult as cT, type BulkDeleteContactsByFilterRequest as cU, type RemoveContactFromTrashBinRequest as cV, type RemoveContactFromTrashBinResponse as cW, type RestoreContactFromTrashBinRequest as cX, type BulkRestoreContactsFromTrashBinRequest as cY, type BulkRestoreContactsFromTrashBinResponseBulkContactResult as cZ, type GetDeletedContactRequest as c_, type ValueAggregation as ca, type ValueAggregationOptionsOneOf as cb, type RangeAggregation as cc, type ScalarAggregation as cd, type DateHistogramAggregation as ce, type NestedAggregationItem as cf, type NestedAggregationItemKindOneOf as cg, type NestedAggregation as ch, type AggregationData as ci, type ValueAggregationResult as cj, type RangeAggregationResult as ck, type ValueResults as cl, type RangeResults as cm, type AggregationResultsScalarResult as cn, type ValueResult as co, type RangeResult as cp, type ScalarResult as cq, type NestedResultValue as cr, type NestedResultValueResultOneOf as cs, type Results as ct, type DateHistogramResult as cu, type DateHistogramResults as cv, type NestedResults as cw, type AggregationResults as cx, type AggregationResultsResultOneOf as cy, type CountContactsRequest as cz, type CreateContactApplicationErrors as d, deleteContactAddress as d$, type BulkAddSegmentToContactsRequest as d0, type BulkAddSegmentToContactsResponse as d1, type BulkRemoveSegmentFromContactsRequest as d2, type BulkRemoveSegmentFromContactsResponse as d3, type QueryContactsByOffsetInternalRequest as d4, type QueryV2 as d5, type QueryV2PagingMethodOneOf as d6, type Paging as d7, type QueryContactsByOffsetInternalResponse as d8, type PagingMetadataV2 as d9, type SortDirectionWithLiterals as dA, type MissingValuesWithLiterals as dB, type ScalarTypeWithLiterals as dC, type NestedAggregationTypeWithLiterals as dD, type IntervalWithLiterals as dE, type AggregationTypeWithLiterals as dF, type NameFormatWithLiterals as dG, type ActionWithLiterals as dH, type QueryContactsByOffsetInternalRequestNameFormatWithLiterals as dI, type ImageUploadStatusWithLiterals as dJ, type CommonQueryWithEntityContext as dK, type CommonSearchWithEntityContext as dL, bulkUpdateContactSubscriptions as dM, createContact as dN, getContact as dO, updateContact as dP, upsertContact as dQ, updateContactTags as dR, mergeContacts as dS, deleteContact as dT, findMatchingContacts as dU, queryContacts as dV, countContacts as dW, generateContactImageUploadUrl as dX, addContactAdditionalEmail as dY, addContactAdditionalPhone as dZ, addContactAddress as d_, type GetContactImageUploadStatusRequest as da, type BulkUpdatePhoneCountryCodeByFilterRequest as db, type Empty as dc, type MediaFileUploadedEvent as dd, type ContactsQueryResult as de, type ContactQuerySpec as df, type ContactSearchSpec as dg, utils as dh, type SubscriptionStatusWithLiterals as di, type EmailDeliverabilityStatusWithLiterals as dj, type PhoneTagWithLiterals as dk, type PhoneDeliverabilityStatusWithLiterals as dl, type AddressTagWithLiterals as dm, type SubdivisionTypeWithLiterals as dn, type MemberStatusWithLiterals as dp, type PrivacyStatusWithLiterals as dq, type RoleWithLiterals as dr, type WebhookIdentityTypeWithLiterals as ds, type ModeWithLiterals as dt, type V5SubscriptionStatusWithLiterals as du, type MatchTypeWithLiterals as dv, type UpsertModeWithLiterals as dw, type UpsertContactResponseActionWithLiterals as dx, type SortOrderWithLiterals as dy, type SortTypeWithLiterals as dz, type UpdateContactOptions as e, updateContactAddress as e0, bulkCreateContacts as e1, bulkUpdateContacts as e2, bulkUpdateContactTags as e3, bulkUpdateContactTagsByFilter as e4, bulkUpsertContacts as e5, bulkDeleteContacts as e6, bulkDeleteContactsByFilter as e7, removeContactFromTrashBin as e8, restoreContactFromTrashBin as e9, bulkRestoreContactsFromTrashBin as ea, getDeletedContact as eb, listDeletedContacts as ec, getContactImageUploadStatus as ed, bulkUpdatePhoneCountryCodeByFilter as ee, type UpdateContactApplicationErrors as f, type UpsertContactOptions as g, type UpsertContactResponse as h, type UpsertContactApplicationErrors as i, type UpdateContactTagsOptions as j, type UpdateContactTagsResponse as k, type UpdateContactTagsApplicationErrors as l, type MergeContactsResponse as m, type MergeContactsApplicationErrors as n, type DeleteContactApplicationErrors as o, type FindMatchingContactsResponse as p, type FindMatchingContactsApplicationErrors as q, type ContactSearch as r, type SearchContactsResponse as s, type CountContactsOptions as t, type CountContactsResponse as u, type GenerateContactImageUploadUrlResponse as v, type GenerateContactImageUploadUrlApplicationErrors as w, type AddContactAdditionalEmailOptions as x, type AddContactAdditionalEmailResponse as y, type AddContactAdditionalEmailApplicationErrors as z };
