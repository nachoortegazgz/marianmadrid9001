/**
 * The Consent Policy API manages site visitor cookie preferences and 3rd-party data transfers for GDPR and CCPA compliance.
 *
 * >**Note:** The APIs in `wix-window-frontend.ConsentPolicy` can only be used in frontend code.
 *
 * The consent policy helps you comply with [GDPR](https://support.wix.com/en/article/preparing-your-wix-site-for-the-gdpr) and [CCPA](https://support.wix.com/en/article/preparing-your-wix-site-for-ccpa) regulations, and allows site visitors to
 * control their information.
 *
 * Set up the site consent policy and cookie consent banners in
 * a site's [Privacy Center](https://www.wix.com/my-account/site-selector/?buttonText=Open%20Privacy%20Center&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Fcookie-consent-settings-app?referralInfo=corvid-docs).
 *
 *  To use the ConsentPolicy API, import `{ consentPolicy }` from the `wix-window-frontend` module:
 *
 *    ```javascript
 *    import { consentPolicy } from 'wix-window-frontend';
 *    ```
 */
export interface ConsentPolicy {
    /**
     * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`getCurrentConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/get-current-consent-policy) method instead.
     *
     * > #### Migration Instructions
     * > If this method is already in your code, it will continue to work.
     * > To stay compatible with future changes, migrate to [`getCurrentConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/get-current-consent-policy).
     * >
     * > To migrate to the new method:
     * >
     * > 1. Add the new import statement:
     * >
     * >    ```javascript
     * >    import { consentPolicy } from "@wix/site";
     * >    ```
     * >
     * > 2. If you plan to migrate all methods that use `getCurrentConsentPolicy()`,
     * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
     * >
     * > 3. Look for any code that uses `consentPolicy.getCurrentConsentPolicy()`,
     * >    and replace it with the new import.
     * >    Update your code to work with the new `getCurrentConsentPolicy()`
     * >    call and response properties.
     * >
     * > 4. Test your changes to make sure your code behaves as expected.
     * >
     * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `getCurrentConsentPolicy()` method.
     * >
     *
     * Gets the site visitor's consent policy regarding allowed cookies and 3rd-party data transfers for GDPR or CCPA compliance.
     *
     * Retrieves the site visitor's consent policy details, including which cookies are allowed and whether data transfer to 3rd parties is permitted.
     *
     * @servicePath wix-window-frontend.ConsentPolicy.PolicyDetails
     */
    getCurrentConsentPolicy(): Promise<PolicyDetails>;
    /**
     * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`onConsentPolicyChanged()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/on-consent-policy-changed) method instead.
     *
     * > #### Migration Instructions
     * > If this method is already in your code, it will continue to work.
     * > To stay compatible with future changes, migrate to [`onConsentPolicyChanged()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/on-consent-policy-changed).
     * >
     * > To migrate to the new method:
     * >
     * > 1. Add the new import statement:
     * >
     * >    ```javascript
     * >    import { consentPolicy } from "@wix/site";
     * >    ```
     * >
     * > 2. If you plan to migrate all methods that use `onConsentPolicyChanged()`,
     * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
     * >
     * > 3. Look for any code that uses `consentPolicy.onConsentPolicyChanged()`,
     * >    and replace it with the new import.
     * >    Update your code to work with the new `onConsentPolicyChanged()`
     * >    call and response properties.
     * >
     * > 4. Test your changes to make sure your code behaves as expected.
     * >
     * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `onConsentPolicyChanged()` method.
     * >
     *
     * Triggered when a site visitor's consent policy was changed using `setConsentPolicy()` or reset using `resetConsentPolicy()`.
     *
     * Use the `onConsentPolicyChanged()` method for code you want to run after
     * the site visitor's current consent policy was changed using
     * `setConsentPolicy()` or reset using
     * `resetConsentPolicy()`.
     *
     *
     * Usually, you want to call the `onConsentPolicyChanged()` method in the **masterpage.js** file so that the `onConsentPolicyChanged()` event runs no matter which
     * page on a site is used to change the policy.
     * @param handler - The name of the method to run when the consent policy changes.
     * @requiredField handler
     * @servicePath wix-window-frontend.ConsentPolicy.ConsentPolicyChangedHandler
     */
    onConsentPolicyChanged(handler: ConsentPolicyChangedHandler): Promise<void>;
    /**
     * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`resetConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/reset-consent-policy) method instead.
     *
     * > #### Migration Instructions
     * > If this method is already in your code, it will continue to work.
     * > To stay compatible with future changes, migrate to [`resetConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/reset-consent-policy).
     * >
     * > To migrate to the new method:
     * >
     * > 1. Add the new import statement:
     * >
     * >    ```javascript
     * >    import { consentPolicy } from "@wix/site";
     * >    ```
     * >
     * > 2. If you plan to migrate all methods that use `resetConsentPolicy()`,
     * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
     * >
     * > 3. Look for any code that uses `consentPolicy.resetConsentPolicy()`,
     * >    and replace it with the new import.
     * >    Update your code to work with the new `resetConsentPolicy()`
     * >    call and response properties.
     * >
     * > 4. Test your changes to make sure your code behaves as expected.
     * >
     * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `resetConsentPolicy()` method.
     * >
     *
     * Removes the current policy from the site visitor's browser and resets the site visitor's consent policy to the default policy for the site.
     *
     * @returns Fulfilled - When the reset is complete.
     * Rejected - Error message.
     */
    resetConsentPolicy(): Promise<void>;
    /**
     * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`setConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/set-consent-policy) method instead.
     *
     * > #### Migration Instructions
     * > If this method is already in your code, it will continue to work.
     * > To stay compatible with future changes, migrate to [`setConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/set-consent-policy).
     * >
     * > To migrate to the new method:
     * >
     * > 1. Add the new import statement:
     * >
     * >    ```javascript
     * >    import { consentPolicy } from "@wix/site";
     * >    ```
     * >
     * > 2. If you plan to migrate all methods that use `setConsentPolicy()`,
     * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
     * >
     * > 3. Look for any code that uses `consentPolicy.setConsentPolicy()`,
     * >    and replace it with the new import.
     * >    Update your code to work with the new `setConsentPolicy()`
     * >    call and response properties.
     * >
     * > 4. Test your changes to make sure your code behaves as expected.
     * >
     * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `setConsentPolicy()` method.
     * >
     *
     * Sets the current site visitor's consent policy regarding allowed cookies and data transfer to 3rd parties, such as for GDPR or CCPA purposes.
     *
     * You can use the `onConsentPolicyChanged()` event to listen for
     * changes made when a site visitor changes their consent policy with `setConsentPolicy()`. Handle
     * the policy change accordingly in the event.
     * Changes to the consent policy take affect after the page is refreshed.
     * @param policy - An object representing the cookies of the site visitor's consent policy.
     * @requiredField policy
     * @servicePath wix-window-frontend.ConsentPolicy.Policy
     * @returns Fulfilled - All the details of a site visitor's consent policy, including if the current policy is the default site policy and the policy's allowed cookies.
     * Rejected - Error message.
     * @servicePath wix-window-frontend.ConsentPolicy.PolicyDetails
     */
    setConsentPolicy(policy: Policy): Promise<PolicyDetails>;
}
/**
 * The event that occurred when the consent policy changed.
 */
export interface ConsentPolicyChangedEvent {
}
/**
 * The current site visitor's consent policy settings.
 *
 * An object containing the consent policy cookie settings, by type, of the site visitor currently viewing the site.
 */
export interface Policy {
    /**
     * Consent for mandatory cookies for Wix websites, such as for security cookies. Wix places these cookies on the site visitor's device. These cookies don't require site visitor consent. Always `true`.
     * @requiredField essential
     */
    essential: boolean;
    /**
     * Consent for cookies placed on the site visitor's device that "remember" their user settings to improve user experience. For example, an indication that the user dismissed a popup. Default: `true`.
     * @requiredField functional
     */
    functional: boolean;
    /**
     * Consent for cookies used for analytics, such as Wix analytics, Google Analytics, Yandex Metrica, and so on. Default: `true`.
     * @requiredField analytics
     */
    analytics: boolean;
    /**
     * Consent for cookies used for advertising purposes. This includes 3rd-party scripts and pixels that may potentially place advertising cookies on the device. For example, Twitter page view and Facebook Pixel. Default: `true`.
     * @requiredField advertising
     */
    advertising: boolean;
    /**
     * Consent for a site visitor's personal data to be transferred to a 3rd party. For example, Google Analytics, Facebook Pixel, and FullStory. Default: `true`.
     * @requiredField dataToThirdParty
     */
    dataToThirdParty: boolean;
}
/**
 * The complete details of the current site visitor's consent policy.
 *
 * An object containing all the details about the consent policy of the site visitor currently viewing the site.
 */
export interface PolicyDetails {
    /**
     * Whether the current consent policy is the default one you set. If `true`, either the site visitor hasn't set a policy, or you've reset it to its default settings.
     * @requiredField defaultPolicy
     */
    defaultPolicy: boolean;
    /**
     * An object representing the site visitor's current consent policy.
     * @requiredField policy
     * @servicePath wix-window-frontend.ConsentPolicy.Policy
     */
    policy: Policy;
    /**
     * The date the consent policy was set, if a cookie defining the current policy exists in the browser. Otherwise, it's undefined.
     */
    createdDate?: Date;
}
/**
 * Method that runs when a site visitor's consent policy was changed using `setConsentPolicy()`.
 * @param event - The event that occurred when the consent policy changed.
 * @requiredField event
 * @servicePath wix-window-frontend.ConsentPolicy.ConsentPolicyChangedEvent
 */
export type ConsentPolicyChangedHandler = (event: ConsentPolicyChangedEvent) => void;
/**
 * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`getCurrentConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/get-current-consent-policy) method instead.
 *
 * > #### Migration Instructions
 * > If this method is already in your code, it will continue to work.
 * > To stay compatible with future changes, migrate to [`getCurrentConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/get-current-consent-policy).
 * >
 * > To migrate to the new method:
 * >
 * > 1. Add the new import statement:
 * >
 * >    ```javascript
 * >    import { consentPolicy } from "@wix/site";
 * >    ```
 * >
 * > 2. If you plan to migrate all methods that use `getCurrentConsentPolicy()`,
 * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
 * >
 * > 3. Look for any code that uses `consentPolicy.getCurrentConsentPolicy()`,
 * >    and replace it with the new import.
 * >    Update your code to work with the new `getCurrentConsentPolicy()`
 * >    call and response properties.
 * >
 * > 4. Test your changes to make sure your code behaves as expected.
 * >
 * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `getCurrentConsentPolicy()` method.
 * >
 *
 * Gets the site visitor's consent policy regarding allowed cookies and 3rd-party data transfers for GDPR or CCPA compliance.
 *
 * Retrieves the site visitor's consent policy details, including which cookies are allowed and whether data transfer to 3rd parties is permitted.
 *
 * @servicePath wix-window-frontend.ConsentPolicy.PolicyDetails
 */
export function getCurrentConsentPolicy(): Promise<PolicyDetails>;
/**
 * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`onConsentPolicyChanged()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/on-consent-policy-changed) method instead.
 *
 * > #### Migration Instructions
 * > If this method is already in your code, it will continue to work.
 * > To stay compatible with future changes, migrate to [`onConsentPolicyChanged()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/on-consent-policy-changed).
 * >
 * > To migrate to the new method:
 * >
 * > 1. Add the new import statement:
 * >
 * >    ```javascript
 * >    import { consentPolicy } from "@wix/site";
 * >    ```
 * >
 * > 2. If you plan to migrate all methods that use `onConsentPolicyChanged()`,
 * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
 * >
 * > 3. Look for any code that uses `consentPolicy.onConsentPolicyChanged()`,
 * >    and replace it with the new import.
 * >    Update your code to work with the new `onConsentPolicyChanged()`
 * >    call and response properties.
 * >
 * > 4. Test your changes to make sure your code behaves as expected.
 * >
 * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `onConsentPolicyChanged()` method.
 * >
 *
 * Triggered when a site visitor's consent policy was changed using `setConsentPolicy()` or reset using `resetConsentPolicy()`.
 *
 * Use the `onConsentPolicyChanged()` method for code you want to run after
 * the site visitor's current consent policy was changed using
 * `setConsentPolicy()` or reset using
 * `resetConsentPolicy()`.
 *
 *
 * Usually, you want to call the `onConsentPolicyChanged()` method in the **masterpage.js** file so that the `onConsentPolicyChanged()` event runs no matter which
 * page on a site is used to change the policy.
 * @param handler - The name of the method to run when the consent policy changes.
 * @requiredField handler
 * @servicePath wix-window-frontend.ConsentPolicy.ConsentPolicyChangedHandler
 */
export function onConsentPolicyChanged(handler: ConsentPolicyChangedHandler): Promise<void>;
/**
 * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`resetConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/reset-consent-policy) method instead.
 *
 * > #### Migration Instructions
 * > If this method is already in your code, it will continue to work.
 * > To stay compatible with future changes, migrate to [`resetConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/reset-consent-policy).
 * >
 * > To migrate to the new method:
 * >
 * > 1. Add the new import statement:
 * >
 * >    ```javascript
 * >    import { consentPolicy } from "@wix/site";
 * >    ```
 * >
 * > 2. If you plan to migrate all methods that use `resetConsentPolicy()`,
 * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
 * >
 * > 3. Look for any code that uses `consentPolicy.resetConsentPolicy()`,
 * >    and replace it with the new import.
 * >    Update your code to work with the new `resetConsentPolicy()`
 * >    call and response properties.
 * >
 * > 4. Test your changes to make sure your code behaves as expected.
 * >
 * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `resetConsentPolicy()` method.
 * >
 *
 * Removes the current policy from the site visitor's browser and resets the site visitor's consent policy to the default policy for the site.
 *
 * @returns Fulfilled - When the reset is complete.
 * Rejected - Error message.
 */
export function resetConsentPolicy(): Promise<void>;
/**
 * **Deprecated.** This method will continue to work, but a newer version is available. Use the [`setConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/set-consent-policy) method instead.
 *
 * > #### Migration Instructions
 * > If this method is already in your code, it will continue to work.
 * > To stay compatible with future changes, migrate to [`setConsentPolicy()`](https://dev.wix.com/docs/sdk/frontend-modules/site/consent-policy-manager/set-consent-policy).
 * >
 * > To migrate to the new method:
 * >
 * > 1. Add the new import statement:
 * >
 * >    ```javascript
 * >    import { consentPolicy } from "@wix/site";
 * >    ```
 * >
 * > 2. If you plan to migrate all methods that use `setConsentPolicy()`,
 * >    remove the original `import { consentPolicy } from "wix-window-frontend";` statement.
 * >
 * > 3. Look for any code that uses `consentPolicy.setConsentPolicy()`,
 * >    and replace it with the new import.
 * >    Update your code to work with the new `setConsentPolicy()`
 * >    call and response properties.
 * >
 * > 4. Test your changes to make sure your code behaves as expected.
 * >
 * > **Note:** Check the new method documentation for any differences in parameters, return values, or behavior compared to the deprecated `setConsentPolicy()` method.
 * >
 *
 * Sets the current site visitor's consent policy regarding allowed cookies and data transfer to 3rd parties, such as for GDPR or CCPA purposes.
 *
 * You can use the `onConsentPolicyChanged()` event to listen for
 * changes made when a site visitor changes their consent policy with `setConsentPolicy()`. Handle
 * the policy change accordingly in the event.
 * Changes to the consent policy take affect after the page is refreshed.
 * @param policy - An object representing the cookies of the site visitor's consent policy.
 * @requiredField policy
 * @servicePath wix-window-frontend.ConsentPolicy.Policy
 * @returns Fulfilled - All the details of a site visitor's consent policy, including if the current policy is the default site policy and the policy's allowed cookies.
 * Rejected - Error message.
 * @servicePath wix-window-frontend.ConsentPolicy.PolicyDetails
 */
export function setConsentPolicy(policy: Policy): Promise<PolicyDetails>;
