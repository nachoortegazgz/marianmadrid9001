import { statusCodeNumericToText } from './statusCodeNumericToText';
export const statusCodeTextToNumeric = Object.entries(statusCodeNumericToText).reduce((acc, _ref) => {
  let [numeric, text] = _ref;
  if (text) {
    acc[text] = Number(numeric);
  }
  return acc;
}, {});
//# sourceMappingURL=statusCodeTextToNumeric.js.map