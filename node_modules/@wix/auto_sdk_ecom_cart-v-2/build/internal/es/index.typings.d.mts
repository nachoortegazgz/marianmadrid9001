import { NonNullablePaths } from '@wix/sdk-types';

/**
 * A shopping cart for a customer on a Wix eCommerce site.
 *
 * A cart holds the customer's selected items, applied discounts (coupons, automatic rules),
 * delivery information, and payment details before the order is placed.
 *
 * Carts may be deleted after a period of inactivity.
 *
 * Use the [Cart Service](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/cart-v2/introduction) to manage carts by ID,
 * or the [Current Cart Service](https://dev.wix.com/docs/api-reference/business-solutions/e-commerce/purchase-flow/cart-v2/introduction) to manage
 * the current customer's cart by session.
 */
interface Cart {
    /**
     * Cart ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Date and time the Cart was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the Cart was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Revision number, which increments by 1 each time the Cart is updated.
     * @readonly
     */
    revision?: string;
    /**
     * Line items added to the Cart by the customer.
     * This list may include in-stock items and invalid or out-of-stock items.
     * @maxSize 300
     * @readonly
     */
    lineItems?: V2LineItem[];
    /**
     * The sum of all line item final prices.
     * @readonly
     */
    subtotal?: ConvertedMoney;
    /**
     * Optional message left by the customer.
     * Usually intended for the merchant and contains special instructions, requests, or clarifications related to the order.
     * @maxLength 1000
     */
    note?: string | null;
    /**
     * List of coupons added to the Cart.
     * Currently only one coupon code is supported.
     * @maxSize 1
     * @readonly
     */
    coupons?: V2Coupon[];
    /** Information about the source of the Cart, detailing the origin or context in which the Cart was created. */
    source?: CartSource;
    /** Information about the site from which the Cart's line items were added. */
    businessInfo?: BusinessInfo;
    /** Information about the customer that owns the Cart. */
    customerInfo?: CustomerInfo;
    /** Delivery-related information associated with the Cart. */
    deliveryInfo?: DeliveryInfo;
    /** Tax-related information associated with the Cart. */
    taxInfo?: TaxInfo;
    /** Payment-related information associated with the Cart. */
    paymentInfo?: PaymentInfo;
    /**
     * Whether the Cart has been placed as an Order.
     * Defaults to `false`.
     * @readonly
     */
    orderPlaced?: boolean;
    /**
     * Order ID.
     * Empty until the checkout process is completed.
     * @format GUID
     * @readonly
     */
    orderId?: string | null;
    /**
     * Persistent ID that correlates the Cart with the created Order after checkout is completed.
     * @format GUID
     * @readonly
     */
    purchaseFlowId?: string | null;
    /**
     * Additional cart settings.
     *
     * Settings can only be configured at cart creation time and can't be changed afterwards.
     * @immutable
     * @readonly
     */
    settings?: CartSettings;
    /**
     * Custom checkout URL to redirect the customer to a checkout page.
     * By default, customers are redirected to the standard Wix checkout page.
     * Clients can override this behavior by providing their own URL here.
     * If the URL contains the placeholder `{checkout_id}`, it will be replaced with the actual Cart ID.
     * @maxLength 2048
     */
    customCheckoutUrl?: string | null;
    /**
     * Custom fields.
     * [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * Whether this is the customer's current cart.
     * The current cart can be accessed using the identity context (for example, `visitorId` from session cookies) without an explicit cart ID.
     * @readonly
     */
    currentCart?: boolean;
    /**
     * Legacy fields for backward compatibility with Cart V1.
     * Should be used only by the Cart API proxy to populate V1 fields.
     * Used to avoid re-calculating these fields in the proxy.
     * @internal
     */
    legacyFields?: LegacyFields;
    /**
     * Forms associated with the Cart's line items.
     * @internal
     * @readonly
     * @maxSize 1000
     */
    forms?: FormInfo[];
    /**
     * Whether this is a demo cart.
     * Demo carts are created in the Wix editor for merchant preview and can't proceed to checkout.
     *
     * Default: `false`
     */
    demo?: boolean;
    /**
     * Indicators that help clients decide whether to call EstimateCart.
     * @internal
     * @readonly
     */
    estimationIndicators?: CartEstimationIndicators;
    /**
     * Applied discounts from totals calculator. Used internally for re-calculation without discounts, and by cart-v1-proxy.
     * @internal
     * @readonly
     * @maxSize 100
     */
    tcAppliedDiscounts?: AppliedDiscount[];
}
interface V2LineItem {
    /**
     * Unique identifier for the line item within the cart.
     * @format GUID
     */
    _id?: string;
    /** Display name for the line item. */
    name?: TranslatableString;
    /** Quantity details for the line item. */
    quantityInfo?: ItemQuantityInfo;
    /** Pricing details for the line item. */
    pricing?: ItemPricingInfo;
    /** Information about the line item's origin. */
    source?: ItemSource;
    /** Display details for the item. */
    attributes?: ItemAttributes;
    /** Delivery configuration for the line item. */
    deliveryConfig?: ItemDeliveryConfig;
    /** Tax configuration for the line item. */
    taxConfig?: ItemTaxConfig;
    /** Payment configuration for the line item. */
    paymentConfig?: ItemPaymentConfig;
    /** Inventory availability status for the line item. */
    status?: ItemStatusWithLiterals;
    /**
     * Custom extended fields for the line item object.
     *
     * [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured using the Checkout & Orders - Line Items Schema Plugin in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * Additional fees added to this item's calculation.
     * @internal
     * @maxSize 5
     */
    additionalFees?: V1AdditionalFee[];
    /**
     * Modifier groups added to the item.
     * @maxSize 25
     */
    modifierGroups?: ModifierGroup[];
    /**
     * Discounts applied to this line item.
     * @maxSize 100
     */
    discounts?: ItemDiscount[];
    /**
     * Whether the line item is a custom line item.
     * Custom line items don't trigger the Catalog service plugin.
     */
    customLineItem?: boolean;
    /**
     * Coupon scopes: which app and items a coupon applies to.
     * Internal to Wix; used by Bookings, Stores, and Events with the [Coupons API](https://bo.wix.com/wix-docs/rest/stores/coupons/valid-scope-values).
     * @internal
     * @maxSize 100
     */
    couponScopes?: Scope[];
}
interface TranslatableString {
    /**
     * __Required.__ String in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     *
     * Min: 1 character.
     * Max: 200 characters.
     * @minLength 1
     * @maxLength 200
     */
    original?: string;
    /**
     * String translated into the buyer's language.
     *
     * Min: 1 character.
     * Max: 400 characters.
     * Default: Same as `original`.
     * @minLength 1
     * @maxLength 400
     */
    translated?: string | null;
}
interface ItemQuantityInfo {
    /**
     * Confirmed quantity to purchase.
     * The minimum of `requestedQuantity` and `availableQuantity`.
     * @min 1
     * @max 100000
     */
    confirmedQuantity?: number;
    /**
     * Quantity the customer originally requested.
     * @min 1
     * @max 100000
     */
    requestedQuantity?: number;
    /** Maximum quantity available for purchase. */
    availableQuantity?: number | null;
    /**
     * Whether the line item quantity is fixed and can't be changed.
     *
     * Default: `false`
     */
    fixedQuantity?: boolean;
}
interface ItemPricingInfo {
    /**
     * Final unit price after discounts.
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    unitPrice?: ConvertedMoney;
    /**
     * Final total price for this item (unit price × quantity).
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    totalPrice?: ConvertedMoney;
    /**
     * Additional price description.
     * For example, when the price is `0` but you need more detail, such as "Starts at $67".
     */
    priceDescription?: PriceDescription;
    /** Whether the price isn't yet defined and will be updated after the order is created. */
    priceUndetermined?: boolean;
    /** Detailed breakdown of price components. */
    breakdown?: ItemPriceBreakdown;
}
interface ConvertedMoney {
    /**
     * Monetary amount in the original currency, as specified in `businessInfo.currencyCode`.
     * @format DECIMAL_VALUE
     * @decimalValue options { gte:0, lte:999999999, maxScale:3 }
     */
    amount?: string;
    /**
     * Monetary amount in the converted currency, as specified in `customerInfo.currencyCode`.
     *
     * Default: `amount`.
     * @format DECIMAL_VALUE
     * @decimalValue options { gte:0, lte:999999999, maxScale:3 }
     * @readonly
     */
    convertedAmount?: string;
}
interface PriceDescription {
    /**
     * __Required.__ Price description in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     * @minLength 1
     * @maxLength 100
     */
    original?: string;
    /**
     * Price description translated into the buyer's language.
     *
     * Default: Same as `original`.
     * @minLength 1
     * @maxLength 100
     */
    translated?: string | null;
}
interface ItemPriceBreakdown {
    /**
     * Full catalog price for a single item before discounts, including modifiers.
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    fullPrice?: ConvertedMoney;
    /**
     * Catalog sale price for a single item, including modifiers.
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    salePrice?: ConvertedMoney;
    /**
     * Total price of all modifiers for a single item.
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    totalModifiersPrice?: ConvertedMoney;
    /**
     * Full catalog price for a single item without modifiers.
     * Used by Cart & Checkout V1 API proxies when converting to legacy entities.
     * @internal
     * @decimalValue options { gte:0, lte:1000000000000000, maxScale:3 }
     */
    fullPriceBeforeModifiers?: string | null;
    /**
     * Catalog sale price for a single item without modifiers.
     * Stored in the site currency.
     * Used by Cart & Checkout V1 API proxies when converting to legacy entities.
     * @internal
     * @decimalValue options { gte:0, lte:1000000000000000, maxScale:3 }
     */
    salePriceBeforeModifiers?: string | null;
}
interface ItemSource {
    /**
     * Catalog and item reference: IDs for the item and catalog, plus optional details.
     * Optional for custom line items, which don't trigger the Catalog service plugin.
     */
    catalogReference?: CatalogReference;
    /** Overriding values for catalog item properties. */
    catalogOverrideFields?: CatalogOverrideFields;
    /**
     * When `catalogReference.catalogItemId` isn't the actual catalog item ID, this field returns the true catalog item ID.
     * For example, for Wix Bookings, `catalogReference.catalogItemId` is the booking ID, so this value is the service ID.
     * In most cases, this matches `catalogReference.catalogItemId`.
     * Used in membership validation.
     * @minLength 1
     * @maxLength 36
     */
    rootCatalogItemId?: string | null;
    /**
     * ID of the app managing the inventory.
     * @format GUID
     */
    inventoryAppId?: string | null;
    /**
     * ID of the app providing upsells for this item.
     * @internal
     * @format GUID
     */
    upsellOptionsAppId?: string | null;
    /**
     * Line-item ID in external systems.
     * @internal
     * @minLength 1
     * @maxLength 100
     */
    externalResourceId?: string | null;
}
/** Used for grouping line items. Sent when an item is added to a cart, checkout, or order. */
interface CatalogReference {
    /**
     * ID of the item within the catalog it belongs to.
     * @minLength 1
     * @maxLength 36
     */
    catalogItemId?: string;
    /**
     * ID of the app providing the catalog.
     *
     * You can get your app's ID from its page in the [app dashboard](https://dev.wix.com/dc3/my-apps/).
     *
     * For items from Wix catalogs, the following values always apply:
     * + Wix Stores: `"215238eb-22a5-4c36-9e7b-e7c08025e04e"`
     * + Wix Bookings: `"13d21c63-b5ec-5912-8397-c3a5ddb27a97"`
     * + Wix Restaurants: `"9a5d83fd-8570-482e-81ab-cfa88942ee60"`
     * @minLength 1
     */
    appId?: string;
    /**
     * Additional item details in `key:value` pairs.
     *
     * Use this optional field for more specificity with item selection. The values of the `options` field differ depending on which catalog is providing the items.
     *
     * For Wix Stores products, learn more about integrating with [Catalog V3](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v3/e-commerce-integration)
     * or [Catalog V1](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-v1/catalog/e-commerce-integration), depending on [the version the site uses](https://dev.wix.com/docs/api-reference/business-solutions/stores/catalog-versioning/introduction).
     */
    options?: Record<string, any> | null;
}
interface CatalogOverrideFields {
    /** Display name for the line item. */
    name?: TranslatableString;
    /**
     * Sale price for a single item after modifiers, before discounts.
     * @decimalValue options { gte:0, lte:999999999, maxScale:3 }
     */
    salePrice?: string | null;
    /**
     * Full price for a single item after modifiers, before discounts.
     * @decimalValue options { gte:0, lte:999999999, maxScale:3 }
     */
    fullPrice?: string | null;
    /**
     * Description lines for the item.
     * Shown when displaying the line item to buyers.
     * @maxSize 20
     */
    descriptionLines?: DescriptionLine[];
    /** Physical properties of the item. */
    physicalProperties?: PhysicalProperties;
    /** Item image. */
    image?: string;
    /** Selected payment option for the item. */
    paymentOption?: PaymentOption;
    /**
     * Partial payment due upfront during checkout.
     * Only when `paymentOption.value` is `DEPOSIT_ONLINE`.
     * @format DECIMAL_VALUE
     * @decimalValue options { gt:0, lte:999999999, maxScale:3 }
     */
    depositAmount?: string | null;
    /**
     * Whether to save the payment method on the order.
     *
     * Default: `false`
     */
    savePaymentMethod?: boolean | null;
    /**
     * ID of the app managing inventory.
     * @format GUID
     */
    inventoryAppId?: string | null;
    /**
     * Whether the line item quantity is fixed and can't be changed.
     * When not set, falls back to the value from the catalog.
     * @internal
     */
    fixedQuantity?: boolean | null;
}
interface DescriptionLine extends DescriptionLineValueOneOf, DescriptionLineDescriptionLineValueOneOf {
    /** Description line plain text value. */
    plainText?: PlainTextValue;
    /** Description line color value. */
    colorInfo?: Color;
    /**
     * Description line plain text value.
     * @internal
     * @deprecated
     */
    plainTextValue?: PlainTextValue;
    /**
     * Description line color.
     * @internal
     * @deprecated
     */
    color?: string;
    /** Description line name. */
    name?: DescriptionLineName;
    /**
     * Description line type.
     * @internal
     * @deprecated
     */
    lineType?: DescriptionLineTypeWithLiterals;
}
/** @oneof */
interface DescriptionLineValueOneOf {
    /** Description line plain text value. */
    plainText?: PlainTextValue;
    /** Description line color value. */
    colorInfo?: Color;
}
/** @oneof */
interface DescriptionLineDescriptionLineValueOneOf {
    /**
     * Description line plain text value.
     * @internal
     * @deprecated
     */
    plainTextValue?: PlainTextValue;
    /**
     * Description line color.
     * @internal
     * @deprecated
     */
    color?: string;
}
interface DescriptionLineName {
    /**
     * Description line name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     * @maxLength 100
     */
    original?: string;
    /**
     * Description line name translated into the buyer's language.
     *
     * Default: Same as `original`.
     * @maxLength 200
     */
    translated?: string | null;
}
interface PlainTextValue {
    /**
     * Description line plain text value in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     * @maxLength 600
     */
    original?: string;
    /**
     * Description line plain text value translated into the buyer's language.
     *
     * Default: Same as `original`.
     * @maxLength 600
     */
    translated?: string | null;
}
interface Color {
    /**
     * Description line color name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     * @maxLength 500
     */
    original?: string;
    /**
     * Description line color name translated into the buyer's language.
     *
     * Default: Same as `original`.
     * @maxLength 500
     */
    translated?: string | null;
    /** HEX or RGB color code for display. */
    code?: string | null;
}
declare enum DescriptionLineType {
    /** Unrecognized type. */
    UNRECOGNISED = "UNRECOGNISED",
    /** Plain text type. */
    PLAIN_TEXT = "PLAIN_TEXT",
    /** Color type. */
    COLOR = "COLOR"
}
/** @enumType */
type DescriptionLineTypeWithLiterals = DescriptionLineType | 'UNRECOGNISED' | 'PLAIN_TEXT' | 'COLOR';
interface PhysicalProperties {
    /** Line item weight. Measurement unit matches the weight unit specified in `weightUnit` in the request. */
    weight?: number | null;
    /**
     * Stock-keeping unit. Learn more about [SKUs](https://www.wix.com/encyclopedia/definition/stock-keeping-unit-sku).
     * @maxLength 40
     */
    sku?: string | null;
    /** Whether this line item is shippable. */
    shippable?: boolean;
    /**
     * Product dimensions (length, width, height of the physical product).
     * @internal
     */
    productDimensions?: Dimensions;
    /**
     * Package dimensions (length, width, height of the shipping package).
     * @internal
     */
    packageDimensions?: Dimensions;
}
interface Dimensions {
    /**
     * Length. Measurement unit is handled at the system level (metric/imperial).
     * @decimalValue options { gte:0, lte:999999999, maxScale:6 }
     */
    length?: string | null;
    /**
     * Width. Measurement unit is handled at the system level (metric/imperial).
     * @decimalValue options { gte:0, lte:999999999, maxScale:6 }
     */
    width?: string | null;
    /**
     * Height. Measurement unit is handled at the system level (metric/imperial).
     * @decimalValue options { gte:0, lte:999999999, maxScale:6 }
     */
    height?: string | null;
    /** Unit of measurement for dimensions (length, width, height). */
    unit?: DimensionsUnitWithLiterals;
}
declare enum DimensionsUnit {
    UNKNOWN_DIMENSIONS_UNIT = "UNKNOWN_DIMENSIONS_UNIT",
    /** Millimeters. */
    MM = "MM",
    /** Centimeters. */
    CM = "CM",
    /** Meters. */
    M = "M",
    /** Inches. */
    IN = "IN",
    /** Feet. */
    FT = "FT",
    /** Yards. */
    YD = "YD"
}
/** @enumType */
type DimensionsUnitWithLiterals = DimensionsUnit | 'UNKNOWN_DIMENSIONS_UNIT' | 'MM' | 'CM' | 'M' | 'IN' | 'FT' | 'YD';
interface PaymentOption {
    /**
     * Selected payment option type for this item.
     *
     * Default: `FULL_PAYMENT_ONLINE`
     */
    value?: PaymentOptionTypeWithLiterals;
}
/** Type of selected payment option for catalog item */
declare enum PaymentOptionType {
    /** The entire payment for this item happens as part of the checkout. */
    FULL_PAYMENT_ONLINE = "FULL_PAYMENT_ONLINE",
    /** The entire payment for this item happens after checkout. For example, when using cash, check, or other offline payment methods. */
    FULL_PAYMENT_OFFLINE = "FULL_PAYMENT_OFFLINE",
    /** Payment for this item is done by charging a membership. Any remaining amount not covered by the membership, such as item modifiers, is paid online. */
    MEMBERSHIP = "MEMBERSHIP",
    /** Partial payment to be paid upfront during checkout. The initial amount to be paid for each line item is specified in `depositAmount`. */
    DEPOSIT_ONLINE = "DEPOSIT_ONLINE",
    /** Payment for this item can only be done by charging a membership and must be manually redeemed in the dashboard by the site admin. When selected, `price` is `0`. */
    MEMBERSHIP_OFFLINE = "MEMBERSHIP_OFFLINE",
    /**
     * Item price is charged to online membership. Any remaining amount not covered by the membership, such as item modifiers, is paid offline.
     * @documentationMaturity preview
     */
    MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER = "MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER"
}
/** @enumType */
type PaymentOptionTypeWithLiterals = PaymentOptionType | 'FULL_PAYMENT_ONLINE' | 'FULL_PAYMENT_OFFLINE' | 'MEMBERSHIP' | 'DEPOSIT_ONLINE' | 'MEMBERSHIP_OFFLINE' | 'MEMBERSHIP_ONLINE_WITH_OFFLINE_REMAINDER';
interface ItemAttributes {
    /**
     * Description lines for the item.
     * @maxSize 100
     */
    descriptionLines?: DescriptionLine[];
    /** Item image. */
    image?: string;
    /** URL of the item's page on the site. */
    url?: string;
    /**
     * Policies shown to the buyer on the checkout page.
     * @maxSize 5
     */
    policies?: Policy[];
    /** Item type: preset or custom. */
    itemType?: ItemType;
    /**
     * Physical properties of the item.
     * When relevant, includes SKU, weight, and shippability.
     */
    physicalProperties?: PhysicalProperties;
    /** Subscription option details. */
    subscriptionInfo?: SubscriptionOptionInfo;
    /**
     * Digital file identifier; relevant only for items with type `DIGITAL`.
     * @internal
     */
    digitalFile?: SecuredMedia;
    /**
     * Service properties.
     * When relevant, includes date and number of participants.
     */
    serviceProperties?: ServiceProperties;
    /**
     * Whether the item can only be purchased by site members.
     *
     * Default: `false`
     */
    membersOnly?: boolean;
}
interface Policy {
    /**
     * Policy title - should be translated
     * @minLength 1
     * @maxLength 29
     */
    title?: string | null;
    /**
     * Policy content - should be translated
     * @minLength 1
     * @maxLength 3000
     */
    content?: string;
}
interface ItemType extends ItemTypeItemTypeDataOneOf {
    /** Preset item type. */
    preset?: ItemTypePresetWithLiterals;
    /** Custom item type. When none of the preset types are suitable, specifies the custom type. */
    custom?: string;
}
/** @oneof */
interface ItemTypeItemTypeDataOneOf {
    /** Preset item type. */
    preset?: ItemTypePresetWithLiterals;
    /** Custom item type. When none of the preset types are suitable, specifies the custom type. */
    custom?: string;
}
declare enum ItemTypePreset {
    UNRECOGNISED = "UNRECOGNISED",
    PHYSICAL = "PHYSICAL",
    DIGITAL = "DIGITAL",
    GIFT_CARD = "GIFT_CARD",
    SERVICE = "SERVICE"
}
/** @enumType */
type ItemTypePresetWithLiterals = ItemTypePreset | 'UNRECOGNISED' | 'PHYSICAL' | 'DIGITAL' | 'GIFT_CARD' | 'SERVICE';
interface SubscriptionOptionInfo {
    /** Subscription option settings. */
    subscriptionSettings?: SubscriptionSettings;
    /** Subscription option title. */
    title?: Title;
    /** Subscription option description. */
    description?: Description;
}
interface SubscriptionSettings {
    /** Frequency of recurring payment. */
    frequency?: SubscriptionFrequencyWithLiterals;
    /**
     * Interval of recurring payment.
     *
     * Default: `1`.
     * If SubscriptionFrequency is Day the minimum interval is 7
     * @min 1
     * @max 3650
     */
    interval?: number | null;
    /** Whether subscription is renewed automatically at the end of each period. */
    autoRenewal?: boolean;
    /**
     * Number of billing cycles before subscription ends. Ignored if `autoRenewal` is `true`.
     * @min 1
     */
    billingCycles?: number | null;
    /** Whether to allow the customer to cancel the subscription.. */
    enableCustomerCancellation?: boolean;
    /**
     * Period until first cycle starts. If applied payNow will be 0
     * If None => no free trial
     */
    freeTrialPeriod?: FreeTrialPeriod;
    /**
     * The date the subscription will start. The subscription will be charged either now or according to freeTrialDays.
     * @internal
     */
    startDate?: Date | null;
    /**
     * Billing configuration for recurring charges.
     * @internal
     */
    billingSettings?: BillingSettings;
    /**
     * Whether the product/service/subscription access period is aligned with the billing period.
     * @internal
     */
    productPeriodAlignment?: ProductPeriodAlignmentWithLiterals;
}
/** Frequency unit of recurring payment */
declare enum SubscriptionFrequency {
    UNDEFINED = "UNDEFINED",
    DAY = "DAY",
    WEEK = "WEEK",
    MONTH = "MONTH",
    YEAR = "YEAR"
}
/** @enumType */
type SubscriptionFrequencyWithLiterals = SubscriptionFrequency | 'UNDEFINED' | 'DAY' | 'WEEK' | 'MONTH' | 'YEAR';
interface FreeTrialPeriod {
    /** Frequency of period. Values: DAY, WEEK, MONTH, YEAR */
    frequency?: SubscriptionFrequencyWithLiterals;
    /**
     * Number of frequency units in the free trial period. For example, frequency `MONTH` with interval `3` means a 3-month free trial.
     * @min 1
     * @max 999
     */
    interval?: number;
}
interface BillingSettings {
    /**
     * Day of month (1-28) to anchor recurring billing to. When set, billing recurs on this day each cycle. Does not apply to weekly subscriptions.
     * @min 1
     * @max 28
     */
    anchorDay?: number | null;
}
declare enum ProductPeriodAlignment {
    /** Billing period equals the product/service/subscription access period. */
    ALIGNED_WITH_BILLING_PERIOD = "ALIGNED_WITH_BILLING_PERIOD",
    /** Billing period is independent of the product/service/subscription access period. */
    NOT_ALIGNED_WITH_BILLING_PERIOD = "NOT_ALIGNED_WITH_BILLING_PERIOD"
}
/** @enumType */
type ProductPeriodAlignmentWithLiterals = ProductPeriodAlignment | 'ALIGNED_WITH_BILLING_PERIOD' | 'NOT_ALIGNED_WITH_BILLING_PERIOD';
interface Title {
    /**
     * Subscription option name in the site's default language as defined in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     * @minLength 1
     * @maxLength 150
     */
    original?: string;
    /**
     * Subscription option name translated into the buyer's language.
     *
     * Default: Same as `original`.
     * @minLength 1
     * @maxLength 150
     */
    translated?: string | null;
}
interface Description {
    /**
     * Subscription option description.
     * @maxLength 500
     */
    original?: string;
    /**
     * Translated subscription option description.
     * @maxLength 500
     */
    translated?: string | null;
}
interface SecuredMedia {
    /**
     * Media ID in Wix Media Manager.
     * @minLength 1
     * @maxLength 100
     */
    _id?: string;
    /**
     * Original filename.
     * @minLength 1
     * @maxLength 1000
     */
    fileName?: string;
    /** File type. */
    fileType?: FileTypeWithLiterals;
}
declare enum FileType {
    UNSPECIFIED = "UNSPECIFIED",
    SECURE_PICTURE = "SECURE_PICTURE",
    SECURE_VIDEO = "SECURE_VIDEO",
    SECURE_DOCUMENT = "SECURE_DOCUMENT",
    SECURE_MUSIC = "SECURE_MUSIC",
    SECURE_ARCHIVE = "SECURE_ARCHIVE",
    SECURE_RAW = "SECURE_RAW"
}
/** @enumType */
type FileTypeWithLiterals = FileType | 'UNSPECIFIED' | 'SECURE_PICTURE' | 'SECURE_VIDEO' | 'SECURE_DOCUMENT' | 'SECURE_MUSIC' | 'SECURE_ARCHIVE' | 'SECURE_RAW';
interface ServiceProperties {
    /**
     * Date and time the service is to be provided, in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
     * For example, the start time of a class.
     */
    scheduledDate?: Date | null;
    /**
     * The number of people participating in the service. For example, the number of people attending a class or the number of people per hotel room.
     * @min 1
     * @max 10000
     */
    numberOfParticipants?: number | null;
    /**
     * Date and time the service is to be ended, in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601#Combined_date_and_time_representations) format.
     * For example, the date and time a class will end.
     * @internal
     */
    endDate?: Date | null;
}
interface ItemDeliveryConfig {
    /**
     * Delivery profile ID.
     * @format GUID
     */
    deliveryProfileId?: string | null;
    /**
     * Optional custom fulfiller ID.
     * @format GUID
     */
    fulfillerId?: string | null;
}
interface ItemTaxConfig {
    /** Address used for tax calculation. */
    taxableAddress?: TaxableAddress;
    /**
     * Tax group ID for this line item.
     * @maxLength 50
     */
    taxGroupId?: string | null;
}
interface TaxableAddress extends TaxableAddressTaxableAddressDataOneOf {
    /** taxable address type. if this field is selected, the address is automatically resolved, and the tax is calculated accordingly. */
    addressType?: TaxableAddressTypeWithLiterals;
}
/** @oneof */
interface TaxableAddressTaxableAddressDataOneOf {
    /** taxable address type. if this field is selected, the address is automatically resolved, and the tax is calculated accordingly. */
    addressType?: TaxableAddressTypeWithLiterals;
}
declare enum TaxableAddressType {
    BUSINESS = "BUSINESS",
    BILLING = "BILLING",
    SHIPPING = "SHIPPING"
}
/** @enumType */
type TaxableAddressTypeWithLiterals = TaxableAddressType | 'BUSINESS' | 'BILLING' | 'SHIPPING';
interface ItemPaymentConfig {
    /**
     * Whether to save the payment method on the order.
     *
     * Default: `false`
     */
    savePaymentMethod?: boolean;
    /**
     * Item payment policy that requires buyer consent to complete the purchase.
     * Displayed on the checkout page.
     * @minLength 1
     * @maxLength 2500
     */
    consentRequiredPaymentPolicy?: string | null;
    /**
     * Partial payment due upfront during checkout.
     * Only when `paymentOption` is `DEPOSIT_ONLINE`.
     */
    depositAmount?: ConvertedMoney;
    /**
     * Selected membership used as payment for this item.
     * Use with `paymentOption` `MEMBERSHIP` or `MEMBERSHIP_OFFLINE`.
     * Can be empty when `paymentOption` is `MEMBERSHIP_OFFLINE`.
     *
     * DEPRECATED: migrate to `membership`. Removed once all clients migrate.
     * @deprecated
     * @replacedBy membership
     * @targetRemovalDate 2026-06-22
     */
    selectedMembership?: V2SelectedMembership;
    /**
     * Selected payment option type for this item.
     *
     * Default: `FULL_PAYMENT_ONLINE`
     */
    paymentOption?: PaymentOptionTypeWithLiterals;
    /**
     * Membership used as payment for this item. Can be an existing membership, or one
     * created from a plan in the same cart.
     *
     * Replaces `selectedMembership`.
     *
     * Use with `paymentOption` `MEMBERSHIP` or `MEMBERSHIP_OFFLINE`.
     */
    membership?: V2Membership;
}
interface V2SelectedMembership {
    /**
     * Membership ID.
     * @minLength 1
     * @maxLength 200
     */
    _id?: string;
    /**
     * ID of the app providing this payment option.
     * @format GUID
     */
    appId?: string;
    /**
     * Display name of the selected membership.
     * @readonly
     */
    name?: TranslatableString;
}
interface V2Membership extends V2MembershipIdentifierOneOf {
    /** Existing, already-provisioned membership. */
    existingMembership?: V2ExistingMembership;
    /** Future membership, created after the plan is purchased. */
    futureMembership?: FutureMembership;
    /**
     * Display name of the membership. For example, "10-class yoga pass".
     * @readonly
     */
    name?: TranslatableString;
    /**
     * Amount deducted from the membership's balance to redeem this line item.
     * `0` when the membership's benefit is unlimited (nothing is deducted).
     * Absent when the cost hasn't been calculated yet.
     * @readonly
     */
    redemptionCost?: number | null;
    /**
     * Kind of balance `redemptionCost` is deducted from.
     * @readonly
     */
    redemptionType?: RedemptionTypeWithLiterals;
}
/** @oneof */
interface V2MembershipIdentifierOneOf {
    /** Existing, already-provisioned membership. */
    existingMembership?: V2ExistingMembership;
    /** Future membership, created after the plan is purchased. */
    futureMembership?: FutureMembership;
}
interface V2ExistingMembership {
    /**
     * ID of the app providing this membership payment option, for example Pricing Plans.
     * @format GUID
     */
    appId?: string;
    /**
     * ID of existing membership.
     * @minLength 1
     * @maxLength 200
     */
    membershipId?: string;
}
interface FutureMembership {
    /**
     * ID of the plan line item, already in the cart, whose future membership pays for this item.
     * @format GUID
     */
    lineItemId?: string;
    /**
     * The selected unit within the plan to redeem.
     * @maxLength 200
     */
    benefitId?: string;
}
declare enum RedemptionType {
    /** Membership balance is measured in a currency-like credit value, for example points. */
    CREDITS = "CREDITS",
    /** Membership balance is measured in discrete sessions, for example visits or classes. */
    SESSIONS = "SESSIONS"
}
/** @enumType */
type RedemptionTypeWithLiterals = RedemptionType | 'CREDITS' | 'SESSIONS';
declare enum ItemStatus {
    /** Item is available in the requested quantity. */
    IN_STOCK = "IN_STOCK",
    /** Only part of the requested quantity is available. */
    PARTIALLY_IN_STOCK = "PARTIALLY_IN_STOCK",
    /** Item is out of stock. */
    OUT_OF_STOCK = "OUT_OF_STOCK",
    /** Item no longer exists in the catalog. */
    REMOVED_FROM_CATALOG = "REMOVED_FROM_CATALOG"
}
/** @enumType */
type ItemStatusWithLiterals = ItemStatus | 'IN_STOCK' | 'PARTIALLY_IN_STOCK' | 'OUT_OF_STOCK' | 'REMOVED_FROM_CATALOG';
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface V1AdditionalFee {
    /**
     * Additional fee's unique code or ID.
     * @minLength 1
     * @maxLength 100
     */
    code?: string | null;
    /**
     * Additional fee's name.
     *
     * Max: 50 characters
     * @minLength 1
     * @maxLength 50
     */
    name?: string;
    /**
     * Total additional fees. This `price` does not include any taxes that may apply to these additional fees.
     * @decimalValue options { gte:0, lte:1000000000000000, maxScale:2 }
     */
    price?: string;
    /**
     * Tax details.
     *
     * > **Note:** Tax is not calculated in the returned `price` even when `taxDetails.taxable` is `true`.
     */
    taxDetails?: TaxDetails;
    /**
     * Number of subscription cycle this fee applies to
     * default None - all billing cycle
     * @internal
     * @min 1
     * @max 999
     */
    subscriptionCycles?: number | null;
}
/** Tax details. */
interface TaxDetails {
    /** Whether additional fee is taxable. */
    taxable?: boolean;
    /**
     * Reserved for internal use.
     * @format GUID
     */
    taxGroupId?: string | null;
}
interface ModifierGroup {
    /**
     * Modifier group ID.
     * @minLength 1
     * @maxLength 36
     * @readonly
     */
    _id?: string;
    /** Modifier group name. */
    name?: TranslatableString;
    /**
     * Modifiers in this group.
     * @minSize 1
     * @maxSize 25
     */
    modifiers?: ItemModifier[];
}
interface ItemModifier {
    /**
     * Modifier ID.
     * @minLength 1
     * @maxLength 36
     * @readonly
     */
    _id?: string;
    /**
     * Modifier quantity.
     * @min 1
     * @max 100000
     */
    quantity?: number | null;
    /** Primary label for the modifier. */
    label?: TranslatableString;
    /** Extra modifier copy. */
    details?: TranslatableString;
    /** Modifier price. */
    price?: ConvertedMoney;
}
interface ItemDiscount {
    /** Discount display name. */
    name?: TranslatableString;
    /** Total amount taken off the item's sale price for the full line item quantity. */
    amountOff?: ConvertedMoney;
    /** Discount source (for example, coupon or automatic discount) and related identifiers. */
    source?: DiscountSource;
}
interface DiscountSource {
    /** Discount source type. */
    sourceType?: DiscountSourceTypeWithLiterals;
    /**
     * Coupon code that triggered the discount. Only relevant for coupon discounts.
     * @maxLength 50
     */
    couponCode?: string | null;
    /**
     * ID of the coupon that triggered the discount, if applicable. Used for V1 compatibility and analytics.
     * @internal
     * @maxLength 50
     */
    couponId?: string | null;
    /**
     * Coupon type that triggered the discount, if applicable (for example, money off or buy X get Y). Used for V1 compatibility.
     * @internal
     * @maxLength 50
     */
    couponType?: string | null;
    /**
     * ID of the automatic rule that triggered the discount, if applicable. Used for V1 compatibility and analytics.
     * @internal
     * @maxLength 50
     */
    ruleId?: string | null;
}
declare enum DiscountSourceType {
    /** Discount applied automatically by a rule. */
    AUTOMATIC_RULE = "AUTOMATIC_RULE",
    /** Discount applied by a coupon code. */
    COUPON = "COUPON"
}
/** @enumType */
type DiscountSourceTypeWithLiterals = DiscountSourceType | 'AUTOMATIC_RULE' | 'COUPON';
interface Scope {
    /** Scope namespace (Wix Stores, Wix Bookings, Wix Events, Wix Pricing Plans) */
    namespace?: string;
    /** Coupon scope's applied group (e.g., event or ticket in Wix Events) */
    group?: Group;
}
interface Group {
    /** Coupon scope's group (e.g., product or collection in Wix Stores). See [valid scope values](https://dev.wix.com/api/rest/coupons/coupons/valid-scope-values). */
    name?: string;
    /** Item ID (when the coupon scope is limited to just one item). */
    entityId?: string | null;
}
interface V2Coupon {
    /**
     * Coupon ID in the cart.
     * @format GUID
     */
    _id?: string;
    /**
     * Coupon code entered at checkout.
     * @minLength 1
     * @maxLength 50
     */
    code?: string;
}
interface CartSource {
    /** Sales channel that created the cart. */
    channelType?: ChannelTypeWithLiterals;
    /**
     * External app and resource references for this cart, used for integration and tracking across platforms.
     * @maxSize 10
     * @immutable
     * @readonly
     */
    externalReferences?: ExternalReference[];
    /**
     * Entity that created the cart.
     * @readonly
     */
    createdBy?: CreatedBy;
    /** App and component IDs for the custom checkout page content provider. */
    customContentReference?: CustomContentReference;
}
declare enum ChannelType {
    /** Unspecified sales channel. This value is not supported. */
    UNSPECIFIED = "UNSPECIFIED",
    /** A web client. */
    WEB = "WEB",
    /** [Point of sale solutions](https://support.wix.com/en/wix-mobile-pos-2196395). */
    POS = "POS",
    /** [eBay shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-ebay-shop). */
    EBAY = "EBAY",
    /** [Amazon shop](https://support.wix.com/en/article/wix-stores-connecting-and-setting-up-an-amazon-shop). */
    AMAZON = "AMAZON",
    /** Other sales platform. */
    OTHER_PLATFORM = "OTHER_PLATFORM",
    /** [Wix Owner app](https://support.wix.com/article/wix-owner-app-an-overview). */
    WIX_APP_STORE = "WIX_APP_STORE",
    /** Wix Invoices app in [your dashboard](https://www.wix.com/my-account/site-selector/?buttonText=Select%20Site&title=Select%20a%20Site&autoSelectOnSingleSite=true&actionUrl=https:%2F%2Fwww.wix.com%2Fdashboard%2F%7B%7BmetaSiteId%7D%7D%2Finvoices/settings/general-settings) */
    WIX_INVOICES = "WIX_INVOICES",
    /** Wix merchant backoffice. */
    BACKOFFICE_MERCHANT = "BACKOFFICE_MERCHANT",
    /** Wish sales channel. */
    WISH = "WISH",
    /** [ClassPass sales channel](https://support.wix.com/en/article/wix-bookings-letting-clients-book-your-services-with-classpass). */
    CLASS_PASS = "CLASS_PASS",
    /** Global-E sales channel. */
    GLOBAL_E = "GLOBAL_E",
    /** [Facebook shop](https://support.wix.com/en/article/wix-stores-changes-to-facebook-shops). */
    FACEBOOK = "FACEBOOK",
    /** [Etsy sales channel](https://support.wix.com/en/article/wix-stores-request-adding-etsy-as-a-sales-channel). */
    ETSY = "ETSY",
    /** [TikTok sales channel](https://support.wix.com/en/article/wix-stores-request-adding-tiktok-as-a-sales-channel). */
    TIKTOK = "TIKTOK",
    /** [Faire marketplace integration](https://support.wix.com/en/article/wix-stores-creating-a-faire-store-using-the-faire-integration-app). */
    FAIRE_COM = "FAIRE_COM",
    /** PayPal Agentic Checkout sales channel. */
    PAYPAL_AGENTIC_CHECKOUT = "PAYPAL_AGENTIC_CHECKOUT",
    /** Stripe Agentic Checkout sales channel. */
    STRIPE_AGENTIC_CHECKOUT = "STRIPE_AGENTIC_CHECKOUT",
    /**
     * Wix Invoices platform app
     * @documentationMaturity preview
     * @internal
     */
    WIX_INVOICES_PLATFORM = "WIX_INVOICES_PLATFORM",
    /**
     * UCP (Universal Checkout Protocol) Agentic Checkout sales channel.
     * @documentationMaturity preview
     * @internal
     */
    UCP_AGENTIC_CHECKOUT = "UCP_AGENTIC_CHECKOUT"
}
/** @enumType */
type ChannelTypeWithLiterals = ChannelType | 'UNSPECIFIED' | 'WEB' | 'POS' | 'EBAY' | 'AMAZON' | 'OTHER_PLATFORM' | 'WIX_APP_STORE' | 'WIX_INVOICES' | 'BACKOFFICE_MERCHANT' | 'WISH' | 'CLASS_PASS' | 'GLOBAL_E' | 'FACEBOOK' | 'ETSY' | 'TIKTOK' | 'FAIRE_COM' | 'PAYPAL_AGENTIC_CHECKOUT' | 'STRIPE_AGENTIC_CHECKOUT';
interface ExternalReference {
    /**
     * ID of the app associated with the purchase flow.
     * For example, the Wix Pay Links app ID.
     * @format GUID
     * @immutable
     */
    appId?: string;
    /**
     * Reference to an external resource ID. Used to link the purchase flow to a specific entity in an external system.
     * For example, a Wix Pay Link ID.
     * @minLength 1
     * @maxLength 100
     * @immutable
     */
    resourceId?: string | null;
}
interface CreatedBy extends CreatedByIdOneOf {
    /**
     * Site visitor ID when the visitor isn't a site member.
     * @format GUID
     * @readonly
     */
    visitorId?: string;
    /**
     * Site member ID.
     * @format GUID
     * @readonly
     */
    memberId?: string;
    /**
     * Wix user ID when the cart was created by a Wix user on behalf of a buyer (for example, POS).
     * @format GUID
     */
    userId?: string;
    /**
     * App ID when the cart was created by an external app or Wix service.
     * @format GUID
     */
    appId?: string;
}
/** @oneof */
interface CreatedByIdOneOf {
    /**
     * Site visitor ID when the visitor isn't a site member.
     * @format GUID
     * @readonly
     */
    visitorId?: string;
    /**
     * Site member ID.
     * @format GUID
     * @readonly
     */
    memberId?: string;
    /**
     * Wix user ID when the cart was created by a Wix user on behalf of a buyer (for example, POS).
     * @format GUID
     */
    userId?: string;
    /**
     * App ID when the cart was created by an external app or Wix service.
     * @format GUID
     */
    appId?: string;
}
interface CustomContentReference {
    /**
     * App ID for the checkout content provider. Find it in the [app dashboard](https://dev.wix.com/dc3/my-apps/).
     * @format GUID
     */
    appId?: string;
    /**
     * Component ID within that app. Find it in the [app dashboard](https://dev.wix.com/dc3/my-apps/).
     * @format GUID
     */
    componentId?: string;
}
interface BusinessInfo {
    /**
     * Business location ID for the cart. See the [Locations API](https://dev.wix.com/docs/rest/business-management/locations/introduction).
     * @format GUID
     */
    locationId?: string | null;
    /**
     * 2-letter language code in ISO 639-1 alpha-2 format. The business's default language.
     * @format LANGUAGE
     * @readonly
     */
    languageCode?: string;
    /**
     * Three-letter currency code in ISO-4217 alphabetic format. The site's base currency; it doesn't change when the [customer selects a different display currency](https://support.wix.com/en/article/multicurrency-an-overview).
     * @format CURRENCY
     * @readonly
     */
    currencyCode?: string;
}
interface CustomerInfo extends CustomerInfoIdOneOf {
    /**
     * Site visitor ID when the visitor isn't a site member.
     * @format GUID
     * @readonly
     */
    visitorId?: string;
    /**
     * Site member ID.
     * @format GUID
     * @readonly
     */
    memberId?: string;
    /**
     * Wix user ID when the cart was created by a Wix user on behalf of a customer (for example, POS).
     * @format GUID
     * @readonly
     */
    userId?: string;
    /**
     * Whether the cart has no owner yet. The first site visitor or member to access the cart becomes its owner.
     *
     * Specify `true` when creating a cart for a customer who isn't identified yet.
     *
     * Used only when an app or a Wix user creates the cart.
     *
     * Default: `true` if no customer is identified when the cart is created, otherwise `false`.
     * @readonly
     */
    openAccess?: boolean;
    /**
     * Customer's first name.
     * @maxLength 100
     */
    firstName?: string | null;
    /**
     * Customer's last name.
     * @maxLength 100
     */
    lastName?: string | null;
    /**
     * Customer's phone number.
     * @format PHONE
     */
    phone?: string | null;
    /**
     * Company name.
     * @maxLength 1000
     */
    company?: string | null;
    /** Business VAT registration number. */
    vatId?: VatId;
    /**
     * Customer's email address.
     * @format EMAIL
     */
    email?: string | null;
    /**
     * Customer's contact ID. Created automatically if one doesn't exist yet. See the [Contacts API](https://dev.wix.com/api/rest/contacts/contacts/introduction).
     * @format GUID
     * @readonly
     */
    contactId?: string | null;
    /**
     * 2-letter language code in ISO 639-1 alpha-2 format. Resolved automatically from request metadata (`x-wix-linguist` header).
     * @format LANGUAGE
     * @readonly
     */
    languageCode?: string;
    /**
     * Three-letter currency code in ISO-4217 alphabetic format. [Currency the customer selects](https://support.wix.com/en/article/multicurrency-an-overview) during checkout; it's their preferred display currency and may differ from the site's default.
     * @format CURRENCY
     * @readonly
     */
    currencyCode?: string;
}
/** @oneof */
interface CustomerInfoIdOneOf {
    /**
     * Site visitor ID when the visitor isn't a site member.
     * @format GUID
     * @readonly
     */
    visitorId?: string;
    /**
     * Site member ID.
     * @format GUID
     * @readonly
     */
    memberId?: string;
    /**
     * Wix user ID when the cart was created by a Wix user on behalf of a customer (for example, POS).
     * @format GUID
     * @readonly
     */
    userId?: string;
    /**
     * Whether the cart has no owner yet. The first site visitor or member to access the cart becomes its owner.
     *
     * Specify `true` when creating a cart for a customer who isn't identified yet.
     *
     * Used only when an app or a Wix user creates the cart.
     *
     * Default: `true` if no customer is identified when the cart is created, otherwise `false`.
     * @readonly
     */
    openAccess?: boolean;
}
interface VatId {
    /** Customer's tax ID. */
    _id?: string;
    /**
     * Tax type.
     *
     * Supported values:
     * + `CPF`: for individual tax payers
     * + `CNPJ`: for corporations
     */
    type?: VatTypeWithLiterals;
}
/** tax info types */
declare enum VatType {
    UNSPECIFIED = "UNSPECIFIED",
    /** CPF - for individual tax payers. */
    CPF = "CPF",
    /** CNPJ - for corporations */
    CNPJ = "CNPJ"
}
/** @enumType */
type VatTypeWithLiterals = VatType | 'UNSPECIFIED' | 'CPF' | 'CNPJ';
interface DeliveryInfo {
    /** Delivery address. */
    address?: Address;
    /**
     * Delivery method selected by the customer.
     * @readonly
     */
    method?: DeliveryMethod;
    /**
     * Weight unit used when shipping items (for example, kg or lb).
     * @readonly
     */
    weightUnit?: WeightUnitWithLiterals;
    /**
     * ID of the delivery address in the legacy addresses service.
     * @internal
     * @maxLength 50
     */
    addressId?: string | null;
}
/** Physical address */
interface Address {
    /**
     * Two-letter country code in [ISO-3166 alpha-2](https://www.iso.org/obp/ui/#search/code/) format.
     * @format COUNTRY
     */
    country?: string | null;
    /**
     * Code for a subdivision (such as state, prefecture, or province) in [ISO 3166-2](https://www.iso.org/standard/72483.html) format.
     * @maxLength 50
     */
    subdivision?: string | null;
    /**
     * City name.
     * @maxLength 50
     */
    city?: string | null;
    /**
     * Postal or zip code.
     * @maxLength 50
     */
    postalCode?: string | null;
    /** Street address. */
    streetAddress?: StreetAddress;
    /**
     * Main address line (usually street name and number).
     * @maxLength 150
     */
    addressLine1?: string | null;
    /**
     * Free text providing more detailed address info. Usually contains apt, suite, floor.
     * @maxLength 100
     */
    addressLine2?: string | null;
    /** Geocode object containing latitude and longitude coordinates. */
    location?: AddressLocation;
    /**
     * Country's full name.
     * @readonly
     */
    countryFullname?: string | null;
    /**
     * Subdivision full-name.
     * @readonly
     */
    subdivisionFullname?: string | null;
}
interface StreetAddress {
    /** Street number. */
    number?: string;
    /** Street name. */
    name?: string;
    /**
     * Apartment number.
     * @internal
     */
    apt?: string;
    /**
     * Optional address line 1
     * @internal
     */
    formattedAddressLine?: string | null;
}
interface AddressLocation {
    /** Address latitude. */
    latitude?: number | null;
    /** Address longitude. */
    longitude?: number | null;
}
interface DeliveryMethod {
    /**
     * Shipping rate code.
     * Example: `"usps_std_overnight"`.
     * @maxLength 100
     */
    code?: string;
    /**
     * Carrier app ID.
     * @format GUID
     */
    appId?: string | null;
    /**
     * Delivery rate title.
     * Examples: `"USPS Standard Overnight Delivery"`, `"Standard"`, `"First-Class Package International"`.
     */
    title?: TranslatableString;
    /** Whether this method is pickup. */
    pickup?: boolean;
}
declare enum WeightUnit {
    /** Weight unit can't be classified due to an error. */
    UNSPECIFIED_WEIGHT_UNIT = "UNSPECIFIED_WEIGHT_UNIT",
    /** Kilograms. */
    KG = "KG",
    /** Pounds. */
    LB = "LB"
}
/** @enumType */
type WeightUnitWithLiterals = WeightUnit | 'UNSPECIFIED_WEIGHT_UNIT' | 'KG' | 'LB';
interface TaxInfo {
    /**
     * Whether tax is already included in prices (line items, delivery, additional fees).
     * @readonly
     */
    pricesIncludeTax?: boolean;
    /**
     * Tax-exempt group ID applied for tax exemptions.
     * @format GUID
     */
    taxExemptGroupId?: string | null;
}
interface PaymentInfo {
    /**
     * Gift cards applied to the payment. Only one gift card is supported.
     * @readonly
     * @maxSize 1
     */
    giftCards?: V2GiftCard[];
    /**
     * Three-letter currency code in ISO-4217 alphabetic format.
     * Currency used to process payment. Matches `customerInfo.currencyCode` when that currency is supported for payment, otherwise falls back to `businessInfo.currencyCode`.
     * @format CURRENCY
     * @readonly
     */
    currencyCode?: string;
    /** Billing address. */
    billingAddress?: Address;
    /** Billing contact details. */
    billingContact?: FullAddressContactDetails;
    /**
     * ID of the billing address in the legacy addresses service.
     * @internal
     * @maxLength 50
     */
    addressId?: string | null;
    /**
     * ID of the site payment method type the customer selected to pay with. See the [Site Payment Method Types API](https://dev.wix.com/docs/api-reference/business-management/payments/site-payment-method-types/introduction).
     * @internal
     * @format GUID
     */
    sitePaymentMethodTypeId?: string | null;
}
interface V2GiftCard {
    /**
     * Unique identifier of the gift card within the cart.
     * @format GUID
     */
    _id?: string;
    /**
     * Partially masked gift card code.
     * @maxLength 50
     */
    obfuscatedCode?: string;
    /**
     * App ID of the gift card provider.
     * @format GUID
     */
    appId?: string;
    /**
     * External ID in the gift card provider's system, used for integration and tracking across platforms.
     * @minLength 1
     * @maxLength 50
     */
    externalId?: string | null;
    /** Amount to redeem from this gift card. */
    requestedRedeemAmount?: ConvertedMoney;
}
/** Full contact details for an address */
interface FullAddressContactDetails {
    /**
     * First name.
     * @maxLength 100
     */
    firstName?: string | null;
    /**
     * Last name.
     * @maxLength 100
     */
    lastName?: string | null;
    /**
     * Phone number.
     * @format PHONE
     */
    phone?: string | null;
    /**
     * Company name.
     * @maxLength 1000
     */
    company?: string | null;
    /** Tax information (for Brazil only). If ID is provided, `vatId.type` must also be set, `UNSPECIFIED` is not allowed. */
    vatId?: VatId;
}
interface CartSettings {
    /** Whether adding or removing a gift card from the cart is restricted. */
    lockGiftCard?: boolean | null;
    /** Whether adding or removing a coupon code from the cart is restricted. */
    lockCouponCode?: boolean | null;
    /** Whether the policy agreement checkbox is disabled on the checkout page. */
    policyAgreementCheckboxDisabled?: boolean | null;
    /** Whether the manual payment option is disabled on the checkout page. */
    manualPaymentDisabled?: boolean | null;
}
interface LegacyFields {
    /**
     * The coupon discount amount.
     * @readonly
     */
    couponDiscount?: ConvertedMoney;
    /**
     * The sum of all line items after applying all discounts.
     * @readonly
     */
    subtotalAfterCouponDiscount?: ConvertedMoney;
    /**
     * Indicates that this cart was created by the Buy Now flow.
     * Used by the Cart API proxy to determine whether to produce legacy V1 domain events.
     * @readonly
     * @immutable
     */
    buyNow?: boolean;
    /**
     * Custom fields.
     * @maxSize 100
     */
    customFields?: CustomField[];
}
interface CustomField {
    /** Custom field value. */
    value?: any;
    /**
     * Custom field title.
     * @minLength 1
     * @maxLength 500
     */
    title?: string;
    /**
     * Translated custom field title.
     * @minLength 1
     * @maxLength 500
     */
    translatedTitle?: string | null;
}
interface FormInfo {
    /**
     * Identifier of the form schema definition.
     * @readonly
     */
    schemaIdentifier?: FormIdentifier;
    /**
     * Form instance ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * IDs of the line items associated with this form.
     * @format GUID
     * @readonly
     * @maxSize 300
     */
    lineItemIds?: string[];
    /**
     * Translated title of the form.
     * @maxLength 100
     * @readonly
     */
    title?: string;
    /**
     * Translated subtitles of the form.
     * @maxSize 10
     * @maxLength 100
     * @readonly
     */
    subtitles?: string[] | null;
    /**
     * Form submission ID.
     * @format GUID
     */
    formSubmissionId?: string | null;
}
interface FormIdentifier {
    /** @format GUID */
    formSchemaId?: string;
    /** @maxLength 100 */
    namespace?: string;
}
interface CartEstimationIndicators {
    /** Whether the cart may contain any additional fees. */
    hasAdditionalFees?: boolean;
    /** Whether the cart may contain any cart-level custom violations. */
    hasPreCheckoutCustomViolations?: boolean;
}
interface AppliedDiscount extends AppliedDiscountDiscountSourceOneOf {
    /** Coupon details. */
    coupon?: Coupon;
    /** Merchant discount. */
    merchantDiscount?: MerchantDiscount;
    /** Discount rule */
    discountRule?: DiscountRule;
    /** Discount type. */
    discountType?: DiscountTypeWithLiterals;
    /**
     * IDs of line items the discount applies to.
     * @format GUID
     */
    lineItemIds?: string[];
    /**
     * Discount ID.
     * @internal
     * @format GUID
     */
    _id?: string | null;
    /**
     * Discount distribution.
     * Each entry contains the discount amount allocated to a specific line item.
     * Returned only when `apply_item_discounts_to_prices` is enabled.
     * @internal
     * @maxSize 300
     * @readonly
     */
    lineItemDiscounts?: LineItemDiscount[];
    /**
     * Number of subscription cycle this discount applies to
     * default None - all billing cycle
     * @internal
     * @min 1
     * @max 999
     */
    subscriptionCycles?: number | null;
    /**
     * A list of item combinations for this applied discount.
     * Each entry represents a unique combination of line items that triggered
     * or received this discount, along with how many times that combination was applied together.
     * Relevant ONLY for BXGY and Quantity-based promotions.
     * In BXGY the combination will contain the "X" items with discount amount of 0.
     * @internal
     * @maxSize 1000
     */
    itemCombinations?: ItemCombination[];
}
/** @oneof */
interface AppliedDiscountDiscountSourceOneOf {
    /** Coupon details. */
    coupon?: Coupon;
    /** Merchant discount. */
    merchantDiscount?: MerchantDiscount;
    /** Discount rule */
    discountRule?: DiscountRule;
}
declare enum DiscountType {
    GLOBAL = "GLOBAL",
    SPECIFIC_ITEMS = "SPECIFIC_ITEMS",
    SHIPPING = "SHIPPING"
}
/** @enumType */
type DiscountTypeWithLiterals = DiscountType | 'GLOBAL' | 'SPECIFIC_ITEMS' | 'SHIPPING';
/** Coupon */
interface Coupon {
    /** Coupon ID. */
    _id?: string;
    /** Coupon code. */
    code?: string;
    /** Coupon value. */
    amount?: MultiCurrencyPrice;
    /** Coupon name. */
    name?: string;
    /**
     * Coupon type: We want it to be an enum and not a string but currently we have no time to do it so we leave it as is to be aligned with cart summary.
     * @internal
     * @deprecated
     */
    couponType?: string;
}
interface MultiCurrencyPrice {
    /**
     * Amount.
     * @decimalValue options { gte:0, lte:1000000000000000 }
     */
    amount?: string;
    /**
     * Converted amount.
     * @readonly
     * @decimalValue options { gte:0, lte:1000000000000000 }
     */
    convertedAmount?: string;
    /**
     * Amount formatted with currency symbol.
     * @readonly
     */
    formattedAmount?: string;
    /**
     * Converted amount formatted with currency symbol.
     * @readonly
     */
    formattedConvertedAmount?: string;
}
interface MerchantDiscount {
    /** Discount value. */
    amount?: MultiCurrencyPrice;
    /**
     * Discount Percentage. Will be calculated from items price before other discounts.
     * @min 0.01
     * @max 100
     */
    percentage?: number | null;
}
interface DiscountRule {
    /**
     * Discount rule ID
     * @format GUID
     */
    _id?: string;
    /** Discount rule name */
    name?: DiscountRuleName;
    /** Discount value. */
    amount?: MultiCurrencyPrice;
}
interface DiscountRuleName {
    /**
     * Original discount rule name (in site's default language).
     * @minLength 1
     * @maxLength 256
     */
    original?: string;
    /**
     * Translated discount rule name according to buyer language. Defaults to `original` when not provided.
     * @minLength 1
     * @maxLength 500
     */
    translated?: string | null;
}
interface LineItemDiscount {
    /**
     * ID of line item the discount applies to.
     * @format GUID
     */
    _id?: string;
    /** Discount value. */
    totalDiscountAmount?: MultiCurrencyPrice;
}
interface ItemCombination {
    /**
     * The number of times this exact combination of items (with the specified quantities) was applied together in the order.
     * @min 1
     * @max 100000
     */
    count?: number;
    /**
     * Line items that participated together in this combination.
     * @minSize 1
     * @maxSize 100
     */
    lineItems?: ItemCombinationLineItem[];
}
interface ItemCombinationLineItem {
    /**
     * The unique ID of the line item to which this discount applies.
     * @format GUID
     */
    lineItemId?: string;
    /** Total discount amount for all units (quantity) of this line item in this combination. */
    discountAmount?: MultiCurrencyPrice;
    /**
     * Number of units from this line item that participated in a single combination.
     * @min 1
     * @max 100000
     */
    quantity?: number;
}
interface CartSummary {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId?: string;
    /** Cart revision. */
    cartRevision?: string;
    /**
     * Unique identifier for this cart calculation.
     * @format GUID
     */
    calculationId?: string;
    /**
     * Summarized details for each line item in the cart.
     * @maxSize 300
     */
    lineItems?: LineItemSummary[];
    /**
     * Discounts applied to the cart.
     * @maxSize 100
     */
    discounts?: Discount[];
    /** Summary of delivery costs. */
    deliverySummary?: DeliverySummary;
    /**
     * Additional fees applied to the cart.
     * @maxSize 100
     */
    additionalFees?: V2AdditionalFee[];
    /** Tax calculation summary for the cart. */
    taxSummary?: V2TaxSummary;
    /** Summary of calculated price components for the cart. */
    priceSummary?: V2PriceSummary;
    /** How the customer pays for the order. */
    paymentSummary?: PaymentSummary;
    /**
     * Opaque token for the cart's calculated prices.
     *
     * Pass this token when placing an order so the prices the customer saw match checkout.
     *
     * If provided, the server validates it by recalculating the cart and comparing the pricing token. If validation fails, checkout is rejected.
     *
     * This limits unexpected price changes or tampering between cart calculation and order placement.
     *
     * Don't parse or modify the token on the client.
     * @maxLength 1024
     */
    priceVerificationToken?: string | null;
    /**
     * Errors from the calculation step.
     *
     * Deprecated. Use `violations` instead.
     * @deprecated Errors from the calculation step.
     *
     * Deprecated. Use `violations` instead.
     * @replacedBy violations
     * @targetRemovalDate 2026-08-01
     */
    calculationErrors?: V2CalculationErrors;
    /**
     * Business violations from the Validations service plugin ([SDK](https://dev.wix.com/docs/sdk/backend-modules/ecom/service-plugins/validations/introduction) | [REST](https://dev.wix.com/docs/rest/business-solutions/e-commerce/service-plugins/validations-integration-service-plugin/introduction)).
     *
     * Deprecated. Use `violations` instead.
     * @maxSize 100
     * @deprecated Business violations from the Validations service plugin ([SDK](https://dev.wix.com/docs/sdk/backend-modules/ecom/service-plugins/validations/introduction) | [REST](https://dev.wix.com/docs/rest/business-solutions/e-commerce/service-plugins/validations-integration-service-plugin/introduction)).
     *
     * Deprecated. Use `violations` instead.
     * @replacedBy violations
     * @targetRemovalDate 2026-08-01
     */
    spiViolations?: Violation[];
    /**
     * All business violations for the cart.
     * @maxSize 100
     */
    violations?: V2Violation[];
}
interface LineItemSummary {
    /**
     * Line item ID.
     * @format GUID
     */
    lineItemId?: string;
    /**
     * Line item quantity.
     * @min 1
     * @max 100000
     */
    quantity?: number;
    /**
     * Final unit price after discounts.
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    unitPrice?: ConvertedMoney;
    /**
     * Line total (unit price × quantity).
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    totalPrice?: ConvertedMoney;
}
interface Discount {
    /** Display name of the discount. */
    name?: TranslatableString;
    /** Source of the discount; how it was applied. */
    source?: DiscountSource;
    /** Part of the cart the discount applies to. */
    scope?: DiscountScopeWithLiterals;
    /**
     * Number of subscription billing cycles this discount applies to. If unset, it applies to all cycles.
     * @min 1
     * @max 1000
     */
    subscriptionCycles?: number | null;
    /** Total amount discounted across all of this discount's benefits. */
    total?: ConvertedMoney;
    /**
     * Per-line-item discount amounts for item-level discounts.
     * @maxSize 100
     */
    lineItemDiscounts?: V2LineItemDiscount[];
}
declare enum DiscountScope {
    /** Unknown discount scope. */
    UNKNOWN_DISCOUNT_SCOPE = "UNKNOWN_DISCOUNT_SCOPE",
    /** Discount applies to the cart subtotal. */
    CART_SUBTOTAL = "CART_SUBTOTAL",
    /** Discount applies to a specific line item. */
    LINE_ITEM = "LINE_ITEM",
    /** Discount applies to the delivery price. */
    DELIVERY = "DELIVERY"
}
/** @enumType */
type DiscountScopeWithLiterals = DiscountScope | 'UNKNOWN_DISCOUNT_SCOPE' | 'CART_SUBTOTAL' | 'LINE_ITEM' | 'DELIVERY';
interface V2LineItemDiscount {
    /**
     * Line item ID the discount applies to.
     * @format GUID
     */
    lineItemId?: string;
    /** Total amount discounted on this line item. */
    amountOff?: ConvertedMoney;
}
interface DeliverySummary {
    /** Calculated delivery method. */
    method?: DeliveryMethod;
    /**
     * Delivery price before discounts.
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    price?: ConvertedMoney;
}
interface V2AdditionalFee {
    /**
     * Additional fee code.
     * @minLength 1
     * @maxLength 100
     */
    code?: string | null;
    /** Additional fee display name. */
    name?: TranslatableString;
    /**
     * App ID of the fee provider.
     * @minLength 1
     * @maxLength 100
     */
    providerAppId?: string | null;
    /**
     * Line items this fee applies to.
     * When empty, the fee applies to the whole cart.
     * @format GUID
     * @maxSize 100
     */
    lineItemIds?: string[];
    /**
     * Additional fee amount.
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    price?: ConvertedMoney;
    /**
     * Number of subscription cycles this fee applies to.
     * When unset and the fee is tied to a subscription line item, it applies to all cycles.
     * @min 1
     * @max 999
     */
    subscriptionCycles?: number | null;
    /** Where the additional fee came from. */
    source?: SourceWithLiterals;
}
declare enum Source {
    /** Unknown fee source. */
    UNKNOWN_ADDITIONAL_FEE_SOURCE = "UNKNOWN_ADDITIONAL_FEE_SOURCE",
    /** Added by an additional fee service plugin. */
    SERVICE_PLUGIN = "SERVICE_PLUGIN",
    /** Added on the item from the catalog or as a custom line item. */
    LINE_ITEM = "LINE_ITEM",
    /** Added by the delivery provider. */
    DELIVERY = "DELIVERY",
    /** Added by a Wix vertical as a platform fee. */
    PLATFORM = "PLATFORM"
}
/** @enumType */
type SourceWithLiterals = Source | 'UNKNOWN_ADDITIONAL_FEE_SOURCE' | 'SERVICE_PLUGIN' | 'LINE_ITEM' | 'DELIVERY' | 'PLATFORM';
interface V2TaxSummary {
    /**
     * Taxes applied to the cart.
     * Multiple entries can appear when advanced tax rates are set, or one entry for the total.
     * See [advanced tax rates](https://support.wix.com/en/article/setting-up-tax-calculation-using-the-manual-method#step-3-optional-customize-advanced-mode-settings).
     * @maxSize 100
     */
    taxes?: Tax[];
    /** Amount exempt from tax. */
    totalExempt?: ConvertedMoney;
    /**
     * Whether tax is included in prices for line items, discounts, delivery, and additional fees.
     * @readonly
     */
    pricesIncludeTax?: boolean;
    /**
     * Per line item tax amounts.
     * @maxSize 300
     */
    lineItemTaxes?: LineItemTax[];
    /** Tax on the delivery method. */
    deliveryTax?: ConvertedMoney;
    /**
     * Tax on additional fees.
     * @maxSize 100
     */
    additionalFeeTaxes?: AdditionalFeeTax[];
}
interface Tax {
    /** Tax name. */
    name?: TranslatableString;
    /** Amount tax is calculated on. */
    taxableAmount?: ConvertedMoney;
    /** Amount exempt from tax. */
    exemptAmount?: ConvertedMoney;
    /** Calculated tax from `taxableAmount`. */
    amount?: ConvertedMoney;
}
interface LineItemTax {
    /**
     * Line item ID.
     * @format GUID
     */
    lineItemId?: string;
    /** Tax amount for the line item. */
    tax?: ConvertedMoney;
    /**
     * Tax rate as a decimal between 0 and 1.
     * @decimalValue options { gte:0, lte:1, maxScale:6 }
     */
    rate?: string | null;
}
interface AdditionalFeeTax {
    /**
     * Additional fee code.
     * @minLength 1
     * @maxLength 100
     */
    additionalFeeCode?: string;
    /** Tax amount for the additional fee. */
    tax?: ConvertedMoney;
}
interface V2PriceSummary {
    /**
     * Total for all line items after item-level discounts.
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    subtotal?: ConvertedMoney;
    /**
     * Cart subtotal discount amount, deducted from the subtotal.
     * Item-level discounts are already reflected in the subtotal.
     * Returns 0 when no cart subtotal discount is applied.
     * For details of all applied discounts (including delivery discounts), see `cartSummary.discounts`.
     * Currently, only a single coupon is supported.
     */
    discount?: ConvertedMoney;
    /**
     * Delivery price after discounts.
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    delivery?: ConvertedMoney;
    /**
     * Total of additional fees.
     * Includes tax when `taxSummary.pricesIncludeTax` is true.
     */
    additionalFees?: ConvertedMoney;
    /**
     * Tax charged on top of the prices, summed across items, discounts, delivery, and fees.
     * When `taxSummary.pricesIncludeTax` is true, tax is already embedded in the prices, so this field is `0`.
     * For the full tax breakdown in either case, see `taxSummary.taxes`.
     */
    tax?: ConvertedMoney;
    /** Amount to pay. */
    total?: ConvertedMoney;
}
interface PaymentSummary {
    /**
     * Per-gift card deduction amounts. Only one gift card is supported today.
     * @maxSize 1
     */
    giftCards?: GiftCardSummary[];
    /**
     * Memberships charged with the order.
     * @maxSize 300
     */
    memberships?: MembershipSummary[];
    /**
     * Subscription charge details for checkout.
     * @maxSize 100
     */
    subscriptionCharges?: SubscriptionCharges[];
    /** Whether additional payment is required to create the order after gift cards. */
    requiresPaymentAfterGiftCard?: boolean;
    /** Total due after gift cards. */
    totalAfterGiftCards?: ConvertedMoney;
    /** Amount charged when the order is placed, after gift cards. */
    payNow?: ConvertedMoney;
    /** Amount collected later. */
    payLater?: ConvertedMoney;
    /** Amount charged after a free trial, often for subscriptions. */
    payAfterFreeTrial?: ConvertedMoney;
}
interface GiftCardSummary {
    /**
     * Gift card ID assigned by the cart.
     * @format GUID
     */
    giftCardId?: string;
    /** Amount to redeem from the gift card balance. */
    redeemAmount?: ConvertedMoney;
}
interface MembershipSummary extends MembershipSummaryMembershipIdentifierOneOf {
    /**
     * Membership the customer already owns.
     * @internal
     */
    existingMembership?: V2ExistingMembership;
    /**
     * Membership created after the plan in this cart is purchased.
     * @internal
     */
    futureMembership?: FutureMembership;
    /**
     * Membership ID.
     * Available only for a membership that already exists. A membership granted by a plan in this cart has no ID until that plan is purchased.
     * @maxLength 200
     */
    _id?: string;
    /**
     * App ID for this payment option.
     * Available only for a membership that already exists.
     * @format GUID
     */
    appId?: string;
    /**
     * Line item IDs this membership applies to.
     * @minSize 1
     * @maxSize 300
     * @minLength 1
     * @maxLength 100
     */
    lineItemIds?: string[];
}
/** @oneof */
interface MembershipSummaryMembershipIdentifierOneOf {
    /**
     * Membership the customer already owns.
     * @internal
     */
    existingMembership?: V2ExistingMembership;
    /**
     * Membership created after the plan in this cart is purchased.
     * @internal
     */
    futureMembership?: FutureMembership;
}
interface SubscriptionCharges {
    /**
     * Line items covered by the subscription.
     * @format GUID
     * @minSize 1
     * @maxSize 300
     */
    lineItemIds?: string[];
    /**
     * Human-readable schedule and amounts for upcoming charges.
     * Localized to the buyer's language for subscription orders, or the site language if none is set.
     * @maxLength 1000
     */
    description?: string | null;
    /**
     * Scheduled charges.
     * @maxSize 50
     */
    charges?: Charge[];
}
interface Charge {
    /** First billing cycle for this charge. */
    cycleFrom?: number;
    /** Number of cycles this charge covers, when set. */
    cycleCount?: number | null;
    /** Billing period start. */
    cycleBillingDate?: Date | null;
    /**
     * Price breakdown for the charge. Used by PricingPlans legacy service. Can be deprecated when PP moves fully to DraftOrder.
     * @internal
     */
    chargeSummary?: PriceSummary;
}
interface PriceSummary {
    /** Subtotal of all line items, before discounts and before tax. */
    subtotal?: Price;
    /** Total shipping price, before discounts and before tax. */
    shipping?: Price;
    /** Total tax applied to the order. */
    tax?: Price;
    /** Total discount amount applied to the order. */
    discount?: Price;
    /**
     * Deprecated - use `total` instead.
     * @internal
     * @deprecated
     */
    totalPrice?: Price;
    /** Order's total price after discounts and tax. */
    total?: Price;
    /**
     * Order's total price including gift card.
     * @internal
     * @deprecated
     */
    totalWithGiftCard?: Price;
    /**
     * Order's total price after without gift card.
     * @internal
     * @deprecated
     */
    totalWithoutGiftCard?: Price;
    /** Total additional fees before tax. */
    totalAdditionalFees?: Price;
}
interface Price {
    /**
     * Amount.
     * @decimalValue options { gte:0, lte:1000000000000000 }
     */
    amount?: string;
    /**
     * Amount formatted with currency symbol.
     * @readonly
     */
    formattedAmount?: string;
}
interface V2CalculationErrors extends V2CalculationErrorsShippingCalculationErrorOneOf {
    /** General shipping calculation error. */
    generalShippingCalculationError?: Details;
    /** Carrier-specific shipping errors. */
    carrierErrors?: V2CarrierErrors;
    /** Error for the selected shipping option. */
    selectedShippingOptionError?: Details;
    /** Tax calculation error. */
    taxCalculationError?: Details;
    /** Coupon calculation error. */
    couponCalculationError?: Details;
    /** Gift card calculation error. */
    giftCardCalculationError?: Details;
    /**
     * Order validation errors.
     * @maxSize 100
     */
    orderValidationErrors?: ApplicationError[];
    /**
     * Membership payment calculation issues.
     * For example, a line item must use membership payment but none is set, or the membership isn't valid.
     */
    membershipError?: Details;
    /** Discount rule calculation error. */
    discountsCalculationError?: Details;
}
/** @oneof */
interface V2CalculationErrorsShippingCalculationErrorOneOf {
    /** General shipping calculation error. */
    generalShippingCalculationError?: Details;
    /** Carrier-specific shipping errors. */
    carrierErrors?: V2CarrierErrors;
    /** Error for the selected shipping option. */
    selectedShippingOptionError?: Details;
}
interface Details extends DetailsKindOneOf {
    applicationError?: ApplicationError;
    validationError?: ValidationError;
    systemError?: SystemError;
    /**
     * deprecated in API's - to enable migration from rendering arbitrary tracing to rest response
     * @deprecated
     */
    tracing?: Record<string, string>;
}
/** @oneof */
interface DetailsKindOneOf {
    applicationError?: ApplicationError;
    validationError?: ValidationError;
    systemError?: SystemError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
/**
 * example result:
 * {
 * "fieldViolations": [
 * {
 * "field": "fieldA",
 * "description": "invalid music note. supported notes: [do,re,mi,fa,sol,la,ti]",
 * "violatedRule": "OTHER",
 * "ruleName": "INVALID_NOTE",
 * "data": {
 * "value": "FI"
 * }
 * },
 * {
 * "field": "fieldB",
 * "description": "field value out of range. supported range: [0-20]",
 * "violatedRule": "MAX",
 * "data": {
 * "threshold": 20
 * }
 * },
 * {
 * "field": "fieldC",
 * "description": "invalid phone number. provide a valid phone number of size: [7-12], supported characters: [0-9, +, -, (, )]",
 * "violatedRule": "FORMAT",
 * "data": {
 * "type": "PHONE"
 * }
 * }
 * ]
 * }
 */
interface ValidationError {
    fieldViolations?: FieldViolation[];
}
declare enum RuleType {
    VALIDATION = "VALIDATION",
    OTHER = "OTHER",
    MAX = "MAX",
    MIN = "MIN",
    MAX_LENGTH = "MAX_LENGTH",
    MIN_LENGTH = "MIN_LENGTH",
    MAX_SIZE = "MAX_SIZE",
    MIN_SIZE = "MIN_SIZE",
    FORMAT = "FORMAT",
    DECIMAL_LTE = "DECIMAL_LTE",
    DECIMAL_GTE = "DECIMAL_GTE",
    DECIMAL_LT = "DECIMAL_LT",
    DECIMAL_GT = "DECIMAL_GT",
    DECIMAL_MAX_SCALE = "DECIMAL_MAX_SCALE",
    INVALID_ENUM_VALUE = "INVALID_ENUM_VALUE",
    REQUIRED_FIELD = "REQUIRED_FIELD",
    FIELD_NOT_ALLOWED = "FIELD_NOT_ALLOWED",
    ONE_OF_ALIGNMENT = "ONE_OF_ALIGNMENT",
    EXACT_LENGTH = "EXACT_LENGTH",
    EXACT_SIZE = "EXACT_SIZE",
    REQUIRED_ONE_OF_FIELD = "REQUIRED_ONE_OF_FIELD"
}
/** @enumType */
type RuleTypeWithLiterals = RuleType | 'VALIDATION' | 'OTHER' | 'MAX' | 'MIN' | 'MAX_LENGTH' | 'MIN_LENGTH' | 'MAX_SIZE' | 'MIN_SIZE' | 'FORMAT' | 'DECIMAL_LTE' | 'DECIMAL_GTE' | 'DECIMAL_LT' | 'DECIMAL_GT' | 'DECIMAL_MAX_SCALE' | 'INVALID_ENUM_VALUE' | 'REQUIRED_FIELD' | 'FIELD_NOT_ALLOWED' | 'ONE_OF_ALIGNMENT' | 'EXACT_LENGTH' | 'EXACT_SIZE' | 'REQUIRED_ONE_OF_FIELD';
interface FieldViolation {
    field?: string;
    description?: string;
    violatedRule?: RuleTypeWithLiterals;
    /** applicable when violated_rule=OTHER */
    ruleName?: string | null;
    data?: Record<string, any> | null;
}
interface SystemError {
    /** Error code. */
    errorCode?: string | null;
}
interface V2CarrierErrors {
    /**
     * Per-carrier errors.
     * @maxSize 100
     */
    errors?: V2CarrierError[];
}
interface V2CarrierError {
    /**
     * Carrier ID.
     * @format GUID
     */
    carrierId?: string;
    /** Error payload. */
    error?: Details;
}
interface Violation {
    /** Severity of the violation. The violations are shown on the cart and checkout pages. A warning is displayed as yellow, and allows a site visitor to proceed with caution. An error is displayed as red, and doesn't allow a site visitor to proceed with the eCommerce flow. */
    severity?: SeverityWithLiterals;
    /** Target location on a checkout or cart page where the violation will be displayed. */
    target?: Target;
    /**
     * Violation description. Can include rich text. Only HTTP or HTTPS links in the following format are allowed: `<a href="https://www.wix.com">Click me</a>`.
     * @minLength 1
     * @maxLength 1000
     */
    description?: string | null;
}
declare enum Severity {
    /** The user is allowed to move forward in the flow. */
    WARNING = "WARNING",
    /**
     * The user is blocked from moving forward in the flow.
     * For example, if callerContext is CART - moving to checkout is blocked. if callerContext is CHECKOUT, placing an order is blocked.
     */
    ERROR = "ERROR"
}
/** @enumType */
type SeverityWithLiterals = Severity | 'WARNING' | 'ERROR';
interface Target extends TargetTargetTypeOneOf {
    /** General (other) violation. */
    other?: Other;
    /** Specific line item violation. */
    lineItem?: LineItem;
}
/** @oneof */
interface TargetTargetTypeOneOf {
    /** General (other) violation. */
    other?: Other;
    /** Specific line item violation. */
    lineItem?: LineItem;
}
/** Available locations on the webpage */
declare enum NameInOther {
    /** Default location, in case no specific location is specified. */
    OTHER_DEFAULT = "OTHER_DEFAULT"
}
/** @enumType */
type NameInOtherWithLiterals = NameInOther | 'OTHER_DEFAULT';
/** Available locations on the line item */
declare enum NameInLineItem {
    /** Default location, in case no specific location is specified. */
    LINE_ITEM_DEFAULT = "LINE_ITEM_DEFAULT"
}
/** @enumType */
type NameInLineItemWithLiterals = NameInLineItem | 'LINE_ITEM_DEFAULT';
declare enum SuggestedFix {
    /** No suggested fix is specified. The user should refer to the violation description to resolve the issue. */
    UNKNOWN_SUGGESTED_FIX = "UNKNOWN_SUGGESTED_FIX",
    /** The line item should be removed from the cart or checkout to resolve the violation. */
    REMOVE_LINE_ITEM = "REMOVE_LINE_ITEM"
}
/** @enumType */
type SuggestedFixWithLiterals = SuggestedFix | 'UNKNOWN_SUGGESTED_FIX' | 'REMOVE_LINE_ITEM';
/** General (other) violation. */
interface Other {
    /** Location on a checkout or a cart page where a general (other) violation will be displayed. */
    name?: NameInOtherWithLiterals;
}
/** Specific line item violation. */
interface LineItem {
    /** Location on a checkout or a cart page where the specific line item violation will be displayed. */
    name?: NameInLineItemWithLiterals;
    /** ID of the line item containing the violation. */
    _id?: string | null;
    /**
     * Suggested fix for resolving the line item violation.
     * @internal
     */
    suggestedFix?: SuggestedFixWithLiterals;
}
interface V2Violation extends V2ViolationDataOneOf {
    /** Payload for Validations service plugin violations. */
    customValidationData?: CustomValidationErrorData;
    /** Payload when a coupon's status is invalid. */
    invalidCouponStatusData?: InvalidCouponStatusErrorData;
    /** Payload when a coupon code is invalid. */
    invalidCouponCodeData?: InvalidCouponCodeErrorData;
    /** Payload when a gift card code is invalid. */
    invalidGiftCardCodeData?: InvalidGiftCardCodeErrorData;
    /** Payload when a gift card's status is invalid. */
    invalidGiftCardStatusData?: InvalidGiftCardStatusErrorData;
    /** Payload when line items are out of stock. */
    lineItemsOutOfStockData?: LineItemsOutOfStockErrorData;
    /** Payload for members-only items. */
    membersOnlyItemData?: MembersOnlyItemErrorData;
    /** Payload when the cart's subtotal is below the minimum order subtotal. */
    subtotalBelowOrderMinimumData?: SubtotalBelowOrderMinimumErrorData;
    /** Payload when a selected membership has an invalid status. */
    invalidMembershipStatusData?: InvalidMembershipStatusErrorData;
    /** Payload when a subscription item can't be purchased on a demo cart. */
    subscriptionItemNotSupportedOnDemoCartData?: SubscriptionItemNotSupportedOnDemoCartErrorData;
    /** Payload when a gift card item can't be purchased on a demo cart. */
    giftCardItemNotSupportedOnDemoCartData?: GiftCardItemNotSupportedOnDemoCartErrorData;
    /** Violation scope. */
    scope?: ViolationScopeWithLiterals;
    /** Violation type code. */
    code?: ViolationCodeWithLiterals;
    /**
     * Human-readable violation message.
     * @maxLength 1000
     */
    description?: string | null;
    /** How serious the violation is; errors block placing an order. */
    severity?: ViolationSeverityWithLiterals;
}
/** @oneof */
interface V2ViolationDataOneOf {
    /** Payload for Validations service plugin violations. */
    customValidationData?: CustomValidationErrorData;
    /** Payload when a coupon's status is invalid. */
    invalidCouponStatusData?: InvalidCouponStatusErrorData;
    /** Payload when a coupon code is invalid. */
    invalidCouponCodeData?: InvalidCouponCodeErrorData;
    /** Payload when a gift card code is invalid. */
    invalidGiftCardCodeData?: InvalidGiftCardCodeErrorData;
    /** Payload when a gift card's status is invalid. */
    invalidGiftCardStatusData?: InvalidGiftCardStatusErrorData;
    /** Payload when line items are out of stock. */
    lineItemsOutOfStockData?: LineItemsOutOfStockErrorData;
    /** Payload for members-only items. */
    membersOnlyItemData?: MembersOnlyItemErrorData;
    /** Payload when the cart's subtotal is below the minimum order subtotal. */
    subtotalBelowOrderMinimumData?: SubtotalBelowOrderMinimumErrorData;
    /** Payload when a selected membership has an invalid status. */
    invalidMembershipStatusData?: InvalidMembershipStatusErrorData;
    /** Payload when a subscription item can't be purchased on a demo cart. */
    subscriptionItemNotSupportedOnDemoCartData?: SubscriptionItemNotSupportedOnDemoCartErrorData;
    /** Payload when a gift card item can't be purchased on a demo cart. */
    giftCardItemNotSupportedOnDemoCartData?: GiftCardItemNotSupportedOnDemoCartErrorData;
}
declare enum ViolationScope {
    /** Violation applies to the cart as a whole. */
    GLOBAL = "GLOBAL",
    /** Violation is tied to a specific line item. */
    LINE_ITEM = "LINE_ITEM",
    /** Violation is tied to a discount. */
    DISCOUNT = "DISCOUNT",
    /** Violation is tied to the delivery method or address. */
    DELIVERY = "DELIVERY",
    /** Violation is tied to tax calculation. */
    TAX = "TAX",
    /** Violation is tied to a membership payment. */
    MEMBERSHIP = "MEMBERSHIP",
    /** Violation is tied to a gift card. */
    GIFT_CARD = "GIFT_CARD",
    /**
     * Violation is tied to the payment method.
     * @documentationMaturity preview
     * @internal
     */
    PAYMENT = "PAYMENT"
}
/** @enumType */
type ViolationScopeWithLiterals = ViolationScope | 'GLOBAL' | 'LINE_ITEM' | 'DISCOUNT' | 'DELIVERY' | 'TAX' | 'MEMBERSHIP' | 'GIFT_CARD';
declare enum ViolationCode {
    /** A general violation not covered by more specific codes. */
    GENERAL_CODE = "GENERAL_CODE",
    /** Custom violation (e.g., violations from Validation Service Plugin). */
    CUSTOM_VALIDATION = "CUSTOM_VALIDATION",
    /** The cart doesn't contain any line items. */
    EMPTY_CART = "EMPTY_CART",
    /** One or more line items are out of stock. */
    LINE_ITEMS_OUT_OF_STOCK = "LINE_ITEMS_OUT_OF_STOCK",
    /** One or more line items can be purchased only by members and the customer isn't a member. */
    MEMBERS_ONLY_ITEM = "MEMBERS_ONLY_ITEM",
    /** No coupon exists with the provided code. */
    INVALID_COUPON_CODE = "INVALID_COUPON_CODE",
    /** The coupon status isn't valid for use (e.g., expired, usage limit reached, etc.). */
    INVALID_COUPON_STATUS = "INVALID_COUPON_STATUS",
    /** A coupon discount can't be applied when purchasing a gift card. */
    CANNOT_APPLY_COUPON_DISCOUNT_WHEN_PURCHASING_GIFT_CARD = "CANNOT_APPLY_COUPON_DISCOUNT_WHEN_PURCHASING_GIFT_CARD",
    /** The cart is missing a delivery address. */
    MISSING_DELIVERY_ADDRESS = "MISSING_DELIVERY_ADDRESS",
    /** The cart is missing a delivery method. */
    MISSING_DELIVERY_METHOD = "MISSING_DELIVERY_METHOD",
    /** The selected delivery method is no longer available. */
    DELIVERY_METHOD_NO_LONGER_AVAILABLE = "DELIVERY_METHOD_NO_LONGER_AVAILABLE",
    /** The selected delivery method doesn't apply to all line items in the cart. */
    DELIVERY_METHOD_NOT_APPLIED_TO_ALL_LINE_ITEMS = "DELIVERY_METHOD_NOT_APPLIED_TO_ALL_LINE_ITEMS",
    /** No gift card exists with the provided code. */
    INVALID_GIFT_CARD_CODE = "INVALID_GIFT_CARD_CODE",
    /** The gift card status isn't valid for use (e.g., expired, fully redeemed, etc.). */
    INVALID_GIFT_CARD_STATUS = "INVALID_GIFT_CARD_STATUS",
    /** Can't pay for a gift card with another gift card. */
    CANNOT_PAY_FOR_GIFT_CARD_WITH_GIFT_CARD = "CANNOT_PAY_FOR_GIFT_CARD_WITH_GIFT_CARD",
    /** The cart's subtotal is below the minimum order subtotal. */
    SUBTOTAL_BELOW_ORDER_MINIMUM = "SUBTOTAL_BELOW_ORDER_MINIMUM",
    /**
     * A selected membership can't be used to pay for the cart. For example, the membership has
     * insufficient funds, or it no longer applies to the line items it was selected for.
     */
    INVALID_MEMBERSHIP_STATUS = "INVALID_MEMBERSHIP_STATUS",
    /** The address's postal code is missing or invalid. */
    INVALID_POSTAL_CODE = "INVALID_POSTAL_CODE",
    /** The address's subdivision is missing or invalid. */
    INVALID_SUBDIVISION = "INVALID_SUBDIVISION",
    /** A subscription item can't be purchased on a demo cart. */
    SUBSCRIPTION_ITEM_NOT_SUPPORTED_ON_DEMO_CART = "SUBSCRIPTION_ITEM_NOT_SUPPORTED_ON_DEMO_CART",
    /** A gift card item can't be purchased on a demo cart. */
    GIFT_CARD_ITEM_NOT_SUPPORTED_ON_DEMO_CART = "GIFT_CARD_ITEM_NOT_SUPPORTED_ON_DEMO_CART",
    /** A gift card can't be used to pay on a demo cart. */
    GIFT_CARD_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART = "GIFT_CARD_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART",
    /** A membership can't be used to pay on a demo cart. */
    MEMBERSHIP_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART = "MEMBERSHIP_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART",
    /**
     * The cart has no selected payment method while a payment is due.
     * @documentationMaturity preview
     * @internal
     */
    MISSING_SITE_PAYMENT_METHOD_TYPE_ID = "MISSING_SITE_PAYMENT_METHOD_TYPE_ID"
}
/** @enumType */
type ViolationCodeWithLiterals = ViolationCode | 'GENERAL_CODE' | 'CUSTOM_VALIDATION' | 'EMPTY_CART' | 'LINE_ITEMS_OUT_OF_STOCK' | 'MEMBERS_ONLY_ITEM' | 'INVALID_COUPON_CODE' | 'INVALID_COUPON_STATUS' | 'CANNOT_APPLY_COUPON_DISCOUNT_WHEN_PURCHASING_GIFT_CARD' | 'MISSING_DELIVERY_ADDRESS' | 'MISSING_DELIVERY_METHOD' | 'DELIVERY_METHOD_NO_LONGER_AVAILABLE' | 'DELIVERY_METHOD_NOT_APPLIED_TO_ALL_LINE_ITEMS' | 'INVALID_GIFT_CARD_CODE' | 'INVALID_GIFT_CARD_STATUS' | 'CANNOT_PAY_FOR_GIFT_CARD_WITH_GIFT_CARD' | 'SUBTOTAL_BELOW_ORDER_MINIMUM' | 'INVALID_MEMBERSHIP_STATUS' | 'INVALID_POSTAL_CODE' | 'INVALID_SUBDIVISION' | 'SUBSCRIPTION_ITEM_NOT_SUPPORTED_ON_DEMO_CART' | 'GIFT_CARD_ITEM_NOT_SUPPORTED_ON_DEMO_CART' | 'GIFT_CARD_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART' | 'MEMBERSHIP_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART';
declare enum ViolationSeverity {
    /** Informational or advisory violation. Doesn't block placing an order. */
    WARNING = "WARNING",
    /** Blocking violation. Must be resolved before an order can be placed. */
    ERROR = "ERROR"
}
/** @enumType */
type ViolationSeverityWithLiterals = ViolationSeverity | 'WARNING' | 'ERROR';
interface CustomValidationErrorData {
    /** Where to show the violation on the cart or checkout page. */
    target?: Target;
}
interface InvalidCouponStatusErrorData extends InvalidCouponStatusErrorDataDetailsOneOf {
    minLineItemQuantityNotReachedDetails?: MinLineItemQuantityNotReachedDetails;
    minSubtotalNotReachedDetails?: MinSubtotalNotReachedDetails;
    /**
     * Coupon code with an invalid status.
     * @maxLength 100
     */
    couponCode?: string;
    /** Why the coupon status is invalid. */
    reason?: InvalidCouponReasonWithLiterals;
}
/** @oneof */
interface InvalidCouponStatusErrorDataDetailsOneOf {
    minLineItemQuantityNotReachedDetails?: MinLineItemQuantityNotReachedDetails;
    minSubtotalNotReachedDetails?: MinSubtotalNotReachedDetails;
}
declare enum InvalidCouponReason {
    /** Unspecified status. */
    UNKNOWN_INVALID_COUPON_REASON = "UNKNOWN_INVALID_COUPON_REASON",
    /** Coupon has expired. */
    EXPIRED = "EXPIRED",
    /** Coupon is disabled. */
    DISABLED = "DISABLED",
    /** Coupon isn't active yet. */
    INACTIVE = "INACTIVE",
    /** Coupon hit its usage limit. */
    MAX_USAGE_EXCEEDED = "MAX_USAGE_EXCEEDED",
    /** Coupon hit its per-customer usage limit. */
    MAX_USAGE_PER_CUSTOMER_EXCEEDED = "MAX_USAGE_PER_CUSTOMER_EXCEEDED",
    /** Coupon doesn't apply to subscriptions. */
    NOT_APPLICABLE_TO_SUBSCRIPTIONS = "NOT_APPLICABLE_TO_SUBSCRIPTIONS",
    /** Coupon doesn't apply to the cart's selected line items. */
    NOT_APPLICABLE_TO_SELECTED_LINE_ITEMS = "NOT_APPLICABLE_TO_SELECTED_LINE_ITEMS",
    /** Coupon needs a higher line item quantity than the cart has. */
    MIN_LINE_ITEM_QUANTITY_NOT_REACHED = "MIN_LINE_ITEM_QUANTITY_NOT_REACHED",
    /** Coupon needs a higher subtotal than the cart has. */
    MIN_SUBTOTAL_NOT_REACHED = "MIN_SUBTOTAL_NOT_REACHED"
}
/** @enumType */
type InvalidCouponReasonWithLiterals = InvalidCouponReason | 'UNKNOWN_INVALID_COUPON_REASON' | 'EXPIRED' | 'DISABLED' | 'INACTIVE' | 'MAX_USAGE_EXCEEDED' | 'MAX_USAGE_PER_CUSTOMER_EXCEEDED' | 'NOT_APPLICABLE_TO_SUBSCRIPTIONS' | 'NOT_APPLICABLE_TO_SELECTED_LINE_ITEMS' | 'MIN_LINE_ITEM_QUANTITY_NOT_REACHED' | 'MIN_SUBTOTAL_NOT_REACHED';
interface MinLineItemQuantityNotReachedDetails {
    /** Minimum line item quantity required. */
    requiredMinimumQuantity?: number;
}
interface MinSubtotalNotReachedDetails {
    /** Minimum subtotal required. */
    requiredMinimumSubtotal?: ConvertedMoney;
    /**
     * Original wording for the minimum subtotal rule.
     * @maxLength 100
     */
    originalDescription?: string | null;
}
interface InvalidCouponCodeErrorData {
    /**
     * Invalid coupon code.
     * @maxLength 100
     */
    couponCode?: string;
}
interface InvalidGiftCardCodeErrorData {
    /**
     * Invalid gift card code.
     * @maxLength 100
     */
    giftCardCode?: string;
    /**
     * Gift card ID. Available when the gift card was found but failed validation.
     * @format GUID
     */
    giftCardId?: string | null;
}
interface InvalidGiftCardStatusErrorData {
    /**
     * Gift card code with an invalid status.
     * @maxLength 100
     */
    giftCardCode?: string;
    /** Why the gift card status is invalid. */
    reason?: InvalidGiftCardReasonWithLiterals;
    /**
     * Gift card ID. Available when the gift card was found but failed validation.
     * @format GUID
     */
    giftCardId?: string | null;
}
declare enum InvalidGiftCardReason {
    /** Unspecified status. */
    UNKNOWN_INVALID_GIFT_CARD_REASON = "UNKNOWN_INVALID_GIFT_CARD_REASON",
    /** Gift card has expired. */
    EXPIRED = "EXPIRED",
    /** Gift card is disabled. */
    DISABLED = "DISABLED",
    /** Gift card has no balance left. */
    EMPTY_BALANCE = "EMPTY_BALANCE"
}
/** @enumType */
type InvalidGiftCardReasonWithLiterals = InvalidGiftCardReason | 'UNKNOWN_INVALID_GIFT_CARD_REASON' | 'EXPIRED' | 'DISABLED' | 'EMPTY_BALANCE';
interface LineItemsOutOfStockErrorData {
    /**
     * Out-of-stock line item IDs.
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
    /** Whether every line item in the cart is out of stock. */
    allLineItemsOutOfStock?: boolean;
}
interface MembersOnlyItemErrorData {
    /**
     * Members-only line item IDs.
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
}
interface SubtotalBelowOrderMinimumErrorData {
    /** Minimum subtotal required to place an order. */
    minimumOrderSubtotal?: ConvertedMoney;
    /** Amount that needs to be added to the current order to reach the minimum subtotal amount. */
    remainingAmount?: ConvertedMoney;
}
interface InvalidMembershipStatusErrorData {
    /**
     * Membership ID with an invalid status. Absent when the offending membership can't be identified.
     * @maxLength 200
     */
    membershipId?: string | null;
    /** Why the membership status is invalid. */
    reason?: InvalidMembershipReasonWithLiterals;
    /**
     * IDs of the line items this violation applies to.
     *
     * Available only when `reason` is `NOT_APPLICABLE_TO_LINE_ITEMS` or `LINE_ITEM_NOT_FOUND`.
     * @internal
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
}
declare enum InvalidMembershipReason {
    /** The membership can't be used, but the specific reason isn't available. */
    UNKNOWN_INVALID_MEMBERSHIP_REASON = "UNKNOWN_INVALID_MEMBERSHIP_REASON",
    /** The membership doesn't have sufficient funds to pay for the cart. */
    INSUFFICIENT_FUNDS = "INSUFFICIENT_FUNDS",
    /** The selected membership doesn't exist or is no longer available to the customer. */
    MEMBERSHIP_NOT_FOUND = "MEMBERSHIP_NOT_FOUND",
    /** The membership can't be used to pay for the line items it was selected for. */
    NOT_APPLICABLE_TO_LINE_ITEMS = "NOT_APPLICABLE_TO_LINE_ITEMS",
    /** The membership was selected for a line item that isn't in the cart. */
    LINE_ITEM_NOT_FOUND = "LINE_ITEM_NOT_FOUND"
}
/** @enumType */
type InvalidMembershipReasonWithLiterals = InvalidMembershipReason | 'UNKNOWN_INVALID_MEMBERSHIP_REASON' | 'INSUFFICIENT_FUNDS' | 'MEMBERSHIP_NOT_FOUND' | 'NOT_APPLICABLE_TO_LINE_ITEMS' | 'LINE_ITEM_NOT_FOUND';
interface SubscriptionItemNotSupportedOnDemoCartErrorData {
    /**
     * IDs of the subscription line items that can't be purchased on a demo cart.
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
}
interface GiftCardItemNotSupportedOnDemoCartErrorData {
    /**
     * IDs of the gift card line items that can't be purchased on a demo cart.
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
}
/** Triggered when a line item is added to the cart. */
interface LineItemAdded {
    /**
     * ID of the item within the catalog it belongs to.
     * @minLength 1
     * @maxLength 36
     */
    catalogItemId?: string | null;
    /**
     * ID of the app providing the catalog.
     * @minLength 1
     * @maxLength 36
     */
    catalogAppId?: string | null;
    /** Whether the line item is a custom line item. */
    customLineItem?: boolean;
    /**
     * ID of the app that added the line item to the cart (from the identity context).
     * @minLength 1
     * @maxLength 36
     */
    appDefId?: string | null;
    /**
     * ID of the line item in the cart.
     * @maxLength 36
     */
    lineItemId?: string | null;
}
/** Triggered when a coupon is added to the cart. */
interface CouponAdded {
    /**
     * Persistent ID that correlates between the Cart and the created Order.
     * @format GUID
     */
    purchaseFlowId?: string | null;
}
/** Triggered when a coupon is removed from the cart. */
interface CouponRemoved {
    /**
     * Persistent ID that correlates between the Cart and the created Order.
     * @format GUID
     */
    purchaseFlowId?: string | null;
}
/** Triggered when a gift card is added to the cart. */
interface GiftCardAdded {
    /**
     * Persistent ID that correlates between the Cart and the created Order.
     * @format GUID
     */
    purchaseFlowId?: string | null;
}
/** Triggered when a gift card is removed from the cart. */
interface GiftCardRemoved {
    /**
     * Persistent ID that correlates between the Cart and the created Order.
     * @format GUID
     */
    purchaseFlowId?: string | null;
}
interface CreateCartRequest {
    /** Cart to create. */
    cart?: Cart;
    /**
     * Catalog items to add to the cart.
     * @maxSize 300
     */
    catalogItems?: CatalogItemInput[];
    /**
     * Custom items to add to the cart.
     * Custom line items don't trigger the Catalog service plugin.
     * @maxSize 300
     */
    customItems?: CustomItemInput[];
    /**
     * Coupons to apply.
     * Currently only one is supported.
     * @maxSize 1
     */
    coupons?: CouponInput[];
    /** Delivery method to set. */
    deliveryMethod?: DeliveryMethodInput;
    /**
     * Gift cards to add.
     * @maxSize 5
     */
    giftCards?: GiftCardInput[];
}
interface CatalogItemInput {
    /** Reference to the catalog item. */
    catalogReference?: CatalogReference;
    /**
     * Number of catalog items the customer requested.
     * @min 1
     * @max 100000
     */
    quantity?: number;
    /**
     * Selected membership used as payment for this item.
     *
     * DEPRECATED: migrate to `membershipPayment`. Removed once all clients migrate.
     * @deprecated
     * @replacedBy membership_payment
     * @targetRemovalDate 2026-06-22
     */
    selectedMembership?: V2SelectedMembership;
    /** Overriding values for catalog item properties. */
    catalogOverrideFields?: CatalogOverrideFields;
    /**
     * Custom extended fields for the line item object.
     *
     * [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured using the Checkout & Orders - Line Items Schema Plugin in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * ID of the app managing inventory.
     * @format GUID
     */
    inventoryAppId?: string | null;
    /**
     * Line-item ID in external systems.
     * @internal
     * @minLength 1
     * @maxLength 100
     */
    externalResourceId?: string | null;
    /**
     * ID of the app providing upsells for this item.
     * @internal
     * @format GUID
     */
    upsellOptionsAppId?: string | null;
    /**
     * Membership used as payment for this item. Can be an existing membership, or a future one
     * created from a plan in the same cart.
     *
     * Replaces `selectedMembership`.
     *
     * Use with `paymentOption` `MEMBERSHIP` or `MEMBERSHIP_OFFLINE`.
     */
    membershipPayment?: MembershipInput;
}
interface MembershipInput extends MembershipInputMembershipOneOf {
    /** Pay with an existing, already-provisioned membership. */
    existingMembership?: ExistingMembershipInput;
    /** Pay with a future membership, created once a plan in this cart is purchased. */
    futureMembership?: FutureMembershipInput;
}
/** @oneof */
interface MembershipInputMembershipOneOf {
    /** Pay with an existing, already-provisioned membership. */
    existingMembership?: ExistingMembershipInput;
    /** Pay with a future membership, created once a plan in this cart is purchased. */
    futureMembership?: FutureMembershipInput;
}
interface ExistingMembershipInput {
    /**
     * ID of the app providing the membership, for example Pricing Plans.
     * @format GUID
     */
    appId?: string;
    /**
     * ID of the existing membership.
     * @minLength 1
     * @maxLength 200
     */
    membershipId?: string;
}
interface FutureMembershipInput {
    /**
     * ID of the plan line item, already in the cart, whose future membership pays for this item.
     * Add the plan line item to the cart first, then reference it by its ID.
     * @format GUID
     */
    lineItemId?: string;
    /**
     * The selected unit within the plan to redeem.
     * @maxLength 200
     */
    benefitId?: string;
}
interface CustomItemInput {
    /** Display name for the line item. */
    name?: TranslatableString;
    /** Quantity details for the line item. */
    quantityInfo?: CustomItemQuantityInfo;
    /** Pricing details for the line item. */
    pricing?: CustomItemPricingInfo;
    /** Information about the line item's origin. */
    source?: CustomItemSource;
    /** Display details for the item. */
    attributes?: CustomItemAttributes;
    /** Delivery configuration for the line item. */
    deliveryConfig?: CustomItemDeliveryConfig;
    /** Tax configuration for the line item. */
    taxConfig?: CustomItemTaxConfig;
    /** Payment configuration for the line item. */
    paymentConfig?: CustomItemPaymentConfig;
    /**
     * Modifier groups added to the item.
     * @maxSize 25
     */
    modifierGroups?: ModifierGroup[];
    /**
     * Platform fee applied to the line item.
     * @internal
     */
    platformFee?: PlatformFee;
}
interface CustomItemQuantityInfo {
    /**
     * Quantity the customer requested.
     * @min 1
     * @max 100000
     */
    requestedQuantity?: number;
    /** Maximum quantity available for purchase. */
    availableQuantity?: number | null;
    /**
     * Whether the line item quantity is fixed and can't be changed.
     *
     * Default: `false`
     */
    fixedQuantity?: boolean;
}
interface CustomItemPricingInfo {
    /**
     * Sale price for a single item, without modifiers.
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    price?: ConvertedMoney;
    /**
     * Full price for a single item before discounts, without modifiers.
     * Defaults to `price` when not provided.
     * Includes tax when `taxInfo.pricesIncludeTax` is true.
     */
    fullPrice?: ConvertedMoney;
    /**
     * Additional price description.
     * For example, when the price is `0` but you need more detail, such as "Starts at $67".
     */
    priceDescription?: PriceDescription;
    /** Whether the price isn't yet defined and will be updated after the order is created. */
    priceUndetermined?: boolean;
}
interface CustomItemSource {
    /**
     * Catalog and item reference: IDs for the item and catalog, plus optional details.
     * Optional for custom line items, which don't trigger the Catalog service plugin.
     */
    catalogReference?: CatalogReference;
    /**
     * When `catalogReference.catalogItemId` isn't the actual catalog item ID, this field returns the true catalog item ID.
     * For example, for Wix Bookings, `catalogReference.catalogItemId` is the booking ID, so this value is the service ID.
     * In most cases, this matches `catalogReference.catalogItemId`.
     * Used in membership validation.
     * @minLength 1
     * @maxLength 36
     */
    rootCatalogItemId?: string | null;
    /**
     * ID of the app managing inventory.
     * @format GUID
     */
    inventoryAppId?: string | null;
}
interface CustomItemAttributes {
    /**
     * Description lines for the item.
     * @maxSize 100
     */
    descriptionLines?: DescriptionLine[];
    /** Item image. */
    image?: string;
    /** URL of the item's page on the site. */
    url?: string;
    /** Item type: preset or custom. */
    itemType?: ItemType;
    /**
     * Physical properties of the item.
     * When relevant, includes SKU, weight, and shippability.
     */
    physicalProperties?: PhysicalProperties;
    /**
     * Digital file identifier; relevant only for items with type `DIGITAL`.
     * @internal
     */
    digitalFile?: SecuredMedia;
    /**
     * Service properties.
     * When relevant, includes date and number of participants.
     */
    serviceProperties?: ServiceProperties;
    /**
     * Whether the item can only be purchased by site members.
     *
     * Default: `false`
     */
    membersOnly?: boolean;
    /**
     * Subscription option details.
     * @internal
     */
    subscriptionInfo?: SubscriptionOptionInfo;
}
interface CustomItemDeliveryConfig {
    /**
     * Delivery profile ID.
     * @format GUID
     */
    deliveryProfileId?: string | null;
}
interface CustomItemTaxConfig {
    /** Address used for tax calculation. */
    taxableAddress?: TaxableAddress;
    /**
     * Tax group ID for this line item.
     * @maxLength 50
     */
    taxGroupId?: string | null;
}
interface CustomItemPaymentConfig {
    /**
     * Partial payment due upfront during checkout.
     * Only when `paymentOption` is `DEPOSIT_ONLINE`.
     */
    depositAmount?: ConvertedMoney;
    /**
     * Selected payment option type for this item.
     *
     * Default: `FULL_PAYMENT_ONLINE`
     */
    paymentOption?: PaymentOptionTypeWithLiterals;
}
interface PlatformFee {
    /** __Required.__ Platform fee name. */
    name?: TranslatableString;
    /** Platform fee charge type. */
    chargeType?: V1PlatformFeeChargeTypeWithLiterals;
    /**
     * Percentage rate charged as platform fee.
     *
     * The fee rate percentage expressed as a decimal fraction between 0 and 1. For example, `0.05` for 5%.
     * @format DECIMAL_VALUE
     * @decimalValue options { gt:0, lte:1, maxScale:4 }
     */
    percentageRate?: string;
}
declare enum V1PlatformFeeChargeType {
    /** The platform fee is passed on to the customer. */
    PASS_ON = "PASS_ON",
    /** The platform fee is absorbed by the merchant. */
    ABSORBED = "ABSORBED"
}
/** @enumType */
type V1PlatformFeeChargeTypeWithLiterals = V1PlatformFeeChargeType | 'PASS_ON' | 'ABSORBED';
interface CouponInput {
    /**
     * Coupon code.
     * @minLength 1
     * @maxLength 50
     */
    code?: string;
}
interface DeliveryMethodInput {
    /**
     * Delivery method code.
     * @maxLength 100
     */
    code?: string;
    /**
     * Carrier app id.
     * @format GUID
     */
    appId?: string | null;
}
interface GiftCardInput {
    /**
     * Gift card code.
     * @minLength 8
     * @maxLength 20
     */
    code?: string;
    /** The amount requested to redeem from this gift card. */
    redeemAmount?: ConvertedMoney;
}
interface CreateCartResponse {
    /** Created cart. */
    cart?: Cart;
}
interface ItemNotFoundInCatalogErrorData {
    /**
     * Line items missing from the catalog.
     * @maxSize 300
     */
    invalidItems?: LineItemIdentifier[];
}
interface LineItemIdentifier extends LineItemIdentifierIdOneOf {
    /**
     * Line item ID.
     * @format GUID
     */
    lineItemId?: string;
    /** Catalog reference. */
    catalogReference?: CatalogReference;
}
/** @oneof */
interface LineItemIdentifierIdOneOf {
    /**
     * Line item ID.
     * @format GUID
     */
    lineItemId?: string;
    /** Catalog reference. */
    catalogReference?: CatalogReference;
}
interface InsufficientInventoryErrorData {
    /**
     * Line items that don't have enough inventory.
     * @maxSize 300
     */
    invalidItems?: InsufficientInventoryEntry[];
}
interface InsufficientInventoryEntry {
    /** Line item with low stock. */
    _id?: LineItemIdentifier;
    /** Requested quantity, including what's already on the cart. */
    requestedQuantity?: number;
    /** Quantity in stock. */
    availableQuantity?: number;
}
interface MaxLineItemsExceededErrorData {
    /** Line item count in the request. */
    requestCount?: number;
    /** Maximum line items allowed. */
    maxCount?: number;
}
interface MaxItemModifiersExceededErrorData {
    /** Modifier count in the request. */
    requestCount?: number;
    /** Maximum modifiers allowed. */
    maxCount?: number;
}
interface DuplicateModifierGroupIdsErrorData {
    /**
     * Catalog items whose modifier group IDs collide.
     * @maxSize 300
     */
    invalidItems?: CatalogReference[];
}
interface DuplicateItemModifierIdsErrorData {
    /**
     * Catalog items with duplicate modifier IDs in a group.
     * @maxSize 300
     */
    invalidItems?: CatalogReference[];
}
interface LineItemPriceConflictErrorData {
    /**
     * Catalog items whose price no longer matches, when present.
     * @maxSize 300
     */
    invalidItems?: CatalogReference[];
}
interface FixedQuantityItemErrorData {
    /**
     * Line items that don't allow quantity changes.
     * @maxSize 300
     */
    invalidItems?: LineItemIdentifier[];
}
interface InvalidMembershipPaymentOptionErrorData {
    /**
     * Line items with invalid membership payments.
     * @maxSize 300
     */
    invalidItems?: InvalidMembershipEntry[];
}
interface InvalidMembershipEntry {
    /** Line item with an invalid membership payment choice. */
    invalidItemId?: LineItemIdentifier;
    /** Membership the buyer tried to use. */
    selectedMembership?: V2SelectedMembership;
}
interface LineItemNotFoundInCartErrorData {
    /**
     * Line item IDs that aren't in the cart.
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
}
interface DeliveryMethodNotFoundErrorData {
    /**
     * Missing delivery method code.
     * @maxLength 100
     */
    code?: string;
    /**
     * Delivery method app ID, if applicable.
     * @maxLength 100
     */
    appId?: string | null;
}
interface GetCartRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Whether to override default cart refresh behavior.
     * If `true`, always refreshes the cart; if `false`, never refreshes; if unset, uses the default refresh logic.
     * Used by the checkout proxy for backward compatibility.
     * @internal
     */
    refreshOverride?: boolean | null;
}
interface GetCartResponse {
    /** Requested cart. */
    cart?: Cart;
}
interface GetUpsellOptionsRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
}
interface GetUpsellOptionsResponse {
    /** Upsell options available for the cart's line items. */
    itemsUpsells?: ItemUpsells[];
}
interface ItemUpsells {
    /**
     * Line item ID.
     * @format GUID
     */
    lineItemId?: string;
    /**
     * Upsell options available for the line item.
     * @maxSize 10
     */
    upsells?: UpsellOption[];
}
/**
 * A single upsell offer for a line item.
 * Accepting it replaces the line item's catalog reference in place, keeping the same lineItemId.
 */
interface UpsellOption {
    /**
     * Upsell option ID.
     * @format GUID
     */
    _id?: string;
    /**
     * Title of the upsell option.
     * @maxLength 200
     */
    title?: string | null;
    /**
     * Description of the upsell option.
     * @maxLength 500
     */
    description?: string | null;
    /** Catalog reference of the item offered as an upsell to replace the existing one. */
    catalogReference?: CatalogReference;
    /** Metadata about the benefit of accepting this upsell option. */
    metadata?: UpsellBenefitMetadata;
    /** Price details of the upsell option. */
    priceDetails?: PriceDetails;
}
interface UpsellBenefitMetadata {
    /** Benefit type. */
    benefitType?: BenefitTypeWithLiterals;
    /**
     * Benefit value. For percentage benefits, this is the percentage (e.g., "10" for 10%). For fixed amount/price benefits, this is the amount.
     * @decimalValue options { gt:0, lte:1000000000000000, maxScale:4 }
     */
    benefitValue?: string | null;
}
declare enum BenefitType {
    /** Percentage benefit. */
    PERCENTAGE = "PERCENTAGE",
    /** Fixed price benefit. */
    FIXED_PRICE = "FIXED_PRICE",
    /** Fixed amount benefit. */
    FIXED_AMOUNT = "FIXED_AMOUNT"
}
/** @enumType */
type BenefitTypeWithLiterals = BenefitType | 'PERCENTAGE' | 'FIXED_PRICE' | 'FIXED_AMOUNT';
interface PriceDetails {
    /** @decimalValue options { gte:0, lte:1000000000000000 } */
    price?: string;
    /** @maxLength 200 */
    description?: string | null;
}
interface UpdateCartRequest {
    /** Cart to update. May be partial. */
    cart: Cart;
    /**
     * Fields to update.
     *
     * Fields that aren't included in `fieldMask.paths` are ignored.
     * @internal
     */
    fieldMask: string[];
}
interface UpdateCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface CartAlreadyOrderedErrorData {
    /**
     * Cart already turned into an order.
     * @format GUID
     */
    cartId?: string;
    /**
     * Matching order ID.
     * @format GUID
     */
    orderId?: string;
}
interface DeleteCartRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
}
interface DeleteCartResponse {
}
interface RefreshCartRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
}
interface RefreshCartResponse {
    /** Refreshed cart. */
    cart?: Cart;
}
interface EstimateCartRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Whether to retrieve the latest item prices, discounts, and inventory data from the catalogs before estimating the cart.
     *
     * Default: `false`.
     */
    refreshCart?: boolean;
    /** Level of business validation to run during estimation via the [Validations service plugin](https://dev.wix.com/api/rest/wix-ecommerce/validations-integration-spi/introduction). */
    validationConfig?: ValidationsConfigWithLiterals;
    /**
     * Whether to calculate delivery costs.
     *
     * Default: `false`.
     */
    calculateDelivery?: boolean;
    /**
     * Whether to calculate additional fees.
     *
     * Default: `false`.
     */
    calculateAdditionalFees?: boolean;
    /**
     * Whether to calculate tax.
     *
     * Default: `false`.
     */
    calculateTax?: boolean;
    /**
     * Whether to include the effect of gift card balances on the cart total.
     * Gift cards aren't redeemed; balances are reflected for estimation only.
     *
     * Default: `false`.
     */
    calculateGiftCards?: boolean;
}
declare enum ValidationsConfig {
    /** No business validations are performed. */
    SKIP_VALIDATIONS = "SKIP_VALIDATIONS",
    /** Perform cart-level business validations as defined by the service plugin implementer. */
    PRE_CHECKOUT_VALIDATIONS = "PRE_CHECKOUT_VALIDATIONS",
    /** Perform checkout-specific business validations as defined by the service plugin implementer. */
    CHECKOUT_VALIDATIONS = "CHECKOUT_VALIDATIONS"
}
/** @enumType */
type ValidationsConfigWithLiterals = ValidationsConfig | 'SKIP_VALIDATIONS' | 'PRE_CHECKOUT_VALIDATIONS' | 'CHECKOUT_VALIDATIONS';
interface EstimateCartResponse {
    /** Cart with estimated values. */
    cart?: Cart;
    /** Estimation summary. */
    summary?: CartSummary;
}
interface CalculateCartRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Whether to retrieve the latest item prices, discounts, and inventory data from the catalogs before calculating the cart.
     *
     * Default: `false`.
     */
    refreshCart?: boolean;
}
interface CalculateCartResponse {
    /** Cart with calculated values. */
    cart?: Cart;
    /** Calculation summary. */
    summary?: CartSummary;
}
interface CalculateCartForV1Request {
    /**
     * ID of the Cart to calculate.
     * @format GUID
     */
    cartId?: string;
    /**
     * Specifies the level of **business validation** to perform during cart calculation,
     * by calling the [Validations service plugin](https://dev.wix.com/api/rest/wix-ecommerce/validations-integration-spi/introduction).
     */
    validationsConfig?: ValidationsConfigWithLiterals;
    /**
     * Optional custom configuration for the calculation.
     * If not provided, all price components are calculated by default.
     */
    customCalculationConfig?: CalculationConfig;
    /** Calculations overrides for backward compatibility with V1. */
    calculationOverrides?: CalculationOverrides;
    /** Cart revision that must still be current for the calculation to run. */
    expectedRevision?: string | null;
}
interface CalculationConfig {
    /**
     * Whether to calculate delivery costs.
     *
     * Default: `false`.
     */
    calculateDelivery?: boolean;
    /**
     * Whether to calculate additional fees.
     *
     * Default: `false`.
     */
    calculateAdditionalFees?: boolean;
    /**
     * Whether to calculate tax.
     *
     * Default: `false`.
     */
    calculateTax?: boolean;
    /**
     * Whether to include the effect of gift card balances on the cart total.
     * Gift cards aren't redeemed; balances are reflected for estimation only.
     *
     * Default: `false`.
     */
    calculateGiftCards?: boolean;
}
interface CalculationOverrides {
    /**
     * Delivery method.
     *
     * Pass this field only when the cart has no delivery method yet.
     */
    deliveryMethod?: DeliveryMethodInput;
    /**
     * Delivery address.
     * Used to calculate tax and shipping when applicable.
     */
    deliveryAddress?: Address;
    /**
     * Billing address.
     * Used to calculate tax when all items in the cart aren't shippable.
     */
    billingAddress?: Address;
    /** Selected membership payment options and the line items they apply to. */
    selectedMemberships?: SelectedMemberships;
}
interface SelectedMemberships {
    /**
     * Selected memberships.
     * @maxSize 300
     */
    memberships?: SelectedMembership[];
}
interface SelectedMembership {
    /**
     * Membership ID.
     * @minLength 1
     * @maxLength 200
     */
    _id?: string;
    /**
     * ID of the app providing this payment option.
     * @format GUID
     */
    appId?: string;
    /**
     * IDs of the line items this membership applies to.
     * @minSize 1
     * @maxSize 300
     * @minLength 1
     * @maxLength 100
     */
    lineItemIds?: string[];
}
interface CalculateCartForV1Response {
    /** The Cart that was calculated. */
    cart?: Cart;
    /** The calculation summary. */
    summary?: CartSummary;
    /**
     * Totals payload used for backward compatibility with legacy clients.
     * Used by the Cart and Checkout proxies.
     */
    totalsResponse?: CalculateTotalsResponse;
}
interface CalculateTotalsResponse {
    /** Calculate line items. */
    calculatedLineItems?: CalculatedLineItem[];
    /** Price summary. */
    priceSummary?: V1PriceSummary;
    /**
     * DEPRECATED: Use gift_cards instead.
     * @deprecated DEPRECATED: Use gift_cards instead.
     * @replacedBy gift_cards
     * @targetRemovalDate 2026-12-01
     */
    giftCard?: GiftCard;
    /** Tax summary. */
    taxSummary?: TaxSummary;
    /** Shipping information. */
    shippingInfo?: ShippingInformation;
    /**
     * Applied discounts.
     * @maxSize 320
     */
    appliedDiscounts?: AppliedDiscount[];
    /** Calculation errors. */
    calculationErrors?: CalculationErrors;
    /** Weight unit. */
    weightUnit?: WeightUnitWithLiterals;
    /**
     * The currency the price calculations were performed in.
     * This is the business’s default currency: the base currency configured for the site, which remains constant regardless of the [customer’s currency selection](https://support.wix.com/en/article/multicurrency-an-overview).
     * When `amountCurrency` is specified on the request, this returns that value instead.
     * @format CURRENCY
     */
    currency?: string;
    /**
     * The currency the converted price calculations are in.
     * This is the currency [selected by the customer](https://support.wix.com/en/article/multicurrency-an-overview), which may differ from `currency`.
     * When `amountCurrency` is specified on the request, nothing is converted and this returns that value.
     * @readonly
     * @format CURRENCY
     */
    conversionCurrency?: string;
    /** Whether tax is included in price. */
    taxIncludedInPrice?: boolean;
    /**
     * Minimal amount to pay in order to place the order.
     * @readonly
     */
    payNow?: V1PriceSummary;
    /**
     * Remaining amount for the order to be fully paid.
     * @readonly
     */
    payLater?: V1PriceSummary;
    /** Information about valid and invalid memberships, and which ones are selected for usage. */
    membershipOptions?: MembershipOptions;
    /**
     * Additional Fees
     * @maxSize 100
     */
    additionalFees?: AdditionalFee[];
    /**
     * The rate used when converting from the site currency to the checkout currency.
     * @readonly
     * @decimalValue options { gt:0, lte:1000000000000000 }
     */
    checkoutConversionRate?: string | null;
    /**
     * The pay now total amount after all gift cards are applied.
     * @readonly
     */
    payNowTotalAfterGiftCard?: MultiCurrencyPrice;
    /**
     * The total amount after all gift cards are applied.
     * @readonly
     */
    totalAfterGiftCard?: MultiCurrencyPrice;
    /**
     * Payment for subscriptions after free trial period.
     * @readonly
     */
    payAfterFreeTrial?: V1PriceSummary;
    /**
     * Platform fee summery.
     * @internal
     */
    platformFeeSummary?: PlatformFeeSummary;
    /**
     * Details of applied gift cards.
     * @internal
     */
    giftCards?: GiftCard[];
}
interface CalculatedLineItem {
    /** Line item ID. */
    lineItemId?: string;
    /** Price breakdown for this line item. */
    pricesBreakdown?: LineItemPricesData;
    /**
     * Type of selected payment option for current item. Supported values:
     * + `"FULL_PAYMENT_ONLINE"` - The entire payment for this item will happen as part of the checkout
     * + `"FULL_PAYMENT_OFFLINE"` - The entire payment for this item will happen after the checkout
     * + `"MEMBERSHIP"` - This item cannot be paid via monetary payment options, only via non monetary option such membership. When this option is used, price must be set to 0
     * + `"DEPOSIT_ONLINE"` -  Partial payment of the given item will happen as part of the checkout. Amount to be paid is defined by deposit_amount field.
     */
    paymentOption?: PaymentOptionTypeWithLiterals;
    /** tax calculation address. */
    taxableAddress?: TaxableAddress;
    /**
     * Calculated modifiers.
     * @internal
     * @maxSize 625
     */
    modifiers?: CalculatedItemModifier[];
}
interface LineItemPricesData {
    /** Total price after discounts, tax, and modifiers. */
    totalPriceAfterTax?: MultiCurrencyPrice;
    /** Total price after discounts and modifiers, and before tax. */
    totalPriceBeforeTax?: MultiCurrencyPrice;
    /** Tax details. */
    taxDetails?: ItemTaxFullDetails;
    /** Total discount applied for the line item. */
    totalDiscount?: MultiCurrencyPrice;
    /** Catalog price after catalog-defined discount, automatic discounts, and modifiers. */
    price?: MultiCurrencyPrice;
    /**
     * Item price before automatic discounts, coupons, and global discounts; after catalog-defined discounts and modifiers.
     * Defaults to `price` when not provided.
     */
    priceBeforeDiscounts?: MultiCurrencyPrice;
    /** Total line item price after catalog-defined discounts, automatic discounts, and modifiers. */
    lineItemPrice?: MultiCurrencyPrice;
    /** Catalog price before any discounts, with modifiers. */
    fullPrice?: MultiCurrencyPrice;
    /** Sum of all modifiers for a single unit of the item. */
    modifiers?: MultiCurrencyPrice;
    /**
     * Item price before tax, automatic discounts, coupons, and global discounts; after catalog-defined discounts and modifiers.
     * @internal
     */
    priceBeforeDiscountsAndTax?: MultiCurrencyPrice;
    /**
     * Partial payment out of `price` to be paid upfront during the checkout.
     * Applies to catalog items with `lineItem.paymentOption` type `DEPOSIT_ONLINE` only.
     */
    depositAmount?: MultiCurrencyPrice;
}
interface ItemTaxFullDetails {
    /** Amount for which tax is calculated. */
    taxableAmount?: MultiCurrencyPrice;
    /**
     * Tax group ID, if specified.
     * @internal
     * @format GUID
     */
    taxGroupId?: string | null;
    /**
     * Tax rate %, as a decimal point between 0 and 1.
     * @decimalValue options { gte:0, lte:1, maxScale:6 }
     */
    taxRate?: string;
    /** Calculated tax, based on `taxable_amount` and `tax_rate`. */
    totalTax?: MultiCurrencyPrice;
    /**
     * If breakdown exists, the sum of rates in the breakdown must equal `tax_rate`. Deprecated - use 'tax_breakdown' instead.
     * @readonly
     * @deprecated
     */
    rateBreakdown?: TaxRateBreakdown[];
    /**
     * The amount of this line item that was exempt.
     * @internal
     */
    exemptAmount?: MultiCurrencyPrice;
    /**
     * True for items we have a tax applied on them
     * @internal
     */
    isItemTaxable?: boolean | null;
    /**
     * True if the lineAmount include tax in it.
     * @internal
     */
    isTaxIncluded?: boolean | null;
    /**
     * The calculator that calculated this line tax
     * @internal
     * @maxLength 25
     */
    calculatorName?: string | null;
    /**
     * tax information for a line item.
     * @maxSize 1000
     * @readonly
     */
    taxBreakdown?: TaxBreakdown[];
}
interface TaxRateBreakdown {
    /**
     * Name of tax against which the calculation was performed.
     * @maxLength 100
     */
    name?: string;
    /**
     * Rate at which this tax detail was calculated.
     * @decimalValue options { gte:0, lte:1, maxScale:6 }
     */
    rate?: string;
    /** Amount of tax for this tax detail. */
    tax?: MultiCurrencyPrice;
    /**
     * The type of tax that was calculated
     * @internal
     */
    taxType?: string | null;
    /**
     * The name of the jurisdiction in which this tax detail applies
     * @internal
     */
    jurisdiction?: string | null;
    /**
     * The type of the jurisdiction in which this tax detail applies(Country,State,County,City,Special).
     * @internal
     */
    jurisdictionType?: string | null;
    /**
     * The amount of this line item that was exempt from this authority.
     * @internal
     */
    exemptAmount?: MultiCurrencyPrice;
    /**
     * ids of the used exemptions from the TaxEstimate exemptions array.
     * @internal
     * @maxSize 1000
     */
    exemptionIds?: number[];
    /**
     * The taxable amount of this tax detail
     * @internal
     */
    taxableAmount?: MultiCurrencyPrice;
    /**
     * The Taxes/Fee component. True if the fee is applied.
     * @internal
     */
    isFee?: boolean | null;
}
/**
 * TaxBreakdown represents tax information for a line item.
 * It holds the tax amount and the tax rate for each tax authority that apply on the line item.
 */
interface TaxBreakdown {
    /**
     * The name of the jurisdiction to which this tax detail applies. For example, "New York" or "Quebec".
     * @maxLength 200
     */
    jurisdiction?: string | null;
    /** The amount of this line item price that was considered nontaxable. (Decimal value) */
    nonTaxableAmount?: MultiCurrencyPrice;
    /**
     * The rate at which this tax detail was calculated, e.g 0.1000 signifies 10% tax and 2.0000 signifies 200% tax. (Decimal value)
     * @decimalValue options { gte:0, maxScale:6 }
     */
    rate?: string | null;
    /** The amount of tax estimated for this line item. (Decimal value) */
    taxAmount?: MultiCurrencyPrice;
    /** The taxable amount of this line item. */
    taxableAmount?: MultiCurrencyPrice;
    /**
     * The type of tax that was calculated. Depends on the jurisdiction's tax laws. For example, "Sales Tax", "Income Tax", "Value Added Tax", etc.
     * @maxLength 200
     */
    taxType?: string | null;
    /**
     * The name of the tax against which this tax amount was calculated. For example, "NY State Sales Tax", "Quebec GST", etc.
     * This name should be explicit enough to allow the merchant to understand what tax was calculated.
     * @maxLength 200
     */
    taxName?: string | null;
    /** The type of the jurisdiction in which this tax detail applies. */
    jurisdictionType?: JurisdictionTypeWithLiterals;
    /**
     * The amount of this line item that was exempt from this authority.
     * @internal
     */
    exemptAmount?: MultiCurrencyPrice;
}
/** JurisdictionType represents the type of the jurisdiction in which this tax detail applies (e.g. Country,State,County,City,Special). */
declare enum JurisdictionType {
    UNDEFINED = "UNDEFINED",
    COUNTRY = "COUNTRY",
    STATE = "STATE",
    COUNTY = "COUNTY",
    CITY = "CITY",
    SPECIAL = "SPECIAL"
}
/** @enumType */
type JurisdictionTypeWithLiterals = JurisdictionType | 'UNDEFINED' | 'COUNTRY' | 'STATE' | 'COUNTY' | 'CITY' | 'SPECIAL';
interface CalculatedItemModifier {
    /**
     * Modifier ID.
     * @minLength 1
     * @maxLength 36
     */
    _id?: string;
    /**
     * ID of the group this modifier belongs to.
     * @minLength 1
     * @maxLength 36
     */
    groupId?: string;
    /**
     * Modifier price.
     * @readonly
     */
    price?: MultiCurrencyPrice;
}
interface V1PriceSummary {
    /** Subtotal of all line items, before discounts and before tax. */
    subtotal?: MultiCurrencyPrice;
    /** Total shipping price, before discounts and before tax. */
    shipping?: MultiCurrencyPrice;
    /** Total tax. */
    tax?: MultiCurrencyPrice;
    /** Total calculated discount value. */
    discount?: MultiCurrencyPrice;
    /** Total price after discounts, gift cards, and tax. */
    total?: MultiCurrencyPrice;
    /** Total additional fees price before tax. */
    additionalFees?: MultiCurrencyPrice;
}
interface GiftCard {
    /**
     * Gift Card ID.
     * @deprecated
     */
    _id?: string;
    /** Gift card obfuscated code. */
    obfuscatedCode?: string;
    /** Actual amount to be redeemed from the gift card. */
    amount?: MultiCurrencyPrice;
    /**
     * App ID of the gift card provider.
     * @format GUID
     */
    appId?: string;
    /**
     * External ID in the gift card provider's system.
     * Used for integration and tracking across different platforms.
     * @minLength 1
     * @maxLength 50
     */
    externalId?: string | null;
    /** Requested amount to redeem from the gift card. */
    requestedAmount?: MultiCurrencyPrice;
    /**
     * Full gift card code. Echoed from the request input for correlation.
     * @internal
     * @minLength 8
     * @maxLength 20
     */
    code?: string;
}
interface TaxSummary {
    /**
     * Amount for which tax is calculated, added from line items.
     * @readonly
     */
    taxableAmount?: MultiCurrencyPrice;
    /**
     * Calculated tax, added from line items.
     * @readonly
     */
    totalTax?: MultiCurrencyPrice;
    /**
     * Manual tax rate
     * @internal
     * @decimalValue options { gte:0, lte:1, maxScale:6 }
     * @readonly
     * @deprecated
     */
    manualTaxRate?: string;
    /**
     * Tax calculator that was active when the order was created.
     * @deprecated
     */
    calculationDetails?: TaxCalculationDetails;
    /**
     * Tax estimation id in tax service
     * @internal
     * @readonly
     */
    taxEstimationId?: string | null;
    /**
     * Average Tax Rate
     * @internal
     * @decimalValue options { gte:0, lte:1, maxScale:6 }
     * @readonly
     */
    averageTaxRate?: string | null;
    /**
     * The amount of this estimate that was exempt (for all line items).
     * @internal
     * @readonly
     */
    totalExempt?: MultiCurrencyPrice;
    /**
     * The summary of the tax breakdown for all the line items. It will hold for each tax name, the aggregated tax amount paid for it and the tax rate.
     * @internal
     * @readonly
     * @maxSize 50
     */
    aggregatedTaxBreakdown?: AggregatedTaxBreakdown[];
    /**
     * Default name of the tax that was calculated.
     * @internal
     * @maxLength 200
     */
    taxName?: string | null;
}
interface TaxCalculationDetails extends TaxCalculationDetailsCalculationDetailsOneOf {
    /** Reason the manual calculation was used. */
    manualRateReason?: ManualCalculationReasonWithLiterals;
    /** Details of the fallback rate calculation. */
    autoTaxFallbackDetails?: AutoTaxFallbackCalculationDetails;
    /** Rate calculation type. */
    rateType?: RateTypeWithLiterals;
}
/** @oneof */
interface TaxCalculationDetailsCalculationDetailsOneOf {
    /** Reason the manual calculation was used. */
    manualRateReason?: ManualCalculationReasonWithLiterals;
    /** Details of the fallback rate calculation. */
    autoTaxFallbackDetails?: AutoTaxFallbackCalculationDetails;
}
declare enum RateType {
    /** no tax being collected for this request due to location of purchase */
    NO_TAX_COLLECTED = "NO_TAX_COLLECTED",
    /** manual rate used for calculation */
    MANUAL_RATE = "MANUAL_RATE",
    /** autotax rate used for calculation */
    AUTO_RATE = "AUTO_RATE",
    /** fallback rate used for calculation */
    FALLBACK_RATE = "FALLBACK_RATE"
}
/** @enumType */
type RateTypeWithLiterals = RateType | 'NO_TAX_COLLECTED' | 'MANUAL_RATE' | 'AUTO_RATE' | 'FALLBACK_RATE';
declare enum ManualCalculationReason {
    /** user set calculator in Business Manager to be Manual */
    GLOBAL_SETTING_TO_MANUAL = "GLOBAL_SETTING_TO_MANUAL",
    /** specific region is on manual even though Global setting is Auto-tax */
    REGION_SETTING_TO_MANUAL = "REGION_SETTING_TO_MANUAL"
}
/** @enumType */
type ManualCalculationReasonWithLiterals = ManualCalculationReason | 'GLOBAL_SETTING_TO_MANUAL' | 'REGION_SETTING_TO_MANUAL';
interface AutoTaxFallbackCalculationDetails {
    /** reason for fallback */
    fallbackReason?: FallbackReasonWithLiterals;
    /** invalid request (i.e. address), timeout, internal error, license error, and others will be encoded here */
    error?: ApplicationError;
}
declare enum FallbackReason {
    /** auto-tax failed to be calculated */
    AUTO_TAX_FAILED = "AUTO_TAX_FAILED",
    /** auto-tax was temporarily deactivated on a system-level */
    AUTO_TAX_DEACTIVATED = "AUTO_TAX_DEACTIVATED"
}
/** @enumType */
type FallbackReasonWithLiterals = FallbackReason | 'AUTO_TAX_FAILED' | 'AUTO_TAX_DEACTIVATED';
/**
 * The summary of the tax breakdown for all the line items. It will hold for each tax name, the aggregated tax amount paid for it and the tax rate.
 * Tax breakdown is the tax amount split to the tax authorities that applied on the line item.
 */
interface AggregatedTaxBreakdown {
    /**
     * The name of the tax against which this tax amount was calculated.
     * @maxLength 200
     */
    taxName?: string;
    /**
     * The type of tax that was calculated. Depends on the company's nexus settings as well as the jurisdiction's tax laws.
     * @maxLength 200
     */
    taxType?: string;
    /**
     * The name of the jurisdiction in which this tax detail applies.
     * @maxLength 200
     */
    jurisdiction?: string;
    /** The type of the jurisdiction in which this tax detail applies (e.g. Country,State,County,City,Special). */
    jurisdictionTypeEnum?: JurisdictionTypeWithLiterals;
    /**
     * The rate at which this tax detail was calculated, e.g 0.1000 signifies 10% tax and 2.000 signifies 200% tax. (Decimal value)
     * @decimalValue options { gte:0, maxScale:6 }
     */
    rate?: string;
    /** The sum of all the tax from line items that calculated by the tax identifiers. */
    aggregatedTaxAmount?: MultiCurrencyPrice;
    /** The sum of all the taxable amount from line items for tax identifiers. */
    aggregatedTaxableAmount?: MultiCurrencyPrice;
    /**
     * The aggregated exempt amount from all line items for tax identifiers.
     * @internal
     */
    aggregatedExemptAmount?: MultiCurrencyPrice;
}
interface ShippingInformation {
    /** Shipping region. */
    region?: ShippingRegion;
    /** Selected shipping option. */
    selectedCarrierServiceOption?: SelectedCarrierServiceOption;
    /** All shipping options. */
    carrierServiceOptions?: CarrierServiceOption[];
}
interface ShippingRegion {
    /**
     * Shipping region ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Shipping region name.
     * @maxLength 100
     */
    name?: string;
}
interface SelectedCarrierServiceOption {
    /**
     * Unique identifier of selected option. For example, "usps_std_overnight".
     * @maxLength 100
     */
    code?: string;
    /**
     * Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
     * For example, "Standard" or "First-Class Package International".
     * @maxLength 250
     * @readonly
     */
    title?: string;
    /**
     * Delivery logistics.
     * @readonly
     */
    logistics?: DeliveryLogistics;
    /**
     * Shipping costs.
     * @readonly
     */
    cost?: SelectedCarrierServiceOptionPrices;
    /**
     * Were we able to find the requested shipping option, or otherwise we fallback to the default one (the first)
     * @readonly
     */
    requestedShippingOption?: boolean;
    /**
     * Other charges
     * @deprecated Other charges
     * @replacedBy additional_fees
     * @targetRemovalDate 2025-10-01
     */
    otherCharges?: SelectedCarrierServiceOptionOtherCharge[];
    /**
     * This carrier's unique ID
     * @format GUID
     */
    carrierId?: string | null;
    /**
     * Delivery solution allocations to different delivery carriers and delivery regions
     * @maxSize 300
     */
    deliveryAllocations?: DeliveryAllocation[];
    /** If the delivery solution is a partial and doesn't apply to all items. */
    partial?: boolean | null;
}
interface DeliveryLogistics {
    /**
     * Expected delivery time, in free text. For example, "3-5 business days".
     * @maxLength 500
     */
    deliveryTime?: string | null;
    /**
     * Instructions for caller, e.g for pickup: "Please deliver during opening hours, and please don't park in disabled parking spot".
     * @maxLength 1000
     */
    instructions?: string | null;
    /** Pickup details. */
    pickupDetails?: PickupDetails;
    /**
     * Expected delivery time slot (from and to time stamps representation)
     * @internal
     */
    deliveryTimeSlot?: DeliveryTimeSlot;
}
interface PickupDetails {
    /** Pickup address. */
    address?: Address;
    /**
     * Whether the pickup address is that of a business - this may effect tax calculation.
     * @deprecated
     */
    businessLocation?: boolean;
    /** Pickup method */
    pickupMethod?: PickupMethodWithLiterals;
}
declare enum PickupMethod {
    STORE_PICKUP = "STORE_PICKUP",
    PICKUP_POINT = "PICKUP_POINT"
}
/** @enumType */
type PickupMethodWithLiterals = PickupMethod | 'STORE_PICKUP' | 'PICKUP_POINT';
interface DeliveryTimeSlot {
    /** starting time of the delivery time slot */
    from?: Date | null;
    /** ending time of the delivery time slot */
    to?: Date | null;
}
interface SelectedCarrierServiceOptionPrices {
    /** Total shipping price, after discount and after tax. */
    totalPriceAfterTax?: MultiCurrencyPrice;
    /** Total price of shipping after discounts (when relevant), and before tax. */
    totalPriceBeforeTax?: MultiCurrencyPrice;
    /** Tax details. */
    taxDetails?: ItemTaxFullDetails;
    /** Shipping discount before tax. */
    totalDiscount?: MultiCurrencyPrice;
    /** Shipping price before discount and before tax. */
    price?: MultiCurrencyPrice;
}
interface SelectedCarrierServiceOptionOtherCharge {
    /** Type of additional cost. */
    type?: ChargeTypeWithLiterals;
    /**
     * Details of the charge, such as 'Full Coverage Insurance of up to 80% of value of shipment'.
     * @maxLength 200
     */
    details?: string | null;
    /** Price of added charge. */
    cost?: SelectedCarrierServiceOptionPrices;
}
declare enum ChargeType {
    HANDLING_FEE = "HANDLING_FEE",
    INSURANCE = "INSURANCE"
}
/** @enumType */
type ChargeTypeWithLiterals = ChargeType | 'HANDLING_FEE' | 'INSURANCE';
interface DeliveryAllocation {
    /** The delivery option's carrier details, could be multiple if the delivery option is a combination of multiple carriers */
    deliveryCarrier?: Carrier;
    /** The delivery region that are relevant for this delivery solution. */
    deliveryRegion?: Region;
    /** Populated if the delivery solution is a partially supplied by this carrier. */
    applicableLineItems?: ApplicableLineItems;
}
interface Carrier {
    /**
     * The carrier app id
     * @format GUID
     */
    appId?: string | null;
    /**
     * Unique code that acts as an ID for a shipping rate. For example, `"usps_std_overnight"`.
     * @maxLength 250
     */
    code?: string;
}
interface Region {
    /**
     * The delivery region id.
     * @format GUID
     */
    _id?: string | null;
    /**
     * The delivery region name.
     * @maxLength 100
     */
    name?: string | null;
}
interface ApplicableLineItems {
    /**
     * Line items that the delivery solution is for.
     * @maxSize 300
     * @minLength 1
     * @maxLength 100
     */
    lineItemIds?: string[];
}
interface CarrierServiceOption {
    /**
     * Carrier ID.
     * @format GUID
     */
    carrierId?: string;
    /** Shipping options offered by this carrier for this request. */
    shippingOptions?: ShippingOption[];
}
interface ShippingOption {
    /**
     * Unique code of provided shipping option like "usps_std_overnight".
     * For legacy calculators this would be the UUID of the option.
     * @maxLength 100
     */
    code?: string;
    /**
     * Title of the option, such as USPS Standard Overnight Delivery (in the requested locale).
     * For example, "Standard" or "First-Class Package International".
     * @maxLength 250
     */
    title?: string;
    /** Delivery logistics. */
    logistics?: DeliveryLogistics;
    /** Sipping price information. */
    cost?: ShippingPrice;
    /**
     * Delivery solution allocations to different delivery carriers and delivery regions
     * @maxSize 300
     */
    deliveryAllocations?: DeliveryAllocation[];
    /** If the delivery solution is a partial and doesn't apply to all items. */
    partial?: boolean | null;
}
interface ShippingPrice {
    /** Shipping price. */
    price?: MultiCurrencyPrice;
    /** Other costs such as insurance, handling & packaging for fragile items, etc. */
    otherCharges?: OtherCharge[];
}
interface OtherCharge {
    /** Type of additional cost. */
    type?: ChargeTypeWithLiterals;
    /** Price of added cost. */
    price?: MultiCurrencyPrice;
    /**
     * Description of the additional charge. For example, `"Handling fee of $5 applied for gift wrapping"`.
     * @maxLength 250
     */
    details?: string | null;
}
interface CalculationErrors extends CalculationErrorsShippingCalculationErrorOneOf {
    /** General shipping calculation error. */
    generalShippingCalculationError?: Details;
    /** Errors related to the delivery carrier. */
    carrierErrors?: CarrierErrors;
    /** Error related to the selected shipping option. */
    selectedShippingOptionError?: Details;
    /** Tax calculation error. */
    taxCalculationError?: Details;
    /** Coupon calculation error. */
    couponCalculationError?: Details;
    /**
     * DEPRECATED: Use gift_card_calculation_errors instead.
     * @deprecated DEPRECATED: Use gift_card_calculation_errors instead.
     * @replacedBy gift_card_calculation_errors
     * @targetRemovalDate 2026-12-01
     */
    giftCardCalculationError?: Details;
    /** Order validation errors. */
    orderValidationErrors?: ApplicationError[];
    /**
     * Membership payment methods calculation errors
     * For example, will indicate that a line item that must be paid with membership payment doesn't have one or selected memberships are invalid
     */
    membershipError?: Details;
    /** Discount Rule calculation error. */
    discountsCalculationError?: Details;
    /**
     * Gift card calculation errors, one per failed card.
     * @internal
     */
    giftCardCalculationErrors?: GiftCardCalculationError[];
}
/** @oneof */
interface CalculationErrorsShippingCalculationErrorOneOf {
    /** General shipping calculation error. */
    generalShippingCalculationError?: Details;
    /** Errors related to the delivery carrier. */
    carrierErrors?: CarrierErrors;
    /** Error related to the selected shipping option. */
    selectedShippingOptionError?: Details;
}
interface CarrierErrors {
    /** Carrier errors. */
    errors?: CarrierError[];
}
interface CarrierError {
    /** Carrier ID. */
    carrierId?: string;
    /** Error details. */
    error?: Details;
}
interface GiftCardCalculationError {
    /** Gift card code with most characters masked for security. */
    obfuscatedCode?: string;
    /** Error details. */
    error?: Details;
    /**
     * Full gift card code. Echoed from the request input for correlation.
     * @internal
     * @minLength 8
     * @maxLength 20
     */
    code?: string;
}
interface MembershipOptions {
    /**
     * List of payment options that can be used.
     * Populated by the legacy ListEligibleMemberships flow.
     * @maxSize 300
     * @deprecated List of payment options that can be used.
     * Populated by the legacy ListEligibleMemberships flow.
     * @replacedBy validated_existing_memberships
     * @targetRemovalDate 2026-10-01
     */
    eligibleMemberships?: Membership[];
    /**
     * List of payment options that are owned by the member, but cannot be used due to reason provided.
     * Populated by the legacy ListEligibleMemberships flow.
     * @maxSize 300
     * @deprecated List of payment options that are owned by the member, but cannot be used due to reason provided.
     * Populated by the legacy ListEligibleMemberships flow.
     * @replacedBy validated_existing_memberships
     * @targetRemovalDate 2026-10-01
     */
    invalidMemberships?: InvalidMembership[];
    /**
     * The selected membership payment options and which line items they apply to.
     * Populated by the legacy ListEligibleMemberships flow.
     * @maxSize 300
     * @deprecated The selected membership payment options and which line items they apply to.
     * Populated by the legacy ListEligibleMemberships flow.
     * @replacedBy validated_existing_memberships
     * @targetRemovalDate 2026-10-01
     */
    selectedMemberships?: SelectedMembership[];
    /**
     * Validated existing memberships selected for redemption, each with the line items it can cover.
     * Populated by the EnhanceAndValidateSelection flow.
     * @internal
     */
    validatedExistingMemberships?: ValidatedExistingMemberships;
    /**
     * Validated future plans (purchasable during this checkout) and how their benefits cover the line items.
     * Populated by the EnhanceAndValidateSelection flow.
     * @internal
     */
    validatedPurchasablePlans?: ValidatedPurchasablePlans;
}
interface Membership {
    /**
     * Membership ID.
     * @minLength 1
     * @maxLength 200
     */
    _id?: string;
    /**
     * ID of the application providing this payment option.
     * @format GUID
     */
    appId?: string;
    /** The name of this membership. */
    name?: MembershipName;
    /**
     * Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.
     *
     * Only populated in the legacy `ListEligibleMemberships` flow, which still requires it. Empty on
     * `ListMembershipsByIds`, a by-id lookup that receives no line items to report against.
     * @maxSize 300
     * @minLength 1
     * @maxLength 100
     */
    lineItemIds?: string[];
    /** Membership's payment credits (balance information). */
    credits?: MembershipPaymentCredits;
    /** Membership expiration date. */
    expirationDate?: Date | null;
    /**
     * Deprecated. Use `offering_description`.
     * @deprecated
     * @replacedBy offering_description
     * @targetRemovalDate 2026-08-30
     */
    additionalData?: Record<string, any> | null;
    /**
     * Human-readable offering summary for display. Provider-supplied.
     * @internal
     * @maxLength 500
     */
    offeringDescription?: string | null;
}
interface MembershipName {
    /**
     * Membership name.
     * @maxLength 100
     */
    original?: string;
    /**
     * Translated membership name. Defaults to `original` when not provided.
     * @maxLength 100
     */
    translated?: string | null;
}
interface MembershipPaymentCredits {
    /**
     * Membership's initial value.
     * @min 1
     */
    total?: number;
    /** Membership's remaining value. */
    remaining?: number;
    /**
     * Deprecated. The new flow no longer exposes a per-item redemption cost on a membership's credits;
     * what a membership can pay for is conveyed by `ExistingMembership.covered_line_items` (per-item redemption cost).
     * @internal
     * @deprecated
     * @replacedBy ExistingMembership.covered_line_items.redemption_cost
     * @targetRemovalDate 2026-08-30
     */
    redemptionCost?: number;
    /**
     * Membership's balance type.
     * @internal
     */
    balanceType?: BalanceTypeWithLiterals;
}
declare enum BalanceType {
    /** Balance is measured in discrete uses (e.g., sessions, visits). */
    PUNCH_CARD = "PUNCH_CARD",
    /** Balance is measured in a currency-like value (e.g., points). */
    CREDIT = "CREDIT"
}
/** @enumType */
type BalanceTypeWithLiterals = BalanceType | 'PUNCH_CARD' | 'CREDIT';
interface InvalidMembership {
    /** Membership details. */
    membership?: Membership;
    /**
     * Reason why this membership is invalid and cannot be used.
     * @minLength 1
     * @maxLength 100
     */
    reason?: string;
}
/** Same shape as `ListExistingMembershipsResponse.memberships`. */
interface ValidatedExistingMemberships {
    /**
     * The selected memberships, each paired with the line items it can redeem and the cost to redeem each.
     * @maxSize 300
     */
    memberships?: ExistingMembership[];
}
/**
 * A membership the member already owns, together with the input line items it can pay for.
 *
 * A membership is a single benefit granted by a plan the member has already paid for, so coverage is
 * nested here per membership (no `benefit_id` linkage is needed, unlike a plan that grants many benefits).
 */
interface ExistingMembership {
    /** The membership the member owns. */
    membership?: V1Membership;
    /**
     * The input line items this membership can pay for, and the cost to redeem each.
     * @maxSize 300
     */
    coveredLineItems?: CoveredLineItem[];
}
interface V1Membership {
    /**
     * Membership ID.
     * @minLength 1
     * @maxLength 200
     */
    _id?: string;
    /** Membership name. */
    name?: MembershipName;
    /**
     * Deprecated. Use `ExistingMembership.covered_line_items` to see which line items this membership covers.
     *
     * Populate this only in the legacy `ListEligibleMemberships` flow, which still requires it and rejects a
     * membership that omits it. Leave it empty everywhere else - `ListMembershipsByIds` receives no line items
     * at all, so there is nothing to report there.
     * @maxSize 300
     * @minLength 1
     * @maxLength 100
     * @deprecated
     * @replacedBy ExistingMembership.covered_line_items.line_item_id
     * @targetRemovalDate 2026-08-30
     */
    lineItemIds?: string[];
    /** Membership's payment credits (balance information). */
    credits?: MembershipPaymentCredits;
    /** Membership expiration date. */
    expirationDate?: Date | null;
    /**
     * Deprecated. Use `offering_description` for human-readable provider data.
     * @deprecated
     * @replacedBy offering_description
     * @targetRemovalDate 2026-08-30
     */
    additionalData?: Record<string, any> | null;
    /**
     * Human-readable offering summary for display (for example price and validity text). Provider-supplied.
     * @internal
     * @maxLength 500
     */
    offeringDescription?: string | null;
}
/** A line item a membership can pay for, and the cost to redeem it. */
interface CoveredLineItem {
    /**
     * The input line item this membership can cover.
     * @minLength 1
     * @maxLength 100
     */
    lineItemId?: string;
    /**
     * The amount to redeem from the membership's benefit quota (`benefit.limited_quota.remaining`) for this item.
     * Always 0 when the benefit is unlimited (nothing is decremented).
     */
    redemptionCost?: number;
}
/** Same shape as `ListPurchasablePlansResponse`. */
interface ValidatedPurchasablePlans {
    /**
     * The selected plans.
     * @maxSize 300
     */
    purchasablePlans?: PurchasablePlan[];
    /**
     * How the plans' benefits cover the line items, linked to a benefit by `benefit_id`.
     * @maxSize 300
     */
    coverages?: LineItemCoverage[];
}
interface PurchasablePlan {
    /** Catalog and item reference for the plan that can be purchased. */
    catalogReference?: CatalogReference;
    /** Plan name. */
    name?: MembershipName;
    /**
     * Benefits the plan would grant once purchased. Multiple entries support multi-benefit plans (e.g., a bundle granting separate yoga and pilates credits).
     * @minSize 1
     * @maxSize 50
     */
    benefits?: Benefit[];
    /**
     * Human-readable offering summary for display (for example price and validity text). Provider-supplied.
     * @maxLength 500
     */
    offeringDescription?: string | null;
    /**
     * URL the buyer is redirected to in order to collect additional info required to purchase this plan.
     * @format WEB_URL
     */
    infoCollectionUrl?: string | null;
    /**
     * Plan price.
     * @decimalValue options { gte:0, maxScale:3 }
     */
    price?: string;
    /**
     * Human-readable description of the plan price (for example "First month free, then $20/month").
     *
     * `price` is simply the price (useful for sorting or comparison, for example), while this is a
     * descriptive, display-oriented summary of it.
     * @maxLength 500
     */
    priceDescription?: string | null;
}
/**
 * A single redeemable benefit. A purchasable plan grants one or more benefits (`PurchasablePlan.benefits`);
 * once the plan is paid, each benefit becomes its own membership (charged via `membership_id`).
 */
interface Benefit extends BenefitQuotaOneOf {
    limitedQuota?: LimitedQuota;
    /**
     * Identifier for this benefit. On a `PurchasablePlan` it must be unique within the response so
     * `coverages` (`LineItemCoverage.benefit_id`) can reference it unambiguously across the plan's benefits.
     * @minLength 1
     * @maxLength 200
     */
    benefitId?: string;
    /**
     * Human-readable label for this benefit (for example, "Manicure" or "Pedicure" on multi-set punchcards).
     * @maxLength 200
     */
    label?: string | null;
    /**
     * The kind of quota this benefit grants: a countable amount drawn down on redemption (`LIMITED`),
     * or an unbounded amount that is never drawn down (`UNLIMITED`).
     */
    quotaType?: QuotaTypeWithLiterals;
}
/** @oneof */
interface BenefitQuotaOneOf {
    limitedQuota?: LimitedQuota;
}
declare enum QuotaType {
    /** The benefit grants a countable quota that is drawn down on redemption. */
    LIMITED = "LIMITED",
    /** The benefit is unlimited (unbounded) and is never drawn down. */
    UNLIMITED = "UNLIMITED"
}
/** @enumType */
type QuotaTypeWithLiterals = QuotaType | 'LIMITED' | 'UNLIMITED';
/** A limited, countable redeemable amount. */
interface LimitedQuota {
    /**
     * The quota's initial value.
     * @min 1
     */
    total?: number;
    /** The quota's remaining value. */
    remaining?: number;
    /** How the quota is measured. */
    limitedQuotaType?: LimitedQuotaTypeWithLiterals;
}
declare enum LimitedQuotaType {
    /** Quota is measured in discrete uses (e.g., sessions, visits). */
    PUNCH_CARD = "PUNCH_CARD",
    /** Quota is measured in a currency-like value (e.g., points). */
    CREDIT = "CREDIT"
}
/** @enumType */
type LimitedQuotaTypeWithLiterals = LimitedQuotaType | 'PUNCH_CARD' | 'CREDIT';
/**
 * Maps a benefit to a line item it can cover, and at what cost, linked to a benefit by `benefit_id`.
 * One entry per (benefit, line item). Returned by `ListPurchasablePlans` (alongside `purchasable_plans`).
 * For the existing-memberships flow the equivalent mapping is `ExistingMembership.covered_line_items`,
 * which needs no `benefit_id` because a membership is a single benefit.
 */
interface LineItemCoverage {
    /**
     * The benefit that covers the line item, as returned in `Benefit.benefit_id`.
     * @minLength 1
     * @maxLength 200
     */
    benefitId?: string;
    /**
     * The line item this benefit covers.
     * @minLength 1
     * @maxLength 100
     */
    lineItemId?: string;
    /**
     * The amount to redeem from the benefit's `limited_quota.remaining` value for this item.
     * Always 0 for an unlimited benefit (nothing is decremented).
     */
    redemptionCost?: number;
}
interface AdditionalFee {
    /**
     * Additional fee's unique code (or ID) for future processing.
     * @minLength 1
     * @maxLength 100
     */
    code?: string | null;
    /**
     * Additional fee's name.
     * @minLength 1
     * @maxLength 50
     */
    name?: string;
    /** Additional fee's price. */
    price?: MultiCurrencyPrice;
    /** Tax details. */
    taxDetails?: ItemTaxFullDetails;
    /**
     * Provider's app id.
     * @minLength 1
     * @maxLength 100
     */
    providerAppId?: string | null;
    /** Additional fee's price before tax. */
    priceBeforeTax?: MultiCurrencyPrice;
    /** Additional fee's price after tax. */
    priceAfterTax?: MultiCurrencyPrice;
    /**
     * Optional - Line items associated with this additional fee.
     * If no `lineItemIds` are provided, the fee will be associated with the whole cart/checkout/order.
     * @format GUID
     */
    lineItemIds?: string[];
    /**
     * Number of subscription cycle this fee applies to
     * default None - all billing cycle
     * @internal
     * @min 1
     * @max 999
     */
    subscriptionCycles?: number | null;
    /** the source the additional fee was added from */
    source?: AdditionalFeeSourceWithLiterals;
    /**
     * The translated name of the additional fee. The translation language is determined by the `languages` field in the [request envelope](https://dev.wix.com/docs/build-apps/develop-your-app/frameworks/self-hosting/supported-extensions/backend-extensions/add-self-hosted-service-plugin-extensions#request-envelope).
     * @minLength 1
     * @maxLength 50
     */
    translatedName?: string | null;
    /** Address type tax was calculated against for this fee. */
    taxableAddress?: TaxableAddress;
}
declare enum AdditionalFeeSource {
    UNKNOWN_ADDITIONAL_FEE_SOURCE = "UNKNOWN_ADDITIONAL_FEE_SOURCE",
    /** The additional fee was added by an additional fee service plugin */
    SERVICE_PLUGIN = "SERVICE_PLUGIN",
    /** The additional fee was added on the item either via the catalog or on custom line item */
    ITEM = "ITEM",
    /** The additional fee was added manually on the request */
    MANUAL = "MANUAL",
    /** The additional fee was added by the shipping provider */
    SHIPPING = "SHIPPING",
    /** The additional fee was added by a Wix vertical and represents a Wix platform fee */
    PLATFORM = "PLATFORM"
}
/** @enumType */
type AdditionalFeeSourceWithLiterals = AdditionalFeeSource | 'UNKNOWN_ADDITIONAL_FEE_SOURCE' | 'SERVICE_PLUGIN' | 'ITEM' | 'MANUAL' | 'SHIPPING' | 'PLATFORM';
interface PlatformFeeSummary {
    /** Total platform fee amount. */
    total?: MultiCurrencyPrice;
    /** Total platform fee passed on to the customer. */
    totalPassOn?: MultiCurrencyPrice;
    /** Total platform fee absorbed by the merchant. */
    totalAbsorbed?: MultiCurrencyPrice;
    /**
     * Calculated platform fees.
     * @maxSize 100
     */
    calculatedPlatformFees?: CalculatedPlatformFee[];
}
interface CalculatedPlatformFee {
    /** Platform fee name. */
    name?: TranslatableString;
    /** Platform fee amount. */
    amount?: MultiCurrencyPrice;
    /**
     * ID of the line item the platform fee applies to.
     * @format GUID
     */
    lineItemId?: string;
    /** Platform fee charge type. */
    chargeType?: PlatformFeeChargeTypeWithLiterals;
    /**
     * Percentage rate charged as platform fee.
     * The fee rate percentage expressed as a decimal fraction between 0 and 1. For example, `0.05` for 5%.
     * @format DECIMAL_VALUE
     * @decimalValue options { gt:0, lte:1, maxScale:4 }
     */
    percentageRate?: string;
}
declare enum PlatformFeeChargeType {
    /** The platform fee is passed on to the customer. */
    PASS_ON = "PASS_ON",
    /** The platform fee is absorbed by the merchant. */
    ABSORBED = "ABSORBED"
}
/** @enumType */
type PlatformFeeChargeTypeWithLiterals = PlatformFeeChargeType | 'PASS_ON' | 'ABSORBED';
interface CartRevisionMismatchErrorData {
    /** Cart revision requested by the caller. */
    expectedRevision?: string;
    /** Current Cart revision. */
    currentRevision?: string;
}
interface GetCheckoutURLRequest {
    /**
     * ID of the Cart to calculate.
     * @format GUID
     */
    cartId: string;
    /**
     * Checkout currency parameter to append as a query param
     * @format CURRENCY
     */
    currencyCode?: string | null;
}
interface GetCheckoutURLResponse {
    /**
     * The checkout URL.
     * @format WEB_URL
     */
    checkoutUrl?: string;
}
interface PlaceOrderRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Price verification token from `CalculateCart` response.
     * Used to verify that prices shown to the customer haven't changed before checkout completes.
     * @maxLength 3000
     */
    priceVerificationToken?: string | null;
    /**
     * Payment token.
     * @internal
     * @maxLength 100
     */
    paymentToken?: string | null;
    /**
     * URLs to use after payment completes.
     * @internal
     */
    redirectUrls?: RedirectUrls;
    /** Whether to save the payment method on the order. */
    savePaymentMethod?: boolean;
    /** Whether to authorize the payment and delay capture. */
    delayCapture?: boolean;
    /**
     * Whether the order is being placed from within a native mobile app
     * (iOS or Android) where the buyer paid via the platform's in-app
     * purchase system.
     * When true and the order contains subscription items,
     * Wix does not collect recurring renewal charges — the mobile platform's
     * in-app purchase system handles all subsequent renewal billing.
     *
     * Default: `false`
     * @internal
     */
    inAppPurchase?: boolean;
}
interface RedirectUrls {
    /**
     * URL to redirect to if payment is canceled (usually the checkout URL). Optional.
     * @maxLength 2000
     */
    cancelUrl?: string | null;
    /**
     * URL to redirect to if a payment error occurs (usually the checkout URL). Optional.
     * @maxLength 2000
     */
    errorUrl?: string | null;
    /**
     * URL to redirect to after successful payment. Optional. May include the `{orderId}` placeholder.
     * @maxLength 2000
     */
    successUrl?: string | null;
    /**
     * URL to redirect to if payment is pending. Optional. May include the `{orderId}` placeholder.
     * @maxLength 2000
     */
    pendingUrl?: string | null;
}
interface PlaceOrderResponse {
    /**
     * Order ID.
     * @format GUID
     */
    orderId?: string;
    /**
     * Payment response token.
     * @internal
     * @maxLength 1000
     */
    paymentResponseToken?: string | null;
    /**
     * Cashier payment gateway order ID when money was charged.
     * Omitted when no charge occurs (for example, a zero-total cart, a fully discounted order, or when another payment method such as a gift card covers the total).
     * @maxLength 50
     */
    paymentGatewayOrderId?: string | null;
    /** Whether an order was created from this cart and the payment completed successfully. */
    completed?: boolean;
}
interface InvalidThirdPartyCheckoutTokenErrorData {
    /**
     * Invalid third-party checkout token.
     * @maxLength 3000
     */
    token?: string;
}
interface InvalidPriceVerificationTokenErrorData {
    /**
     * Invalid price verification token.
     * @maxLength 3000
     */
    token?: string;
}
interface ViolationWithErrorSeverityErrorData {
    /** Violations payload. */
    violationsList?: ViolationsList;
}
interface ViolationsList {
    /**
     * Violations raised by Validations SPI implementers.
     * @maxSize 100
     */
    violations?: Violation[];
}
interface InvalidMembershipErrorData {
    /**
     * Message explaining the membership issue.
     * @maxLength 100
     */
    message?: string;
}
interface InvalidPaymentStatusErrorData {
    /**
     * Payment token when present.
     * @maxLength 100
     */
    paymentToken?: string | null;
    /**
     * Transaction status.
     * @maxLength 100
     */
    transactionStatus?: string;
    /**
     * Failure details when available.
     * @maxLength 100
     */
    failureDetails?: string | null;
}
interface GiftCardRedeemErrorData {
    /**
     * Message for the gift card redemption failure.
     * @maxLength 100
     */
    error?: string;
}
interface EmptyPaymentTokenErrorData {
    /**
     * Order total when the payment token was empty.
     * @maxLength 100
     */
    total?: string;
}
interface InvalidCartErrorData {
    /**
     * Related violations.
     * @maxSize 100
     */
    violations?: V2Violation[];
}
interface AddLineItemsRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Catalog items to add to the cart.
     * @maxSize 300
     */
    catalogItems?: CatalogItemInput[];
    /**
     * Custom items to add to the cart.
     * @maxSize 300
     */
    customItems?: CustomItemInput[];
}
interface AddLineItemsResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface RemoveLineItemsRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Line item IDs to remove from the cart.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    lineItemIds: string[];
}
interface RemoveLineItemsResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface UpdateLineItemsRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Line item updates to apply to the cart.
     * @minSize 1
     * @maxSize 300
     */
    lineItems?: LineItemUpdate[];
}
interface LineItemUpdate {
    /**
     * Line item ID.
     * @format GUID
     */
    lineItemId?: string;
    /** Quantity update. If omitted, quantity isn't updated. */
    quantity?: QuantityUpdate;
    /**
     * Selected membership update. If omitted, membership isn't updated.
     *
     * DEPRECATED — migrate to `membership_payment`.
     * @deprecated
     * @replacedBy membership_payment
     * @targetRemovalDate 2026-06-22
     */
    selectedMembership?: SelectedMembershipUpdate;
    /**
     * New catalog reference to swap the line item to, keeping the same `lineItemId` and quantity.
     * For example, accepting an upsell. If omitted, the catalog reference isn't changed.
     * @internal
     */
    catalogReference?: CatalogReference;
    /**
     * Membership payment update. If omitted, the membership payment isn't updated.
     *
     * Specify an empty value to remove the current membership without selecting a new one.
     */
    membershipPayment?: MembershipInput;
}
interface QuantityUpdate {
    /**
     * New quantity for the line item.
     * @min 1
     * @max 100000
     */
    newQuantity?: number;
}
interface SelectedMembershipUpdate {
    /**
     * New `selectedMembership` for the line item.
     *
     * Pass an empty value to remove the current membership without selecting a new one.
     */
    newMembership?: V2SelectedMembership;
}
interface UpdateLineItemsResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface EmptyLineItemUpdatesErrorData {
    /**
     * Line item IDs with no update fields set.
     * @format GUID
     * @maxSize 300
     */
    lineItemIds?: string[];
}
interface DuplicateLineItemUpdatesErrorData {
    /**
     * Line item IDs repeated in the update request.
     * @format GUID
     * @maxSize 300
     */
    duplicateLineItemIds?: string[];
}
interface AddCouponRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Coupon to apply.
     *
     * Currently only one is supported.
     */
    coupon: CouponInput;
}
interface AddCouponResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface CouponAlreadyExistsErrorData {
    /**
     * Coupon code already on the cart.
     * @maxLength 100
     */
    couponCode?: string;
}
interface RemoveCouponRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Coupon ID.
     * @format GUID
     */
    couponId: string;
}
interface RemoveCouponResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface CouponNotFoundInCartErrorData {
    /**
     * Coupon ID that isn't in the cart.
     * @format GUID
     */
    couponId?: string;
}
interface SetDeliveryMethodRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /** Delivery method. */
    deliveryMethod: DeliveryMethodInput;
}
interface SetDeliveryMethodResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface RemoveDeliveryMethodRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
}
interface RemoveDeliveryMethodResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface AddGiftCardRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Gift card to add.
     *
     * Currently only one is supported.
     */
    giftCard: GiftCardInput;
}
interface AddGiftCardResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface GiftCardAlreadyExistsErrorData {
    /**
     * Gift card code already on the cart.
     * @maxLength 100
     */
    giftCardCode?: string;
}
interface RemoveGiftCardRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Gift card ID.
     * @format GUID
     */
    giftCardId: string;
}
interface RemoveGiftCardResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface GiftCardNotFoundInCartErrorData {
    /**
     * Gift card ID that isn't in the cart.
     * @format GUID
     */
    giftCardId?: string;
}
interface MarkCartAsCompletedRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Order ID created from the cart.
     * @format GUID
     */
    orderId?: string | null;
}
interface MarkCartAsCompletedResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface HandleAsyncCheckoutCompletionRequest {
    /**
     * Server-signed JWT with data required to complete checkout.
     * @maxLength 4096
     */
    token: string;
}
interface RawHttpResponse {
    body?: Uint8Array;
    statusCode?: number | null;
    headers?: HeadersEntry[];
}
interface HeadersEntry {
    key?: string;
    value?: string;
}
interface UpdateFormSubmissionsRequest {
    /**
     * Cart ID.
     * @format GUID
     */
    cartId: string;
    /**
     * Form submission data to set on the cart.
     * @minSize 1
     * @maxSize 1000
     */
    formSubmissions?: FormSubmission[];
}
interface FormSubmission {
    /**
     * Form instance ID.
     * @format GUID
     */
    _id?: string;
    /**
     * Form submission ID.
     * @format GUID
     */
    formSubmissionId?: string;
}
interface UpdateFormSubmissionsResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
interface CreateCurrentCartRequest {
    /** Cart to create. */
    cart?: Cart;
    /**
     * A list of catalog items to add to the current cart.
     * @maxSize 300
     */
    catalogItems?: CatalogItemInput[];
    /**
     * A list of custom items to add to the current cart.
     * Custom line items don't trigger the Catalog service plugin.
     * @maxSize 300
     */
    customItems?: CustomItemInput[];
    /**
     * Coupons to apply.
     * Currently only one is supported.
     * @maxSize 1
     */
    coupons?: CouponInput[];
    /** Delivery method to set. */
    deliveryMethod?: DeliveryMethodInput;
    /**
     * Gift cards to add.
     * @maxSize 5
     */
    giftCards?: GiftCardInput[];
}
interface CreateCurrentCartResponse {
    /** The customer's current cart. */
    cart?: Cart;
}
interface GetCurrentCartRequest {
}
interface GetCurrentCartResponse {
    /** The customer's current cart. */
    cart?: Cart;
}
interface UpdateCurrentCartRequest {
    /** Cart to update; may be partial. */
    cart?: Cart;
    /**
     * Set of fields to update.
     *
     * Fields that aren't included in `fieldMask.paths` are ignored.
     * @internal
     */
    fieldMask?: string[];
}
interface UpdateCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface DeleteCurrentCartRequest {
}
interface DeleteCurrentCartResponse {
}
interface RefreshCurrentCartRequest {
}
interface RefreshCurrentCartResponse {
    /** Synchronized cart. */
    cart?: Cart;
}
interface CalculateCurrentCartRequest {
    /**
     * Whether to retrieve the latest item prices, discounts, and inventory data from the catalogs before calculating the cart.
     *
     * Default: `false`.
     */
    refreshCart?: boolean;
}
interface CalculateCurrentCartResponse {
    /** The cart that was calculated. */
    cart?: Cart;
    /** The calculation summary. */
    summary?: CartSummary;
}
interface EstimateCurrentCartRequest {
    /**
     * Whether to retrieve the latest item prices, discounts, and inventory data from the catalogs before estimating the cart.
     *
     * Default: `false`.
     */
    refreshCart?: boolean;
    /**
     * Specifies the level of **business validation** to perform during cart calculation,
     * by calling the [Validations service plugin](https://dev.wix.com/api/rest/wix-ecommerce/validations-integration-spi/introduction).
     */
    validationConfig?: ValidationsConfigWithLiterals;
    /**
     * Whether to calculate delivery costs.
     * Default: `false`.
     */
    calculateDelivery?: boolean;
    /**
     * Whether to calculate any additional fees.
     * Default: `false`.
     */
    calculateAdditionalFees?: boolean;
    /**
     * Whether to include tax calculation.
     * Default: `false`.
     */
    calculateTax?: boolean;
    /**
     * Whether to calculate the effect of gift card balances on the cart total.
     * Gift cards aren't redeemed - only reflected for estimation purposes.
     * Default: `false`.
     */
    calculateGiftCards?: boolean;
}
interface EstimateCurrentCartResponse {
    /** The cart that was calculated. */
    cart?: Cart;
    /** The calculation summary. */
    summary?: CartSummary;
}
interface AddLineItemsToCurrentCartRequest {
    /**
     * A list of catalog items to add to the current cart.
     * @maxSize 300
     */
    catalogItems?: CatalogItemInput[];
    /**
     * A list of custom items to add to the current cart.
     * @maxSize 300
     */
    customItems?: CustomItemInput[];
}
interface AddLineItemsToCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface RemoveLineItemsFromCurrentCartRequest {
    /**
     * Line item IDs to remove from the current cart.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    lineItemIds?: string[];
}
interface RemoveLineItemsFromCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface UpdateLineItemsInCurrentCartRequest {
    /**
     * Line item updates to apply.
     * @minSize 1
     * @maxSize 100
     */
    lineItems?: LineItemUpdate[];
}
interface UpdateLineItemsInCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface AddCouponToCurrentCartRequest {
    /**
     * Coupon to apply.
     *
     * Currently only one is supported.
     */
    coupon?: CouponInput;
}
interface AddCouponToCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface RemoveCouponFromCurrentCartRequest {
    /**
     * Coupon ID.
     * @format GUID
     */
    couponId?: string;
}
interface RemoveCouponFromCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface SetDeliveryMethodForCurrentCartRequest {
    /** Delivery method. */
    deliveryMethod?: DeliveryMethodInput;
}
interface SetDeliveryMethodForCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface RemoveDeliveryMethodFromCurrentCartRequest {
}
interface RemoveDeliveryMethodFromCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface AddGiftCardToCurrentCartRequest {
    /**
     * Gift card to add.
     *
     * Currently only one is supported.
     */
    giftCard?: GiftCardInput;
}
interface AddGiftCardToCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface RemoveGiftCardFromCurrentCartRequest {
    /**
     * Gift card ID.
     * @format GUID
     */
    giftCardId?: string;
}
interface RemoveGiftCardFromCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface UnmarkAsCurrentCartRequest {
}
interface UnmarkAsCurrentCartResponse {
    /** The cart that was unmarked as the current cart, if one existed. */
    cart?: Cart;
}
interface UpdateFormSubmissionsInCurrentCartRequest {
    /**
     * Form submission data to set on the current cart.
     * @minSize 1
     * @maxSize 1000
     */
    formSubmissions?: FormSubmission[];
}
interface UpdateFormSubmissionsInCurrentCartResponse {
    /** Updated cart. */
    cart?: Cart;
}
interface GetUpsellOptionsForCurrentCartRequest {
}
interface GetUpsellOptionsForCurrentCartResponse {
    /** Upsell options available for the cart's line items. */
    itemsUpsells?: ItemUpsells[];
}
/** @docsIgnore */
type CreateCartApplicationErrors = {
    code?: 'ITEM_NOT_FOUND_IN_CATALOG';
    description?: string;
    data?: ItemNotFoundInCatalogErrorData;
} | {
    code?: 'INSUFFICIENT_INVENTORY';
    description?: string;
    data?: InsufficientInventoryErrorData;
} | {
    code?: 'MAX_LINE_ITEMS_EXCEEDED';
    description?: string;
    data?: MaxLineItemsExceededErrorData;
} | {
    code?: 'MAX_ITEM_MODIFIERS_EXCEEDED';
    description?: string;
    data?: MaxItemModifiersExceededErrorData;
} | {
    code?: 'DUPLICATE_MODIFIER_GROUP_IDS';
    description?: string;
    data?: DuplicateModifierGroupIdsErrorData;
} | {
    code?: 'DUPLICATE_ITEM_MODIFIER_IDS';
    description?: string;
    data?: DuplicateItemModifierIdsErrorData;
} | {
    code?: 'LINE_ITEM_PRICE_CONFLICT';
    description?: string;
    data?: LineItemPriceConflictErrorData;
} | {
    code?: 'FIXED_QUANTITY_ITEM';
    description?: string;
    data?: FixedQuantityItemErrorData;
} | {
    code?: 'INVALID_MEMBERSHIP_PAYMENT_OPTION';
    description?: string;
    data?: InvalidMembershipPaymentOptionErrorData;
} | {
    code?: 'PLAN_LINE_ITEM_NOT_FOUND';
    description?: string;
    data?: LineItemNotFoundInCartErrorData;
} | {
    code?: 'INVALID_COUPON_CODE';
    description?: string;
    data?: InvalidCouponCodeErrorData;
} | {
    code?: 'INVALID_COUPON_STATUS';
    description?: string;
    data?: InvalidCouponStatusErrorData;
} | {
    code?: 'INVALID_GIFT_CARD_CODE';
    description?: string;
    data?: InvalidGiftCardCodeErrorData;
} | {
    code?: 'INVALID_GIFT_CARD_STATUS';
    description?: string;
    data?: InvalidGiftCardStatusErrorData;
} | {
    code?: 'DELIVERY_METHOD_NOT_FOUND';
    description?: string;
    data?: DeliveryMethodNotFoundErrorData;
} | {
    code?: 'DELIVERY_METHOD_MISSING_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DELIVERY_METHOD_MISSING_LINE_ITEMS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DEPOSIT_AMOUNT_GREATER_THAN_UNIT_PRICE';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type UpdateCartApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'BUSINESS_LOCATION_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_ADDRESS_FORMAT';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DATA_EXTENSION_UPDATE_FAILED';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type RefreshCartApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
};
/** @docsIgnore */
type EstimateCartApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
};
/** @docsIgnore */
type CalculateCartApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
};
/** @docsIgnore */
type GetCheckoutUrlApplicationErrors = {
    code?: 'CHECKOUT_PAGE_NOT_FOUND';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SITE_NOT_PUBLISHED';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type PlaceOrderApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'SITE_NOT_ACCEPTING_PAYMENTS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'EMPTY_CART';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'LINE_ITEMS_OUT_OF_STOCK';
    description?: string;
    data?: LineItemsOutOfStockErrorData;
} | {
    code?: 'CART_PRICES_OUT_OF_SYNC';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_THIRD_PARTY_CHECKOUT_TOKEN';
    description?: string;
    data?: InvalidThirdPartyCheckoutTokenErrorData;
} | {
    code?: 'INVALID_PRICE_VERIFICATION_TOKEN';
    description?: string;
    data?: InvalidPriceVerificationTokenErrorData;
} | {
    code?: 'VIOLATIONS_WITH_ERROR_SEVERITY';
    description?: string;
    data?: ViolationWithErrorSeverityErrorData;
} | {
    code?: 'CALCULATION_ERROR';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_PAY_FOR_GIFT_CARD_WITH_GIFT_CARD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_REDEEM_COUPON_WHEN_PURCHASING_GIFT_CARD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CANNOT_PAY_FOR_SUBSCRIPTION_WITH_GIFT_CARD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SUBSCRIPTION_WITH_INVALID_FREQUENCY';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SHIPPABLE_LINE_ITEM_CONTAINS_FREE_TRIAL_SUBSCRIPTION';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNCONFIRMED_SAVE_PAYMENT_METHOD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DELIVERY_METHOD_NO_LONGER_AVAILABLE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DELIVERY_METHOD_NOT_APPLIED_TO_ALL_LINE_ITEMS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MISSING_DELIVERY_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'MISSING_DELIVERY_METHOD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_MEMBERSHIP';
    description?: string;
    data?: InvalidMembershipErrorData;
} | {
    code?: 'INVALID_PAYMENT_STATUS';
    description?: string;
    data?: InvalidPaymentStatusErrorData;
} | {
    code?: 'GIFT_CARD_REDEEM_ERROR';
    description?: string;
    data?: GiftCardRedeemErrorData;
} | {
    code?: 'PAYMENT_ALREADY_IN_PROGRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'EMPTY_PAYMENT_TOKEN';
    description?: string;
    data?: EmptyPaymentTokenErrorData;
} | {
    code?: 'MEMBERS_ONLY_ITEM';
    description?: string;
    data?: MembersOnlyItemErrorData;
} | {
    code?: 'PLACE_ORDER_NOT_ALLOWED_FOR_DEMO_CART';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DEMO_ORDER_NOT_ALLOWED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'PAYMENT_ATTEMPT_EXPIRED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'UNAVAILABLE_PAYMENT_METHOD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_CART';
    description?: string;
    data?: InvalidCartErrorData;
};
/** @docsIgnore */
type AddLineItemsApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'NO_LINE_ITEMS_TO_ADD';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'ITEM_NOT_FOUND_IN_CATALOG';
    description?: string;
    data?: ItemNotFoundInCatalogErrorData;
} | {
    code?: 'INSUFFICIENT_INVENTORY';
    description?: string;
    data?: InsufficientInventoryErrorData;
} | {
    code?: 'MAX_LINE_ITEMS_EXCEEDED';
    description?: string;
    data?: MaxLineItemsExceededErrorData;
} | {
    code?: 'MAX_ITEM_MODIFIERS_EXCEEDED';
    description?: string;
    data?: MaxItemModifiersExceededErrorData;
} | {
    code?: 'DUPLICATE_MODIFIER_GROUP_IDS';
    description?: string;
    data?: DuplicateModifierGroupIdsErrorData;
} | {
    code?: 'DUPLICATE_ITEM_MODIFIER_IDS';
    description?: string;
    data?: DuplicateItemModifierIdsErrorData;
} | {
    code?: 'LINE_ITEM_PRICE_CONFLICT';
    description?: string;
    data?: LineItemPriceConflictErrorData;
} | {
    code?: 'FIXED_QUANTITY_ITEM';
    description?: string;
    data?: FixedQuantityItemErrorData;
} | {
    code?: 'INVALID_MEMBERSHIP_PAYMENT_OPTION';
    description?: string;
    data?: InvalidMembershipPaymentOptionErrorData;
} | {
    code?: 'PLAN_LINE_ITEM_NOT_FOUND';
    description?: string;
    data?: LineItemNotFoundInCartErrorData;
} | {
    code?: 'DEPOSIT_AMOUNT_GREATER_THAN_UNIT_PRICE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SUBSCRIPTION_WITH_ANCHOR_DAY_AND_FREE_TRIAL';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'SUBSCRIPTION_WITH_ANCHOR_DAY_AND_NON_MONTHLY_FREQUENCY';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type RemoveLineItemsApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'LINE_ITEM_NOT_FOUND_IN_CART';
    description?: string;
    data?: LineItemNotFoundInCartErrorData;
};
/** @docsIgnore */
type UpdateLineItemsApplicationErrors = {
    code?: 'EMPTY_LINE_ITEM_UPDATES';
    description?: string;
    data?: EmptyLineItemUpdatesErrorData;
} | {
    code?: 'DUPLICATE_LINE_ITEM_UPDATES';
    description?: string;
    data?: DuplicateLineItemUpdatesErrorData;
} | {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'LINE_ITEM_NOT_FOUND_IN_CART';
    description?: string;
    data?: LineItemNotFoundInCartErrorData;
} | {
    code?: 'ITEM_NOT_FOUND_IN_CATALOG';
    description?: string;
    data?: ItemNotFoundInCatalogErrorData;
} | {
    code?: 'INSUFFICIENT_INVENTORY';
    description?: string;
    data?: InsufficientInventoryErrorData;
} | {
    code?: 'INVALID_MEMBERSHIP_PAYMENT_OPTION';
    description?: string;
    data?: InvalidMembershipPaymentOptionErrorData;
} | {
    code?: 'PLAN_LINE_ITEM_NOT_FOUND';
    description?: string;
    data?: LineItemNotFoundInCartErrorData;
};
/** @docsIgnore */
type AddCouponApplicationErrors = {
    code?: 'COUPON_ALREADY_EXISTS';
    description?: string;
    data?: CouponAlreadyExistsErrorData;
} | {
    code?: 'MAX_COUPONS_PER_CART_EXCEEDED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'COUPON_LOCKED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'INVALID_COUPON_CODE';
    description?: string;
    data?: InvalidCouponCodeErrorData;
} | {
    code?: 'INVALID_COUPON_STATUS';
    description?: string;
    data?: InvalidCouponStatusErrorData;
};
/** @docsIgnore */
type RemoveCouponApplicationErrors = {
    code?: 'COUPON_NOT_FOUND_IN_CART';
    description?: string;
    data?: CouponNotFoundInCartErrorData;
} | {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'COUPON_LOCKED';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type SetDeliveryMethodApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'DELIVERY_METHOD_NOT_FOUND';
    description?: string;
    data?: DeliveryMethodNotFoundErrorData;
} | {
    code?: 'DELIVERY_METHOD_MISSING_ADDRESS';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DELIVERY_METHOD_MISSING_LINE_ITEMS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type RemoveDeliveryMethodApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
};
/** @docsIgnore */
type AddGiftCardApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'INVALID_GIFT_CARD_CODE';
    description?: string;
    data?: InvalidGiftCardCodeErrorData;
} | {
    code?: 'INVALID_GIFT_CARD_STATUS';
    description?: string;
    data?: InvalidGiftCardStatusErrorData;
} | {
    code?: 'MAX_GIFT_CARDS_PER_CART_EXCEEDED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'GIFT_CARD_ALREADY_EXISTS';
    description?: string;
    data?: GiftCardAlreadyExistsErrorData;
} | {
    code?: 'GIFT_CARD_LOCKED';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'GIFT_CARD_PAYMENT_NOT_SUPPORTED_ON_DEMO_CART';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type RemoveGiftCardApplicationErrors = {
    code?: 'CART_ALREADY_ORDERED';
    description?: string;
    data?: CartAlreadyOrderedErrorData;
} | {
    code?: 'GIFT_CARD_NOT_FOUND_IN_CART';
    description?: string;
    data?: GiftCardNotFoundInCartErrorData;
} | {
    code?: 'GIFT_CARD_LOCKED';
    description?: string;
    data?: Record<string, any>;
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
    accountInfo?: AccountInfoMetadata;
}
interface AccountInfoMetadata {
    /** ID of the Wix account associated with the event */
    accountId: string;
    /** ID of the Wix site associated with the event. Only included when the event is tied to a specific site. */
    siteId?: string;
    /** ID of the parent Wix account. Only included when 'accountId' belongs to a child account. */
    parentAccountId?: string;
}
interface CartCalculatedEnvelope {
    data: CartSummary;
    metadata: EventMetadata;
}
/**
 * Triggered when a cart is calculated.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Read eCommerce - all read permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.READ-ECOM
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-STORES.READ-ORDERS
 * @permissionScope Read Stores - all read permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.READ-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId ECOM.READ_CARTS
 * @webhook
 * @eventType wix.ecom.v2.cart_calculated
 * @slug calculated
 */
declare function onCartCalculated(handler: (event: CartCalculatedEnvelope) => void | Promise<void>): void;
interface CartCreatedEnvelope {
    entity: Cart;
    metadata: EventMetadata;
}
/**
 * Triggered when a cart is created.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Read eCommerce - all read permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.READ-ECOM
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-STORES.READ-ORDERS
 * @permissionScope Read Stores - all read permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.READ-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId ECOM.READ_CARTS
 * @webhook
 * @eventType wix.ecom.v2.cart_created
 * @slug created
 */
declare function onCartCreated(handler: (event: CartCreatedEnvelope) => void | Promise<void>): void;
interface CartDeletedEnvelope {
    entity: Cart;
    metadata: EventMetadata;
}
/**
 * Triggered when a cart is deleted.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Read eCommerce - all read permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.READ-ECOM
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-STORES.READ-ORDERS
 * @permissionScope Read Stores - all read permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.READ-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId ECOM.READ_CARTS
 * @webhook
 * @eventType wix.ecom.v2.cart_deleted
 * @slug deleted
 */
declare function onCartDeleted(handler: (event: CartDeletedEnvelope) => void | Promise<void>): void;
interface CartUpdatedEnvelope {
    entity: Cart;
    metadata: EventMetadata;
    /** @hidden */
    modifiedFields: Record<string, any>;
}
/**
 * Triggered when a cart is updated.
 * @permissionScope Manage Stores - all permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.MANAGE-STORES
 * @permissionScope Read eCommerce - all read permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.READ-ECOM
 * @permissionScope Read Orders
 * @permissionScopeId SCOPE.DC-STORES.READ-ORDERS
 * @permissionScope Read Stores - all read permissions
 * @permissionScopeId SCOPE.DC-STORES-MEGA.READ-STORES
 * @permissionScope Manage Restaurants - all permissions
 * @permissionScopeId SCOPE.RESTAURANTS.MEGA-SCOPES
 * @permissionScope Manage eCommerce - all permissions
 * @permissionScopeId SCOPE.DC-ECOM-MEGA.MANAGE-ECOM
 * @permissionScope Manage Orders
 * @permissionScopeId SCOPE.DC-STORES.MANAGE-ORDERS
 * @permissionId ECOM.READ_CARTS
 * @webhook
 * @eventType wix.ecom.v2.cart_updated
 * @slug updated
 */
declare function onCartUpdated(handler: (event: CartUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Creates a cart.
 * @public
 * @requiredField options.catalogItems.catalogReference
 * @requiredField options.catalogItems.quantity
 * @requiredField options.catalogItems.selectedMembership._id
 * @requiredField options.catalogItems.selectedMembership.appId
 * @requiredField options.customItems.attributes
 * @requiredField options.customItems.name
 * @requiredField options.customItems.pricing
 * @requiredField options.customItems.quantityInfo
 * @requiredField options.customItems.quantityInfo.requestedQuantity
 * @permissionId ecom:v2:cart:create_cart
 * @permissionId ECOM.ADMIN_MODIFY_CARTS
 * @permissionId ECOM.ADMIN_MODIFY_CHECKOUTS
 * @applicableIdentity APP
 * @returns Created cart.
 * @fqn wix.ecom.cart.v2.CartService.CreateCart
 */
declare function createCart(options?: NonNullablePaths<CreateCartOptions, `catalogItems.${number}.catalogReference` | `catalogItems.${number}.quantity` | `catalogItems.${number}.selectedMembership._id` | `catalogItems.${number}.selectedMembership.appId` | `customItems.${number}.attributes` | `customItems.${number}.attributes.descriptionLines.${number}.name` | `customItems.${number}.attributes.itemType` | `customItems.${number}.name` | `customItems.${number}.pricing` | `customItems.${number}.pricing.price` | `customItems.${number}.quantityInfo` | `customItems.${number}.quantityInfo.requestedQuantity`, 5>): Promise<NonNullablePaths<Cart, `_id` | `revision` | `lineItems` | `lineItems.${number}._id` | `lineItems.${number}.name.original` | `lineItems.${number}.quantityInfo.confirmedQuantity` | `lineItems.${number}.quantityInfo.requestedQuantity` | `lineItems.${number}.quantityInfo.fixedQuantity` | `lineItems.${number}.pricing.unitPrice.amount` | `lineItems.${number}.pricing.unitPrice.convertedAmount` | `lineItems.${number}.pricing.priceDescription.original` | `lineItems.${number}.pricing.priceUndetermined` | `lineItems.${number}.source.catalogReference.catalogItemId` | `lineItems.${number}.source.catalogReference.appId` | `lineItems.${number}.attributes.itemType.preset` | `lineItems.${number}.attributes.itemType.custom` | `lineItems.${number}.attributes.physicalProperties.shippable` | `lineItems.${number}.attributes.membersOnly` | `lineItems.${number}.taxConfig.taxableAddress.addressType` | `lineItems.${number}.paymentConfig.savePaymentMethod` | `lineItems.${number}.paymentConfig.selectedMembership._id` | `lineItems.${number}.paymentConfig.selectedMembership.appId` | `lineItems.${number}.paymentConfig.paymentOption` | `lineItems.${number}.paymentConfig.membership.redemptionType` | `lineItems.${number}.status` | `lineItems.${number}.customLineItem` | `coupons` | `coupons.${number}._id` | `coupons.${number}.code` | `source.channelType` | `source.externalReferences` | `source.externalReferences.${number}.appId` | `source.createdBy.visitorId` | `source.createdBy.memberId` | `source.createdBy.userId` | `source.createdBy.appId` | `source.customContentReference.appId` | `source.customContentReference.componentId` | `businessInfo.languageCode` | `businessInfo.currencyCode` | `customerInfo.visitorId` | `customerInfo.memberId` | `customerInfo.userId` | `customerInfo.openAccess` | `customerInfo.vatId._id` | `customerInfo.vatId.type` | `customerInfo.languageCode` | `customerInfo.currencyCode` | `deliveryInfo.address.streetAddress.number` | `deliveryInfo.address.streetAddress.name` | `deliveryInfo.method.code` | `deliveryInfo.method.pickup` | `deliveryInfo.weightUnit` | `taxInfo.pricesIncludeTax` | `paymentInfo.giftCards` | `paymentInfo.giftCards.${number}._id` | `paymentInfo.giftCards.${number}.obfuscatedCode` | `paymentInfo.giftCards.${number}.appId` | `paymentInfo.currencyCode` | `orderPlaced` | `currentCart` | `demo`, 4> & {
    __applicationErrorsType?: CreateCartApplicationErrors;
}>;
interface CreateCartOptions {
    /** Cart to create. */
    cart?: Cart;
    /**
     * Catalog items to add to the cart.
     * @maxSize 300
     */
    catalogItems?: CatalogItemInput[];
    /**
     * Custom items to add to the cart.
     * Custom line items don't trigger the Catalog service plugin.
     * @maxSize 300
     */
    customItems?: CustomItemInput[];
    /**
     * Coupons to apply.
     * Currently only one is supported.
     * @maxSize 1
     */
    coupons?: CouponInput[];
    /** Delivery method to set. */
    deliveryMethod?: DeliveryMethodInput;
    /**
     * Gift cards to add.
     * @maxSize 5
     */
    giftCards?: GiftCardInput[];
}
/**
 * Retrieves a cart.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:get_cart
 * @applicableIdentity APP
 * @returns Requested cart.
 * @fqn wix.ecom.cart.v2.CartService.GetCart
 */
declare function getCart(cartId: string): Promise<NonNullablePaths<Cart, `_id` | `revision` | `lineItems` | `lineItems.${number}._id` | `lineItems.${number}.name.original` | `lineItems.${number}.quantityInfo.confirmedQuantity` | `lineItems.${number}.quantityInfo.requestedQuantity` | `lineItems.${number}.quantityInfo.fixedQuantity` | `lineItems.${number}.pricing.unitPrice.amount` | `lineItems.${number}.pricing.unitPrice.convertedAmount` | `lineItems.${number}.pricing.priceDescription.original` | `lineItems.${number}.pricing.priceUndetermined` | `lineItems.${number}.source.catalogReference.catalogItemId` | `lineItems.${number}.source.catalogReference.appId` | `lineItems.${number}.attributes.itemType.preset` | `lineItems.${number}.attributes.itemType.custom` | `lineItems.${number}.attributes.physicalProperties.shippable` | `lineItems.${number}.attributes.membersOnly` | `lineItems.${number}.taxConfig.taxableAddress.addressType` | `lineItems.${number}.paymentConfig.savePaymentMethod` | `lineItems.${number}.paymentConfig.selectedMembership._id` | `lineItems.${number}.paymentConfig.selectedMembership.appId` | `lineItems.${number}.paymentConfig.paymentOption` | `lineItems.${number}.paymentConfig.membership.redemptionType` | `lineItems.${number}.status` | `lineItems.${number}.customLineItem` | `coupons` | `coupons.${number}._id` | `coupons.${number}.code` | `source.channelType` | `source.externalReferences` | `source.externalReferences.${number}.appId` | `source.createdBy.visitorId` | `source.createdBy.memberId` | `source.createdBy.userId` | `source.createdBy.appId` | `source.customContentReference.appId` | `source.customContentReference.componentId` | `businessInfo.languageCode` | `businessInfo.currencyCode` | `customerInfo.visitorId` | `customerInfo.memberId` | `customerInfo.userId` | `customerInfo.openAccess` | `customerInfo.vatId._id` | `customerInfo.vatId.type` | `customerInfo.languageCode` | `customerInfo.currencyCode` | `deliveryInfo.address.streetAddress.number` | `deliveryInfo.address.streetAddress.name` | `deliveryInfo.method.code` | `deliveryInfo.method.pickup` | `deliveryInfo.weightUnit` | `taxInfo.pricesIncludeTax` | `paymentInfo.giftCards` | `paymentInfo.giftCards.${number}._id` | `paymentInfo.giftCards.${number}.obfuscatedCode` | `paymentInfo.giftCards.${number}.appId` | `paymentInfo.currencyCode` | `orderPlaced` | `currentCart` | `demo`, 4>>;
/**
 * Returns the upsell options available for the cart's line items.
 * @param cartId - Cart ID.
 * @internal
 * @documentationMaturity preview
 * @requiredField cartId
 * @permissionId ecom:v2:cart:get_upsell_options
 * @fqn wix.ecom.cart.v2.CartService.GetUpsellOptions
 */
declare function getUpsellOptions(cartId: string): Promise<NonNullablePaths<GetUpsellOptionsResponse, `itemsUpsells` | `itemsUpsells.${number}.lineItemId`, 2>>;
/**
 * Updates a cart.
 *
 * Use this endpoint to set and replace fields such as `note` and the delivery method.
 * To add or remove list elements (line items, coupons, gift cards), use the dedicated endpoints.
 * @param _id - Cart ID.
 * @public
 * @requiredField _id
 * @requiredField cart
 * @permissionId ecom:v2:cart:update_cart
 * @permissionId ECOM.ADMIN_MODIFY_CARTS
 * @permissionId ECOM.ADMIN_MODIFY_CHECKOUTS
 * @applicableIdentity APP
 * @returns Updated cart.
 * @fqn wix.ecom.cart.v2.CartService.UpdateCart
 */
declare function updateCart(_id: string, cart: UpdateCart): Promise<NonNullablePaths<Cart, `_id` | `revision` | `lineItems` | `lineItems.${number}._id` | `lineItems.${number}.name.original` | `lineItems.${number}.quantityInfo.confirmedQuantity` | `lineItems.${number}.quantityInfo.requestedQuantity` | `lineItems.${number}.quantityInfo.fixedQuantity` | `lineItems.${number}.pricing.unitPrice.amount` | `lineItems.${number}.pricing.unitPrice.convertedAmount` | `lineItems.${number}.pricing.priceDescription.original` | `lineItems.${number}.pricing.priceUndetermined` | `lineItems.${number}.source.catalogReference.catalogItemId` | `lineItems.${number}.source.catalogReference.appId` | `lineItems.${number}.attributes.itemType.preset` | `lineItems.${number}.attributes.itemType.custom` | `lineItems.${number}.attributes.physicalProperties.shippable` | `lineItems.${number}.attributes.membersOnly` | `lineItems.${number}.taxConfig.taxableAddress.addressType` | `lineItems.${number}.paymentConfig.savePaymentMethod` | `lineItems.${number}.paymentConfig.selectedMembership._id` | `lineItems.${number}.paymentConfig.selectedMembership.appId` | `lineItems.${number}.paymentConfig.paymentOption` | `lineItems.${number}.paymentConfig.membership.redemptionType` | `lineItems.${number}.status` | `lineItems.${number}.customLineItem` | `coupons` | `coupons.${number}._id` | `coupons.${number}.code` | `source.channelType` | `source.externalReferences` | `source.externalReferences.${number}.appId` | `source.createdBy.visitorId` | `source.createdBy.memberId` | `source.createdBy.userId` | `source.createdBy.appId` | `source.customContentReference.appId` | `source.customContentReference.componentId` | `businessInfo.languageCode` | `businessInfo.currencyCode` | `customerInfo.visitorId` | `customerInfo.memberId` | `customerInfo.userId` | `customerInfo.openAccess` | `customerInfo.vatId._id` | `customerInfo.vatId.type` | `customerInfo.languageCode` | `customerInfo.currencyCode` | `deliveryInfo.address.streetAddress.number` | `deliveryInfo.address.streetAddress.name` | `deliveryInfo.method.code` | `deliveryInfo.method.pickup` | `deliveryInfo.weightUnit` | `taxInfo.pricesIncludeTax` | `paymentInfo.giftCards` | `paymentInfo.giftCards.${number}._id` | `paymentInfo.giftCards.${number}.obfuscatedCode` | `paymentInfo.giftCards.${number}.appId` | `paymentInfo.currencyCode` | `orderPlaced` | `currentCart` | `demo`, 4> & {
    __applicationErrorsType?: UpdateCartApplicationErrors;
}>;
interface UpdateCart {
    /**
     * Cart ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Date and time the Cart was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the Cart was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Revision number, which increments by 1 each time the Cart is updated.
     * @readonly
     */
    revision?: string;
    /**
     * Line items added to the Cart by the customer.
     * This list may include in-stock items and invalid or out-of-stock items.
     * @maxSize 300
     * @readonly
     */
    lineItems?: V2LineItem[];
    /**
     * The sum of all line item final prices.
     * @readonly
     */
    subtotal?: ConvertedMoney;
    /**
     * Optional message left by the customer.
     * Usually intended for the merchant and contains special instructions, requests, or clarifications related to the order.
     * @maxLength 1000
     */
    note?: string | null;
    /**
     * List of coupons added to the Cart.
     * Currently only one coupon code is supported.
     * @maxSize 1
     * @readonly
     */
    coupons?: V2Coupon[];
    /** Information about the source of the Cart, detailing the origin or context in which the Cart was created. */
    source?: CartSource;
    /** Information about the site from which the Cart's line items were added. */
    businessInfo?: BusinessInfo;
    /** Information about the customer that owns the Cart. */
    customerInfo?: CustomerInfo;
    /** Delivery-related information associated with the Cart. */
    deliveryInfo?: DeliveryInfo;
    /** Tax-related information associated with the Cart. */
    taxInfo?: TaxInfo;
    /** Payment-related information associated with the Cart. */
    paymentInfo?: PaymentInfo;
    /**
     * Whether the Cart has been placed as an Order.
     * Defaults to `false`.
     * @readonly
     */
    orderPlaced?: boolean;
    /**
     * Order ID.
     * Empty until the checkout process is completed.
     * @format GUID
     * @readonly
     */
    orderId?: string | null;
    /**
     * Persistent ID that correlates the Cart with the created Order after checkout is completed.
     * @format GUID
     * @readonly
     */
    purchaseFlowId?: string | null;
    /**
     * Additional cart settings.
     *
     * Settings can only be configured at cart creation time and can't be changed afterwards.
     * @immutable
     * @readonly
     */
    settings?: CartSettings;
    /**
     * Custom checkout URL to redirect the customer to a checkout page.
     * By default, customers are redirected to the standard Wix checkout page.
     * Clients can override this behavior by providing their own URL here.
     * If the URL contains the placeholder `{checkout_id}`, it will be replaced with the actual Cart ID.
     * @maxLength 2048
     */
    customCheckoutUrl?: string | null;
    /**
     * Custom fields.
     * [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * Whether this is the customer's current cart.
     * The current cart can be accessed using the identity context (for example, `visitorId` from session cookies) without an explicit cart ID.
     * @readonly
     */
    currentCart?: boolean;
    /**
     * Legacy fields for backward compatibility with Cart V1.
     * Should be used only by the Cart API proxy to populate V1 fields.
     * Used to avoid re-calculating these fields in the proxy.
     * @internal
     */
    legacyFields?: LegacyFields;
    /**
     * Forms associated with the Cart's line items.
     * @internal
     * @readonly
     * @maxSize 1000
     */
    forms?: FormInfo[];
    /**
     * Whether this is a demo cart.
     * Demo carts are created in the Wix editor for merchant preview and can't proceed to checkout.
     *
     * Default: `false`
     */
    demo?: boolean;
    /**
     * Indicators that help clients decide whether to call EstimateCart.
     * @internal
     * @readonly
     */
    estimationIndicators?: CartEstimationIndicators;
    /**
     * Applied discounts from totals calculator. Used internally for re-calculation without discounts, and by cart-v1-proxy.
     * @internal
     * @readonly
     * @maxSize 100
     */
    tcAppliedDiscounts?: AppliedDiscount[];
}
/**
 * Deletes a cart.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:delete_cart
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.DeleteCart
 */
declare function deleteCart(cartId: string): Promise<void>;
/**
 * Refreshes the cart by re-evaluating prices, inventory, and discounts so it reflects the latest catalog data.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:refresh_cart
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.RefreshCart
 */
declare function refreshCart(cartId: string): Promise<NonNullablePaths<RefreshCartResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: RefreshCartApplicationErrors;
}>;
/**
 * Estimates cart totals from the cart's current state and selected calculation components.
 *
 * Provides a partial, component-based estimate for preview (for example, partial estimated totals).
 * Boolean flags control which components are included: delivery costs, additional fees, taxes, and gift cards.
 * Components that aren't enabled are excluded. Line item discounts are already applied to the cart.
 *
 * This isn't a full cart calculation.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:estimate_cart
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.EstimateCart
 */
declare function estimateCart(cartId: string, options?: EstimateCartOptions): Promise<NonNullablePaths<EstimateCartResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo` | `summary.cartId` | `summary.cartRevision` | `summary.calculationId` | `summary.lineItems` | `summary.lineItems.${number}.lineItemId` | `summary.lineItems.${number}.quantity` | `summary.lineItems.${number}.unitPrice.amount` | `summary.lineItems.${number}.unitPrice.convertedAmount` | `summary.discounts` | `summary.discounts.${number}.name.original` | `summary.discounts.${number}.source.sourceType` | `summary.discounts.${number}.scope` | `summary.deliverySummary.method.code` | `summary.deliverySummary.method.pickup` | `summary.additionalFees` | `summary.additionalFees.${number}.source` | `summary.taxSummary.taxes` | `summary.taxSummary.pricesIncludeTax` | `summary.taxSummary.lineItemTaxes` | `summary.taxSummary.lineItemTaxes.${number}.lineItemId` | `summary.taxSummary.additionalFeeTaxes` | `summary.taxSummary.additionalFeeTaxes.${number}.additionalFeeCode` | `summary.paymentSummary.giftCards` | `summary.paymentSummary.giftCards.${number}.giftCardId` | `summary.paymentSummary.memberships` | `summary.paymentSummary.memberships.${number}._id` | `summary.paymentSummary.memberships.${number}.appId` | `summary.paymentSummary.subscriptionCharges` | `summary.paymentSummary.requiresPaymentAfterGiftCard` | `summary.calculationErrors.generalShippingCalculationError.applicationError.code` | `summary.calculationErrors.generalShippingCalculationError.applicationError.description` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations.${number}.field` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations.${number}.description` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations.${number}.violatedRule` | `summary.calculationErrors.carrierErrors.errors` | `summary.calculationErrors.carrierErrors.errors.${number}.carrierId` | `summary.calculationErrors.orderValidationErrors` | `summary.spiViolations` | `summary.spiViolations.${number}.severity` | `summary.spiViolations.${number}.target.other.name` | `summary.spiViolations.${number}.target.lineItem.name` | `summary.violations` | `summary.violations.${number}.invalidCouponStatusData.minLineItemQuantityNotReachedDetails.requiredMinimumQuantity` | `summary.violations.${number}.invalidCouponStatusData.couponCode` | `summary.violations.${number}.invalidCouponStatusData.reason` | `summary.violations.${number}.invalidCouponCodeData.couponCode` | `summary.violations.${number}.invalidGiftCardCodeData.giftCardCode` | `summary.violations.${number}.invalidGiftCardStatusData.giftCardCode` | `summary.violations.${number}.invalidGiftCardStatusData.reason` | `summary.violations.${number}.lineItemsOutOfStockData.allLineItemsOutOfStock` | `summary.violations.${number}.invalidMembershipStatusData.reason` | `summary.violations.${number}.scope` | `summary.violations.${number}.code` | `summary.violations.${number}.severity`, 6> & {
    __applicationErrorsType?: EstimateCartApplicationErrors;
}>;
interface EstimateCartOptions {
    /**
     * Whether to retrieve the latest item prices, discounts, and inventory data from the catalogs before estimating the cart.
     *
     * Default: `false`.
     */
    refreshCart?: boolean;
    /** Level of business validation to run during estimation via the [Validations service plugin](https://dev.wix.com/api/rest/wix-ecommerce/validations-integration-spi/introduction). */
    validationConfig?: ValidationsConfigWithLiterals;
    /**
     * Whether to calculate delivery costs.
     *
     * Default: `false`.
     */
    calculateDelivery?: boolean;
    /**
     * Whether to calculate additional fees.
     *
     * Default: `false`.
     */
    calculateAdditionalFees?: boolean;
    /**
     * Whether to calculate tax.
     *
     * Default: `false`.
     */
    calculateTax?: boolean;
    /**
     * Whether to include the effect of gift card balances on the cart total.
     * Gift cards aren't redeemed; balances are reflected for estimation only.
     *
     * Default: `false`.
     */
    calculateGiftCards?: boolean;
}
/**
 * Calculates the cart from its current state (line items, discounts, delivery method, and more) and returns a detailed summary, including subtotal, delivery costs, taxes, fees, and total price.
 *
 * Line item discounts are already applied to the cart.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:calculate_cart
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.CalculateCart
 */
declare function calculateCart(cartId: string, options?: CalculateCartOptions): Promise<NonNullablePaths<CalculateCartResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo` | `summary.cartId` | `summary.cartRevision` | `summary.calculationId` | `summary.lineItems` | `summary.lineItems.${number}.lineItemId` | `summary.lineItems.${number}.quantity` | `summary.lineItems.${number}.unitPrice.amount` | `summary.lineItems.${number}.unitPrice.convertedAmount` | `summary.discounts` | `summary.discounts.${number}.name.original` | `summary.discounts.${number}.source.sourceType` | `summary.discounts.${number}.scope` | `summary.deliverySummary.method.code` | `summary.deliverySummary.method.pickup` | `summary.additionalFees` | `summary.additionalFees.${number}.source` | `summary.taxSummary.taxes` | `summary.taxSummary.pricesIncludeTax` | `summary.taxSummary.lineItemTaxes` | `summary.taxSummary.lineItemTaxes.${number}.lineItemId` | `summary.taxSummary.additionalFeeTaxes` | `summary.taxSummary.additionalFeeTaxes.${number}.additionalFeeCode` | `summary.paymentSummary.giftCards` | `summary.paymentSummary.giftCards.${number}.giftCardId` | `summary.paymentSummary.memberships` | `summary.paymentSummary.memberships.${number}._id` | `summary.paymentSummary.memberships.${number}.appId` | `summary.paymentSummary.subscriptionCharges` | `summary.paymentSummary.requiresPaymentAfterGiftCard` | `summary.calculationErrors.generalShippingCalculationError.applicationError.code` | `summary.calculationErrors.generalShippingCalculationError.applicationError.description` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations.${number}.field` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations.${number}.description` | `summary.calculationErrors.generalShippingCalculationError.validationError.fieldViolations.${number}.violatedRule` | `summary.calculationErrors.carrierErrors.errors` | `summary.calculationErrors.carrierErrors.errors.${number}.carrierId` | `summary.calculationErrors.orderValidationErrors` | `summary.spiViolations` | `summary.spiViolations.${number}.severity` | `summary.spiViolations.${number}.target.other.name` | `summary.spiViolations.${number}.target.lineItem.name` | `summary.violations` | `summary.violations.${number}.invalidCouponStatusData.minLineItemQuantityNotReachedDetails.requiredMinimumQuantity` | `summary.violations.${number}.invalidCouponStatusData.couponCode` | `summary.violations.${number}.invalidCouponStatusData.reason` | `summary.violations.${number}.invalidCouponCodeData.couponCode` | `summary.violations.${number}.invalidGiftCardCodeData.giftCardCode` | `summary.violations.${number}.invalidGiftCardStatusData.giftCardCode` | `summary.violations.${number}.invalidGiftCardStatusData.reason` | `summary.violations.${number}.lineItemsOutOfStockData.allLineItemsOutOfStock` | `summary.violations.${number}.invalidMembershipStatusData.reason` | `summary.violations.${number}.scope` | `summary.violations.${number}.code` | `summary.violations.${number}.severity`, 6> & {
    __applicationErrorsType?: CalculateCartApplicationErrors;
}>;
interface CalculateCartOptions {
    /**
     * Whether to retrieve the latest item prices, discounts, and inventory data from the catalogs before calculating the cart.
     *
     * Default: `false`.
     */
    refreshCart?: boolean;
}
/**
 * Retrieves the checkout URL for the cart.
 *
 * By default, this is the standard Wix checkout page.
 * If `customCheckoutUrl` is set, it overrides the default and is returned as the checkout URL.
 * @param cartId - ID of the Cart to calculate.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:get_checkout_url
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.GetCheckoutURL
 */
declare function getCheckoutUrl(cartId: string, options?: GetCheckoutUrlOptions): Promise<NonNullablePaths<GetCheckoutURLResponse, `checkoutUrl`, 0> & {
    __applicationErrorsType?: GetCheckoutUrlApplicationErrors;
}>;
interface GetCheckoutUrlOptions {
    /**
     * Checkout currency parameter to append as a query param
     * @format CURRENCY
     */
    currencyCode?: string | null;
}
/**
 * Completes checkout and creates an order.
 * This endpoint may charge the customer.
 *
 * Read more about the [eCommerce Orders API](https://dev.wix.com/api/rest/wix-ecommerce/orders-api/introduction).
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:place_order
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.PlaceOrder
 */
declare function placeOrder(cartId: string, options?: PlaceOrderOptions): Promise<NonNullablePaths<PlaceOrderResponse, `orderId` | `completed`, 0> & {
    __applicationErrorsType?: PlaceOrderApplicationErrors;
}>;
interface PlaceOrderOptions {
    /**
     * Price verification token from `CalculateCart` response.
     * Used to verify that prices shown to the customer haven't changed before checkout completes.
     * @maxLength 3000
     */
    priceVerificationToken?: string | null;
    /**
     * Payment token.
     * @internal
     * @maxLength 100
     */
    paymentToken?: string | null;
    /**
     * URLs to use after payment completes.
     * @internal
     */
    redirectUrls?: RedirectUrls;
    /** Whether to save the payment method on the order. */
    savePaymentMethod?: boolean;
    /** Whether to authorize the payment and delay capture. */
    delayCapture?: boolean;
    /**
     * Whether the order is being placed from within a native mobile app
     * (iOS or Android) where the buyer paid via the platform's in-app
     * purchase system.
     * When true and the order contains subscription items,
     * Wix does not collect recurring renewal charges — the mobile platform's
     * in-app purchase system handles all subsequent renewal billing.
     *
     * Default: `false`
     * @internal
     */
    inAppPurchase?: boolean;
}
/**
 * Adds line items to the cart.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @requiredField options.catalogItems.catalogReference
 * @requiredField options.catalogItems.quantity
 * @requiredField options.catalogItems.selectedMembership._id
 * @requiredField options.catalogItems.selectedMembership.appId
 * @requiredField options.customItems.attributes
 * @requiredField options.customItems.name
 * @requiredField options.customItems.pricing
 * @requiredField options.customItems.quantityInfo
 * @requiredField options.customItems.quantityInfo.requestedQuantity
 * @permissionId ecom:v2:cart:add_line_items
 * @permissionId ECOM.ADMIN_MODIFY_CARTS
 * @permissionId ECOM.ADMIN_MODIFY_CHECKOUTS
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.AddLineItems
 */
declare function addLineItems(cartId: string, options?: NonNullablePaths<AddLineItemsOptions, `catalogItems.${number}.catalogReference` | `catalogItems.${number}.quantity` | `catalogItems.${number}.selectedMembership._id` | `catalogItems.${number}.selectedMembership.appId` | `customItems.${number}.attributes` | `customItems.${number}.attributes.descriptionLines.${number}.name` | `customItems.${number}.attributes.itemType` | `customItems.${number}.name` | `customItems.${number}.pricing` | `customItems.${number}.pricing.price` | `customItems.${number}.quantityInfo` | `customItems.${number}.quantityInfo.requestedQuantity`, 5>): Promise<NonNullablePaths<AddLineItemsResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: AddLineItemsApplicationErrors;
}>;
interface AddLineItemsOptions {
    /**
     * Catalog items to add to the cart.
     * @maxSize 300
     */
    catalogItems?: CatalogItemInput[];
    /**
     * Custom items to add to the cart.
     * @maxSize 300
     */
    customItems?: CustomItemInput[];
}
/**
 * Removes line items from the cart.
 * @param cartId - Cart ID.
 * @param lineItemIds - Line item IDs to remove from the cart.
 * @public
 * @requiredField cartId
 * @requiredField lineItemIds
 * @permissionId ecom:v2:cart:remove_line_items
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.RemoveLineItems
 */
declare function removeLineItems(cartId: string, lineItemIds: string[]): Promise<NonNullablePaths<RemoveLineItemsResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: RemoveLineItemsApplicationErrors;
}>;
/**
 * Updates line items in the cart.
 *
 * Use this endpoint to update line item properties such as `quantity` and `selectedMembership`.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @requiredField options.lineItems.lineItemId
 * @requiredField options.lineItems.quantity.newQuantity
 * @permissionId ecom:v2:cart:update_line_items
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.UpdateLineItems
 */
declare function updateLineItems(cartId: string, options?: NonNullablePaths<UpdateLineItemsOptions, `lineItems.${number}.lineItemId` | `lineItems.${number}.quantity.newQuantity`, 3>): Promise<NonNullablePaths<UpdateLineItemsResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: UpdateLineItemsApplicationErrors;
}>;
interface UpdateLineItemsOptions {
    /**
     * Line item updates to apply to the cart.
     * @minSize 1
     * @maxSize 300
     */
    lineItems?: LineItemUpdate[];
}
/**
 * Adds a coupon to the cart.
 * @param cartId - Cart ID.
 * @param coupon - Coupon to apply.
 *
 * Currently only one is supported.
 * @public
 * @requiredField cartId
 * @requiredField coupon
 * @requiredField coupon.code
 * @permissionId ecom:v2:cart:add_coupon
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.AddCoupon
 */
declare function addCoupon(cartId: string, coupon: NonNullablePaths<CouponInput, `code`, 0>): Promise<NonNullablePaths<AddCouponResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: AddCouponApplicationErrors;
}>;
/**
 * Removes a coupon from the cart.
 * @param cartId - Cart ID.
 * @param couponId - Coupon ID.
 * @public
 * @requiredField cartId
 * @requiredField couponId
 * @permissionId ecom:v2:cart:remove_coupon
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.RemoveCoupon
 */
declare function removeCoupon(cartId: string, couponId: string): Promise<NonNullablePaths<RemoveCouponResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: RemoveCouponApplicationErrors;
}>;
/**
 * Sets the delivery method for the cart.
 * @param cartId - Cart ID.
 * @param deliveryMethod - Delivery method.
 * @public
 * @requiredField cartId
 * @requiredField deliveryMethod
 * @requiredField deliveryMethod.code
 * @permissionId ecom:v2:cart:set_delivery_method
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.SetDeliveryMethod
 */
declare function setDeliveryMethod(cartId: string, deliveryMethod: NonNullablePaths<DeliveryMethodInput, `code`, 0>): Promise<NonNullablePaths<SetDeliveryMethodResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: SetDeliveryMethodApplicationErrors;
}>;
/**
 * Removes the selected delivery method from the cart.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:remove_delivery_method
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.RemoveDeliveryMethod
 */
declare function removeDeliveryMethod(cartId: string): Promise<NonNullablePaths<RemoveDeliveryMethodResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: RemoveDeliveryMethodApplicationErrors;
}>;
/**
 * Adds a gift card to the cart.
 *
 * Once added, the gift card balance is used as a payment method during checkout, either partially or fully covering the cart total.
 * @param cartId - Cart ID.
 * @param giftCard - Gift card to add.
 *
 * Currently only one is supported.
 * @public
 * @requiredField cartId
 * @requiredField giftCard
 * @requiredField giftCard.code
 * @permissionId ecom:v2:cart:add_gift_card
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.AddGiftCard
 */
declare function addGiftCard(cartId: string, giftCard: NonNullablePaths<GiftCardInput, `code`, 0>): Promise<NonNullablePaths<AddGiftCardResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: AddGiftCardApplicationErrors;
}>;
/**
 * Removes a gift card from the cart.
 * @param cartId - Cart ID.
 * @param giftCardId - Gift card ID.
 * @public
 * @requiredField cartId
 * @requiredField giftCardId
 * @permissionId ecom:v2:cart:remove_gift_card
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.RemoveGiftCard
 */
declare function removeGiftCard(cartId: string, giftCardId: string): Promise<NonNullablePaths<RemoveGiftCardResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5> & {
    __applicationErrorsType?: RemoveGiftCardApplicationErrors;
}>;
/**
 * Marks the cart as completed after an order is created from it.
 *
 * Sets `orderPlaced` to `true`.
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:mark_cart_as_completed
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.MarkCartAsCompleted
 */
declare function markCartAsCompleted(cartId: string, options?: MarkCartAsCompletedOptions): Promise<NonNullablePaths<MarkCartAsCompletedResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5>>;
interface MarkCartAsCompletedOptions {
    /**
     * Order ID created from the cart.
     * @format GUID
     */
    orderId?: string | null;
}
/**
 * Handles asynchronous checkout completion and redirects to a specified URL.
 *
 * Triggered by Wix Cashier after payment completion, this endpoint finalizes the checkout process and redirects the user to the previously provided success URL.
 * In some cases, this endpoint might be called directly by the external payment provider.
 * @param token - Server-signed JWT with data required to complete checkout.
 * @public
 * @requiredField token
 * @permissionId ecom:v2:cart:handle_async_checkout_completion
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.HandleAsyncCheckoutCompletion
 */
declare function handleAsyncCheckoutCompletion(token: string): Promise<NonNullablePaths<RawHttpResponse, `body` | `headers` | `headers.${number}.key` | `headers.${number}.value`, 2>>;
/**
 * Updates form submissions associated with the cart's line items.
 *
 * Forms are defined by catalog apps and collect additional information per line item (for example, personalization text or event details).
 * @param cartId - Cart ID.
 * @public
 * @requiredField cartId
 * @permissionId ecom:v2:cart:update_form_submissions
 * @applicableIdentity APP
 * @fqn wix.ecom.cart.v2.CartService.UpdateFormSubmissions
 */
declare function updateFormSubmissions(cartId: string, options?: UpdateFormSubmissionsOptions): Promise<NonNullablePaths<UpdateFormSubmissionsResponse, `cart._id` | `cart.revision` | `cart.lineItems` | `cart.lineItems.${number}._id` | `cart.lineItems.${number}.name.original` | `cart.lineItems.${number}.quantityInfo.confirmedQuantity` | `cart.lineItems.${number}.quantityInfo.requestedQuantity` | `cart.lineItems.${number}.quantityInfo.fixedQuantity` | `cart.lineItems.${number}.pricing.unitPrice.amount` | `cart.lineItems.${number}.pricing.unitPrice.convertedAmount` | `cart.lineItems.${number}.pricing.priceDescription.original` | `cart.lineItems.${number}.pricing.priceUndetermined` | `cart.lineItems.${number}.source.catalogReference.catalogItemId` | `cart.lineItems.${number}.source.catalogReference.appId` | `cart.lineItems.${number}.attributes.itemType.preset` | `cart.lineItems.${number}.attributes.itemType.custom` | `cart.lineItems.${number}.attributes.physicalProperties.shippable` | `cart.lineItems.${number}.attributes.membersOnly` | `cart.lineItems.${number}.taxConfig.taxableAddress.addressType` | `cart.lineItems.${number}.paymentConfig.savePaymentMethod` | `cart.lineItems.${number}.paymentConfig.selectedMembership._id` | `cart.lineItems.${number}.paymentConfig.selectedMembership.appId` | `cart.lineItems.${number}.paymentConfig.paymentOption` | `cart.lineItems.${number}.paymentConfig.membership.redemptionType` | `cart.lineItems.${number}.status` | `cart.lineItems.${number}.customLineItem` | `cart.coupons` | `cart.coupons.${number}._id` | `cart.coupons.${number}.code` | `cart.source.channelType` | `cart.source.externalReferences` | `cart.source.externalReferences.${number}.appId` | `cart.source.createdBy.visitorId` | `cart.source.createdBy.memberId` | `cart.source.createdBy.userId` | `cart.source.createdBy.appId` | `cart.source.customContentReference.appId` | `cart.source.customContentReference.componentId` | `cart.businessInfo.languageCode` | `cart.businessInfo.currencyCode` | `cart.customerInfo.visitorId` | `cart.customerInfo.memberId` | `cart.customerInfo.userId` | `cart.customerInfo.openAccess` | `cart.customerInfo.vatId._id` | `cart.customerInfo.vatId.type` | `cart.customerInfo.languageCode` | `cart.customerInfo.currencyCode` | `cart.deliveryInfo.address.streetAddress.number` | `cart.deliveryInfo.address.streetAddress.name` | `cart.deliveryInfo.method.code` | `cart.deliveryInfo.method.pickup` | `cart.deliveryInfo.weightUnit` | `cart.taxInfo.pricesIncludeTax` | `cart.paymentInfo.giftCards` | `cart.paymentInfo.giftCards.${number}._id` | `cart.paymentInfo.giftCards.${number}.obfuscatedCode` | `cart.paymentInfo.giftCards.${number}.appId` | `cart.paymentInfo.currencyCode` | `cart.orderPlaced` | `cart.currentCart` | `cart.demo`, 5>>;
interface UpdateFormSubmissionsOptions {
    /**
     * Form submission data to set on the cart.
     * @minSize 1
     * @maxSize 1000
     */
    formSubmissions?: FormSubmission[];
}

export { type AccountInfo, type AccountInfoMetadata, type ActionEvent, type AddCouponApplicationErrors, type AddCouponRequest, type AddCouponResponse, type AddCouponToCurrentCartRequest, type AddCouponToCurrentCartResponse, type AddGiftCardApplicationErrors, type AddGiftCardRequest, type AddGiftCardResponse, type AddGiftCardToCurrentCartRequest, type AddGiftCardToCurrentCartResponse, type AddLineItemsApplicationErrors, type AddLineItemsOptions, type AddLineItemsRequest, type AddLineItemsResponse, type AddLineItemsToCurrentCartRequest, type AddLineItemsToCurrentCartResponse, type AdditionalFee, AdditionalFeeSource, type AdditionalFeeSourceWithLiterals, type AdditionalFeeTax, type Address, type AddressLocation, type AggregatedTaxBreakdown, type ApplicableLineItems, type ApplicationError, type AppliedDiscount, type AppliedDiscountDiscountSourceOneOf, type AutoTaxFallbackCalculationDetails, BalanceType, type BalanceTypeWithLiterals, type BaseEventMetadata, type Benefit, type BenefitQuotaOneOf, BenefitType, type BenefitTypeWithLiterals, type BillingSettings, type BusinessInfo, type CalculateCartApplicationErrors, type CalculateCartForV1Request, type CalculateCartForV1Response, type CalculateCartOptions, type CalculateCartRequest, type CalculateCartResponse, type CalculateCurrentCartRequest, type CalculateCurrentCartResponse, type CalculateTotalsResponse, type CalculatedItemModifier, type CalculatedLineItem, type CalculatedPlatformFee, type CalculationConfig, type CalculationErrors, type CalculationErrorsShippingCalculationErrorOneOf, type CalculationOverrides, type Carrier, type CarrierError, type CarrierErrors, type CarrierServiceOption, type Cart, type CartAlreadyOrderedErrorData, type CartCalculatedEnvelope, type CartCreatedEnvelope, type CartDeletedEnvelope, type CartEstimationIndicators, type CartRevisionMismatchErrorData, type CartSettings, type CartSource, type CartSummary, type CartUpdatedEnvelope, type CatalogItemInput, type CatalogOverrideFields, type CatalogReference, ChannelType, type ChannelTypeWithLiterals, type Charge, ChargeType, type ChargeTypeWithLiterals, type Color, type ConvertedMoney, type Coupon, type CouponAdded, type CouponAlreadyExistsErrorData, type CouponInput, type CouponNotFoundInCartErrorData, type CouponRemoved, type CoveredLineItem, type CreateCartApplicationErrors, type CreateCartOptions, type CreateCartRequest, type CreateCartResponse, type CreateCurrentCartRequest, type CreateCurrentCartResponse, type CreatedBy, type CreatedByIdOneOf, type CustomContentReference, type CustomField, type CustomItemAttributes, type CustomItemDeliveryConfig, type CustomItemInput, type CustomItemPaymentConfig, type CustomItemPricingInfo, type CustomItemQuantityInfo, type CustomItemSource, type CustomItemTaxConfig, type CustomValidationErrorData, type CustomerInfo, type CustomerInfoIdOneOf, type DeleteCartRequest, type DeleteCartResponse, type DeleteCurrentCartRequest, type DeleteCurrentCartResponse, type DeliveryAllocation, type DeliveryInfo, type DeliveryLogistics, type DeliveryMethod, type DeliveryMethodInput, type DeliveryMethodNotFoundErrorData, type DeliverySummary, type DeliveryTimeSlot, type Description, type DescriptionLine, type DescriptionLineDescriptionLineValueOneOf, type DescriptionLineName, DescriptionLineType, type DescriptionLineTypeWithLiterals, type DescriptionLineValueOneOf, type Details, type DetailsKindOneOf, type Dimensions, DimensionsUnit, type DimensionsUnitWithLiterals, type Discount, type DiscountRule, type DiscountRuleName, DiscountScope, type DiscountScopeWithLiterals, type DiscountSource, DiscountSourceType, type DiscountSourceTypeWithLiterals, DiscountType, type DiscountTypeWithLiterals, type DomainEvent, type DomainEventBodyOneOf, type DuplicateItemModifierIdsErrorData, type DuplicateLineItemUpdatesErrorData, type DuplicateModifierGroupIdsErrorData, type Empty, type EmptyLineItemUpdatesErrorData, type EmptyPaymentTokenErrorData, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EstimateCartApplicationErrors, type EstimateCartOptions, type EstimateCartRequest, type EstimateCartResponse, type EstimateCurrentCartRequest, type EstimateCurrentCartResponse, type EventMetadata, type ExistingMembership, type ExistingMembershipInput, type ExtendedFields, type ExternalReference, FallbackReason, type FallbackReasonWithLiterals, type FieldViolation, FileType, type FileTypeWithLiterals, type FixedQuantityItemErrorData, type FormIdentifier, type FormInfo, type FormSubmission, type FreeTrialPeriod, type FullAddressContactDetails, type FutureMembership, type FutureMembershipInput, type GetCartRequest, type GetCartResponse, type GetCheckoutURLRequest, type GetCheckoutURLResponse, type GetCheckoutUrlApplicationErrors, type GetCheckoutUrlOptions, type GetCurrentCartRequest, type GetCurrentCartResponse, type GetUpsellOptionsForCurrentCartRequest, type GetUpsellOptionsForCurrentCartResponse, type GetUpsellOptionsRequest, type GetUpsellOptionsResponse, type GiftCard, type GiftCardAdded, type GiftCardAlreadyExistsErrorData, type GiftCardCalculationError, type GiftCardInput, type GiftCardItemNotSupportedOnDemoCartErrorData, type GiftCardNotFoundInCartErrorData, type GiftCardRedeemErrorData, type GiftCardRemoved, type GiftCardSummary, type Group, type HandleAsyncCheckoutCompletionRequest, type HeadersEntry, type IdentificationData, type IdentificationDataIdOneOf, type InsufficientInventoryEntry, type InsufficientInventoryErrorData, type InvalidCartErrorData, type InvalidCouponCodeErrorData, InvalidCouponReason, type InvalidCouponReasonWithLiterals, type InvalidCouponStatusErrorData, type InvalidCouponStatusErrorDataDetailsOneOf, type InvalidGiftCardCodeErrorData, InvalidGiftCardReason, type InvalidGiftCardReasonWithLiterals, type InvalidGiftCardStatusErrorData, type InvalidMembership, type InvalidMembershipEntry, type InvalidMembershipErrorData, type InvalidMembershipPaymentOptionErrorData, InvalidMembershipReason, type InvalidMembershipReasonWithLiterals, type InvalidMembershipStatusErrorData, type InvalidPaymentStatusErrorData, type InvalidPriceVerificationTokenErrorData, type InvalidThirdPartyCheckoutTokenErrorData, type ItemAttributes, type ItemCombination, type ItemCombinationLineItem, type ItemDeliveryConfig, type ItemDiscount, type ItemModifier, type ItemNotFoundInCatalogErrorData, type ItemPaymentConfig, type ItemPriceBreakdown, type ItemPricingInfo, type ItemQuantityInfo, type ItemSource, ItemStatus, type ItemStatusWithLiterals, type ItemTaxConfig, type ItemTaxFullDetails, type ItemType, type ItemTypeItemTypeDataOneOf, ItemTypePreset, type ItemTypePresetWithLiterals, type ItemUpsells, JurisdictionType, type JurisdictionTypeWithLiterals, type LegacyFields, type LimitedQuota, LimitedQuotaType, type LimitedQuotaTypeWithLiterals, type LineItem, type LineItemAdded, type LineItemCoverage, type LineItemDiscount, type LineItemIdentifier, type LineItemIdentifierIdOneOf, type LineItemNotFoundInCartErrorData, type LineItemPriceConflictErrorData, type LineItemPricesData, type LineItemSummary, type LineItemTax, type LineItemUpdate, type LineItemsOutOfStockErrorData, ManualCalculationReason, type ManualCalculationReasonWithLiterals, type MarkCartAsCompletedOptions, type MarkCartAsCompletedRequest, type MarkCartAsCompletedResponse, type MaxItemModifiersExceededErrorData, type MaxLineItemsExceededErrorData, type MembersOnlyItemErrorData, type Membership, type MembershipInput, type MembershipInputMembershipOneOf, type MembershipName, type MembershipOptions, type MembershipPaymentCredits, type MembershipSummary, type MembershipSummaryMembershipIdentifierOneOf, type MerchantDiscount, type MessageEnvelope, type MinLineItemQuantityNotReachedDetails, type MinSubtotalNotReachedDetails, type ModifierGroup, type MultiCurrencyPrice, NameInLineItem, type NameInLineItemWithLiterals, NameInOther, type NameInOtherWithLiterals, type Other, type OtherCharge, type PaymentInfo, type PaymentOption, PaymentOptionType, type PaymentOptionTypeWithLiterals, type PaymentSummary, type PhysicalProperties, type PickupDetails, PickupMethod, type PickupMethodWithLiterals, type PlaceOrderApplicationErrors, type PlaceOrderOptions, type PlaceOrderRequest, type PlaceOrderResponse, type PlainTextValue, type PlatformFee, PlatformFeeChargeType, type PlatformFeeChargeTypeWithLiterals, type PlatformFeeSummary, type Policy, type Price, type PriceDescription, type PriceDetails, type PriceSummary, ProductPeriodAlignment, type ProductPeriodAlignmentWithLiterals, type PurchasablePlan, type QuantityUpdate, QuotaType, type QuotaTypeWithLiterals, RateType, type RateTypeWithLiterals, type RawHttpResponse, RedemptionType, type RedemptionTypeWithLiterals, type RedirectUrls, type RefreshCartApplicationErrors, type RefreshCartRequest, type RefreshCartResponse, type RefreshCurrentCartRequest, type RefreshCurrentCartResponse, type Region, type RemoveCouponApplicationErrors, type RemoveCouponFromCurrentCartRequest, type RemoveCouponFromCurrentCartResponse, type RemoveCouponRequest, type RemoveCouponResponse, type RemoveDeliveryMethodApplicationErrors, type RemoveDeliveryMethodFromCurrentCartRequest, type RemoveDeliveryMethodFromCurrentCartResponse, type RemoveDeliveryMethodRequest, type RemoveDeliveryMethodResponse, type RemoveGiftCardApplicationErrors, type RemoveGiftCardFromCurrentCartRequest, type RemoveGiftCardFromCurrentCartResponse, type RemoveGiftCardRequest, type RemoveGiftCardResponse, type RemoveLineItemsApplicationErrors, type RemoveLineItemsFromCurrentCartRequest, type RemoveLineItemsFromCurrentCartResponse, type RemoveLineItemsRequest, type RemoveLineItemsResponse, type RestoreInfo, RuleType, type RuleTypeWithLiterals, type Scope, type SecuredMedia, type SelectedCarrierServiceOption, type SelectedCarrierServiceOptionOtherCharge, type SelectedCarrierServiceOptionPrices, type SelectedMembership, type SelectedMembershipUpdate, type SelectedMemberships, type ServiceProperties, type SetDeliveryMethodApplicationErrors, type SetDeliveryMethodForCurrentCartRequest, type SetDeliveryMethodForCurrentCartResponse, type SetDeliveryMethodRequest, type SetDeliveryMethodResponse, Severity, type SeverityWithLiterals, type ShippingInformation, type ShippingOption, type ShippingPrice, type ShippingRegion, Source, type SourceWithLiterals, type StreetAddress, type SubscriptionCharges, SubscriptionFrequency, type SubscriptionFrequencyWithLiterals, type SubscriptionItemNotSupportedOnDemoCartErrorData, type SubscriptionOptionInfo, type SubscriptionSettings, type SubtotalBelowOrderMinimumErrorData, SuggestedFix, type SuggestedFixWithLiterals, type SystemError, type Target, type TargetTargetTypeOneOf, type Tax, type TaxBreakdown, type TaxCalculationDetails, type TaxCalculationDetailsCalculationDetailsOneOf, type TaxDetails, type TaxInfo, type TaxRateBreakdown, type TaxSummary, type TaxableAddress, type TaxableAddressTaxableAddressDataOneOf, TaxableAddressType, type TaxableAddressTypeWithLiterals, type Title, type TranslatableString, type UnmarkAsCurrentCartRequest, type UnmarkAsCurrentCartResponse, type UpdateCart, type UpdateCartApplicationErrors, type UpdateCartRequest, type UpdateCartResponse, type UpdateCurrentCartRequest, type UpdateCurrentCartResponse, type UpdateFormSubmissionsInCurrentCartRequest, type UpdateFormSubmissionsInCurrentCartResponse, type UpdateFormSubmissionsOptions, type UpdateFormSubmissionsRequest, type UpdateFormSubmissionsResponse, type UpdateLineItemsApplicationErrors, type UpdateLineItemsInCurrentCartRequest, type UpdateLineItemsInCurrentCartResponse, type UpdateLineItemsOptions, type UpdateLineItemsRequest, type UpdateLineItemsResponse, type UpsellBenefitMetadata, type UpsellOption, type V1AdditionalFee, type V1Membership, V1PlatformFeeChargeType, type V1PlatformFeeChargeTypeWithLiterals, type V1PriceSummary, type V2AdditionalFee, type V2CalculationErrors, type V2CalculationErrorsShippingCalculationErrorOneOf, type V2CarrierError, type V2CarrierErrors, type V2Coupon, type V2ExistingMembership, type V2GiftCard, type V2LineItem, type V2LineItemDiscount, type V2Membership, type V2MembershipIdentifierOneOf, type V2PriceSummary, type V2SelectedMembership, type V2TaxSummary, type V2Violation, type V2ViolationDataOneOf, type ValidatedExistingMemberships, type ValidatedPurchasablePlans, type ValidationError, ValidationsConfig, type ValidationsConfigWithLiterals, type VatId, VatType, type VatTypeWithLiterals, type Violation, ViolationCode, type ViolationCodeWithLiterals, ViolationScope, type ViolationScopeWithLiterals, ViolationSeverity, type ViolationSeverityWithLiterals, type ViolationWithErrorSeverityErrorData, type ViolationsList, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, WeightUnit, type WeightUnitWithLiterals, addCoupon, addGiftCard, addLineItems, calculateCart, createCart, deleteCart, estimateCart, getCart, getCheckoutUrl, getUpsellOptions, handleAsyncCheckoutCompletion, markCartAsCompleted, onCartCalculated, onCartCreated, onCartDeleted, onCartUpdated, placeOrder, refreshCart, removeCoupon, removeDeliveryMethod, removeGiftCard, removeLineItems, setDeliveryMethod, updateCart, updateFormSubmissions, updateLineItems };
