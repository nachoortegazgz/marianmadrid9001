import type { WQLSpec, FilterFactory, SortFactory } from '@wix/sdk-types';
import { type FieldRenameOptions } from './field-rename.js';
/**
 * Creates the Filter factory with field selector and logical operators
 * @param options - Field rename options; system field renaming is applied by default
 * @returns A FilterFactory with field selector and logical operators (and, or, not)
 */
export declare function createFilterFactory<T, S extends WQLSpec>(options?: FieldRenameOptions): FilterFactory<T, S>;
/**
 * Creates the Sort factory
 * @param options - Field rename options; system field renaming is applied by default
 * @returns A SortFactory for creating sort expressions
 */
export declare function createSortFactory<S extends WQLSpec>(options?: FieldRenameOptions): SortFactory<S>;
