"use strict";

exports.__esModule = true;
exports.AsyncPatchByFilterOptionsBuilder = void 0;
/**
 * Options builder for async patch-by-filter operations.
 *
 * Use this builder to configure async patch behavior such as hooks suppression.
 * @public
 * @builder
 * @documentationMaturity preview
 */

class WixDataAsyncPatchByFilterOptionsBuilderImpl {
  constructor(options = {}) {
    this.options = options;
  }
  withSuppressHooks(value) {
    return this.copy({
      suppressHooks: value
    });
  }
  withShowDrafts(value) {
    return this.copy({
      showDrafts: value
    });
  }
  build() {
    return {
      ...this.options
    };
  }
  copy(options) {
    return new WixDataAsyncPatchByFilterOptionsBuilderImpl({
      ...this.options,
      ...options
    });
  }
}

/**
 * Creates a new async patch-by-filter options builder.
 * @public
 * @documentationMaturity preview
 *
 * @example
 * ```ts title="Create an async patch-by-filter options builder"
 * import { items, AsyncPatchByFilterOptionsBuilder } from '@wix/data'
 *
 * const options = AsyncPatchByFilterOptionsBuilder()
 *   .withSuppressHooks(true)
 *   .build()
 *
 * const job = await items.asyncPatchByFilter(
 *   'myCollection',
 *   { archived: false },
 *   [],
 *   options
 * )
 * ```
 */
const AsyncPatchByFilterOptionsBuilder = () => new WixDataAsyncPatchByFilterOptionsBuilderImpl();
exports.AsyncPatchByFilterOptionsBuilder = AsyncPatchByFilterOptionsBuilder;
//# sourceMappingURL=WixDataAsyncPatchByFilterOptionsBuilder.js.map