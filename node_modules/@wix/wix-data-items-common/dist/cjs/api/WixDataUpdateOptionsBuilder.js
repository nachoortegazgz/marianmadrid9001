"use strict";

exports.__esModule = true;
exports.UpdateOptionsBuilder = void 0;
/**
 * Options builder for update operations.
 *
 * Use this builder to configure update behavior such as hooks suppression,
 * reference handling, and conditional updates.
 * @public
 * @builder
 * @documentationMaturity preview
 */

class WixDataUpdateOptionsBuilderImpl {
  constructor(options = {}) {
    this.options = options;
  }
  withSuppressHooks(value) {
    return this.copy({
      suppressHooks: value
    });
  }
  withShowDrafts(value) {
    return this.copy({
      showDrafts: value
    });
  }
  withIncludeReferences(value) {
    return this.copy({
      includeReferences: value
    });
  }
  withSaveReferences(value) {
    return this.copy({
      saveReferences: value
    });
  }
  withCondition(value) {
    return this.copy({
      condition: value.filter
    });
  }
  build() {
    return {
      ...this.options
    };
  }
  copy(options) {
    return new WixDataUpdateOptionsBuilderImpl({
      ...this.options,
      ...options
    });
  }
}

/**
 * Creates a new update options builder.
 * @public
 * @documentationMaturity preview
 *
 * @example
 * ```ts title="Create an update options builder"
 * import { items, Filter, UpdateOptionsBuilder } from '@wix/data'
 *
 * const options = UpdateOptionsBuilder()
 *   .withSuppressHooks(true)
 *   .withCondition(Filter('status').eq('active'))
 *   .build()
 *
 * const updatedItem = await items.update<MyItem>(
 *   'myCollection',
 *   { _id: 'myItemId', status: 'active' },
 *   options
 * )
 * ```
 */
const UpdateOptionsBuilder = () => new WixDataUpdateOptionsBuilderImpl();
exports.UpdateOptionsBuilder = UpdateOptionsBuilder;
//# sourceMappingURL=WixDataUpdateOptionsBuilder.js.map