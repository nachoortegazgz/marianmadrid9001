"use strict";

exports.__esModule = true;
exports.AsyncRemoveByFilterOptionsBuilder = void 0;
/**
 * Options builder for async remove-by-filter operations.
 *
 * Use this builder to configure async remove behavior such as hooks suppression.
 * @public
 * @builder
 * @documentationMaturity preview
 */

class WixDataAsyncRemoveByFilterOptionsBuilderImpl {
  constructor(options = {}) {
    this.options = options;
  }
  withSuppressHooks(value) {
    return this.copy({
      suppressHooks: value
    });
  }
  withShowDrafts(value) {
    return this.copy({
      showDrafts: value
    });
  }
  build() {
    return {
      ...this.options
    };
  }
  copy(options) {
    return new WixDataAsyncRemoveByFilterOptionsBuilderImpl({
      ...this.options,
      ...options
    });
  }
}

/**
 * Creates a new async remove-by-filter options builder.
 * @public
 * @documentationMaturity preview
 *
 * @example
 * ```ts title="Create an async remove-by-filter options builder"
 * import { items, AsyncRemoveByFilterOptionsBuilder } from '@wix/data'
 *
 * const options = AsyncRemoveByFilterOptionsBuilder()
 *   .withSuppressHooks(true)
 *   .build()
 *
 * const job = await items.asyncRemoveByFilter(
 *   'myCollection',
 *   { archived: true },
 *   options
 * )
 * ```
 */
const AsyncRemoveByFilterOptionsBuilder = () => new WixDataAsyncRemoveByFilterOptionsBuilderImpl();
exports.AsyncRemoveByFilterOptionsBuilder = AsyncRemoveByFilterOptionsBuilder;
//# sourceMappingURL=WixDataAsyncRemoveByFilterOptionsBuilder.js.map