import type { QueryBuilder as SDKQueryBuilder, FilterFactory, SortFactory } from '@wix/sdk-types';
import { WixDataQueryRequest, WixDataQuerySpec, WixDataItem } from './types';
/**
 * WixData QueryBuilder type.
 * @internal
 */
export type WixDataQueryBuilder = SDKQueryBuilder<WixDataItem, WixDataQuerySpec, WixDataQueryRequest>;
/**
 * Creates a new query builder.
 * @public
 * @documentationMaturity preview
 *
 * @example
 * ```ts title="Build a query"
 * import { items, QueryBuilder, Filter, Sort } from '@wix/data'
 *
 * const request = QueryBuilder()
 *   .withFilter(
 *     Filter.and(
 *       Filter('status').eq('active'),
 *       Filter('age').gt(25)
 *     )
 *   )
 *   .withSorting(Sort('lastName').asc())
 *   .withPaging({ limit: 10, offset: 0 })
 *   .build()
 *
 * const result = await items.query<MyItem>('myCollection', request)
 * ```
 */
export declare const QueryBuilder: () => WixDataQueryBuilder;
/**
 * Filter factory for creating filter expressions.
 * @public
 * @documentationMaturity preview
 *
 * @example
 * ```ts title="Create filter expressions"
 * import { Filter } from '@wix/data'
 *
 * // Simple field filter
 * Filter('status').eq('active')
 *
 * // Combining filters with AND
 * Filter.and(
 *   Filter('orderId').eq('123'),
 *   Filter('amount').gt(100)
 * )
 *
 * // Combining filters with OR
 * Filter.or(
 *   Filter('status').eq('pending'),
 *   Filter('status').eq('active')
 * )
 *
 * // Negating a filter
 * Filter.not(Filter('status').eq('deleted'))
 * ```
 */
export declare const Filter: FilterFactory<WixDataItem, WixDataQuerySpec>;
/**
 * Sort factory for creating sort expressions.
 * @public
 * @documentationMaturity preview
 *
 * @example
 * ```ts title="Create sort expressions"
 * import { Sort, QueryBuilder } from '@wix/data'
 *
 * // Descending sort
 * Sort('amount').desc()
 *
 * // Ascending sort
 * Sort('name').asc()
 *
 * // Use with QueryBuilder
 * QueryBuilder()
 *   .withSorting(Sort('amount').desc(), Sort('name').asc())
 *   .build()
 * ```
 */
export declare const Sort: SortFactory<WixDataQuerySpec>;
export type { FilterExpression, SortExpression, PagingFor, } from '@wix/sdk-types';
//# sourceMappingURL=WixDataQueryBuilder.d.ts.map