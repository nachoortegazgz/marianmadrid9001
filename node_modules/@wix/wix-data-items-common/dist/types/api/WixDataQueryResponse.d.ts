import { PagingMetadata } from './PagingMetadata';
import { WixDataItem } from './types';
export interface WixDataQueryResponse<Item = WixDataItem> {
    /** Retrieved items. */
    items: Item[];
    /** Paging information. */
    pagingMetadata: PagingMetadata;
}
//# sourceMappingURL=WixDataQueryResponse.d.ts.map