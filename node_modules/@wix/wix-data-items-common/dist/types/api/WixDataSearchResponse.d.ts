import { PagingMetadata } from './PagingMetadata';
import { WixDataItem } from './types';
export interface WixDataSearchResponse<Item = WixDataItem> {
    /** Retrieved items. */
    items: Item[];
    /** Paging information. */
    pagingMetadata: PagingMetadata;
}
//# sourceMappingURL=WixDataSearchResponse.d.ts.map