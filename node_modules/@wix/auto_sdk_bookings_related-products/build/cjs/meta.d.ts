import { ListRelatedProductsByServiceIdsRequest as ListRelatedProductsByServiceIdsRequest$1, ListRelatedProductsByServiceIdsResponse as ListRelatedProductsByServiceIdsResponse$1 } from './index.js';

/**
 * Associates a Bookings service with a set of Stores product tag IDs.
 * Products tagged with any of the stored IDs appear in the booking form.
 */
interface RelatedProducts {
    /**
     * RelatedProducts ID.
     * @format GUID
     * @readonly
     */
    id?: string;
    /**
     * Revision number, which increments by 1 each time the RelatedProducts is updated.
     * To prevent conflicting changes, the current revision must be passed when updating.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the RelatedProducts was created.
     * @readonly
     */
    createdDate?: Date | null;
    /**
     * Date and time the RelatedProducts was last updated.
     * @readonly
     */
    updatedDate?: Date | null;
    /**
     * ID of the Bookings service this record belongs to. One per service. Immutable after creation.
     * @format GUID
     * @immutable
     */
    serviceId?: string;
    /**
     * Tag IDs from `wix.os.tags.v1.tag`. Products tagged with any of these are shown in the booking form. Max 100.
     * @maxSize 100
     * @maxLength 5
     */
    productTagIds?: string[];
    /**
     * Optional display name.
     * @maxLength 200
     */
    name?: string | null;
    /**
     * Optional description.
     * @maxLength 2000
     */
    description?: string | null;
}
interface CreateRelatedProductsRequest {
    /** RelatedProducts to be created. */
    relatedProducts?: RelatedProducts;
}
interface CreateRelatedProductsResponse {
    /** The created RelatedProducts. */
    relatedProducts?: RelatedProducts;
}
interface GetRelatedProductsRequest {
    /**
     * ID of the RelatedProducts to retrieve.
     * @format GUID
     */
    relatedProductsId?: string;
}
interface GetRelatedProductsResponse {
    /** The requested RelatedProducts. */
    relatedProducts?: RelatedProducts;
}
interface GetRelatedProductsByServiceIdRequest {
    /**
     * ID of the Bookings service.
     * @format GUID
     */
    serviceId?: string;
    /** When true, fetches enriched product data from Stores alongside the config. */
    includeProducts?: boolean;
}
interface GetRelatedProductsByServiceIdResponse {
    /** RelatedProducts for this service. */
    relatedProducts?: RelatedProducts;
    /**
     * Summaries for products tagged with any of the product_tag_ids.
     * Populated only when include_products is true and product_tag_ids is non-empty.
     */
    productSummaries?: RelatedProductSummary[];
}
/**
 * Slim product representation returned when include_products is true.
 * Contains display fields for a product card. For full catalog data, call Stores Catalog APIs directly.
 */
interface RelatedProductSummary {
    /**
     * Product ID.
     * @format GUID
     */
    id?: string;
    /**
     * Display name.
     * @maxLength 500
     */
    name?: string | null;
    /**
     * URL slug for the product page.
     * @maxLength 500
     */
    slug?: string | null;
    /**
     * Full product page URL.
     * @maxLength 2048
     */
    url?: string | null;
    /**
     * Short plain-text description.
     * @maxLength 5000
     */
    description?: string | null;
    /** Primary product image. */
    media?: Image;
    /** Current selling price (amount, currency, formatted). */
    price?: Money;
    /** Compare-at price (original price before discount). Present only when the product is discounted. */
    compareAtPrice?: Money;
    /**
     * Optional badge text displayed on the product card (e.g., "Sale", "New").
     * @maxLength 100
     */
    ribbon?: string | null;
    /** Stock availability status. */
    availabilityStatus?: AvailabilityStatusWithLiterals;
    /**
     * True when the product has variant options or modifiers. The client must navigate the user to the
     * product page for option selection before purchase rather than enabling a direct "buy" action.
     */
    optionsOrModifiersSupported?: boolean | null;
    /** Tag IDs associated with this product (public and private). */
    tags?: Tags;
    /** Automatic discount info applied to this product. */
    discountInfo?: DiscountInfo;
    /** Variant count and the lowest-priced visible variant. */
    variantSummary?: VariantSummary;
    /** Pricing of the cheapest variant by base price. */
    minVariantPriceInfo?: MinVariantPriceInfo;
}
interface Image {
    /**
     * WixMedia image ID.
     * @maxLength 2048
     */
    id?: string;
    /**
     * Image URL.
     * @maxLength 2048
     */
    url?: string;
    /**
     * Original image height.
     * @readonly
     */
    height?: number;
    /**
     * Original image width.
     * @readonly
     */
    width?: number;
    /**
     * Image alt text.
     * @maxLength 2048
     */
    altText?: string | null;
}
/**
 * Money.
 * Default format to use. Sufficiently compliant with majority of standards: w3c, ISO 4217, ISO 20022, ISO 8583:2003.
 */
interface Money {
    /**
     * Monetary amount. Decimal string with a period as a decimal separator (e.g., 3.99).
     * @format DECIMAL_VALUE
     * @decimalValue options { gte:0, maxScale:2 }
     */
    value?: string;
    /**
     * Currency code. Must be valid ISO 4217 currency code (e.g., USD).
     * @format CURRENCY
     */
    currency?: string;
    /**
     * Monetary amount. Decimal string in local format (e.g., 1 000,30).
     * @maxLength 50
     * @readonly
     */
    formattedValue?: string | null;
}
/** Stock availability of a product. */
declare enum AvailabilityStatus {
    AVAILABILITY_STATUS_UNKNOWN = "AVAILABILITY_STATUS_UNKNOWN",
    IN_STOCK = "IN_STOCK",
    OUT_OF_STOCK = "OUT_OF_STOCK",
    PARTIALLY_OUT_OF_STOCK = "PARTIALLY_OUT_OF_STOCK"
}
/** @enumType */
type AvailabilityStatusWithLiterals = AvailabilityStatus | 'AVAILABILITY_STATUS_UNKNOWN' | 'IN_STOCK' | 'OUT_OF_STOCK' | 'PARTIALLY_OUT_OF_STOCK';
/** Tag IDs associated with a product (public and private). */
interface Tags {
    publicTagIds?: string[];
    privateTagIds?: string[];
}
/** Automatic discount info for a product. */
interface DiscountInfo {
    /**
     * Names of automatic discount rules that apply to this product.
     * @maxSize 100
     * @maxLength 100
     */
    discountRuleNames?: string[];
}
/** Summary of a product's variants. */
interface VariantSummary {
    /** Total number of variants for the product. */
    variantCount?: number;
    /** The lowest-priced visible variant. Present only when the product has visible variants. */
    minPriceVariant?: MinPriceVariant;
}
/** Slim representation of the lowest-priced variant for a product card. */
interface MinPriceVariant {
    /**
     * Variant ID.
     * @format GUID
     */
    id?: string;
    /** Current selling price of the variant. */
    price?: Money;
    /** Compare-at (original) price of the variant. */
    compareAtPrice?: Money;
    /** Price after automatic discounts applied. */
    priceAfterDiscount?: Money;
}
/** Pricing of the cheapest variant by base price. */
interface MinVariantPriceInfo {
    /** Current selling price of the cheapest variant. */
    actualPrice?: Money;
    /** Compare-at (original) price of the cheapest variant. */
    compareAtPrice?: Money;
    /**
     * SKU of the cheapest variant.
     * @maxLength 40
     */
    sku?: string | null;
}
interface ListRelatedProductsByServiceIdsRequest {
    /**
     * IDs of the Bookings services to look up. Max 100.
     * @format GUID
     * @maxSize 100
     */
    serviceIds: string[];
    /** When true, fetches enriched product data from Stores alongside the config. */
    includeProducts?: boolean;
    /**
     * Paging for the enriched product_summaries. Ignored when include_products is false.
     * When omitted, defaults to limit = 100.
     * The cursor returned in paging_metadata is only valid when paired with the same
     * service_ids in the next request.
     */
    cursorPaging?: CursorPaging;
}
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface ListRelatedProductsByServiceIdsResponse {
    /** One entry per service_id that has a RelatedProducts record. */
    relatedProducts?: RelatedProducts[];
    /**
     * Flat product summaries across all found records.
     * Populated only when include_products is true and at least one record has product_tag_ids.
     */
    productSummaries?: RelatedProductSummary[];
    /**
     * Paging metadata for product_summaries. Populated whenever include_products is true.
     * When no products are fetched (no matching configs, or no tags), count=0 and has_next=false.
     */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in current page. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface UpsertRelatedProductsRequest {
    /** RelatedProducts to create or update. */
    relatedProducts?: RelatedProducts;
}
interface UpsertRelatedProductsResponse {
    /** The created or updated RelatedProducts. */
    relatedProducts?: RelatedProducts;
    /** Indicates whether the record was newly created or an existing record was updated. */
    action?: UpsertActionWithLiterals;
}
/** Result of an upsert operation. */
declare enum UpsertAction {
    UPSERT_ACTION_UNKNOWN = "UPSERT_ACTION_UNKNOWN",
    UPSERT_ACTION_CREATED = "UPSERT_ACTION_CREATED",
    UPSERT_ACTION_UPDATED = "UPSERT_ACTION_UPDATED"
}
/** @enumType */
type UpsertActionWithLiterals = UpsertAction | 'UPSERT_ACTION_UNKNOWN' | 'UPSERT_ACTION_CREATED' | 'UPSERT_ACTION_UPDATED';
interface UpdateRelatedProductsRequest {
    /** RelatedProducts to be updated, may be partial. */
    relatedProducts?: RelatedProducts;
}
interface UpdateRelatedProductsResponse {
    /** Updated RelatedProducts. */
    relatedProducts?: RelatedProducts;
}
interface DeleteRelatedProductsRequest {
    /**
     * ID of the RelatedProducts to delete.
     * @format GUID
     */
    relatedProductsId?: string;
}
interface DeleteRelatedProductsResponse {
}
interface QueryRelatedProductsRequest {
    /** WQL expression. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface QueryRelatedProductsResponse {
    /** List of RelatedProducts. */
    relatedProducts?: RelatedProducts[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entityAsJson?: string;
    /** Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity */
    restoreInfo?: RestoreInfo;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntityAsJson?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntityAsJson?: string | null;
}
interface ActionEvent {
    bodyAsJson?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
/** @docsIgnore */
type ListRelatedProductsByServiceIdsApplicationErrors = {
    code?: 'STORES_UNAVAILABLE';
    description?: string;
    data?: Record<string, any>;
};

type __PublicMethodMetaInfo<K = string, M = unknown, T = unknown, S = unknown, Q = unknown, R = unknown> = {
    getUrl: (context: any) => string;
    httpMethod: K;
    path: string;
    pathParams: M;
    __requestType: T;
    __originalRequestType: S;
    __responseType: Q;
    __originalResponseType: R;
};
declare function listRelatedProductsByServiceIds(): __PublicMethodMetaInfo<'POST', {}, ListRelatedProductsByServiceIdsRequest$1, ListRelatedProductsByServiceIdsRequest, ListRelatedProductsByServiceIdsResponse$1, ListRelatedProductsByServiceIdsResponse>;

export { type AccountInfo as AccountInfoOriginal, type ActionEvent as ActionEventOriginal, AvailabilityStatus as AvailabilityStatusOriginal, type AvailabilityStatusWithLiterals as AvailabilityStatusWithLiteralsOriginal, type CreateRelatedProductsRequest as CreateRelatedProductsRequestOriginal, type CreateRelatedProductsResponse as CreateRelatedProductsResponseOriginal, type CursorPagingMetadata as CursorPagingMetadataOriginal, type CursorPaging as CursorPagingOriginal, type CursorQuery as CursorQueryOriginal, type CursorQueryPagingMethodOneOf as CursorQueryPagingMethodOneOfOriginal, type Cursors as CursorsOriginal, type DeleteRelatedProductsRequest as DeleteRelatedProductsRequestOriginal, type DeleteRelatedProductsResponse as DeleteRelatedProductsResponseOriginal, type DiscountInfo as DiscountInfoOriginal, type DomainEventBodyOneOf as DomainEventBodyOneOfOriginal, type DomainEvent as DomainEventOriginal, type Empty as EmptyOriginal, type EntityCreatedEvent as EntityCreatedEventOriginal, type EntityDeletedEvent as EntityDeletedEventOriginal, type EntityUpdatedEvent as EntityUpdatedEventOriginal, type GetRelatedProductsByServiceIdRequest as GetRelatedProductsByServiceIdRequestOriginal, type GetRelatedProductsByServiceIdResponse as GetRelatedProductsByServiceIdResponseOriginal, type GetRelatedProductsRequest as GetRelatedProductsRequestOriginal, type GetRelatedProductsResponse as GetRelatedProductsResponseOriginal, type IdentificationDataIdOneOf as IdentificationDataIdOneOfOriginal, type IdentificationData as IdentificationDataOriginal, type Image as ImageOriginal, type ListRelatedProductsByServiceIdsApplicationErrors as ListRelatedProductsByServiceIdsApplicationErrorsOriginal, type ListRelatedProductsByServiceIdsRequest as ListRelatedProductsByServiceIdsRequestOriginal, type ListRelatedProductsByServiceIdsResponse as ListRelatedProductsByServiceIdsResponseOriginal, type MessageEnvelope as MessageEnvelopeOriginal, type MinPriceVariant as MinPriceVariantOriginal, type MinVariantPriceInfo as MinVariantPriceInfoOriginal, type Money as MoneyOriginal, type QueryRelatedProductsRequest as QueryRelatedProductsRequestOriginal, type QueryRelatedProductsResponse as QueryRelatedProductsResponseOriginal, type RelatedProductSummary as RelatedProductSummaryOriginal, type RelatedProducts as RelatedProductsOriginal, type RestoreInfo as RestoreInfoOriginal, SortOrder as SortOrderOriginal, type SortOrderWithLiterals as SortOrderWithLiteralsOriginal, type Sorting as SortingOriginal, type Tags as TagsOriginal, type UpdateRelatedProductsRequest as UpdateRelatedProductsRequestOriginal, type UpdateRelatedProductsResponse as UpdateRelatedProductsResponseOriginal, UpsertAction as UpsertActionOriginal, type UpsertActionWithLiterals as UpsertActionWithLiteralsOriginal, type UpsertRelatedProductsRequest as UpsertRelatedProductsRequestOriginal, type UpsertRelatedProductsResponse as UpsertRelatedProductsResponseOriginal, type VariantSummary as VariantSummaryOriginal, WebhookIdentityType as WebhookIdentityTypeOriginal, type WebhookIdentityTypeWithLiterals as WebhookIdentityTypeWithLiteralsOriginal, type __PublicMethodMetaInfo, listRelatedProductsByServiceIds };
