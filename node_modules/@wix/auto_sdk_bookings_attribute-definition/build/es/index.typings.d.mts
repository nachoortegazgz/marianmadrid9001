import * as _wix_sdk_types from '@wix/sdk-types';
import { QuerySpec, Query, NonNullablePaths } from '@wix/sdk-types';

/**
 * An attribute definition is a typed custom property (string, number, or boolean) that can be
 * applied to entities. Each definition has a display name shown to end users, a value type,
 * optional type-specific configuration (such as a list of allowed string values), and a
 * default value.
 *
 * Attribute definitions are defined once per site. To set a value for a specific entity,
 * call Bulk Upsert Attribute Values; to read an entity's values together with their
 * definitions, call List Entity Attributes.
 */
interface AttributeDefinition extends AttributeDefinitionConfigOneOf {
    /** String configuration: the list of allowed string values. */
    stringConfig?: StringConfig;
    /** Number configuration: the valid integer range. */
    numberConfig?: NumberConfig;
    /**
     * Attribute definition ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Revision number, which increments by 1 each time the attribute definition is updated.
     * To prevent conflicting changes, the current revision must be passed when updating.
     * Ignored when creating an attribute definition.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the attribute definition was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the attribute definition was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Display name for the attribute, shown to end users (for example, `Capacity`).
     *
     * Must be unique within the site. Required when creating an attribute definition.
     * @maxLength 40
     * @minLength 1
     */
    name?: string | null;
    /**
     * Value type of this attribute.
     * Immutable after creation. `UpdateAttributeDefinition` rejects requests that change `value_type`.
     * Required when creating an attribute definition.
     */
    valueType?: ValueTypeWithLiterals;
    /**
     * Whether this attribute is shown to end users on the public site.
     *
     * - `true`: shown in the UI and available for filtering.
     * - `false`: hidden from end users. Back-office admins can still see and edit the attribute.
     *
     * Default: `true`.
     */
    visibility?: boolean | null;
    /**
     * Default value for the attribute. When a configuration change leaves a stored
     * attribute value out of range or outside the allowed values, that value is reset to this
     * default (see Update Attribute Definition).
     *
     * The variant must match `valueType`. Required when creating an attribute definition.
     */
    defaultValue?: AttributeDefaultValue;
    /**
     * Custom field data for the attribute definition.
     * [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * Custom tags for organizing and filtering attribute definitions.
     * Manage tags using `BulkUpdateAttributeDefinitionTags` or `BulkUpdateAttributeDefinitionTagsByFilter`.
     */
    tags?: Tags;
}
/** @oneof */
interface AttributeDefinitionConfigOneOf {
    /** String configuration: the list of allowed string values. */
    stringConfig?: StringConfig;
    /** Number configuration: the valid integer range. */
    numberConfig?: NumberConfig;
}
/** Value type of an attribute. */
declare enum ValueType {
    /** String attribute: single-select from a predefined list of allowed string values. */
    STRING = "STRING",
    /** Integer number attribute with a required min/max range. */
    NUMBER = "NUMBER",
    /** Boolean attribute (true/false). */
    BOOL = "BOOL"
}
/** @enumType */
type ValueTypeWithLiterals = ValueType | 'STRING' | 'NUMBER' | 'BOOL';
/** Configuration for STRING-type attributes. */
interface StringConfig {
    /**
     * Allowed string values for this attribute. Values can be added, removed, or renamed; on update, any entity value
     * that no longer matches the allowed values is reset to the attribute's default.
     * @minSize 1
     * @maxSize 50
     * @maxLength 40
     */
    allowedValues?: string[];
}
/**
 * Configuration for NUMBER-type attributes. Defines the valid range of integer values.
 * On update, any entity value that falls outside the range is reset to the attribute's default.
 */
interface NumberConfig {
    /** Minimum allowed value (integer). */
    min?: number;
    /** Maximum allowed value (integer). Must be greater than or equal to `min`. */
    max?: number;
}
/** Default value for an attribute. Exactly one variant is set, matching the attribute's value_type. */
interface AttributeDefaultValue extends AttributeDefaultValueValueOneOf {
    /**
     * Default value for a STRING-type attribute. Must be in StringConfig.allowed_values.
     * @maxLength 40
     */
    stringDefault?: string;
    /** Default value for a NUMBER-type attribute. Must be within NumberConfig [min, max]. */
    numberDefault?: number;
    /** Default value for a BOOL-type attribute. */
    boolDefault?: boolean;
}
/** @oneof */
interface AttributeDefaultValueValueOneOf {
    /**
     * Default value for a STRING-type attribute. Must be in StringConfig.allowed_values.
     * @maxLength 40
     */
    stringDefault?: string;
    /** Default value for a NUMBER-type attribute. Must be within NumberConfig [min, max]. */
    numberDefault?: number;
    /** Default value for a BOOL-type attribute. */
    boolDefault?: boolean;
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
/**
 * Common object for tags.
 * Should be use as in this example:
 * message Foo {
 * option (.wix.api.decomposite_of) = "wix.commons.v2.tags.Foo";
 * string id = 1;
 * ...
 * Tags tags = 5
 * }
 *
 * example of taggable entity
 * {
 * id: "123"
 * tags: {
 * public_tags: {
 * tag_ids:["11","22"]
 * },
 * private_tags: {
 * tag_ids: ["33", "44"]
 * }
 * }
 * }
 */
interface Tags {
    /** Tags that require an additional permission in order to access them, normally not given to site members or visitors. */
    privateTags?: TagList;
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    publicTags?: TagList;
}
interface TagList {
    /**
     * List of tag IDs.
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
interface AttributeDefinitionTagsModified {
    /** Updated attribute definition. */
    attributeDefinition?: AttributeDefinition;
    /** Tags that were assigned to the attribute definition. */
    assignedTags?: Tags;
    /** Tags that were unassigned from the attribute definition. */
    unassignedTags?: Tags;
}
interface CreateAttributeDefinitionRequest {
    /** Attribute definition to be created. */
    attributeDefinition: AttributeDefinition;
}
interface CreateAttributeDefinitionResponse {
    /** The created attribute definition. */
    attributeDefinition?: AttributeDefinition;
}
interface GetAttributeDefinitionRequest {
    /**
     * ID of the attribute definition to retrieve.
     * @format GUID
     */
    attributeDefinitionId: string;
}
interface GetAttributeDefinitionResponse {
    /** The requested attribute definition. */
    attributeDefinition?: AttributeDefinition;
}
interface UpdateAttributeDefinitionRequest {
    /** Attribute definition to be updated, may be partial. */
    attributeDefinition: AttributeDefinition;
}
interface UpdateAttributeDefinitionResponse {
    /** Updated attribute definition. */
    attributeDefinition?: AttributeDefinition;
}
interface DeleteAttributeDefinitionRequest {
    /**
     * ID of the attribute definition to delete.
     * @format GUID
     */
    attributeDefinitionId: string;
}
interface DeleteAttributeDefinitionResponse {
}
interface QueryAttributeDefinitionsRequest {
    /** WQL expression. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /**
     * Cursor paging options.
     *
     * Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
     */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryAttributeDefinitionsResponse {
    /** List of attribute definitions. */
    attributeDefinitions?: AttributeDefinition[];
    /** Paging metadata. */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in current page. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface AssignGroupToAttributesRequest {
    /**
     * ID of the group to assign.
     * @format GUID
     */
    groupId?: string;
    /**
     * IDs of attribute definitions to assign the group to (max 50 per call).
     * @format GUID
     * @minSize 1
     * @maxSize 50
     */
    attributeDefinitionIds?: string[];
}
interface AssignGroupToAttributesResponse {
    /** The updated attribute definitions, each with the group now included in group_ids. */
    attributeDefinitions?: AttributeDefinition[];
}
interface UnassignGroupFromAttributesRequest {
    /**
     * ID of the group to unassign.
     * @format GUID
     */
    groupId?: string;
    /**
     * IDs of the attribute definitions to unassign the group from (max 50 per call).
     * @format GUID
     * @minSize 1
     * @maxSize 50
     */
    attributeDefinitionIds?: string[];
}
interface UnassignGroupFromAttributesResponse {
}
interface BulkUpdateAttributeDefinitionTagsRequest {
    /**
     * IDs of attribute definitions whose tags will be updated.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    attributeDefinitionIds: string[];
    /** Tags to assign. */
    assignTags?: Tags;
    /** Tags to unassign. */
    unassignTags?: Tags;
}
interface BulkUpdateAttributeDefinitionTagsResponse {
    /**
     * Results.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateAttributeDefinitionTagsResult[];
    /** Metadata regarding the bulk update operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface ItemMetadata {
    /**
     * Item ID. Provided only whenever possible. For example, `itemId` can't be provided when item creation has failed.
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action for this item was successful. When `false`, the `error` field is returned. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkUpdateAttributeDefinitionTagsResult {
    /** Metadata regarding the specific single update operation. */
    itemMetadata?: ItemMetadata;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface BulkUpdateAttributeDefinitionTagsByFilterRequest {
    /** Filter expression. An empty filter updates all attribute definitions. */
    filter: Record<string, any> | null;
    /** Tags to assign. */
    assignTags?: Tags;
    /** Tags to unassign. */
    unassignTags?: Tags;
}
interface BulkUpdateAttributeDefinitionTagsByFilterResponse {
    /**
     * Job ID for tracking the async operation. Pass this ID to Get Async Job to track the operation's progress.
     * @format GUID
     */
    jobId?: string;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
/** @docsIgnore */
type CreateAttributeDefinitionApplicationErrors = {
    code?: 'NUMBER_CONFIG_MAX_LESS_THAN_MIN';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DEFAULT_VALUE_TYPE_MISMATCH';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DEFAULT_VALUE_NOT_IN_ALLOWED_VALUES';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DEFAULT_VALUE_OUT_OF_RANGE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ATTRIBUTE_NAME';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type UpdateAttributeDefinitionApplicationErrors = {
    code?: 'VALUE_TYPE_IMMUTABLE';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'NUMBER_CONFIG_MAX_LESS_THAN_MIN';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DUPLICATE_ATTRIBUTE_NAME';
    description?: string;
    data?: Record<string, any>;
} | {
    code?: 'DEFAULT_VALUE_NO_LONGER_VALID';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkUpdateAttributeDefinitionTagsApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
/** @docsIgnore */
type BulkUpdateAttributeDefinitionTagsByFilterApplicationErrors = {
    code?: 'EMPTY_ASSIGN_AND_UNASSIGN_LISTS';
    description?: string;
    data?: Record<string, any>;
};
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
    accountInfo?: AccountInfoMetadata;
}
interface AccountInfoMetadata {
    /** ID of the Wix account associated with the event */
    accountId: string;
    /** ID of the Wix site associated with the event. Only included when the event is tied to a specific site. */
    siteId?: string;
    /** ID of the parent Wix account. Only included when 'accountId' belongs to a child account. */
    parentAccountId?: string;
}
interface AttributeDefinitionCreatedEnvelope {
    entity: AttributeDefinition;
    metadata: EventMetadata;
}
/**
 * Triggered when an attribute definition is created.
 * @permissionId bookings:attributes:v1:attribute_definition:read_events
 * @webhook
 * @eventType wix.bookings.attributes.v1.attribute_definition_created
 * @slug created
 * @documentationMaturity preview
 */
declare function onAttributeDefinitionCreated(handler: (event: AttributeDefinitionCreatedEnvelope) => void | Promise<void>): void;
interface AttributeDefinitionDeletedEnvelope {
    entity: AttributeDefinition;
    metadata: EventMetadata;
}
/**
 * Triggered when an attribute definition is deleted. Its attribute values are also removed.
 * @permissionId bookings:attributes:v1:attribute_definition:read_events
 * @webhook
 * @eventType wix.bookings.attributes.v1.attribute_definition_deleted
 * @slug deleted
 * @documentationMaturity preview
 */
declare function onAttributeDefinitionDeleted(handler: (event: AttributeDefinitionDeletedEnvelope) => void | Promise<void>): void;
interface AttributeDefinitionTagsModifiedEnvelope {
    data: AttributeDefinitionTagsModified;
    metadata: EventMetadata;
}
/**
 * Triggered when tags are assigned to or unassigned from an attribute definition.
 * @permissionId bookings:attributes:v1:attribute_definition:read_events
 * @webhook
 * @eventType wix.bookings.attributes.v1.attribute_definition_tags_modified
 * @slug tags_modified
 * @documentationMaturity preview
 */
declare function onAttributeDefinitionTagsModified(handler: (event: AttributeDefinitionTagsModifiedEnvelope) => void | Promise<void>): void;
interface AttributeDefinitionUpdatedEnvelope {
    entity: AttributeDefinition;
    metadata: EventMetadata;
    /** @hidden */
    modifiedFields: Record<string, any>;
}
/**
 * Triggered when an attribute definition is updated.
 * @permissionId bookings:attributes:v1:attribute_definition:read_events
 * @webhook
 * @eventType wix.bookings.attributes.v1.attribute_definition_updated
 * @slug updated
 * @documentationMaturity preview
 */
declare function onAttributeDefinitionUpdated(handler: (event: AttributeDefinitionUpdatedEnvelope) => void | Promise<void>): void;
/**
 * Creates an attribute definition.
 * @param attributeDefinition - Attribute definition to be created.
 * @public
 * @documentationMaturity preview
 * @requiredField attributeDefinition
 * @requiredField attributeDefinition.defaultValue
 * @requiredField attributeDefinition.name
 * @requiredField attributeDefinition.numberConfig.max
 * @requiredField attributeDefinition.numberConfig.min
 * @requiredField attributeDefinition.valueType
 * @permissionId bookings:attributes:v1:attribute_definition:create_attribute_definition
 * @applicableIdentity APP
 * @returns The created attribute definition.
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.CreateAttributeDefinition
 */
declare function createAttributeDefinition(attributeDefinition: NonNullablePaths<AttributeDefinition, `defaultValue` | `name` | `numberConfig.max` | `numberConfig.min` | `valueType`, 1>): Promise<NonNullablePaths<AttributeDefinition, `stringConfig.allowedValues` | `numberConfig.min` | `numberConfig.max` | `_id` | `valueType` | `defaultValue.stringDefault` | `defaultValue.numberDefault` | `defaultValue.boolDefault` | `tags.privateTags.tagIds`, 2> & {
    __applicationErrorsType?: CreateAttributeDefinitionApplicationErrors;
}>;
/**
 * Retrieves an attribute definition by its ID.
 * @param attributeDefinitionId - ID of the attribute definition to retrieve.
 * @public
 * @documentationMaturity preview
 * @requiredField attributeDefinitionId
 * @permissionId bookings:attributes:v1:attribute_definition:get_attribute_definition
 * @applicableIdentity APP
 * @returns The requested attribute definition.
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.GetAttributeDefinition
 */
declare function getAttributeDefinition(attributeDefinitionId: string): Promise<NonNullablePaths<AttributeDefinition, `stringConfig.allowedValues` | `numberConfig.min` | `numberConfig.max` | `_id` | `valueType` | `defaultValue.stringDefault` | `defaultValue.numberDefault` | `defaultValue.boolDefault` | `tags.privateTags.tagIds`, 2>>;
/**
 * Updates an attribute definition.
 *
 * Each time the attribute definition is updated, `revision` increments by 1. The current `revision` must be passed when updating the attribute definition. This ensures you're working with the latest attribute definition and prevents unintended overwrites.
 *
 * `valueType` is immutable. The request is rejected if a different `valueType` is passed.
 *
 * Changing the attribute's `config` (a string attribute's `allowedValues`, or a number attribute's `min`/`max`) re-validates every stored attribute value for this definition across all entities: any value that no longer conforms to the new configuration is reset to the attribute's `defaultValue`.
 * @param _id - Attribute definition ID.
 * @public
 * @documentationMaturity preview
 * @requiredField _id
 * @requiredField attributeDefinition
 * @requiredField attributeDefinition.revision
 * @permissionId bookings:attributes:v1:attribute_definition:update_attribute_definition
 * @applicableIdentity APP
 * @returns Updated attribute definition.
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.UpdateAttributeDefinition
 */
declare function updateAttributeDefinition(_id: string, attributeDefinition: NonNullablePaths<UpdateAttributeDefinition, `revision`, 0>): Promise<NonNullablePaths<AttributeDefinition, `stringConfig.allowedValues` | `numberConfig.min` | `numberConfig.max` | `_id` | `valueType` | `defaultValue.stringDefault` | `defaultValue.numberDefault` | `defaultValue.boolDefault` | `tags.privateTags.tagIds`, 2> & {
    __applicationErrorsType?: UpdateAttributeDefinitionApplicationErrors;
}>;
interface UpdateAttributeDefinition {
    /** String configuration: the list of allowed string values. */
    stringConfig?: StringConfig;
    /** Number configuration: the valid integer range. */
    numberConfig?: NumberConfig;
    /**
     * Attribute definition ID.
     * @format GUID
     * @readonly
     */
    _id?: string;
    /**
     * Revision number, which increments by 1 each time the attribute definition is updated.
     * To prevent conflicting changes, the current revision must be passed when updating.
     * Ignored when creating an attribute definition.
     * @readonly
     */
    revision?: string | null;
    /**
     * Date and time the attribute definition was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the attribute definition was last updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Display name for the attribute, shown to end users (for example, `Capacity`).
     *
     * Must be unique within the site. Required when creating an attribute definition.
     * @maxLength 40
     * @minLength 1
     */
    name?: string | null;
    /**
     * Value type of this attribute.
     * Immutable after creation. `UpdateAttributeDefinition` rejects requests that change `value_type`.
     * Required when creating an attribute definition.
     */
    valueType?: ValueTypeWithLiterals;
    /**
     * Whether this attribute is shown to end users on the public site.
     *
     * - `true`: shown in the UI and available for filtering.
     * - `false`: hidden from end users. Back-office admins can still see and edit the attribute.
     *
     * Default: `true`.
     */
    visibility?: boolean | null;
    /**
     * Default value for the attribute. When a configuration change leaves a stored
     * attribute value out of range or outside the allowed values, that value is reset to this
     * default (see Update Attribute Definition).
     *
     * The variant must match `valueType`. Required when creating an attribute definition.
     */
    defaultValue?: AttributeDefaultValue;
    /**
     * Custom field data for the attribute definition.
     * [Extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields) must be configured in the app dashboard before they can be accessed with API calls.
     */
    extendedFields?: ExtendedFields;
    /**
     * Custom tags for organizing and filtering attribute definitions.
     * Manage tags using `BulkUpdateAttributeDefinitionTags` or `BulkUpdateAttributeDefinitionTagsByFilter`.
     */
    tags?: Tags;
}
/**
 * Deletes an attribute definition.
 *
 * All attribute values for this attribute definition are deleted across every entity.
 * @param attributeDefinitionId - ID of the attribute definition to delete.
 * @public
 * @documentationMaturity preview
 * @requiredField attributeDefinitionId
 * @permissionId bookings:attributes:v1:attribute_definition:delete_attribute_definition
 * @applicableIdentity APP
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.DeleteAttributeDefinition
 */
declare function deleteAttributeDefinition(attributeDefinitionId: string): Promise<void>;
/**
 * Retrieves a list of up to 1000 attribute definitions, given the provided paging, filtering, and sorting.
 *
 * Query Attribute Definitions runs with these defaults, which you can override:
 *
 * - `createdDate` is sorted in `DESC` order
 * - `paging.limit` is `50`
 * - `paging.offset` is `0`
 *
 * Supported filter and sort fields: `id`, `name`, `valueType`, `visibility`, `tags.publicTags.tagIds`, `tags.privateTags.tagIds`.
 *
 * To learn about working with Query methods, see [API Query Language](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-the-wix-api-query-language), [Sorting and Paging](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-sorting-and-paging), and [Field Projection](https://dev.wix.com/docs/api-reference/articles/work-with-wix-apis/data-retrieval/about-field-projection).
 * @public
 * @documentationMaturity preview
 * @permissionId bookings:attributes:v1:attribute_definition:query_attribute_definitions
 * @applicableIdentity APP
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.QueryAttributeDefinitions
 */
declare function queryAttributeDefinitions(): AttributeDefinitionsQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface AttributeDefinitionsQueryResult extends QueryCursorResult {
    items: AttributeDefinition[];
    query: AttributeDefinitionsQueryBuilder;
    next: () => Promise<AttributeDefinitionsQueryResult>;
    prev: () => Promise<AttributeDefinitionsQueryResult>;
}
interface AttributeDefinitionsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    eq: (propertyName: '_id' | 'name' | 'valueType' | 'visibility', value: any) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ne: (propertyName: '_id' | 'name' | 'valueType' | 'visibility', value: any) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    ge: (propertyName: '_id' | 'name', value: any) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    gt: (propertyName: '_id' | 'name', value: any) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    le: (propertyName: '_id' | 'name', value: any) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     * @documentationMaturity preview
     */
    lt: (propertyName: '_id' | 'name', value: any) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     * @documentationMaturity preview
     */
    startsWith: (propertyName: '_id' | 'name', value: string) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     * @documentationMaturity preview
     */
    hasSome: (propertyName: 'tags.privateTags.tagIds' | 'tags.publicTags.tagIds', value: any[]) => AttributeDefinitionsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `values`.
     * @param values - List of values to compare against.
     * @documentationMaturity preview
     */
    hasAll: (propertyName: 'tags.privateTags.tagIds' | 'tags.publicTags.tagIds', value: any[]) => AttributeDefinitionsQueryBuilder;
    /** @documentationMaturity preview */
    in: (propertyName: '_id' | 'name' | 'valueType' | 'visibility', value: any) => AttributeDefinitionsQueryBuilder;
    /** @documentationMaturity preview */
    exists: (propertyName: '_id' | 'name' | 'valueType' | 'visibility' | 'tags.privateTags.tagIds' | 'tags.publicTags.tagIds', value: boolean) => AttributeDefinitionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    ascending: (...propertyNames: Array<'_id' | 'name' | 'valueType' | 'visibility' | 'tags.privateTags.tagIds' | 'tags.publicTags.tagIds'>) => AttributeDefinitionsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments.
     * @documentationMaturity preview
     */
    descending: (...propertyNames: Array<'_id' | 'name' | 'valueType' | 'visibility' | 'tags.privateTags.tagIds' | 'tags.publicTags.tagIds'>) => AttributeDefinitionsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object.
     * @documentationMaturity preview
     */
    limit: (limit: number) => AttributeDefinitionsQueryBuilder;
    /** @param cursor - A pointer to specific record
     * @documentationMaturity preview
     */
    skipTo: (cursor: string) => AttributeDefinitionsQueryBuilder;
    /** @documentationMaturity preview */
    find: () => Promise<AttributeDefinitionsQueryResult>;
}
/**
 * @hidden
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.QueryAttributeDefinitions
 * @requiredField query
 */
declare function typedQueryAttributeDefinitions(query: AttributeDefinitionQuery): Promise<NonNullablePaths<QueryAttributeDefinitionsResponse, `attributeDefinitions` | `attributeDefinitions.${number}.numberConfig.min` | `attributeDefinitions.${number}.numberConfig.max` | `attributeDefinitions.${number}._id` | `attributeDefinitions.${number}.valueType` | `attributeDefinitions.${number}.defaultValue.stringDefault` | `attributeDefinitions.${number}.defaultValue.numberDefault` | `attributeDefinitions.${number}.defaultValue.boolDefault`, 3>>;
interface AttributeDefinitionQuerySpec extends QuerySpec {
    paging: 'cursor';
    wql: [
        {
            fields: ['tags.privateTags.tagIds', 'tags.publicTags.tagIds'];
            operators: ['$hasAll', '$hasSome'];
            sort: 'BOTH';
        },
        {
            fields: ['_id', 'name', 'valueType', 'visibility'];
            operators: '*';
            sort: 'BOTH';
        }
    ];
}
type CommonQueryWithEntityContext = Query<AttributeDefinition, AttributeDefinitionQuerySpec>;
type AttributeDefinitionQuery = {
    /**
    Cursor paging options.
  
    Learn more about [cursor paging](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#cursor-paging).
    */
    cursorPaging?: {
        /**
      Maximum number of items to return in the results.
      @max: 100
      */
        limit?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['limit'] | null;
        /**
      Pointer to the next or previous page in the list of results.
    
      Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
      Not relevant for the first request.
      @maxLength: 16000
      */
        cursor?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['cursor'] | null;
    };
    /**
    Filter object.
  
    Learn more about the [filter section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-filter-section).
    */
    filter?: CommonQueryWithEntityContext['filter'] | null;
    /**
    Sort object.
  
    Learn more about the [sort section](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#the-sort-section).
    @maxSize: 5
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 512
      */
        fieldName?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
      */
        order?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['order'];
    }[];
};
declare const utils: {
    query: _wix_sdk_types.QueryHelpers<AttributeDefinition, AttributeDefinitionQuerySpec, AttributeDefinitionQuery>;
};
/**
 * Adds or removes tags on multiple attribute definitions.
 *
 * Specify which tags to assign and which tags to unassign. A tag that appears in both the list to assign and the list to unassign will be assigned. Tags that don't exist are ignored.
 * @param attributeDefinitionIds - IDs of attribute definitions whose tags will be updated.
 * @public
 * @documentationMaturity preview
 * @requiredField attributeDefinitionIds
 * @permissionId bookings:attributes:v1:attribute_definition:bulk_update_attribute_definition_tags
 * @applicableIdentity APP
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.BulkUpdateAttributeDefinitionTags
 */
declare function bulkUpdateAttributeDefinitionTags(attributeDefinitionIds: string[], options?: BulkUpdateAttributeDefinitionTagsOptions): Promise<NonNullablePaths<BulkUpdateAttributeDefinitionTagsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4> & {
    __applicationErrorsType?: BulkUpdateAttributeDefinitionTagsApplicationErrors;
}>;
interface BulkUpdateAttributeDefinitionTagsOptions {
    /** Tags to assign. */
    assignTags?: Tags;
    /** Tags to unassign. */
    unassignTags?: Tags;
}
/**
 * Asynchronously updates tags on multiple attribute definitions that match the provided filter criteria.
 *
 * An empty filter updates all attribute definitions. A tag that appears in both the list to assign and the list to unassign will be assigned. Tags that don't exist are ignored.
 *
 * To check the status of the bulk update, pass the returned `jobId` to [Get Async Job](https://dev.wix.com/docs/api-reference/business-management/async-job/get-async-job).
 * @param filter - Filter expression. An empty filter updates all attribute definitions.
 * @public
 * @documentationMaturity preview
 * @requiredField filter
 * @permissionId bookings:attributes:v1:attribute_definition:bulk_update_attribute_definition_tags_by_filter
 * @applicableIdentity APP
 * @fqn wix.bookings.attributes.v1.AttributeDefinitionsService.BulkUpdateAttributeDefinitionTagsByFilter
 */
declare function bulkUpdateAttributeDefinitionTagsByFilter(filter: Record<string, any>, options?: BulkUpdateAttributeDefinitionTagsByFilterOptions): Promise<NonNullablePaths<BulkUpdateAttributeDefinitionTagsByFilterResponse, `jobId`, 0> & {
    __applicationErrorsType?: BulkUpdateAttributeDefinitionTagsByFilterApplicationErrors;
}>;
interface BulkUpdateAttributeDefinitionTagsByFilterOptions {
    /** Tags to assign. */
    assignTags?: Tags;
    /** Tags to unassign. */
    unassignTags?: Tags;
}

export { type AccountInfo, type AccountInfoMetadata, type ActionEvent, type ApplicationError, type AssignGroupToAttributesRequest, type AssignGroupToAttributesResponse, type AttributeDefaultValue, type AttributeDefaultValueValueOneOf, type AttributeDefinition, type AttributeDefinitionConfigOneOf, type AttributeDefinitionCreatedEnvelope, type AttributeDefinitionDeletedEnvelope, type AttributeDefinitionQuery, type AttributeDefinitionQuerySpec, type AttributeDefinitionTagsModified, type AttributeDefinitionTagsModifiedEnvelope, type AttributeDefinitionUpdatedEnvelope, type AttributeDefinitionsQueryBuilder, type AttributeDefinitionsQueryResult, type BaseEventMetadata, type BulkActionMetadata, type BulkUpdateAttributeDefinitionTagsApplicationErrors, type BulkUpdateAttributeDefinitionTagsByFilterApplicationErrors, type BulkUpdateAttributeDefinitionTagsByFilterOptions, type BulkUpdateAttributeDefinitionTagsByFilterRequest, type BulkUpdateAttributeDefinitionTagsByFilterResponse, type BulkUpdateAttributeDefinitionTagsOptions, type BulkUpdateAttributeDefinitionTagsRequest, type BulkUpdateAttributeDefinitionTagsResponse, type BulkUpdateAttributeDefinitionTagsResult, type CommonQueryWithEntityContext, type CreateAttributeDefinitionApplicationErrors, type CreateAttributeDefinitionRequest, type CreateAttributeDefinitionResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteAttributeDefinitionRequest, type DeleteAttributeDefinitionResponse, type DomainEvent, type DomainEventBodyOneOf, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, type ExtendedFields, type GetAttributeDefinitionRequest, type GetAttributeDefinitionResponse, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, type MessageEnvelope, type NumberConfig, type QueryAttributeDefinitionsRequest, type QueryAttributeDefinitionsResponse, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, type StringConfig, type TagList, type Tags, type UnassignGroupFromAttributesRequest, type UnassignGroupFromAttributesResponse, type UpdateAttributeDefinition, type UpdateAttributeDefinitionApplicationErrors, type UpdateAttributeDefinitionRequest, type UpdateAttributeDefinitionResponse, ValueType, type ValueTypeWithLiterals, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, bulkUpdateAttributeDefinitionTags, bulkUpdateAttributeDefinitionTagsByFilter, createAttributeDefinition, deleteAttributeDefinition, getAttributeDefinition, onAttributeDefinitionCreated, onAttributeDefinitionDeleted, onAttributeDefinitionTagsModified, onAttributeDefinitionUpdated, queryAttributeDefinitions, typedQueryAttributeDefinitions, updateAttributeDefinition, utils };
