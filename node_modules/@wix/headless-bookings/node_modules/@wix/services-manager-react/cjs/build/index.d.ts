import { useSignals } from '@preact/signals-react/runtime';
import { ServiceAPI, ServiceDefinition, ServicesManager, ServicesRegistrar } from '@wix/services-manager/types';
import React, { PropsWithChildren } from 'react';
export declare function ServicesManagerProvider({ servicesManager, children, }: PropsWithChildren<{
    servicesManager: ServicesManager;
}>): React.JSX.Element;
export declare function useGetService(): import("@wix/services-definitions").GetService;
export declare function useService<T extends ServiceDefinition<any, any>>(service: T): ServiceAPI<T>;
export declare function useServices<T extends ServiceDefinition<any, any>[]>(services: T): {
    [K in keyof T]: ServiceAPI<T[K]>;
};
export declare function WixServices(props: {
    servicesMap: ServicesRegistrar;
    children: React.ReactNode;
    disposeOnUnmount?: boolean;
}): React.JSX.Element;
export { useSignals };
