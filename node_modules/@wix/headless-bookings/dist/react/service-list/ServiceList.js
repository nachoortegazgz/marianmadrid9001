import { jsx as _jsx } from "react/jsx-runtime";
/**
 * ServiceList - High-level component for displaying services list
 * Provides components for displaying services list with pagination
 */
import React from 'react';
import { GenericList, Filter as FilterPrimitive, } from '@wix/headless-components/react';
import { useService } from '@wix/services-manager-react';
import { ServiceListServiceDefinition } from '../../services/service-list/service-list.js';
import * as CoreServiceList from '../core/service-list/ServiceList.js';
import * as CoreServiceListFilter from '../core/service-list/ServiceListFilter.js';
import * as Service from '../service/Service.js';
export const TestIds = {
    serviceListRoot: 'service-list-root',
    serviceListServices: 'service-list-services',
    serviceListService: 'service-list-service',
    serviceListLoadMore: 'service-list-load-more',
    serviceListFilter: 'service-list-filter',
};
/**
 * Root component that provides the ServiceList service context for rendering services lists.
 *
 * @order 1
 * @component
 * @example
 * ```tsx
 * import { ServiceList } from '@wix/bookings/components';
 *
 * function ServicesPage({ serviceListConfig }) {
 *   return (
 *     <ServiceList.Root serviceListConfig={serviceListConfig}>
 *       <ServiceList.Services emptyState={<div>No services available</div>}>
 *         <ServiceList.ServiceRepeater>
 *           <Service.Name />
 *           <Service.Price />
 *         </ServiceList.ServiceRepeater>
 *       </ServiceList.Services>
 *     </ServiceList.Root>
 *   );
 * }
 * ```
 */
export const Root = React.forwardRef((props, ref) => {
    const { children, serviceListConfig = {}, className, variant } = props;
    return (_jsx(CoreServiceList.Root, { serviceListConfig: serviceListConfig, children: _jsx(RootContent, { className: className, ref: ref, variant: variant, children: children }) }));
});
Root.displayName = 'ServiceList.Root';
/**
 * Internal component to handle the Root content with service access
 */
const RootContent = React.forwardRef((props, ref) => {
    const { children, className, variant } = props;
    return (_jsx(CoreServiceList.Services, { children: ({ services: servicesList, isLoading, hasMore, loadMore, error }) => {
            const items = servicesList.map((service) => ({
                ...service,
                id: service._id,
            }));
            return (_jsx(GenericList.Root, { items: items, loadMore: () => loadMore(), hasMore: hasMore, isLoading: isLoading, error: error, className: className, ref: ref, "data-testid": TestIds.serviceListRoot, variant: variant, children: children }));
        } }));
});
/**
 * Container for the services list with empty, loading, and error state support.
 * Wraps GenericList.Items. Follows List Container Level pattern.
 *
 * @component
 * @example
 * ```tsx
 * <ServiceList.Services
 *   emptyState={<div>No services found</div>}
 *   loadingState={<div>Loading services...</div>}
 *   errorState={<div>Failed to load services</div>}
 * >
 *   <ServiceList.ServiceRepeater>
 *     <Service.Name />
 *     <Service.Price />
 *   </ServiceList.ServiceRepeater>
 * </ServiceList.Services>
 * ```
 */
export const Services = React.forwardRef((props, ref) => {
    const { children, emptyState, loadingState, errorState, ...otherProps } = props;
    return (_jsx(GenericList.Items, { ref: ref, "data-testid": TestIds.serviceListServices, emptyState: emptyState, loadingState: loadingState, errorState: errorState, ...otherProps, children: children }));
});
Services.displayName = 'ServiceList.Services';
/**
 * Repeater component that renders Service.Root for each service.
 * Follows Repeater Level pattern and uses GenericList.Repeater for consistency.
 * Supports asChild pattern for advanced layout components.
 *
 * @component
 * @example
 * ```tsx
 * // Standard usage
 * <ServiceList.ServiceRepeater>
 *   <Service.Name />
 *   <Service.Price />
 * </ServiceList.ServiceRepeater>
 *
 * // AsChild usage with custom wrapper
 * <ServiceList.ServiceRepeater asChild>
 *   {({ items, variant, itemWrapper }) => (
 *     <CustomWrapper
 *       items={items}
 *       variant={variant}
 *       itemRenderer={(item, index) =>
 *         itemWrapper({ item, index, children: <>
 *           <Service.Name />
 *           <Service.Price />
 *         </> })
 *       }
 *     />
 *   )}
 * </ServiceList.ServiceRepeater>
 * ```
 */
export const ServiceRepeater = React.forwardRef((props, ref) => {
    const { children, asChild } = props;
    // Check if children is a function that matches ServiceRootChildren pattern
    // (function with single parameter that receives { selected, bookable })
    const isServiceRootFunction = typeof children === 'function' && children.length === 1 && !asChild;
    // Custom itemWrapper that matches ItemWrapperProps signature
    const serviceItemWrapper = (wrapperProps) => {
        const { item: service, children: itemChildren } = wrapperProps;
        return (_jsx(Service.Root, { service: service, "data-testid": TestIds.serviceListService, "data-service-id": service._id, "data-item-id": service._id, "data-available": service.available, children: isServiceRootFunction
                ? children
                : itemChildren }, service._id));
    };
    // When isServiceRootFunction is true, children is a function for Service.Root (not repeater render props)
    // so we pass undefined and let itemWrapper handle the children
    const repeaterChildren = isServiceRootFunction ? undefined : children;
    return (_jsx(GenericList.Repeater, { ref: ref, asChild: asChild, itemWrapper: serviceItemWrapper, children: repeaterChildren }));
});
ServiceRepeater.displayName = 'ServiceList.ServiceRepeater';
/**
 * Displays a button to load more services. Not rendered if infiniteScroll is false or no services are left to load.
 * Follows the architecture rules - does not support asChild as it's a simple trigger component.
 *
 * @component
 * @example
 * ```tsx
 * <ServiceList.Actions.LoadMore asChild>
 *   <button>Load More</button>
 * </ServiceList.Actions.LoadMore>
 * ```
 */
export const LoadMore = React.forwardRef((props, ref) => {
    const { children = _jsx("button", {}), ...otherProps } = props;
    return (_jsx(GenericList.Actions.LoadMore, { ref: ref, "data-testid": TestIds.serviceListLoadMore, ...otherProps, children: children }));
});
LoadMore.displayName = 'ServiceList.Actions.LoadMore';
/**
 * Actions namespace for ServiceList
 */
export const Actions = {
    LoadMore,
};
/**
 * Filter component that provides filtering functionality for service lists.
 * Integrates with ServiceListService for categories and BookingService for location.
 * Delegates asChild behavior to the underlying Filter.Root primitive.
 *
 * @component
 * @example
 * ```tsx
 * import { Filter } from '@wix/headless-components/react';
 *
 * // Pattern 1 - Plain usage (renders with default div wrapper)
 * <ServiceList.Filter className="filter-sidebar">
 *   <Filter.FilterOptions>
 *     <Filter.FilterOptionRepeater>
 *       <Filter.FilterOption.Label />
 *       <Filter.FilterOption.SingleFilter />
 *     </Filter.FilterOptionRepeater>
 *   </Filter.FilterOptions>
 *   <Filter.Filtered>
 *     <Filter.Action.Clear label="Clear Filters" />
 *   </Filter.Filtered>
 * </ServiceList.Filter>
 *
 * // Pattern 2 - asChild with primitive (clones child, passes props)
 * <ServiceList.Filter asChild>
 *   <aside className="sidebar">
 *     <Filter.FilterOptions>
 *       <Filter.FilterOptionRepeater>
 *         <Filter.FilterOption.Label />
 *       </Filter.FilterOptionRepeater>
 *     </Filter.FilterOptions>
 *   </aside>
 * </ServiceList.Filter>
 *
 * // Pattern 3 - asChild with React component (render function receiving props)
 * <ServiceList.Filter asChild>
 *   {React.forwardRef(({ value, onChange, filterOptions, hasFilters, ...props }, ref) => (
 *     <aside ref={ref} {...props} className="custom-sidebar">
 *       {hasFilters && <button onClick={() => onChange({})}>Clear</button>}
 *       <Filter.FilterOptions>...</Filter.FilterOptions>
 *     </aside>
 *   ))}
 * </ServiceList.Filter>
 * ```
 */
export const Filter = React.forwardRef((props, ref) => {
    const { children, ...otherProps } = props;
    // Get filter value directly from service - single source of truth
    const serviceListService = useService(ServiceListServiceDefinition);
    const currentFilter = serviceListService.queryOptionsSignal.get().filter;
    return (_jsx(CoreServiceListFilter.Root, { children: ({ filterOptions, updateFilter, isFiltered }) => (_jsx(FilterPrimitive.Root, { ref: ref, value: (currentFilter ?? {}), onChange: (value) => updateFilter(value), filterOptions: filterOptions, "data-testid": TestIds.serviceListFilter, "data-is-filtered": isFiltered, ...otherProps, children: children })) }));
});
Filter.displayName = 'ServiceList.Filter';
