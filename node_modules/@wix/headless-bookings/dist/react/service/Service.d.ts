/**
 * Service - High-level component for displaying service information
 * Provides components for displaying service details and booking actions
 */
import React from 'react';
import { AsChildChildren } from '@wix/headless-utils/react';
import { MoneyData } from '@wix/headless-components/react';
import { type Service, type Payment, type DurationRange as DurationRangeData } from '@wix/auto_sdk_bookings_services';
import * as ServiceMediaModule from './ServiceMedia.js';
export declare const TestIds: {
    readonly serviceRoot: "service-root";
    readonly serviceName: "service-name";
    readonly serviceDescription: "service-description";
    readonly servicePrice: "service-price";
    readonly serviceTagline: "service-tagline";
    readonly serviceDurationInMinutes: "service-duration-in-minutes";
    readonly serviceDurationRange: "service-duration-range";
    readonly serviceType: "service-type";
    readonly serviceCategory: "service-category";
    readonly serviceLocations: "service-locations";
    readonly serviceLocation: "service-location";
    readonly serviceLocationRepeater: "service-location-repeater";
    readonly locationRoot: "location-root";
    readonly serviceStaffMembers: "service-staff-members";
    readonly serviceStaffMemberRepeater: "service-staff-member-repeater";
    readonly serviceSchedule: "service-schedule";
    readonly serviceDefaultCapacity: "service-default-capacity";
    readonly serviceRaw: "service-raw";
    readonly serviceActionSelect: "service-action-select";
    readonly serviceActionAdd: "service-action-add";
    readonly serviceActionRemove: "service-action-remove";
    readonly servicePolicyLatestCancellationInMinutes: "service-policy-latest-cancellation-in-minutes";
    readonly servicePolicyLatestBookingInMinutes: "service-policy-latest-booking-in-minutes";
    readonly servicePolicyEarliestBookingInMinutes: "service-policy-earliest-booking-in-minutes";
    readonly servicePolicyLatestRescheduleInMinutes: "service-policy-latest-reschedule-in-minutes";
    readonly servicePolicyWaitlistCapacity: "service-policy-waitlist-capacity";
    readonly servicePolicyBookAfterStartEnabled: "service-policy-book-after-start-enabled";
    readonly servicePolicyMaxParticipantsPerBooking: "service-policy-max-participants-per-booking";
    readonly serviceScheduleFirstSessionStart: "service-schedule-first-session-start";
    readonly serviceScheduleLastSessionEnd: "service-schedule-last-session-end";
    readonly serviceScheduleSessionDuration: "service-schedule-session-duration";
    readonly serviceAvailableOnline: "service-available-online";
};
/**
 * Base props shared by all Service.Root variants
 */
interface ServiceRootBaseProps {
    children?: React.ReactNode | AsChildChildren<{
        selected: boolean;
        bookable: boolean;
    }>;
    appId?: string;
    asChild?: boolean;
    className?: string;
}
/**
 * Props for Service.Root with pre-loaded service object (SSR pattern)
 */
type ServiceRootWithService = ServiceRootBaseProps & {
    service: Service;
    serviceId?: never;
    serviceSlug?: never;
};
/**
 * Props for Service.Root with dynamic loading by service ID
 */
type ServiceRootWithId = ServiceRootBaseProps & {
    service?: never;
    serviceId: string;
    serviceSlug?: never;
};
/**
 * Props for Service.Root with dynamic loading by service slug
 */
type ServiceRootWithSlug = ServiceRootBaseProps & {
    service?: never;
    serviceId?: never;
    serviceSlug: string;
};
/**
 * Props for Service.Root that reads from BookingService's selected service.
 * When used without service config, the component reads from the booking signal.
 * Requires BookingService to be available in the context.
 */
type ServiceRootFromBooking = ServiceRootBaseProps & {
    service?: never;
    serviceId?: never;
    serviceSlug?: never;
};
/**
 * Props for Service.Root component.
 * Accepts one of:
 * - service: Pre-loaded service object (SSR pattern)
 * - serviceId: Service ID for dynamic loading
 * - serviceSlug: Service slug for dynamic loading
 * - No service config: Reads from BookingService's selected service
 */
export type ServiceRootProps = ServiceRootWithService | ServiceRootWithId | ServiceRootWithSlug | ServiceRootFromBooking;
/**
 * Root component that provides service context to the entire app.
 * Can be used with:
 * - service: Pre-loaded service object (SSR pattern)
 * - serviceId: Service ID for dynamic loading
 * - serviceSlug: Service slug for dynamic loading
 * - No config: Reads from BookingService's selected service (requires BookingService)
 *
 * @order 1
 * @component
 * @example
 * ```tsx
 * import { Service } from '@wix/bookings/components';
 *
 * // With pre-loaded service object (SSR pattern)
 * function ServicePage({ service }) {
 *   return (
 *     <Service.Root service={service}>
 *       <Service.Name />
 *       <Service.Description />
 *     </Service.Root>
 *   );
 * }
 *
 * // With dynamic loading by serviceId
 * function ServicePageById({ serviceId }) {
 *   return (
 *     <Service.Root serviceId={serviceId}>
 *       <Service.Name />
 *       <Service.Description />
 *     </Service.Root>
 *   );
 * }
 *
 * // With dynamic loading by serviceSlug
 * function ServicePageBySlug({ slug }) {
 *   return (
 *     <Service.Root serviceSlug={slug}>
 *       <Service.Name />
 *       <Service.Description />
 *     </Service.Root>
 *   );
 * }
 *
 * // Reading from BookingService's selected service
 * // (requires Booking.Root parent context)
 * function SelectedServiceDisplay() {
 *   return (
 *     <Service.Root>
 *       <Service.Name />
 *       <Service.Price />
 *     </Service.Root>
 *   );
 * }
 * ```
 */
export declare const Root: React.ForwardRefExoticComponent<ServiceRootProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Name component
 */
export interface NameProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        name: string;
    }>;
}
/**
 * Displays the service name with customizable rendering.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Name className="text-4xl font-bold" />
 *
 * // asChild with primitive
 * <Service.Name asChild>
 *   <h1 className="text-4xl font-bold" />
 * </Service.Name>
 *
 * // asChild with custom component
 * <Service.Name asChild>
 *   {React.forwardRef(({name, ...props}, ref) => (
 *     <h1 ref={ref} {...props} className="text-4xl font-bold">
 *       {name}
 *     </h1>
 *   ))}
 * </Service.Name>
 * ```
 */
export declare const Name: React.ForwardRefExoticComponent<NameProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Description component
 */
export interface DescriptionProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        description: string;
    }>;
}
/**
 * Displays the service description with customizable rendering.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Description className="text-gray-600" />
 *
 * // asChild with primitive
 * <Service.Description asChild>
 *   <p className="text-gray-600" />
 * </Service.Description>
 * ```
 */
export declare const Description: React.ForwardRefExoticComponent<DescriptionProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Price component
 */
export interface PriceProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        servicePayment: Payment | undefined;
        rateType: Payment['rateType'] | undefined;
        money: MoneyData | null;
        minPrice: MoneyData | null;
        maxPrice: MoneyData | null;
        priceDescription: string | null;
        hasAddOns: boolean;
        hasPricingPlans: boolean;
    }>;
    className?: string;
}
/**
 * Displays the service price using the Money component with customizable rendering.
 * Renders different defaults based on rate type:
 * - FIXED: Shows formatted price (e.g., '$50.00')
 * - VARIED: Shows price range (e.g., '$30.00 - $50.00')
 * - CUSTOM: Shows custom description text
 * - NO_FEE: Shows zero price (e.g., '$0.00')
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - renders based on rate type
 * <Service.Price className="text-3xl font-bold" />
 *
 * // Custom rendering with full price data
 * <Service.Price asChild>
 *   {({ rateType, money, minPrice, maxPrice, priceDescription, hasAddOns, hasPricingPlans }) => (
 *     <div>
 *       {rateType === 'FIXED' && money && <Money money={money} />}
 *       {rateType === 'VARIED' && minPrice && maxPrice && (
 *         <span><Money money={minPrice} /> - <Money money={maxPrice} /></span>
 *       )}
 *       {rateType === 'CUSTOM' && priceDescription && <span>{priceDescription}</span>}
 *       {rateType === 'NO_FEE' && money && <Money money={money} />}
 *       {hasPricingPlans && <span>Pricing plans available</span>}
 *     </div>
 *   )}
 * </Service.Price>
 * ```
 */
export declare const Price: React.ForwardRefExoticComponent<PriceProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Tagline component
 */
export interface TaglineProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        tagline: string;
    }>;
}
/**
 * Displays the service tagline with customizable rendering.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Tagline className="text-lg text-secondary-foreground" />
 * ```
 *
 * @example
 * ```tsx
 * // asChild with primitive element
 * <Service.Tagline asChild>
 *   <p className="italic text-secondary-foreground" />
 * </Service.Tagline>
 * ```
 *
 * @example
 * ```tsx
 * // asChild with custom component accessing tagline data
 * <Service.Tagline asChild>
 *   {React.forwardRef(({tagline, ...props}, ref) => (
 *     <blockquote ref={ref} {...props} className="border-l-4 pl-4">
 *       "{tagline}"
 *     </blockquote>
 *   ))}
 * </Service.Tagline>
 * ```
 */
export declare const Tagline: React.ForwardRefExoticComponent<TaglineProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service DurationInMinutes component
 */
export interface DurationInMinutesProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        duration: number;
    }>;
}
/**
 * Displays the service duration in minutes (raw number) with customizable rendering.
 * Returns the duration as a numeric value representing minutes.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays raw minutes
 * <Service.DurationInMinutes className="text-base text-secondary-foreground" />
 * ```
 *
 * @example
 * ```tsx
 * // asChild with primitive element
 * <Service.DurationInMinutes asChild>
 *   <span className="font-semibold" />
 * </Service.DurationInMinutes>
 * ```
 *
 * @example
 * ```tsx
 * // asChild with custom component to format duration
 * <Service.DurationInMinutes asChild>
 *   {React.forwardRef(({duration, ...props}, ref) => (
 *     <span ref={ref} {...props}>
 *       {duration} minutes
 *     </span>
 *   ))}
 * </Service.DurationInMinutes>
 * ```
 */
export declare const DurationInMinutes: React.ForwardRefExoticComponent<DurationInMinutesProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service DurationRange component
 */
export interface DurationRangeProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        range: DurationRangeData;
    }>;
}
/**
 * Provides the service's supported session duration range as raw SDK data.
 * Renders nothing when the service has fewer than two distinct supported
 * durations — use {@link DurationInMinutes} for that case.
 *
 * The component is intentionally UI-agnostic: it does not pick formatting,
 * units, or labels for you. The default render outputs only the raw
 * `min - max` numbers (no unit text). Consumers wanting unit-aware UI use
 * `asChild` with a custom render and narrow on the SDK `range` payload
 * (`range.hourOptions` / `range.dayOptions` / `range.unitType`, or any
 * other unit configuration the SDK adds in the future).
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays raw min/max, e.g. "120 - 540" or "2 - 5"
 * <Service.DurationRange className="text-base text-secondary-foreground" />
 * ```
 *
 * @example
 * ```tsx
 * // asChild with primitive element
 * <Service.DurationRange asChild>
 *   <span className="font-semibold" />
 * </Service.DurationRange>
 * ```
 *
 * @example
 * ```tsx
 * // asChild with custom component - narrow on the SDK range payload to
 * // decide unit-specific formatting.
 * <Service.DurationRange asChild>
 *   {React.forwardRef(({ range, ...props }, ref) => {
 *     if (range.hourOptions) {
 *       const { minDurationInMinutes: min, maxDurationInMinutes: max } =
 *         range.hourOptions;
 *       return (
 *         <span ref={ref} {...props}>
 *           {min}-{max} min
 *         </span>
 *       );
 *     }
 *     if (range.dayOptions) {
 *       const { minDurationInDays: min, maxDurationInDays: max } =
 *         range.dayOptions;
 *       return (
 *         <span ref={ref} {...props}>
 *           {min}-{max} days
 *         </span>
 *       );
 *     }
 *     return <span ref={ref} {...props} />;
 *   })}
 * </Service.DurationRange>
 * ```
 */
export declare const DurationRange: React.ForwardRefExoticComponent<DurationRangeProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Type component
 */
export interface TypeProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        type: string;
    }>;
}
/**
 * Displays the service type (APPOINTMENT, CLASS, COURSE, etc.) with customizable rendering.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Type className="text-sm text-secondary-foreground uppercase" />
 * ```
 *
 * @example
 * ```tsx
 * // asChild with badge styling
 * <Service.Type asChild>
 *   <span className="px-2 py-1 bg-secondary text-secondary-foreground rounded-full text-xs" />
 * </Service.Type>
 * ```
 *
 * @example
 * ```tsx
 * // asChild with custom component to format type
 * <Service.Type asChild>
 *   {React.forwardRef(({type, ...props}, ref) => {
 *     const label = {
 *       APPOINTMENT: '1-on-1',
 *       CLASS: 'Group Class',
 *       COURSE: 'Course',
 *     }[type] || type;
 *     return <span ref={ref} {...props}>{label}</span>;
 *   })}
 * </Service.Type>
 * ```
 */
export declare const Type: React.ForwardRefExoticComponent<TypeProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Category component
 */
export interface CategoryProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        category: {
            name?: string;
            _id?: string;
        };
    }>;
}
/**
 * Displays the service category with customizable rendering.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Category className="text-sm text-secondary-foreground" />
 * ```
 *
 * @example
 * ```tsx
 * // asChild with link styling
 * <Service.Category asChild>
 *   <a href="#" className="text-primary hover:underline" />
 * </Service.Category>
 * ```
 *
 * @example
 * ```tsx
 * // asChild with custom component accessing category data
 * <Service.Category asChild>
 *   {React.forwardRef(({category, ...props}, ref) => (
 *     <a
 *       ref={ref}
 *       {...props}
 *       href={`/category/${category._id}`}
 *       className="text-primary hover:underline"
 *     >
 *       {category.name}
 *     </a>
 *   ))}
 * </Service.Category>
 * ```
 */
export declare const Category: React.ForwardRefExoticComponent<CategoryProps & React.RefAttributes<HTMLElement>>;
/**
 * Type for location
 */
type LocationType = NonNullable<Service['locations']>[number] & {
    id: string;
};
/**
 * Props for Service Locations component (Container Level)
 */
export interface LocationsProps {
    asChild?: boolean;
    children?: React.ReactNode;
    className?: string;
}
/**
 * Container for service locations list (Container Level).
 * Follows the 3-level List/Options/Repeater pattern.
 * Provides context and doesn't render when there are no locations.
 * Internally uses LocationList.Root for consistent location handling.
 *
 * @component
 * @example
 * ```tsx
 * // Basic usage with 3-level pattern
 * <Service.Locations>
 *   <Service.Locations.List emptyState={<div>No locations available</div>}>
 *     <Service.Locations.LocationRepeater>
 *       <Location.Name />
 *       <Location.Address />
 *     </Service.Locations.LocationRepeater>
 *   </Service.Locations.List>
 * </Service.Locations>
 *
 * // With asChild pattern for custom container
 * <Service.Locations asChild>
 *   <ul className="locations-list">
 *     <Service.Locations.List>
 *       <Service.Locations.LocationRepeater>
 *         <li><Location.Name /></li>
 *       </Service.Locations.LocationRepeater>
 *     </Service.Locations.List>
 *   </ul>
 * </Service.Locations>
 * ```
 */
declare const LocationsBase: React.ForwardRefExoticComponent<LocationsProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Locations.List component (List Container Level)
 */
export interface LocationsListProps {
    children?: React.ReactNode;
    emptyState?: React.ReactNode;
    className?: string;
}
/**
 * List container for locations with empty state support (List Container Level).
 * Renders emptyState when no locations, otherwise renders children.
 * Internally delegates to LocationList.Locations.
 *
 * @component
 * @example
 * ```tsx
 * <Service.Locations.List emptyState={<div>No locations available</div>}>
 *   <Service.Locations.LocationRepeater />
 * </Service.Locations.List>
 * ```
 */
export declare const LocationsList: React.ForwardRefExoticComponent<LocationsListProps & React.RefAttributes<HTMLElement>>;
/**
 * Render props for LocationRepeater asChild pattern
 */
export interface LocationRepeaterRenderProps {
    items: LocationType[];
    itemWrapper: (props: {
        item: LocationType;
        index: number;
        children: React.ReactNode;
    }) => React.ReactNode;
}
/**
 * Props for Service Locations.LocationRepeater component
 */
export interface LocationRepeaterProps {
    children?: React.ReactNode;
}
/**
 * Repeater component that renders Location.Root for each location.
 * Internally delegates to LocationList.LocationRepeater.
 * Children are rendered inside each Location.Root context.
 *
 * @component
 * @example
 * ```tsx
 * // Standard usage - renders Location.Root for each location
 * <Service.Locations.LocationRepeater>
 *   <Location.Name />
 *   <Location.Address />
 * </Service.Locations.LocationRepeater>
 *
 * // With custom styling
 * <Service.Locations.LocationRepeater>
 *   <div className="location-card">
 *     <Location.Name className="font-bold" />
 *     <Location.Address />
 *     <Location.Phone />
 *   </div>
 * </Service.Locations.LocationRepeater>
 * ```
 */
export declare const LocationRepeater: React.ForwardRefExoticComponent<LocationRepeaterProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service StaffMembers component (Container Level)
 */
export interface StaffMembersProps {
    asChild?: boolean;
    children?: React.ReactNode;
    className?: string;
}
/**
 * Container for service staff members list (Container Level).
 * Follows the 3-level List/Options/Repeater pattern.
 * Provides context and doesn't render when there are no staff members.
 * Internally uses StaffMemberList.Root for consistent staff member handling.
 *
 * @component
 * @example
 * ```tsx
 * // Basic usage with 3-level pattern
 * <Service.StaffMembers>
 *   <Service.StaffMembers.List emptyState={<div>No staff assigned</div>}>
 *     <Service.StaffMembers.StaffMemberRepeater>
 *       <StaffMember.Name />
 *     </Service.StaffMembers.StaffMemberRepeater>
 *   </Service.StaffMembers.List>
 * </Service.StaffMembers>
 *
 * // With asChild pattern for custom container
 * <Service.StaffMembers asChild>
 *   <ul className="staff-list">
 *     <Service.StaffMembers.List>
 *       <Service.StaffMembers.StaffMemberRepeater>
 *         <li><StaffMember.Name /></li>
 *       </Service.StaffMembers.StaffMemberRepeater>
 *     </Service.StaffMembers.List>
 *   </ul>
 * </Service.StaffMembers>
 * ```
 */
declare const StaffMembersBase: React.ForwardRefExoticComponent<StaffMembersProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service StaffMembers.List component (List Container Level)
 */
export interface StaffMembersListProps {
    children?: React.ReactNode;
    emptyState?: React.ReactNode;
    className?: string;
}
/**
 * List container for staff members with empty state support (List Container Level).
 * Renders emptyState when no staff members, otherwise renders children.
 * Internally delegates to StaffMemberList.StaffMembers.
 *
 * @component
 * @example
 * ```tsx
 * <Service.StaffMembers.List emptyState={<div>No staff assigned</div>}>
 *   <Service.StaffMembers.StaffMemberRepeater />
 * </Service.StaffMembers.List>
 * ```
 */
export declare const StaffMembersList: React.ForwardRefExoticComponent<StaffMembersListProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service StaffMembers.StaffMemberRepeater component
 */
export interface StaffMemberRepeaterProps {
    children?: React.ReactNode;
}
/**
 * Repeater component that renders StaffMember.Root for each staff member.
 * Internally delegates to StaffMemberList.StaffMemberRepeater.
 * Children are rendered inside each StaffMember.Root context.
 *
 * @component
 * @example
 * ```tsx
 * // Standard usage - renders StaffMember.Root for each staff member
 * <Service.StaffMembers.StaffMemberRepeater>
 *   <StaffMember.Name />
 * </Service.StaffMembers.StaffMemberRepeater>
 *
 * // With custom styling
 * <Service.StaffMembers.StaffMemberRepeater>
 *   <div className="staff-card">
 *     <StaffMember.Name className="font-bold" />
 *   </div>
 * </Service.StaffMembers.StaffMemberRepeater>
 * ```
 */
export declare const StaffMemberRepeater: React.ForwardRefExoticComponent<StaffMemberRepeaterProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service Raw component
 */
export interface RawProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        service: Service;
        isLoading: boolean;
        error: string | null;
    }>;
    className?: string;
}
/**
 * Provides direct access to service object. Should be used only in rare cases.
 *
 * @component
 * @example
 * ```tsx
 * // Using render prop pattern to access raw service data
 * <Service.Raw>
 *   {({ service, isLoading, error }) => (
 *     <div>
 *       {isLoading && <div>Loading...</div>}
 *       {error && <div>Error: {error}</div>}
 *       {service && <pre>{JSON.stringify(service, null, 2)}</pre>}
 *     </div>
 *   )}
 * </Service.Raw>
 *
 * // With asChild pattern
 * <Service.Raw asChild>
 *   <CustomServiceDisplay />
 * </Service.Raw>
 * ```
 */
export declare const Raw: React.ForwardRefExoticComponent<RawProps & React.RefAttributes<HTMLElement>>;
/**
 * Actions namespace with individual action components
 */
export declare namespace Actions {
    /**
     * Props for Select action component
     */
    interface SelectProps extends Omit<React.ButtonHTMLAttributes<HTMLButtonElement>, 'children' | 'disabled' | 'onClick'> {
        asChild?: boolean;
        children?: React.ReactNode | ((props: {
            bookable: boolean;
            selected: boolean;
            requiresAvailability: boolean;
            onClick: () => void;
            service: Service;
        }) => React.ReactNode);
        label?: string;
        /** Called after selection with the selected service */
        onClick?: (service: Service) => void;
    }
    /**
     * Select action component - replaces selection with only this service.
     *
     * @component
     * @example
     * ```tsx
     * // Default button
     * <Service.Actions.Select label="Select This Service" />
     *
     * // With asChild using custom Button component
     * <Service.Actions.Select asChild>
     *   <Button variant="primary">Select Service</Button>
     * </Service.Actions.Select>
     *
     * // With onClick callback for navigation
     * <Service.Actions.Select
     *   onClick={() => router.push('/booking/time-slots')}
     * />
     *
     * // Using render prop pattern to access bookable state
     * <Service.Actions.Select>
     *   {({ bookable, selected, onClick }) => (
     *     <button onClick={onClick} disabled={!bookable}>
     *       {selected ? 'Selected' : 'Select Service'}
     *     </button>
     *   )}
     * </Service.Actions.Select>
     * ```
     */
    const Select: React.ForwardRefExoticComponent<SelectProps & React.RefAttributes<HTMLButtonElement>>;
    /**
     * Props for Add action component
     */
    interface AddProps extends Omit<React.ButtonHTMLAttributes<HTMLButtonElement>, 'children' | 'disabled' | 'onClick'> {
        asChild?: boolean;
        children?: React.ReactNode | ((props: {
            bookable: boolean;
            selected: boolean;
            requiresAvailability: boolean;
            onClick: () => void;
            service: Service;
        }) => React.ReactNode);
        label?: string;
        /** Called after adding with the added service */
        onClick?: (service: Service) => void;
    }
    /**
     * Add action component - adds service to selection array.
     *
     * @component
     * @example
     * ```tsx
     * // Default button
     * <Service.Actions.Add label="Add to Selection" />
     *
     * // With asChild using custom Button component
     * <Service.Actions.Add asChild>
     *   <Button variant="secondary">Add Service</Button>
     * </Service.Actions.Add>
     *
     * // With onClick callback
     * <Service.Actions.Add
     *   onClick={() => console.log('Service added')}
     * />
     *
     * // Using render prop pattern to access bookable state
     * <Service.Actions.Add>
     *   {({ bookable, selected, onClick }) => (
     *     <button onClick={onClick} disabled={!bookable}>
     *       {selected ? 'Added' : 'Add to Booking'}
     *     </button>
     *   )}
     * </Service.Actions.Add>
     * ```
     */
    const Add: React.ForwardRefExoticComponent<AddProps & React.RefAttributes<HTMLButtonElement>>;
    /**
     * Props for Remove action component
     */
    interface RemoveProps extends Omit<React.ButtonHTMLAttributes<HTMLButtonElement>, 'children' | 'disabled' | 'onClick'> {
        asChild?: boolean;
        children?: React.ReactNode | ((props: {
            bookable: boolean;
            selected: boolean;
            requiresAvailability: boolean;
            onClick: () => void;
            service: Service;
        }) => React.ReactNode);
        label?: string;
        /** Called after removing with the removed service */
        onClick?: (service: Service) => void;
    }
    /**
     * Remove action component - removes service from selection array.
     *
     * @component
     * @example
     * ```tsx
     * // Default button
     * <Service.Actions.Remove label="Remove from Selection" />
     *
     * // With asChild using custom Button component
     * <Service.Actions.Remove asChild>
     *   <Button variant="destructive">Remove Service</Button>
     * </Service.Actions.Remove>
     *
     * // With onClick callback
     * <Service.Actions.Remove
     *   onClick={() => console.log('Service removed')}
     * />
     *
     * // Using render prop pattern to access bookable state
     * <Service.Actions.Remove>
     *   {({ bookable, selected, onClick }) => (
     *     <button onClick={onClick} disabled={!bookable}>
     *       Remove Service
     *     </button>
     *   )}
     * </Service.Actions.Remove>
     * ```
     */
    const Remove: React.ForwardRefExoticComponent<RemoveProps & React.RefAttributes<HTMLButtonElement>>;
}
/**
 * Props for Service.Policy.LatestCancellationInMinutes component
 */
export interface PolicyLatestCancellationInMinutesProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        cancellationPolicyEnabled: boolean;
        limitLatestCancellationEnabled: boolean;
        latestCancellationInMinutes: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays latestCancellationInMinutes as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.cancellationPolicy.latestCancellationInMinutes
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays raw number only (or null if disabled/undefined)
 * <Service.Policy.LatestCancellationInMinutes className="text-foreground" />
 * // Renders: <span>30</span> or null
 * ```
 *
 * @example
 * ```tsx
 * // Consumer adds formatting logic via asChild
 * <Service.Policy.LatestCancellationInMinutes asChild>
 *   {({ cancellationPolicyEnabled, limitLatestCancellationEnabled, latestCancellationInMinutes }) => (
 *     cancellationPolicyEnabled ? (
 *       <div className="policy-card">
 *         {limitLatestCancellationEnabled
 *           ? `Cancel up to ${latestCancellationInMinutes} minutes before`
 *           : 'Free cancellation anytime'
 *         }
 *       </div>
 *     ) : null
 *   )}
 * </Service.Policy.LatestCancellationInMinutes>
 * ```
 */
export declare const PolicyLatestCancellationInMinutes: React.ForwardRefExoticComponent<PolicyLatestCancellationInMinutesProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Policy.LatestBookingInMinutes component
 */
export interface PolicyLatestBookingInMinutesProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        latestBookingPolicyEnabled: boolean;
        latestBookingInMinutes: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays latestBookingInMinutes as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.limitLateBookingPolicy.latestBookingInMinutes
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Policy.LatestBookingInMinutes className="text-foreground" />
 * ```
 */
export declare const PolicyLatestBookingInMinutes: React.ForwardRefExoticComponent<PolicyLatestBookingInMinutesProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Policy.EarliestBookingInMinutes component
 */
export interface PolicyEarliestBookingInMinutesProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        earliestBookingPolicyEnabled: boolean;
        earliestBookingInMinutes: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays earliestBookingInMinutes as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.limitEarlyBookingPolicy.earliestBookingInMinutes
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Policy.EarliestBookingInMinutes className="text-foreground" />
 * ```
 */
export declare const PolicyEarliestBookingInMinutes: React.ForwardRefExoticComponent<PolicyEarliestBookingInMinutesProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Policy.LatestRescheduleInMinutes component
 */
export interface PolicyLatestRescheduleInMinutesProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        reschedulePolicyEnabled: boolean;
        limitLatestRescheduleEnabled: boolean;
        latestRescheduleInMinutes: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays latestRescheduleInMinutes as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.reschedulePolicy.latestRescheduleInMinutes
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Policy.LatestRescheduleInMinutes className="text-foreground" />
 * ```
 */
export declare const PolicyLatestRescheduleInMinutes: React.ForwardRefExoticComponent<PolicyLatestRescheduleInMinutesProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Policy.WaitlistCapacity component
 */
export interface PolicyWaitlistCapacityProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        waitlistPolicyEnabled: boolean;
        waitlistCapacity: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays waitlist capacity as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.waitlistPolicy.capacity
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Policy.WaitlistCapacity className="text-foreground" />
 * ```
 */
export declare const PolicyWaitlistCapacity: React.ForwardRefExoticComponent<PolicyWaitlistCapacityProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Policy.BookAfterStartEnabled component
 */
export interface PolicyBookAfterStartEnabledProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        bookAfterStartEnabled: boolean;
    }>;
    className?: string;
}
/**
 * Displays whether booking after start is enabled.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.bookAfterStartPolicy.enabled
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Policy.BookAfterStartEnabled className="text-foreground" />
 * ```
 */
export declare const PolicyBookAfterStartEnabled: React.ForwardRefExoticComponent<PolicyBookAfterStartEnabledProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Policy.MaxParticipantsPerBooking component
 */
export interface PolicyMaxParticipantsPerBookingProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        maxParticipantsPerBooking: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays maximum participants per booking as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * Maps to: bookingPolicy.participantsPolicy.maxParticipantsPerBooking
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.Policy.MaxParticipantsPerBooking className="text-foreground" />
 * ```
 */
export declare const PolicyMaxParticipantsPerBooking: React.ForwardRefExoticComponent<PolicyMaxParticipantsPerBookingProps & React.RefAttributes<HTMLElement>>;
/**
 * Policy namespace containing all policy sub-components
 */
export declare namespace Policy {
    const LatestCancellationInMinutes: React.ForwardRefExoticComponent<PolicyLatestCancellationInMinutesProps & React.RefAttributes<HTMLElement>>;
    const LatestBookingInMinutes: React.ForwardRefExoticComponent<PolicyLatestBookingInMinutesProps & React.RefAttributes<HTMLElement>>;
    const EarliestBookingInMinutes: React.ForwardRefExoticComponent<PolicyEarliestBookingInMinutesProps & React.RefAttributes<HTMLElement>>;
    const LatestRescheduleInMinutes: React.ForwardRefExoticComponent<PolicyLatestRescheduleInMinutesProps & React.RefAttributes<HTMLElement>>;
    const WaitlistCapacity: React.ForwardRefExoticComponent<PolicyWaitlistCapacityProps & React.RefAttributes<HTMLElement>>;
    const BookAfterStartEnabled: React.ForwardRefExoticComponent<PolicyBookAfterStartEnabledProps & React.RefAttributes<HTMLElement>>;
    const MaxParticipantsPerBooking: React.ForwardRefExoticComponent<PolicyMaxParticipantsPerBookingProps & React.RefAttributes<HTMLElement>>;
}
/**
 * Props for Service.Schedule.FirstSessionStart component
 */
export interface ScheduleFirstSessionStartProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        firstSessionStart: Date | undefined;
    }>;
    className?: string;
}
/**
 * Displays the first session start date as a raw Date value.
 * Headless component - contains zero formatting logic.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays ISO string
 * <Service.Schedule.FirstSessionStart className="text-foreground" />
 *
 * // With custom formatting
 * <Service.Schedule.FirstSessionStart asChild>
 *   {({ firstSessionStart }) => (
 *     firstSessionStart ? (
 *       <span>Starts: {formatDate(firstSessionStart)}</span>
 *     ) : null
 *   )}
 * </Service.Schedule.FirstSessionStart>
 * ```
 */
export declare const ScheduleFirstSessionStart: React.ForwardRefExoticComponent<ScheduleFirstSessionStartProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Schedule.LastSessionEnd component
 */
export interface ScheduleLastSessionEndProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        lastSessionEnd: Date | undefined;
    }>;
    className?: string;
}
/**
 * Displays the last session end date as a raw Date value.
 * Headless component - contains zero formatting logic.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays ISO string
 * <Service.Schedule.LastSessionEnd className="text-foreground" />
 *
 * // With custom formatting
 * <Service.Schedule.LastSessionEnd asChild>
 *   {({ lastSessionEnd }) => (
 *     lastSessionEnd ? (
 *       <span>Ends: {formatDate(lastSessionEnd)}</span>
 *     ) : null
 *   )}
 * </Service.Schedule.LastSessionEnd>
 * ```
 */
export declare const ScheduleLastSessionEnd: React.ForwardRefExoticComponent<ScheduleLastSessionEndProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service.Schedule.SessionDuration component
 */
export interface ScheduleSessionDurationProps {
    asChild?: boolean;
    children?: AsChildChildren<{
        sessionDuration: number | undefined;
    }>;
    className?: string;
}
/**
 * Displays the session duration in minutes as a raw number.
 * Headless component - contains zero formatting logic.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays raw minutes
 * <Service.Schedule.SessionDuration className="text-foreground" />
 *
 * // With custom formatting
 * <Service.Schedule.SessionDuration asChild>
 *   {({ sessionDuration }) => (
 *     sessionDuration ? (
 *       <span>{formatDuration(sessionDuration)}</span>
 *     ) : null
 *   )}
 * </Service.Schedule.SessionDuration>
 * ```
 */
export declare const ScheduleSessionDuration: React.ForwardRefExoticComponent<ScheduleSessionDurationProps & React.RefAttributes<HTMLElement>>;
/**
 * Schedule namespace containing all schedule sub-components
 */
export declare namespace Schedule {
    const FirstSessionStart: React.ForwardRefExoticComponent<ScheduleFirstSessionStartProps & React.RefAttributes<HTMLElement>>;
    const LastSessionEnd: React.ForwardRefExoticComponent<ScheduleLastSessionEndProps & React.RefAttributes<HTMLElement>>;
    const SessionDuration: React.ForwardRefExoticComponent<ScheduleSessionDurationProps & React.RefAttributes<HTMLElement>>;
}
/**
 * Props for Service DefaultCapacity component
 */
export interface DefaultCapacityProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        defaultCapacity: number;
        isClassOrCourse: boolean;
    }>;
}
/**
 * Displays the service default capacity with customizable rendering.
 * Only renders for CLASS or COURSE service types.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage
 * <Service.DefaultCapacity className="text-sm" />
 *
 * // asChild with custom component
 * <Service.DefaultCapacity asChild>
 *   {({ defaultCapacity, isClassOrCourse }) => isClassOrCourse ? (
 *     <span>Max participants: {defaultCapacity}</span>
 *   ) : null}
 * </Service.DefaultCapacity>
 * ```
 */
export declare const DefaultCapacity: React.ForwardRefExoticComponent<DefaultCapacityProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for Service AvailableOnline component
 */
export interface AvailableOnlineProps extends Omit<React.HTMLAttributes<HTMLElement>, 'children'> {
    asChild?: boolean;
    children?: AsChildChildren<{
        availableOnline: boolean;
    }>;
    label?: string;
}
/**
 * Displays whether the service is available online (conferencing enabled).
 * Headless component - contains zero formatting logic.
 *
 * Maps to: service.conferencing.enabled
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - displays label only when available online
 * <Service.AvailableOnline label="Available Online" className="text-sm text-secondary-foreground" />
 * ```
 *
 * @example
 * ```tsx
 * // asChild with custom component
 * <Service.AvailableOnline asChild>
 *   {({ availableOnline }) => availableOnline ? (
 *     <span className="badge bg-green-500">Online</span>
 *   ) : null}
 * </Service.AvailableOnline>
 * ```
 */
export declare const AvailableOnline: React.ForwardRefExoticComponent<AvailableOnlineProps & React.RefAttributes<HTMLElement>>;
export declare const Locations: typeof LocationsBase & {
    List: typeof LocationsList;
    LocationRepeater: typeof LocationRepeater;
};
export declare const StaffMembers: typeof StaffMembersBase & {
    List: typeof StaffMembersList;
    StaffMemberRepeater: typeof StaffMemberRepeater;
};
export declare const MainMedia: React.ForwardRefExoticComponent<ServiceMediaModule.MainProps & React.RefAttributes<HTMLImageElement>>;
export declare const CoverMedia: React.ForwardRefExoticComponent<ServiceMediaModule.CoverProps & React.RefAttributes<HTMLImageElement>>;
export type { MainProps as MainMediaProps } from './ServiceMedia.js';
export type { CoverProps as CoverMediaProps } from './ServiceMedia.js';
