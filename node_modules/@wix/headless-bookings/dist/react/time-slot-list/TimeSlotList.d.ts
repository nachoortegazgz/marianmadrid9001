/**
 * TimeSlotList - High-level component for displaying time slot list information
 * Provides components for displaying timezone and time slot data
 */
import React from 'react';
import { ListVariant } from '@wix/headless-components/react';
import { AsChildChildren } from '@wix/headless-utils/react';
import type { TimeSlotListServiceConfig } from '../../services/time-slot-list/time-slot-list.def.js';
export type { StartDateProps as TimeSlotStartDateProps, EndDateProps as TimeSlotEndDateProps, DurationProps as TimeSlotDurationProps, SelectProps as TimeSlotSelectProps, LocationNameProps as TimeSlotLocationNameProps, StaffMembersProps as TimeSlotStaffMembersProps, StaffMemberRepeaterProps as TimeSlotStaffMemberRepeaterProps, StaffMemberNameProps as TimeSlotStaffMemberNameProps, SelectStaffMemberProps as TimeSlotSelectStaffMemberProps, ClearStaffSelectionProps as TimeSlotClearStaffSelectionProps, } from './TimeSlot.js';
/**
 * TimeSlot with required id field for GenericList
 */
export declare const TestIds: {
    readonly timeSlotListRoot: "time-slot-list-root";
    readonly timeSlotListTimeSlots: "time-slot-list-time-slots";
    readonly timeSlotListTimeSlot: "time-slot-list-time-slot";
    readonly timeSlotListTimezone: "time-slot-list-timezone";
    readonly timeSlotListLoadMore: "time-slot-list-load-more";
    readonly timeSlotListContinue: "time-slot-list-continue";
    readonly timeSlotListDateRangeInput: "time-slot-list-date-range-input";
};
/**
 * Render props for TimeSlotList.Timezone component
 */
export interface TimezoneRenderProps {
    /** Raw IANA timezone string (e.g., "America/New_York") */
    timezone: string | undefined;
    /** User-friendly display name (e.g., "Eastern Standard Time") */
    displayName: string;
}
/**
 * Props for TimeSlotList.Timezone component
 */
export interface TimezoneProps {
    asChild?: boolean;
    children?: AsChildChildren<TimezoneRenderProps>;
    className?: string;
}
/**
 * Displays the current timezone with customizable rendering.
 *
 * @component
 * @example
 * ```tsx
 * // Default usage - shows display name
 * <TimeSlotList.Timezone className="text-sm text-gray-600" />
 *
 * // asChild with render function for custom formatting
 * <TimeSlotList.Timezone asChild>
 *   {({ timezone, displayName }) => (
 *     <span className="text-sm">
 *       {displayName}  // "Eastern Standard Time"
 *     </span>
 *   )}
 * </TimeSlotList.Timezone>
 * ```
 */
export declare const Timezone: React.ForwardRefExoticComponent<TimezoneProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for TimeSlotList.Root component
 */
export interface RootProps {
    children: React.ReactNode;
    config: TimeSlotListServiceConfig;
    className?: string;
    variant?: ListVariant;
}
/**
 * Root component that provides the TimeSlotList service context.
 *
 * @order 1
 * @component
 * @example
 * ```tsx
 * import { TimeSlotList } from '@wix/bookings/components';
 *
 * function TimeSlotsPage({ config }) {
 *   return (
 *     <TimeSlotList.Root config={config}>
 *       <TimeSlotList.TimeSlots>
 *         <TimeSlotList.TimeSlotRepeater>
 *           {children}
 *         </TimeSlotList.TimeSlotRepeater>
 *       </TimeSlotList.TimeSlots>
 *     </TimeSlotList.Root>
 *   );
 * }
 * ```
 */
export declare const Root: React.ForwardRefExoticComponent<RootProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for TimeSlotList.TimeSlots component
 */
export interface TimeSlotsProps {
    children: React.ReactNode;
    /** Content to display when no time slots are available */
    emptyState?: React.ReactNode;
    /** Content to display while time slots are loading */
    loadingState?: React.ReactNode;
    /** Content to display when an error occurs while loading time slots */
    errorState?: React.ReactNode;
    className?: string;
}
/**
 * Container for the time slots list with empty state and loading state support.
 * Wraps GenericList.Items. Follows List Container Level pattern.
 *
 * @component
 * @example
 * ```tsx
 * <TimeSlotList.TimeSlots
 *   emptyState={<div>No slots available</div>}
 *   loadingState={<div>Loading...</div>}
 * >
 *   <TimeSlotList.TimeSlotRepeater>
 *     {children}
 *   </TimeSlotList.TimeSlotRepeater>
 * </TimeSlotList.TimeSlots>
 * ```
 */
export declare const TimeSlots: React.ForwardRefExoticComponent<TimeSlotsProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for TimeSlotList.TimeSlotRepeater component
 */
export interface TimeSlotRepeaterProps {
    children: React.ReactNode;
}
export declare const TimeSlotRepeater: React.ForwardRefExoticComponent<TimeSlotRepeaterProps & React.RefAttributes<HTMLElement>>;
/**
 * Props for TimeSlotList.Actions.LoadMore component
 */
export interface LoadMoreProps {
    /** Children content */
    children?: React.ReactNode;
    /** Whether to render as a child component */
    asChild?: boolean;
    /** CSS classes to apply to the button */
    className?: string;
    /** The label to display inside the button */
    label?: string;
    /** The loading state to display inside the button */
    loadingState?: React.ReactNode;
}
/**
 * Displays a button to load more time slots. Not rendered if no more slots are left to load.
 * Follows the architecture rules - does not support asChild as it's a simple trigger component.
 *
 * @component
 * @example
 * ```tsx
 * <TimeSlotList.Actions.LoadMore asChild>
 *   <button>Load More</button>
 * </TimeSlotList.Actions.LoadMore>
 * ```
 */
export declare const LoadMore: React.ForwardRefExoticComponent<LoadMoreProps & React.RefAttributes<HTMLButtonElement>>;
/**
 * Actions namespace for TimeSlotList
 */
export declare const Actions: {
    LoadMore: React.ForwardRefExoticComponent<LoadMoreProps & React.RefAttributes<HTMLButtonElement>>;
};
/**
 * Props for DateRange.Input component
 */
export interface DateRangeInputProps extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'children' | 'type' | 'value' | 'defaultValue'> {
    /** Whether to render as a child component */
    asChild?: boolean;
    /** Children for custom rendering */
    children?: AsChildChildren<{
        startDate: Date;
        endDate: Date;
        onChange: (start: Date, end: Date) => void;
        /** IANA timezone string for the time slots (e.g., "America/New_York") */
        timezone: string | undefined;
    }>;
}
/**
 * DateRange namespace for TimeSlotList date range filter
 */
export declare const DateRange: {
    Input: React.ForwardRefExoticComponent<DateRangeInputProps & React.RefAttributes<HTMLElement>>;
};
/**
 * TimeSlot namespace containing components for displaying individual time slot data.
 * These components must be used within TimeSlotList.TimeSlotRepeater.
 *
 * @example
 * ```tsx
 * <TimeSlotList.TimeSlotRepeater>
 *   <TimeSlot.StartDate />
 *   <TimeSlot.Duration />
 *   <TimeSlot.Actions.Select />
 *
 *   // Location selection - visible when slot is selected
 *   <TimeSlot.Locations>
 *     <TimeSlot.LocationRepeater>
 *       <TimeSlot.Location.Name />
 *       <TimeSlot.Location.Actions.Select />
 *     </TimeSlot.LocationRepeater>
 *   </TimeSlot.Locations>
 *
 *   // Staff member selection - visible when slot is selected
 *   <TimeSlot.StaffMembers>
 *     <TimeSlot.StaffMemberRepeater>
 *       <TimeSlot.StaffMember.Name />
 *       <TimeSlot.StaffMember.Actions.Select />
 *     </TimeSlot.StaffMemberRepeater>
 *   </TimeSlot.StaffMembers>
 * </TimeSlotList.TimeSlotRepeater>
 * ```
 */
export declare const TimeSlot: {
    StartDate: React.ForwardRefExoticComponent<import("./TimeSlot.js").StartDateProps & React.RefAttributes<HTMLElement>>;
    EndDate: React.ForwardRefExoticComponent<import("./TimeSlot.js").EndDateProps & React.RefAttributes<HTMLElement>>;
    Duration: React.ForwardRefExoticComponent<import("./TimeSlot.js").DurationProps & React.RefAttributes<HTMLElement>>;
    Actions: {
        Select: React.ForwardRefExoticComponent<import("./TimeSlot.js").SelectProps & React.RefAttributes<HTMLButtonElement>>;
        ClearStaffSelection: React.ForwardRefExoticComponent<import("./TimeSlot.js").ClearStaffSelectionProps & React.RefAttributes<HTMLButtonElement>>;
    };
    Location: {
        Name: React.ForwardRefExoticComponent<import("./TimeSlot.js").LocationNameProps & React.RefAttributes<HTMLElement>>;
    };
    StaffMembers: React.ForwardRefExoticComponent<import("./TimeSlot.js").StaffMembersProps & React.RefAttributes<HTMLElement>>;
    StaffMemberRepeater: React.ForwardRefExoticComponent<import("./TimeSlot.js").StaffMemberRepeaterProps & React.RefAttributes<HTMLElement>>;
    StaffMember: {
        Name: React.ForwardRefExoticComponent<import("./TimeSlot.js").StaffMemberNameProps & React.RefAttributes<HTMLElement>>;
        Actions: {
            Select: React.ForwardRefExoticComponent<import("./TimeSlot.js").SelectStaffMemberProps & React.RefAttributes<HTMLButtonElement>>;
        };
    };
};
