/**
 * Location List Service Definition
 * Defines types and service interface for managing location lists
 *
 * @module Services/LocationList/Definition
 */
import type { Signal, ReadOnlySignal } from '@wix/services-definitions/core-services/signals';
import { LocationTypeEnumLocationType as LocationType, type Service } from '@wix/auto_sdk_bookings_services';
/**
 * Location type from the SDK
 * Represents a service location (BUSINESS, CUSTOM, or CUSTOMER type)
 */
export type Location = NonNullable<Service['locations']>[number];
/**
 * Actions interface for location list management
 */
export interface LocationListActions {
    /** Select a location (updates BookingService.location if available) */
    select: (location: Location) => void;
}
/**
 * API interface for the LocationList service
 */
export interface LocationListServiceAPI {
    /** Reactive signal containing the raw locations array */
    locations: Signal<Location[]>;
    /** Reactive signal containing the display locations (processed for UI) */
    displayLocations: ReadOnlySignal<Location[]>;
    /** Whether custom locations exist */
    hasCustomLocations: ReadOnlySignal<boolean>;
    /** Whether customer locations exist */
    hasCustomerLocations: ReadOnlySignal<boolean>;
    /** Reactive signal indicating if locations are loading */
    isLoading: Signal<boolean>;
    /** Reactive signal containing any error message */
    error: Signal<string | null>;
    /** Computed signal for selected location (from BookingService, null if standalone) */
    selectedLocation: ReadOnlySignal<Location | null>;
    /** Custom label for CUSTOM location type (used as fallback when no name exists) */
    customLocationLabel: ReadOnlySignal<string | undefined>;
    /** Custom label for CUSTOMER location type (used as fallback when no name exists) */
    customerLocationLabel: ReadOnlySignal<string | undefined>;
    /** Actions for location management */
    actions: LocationListActions;
}
/**
 * Service definition for LocationList
 */
export declare const LocationListServiceDefinition: string & {
    __api: LocationListServiceAPI;
    __config: {};
    isServiceDefinition?: boolean;
} & LocationListServiceAPI;
export { LocationType };
