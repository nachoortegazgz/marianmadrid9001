"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ThumbnailItem = exports.ThumbnailRepeater = exports.Thumbnails = exports.Indicator = exports.Viewport = exports.Previous = exports.Next = exports.Root = exports.TestIds = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
/**
 * @fileoverview MediaGallery Primitive Components
 *
 * This module provides unstyled, composable components for building media galleries.
 * These components follow the Radix UI primitive pattern, offering:
 *
 * - **Unstyled**: No default styling, only functional behavior
 * - **Composable**: Support for the `asChild` pattern for flexible DOM structure
 * - **Accessible**: Built-in keyboard navigation and ARIA attributes
 * - **Flexible**: Render props pattern for maximum customization
 *
 * ## Architecture
 *
 * These components are the **primitive layer** that sits between:
 * 1. **Core components** (pure logic, no DOM)
 * 2. **Styled components** (project-specific styling)
 *
 * ## Usage
 *
 * ```tsx
 * import { MediaGallery } from '@wix/headless-media/react';
 *
 * function ProductGallery({ productMedia }) {
 *   return (
 *     <MediaGallery.Root mediaGalleryServiceConfig={{ media: productMedia }}>
 *       <MediaGallery.Viewport />
 *       <MediaGallery.Previous />
 *       <MediaGallery.Next />
 *       <MediaGallery.Indicator />
 *       <MediaGallery.Thumbnails>
 *         <MediaGallery.ThumbnailRepeater>
 *           <MediaGallery.ThumbnailItem />
 *         </MediaGallery.ThumbnailRepeater>
 *       </MediaGallery.Thumbnails>
 *     </MediaGallery.Root>
 *   );
 * }
 * ```
 *
 * @module MediaGallery
 */
const MediaGallery_js_1 = require("./core/MediaGallery.js");
const react_1 = __importStar(require("react"));
const react_2 = require("@wix/headless-utils/react");
const react_slot_1 = require("@radix-ui/react-slot");
const WixMediaImage_js_1 = require("./WixMediaImage.js");
// Components that render actual DOM elements get test IDs on their rendered elements
// Components that only provide context/logic don't introduce new DOM elements
var TestIds;
(function (TestIds) {
    TestIds["mediaGalleryRoot"] = "media-gallery-root";
    TestIds["mediaGalleryNext"] = "media-gallery-next";
    TestIds["mediaGalleryPrevious"] = "media-gallery-previous";
    TestIds["mediaGalleryViewport"] = "media-gallery-viewport";
    TestIds["mediaGalleryIndicator"] = "media-gallery-indicator";
    TestIds["mediaGalleryThumbnailItem"] = "media-gallery-thumbnail-item";
})(TestIds || (exports.TestIds = TestIds = {}));
/**
 * Root component that provides media gallery service context to its children.
 * This is a primitive wrapper around the core Root component that maintains
 * the same API while providing a foundation for composition patterns.
 *
 * @component
 * @example
 * ```tsx
 * import { MediaGallery } from '@wix/headless-media/react';
 *
 * function ProductGallery({ productMedia }) {
 *   return (
 *     <MediaGallery.Root mediaGalleryServiceConfig={{ media: productMedia }}>
 *       <MediaGallery.Viewport className="main-viewer" />
 *       <MediaGallery.Thumbnails>
 *         <MediaGallery.ThumbnailRepeater>
 *           <MediaGallery.ThumbnailItem />
 *         </MediaGallery.ThumbnailRepeater>
 *       </MediaGallery.Thumbnails>
 *     </MediaGallery.Root>
 *   );
 * }
 * ```
 */
const Root = ({ children, mediaGalleryServiceConfig }) => {
    return ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.Root, { mediaGalleryServiceConfig: mediaGalleryServiceConfig, "data-testid": TestIds.mediaGalleryRoot, children: children }));
};
exports.Root = Root;
/**
 * Next button component that navigates to the next media item.
 * Supports the asChild pattern for flexible composition.
 *
 * @component
 * @example
 * ```tsx
 * // Default button
 * <MediaGallery.Next />
 *
 * // Custom button with asChild
 * <MediaGallery.Next asChild>
 *   <button className="custom-next-btn">
 *     <ChevronRightIcon />
 *   </button>
 * </MediaGallery.Next>
 *
 * // With custom content
 * <MediaGallery.Next className="nav-button">
 *   Next Image →
 * </MediaGallery.Next>
 * ```
 */
exports.Next = react_1.default.forwardRef(({ children, ...props }, ref) => {
    const Comp = props.asChild ? react_slot_1.Slot : 'button';
    return ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.Next, { children: ({ next, canGoNext }) => ((0, jsx_runtime_1.jsx)(Comp, { ref: ref, onClick: next, disabled: !canGoNext, "data-testid": TestIds.mediaGalleryNext, ...props, children: children })) }));
});
/**
 * Previous button component that navigates to the previous media item.
 * Supports the asChild pattern for flexible composition.
 *
 * @component
 * @example
 * ```tsx
 * // Default button
 * <MediaGallery.Previous />
 *
 * // Custom button with asChild
 * <MediaGallery.Previous asChild>
 *   <button className="custom-prev-btn">
 *     <ChevronLeftIcon />
 *   </button>
 * </MediaGallery.Previous>
 *
 * // With custom content
 * <MediaGallery.Previous className="nav-button">
 *   ← Previous Image
 * </MediaGallery.Previous>
 * ```
 */
exports.Previous = react_1.default.forwardRef(({ children, ...props }, ref) => {
    const Comp = props.asChild ? react_slot_1.Slot : 'button';
    return ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.Previous, { children: ({ previous, canGoPrevious }) => ((0, jsx_runtime_1.jsx)(Comp, { ref: ref, onClick: previous, disabled: !canGoPrevious, "data-testid": TestIds.mediaGalleryPrevious, ...props, children: children })) }));
});
/**
 * Viewport component that displays the currently selected media item.
 * Automatically renders the active media using WixMediaImage for optimization.
 * Supports the asChild pattern and custom empty states.
 *
 * @component
 * @example
 * ```tsx
 * // Default viewport
 * <MediaGallery.Viewport />
 *
 * // With custom styling
 * <MediaGallery.Viewport className="rounded-lg border" />
 *
 * // With custom empty state
 * <MediaGallery.Viewport
 *   emptyState={<div>No images available</div>}
 * />
 *
 * // Using asChild for custom wrapper
 * <MediaGallery.Viewport asChild>
 *   <div className="custom-viewport-wrapper">
 *     Content will be rendered here
 *   </div>
 * </MediaGallery.Viewport>
 *
 * // With completely custom children
 * <MediaGallery.Viewport>
 *   <CustomImageComponent />
 * </MediaGallery.Viewport>
 * ```
 */
exports.Viewport = react_1.default.forwardRef(({ children, asChild, emptyState, ...otherProps }, ref) => {
    return ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.Viewport, { children: ({ src, alt }) => {
            const defaultContent = children ??
                (src ? ((0, jsx_runtime_1.jsx)(WixMediaImage_js_1.WixMediaImage, { media: { image: src }, alt: alt })) : ((emptyState ?? (0, jsx_runtime_1.jsx)("div", { children: "No image" }))));
            return ((0, jsx_runtime_1.jsx)(react_2.AsChildSlot, { ref: ref, asChild: asChild, "data-src": src, "data-alt": alt, "data-testid": TestIds.mediaGalleryViewport, customElement: children, customElementProps: { src, alt }, content: defaultContent, ...otherProps, children: (0, jsx_runtime_1.jsx)("div", { children: react_1.default.isValidElement(defaultContent) ? defaultContent : null }) }));
        } }));
});
/**
 * Indicator component that displays the current media position (e.g., "1 / 5").
 * Automatically tracks the current and total media count.
 * Supports the asChild pattern for flexible styling.
 *
 * @component
 * @example
 * ```tsx
 * // Default indicator
 * <MediaGallery.Indicator />
 *
 * // With custom styling
 * <MediaGallery.Indicator className="text-sm opacity-80" />
 *
 * // Using asChild for custom wrapper
 * <MediaGallery.Indicator asChild>
 *   <span className="badge">
 *    "1 / 5" will be rendered here
 *   </span>
 * </MediaGallery.Indicator>
 *
 * // With completely custom children
 * <MediaGallery.Indicator>
 *   <CustomIndicatorComponent />
 * </MediaGallery.Indicator>
 * ```
 */
exports.Indicator = react_1.default.forwardRef(({ children, asChild, ...props }, ref) => {
    return ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.Indicator, { children: ({ current, total }) => {
            const Comp = asChild ? react_slot_1.Slot : 'div';
            return ((0, jsx_runtime_1.jsx)(Comp, { ref: ref, "data-current": current, "data-total": total, "data-testid": TestIds.mediaGalleryIndicator, ...props, children: children ?? ((0, jsx_runtime_1.jsxs)("div", { children: [current, " / ", total] })) }));
        } }));
});
const ThumbnailsContext = (0, react_1.createContext)(null);
const ThumbnailItemContext = (0, react_1.createContext)(null);
/**
 * Thumbnails container component that provides thumbnail context to its children.
 * Only renders when there are multiple media items to display.
 *
 * @component
 * @example
 * ```tsx
 * <MediaGallery.Thumbnails>
 *   <MediaGallery.ThumbnailRepeater>
 *     <MediaGallery.ThumbnailItem />
 *   </MediaGallery.ThumbnailRepeater>
 * </MediaGallery.Thumbnails>
 * ```
 */
const Thumbnails = ({ children }) => ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.ThumbnailList, { children: ({ items }) => ((0, jsx_runtime_1.jsx)(ThumbnailsContext.Provider, { value: { items: items }, children: children })) }));
exports.Thumbnails = Thumbnails;
/**
 * ThumbnailRepeater component that renders a template for each media item.
 * Provides index context to each thumbnail item. Only renders when there are
 * multiple media items available.
 *
 * @component
 * @example
 * ```tsx
 * <MediaGallery.ThumbnailRepeater>
 *   <MediaGallery.ThumbnailItem className="thumbnail" />
 * </MediaGallery.ThumbnailRepeater>
 * ```
 */
const ThumbnailRepeater = ({ children }) => {
    const ctx = (0, react_1.useContext)(ThumbnailsContext);
    if (!ctx || !ctx.items || ctx.items.length <= 1)
        return null;
    return ((0, jsx_runtime_1.jsx)(jsx_runtime_1.Fragment, { children: ctx.items.map((_, i) => ((0, jsx_runtime_1.jsx)(ThumbnailItemContext.Provider, { value: { index: i }, children: children }, i))) }));
};
exports.ThumbnailRepeater = ThumbnailRepeater;
/**
 * ThumbnailItem component that renders an individual thumbnail for media selection.
 * Automatically handles selection state, click events, and renders the thumbnail image.
 * Must be used within a ThumbnailRepeater context.
 *
 * @component
 * @example
 * ```tsx
 * // Default thumbnail item
 * <MediaGallery.ThumbnailItem />
 *
 * // With custom styling
 * <MediaGallery.ThumbnailItem className="border rounded-lg hover:shadow-lg" />
 *
 * // With custom empty state
 * <MediaGallery.ThumbnailItem
 *   emptyState={<div className="placeholder">No preview</div>}
 * />
 *
 * // Using asChild for custom wrapper
 * <MediaGallery.ThumbnailItem asChild>
 *   <button className="custom-thumbnail-button">
 *    Thumbnail content will be rendered here
 *   </button>
 * </MediaGallery.ThumbnailItem>
 *
 * // With completely custom children
 * <MediaGallery.ThumbnailItem>
 *   <CustomThumbnailComponent />
 * </MediaGallery.ThumbnailItem>
 * ```
 */
exports.ThumbnailItem = react_1.default.forwardRef(({ children, asChild, emptyState, ...otherProps }, ref) => {
    const itemCtx = (0, react_1.useContext)(ThumbnailItemContext);
    if (!itemCtx)
        return null;
    const { index } = itemCtx;
    return ((0, jsx_runtime_1.jsx)(MediaGallery_js_1.ThumbnailItem, { index: index, children: ({ src, isActive, select, alt }) => {
            const defaultContent = children ??
                (src ? ((0, jsx_runtime_1.jsx)(WixMediaImage_js_1.WixMediaImage, { media: { image: src }, alt: alt })) : ((emptyState ?? (0, jsx_runtime_1.jsx)("div", { children: "No image" }))));
            return ((0, jsx_runtime_1.jsx)(react_2.AsChildSlot, { ref: ref, asChild: asChild, onClick: select, "data-active": isActive, "data-src": src, "data-alt": alt, "data-index": index, "data-available": true, "data-testid": TestIds.mediaGalleryThumbnailItem, customElement: children, customElementProps: { src, alt, isActive, select, index }, content: defaultContent, ...otherProps, children: (0, jsx_runtime_1.jsx)("div", { children: react_1.default.isValidElement(defaultContent) ? defaultContent : null }) }));
        } }));
});
