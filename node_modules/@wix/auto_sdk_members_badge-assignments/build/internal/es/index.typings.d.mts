import * as _wix_sdk_types from '@wix/sdk-types';
import { QuerySpec, Query, NonNullablePaths } from '@wix/sdk-types';

/**
 * A badge assignment represents the relationship between a badge and a member.
 * When a badge assignment is created, the specified member receives the badge
 * and any associated permissions or privileges.
 */
interface BadgeAssignment {
    /**
     * Badge assignment ID.
     * @format GUID
     * @readonly
     */
    _id?: string | null;
    /**
     * ID of the badge being assigned to the member. Call the [Badges API](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badges-v4/introduction) to retrieve available badges.
     * @format GUID
     */
    badgeId?: string | null;
    /**
     * ID of the member receiving the badge assignment. Call the [Members API](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/members/introduction) to retrieve member information.
     * @format GUID
     */
    memberId?: string | null;
    /**
     * Date and time the badge assignment was created.
     * @readonly
     */
    _createdDate?: Date | null;
    /**
     * Date and time the badge assignment was updated.
     * @readonly
     */
    _updatedDate?: Date | null;
    /**
     * Tags allow you to classify ticket reservations.
     *
     * Learn more about [tags](https://dev.wix.com/docs/rest/business-management/tags/introduction).
     */
    tags?: Tags;
    /**
     * Extended fields allow you to add custom properties beyond the standard reservation data.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions).
     */
    extendedFields?: ExtendedFields;
}
/**
 * Common object for tags.
 * Should be use as in this example:
 * message Foo {
 * option (.wix.api.decomposite_of) = "wix.common.tags.Foo";
 * string id = 1;
 * ...
 * Tags tags = 5
 * }
 *
 * example of taggable entity
 * {
 * id: "123"
 * tags: {
 * tags: {
 * tag_ids:["11","22"]
 * },
 * private_tags: {
 * tag_ids: ["33", "44"]
 * }
 * }
 * }
 */
interface Tags {
    /** Tags that require an additional permission in order to access them, normally not given to site members or visitors. */
    privateTags?: TagList;
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    tags?: TagList;
}
interface TagList {
    /**
     * List of tag IDs
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface TagsModified {
    /** Updated function. */
    badgeAssignment?: BadgeAssignment;
    /** Tags that were assigned to the Function. */
    assignedTags?: Tags;
    /** Tags that were unassigned from the Function. */
    unassignedTags?: Tags;
}
interface CreateBadgeAssignmentRequest {
    /** Badge assignment to create. */
    badgeAssignment: BadgeAssignment;
}
interface CreateBadgeAssignmentResponse {
    /** Created badge assignment. */
    badgeAssignment?: BadgeAssignment;
}
interface BulkCreateBadgeAssignmentsRequest {
    /**
     * List of badge assignments to create.
     * @minSize 1
     * @maxSize 100
     */
    badgeAssignments: BadgeAssignment[];
    /** Whether to return the created badge assignments in the response. Set to `true` to receive full badge assignment objects. */
    returnEntity?: boolean;
}
interface BulkCreateBadgeAssignmentsResponse {
    /**
     * Results of the bulk create operation.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkBadgeAssignmentResult[];
    /** Metadata about the bulk operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkBadgeAssignmentResult {
    /** Metadata regarding the specific single update operation */
    itemMetadata?: ItemMetadata;
    /** Only exists if `returnEntity` was set to true in the request */
    item?: BadgeAssignment;
}
interface ItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    _id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface QueryBadgeAssignmentsRequest {
    /** Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
    /**
     * When `field_name` is a property of repeated field that is marked as `MATCH_ITEMS` and sort should be done by
     * a specific element from a collection, filter can/should be provided to ensure correct sort value is picked.
     *
     * If multiple filters are provided, they are combined with AND operator.
     *
     * Example:
     * Given we have document like {"id": "1", "nestedField": [{"price": 10, "region": "EU"}, {"price": 20, "region": "US"}]}
     * and `nestedField` is marked as `MATCH_ITEMS`, to ensure that sorting is done by correct region, filter should be
     * { fieldName: "nestedField.price", "select_items_by": [{"nestedField.region": "US"}] }
     * @internal
     * @maxSize 10
     */
    selectItemsBy?: Record<string, any>[] | null;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryBadgeAssignmentsResponse {
    /** List of badge assignments matching the query criteria. */
    badgeAssignments?: BadgeAssignment[];
    /** Paging metadata including cursors for pagination. */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface QueryBadgeAssignmentsLegacyRequest {
    queryV2?: QueryV2;
}
interface QueryV2 extends QueryV2PagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     * @maxSize 100
     */
    sort?: Sorting[];
    /**
     * Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.
     * @maxSize 100
     * @maxLength 1000
     */
    fields?: string[];
    /**
     * Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.
     * @maxSize 100
     * @maxLength 1000
     */
    fieldsets?: string[];
}
/** @oneof */
interface QueryV2PagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Paging {
    /**
     * Number of items to load.
     * @max 1000
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
interface QueryBadgeAssignmentsLegacyResponse {
    /** List of badge assignments matching the query criteria. */
    badgeAssignments?: BadgeAssignment[];
    pagingMetadataV2?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Flag that indicates the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
    /**
     * Indicates if there are more results after the current page.
     * If `true`, another page of results can be retrieved.
     * If `false`, this is the last page.
     * @internal
     */
    hasNext?: boolean | null;
}
interface GetMemberCountsPerBadgeLegacyRequest {
}
interface GetMemberCountsPerBadgeLegacyResponse {
    /** Badges and the amount of members who have each badge. */
    badgeMemberCounts?: BadgeMemberCount[];
}
interface BadgeMemberCount {
    /**
     * Badge ID.
     * @format GUID
     */
    badgeId?: string;
    /** Number of members who have the badge. */
    memberCount?: number;
}
interface DeleteBadgeAssignmentRequest {
    /**
     * ID of the badge assignment to delete.
     * @format GUID
     */
    badgeAssignmentId: string;
}
interface DeleteBadgeAssignmentResponse {
}
interface BulkDeleteBadgeAssignmentsRequest {
    /**
     * ID of the badge assignments to delete.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    badgeAssignmentIds: string[];
}
interface BulkDeleteBadgeAssignmentsResponse {
    /**
     * Results of the bulk delete operation.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkBadgeAssignmentResult[];
    /** Metadata about the bulk operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateBadgeAssignmentTagsRequest {
    /**
     * List of badge assignment IDs to update tags for.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    ids: string[];
    /** Tags to assign to the badge assignments. */
    assignTags: Tags;
    /** Tags to remove from the badge assignments. If a tag appears in both assign and unassign lists, it will be assigned. */
    unassignTags?: Tags;
}
interface BulkUpdateBadgeAssignmentTagsResponse {
    /**
     * Results of the bulk update operation.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateBadgeAssignmentTagsResult[];
    /** Metadata about the bulk operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateBadgeAssignmentTagsResult {
    /** Metadata about the specific single update operation. */
    itemMetadata?: ItemMetadata;
}
interface BulkUpdateBadgeAssignmentTagsByFilterRequest {
    /** Filter criteria to select which badge assignments to update. Use an empty filter to update all badge assignments. */
    filter: Record<string, any> | null;
    /** Tags to assign to the filtered badge assignments. */
    assignTags: Tags;
    /** Tags to remove from the filtered badge assignments. If a tag appears in both assign and unassign lists, it will be assigned. */
    unassignTags?: Tags;
}
interface BulkUpdateBadgeAssignmentTagsByFilterResponse {
    /**
     * Job ID for tracking the asynchronous bulk update operation.
     * @format GUID
     */
    jobId?: string;
}
interface UpdateExtendedFieldsRequest {
    /** ID of the entity to update. */
    _id?: string;
    /** Identifier for the app whose extended fields are being updated. */
    namespace?: string;
    /** Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured. */
    namespaceData?: Record<string, any> | null;
}
interface UpdateExtendedFieldsResponse {
    /** Updated BadgeAssignment. */
    badgeAssignment?: BadgeAssignment;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entity?: string;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntity?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntity?: string | null;
}
interface ActionEvent {
    body?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}
interface BaseEventMetadata {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface EventMetadata extends BaseEventMetadata {
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    _id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
    accountInfo?: AccountInfoMetadata;
}
interface AccountInfoMetadata {
    /** ID of the Wix account associated with the event */
    accountId: string;
    /** ID of the Wix site associated with the event. Only included when the event is tied to a specific site. */
    siteId?: string;
    /** ID of the parent Wix account. Only included when 'accountId' belongs to a child account. */
    parentAccountId?: string;
}
interface BadgeAssignmentCreatedEnvelope {
    entity: BadgeAssignment;
    metadata: EventMetadata;
}
/**
 * Triggered when a badge assignment is created.
 * @permissionScope Manage_Wix_Rentals_App_Forms_submissions
 * @permissionScopeId SCOPE.RENTALS.MANAGE
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Events
 * @permissionScopeId SCOPE.EVENTS.MANAGE-EVENTS
 * @permissionScope Manage Online Programs
 * @permissionScopeId SCOPE.CHALLENGES.MANAGE
 * @permissionScope Manage Badges
 * @permissionScopeId SCOPE.DC-MEMBERS.MANAGE-BADGES
 * @permissionScope Read Badges
 * @permissionScopeId SCOPE.DC-MEMBERS.READ-BADGES
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_READ
 * @webhook
 * @eventType wix.badges.v4.badge_assignment_created
 * @slug created
 */
declare function onBadgeAssignmentCreated(handler: (event: BadgeAssignmentCreatedEnvelope) => void | Promise<void>): void;
interface BadgeAssignmentDeletedEnvelope {
    entity: BadgeAssignment;
    metadata: EventMetadata;
}
/**
 * Triggered when a badge assignment is deleted.
 * @permissionScope Manage_Wix_Rentals_App_Forms_submissions
 * @permissionScopeId SCOPE.RENTALS.MANAGE
 * @permissionScope Manage Stores
 * @permissionScopeId SCOPE.STORES.MANAGE-STORES
 * @permissionScope Manage Events
 * @permissionScopeId SCOPE.EVENTS.MANAGE-EVENTS
 * @permissionScope Manage Online Programs
 * @permissionScopeId SCOPE.CHALLENGES.MANAGE
 * @permissionScope Manage Badges
 * @permissionScopeId SCOPE.DC-MEMBERS.MANAGE-BADGES
 * @permissionScope Read Badges
 * @permissionScopeId SCOPE.DC-MEMBERS.READ-BADGES
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_READ
 * @webhook
 * @eventType wix.badges.v4.badge_assignment_deleted
 * @slug deleted
 */
declare function onBadgeAssignmentDeleted(handler: (event: BadgeAssignmentDeletedEnvelope) => void | Promise<void>): void;
/**
 * Assigns a badge to a member.
 *
 * To assign multiple badges to multiple members, call [Bulk Create Badge Assignments](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/bulk-create-badge-assignments).
 * @param badgeAssignment - Badge assignment to create.
 * @public
 * @requiredField badgeAssignment
 * @requiredField badgeAssignment.badgeId
 * @requiredField badgeAssignment.memberId
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_CREATE
 * @applicableIdentity APP
 * @returns Created badge assignment.
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.CreateBadgeAssignment
 */
declare function createBadgeAssignment(badgeAssignment: NonNullablePaths<BadgeAssignment, `badgeId` | `memberId`, 0>): Promise<NonNullablePaths<BadgeAssignment, `tags.privateTags.tagIds`, 2>>;
/**
 * Assigns multiple badges to multiple members in a single request.
 *
 * To assign a single badge for a member, call [Create Badge Assignment](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/create-badge-assignment).
 * @param badgeAssignments - List of badge assignments to create.
 * @public
 * @requiredField badgeAssignments
 * @requiredField badgeAssignments.badgeId
 * @requiredField badgeAssignments.memberId
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_CREATE
 * @applicableIdentity APP
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.BulkCreateBadgeAssignments
 */
declare function bulkCreateBadgeAssignments(badgeAssignments: NonNullablePaths<BadgeAssignment, `badgeId` | `memberId`, 0>[], options?: BulkCreateBadgeAssignmentsOptions): Promise<NonNullablePaths<BulkCreateBadgeAssignmentsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkCreateBadgeAssignmentsOptions {
    /** Whether to return the created badge assignments in the response. Set to `true` to receive full badge assignment objects. */
    returnEntity?: boolean;
}
/**
 * Creates a query to retrieve a list of badge assignments.
 *
 * The `queryBadgeAssignments()` method builds a query to retrieve a list of badge assignments and returns a `BadgeAssignmentsQueryBuilder` object.
 *
 * The returned object contains the query definition, which is typically used to run the query using the `find()` method.
 *
 * You can refine the query by chaining `BadgeAssignmentsQueryBuilder` methods onto the query. `BadgeAssignmentsQueryBuilder` methods enable you to sort, filter, and control the results `queryBadgeAssignments()` returns.
 *
 * `queryBadgeAssignments()` runs with these `BadgeAssignmentsQueryBuilder` defaults, which you can override:
 *
 * - limit(50)
 * - descending("_createdDate")
 *
 * The methods that are chained to `queryBadgeAssignments()` are applied in the order they're called.
 * @public
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_READ
 * @applicableIdentity APP
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.QueryBadgeAssignments
 */
declare function queryBadgeAssignments(): BadgeAssignmentsQueryBuilder;
interface QueryCursorResult {
    cursors: Cursors;
    hasNext: () => boolean;
    hasPrev: () => boolean;
    length: number;
    pageSize: number;
}
interface BadgeAssignmentsQueryResult extends QueryCursorResult {
    items: BadgeAssignment[];
    query: BadgeAssignmentsQueryBuilder;
    next: () => Promise<BadgeAssignmentsQueryResult>;
    prev: () => Promise<BadgeAssignmentsQueryResult>;
}
interface BadgeAssignmentsQueryBuilder {
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    eq: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ne: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    ge: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    gt: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    le: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `value`.
     * @param value - Value to compare against.
     */
    lt: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    /** @param propertyName - Property whose value is compared with `string`.
     * @param string - String to compare against. Case-insensitive.
     */
    startsWith: (propertyName: 'badgeId' | 'memberId', value: string) => BadgeAssignmentsQueryBuilder;
    in: (propertyName: 'badgeId' | 'memberId', value: any) => BadgeAssignmentsQueryBuilder;
    exists: (propertyName: 'badgeId' | 'memberId', value: boolean) => BadgeAssignmentsQueryBuilder;
    /** @param propertyNames - Properties used in the sort. To sort by multiple properties, pass properties as additional arguments. */
    ascending: (...propertyNames: Array<'badgeId' | 'memberId'>) => BadgeAssignmentsQueryBuilder;
    /** @param limit - Number of items to return, which is also the `pageSize` of the results object. */
    limit: (limit: number) => BadgeAssignmentsQueryBuilder;
    /** @param cursor - A pointer to specific record */
    skipTo: (cursor: string) => BadgeAssignmentsQueryBuilder;
    find: () => Promise<BadgeAssignmentsQueryResult>;
}
/**
 * @hidden
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.QueryBadgeAssignments
 * @requiredField query
 */
declare function typedQueryBadgeAssignments(query: BadgeAssignmentQuery): Promise<NonNullablePaths<QueryBadgeAssignmentsResponse, `badgeAssignments`, 0>>;
interface BadgeAssignmentQuerySpec extends QuerySpec {
    paging: 'cursor';
    wql: [
        {
            fields: ['badgeId', 'memberId'];
            operators: '*';
            sort: 'ASC';
        }
    ];
}
type CommonQueryWithEntityContext = Query<BadgeAssignment, BadgeAssignmentQuerySpec>;
type BadgeAssignmentQuery = {
    /**
    Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`.
    */
    cursorPaging?: {
        /**
      Maximum number of items to return in the results.
      @max: 100
      */
        limit?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['limit'] | null;
        /**
      Pointer to the next or previous page in the list of results.
    
      Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
      Not relevant for the first request.
      @maxLength: 16000
      */
        cursor?: NonNullable<CommonQueryWithEntityContext['cursorPaging']>['cursor'] | null;
    };
    /**
    Filter object.
  
    Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).
    */
    filter?: CommonQueryWithEntityContext['filter'] | null;
    /**
    Sort object.
  
    Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).
    @maxSize: 5
    */
    sort?: {
        /**
      Name of the field to sort by.
      @maxLength: 512
      */
        fieldName?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['fieldName'];
        /**
      Sort order.
      */
        order?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['order'];
        /**
      When `field_name` is a property of repeated field that is marked as `MATCH_ITEMS` and sort should be done by
      a specific element from a collection, filter can/should be provided to ensure correct sort value is picked.
    
      If multiple filters are provided, they are combined with AND operator.
    
      Example:
      Given we have document like {"id": "1", "nestedField": [{"price": 10, "region": "EU"}, {"price": 20, "region": "US"}]}
      and `nestedField` is marked as `MATCH_ITEMS`, to ensure that sorting is done by correct region, filter should be
      { fieldName: "nestedField.price", "select_items_by": [{"nestedField.region": "US"}] }
      @internal: undefined,
      @maxSize: 10
      */
        selectItemsBy?: NonNullable<CommonQueryWithEntityContext['sort']>[number]['selectItemsBy'] | null;
    }[];
};
declare const utils: {
    query: _wix_sdk_types.QueryHelpers<BadgeAssignment, BadgeAssignmentQuerySpec, BadgeAssignmentQuery>;
};
/**
 * Removes a badge assignment, removing the badge from the specified member.
 *
 * When a badge assignment is deleted, the member loses any permissions or privileges associated with that badge.
 * This action can't be undone.
 *
 * To delete multiple assignments, call [Bulk Delete Badge Assignments](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/bulk-delete-badge-assignments).
 * @param badgeAssignmentId - ID of the badge assignment to delete.
 * @public
 * @requiredField badgeAssignmentId
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_DELETE
 * @applicableIdentity APP
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.DeleteBadgeAssignment
 */
declare function deleteBadgeAssignment(badgeAssignmentId: string): Promise<void>;
/**
 * Deletes multiple badge assignments, removing badges from the specified members.
 *
 * When a badge assignment is deleted, the member loses any permissions or privileges associated with that badge.
 * This action can't be undone.
 *
 * To delete a single assignment, call [Delete Badge Assignment](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badge-assignments/delete-badge-assignment).
 * @param badgeAssignmentIds - ID of the badge assignments to delete.
 * @public
 * @requiredField badgeAssignmentIds
 * @permissionId BADGE_ASSIGNMENTS.BADGE_ASSIGNMENT_DELETE
 * @applicableIdentity APP
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.BulkDeleteBadgeAssignments
 */
declare function bulkDeleteBadgeAssignments(badgeAssignmentIds: string[]): Promise<NonNullablePaths<BulkDeleteBadgeAssignmentsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
/**
 * Updates tags on multiple badge assignments.
 *
 * Allows adding or removing tags from multiple badge assignments in a single operation. Specify badge IDs and the tags to assign or unassign. If a tag appears in both assign and unassign lists, it will be assigned.
 *
 * Use this for immediate tag updates on a known set of badge assignments.
 * @param ids - List of badge assignment IDs to update tags for.
 * @public
 * @requiredField ids
 * @requiredField options.assignTags
 * @permissionId BADGE_ASSIGNMENTS.BADGE_UPDATE_TAGS
 * @applicableIdentity APP
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.BulkUpdateBadgeAssignmentTags
 */
declare function bulkUpdateBadgeAssignmentTags(ids: string[], options?: NonNullablePaths<BulkUpdateBadgeAssignmentTagsOptions, `assignTags`, 0>): Promise<NonNullablePaths<BulkUpdateBadgeAssignmentTagsResponse, `results` | `results.${number}.itemMetadata.originalIndex` | `results.${number}.itemMetadata.success` | `results.${number}.itemMetadata.error.code` | `results.${number}.itemMetadata.error.description` | `bulkActionMetadata.totalSuccesses` | `bulkActionMetadata.totalFailures` | `bulkActionMetadata.undetailedFailures`, 4>>;
interface BulkUpdateBadgeAssignmentTagsOptions {
    /** Tags to assign to the badge assignments. */
    assignTags: Tags;
    /** Tags to remove from the badge assignments. If a tag appears in both assign and unassign lists, it will be assigned. */
    unassignTags?: Tags;
}
/**
 * Updates tags on multiple badge assignments asynchronously using filters.
 *
 * Allows bulk tag updates on reservations matching specified criteria. The operation runs asynchronously and returns a job ID for tracking progress. An empty filter will update all reservations.
 *
 * If a tag appears in both assign and unassign lists, it will be assigned. Use this for large-scale tag operations based on reservation properties.
 * @param filter - Filter criteria to select which badge assignments to update. Use an empty filter to update all badge assignments.
 * @public
 * @requiredField filter
 * @requiredField options.assignTags
 * @permissionId BADGE_ASSIGNMENTS.BADGE_UPDATE_TAGS
 * @applicableIdentity APP
 * @fqn wix.badges.badgeassignments.v4.BadgeAssignments.BulkUpdateBadgeAssignmentTagsByFilter
 */
declare function bulkUpdateBadgeAssignmentTagsByFilter(filter: Record<string, any>, options?: NonNullablePaths<BulkUpdateBadgeAssignmentTagsByFilterOptions, `assignTags`, 0>): Promise<NonNullablePaths<BulkUpdateBadgeAssignmentTagsByFilterResponse, `jobId`, 0>>;
interface BulkUpdateBadgeAssignmentTagsByFilterOptions {
    /** Tags to assign to the filtered badge assignments. */
    assignTags: Tags;
    /** Tags to remove from the filtered badge assignments. If a tag appears in both assign and unassign lists, it will be assigned. */
    unassignTags?: Tags;
}

export { type AccountInfo, type AccountInfoMetadata, type ActionEvent, type ApplicationError, type BadgeAssignment, type BadgeAssignmentCreatedEnvelope, type BadgeAssignmentDeletedEnvelope, type BadgeAssignmentQuery, type BadgeAssignmentQuerySpec, type BadgeAssignmentsQueryBuilder, type BadgeAssignmentsQueryResult, type BadgeMemberCount, type BaseEventMetadata, type BulkActionMetadata, type BulkBadgeAssignmentResult, type BulkCreateBadgeAssignmentsOptions, type BulkCreateBadgeAssignmentsRequest, type BulkCreateBadgeAssignmentsResponse, type BulkDeleteBadgeAssignmentsRequest, type BulkDeleteBadgeAssignmentsResponse, type BulkUpdateBadgeAssignmentTagsByFilterOptions, type BulkUpdateBadgeAssignmentTagsByFilterRequest, type BulkUpdateBadgeAssignmentTagsByFilterResponse, type BulkUpdateBadgeAssignmentTagsOptions, type BulkUpdateBadgeAssignmentTagsRequest, type BulkUpdateBadgeAssignmentTagsResponse, type BulkUpdateBadgeAssignmentTagsResult, type CommonQueryWithEntityContext, type CreateBadgeAssignmentRequest, type CreateBadgeAssignmentResponse, type CursorPaging, type CursorPagingMetadata, type CursorQuery, type CursorQueryPagingMethodOneOf, type Cursors, type DeleteBadgeAssignmentRequest, type DeleteBadgeAssignmentResponse, type DomainEvent, type DomainEventBodyOneOf, type Empty, type EntityCreatedEvent, type EntityDeletedEvent, type EntityUpdatedEvent, type EventMetadata, type ExtendedFields, type GetMemberCountsPerBadgeLegacyRequest, type GetMemberCountsPerBadgeLegacyResponse, type IdentificationData, type IdentificationDataIdOneOf, type ItemMetadata, type MessageEnvelope, type Paging, type PagingMetadataV2, type QueryBadgeAssignmentsLegacyRequest, type QueryBadgeAssignmentsLegacyResponse, type QueryBadgeAssignmentsRequest, type QueryBadgeAssignmentsResponse, type QueryV2, type QueryV2PagingMethodOneOf, type RestoreInfo, SortOrder, type SortOrderWithLiterals, type Sorting, type TagList, type Tags, type TagsModified, type UpdateExtendedFieldsRequest, type UpdateExtendedFieldsResponse, WebhookIdentityType, type WebhookIdentityTypeWithLiterals, bulkCreateBadgeAssignments, bulkDeleteBadgeAssignments, bulkUpdateBadgeAssignmentTags, bulkUpdateBadgeAssignmentTagsByFilter, createBadgeAssignment, deleteBadgeAssignment, onBadgeAssignmentCreated, onBadgeAssignmentDeleted, queryBadgeAssignments, typedQueryBadgeAssignments, utils };
