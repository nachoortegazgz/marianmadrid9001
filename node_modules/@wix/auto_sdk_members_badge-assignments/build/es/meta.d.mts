import { CreateBadgeAssignmentRequest as CreateBadgeAssignmentRequest$1, CreateBadgeAssignmentResponse as CreateBadgeAssignmentResponse$1, BulkCreateBadgeAssignmentsRequest as BulkCreateBadgeAssignmentsRequest$1, BulkCreateBadgeAssignmentsResponse as BulkCreateBadgeAssignmentsResponse$1, QueryBadgeAssignmentsRequest as QueryBadgeAssignmentsRequest$1, QueryBadgeAssignmentsResponse as QueryBadgeAssignmentsResponse$1, DeleteBadgeAssignmentRequest as DeleteBadgeAssignmentRequest$1, DeleteBadgeAssignmentResponse as DeleteBadgeAssignmentResponse$1, BulkDeleteBadgeAssignmentsRequest as BulkDeleteBadgeAssignmentsRequest$1, BulkDeleteBadgeAssignmentsResponse as BulkDeleteBadgeAssignmentsResponse$1, BulkUpdateBadgeAssignmentTagsRequest as BulkUpdateBadgeAssignmentTagsRequest$1, BulkUpdateBadgeAssignmentTagsResponse as BulkUpdateBadgeAssignmentTagsResponse$1, BulkUpdateBadgeAssignmentTagsByFilterRequest as BulkUpdateBadgeAssignmentTagsByFilterRequest$1, BulkUpdateBadgeAssignmentTagsByFilterResponse as BulkUpdateBadgeAssignmentTagsByFilterResponse$1 } from './index.typings.mjs';
import '@wix/sdk-types';

/**
 * A badge assignment represents the relationship between a badge and a member.
 * When a badge assignment is created, the specified member receives the badge
 * and any associated permissions or privileges.
 */
interface BadgeAssignment {
    /**
     * Badge assignment ID.
     * @format GUID
     * @readonly
     */
    id?: string | null;
    /**
     * ID of the badge being assigned to the member. Call the [Badges API](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/badges-v4/introduction) to retrieve available badges.
     * @format GUID
     */
    badgeId?: string | null;
    /**
     * ID of the member receiving the badge assignment. Call the [Members API](https://dev.wix.com/docs/api-reference/crm/members-contacts/members/members/introduction) to retrieve member information.
     * @format GUID
     */
    memberId?: string | null;
    /**
     * Date and time the badge assignment was created.
     * @readonly
     */
    createdDate?: Date | null;
    /**
     * Date and time the badge assignment was updated.
     * @readonly
     */
    updatedDate?: Date | null;
    /**
     * Tags allow you to classify ticket reservations.
     *
     * Learn more about [tags](https://dev.wix.com/docs/rest/business-management/tags/introduction).
     */
    tags?: Tags;
    /**
     * Extended fields allow you to add custom properties beyond the standard reservation data.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/build-apps/develop-your-app/extensions/backend-extensions/schema-plugins/about-schema-plugin-extensions).
     */
    extendedFields?: ExtendedFields;
}
/**
 * Common object for tags.
 * Should be use as in this example:
 * message Foo {
 * option (.wix.api.decomposite_of) = "wix.common.tags.Foo";
 * string id = 1;
 * ...
 * Tags tags = 5
 * }
 *
 * example of taggable entity
 * {
 * id: "123"
 * tags: {
 * tags: {
 * tag_ids:["11","22"]
 * },
 * private_tags: {
 * tag_ids: ["33", "44"]
 * }
 * }
 * }
 */
interface Tags {
    /** Tags that require an additional permission in order to access them, normally not given to site members or visitors. */
    privateTags?: TagList;
    /** Tags that are exposed to anyone who has access to the labeled entity itself, including site members and visitors. */
    tags?: TagList;
}
interface TagList {
    /**
     * List of tag IDs
     * @maxSize 100
     * @maxLength 5
     */
    tagIds?: string[];
}
interface ExtendedFields {
    /**
     * Extended field data. Each key corresponds to the namespace of the app that created the extended fields.
     * The value of each key is structured according to the schema defined when the extended fields were configured.
     *
     * You can only access fields for which you have the appropriate permissions.
     *
     * Learn more about [extended fields](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields).
     */
    namespaces?: Record<string, Record<string, any>>;
}
interface TagsModified {
    /** Updated function. */
    badgeAssignment?: BadgeAssignment;
    /** Tags that were assigned to the Function. */
    assignedTags?: Tags;
    /** Tags that were unassigned from the Function. */
    unassignedTags?: Tags;
}
interface CreateBadgeAssignmentRequest {
    /** Badge assignment to create. */
    badgeAssignment: BadgeAssignment;
}
interface CreateBadgeAssignmentResponse {
    /** Created badge assignment. */
    badgeAssignment?: BadgeAssignment;
}
interface BulkCreateBadgeAssignmentsRequest {
    /**
     * List of badge assignments to create.
     * @minSize 1
     * @maxSize 100
     */
    badgeAssignments: BadgeAssignment[];
    /** Whether to return the created badge assignments in the response. Set to `true` to receive full badge assignment objects. */
    returnEntity?: boolean;
}
interface BulkCreateBadgeAssignmentsResponse {
    /**
     * Results of the bulk create operation.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkBadgeAssignmentResult[];
    /** Metadata about the bulk operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkBadgeAssignmentResult {
    /** Metadata regarding the specific single update operation */
    itemMetadata?: ItemMetadata;
    /** Only exists if `returnEntity` was set to true in the request */
    item?: BadgeAssignment;
}
interface ItemMetadata {
    /**
     * Item ID. Should always be available, unless it's impossible (for example, when failing to create an item).
     * @format GUID
     */
    id?: string | null;
    /** Index of the item within the request array. Allows for correlation between request and response items. */
    originalIndex?: number;
    /** Whether the requested action was successful for this item. When `false`, the `error` field is populated. */
    success?: boolean;
    /** Details about the error in case of failure. */
    error?: ApplicationError;
}
interface ApplicationError {
    /** Error code. */
    code?: string;
    /** Description of the error. */
    description?: string;
    /** Data related to the error. */
    data?: Record<string, any> | null;
}
interface BulkActionMetadata {
    /** Number of items that were successfully processed. */
    totalSuccesses?: number;
    /** Number of items that couldn't be processed. */
    totalFailures?: number;
    /** Number of failures without details because detailed failure threshold was exceeded. */
    undetailedFailures?: number;
}
interface QueryBadgeAssignmentsRequest {
    /** Query options. See [API Query Language](https://dev.wix.com/docs/rest/articles/get-started/api-query-language) for more details. */
    query?: CursorQuery;
}
interface CursorQuery extends CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object.
     *
     * Learn more about [filtering](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#filters).
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object.
     *
     * Learn more about [sorting](https://dev.wix.com/docs/rest/articles/getting-started/api-query-language#sorting).
     * @maxSize 5
     */
    sort?: Sorting[];
}
/** @oneof */
interface CursorQueryPagingMethodOneOf {
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Sorting {
    /**
     * Name of the field to sort by.
     * @maxLength 512
     */
    fieldName?: string;
    /** Sort order. */
    order?: SortOrderWithLiterals;
}
declare enum SortOrder {
    ASC = "ASC",
    DESC = "DESC"
}
/** @enumType */
type SortOrderWithLiterals = SortOrder | 'ASC' | 'DESC';
interface CursorPaging {
    /**
     * Maximum number of items to return in the results.
     * @max 100
     */
    limit?: number | null;
    /**
     * Pointer to the next or previous page in the list of results.
     *
     * Pass the relevant cursor token from the `pagingMetadata` object in the previous call's response.
     * Not relevant for the first request.
     * @maxLength 16000
     */
    cursor?: string | null;
}
interface QueryBadgeAssignmentsResponse {
    /** List of badge assignments matching the query criteria. */
    badgeAssignments?: BadgeAssignment[];
    /** Paging metadata including cursors for pagination. */
    pagingMetadata?: CursorPagingMetadata;
}
interface CursorPagingMetadata {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Cursor strings that point to the next page, previous page, or both. */
    cursors?: Cursors;
    /**
     * Whether there are more pages to retrieve following the current page.
     *
     * + `true`: Another page of results can be retrieved.
     * + `false`: This is the last page.
     */
    hasNext?: boolean | null;
}
interface Cursors {
    /**
     * Cursor string pointing to the next page in the list of results.
     * @maxLength 16000
     */
    next?: string | null;
    /**
     * Cursor pointing to the previous page in the list of results.
     * @maxLength 16000
     */
    prev?: string | null;
}
interface QueryBadgeAssignmentsLegacyRequest {
    queryV2?: QueryV2;
}
interface QueryV2 extends QueryV2PagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
    /**
     * Filter object in the following format:
     * `"filter" : {
     * "fieldName1": "value1",
     * "fieldName2":{"$operator":"value2"}
     * }`
     * Example of operators: `$eq`, `$ne`, `$lt`, `$lte`, `$gt`, `$gte`, `$in`, `$hasSome`, `$hasAll`, `$startsWith`, `$contains`
     */
    filter?: Record<string, any> | null;
    /**
     * Sort object in the following format:
     * `[{"fieldName":"sortField1","order":"ASC"},{"fieldName":"sortField2","order":"DESC"}]`
     * @maxSize 100
     */
    sort?: Sorting[];
    /**
     * Array of projected fields. A list of specific field names to return. If `fieldsets` are also specified, the union of `fieldsets` and `fields` is returned.
     * @maxSize 100
     * @maxLength 1000
     */
    fields?: string[];
    /**
     * Array of named, predefined sets of projected fields. A array of predefined named sets of fields to be returned. Specifying multiple `fieldsets` will return the union of fields from all sets. If `fields` are also specified, the union of `fieldsets` and `fields` is returned.
     * @maxSize 100
     * @maxLength 1000
     */
    fieldsets?: string[];
}
/** @oneof */
interface QueryV2PagingMethodOneOf {
    /** Paging options to limit and skip the number of items. */
    paging?: Paging;
    /** Cursor token pointing to a page of results. Not used in the first request. Following requests use the cursor token and not `filter` or `sort`. */
    cursorPaging?: CursorPaging;
}
interface Paging {
    /**
     * Number of items to load.
     * @max 1000
     */
    limit?: number | null;
    /** Number of items to skip in the current sort order. */
    offset?: number | null;
}
interface QueryBadgeAssignmentsLegacyResponse {
    /** List of badge assignments matching the query criteria. */
    badgeAssignments?: BadgeAssignment[];
    pagingMetadataV2?: PagingMetadataV2;
}
interface PagingMetadataV2 {
    /** Number of items returned in the response. */
    count?: number | null;
    /** Offset that was requested. */
    offset?: number | null;
    /** Total number of items that match the query. Returned if offset paging is used and the `tooManyToCount` flag is not set. */
    total?: number | null;
    /** Flag that indicates the server failed to calculate the `total` field. */
    tooManyToCount?: boolean | null;
    /** Cursors to navigate through the result pages using `next` and `prev`. Returned if cursor paging is used. */
    cursors?: Cursors;
}
interface GetMemberCountsPerBadgeLegacyRequest {
}
interface GetMemberCountsPerBadgeLegacyResponse {
    /** Badges and the amount of members who have each badge. */
    badgeMemberCounts?: BadgeMemberCount[];
}
interface BadgeMemberCount {
    /**
     * Badge ID.
     * @format GUID
     */
    badgeId?: string;
    /** Number of members who have the badge. */
    memberCount?: number;
}
interface DeleteBadgeAssignmentRequest {
    /**
     * ID of the badge assignment to delete.
     * @format GUID
     */
    badgeAssignmentId: string;
}
interface DeleteBadgeAssignmentResponse {
}
interface BulkDeleteBadgeAssignmentsRequest {
    /**
     * ID of the badge assignments to delete.
     * @format GUID
     * @minSize 1
     * @maxSize 100
     */
    badgeAssignmentIds: string[];
}
interface BulkDeleteBadgeAssignmentsResponse {
    /**
     * Results of the bulk delete operation.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkBadgeAssignmentResult[];
    /** Metadata about the bulk operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateBadgeAssignmentTagsRequest {
    /**
     * List of badge assignment IDs to update tags for.
     * @minSize 1
     * @maxSize 100
     * @format GUID
     */
    ids: string[];
    /** Tags to assign to the badge assignments. */
    assignTags: Tags;
    /** Tags to remove from the badge assignments. If a tag appears in both assign and unassign lists, it will be assigned. */
    unassignTags?: Tags;
}
interface BulkUpdateBadgeAssignmentTagsResponse {
    /**
     * Results of the bulk update operation.
     * @minSize 1
     * @maxSize 100
     */
    results?: BulkUpdateBadgeAssignmentTagsResult[];
    /** Metadata about the bulk operation. */
    bulkActionMetadata?: BulkActionMetadata;
}
interface BulkUpdateBadgeAssignmentTagsResult {
    /** Metadata about the specific single update operation. */
    itemMetadata?: ItemMetadata;
}
interface BulkUpdateBadgeAssignmentTagsByFilterRequest {
    /** Filter criteria to select which badge assignments to update. Use an empty filter to update all badge assignments. */
    filter: Record<string, any> | null;
    /** Tags to assign to the filtered badge assignments. */
    assignTags: Tags;
    /** Tags to remove from the filtered badge assignments. If a tag appears in both assign and unassign lists, it will be assigned. */
    unassignTags?: Tags;
}
interface BulkUpdateBadgeAssignmentTagsByFilterResponse {
    /**
     * Job ID for tracking the asynchronous bulk update operation.
     * @format GUID
     */
    jobId?: string;
}
interface UpdateExtendedFieldsRequest {
    /** ID of the entity to update. */
    id?: string;
    /** Identifier for the app whose extended fields are being updated. */
    namespace?: string;
    /** Data to update. Structured according to the [schema](https://dev.wix.com/docs/rest/articles/getting-started/extended-fields#json-schema-for-extended-fields) defined when the extended fields were configured. */
    namespaceData?: Record<string, any> | null;
}
interface UpdateExtendedFieldsResponse {
    /** Updated BadgeAssignment. */
    badgeAssignment?: BadgeAssignment;
}
interface DomainEvent extends DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
    /** Event ID. With this ID you can easily spot duplicated events and ignore them. */
    id?: string;
    /**
     * Fully Qualified Domain Name of an entity. This is a unique identifier assigned to the API main business entities.
     * For example, `wix.stores.catalog.product`, `wix.bookings.session`, `wix.payments.transaction`.
     */
    entityFqdn?: string;
    /**
     * Event action name, placed at the top level to make it easier for users to dispatch messages.
     * For example: `created`/`updated`/`deleted`/`started`/`completed`/`email_opened`.
     */
    slug?: string;
    /** ID of the entity associated with the event. */
    entityId?: string;
    /** Event timestamp in [ISO-8601](https://en.wikipedia.org/wiki/ISO_8601) format and UTC time. For example, `2020-04-26T13:57:50.699Z`. */
    eventTime?: Date | null;
    /**
     * Whether the event was triggered as a result of a privacy regulation application
     * (for example, GDPR).
     */
    triggeredByAnonymizeRequest?: boolean | null;
    /** If present, indicates the action that triggered the event. */
    originatedFrom?: string | null;
    /**
     * A sequence number that indicates the order of updates to an entity. For example, if an entity was updated at `16:00` and then again at `16:01`, the second update will always have a higher sequence number.
     * You can use this number to make sure you're handling updates in the right order. Just save the latest sequence number on your end and compare it to the one in each new message. If the new message has an older (lower) number, you can safely ignore it.
     */
    entityEventSequence?: string | null;
}
/** @oneof */
interface DomainEventBodyOneOf {
    createdEvent?: EntityCreatedEvent;
    updatedEvent?: EntityUpdatedEvent;
    deletedEvent?: EntityDeletedEvent;
    actionEvent?: ActionEvent;
}
interface EntityCreatedEvent {
    entityAsJson?: string;
    /** Indicates the event was triggered by a restore-from-trashbin operation for a previously deleted entity */
    restoreInfo?: RestoreInfo;
}
interface RestoreInfo {
    deletedDate?: Date | null;
}
interface EntityUpdatedEvent {
    /**
     * Since platformized APIs only expose PATCH and not PUT we can't assume that the fields sent from the client are the actual diff.
     * This means that to generate a list of changed fields (as opposed to sent fields) one needs to traverse both objects.
     * We don't want to impose this on all developers and so we leave this traversal to the notification recipients which need it.
     */
    currentEntityAsJson?: string;
}
interface EntityDeletedEvent {
    /** Entity that was deleted. */
    deletedEntityAsJson?: string | null;
}
interface ActionEvent {
    bodyAsJson?: string;
}
interface Empty {
}
interface MessageEnvelope {
    /**
     * App instance ID.
     * @format GUID
     */
    instanceId?: string | null;
    /**
     * Event type.
     * @maxLength 150
     */
    eventType?: string;
    /** The identification type and identity data. */
    identity?: IdentificationData;
    /** Stringify payload. */
    data?: string;
    /** Details related to the account */
    accountInfo?: AccountInfo;
}
interface IdentificationData extends IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
    /** @readonly */
    identityType?: WebhookIdentityTypeWithLiterals;
}
/** @oneof */
interface IdentificationDataIdOneOf {
    /**
     * ID of a site visitor that has not logged in to the site.
     * @format GUID
     */
    anonymousVisitorId?: string;
    /**
     * ID of a site visitor that has logged in to the site.
     * @format GUID
     */
    memberId?: string;
    /**
     * ID of a Wix user (site owner, contributor, etc.).
     * @format GUID
     */
    wixUserId?: string;
    /**
     * ID of an app.
     * @format GUID
     */
    appId?: string;
}
declare enum WebhookIdentityType {
    UNKNOWN = "UNKNOWN",
    ANONYMOUS_VISITOR = "ANONYMOUS_VISITOR",
    MEMBER = "MEMBER",
    WIX_USER = "WIX_USER",
    APP = "APP"
}
/** @enumType */
type WebhookIdentityTypeWithLiterals = WebhookIdentityType | 'UNKNOWN' | 'ANONYMOUS_VISITOR' | 'MEMBER' | 'WIX_USER' | 'APP';
interface AccountInfo {
    /**
     * ID of the Wix account associated with the event.
     * @format GUID
     */
    accountId?: string | null;
    /**
     * ID of the parent Wix account. Only included when accountId belongs to a child account.
     * @format GUID
     */
    parentAccountId?: string | null;
    /**
     * ID of the Wix site associated with the event. Only included when the event is tied to a specific site.
     * @format GUID
     */
    siteId?: string | null;
}

type __PublicMethodMetaInfo<K = string, M = unknown, T = unknown, S = unknown, Q = unknown, R = unknown> = {
    getUrl: (context: any) => string;
    httpMethod: K;
    path: string;
    pathParams: M;
    __requestType: T;
    __originalRequestType: S;
    __responseType: Q;
    __originalResponseType: R;
};
declare function createBadgeAssignment(): __PublicMethodMetaInfo<'POST', {}, CreateBadgeAssignmentRequest$1, CreateBadgeAssignmentRequest, CreateBadgeAssignmentResponse$1, CreateBadgeAssignmentResponse>;
declare function bulkCreateBadgeAssignments(): __PublicMethodMetaInfo<'POST', {}, BulkCreateBadgeAssignmentsRequest$1, BulkCreateBadgeAssignmentsRequest, BulkCreateBadgeAssignmentsResponse$1, BulkCreateBadgeAssignmentsResponse>;
declare function queryBadgeAssignments(): __PublicMethodMetaInfo<'POST', {}, QueryBadgeAssignmentsRequest$1, QueryBadgeAssignmentsRequest, QueryBadgeAssignmentsResponse$1, QueryBadgeAssignmentsResponse>;
declare function deleteBadgeAssignment(): __PublicMethodMetaInfo<'DELETE', {
    badgeAssignmentId: string;
}, DeleteBadgeAssignmentRequest$1, DeleteBadgeAssignmentRequest, DeleteBadgeAssignmentResponse$1, DeleteBadgeAssignmentResponse>;
declare function bulkDeleteBadgeAssignments(): __PublicMethodMetaInfo<'POST', {}, BulkDeleteBadgeAssignmentsRequest$1, BulkDeleteBadgeAssignmentsRequest, BulkDeleteBadgeAssignmentsResponse$1, BulkDeleteBadgeAssignmentsResponse>;
declare function bulkUpdateBadgeAssignmentTags(): __PublicMethodMetaInfo<'POST', {}, BulkUpdateBadgeAssignmentTagsRequest$1, BulkUpdateBadgeAssignmentTagsRequest, BulkUpdateBadgeAssignmentTagsResponse$1, BulkUpdateBadgeAssignmentTagsResponse>;
declare function bulkUpdateBadgeAssignmentTagsByFilter(): __PublicMethodMetaInfo<'POST', {}, BulkUpdateBadgeAssignmentTagsByFilterRequest$1, BulkUpdateBadgeAssignmentTagsByFilterRequest, BulkUpdateBadgeAssignmentTagsByFilterResponse$1, BulkUpdateBadgeAssignmentTagsByFilterResponse>;

export { type AccountInfo as AccountInfoOriginal, type ActionEvent as ActionEventOriginal, type ApplicationError as ApplicationErrorOriginal, type BadgeAssignment as BadgeAssignmentOriginal, type BadgeMemberCount as BadgeMemberCountOriginal, type BulkActionMetadata as BulkActionMetadataOriginal, type BulkBadgeAssignmentResult as BulkBadgeAssignmentResultOriginal, type BulkCreateBadgeAssignmentsRequest as BulkCreateBadgeAssignmentsRequestOriginal, type BulkCreateBadgeAssignmentsResponse as BulkCreateBadgeAssignmentsResponseOriginal, type BulkDeleteBadgeAssignmentsRequest as BulkDeleteBadgeAssignmentsRequestOriginal, type BulkDeleteBadgeAssignmentsResponse as BulkDeleteBadgeAssignmentsResponseOriginal, type BulkUpdateBadgeAssignmentTagsByFilterRequest as BulkUpdateBadgeAssignmentTagsByFilterRequestOriginal, type BulkUpdateBadgeAssignmentTagsByFilterResponse as BulkUpdateBadgeAssignmentTagsByFilterResponseOriginal, type BulkUpdateBadgeAssignmentTagsRequest as BulkUpdateBadgeAssignmentTagsRequestOriginal, type BulkUpdateBadgeAssignmentTagsResponse as BulkUpdateBadgeAssignmentTagsResponseOriginal, type BulkUpdateBadgeAssignmentTagsResult as BulkUpdateBadgeAssignmentTagsResultOriginal, type CreateBadgeAssignmentRequest as CreateBadgeAssignmentRequestOriginal, type CreateBadgeAssignmentResponse as CreateBadgeAssignmentResponseOriginal, type CursorPagingMetadata as CursorPagingMetadataOriginal, type CursorPaging as CursorPagingOriginal, type CursorQuery as CursorQueryOriginal, type CursorQueryPagingMethodOneOf as CursorQueryPagingMethodOneOfOriginal, type Cursors as CursorsOriginal, type DeleteBadgeAssignmentRequest as DeleteBadgeAssignmentRequestOriginal, type DeleteBadgeAssignmentResponse as DeleteBadgeAssignmentResponseOriginal, type DomainEventBodyOneOf as DomainEventBodyOneOfOriginal, type DomainEvent as DomainEventOriginal, type Empty as EmptyOriginal, type EntityCreatedEvent as EntityCreatedEventOriginal, type EntityDeletedEvent as EntityDeletedEventOriginal, type EntityUpdatedEvent as EntityUpdatedEventOriginal, type ExtendedFields as ExtendedFieldsOriginal, type GetMemberCountsPerBadgeLegacyRequest as GetMemberCountsPerBadgeLegacyRequestOriginal, type GetMemberCountsPerBadgeLegacyResponse as GetMemberCountsPerBadgeLegacyResponseOriginal, type IdentificationDataIdOneOf as IdentificationDataIdOneOfOriginal, type IdentificationData as IdentificationDataOriginal, type ItemMetadata as ItemMetadataOriginal, type MessageEnvelope as MessageEnvelopeOriginal, type PagingMetadataV2 as PagingMetadataV2Original, type Paging as PagingOriginal, type QueryBadgeAssignmentsLegacyRequest as QueryBadgeAssignmentsLegacyRequestOriginal, type QueryBadgeAssignmentsLegacyResponse as QueryBadgeAssignmentsLegacyResponseOriginal, type QueryBadgeAssignmentsRequest as QueryBadgeAssignmentsRequestOriginal, type QueryBadgeAssignmentsResponse as QueryBadgeAssignmentsResponseOriginal, type QueryV2 as QueryV2Original, type QueryV2PagingMethodOneOf as QueryV2PagingMethodOneOfOriginal, type RestoreInfo as RestoreInfoOriginal, SortOrder as SortOrderOriginal, type SortOrderWithLiterals as SortOrderWithLiteralsOriginal, type Sorting as SortingOriginal, type TagList as TagListOriginal, type TagsModified as TagsModifiedOriginal, type Tags as TagsOriginal, type UpdateExtendedFieldsRequest as UpdateExtendedFieldsRequestOriginal, type UpdateExtendedFieldsResponse as UpdateExtendedFieldsResponseOriginal, WebhookIdentityType as WebhookIdentityTypeOriginal, type WebhookIdentityTypeWithLiterals as WebhookIdentityTypeWithLiteralsOriginal, type __PublicMethodMetaInfo, bulkCreateBadgeAssignments, bulkDeleteBadgeAssignments, bulkUpdateBadgeAssignmentTags, bulkUpdateBadgeAssignmentTagsByFilter, createBadgeAssignment, deleteBadgeAssignment, queryBadgeAssignments };
