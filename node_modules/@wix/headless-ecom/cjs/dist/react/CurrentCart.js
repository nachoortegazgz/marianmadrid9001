import { jsx as _jsx } from "react/jsx-runtime";
import { Root as CoreRoot } from './core/CurrentCart.js';
import { Root as CartRoot } from './Cart.js';
var TestIds;
(function (TestIds) {
    TestIds["currentCartRoot"] = "current-cart-root";
})(TestIds || (TestIds = {}));
export const Root = ({ children, currentCartServiceConfig }) => {
    return (
    // we don't supply data-component-tag here because it's already provided by the CartRoot component
    _jsx(CoreRoot, { currentCartServiceConfig: currentCartServiceConfig, "data-testid": TestIds.currentCartRoot, children: _jsx(CartRoot, { children: children }) }));
};
Root.displayName = 'CurrentCart.Root';
