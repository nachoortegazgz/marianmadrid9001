import * as _wix_services_definitions from '@wix/services-definitions';
export { useSignals } from '@preact/signals-react/runtime';
import { ServicesManager, ServiceDefinition, ServiceAPI, ServicesRegistrar } from '@wix/services-manager/types';
import React, { PropsWithChildren } from 'react';

declare function ServicesManagerProvider({ servicesManager, children, }: PropsWithChildren<{
    servicesManager: ServicesManager;
}>): React.JSX.Element;
declare function useGetService(): _wix_services_definitions.GetService;
declare function useService<T extends ServiceDefinition<any, any>>(service: T): ServiceAPI<T>;
declare function useServices<T extends ServiceDefinition<any, any>[]>(services: T): {
    [K in keyof T]: ServiceAPI<T[K]>;
};
declare function WixServices(props: {
    servicesMap: ServicesRegistrar;
    children: React.ReactNode;
    disposeOnUnmount?: boolean;
}): React.JSX.Element;

export { ServicesManagerProvider, WixServices, useGetService, useService, useServices };
